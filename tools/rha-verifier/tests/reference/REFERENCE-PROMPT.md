You are an INDEPENDENT REFERENCE AUTHOR (plan docs/plan/IMPLEMENTATION-PLAN.md §2.4 rule 4) in this repository's P-B worktree. Read docs/architecture/verifier-contract.md and spec docs/spec/rha-spec-v0.10.md §11.7.6. Do NOT open, read, list or grep anything under `tools/rha-verifier/tests/corpus/` (the graded corpus) or `tools/rha-verifier/src/` (the implementation under test). Do not commit, push, run cargo, or use jj.

Write exactly one file, `tools/rha-verifier/tests/reference/mod.rs`, containing a self-contained reference implementation of contract §1 (verifier fixtures), written independently and as plainly as possible:

    pub fn evaluate(fixture: &serde_json::Value) -> serde_json::Value

It takes a fixture object in the contract's format (ignore any `expected` member) and returns exactly the contract's `expected` object: `r_eff` (sorted by id, or null on conflict), `conflict`, `authentic`, `applicable`, `complete`, `passed`, `eligible`, `valid_exception` (null when there is no exception), and `merge_allowed`. Implement §1.1 obligations (glob scope matching with `**` and `*`, total classification with default_obligations, local policies that only add, the strictness table, the join and its conflicts), §1.2 predicates, and §1.3 exceptions and MergeAllowed exactly as written, including the (choice) rules. On a conflict, every predicate after it is false (valid_exception false if an exception exists, else null), and merge_allowed is false. Use only serde_json and std. Start the file with `#![allow(dead_code, clippy::unwrap_used, clippy::expect_used)]` and a comment stating that it was written without reading the corpus or the implementation. When done, print the file path and a short summary of how each part was implemented. Do not print the file.
