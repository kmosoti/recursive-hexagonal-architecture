[package]
name = "graph"
version = "0.1.0"
description = "rhawiki core: wikilink resolution, backlinks, and link witnesses"
edition.workspace = true
rust-version.workspace = true
publish = false

[package.metadata.rha]
role = "core"

[dependencies]
document = { path = "../document" }
library = { path = "../library" }

[dev-dependencies]
proptest = "1.11.0"
serde_json = "1.0.151"

[lints]
workspace = true
