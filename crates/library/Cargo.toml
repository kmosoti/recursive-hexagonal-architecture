[package]
name = "library"
version = "0.1.0"
description = "rhawiki core: page identity, sources, and complete-or-fail loading"
edition.workspace = true
rust-version.workspace = true
publish = false

[package.metadata.rha]
role = "core"

[dependencies]
sha2 = "0.11.0"
unicode-normalization = "0.1.25"

[dev-dependencies]
proptest = "1.11.0"

[lints]
workspace = true
