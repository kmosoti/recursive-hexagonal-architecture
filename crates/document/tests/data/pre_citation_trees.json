{
  "brackets": {
    "body": "[Paragraph([Text(\"a \"), Text(\"[\"), Text(\"b\"), Text(\"]\"), Text(\" c \"), Text(\"[\"), Text(\"d e\")])]",
    "headings": "[]",
    "markdown": "a [b] c [d e\n"
  },
  "docs/adr/ADR-0001-bootstrap-decisions.md": {
    "body": "[Heading { level: 1, slug: \"adr-0001-repository-bootstrap-decisions\", children: [Text(\"ADR-0001: Repository bootstrap decisions\")] }, List { start: None, items: [[Strong([Text(\"Status:\")]), Text(\" proposed in CHG-000; accepted when Kennedy accepts CHG-000.\")], [Strong([Text(\"Date:\")]), Text(\" 2026-09-19\")], [Strong([Text(\"Decided by:\")]), Text(\" \"), Code(\"human:kennedy\"), Text(\" (DP-0.1 to DP-0.6); the Executor's choices are marked as such below.\")]] }, Heading { level: 2, slug: \"context\", children: [Text(\"Context\")] }, Paragraph([Text(\"The v0.10 specification describes a Rust workspace, baseline tooling, machine policy, and contribution records, but it installs none of them (spec §1.4, §11.7.9). CHG-000 creates the repository in which the other work items run and are recorded.\")]), Heading { level: 2, slug: \"decisions\", children: [Text(\"Decisions\")] }, Table { align: [None, None, None], head: [[Text(\"#\")], [Text(\"Decision\")], [Text(\"Source\")]], rows: [[[Text(\"1\")], [Text(\"Git identity \"), Code(\"Kennedy Mosoti <47609243+kmosoti@users.noreply.github.com>\"), Text(\", repo-local. The first choice, the personal address, was rejected by GitHub's email-privacy push protection (GH007).\")], [Text(\"DP-0.1\")]], [[Text(\"2\")], [Text(\"Public repository \"), Code(\"kmosoti/recursive-hexagonal-architecture\"), Text(\"; product name \"), Code(\"rhawiki\"), Text(\".\")], [Text(\"DP-0.2\")]], [[Text(\"3\")], [Text(\"The spec lives at \"), Code(\"docs/spec/rha-spec-v0.10.md\"), Text(\", byte-for-byte as committed in the base revision \"), Code(\"8164186\"), Text(\".\")], [Text(\"DP-0.3\")]], [[Text(\"4\")], [Code(\".rha/\"), Text(\" records are TOML, not the spec's YAML examples: \"), Code(\"serde_yaml\"), Text(\" is unmaintained and would fail \"), Code(\"cargo deny\"), Text(\", and §11.7.11 permits one equivalent record store.\")], [Text(\"DP-0.4\")]], [[Text(\"5\")], [Text(\"Policy parameters: 24 h cooling-off, 3 repair attempts, proptest 256 cases in L0 and 4096 in L1, every L0 check non-waivable, protected surfaces as listed in \"), Code(\".rha/policy.toml\"), Text(\".\")], [Text(\"DP-0.5\")]], [[Text(\"6\")], [Text(\"Branch protection on \"), Code(\"main\"), Text(\": pull request required (0 approvals, because GitHub does not let an author approve their own pull request), required status \"), Code(\"ci\"), Text(\" with the branch up to date, applies to admins, no force-push or deletion.\")], [Text(\"DP-0.6\")]], [[Text(\"7\")], [Text(\"Toolchain pinned to Rust 1.98.1, as §6.4 prints it. 1.98.1 installed, so the plan's fallback to 1.98.0 was not needed.\")], [Text(\"spec §6.4\")]], [[Text(\"8\")], [Text(\"Workspace members are \"), Code(\"crates/*\"), Text(\" and \"), Code(\"xtask\"), Text(\", exactly as §6.4 prints them. The plan's extra \"), Code(\"tools/*\"), Text(\" glob is left out until the first \"), Code(\"tools/\"), Text(\" crate exists (CHG-020): Cargo treats a glob that matches nothing as a literal path and fails. For the same reason, \"), Code(\"crates/.gitkeep\"), Text(\" must stay until the first crate exists.\")], [Text(\"Executor, observed\")]], [[Text(\"9\")], [Text(\"The exit status of \"), Code(\"cargo xtask ci\"), Text(\" reports failures (and, under \"), Code(\"--label ci\"), Text(\", missing tools), not eligibility. Otherwise a required check that is \"), Code(\"not_run\"), Text(\" by design, \"), Code(\"L0.architecture\"), Text(\" until CHG-003, would make every merge impossible under branch protection. Eligibility is reported only in the record.\")], [Text(\"Executor; needs Kennedy's review\")]], [[Text(\"10\")], [Text(\"Generated docs carry a digest of their sources in the header, not a git revision. A revision in the header would change with every commit and make \"), Code(\"cargo xtask docs --check\"), Text(\" permanently stale.\")], [Text(\"Executor\")]]] }, Heading { level: 2, slug: \"consequences\", children: [Text(\"Consequences\")] }, List { start: None, items: [[Text(\"Every evidence record is advisory (class \"), Code(\"local\"), Text(\" or \"), Code(\"ci\"), Text(\") until CHG-020; see \"), Link { href: \"../threat-model.md\", children: [Text(\"the threat model\")] }, Text(\".\")], [Text(\"CHG-000 is not governed by any approved policy, because its base revision has none. Its acceptance is a controlled transition decided by Kennedy (§11.5).\")], [Code(\"L0.architecture\"), Text(\" is required and non-waivable, but it cannot pass before CHG-003. CHG-001 and CHG-002 therefore cannot be \"), Code(\"Eligible\"), Text(\" under this policy either; see the acceptance concerns in \"), Link { href: \"../changes/CHG-000-bootstrap.md\", children: [Text(\"CHG-000\")] }, Text(\".\")]] }]",
    "headings": "[Heading { level: 1, text: \"ADR-0001: Repository bootstrap decisions\", slug: \"adr-0001-repository-bootstrap-decisions\", base_slug: \"adr-0001-repository-bootstrap-decisions\", line: 1 }, Heading { level: 2, text: \"Context\", slug: \"context\", base_slug: \"context\", line: 7 }, Heading { level: 2, text: \"Decisions\", slug: \"decisions\", base_slug: \"decisions\", line: 11 }, Heading { level: 2, text: \"Consequences\", slug: \"consequences\", base_slug: \"consequences\", line: 26 }]",
    "markdown": "# ADR-0001: Repository bootstrap decisions\n\n- **Status:** proposed in CHG-000; accepted when Kennedy accepts CHG-000.\n- **Date:** 2026-09-19\n- **Decided by:** `human:kennedy` (DP-0.1 to DP-0.6); the Executor's choices are marked as such below.\n\n## Context\n\nThe v0.10 specification describes a Rust workspace, baseline tooling, machine policy, and contribution records, but it installs none of them (spec §1.4, §11.7.9). CHG-000 creates the repository in which the other work items run and are recorded.\n\n## Decisions\n\n| # | Decision | Source |\n| --- | --- | --- |\n| 1 | Git identity `Kennedy Mosoti <47609243+kmosoti@users.noreply.github.com>`, repo-local. The first choice, the personal address, was rejected by GitHub's email-privacy push protection (GH007). | DP-0.1 |\n| 2 | Public repository `kmosoti/recursive-hexagonal-architecture`; product name `rhawiki`. | DP-0.2 |\n| 3 | The spec lives at `docs/spec/rha-spec-v0.10.md`, byte-for-byte as committed in the base revision `8164186`. | DP-0.3 |\n| 4 | `.rha/` records are TOML, not the spec's YAML examples: `serde_yaml` is unmaintained and would fail `cargo deny`, and §11.7.11 permits one equivalent record store. | DP-0.4 |\n| 5 | Policy parameters: 24 h cooling-off, 3 repair attempts, proptest 256 cases in L0 and 4096 in L1, every L0 check non-waivable, protected surfaces as listed in `.rha/policy.toml`. | DP-0.5 |\n| 6 | Branch protection on `main`: pull request required (0 approvals, because GitHub does not let an author approve their own pull request), required status `ci` with the branch up to date, applies to admins, no force-push or deletion. | DP-0.6 |\n| 7 | Toolchain pinned to Rust 1.98.1, as §6.4 prints it. 1.98.1 installed, so the plan's fallback to 1.98.0 was not needed. | spec §6.4 |\n| 8 | Workspace members are `crates/*` and `xtask`, exactly as §6.4 prints them. The plan's extra `tools/*` glob is left out until the first `tools/` crate exists (CHG-020): Cargo treats a glob that matches nothing as a literal path and fails. For the same reason, `crates/.gitkeep` must stay until the first crate exists. | Executor, observed |\n| 9 | The exit status of `cargo xtask ci` reports failures (and, under `--label ci`, missing tools), not eligibility. Otherwise a required check that is `not_run` by design, `L0.architecture` until CHG-003, would make every merge impossible under branch protection. Eligibility is reported only in the record. | Executor; needs Kennedy's review |\n| 10 | Generated docs carry a digest of their sources in the header, not a git revision. A revision in the header would change with every commit and make `cargo xtask docs --check` permanently stale. | Executor |\n\n## Consequences\n\n- Every evidence record is advisory (class `local` or `ci`) until CHG-020; see [the threat model](../threat-model.md).\n- CHG-000 is not governed by any approved policy, because its base revision has none. Its acceptance is a controlled transition decided by Kennedy (§11.5).\n- `L0.architecture` is required and non-waivable, but it cannot pass before CHG-003. CHG-001 and CHG-002 therefore cannot be `Eligible` under this policy either; see the acceptance concerns in [CHG-000](../changes/CHG-000-bootstrap.md).\n"
  },
  "docs/adr/ADR-0002-clippy-config-discovery.md": {
    "body": "[Heading { level: 1, slug: \"adr-0002-clippy-configuration-discovery-and-the-core-crate-template\", children: [Text(\"ADR-0002: Clippy configuration discovery and the core-crate template\")] }, List { start: None, items: [[Strong([Text(\"Status:\")]), Text(\" proposed in CHG-001, revised in CHG-001.1 and CHG-001.2; accepted when Kennedy accepts CHG-001.\")], [Strong([Text(\"Date:\")]), Text(\" 2026-09-19\")], [Strong([Text(\"Environment:\")]), Text(\" \"), Code(\"clippy 0.1.98 (48a229ceae 2026-09-01)\"), Text(\", rustc 1.98.1, Linux (WSL2). Every experiment runs with \"), Code(\"CLIPPY_CONF_DIR\"), Text(\", \"), Code(\"RUSTFLAGS\"), Text(\", \"), Code(\"CARGO_ENCODED_RUSTFLAGS\"), Text(\", and \"), Code(\"CARGO_BUILD_RUSTFLAGS\"), Text(\" unset, unless it sets one on purpose. The same runs happen on GitHub \"), Code(\"ubuntu24\"), Text(\" in \"), Code(\"L0.nextest\"), Text(\".\")]] }, Heading { level: 2, slug: \"context\", children: [Text(\"Context\")] }, Paragraph([Text(\"Spec §6.8 gives core crates a \"), Code(\"clippy.toml\"), Text(\" deny list of ambient-effect paths and says: \\\"Verify how the pinned Clippy locates and merges configuration files.\\\" The Clippy book says the search starts at \"), Code(\"CLIPPY_CONF_DIR\"), Text(\", else \"), Code(\"CARGO_MANIFEST_DIR\"), Text(\", else the current directory, and walks up until one file is found; it does not document merging. The plan expected \\\"nearest file wins, no merge\\\" and named a fallback, \"), Code(\"CLIPPY_CONF_DIR\"), Text(\" set per crate by \"), Code(\"xtask ci\"), Text(\", in case the observation disagreed.\")]), Heading { level: 2, slug: \"observations\", children: [Text(\"Observations\")] }, Paragraph([Text(\"Each experiment runs in a fixture workspace that \"), Link { href: \"../../xtask/tests/clippy_corpus.rs\", children: [Code(\"xtask/tests/clippy_corpus.rs\")] }, Text(\" generates under \"), Code(\"target/clippy-corpus/\"), Text(\" from the three files that own the facts involved: the root \"), Code(\"Cargo.toml\"), Text(\" (\"), Code(\"[workspace.package]\"), Text(\" and the lint tables), the root \"), Code(\"clippy.toml\"), Text(\", and \"), Link { href: \"../../xtask/templates/core-clippy.toml\", children: [Text(\"the template\")] }, Text(\". Only the crate sources are committed, in \"), Link { href: \"../../xtask/tests/corpus/clippy/\", children: [Code(\"xtask/tests/corpus/clippy/\")] }, Text(\". The fixture is emptied first, so no cached build stands in for a fresh one. The same file runs the experiments in every \"), Code(\"L0.nextest\"), Text(\" run and, with \"), Code(\"RHA_CLIPPY_RECORD=<dir>\"), Text(\", writes one record per experiment: the generated files with their digests, the literal command, the exit status, and the captured output. The records of these experiments are in \"), Link { href: \"../../evidence/w1-clippy/20260919T221927Z-edd4bac756f9/\", children: [Code(\"evidence/w1-clippy/20260919T221927Z-edd4bac756f9/\")] }, Text(\". Seven loose files beside that directory are the first run, at \"), Code(\"28ae543\"), Text(\", written by the shell script CHG-001.1 replaced; they name the committed fixtures of the time, which no longer exist.\")]), Paragraph([Strong([Text(\"Discovery and merging.\")]), Text(\" Crates marked \"), Emphasis([Text(\"template\")]), Text(\" hold a byte-equal copy of the template; \"), Emphasis([Text(\"reduced\")]), Text(\" holds the template without the settings the root file sets.\")]), Table { align: [None, None, None], head: [[Text(\"#\")], [Text(\"Crate and command\")], [Text(\"Observed\")]], rows: [[[Text(\"1\")], [Text(\"core, template, calls \"), Code(\"std::time::SystemTime::now()\"), Text(\": \"), Code(\"cargo clippy -p core-seeded\")], [Text(\"Exit 101. \"), Code(\"error: use of a disallowed method `std::time::SystemTime::now` \"), Text(\" and \"), Code(\"= note: `-D clippy::disallowed-methods` implied by `-D clippy::all`\"), Text(\".\")]], [[Text(\"2\")], [Text(\"adapter, no local file, same call: \"), Code(\"cargo clippy -p adapter-x\")], [Text(\"Exit 0, no finding.\")]], [[Text(\"3\")], [Text(\"core, reduced, a test calls \"), Code(\"unwrap()\"), Text(\": \"), Code(\"cargo clippy -p core-deny-only --all-targets\")], [Text(\"Exit 0 with \"), Code(\"warning: used `unwrap()` on a `Result` value\"), Text(\": \"), Strong([Code(\"unwrap_used\"), Text(\" fired\")]), Text(\", although the fixture root sets \"), Code(\"allow-unwrap-in-tests = true\"), Text(\".\")]], [[Text(\"3c\")], [Text(\"control for 3: the same test \"), Code(\"unwrap()\"), Text(\" in the adapter, whose nearest file is the fixture root\")], [Text(\"Exit 0, no \"), Code(\"unwrap_used\"), Text(\": the root allowance works where the root file is the nearest.\")]], [[Text(\"3b\")], [Text(\"core, template, a test calls \"), Code(\"unwrap()\"), Text(\": \"), Code(\"cargo clippy -p core-clean --all-targets\")], [Text(\"Exit 0, no \"), Code(\"unwrap_used\"), Text(\": the template's repeat of the root settings restores the allowance.\")]], [[Text(\"2-control\")], [Text(\"core, template, the same call as 2: \"), Code(\"cargo clippy -p core-seeded\")], [Text(\"Exit 101 with the disallowed-method error: the deny list does fire in this fixture, which is what makes 2 and 3c meaningful.\")]], [[Text(\"4\")], [Text(\"core, template, one use of every template entry: \"), Code(\"cargo clippy -p core-every\")], [Text(\"Exit 101; every entry reported as \"), Code(\"use of a disallowed method/type/macro\"), Text(\", 122 findings for 111 entries, because a use that names a type and calls one of its methods fires both. No unresolved-path warning.\")]], [[Text(\"5\")], [Text(\"core, \"), Code(\"clippy.toml\"), Text(\" the template and \"), Code(\".clippy.toml\"), Text(\" the root file, calls the clock\")], [Text(\"Exit 0 and no finding, with \"), Code(\"warning: using config file `….clippy.toml`, `…clippy.toml` will be ignored\"), Text(\": the dotfile \"), Strong([Text(\"silently replaces the deny list\")]), Text(\".\")]]] }, Paragraph([Strong([Text(\"Lint attributes and flags (experiment 6).\")]), Text(\" Every crate holds the template. 6a–6e use the repository's lint table; 6f–6n use one where \"), Code(\"disallowed_methods\"), Text(\", \"), Code(\"disallowed_types\"), Text(\", and \"), Code(\"disallowed_macros\"), Text(\" are \"), Code(\"forbid\"), Text(\".\")]), Table { align: [None, None, None], head: [[Text(\"#\")], [Text(\"Crate or flag\")], [Text(\"Observed\")]], rows: [[[Text(\"6-control\")], [Text(\"no attribute and no flag: \"), Code(\"cargo clippy -p core-seeded\")], [Text(\"Exit 101 with the disallowed-method error, so the silent runs below mean something.\")]], [[Text(\"6a\")], [Code(\"#[allow(clippy::disallowed_methods)]\"), Text(\" on the item\")], [Text(\"Exit 0, no finding: the deny list is off for that item.\")]], [[Text(\"6b\")], [Code(\"#[expect(clippy::disallowed_methods)]\"), Text(\" on the item\")], [Text(\"Exit 0, no finding.\")]], [[Text(\"6c\")], [Code(\"#![allow(clippy::disallowed_methods)]\"), Text(\" at the crate root\")], [Text(\"Exit 0, no finding.\")]], [[Text(\"6d\")], [Code(\"#![forbid(...)]\"), Text(\" at the crate root above an item \"), Code(\"#[allow]\")], [Text(\"Exit 101, \"), Code(\"error[E0453]: allow(clippy::disallowed_methods) incompatible with previous forbid\"), Text(\".\")]], [[Text(\"6e\")], [Text(\"no attribute, \"), Code(\"RUSTFLAGS=-Aclippy::disallowed_methods\")], [Text(\"Exit 0, no finding.\")]], [[Text(\"6f, 6g, 6h\")], [Text(\"the three attribute forms of 6a, 6b, 6c, under the forbidding lint table\")], [Text(\"Exit 101, \"), Code(\"E0453\"), Text(\" in each.\")]], [[Text(\"6i\")], [Text(\"no attribute, under forbid\")], [Text(\"Exit 101, the ordinary disallowed-method error.\")]], [[Text(\"6j\")], [Text(\"core crate with nothing on the deny list, under forbid\")], [Text(\"Exit 0: no false alarm.\")]], [[Text(\"6k\")], [Text(\"adapter making the same call, under forbid\")], [Text(\"Exit 0: adapters are unaffected, because their configuration lists no paths.\")]], [[Text(\"6l\")], [Text(\"no attribute, under forbid, \"), Code(\"RUSTFLAGS=-Aclippy::disallowed_methods\")], [Text(\"Exit 101, the error stands: \"), Code(\"forbid\"), Text(\" is not lowered by an allow flag.\")]], [[Text(\"6m\")], [Text(\"no attribute, under forbid, \"), Code(\"RUSTFLAGS=--cap-lints=warn\")], [Text(\"Exit 0, the finding appears as a warning.\")]], [[Text(\"6n\")], [Text(\"the same \"), Code(\"--cap-lints=warn\"), Text(\" in the fixture's \"), Code(\".cargo/config.toml\")], [Text(\"Exit 0, the finding appears as a warning.\")]], [[Text(\"7a\")], [Text(\"a crate whose macro expands to \"), Code(\"#[allow(clippy::style)]\"), Text(\", under the forbidding lint table\")], [Text(\"Exit 101, \"), Code(\"E0453\"), Text(\": a group allow a macro wrote is refused exactly as one written by hand.\")]], [[Text(\"7b\")], [Text(\"the same crate under the deny level\")], [Text(\"Exit 0.\")]], [[Text(\"7c\")], [Text(\"a core crate with \"), Code(\"#![forbid(...)]\"), Text(\" at its own root and nothing on the deny list\")], [Text(\"Exit 0: the per-crate route costs clean code nothing.\")]]] }, Heading { level: 2, slug: \"observed-rule\", children: [Text(\"Observed rule\")] }, Paragraph([Text(\"This record owns these observations (§11.7.1). The specification states the consequence and points here; what a finding would change in its text is queued in \"), Link { href: \"../proposals/spec-v0.11.md\", children: [Code(\"docs/proposals/spec-v0.11.md\")] }, Text(\" for the version bump.\")]), List { start: Some(1), items: [[Paragraph([Strong([Text(\"Per-crate discovery.\")]), Text(\" Clippy configures each crate from the first configuration file found in the crate's directory or its ancestors (1, 2, 3c). A core crate's local file does not apply to a sibling adapter.\")])], [Paragraph([Strong([Text(\"No merge.\")]), Text(\" The file found replaces every file further up; a crate with its own file loses the root file's settings (3; control 3c; 3b).\")])], [Paragraph([Strong([Text(\"Dotfile precedence.\")]), Text(\" In one directory, \"), Code(\".clippy.toml\"), Text(\" wins over \"), Code(\"clippy.toml\"), Text(\". The warning does not change the exit status, so a deny list can be replaced silently (5).\")])], [Paragraph([Strong([Text(\"Deny level.\")]), Text(\" Under the workspace lint table (\"), Code(\"clippy::all\"), Text(\" at deny), every template entry resolves and fails the build (1, 4).\")])], [Paragraph([Strong([Text(\"Attributes outrank the configuration file.\")]), Text(\" The file decides \"), Emphasis([Text(\"which paths\")]), Text(\" are reported; the lint level decides \"), Emphasis([Text(\"whether\")]), Text(\" a report is an error, and \"), Code(\"#[allow]\"), Text(\", \"), Code(\"#[expect]\"), Text(\", and \"), Code(\"#![allow]\"), Text(\" in the crate set that level locally (6a, 6b, 6c). A \"), Code(\"forbid\"), Text(\" above them, from the crate root or the lint table, makes each of those attributes \"), Code(\"E0453\"), Text(\" (6d, 6f, 6g, 6h) without changing anything else (6i, 6j, 6k).\")])], [Paragraph([Strong([Text(\"A level is not a ceiling.\")]), Text(\" \"), Code(\"--cap-lints=warn\"), Text(\", from \"), Code(\"RUSTFLAGS\"), Text(\" or from \"), Code(\".cargo/config.toml\"), Text(\", lowers a forbidden finding to a warning (6m, 6n). An \"), Code(\"-A\"), Text(\" flag does not (6l).\")])], [Paragraph([Strong([Text(\"A forbidding lint table refuses macro-generated group allows.\")]), Text(\" Where any macro in the crate expands to \"), Code(\"#[allow(clippy::style)]\"), Text(\", forbidding a lint of that group is \"), Code(\"E0453\"), Text(\" (7a), and at the deny level the same crate is silent (7b). This is not hypothetical: setting the three lints to \"), Code(\"forbid\"), Text(\" in this repository's own \"), Code(\"Cargo.toml\"), Text(\" stopped \"), Code(\"xtask\"), Text(\" compiling with 15 such errors, all of them from \"), Code(\"clap\"), Text(\"'s \"), Code(\"derive(Parser)\"), Text(\" and \"), Code(\"derive(Subcommand)\"), Text(\". A \"), Code(\"serde\"), Text(\" derive does not do this, so a core crate can forbid the lints at its own root (7c).\")])], [Paragraph([Strong([Text(\"A type entry fires where the type is named.\")]), Text(\" It catches a type in a signature, a type annotation, or a qualified path such as \"), Code(\"File::open\"), Text(\", but not a value a dependency hands back, and not a bare unit-struct value: \"), Code(\"let a = std::alloc::System;\"), Text(\" is silent where \"), Code(\"let a: std::alloc::System = …;\"), Text(\" fires. The effectful method of each such type is therefore listed beside it.\")])]] }, Paragraph([Text(\"Rules 1 and 2 are what the plan pre-registered as its expectation, and both hold, so the \"), Code(\"CLIPPY_CONF_DIR\"), Text(\" fallback is not used. Rule 3 answers the plan's listed Unknown, and its answer is the outcome the plan told the Executor to avoid rather than one it expected. Rules 4 to 7 were not pre-registered.\")]), Paragraph([Strong([Text(\"Not observed.\")]), Text(\" Whether the upward search stops at the workspace root or continues to the filesystem root: in every fixture a file exists at the workspace root. \"), Code(\"CLIPPY_CONF_DIR\"), Text(\" itself. Whether the attribute results carry to \"), Code(\"disallowed_types\"), Text(\" and \"), Code(\"disallowed_macros\"), Text(\": 6a to 6n name \"), Code(\"clippy::disallowed_methods\"), Text(\" only, although all three lints were forbidden in the second fixture. Other Clippy versions: the corpus reruns every experiment whenever the toolchain changes.\")]), Heading { level: 2, slug: \"decision\", children: [Text(\"Decision\")] }, List { start: None, items: [[Text(\"Every crate with role \"), Code(\"core\"), Text(\" has a \"), Code(\"clippy.toml\"), Text(\" byte-equal to \"), Link { href: \"../../xtask/templates/core-clippy.toml\", children: [Code(\"xtask/templates/core-clippy.toml\")] }, Text(\". Adapters, apps, and tools have none and use the root file. The template holds the §6.8 entries, the plan's W1 additions, the entries added in CHG-001.2, and, because of rule 2, every setting of the root \"), Code(\"clippy.toml\"), Text(\".\")], [Text(\"Rule \"), Code(\"effect.core_clippy_template\"), Text(\" reports, per core crate, a missing \"), Code(\"clippy.toml\"), Text(\", one that differs from the template, and a \"), Code(\".clippy.toml\"), Text(\" beside it (rule 3). Its witness is the path and the sha256 digests. It is implemented in \"), Link { href: \"../../xtask/src/clippy_template.rs\", children: [Code(\"xtask/src/clippy_template.rs\")] }, Text(\" and runs in L0 once \"), Code(\"cargo xtask architecture\"), Text(\" exists (CHG-003).\")], [Text(\"Fixtures are generated, not committed (CHG-001.1). A committed fixture is a copy of the root lint table, the root \"), Code(\"clippy.toml\"), Text(\", or the template, and copies need drift tests to stay true. The one copy left is the template's repeat of the root settings, which rule 2 forces; \"), Code(\"template_repeats_every_root_setting\"), Text(\" guards it.\")], [Text(\"The deny list is extended in CHG-001.2 from 15 entries to 111, and experiment 4 keeps every entry honest: a path that stops resolving stops firing, and the test fails. The entries come from the ten paths this record named in CHG-001, a sweep of the Rust 1.98.1 standard-library source, and two adversarial reviews of that sweep, all kept to the effects §6.8 names.\")]] }, Table { align: [None, None, None], head: [[Text(\"Category\")], [Text(\"Entries\")], [Text(\"Examples\")]], rows: [[[Text(\"Clocks\")], [Text(\"4\")], [Code(\"SystemTime::now\"), Text(\", \"), Code(\"Instant::elapsed\")]], [[Text(\"Environment, arguments, working directory\")], [Text(\"16\")], [Code(\"env::var_os\"), Text(\", \"), Code(\"env::args\"), Text(\", \"), Code(\"env::current_dir\"), Text(\", \"), Code(\"path::absolute\"), Text(\", \"), Code(\"IsTerminal::is_terminal\")]], [[Text(\"Storage\")], [Text(\"36\")], [Text(\"19 \"), Code(\"std::fs\"), Text(\" functions, \"), Code(\"OpenOptions::open\"), Text(\", \"), Code(\"DirBuilder::create\"), Text(\", the 10 \"), Code(\"Path\"), Text(\" methods that touch the filesystem, and the types \"), Code(\"File\"), Text(\", \"), Code(\"OpenOptions\"), Text(\", \"), Code(\"DirBuilder\"), Text(\", \"), Code(\"ReadDir\"), Text(\", \"), Code(\"DirEntry\")]], [[Text(\"Network\")], [Text(\"4\")], [Text(\"the types \"), Code(\"TcpStream\"), Text(\", \"), Code(\"TcpListener\"), Text(\", \"), Code(\"UdpSocket\"), Text(\", and \"), Code(\"ToSocketAddrs::to_socket_addrs\")]], [[Text(\"Process, process-global state, telemetry\")], [Text(\"18\")], [Text(\"the types \"), Code(\"Command\"), Text(\", \"), Code(\"Child\"), Text(\" and \"), Code(\"alloc::System\"), Text(\", the effectful methods of the first two, \"), Code(\"process::exit\"), Text(\", \"), Code(\"abort\"), Text(\", \"), Code(\"id\"), Text(\", \"), Code(\"panic::set_hook\"), Text(\", \"), Code(\"take_hook\"), Text(\", \"), Code(\"Backtrace::capture\"), Text(\", \"), Code(\"force_capture\"), Text(\", \"), Code(\"alloc::handle_alloc_error\")]], [[Text(\"Concurrency and scheduling\")], [Text(\"20\")], [Code(\"thread::spawn\"), Text(\", \"), Code(\"scope\"), Text(\", \"), Code(\"sleep\"), Text(\", \"), Code(\"park\"), Text(\", \"), Code(\"current\"), Text(\", the \"), Code(\"Builder\"), Text(\" type, and the blocking parts of \"), Code(\"std::sync\"), Text(\": \"), Code(\"mpsc::channel\"), Text(\", \"), Code(\"sync_channel\"), Text(\", the \"), Code(\"Receiver\"), Text(\" type with \"), Code(\"recv\"), Text(\" and \"), Code(\"recv_timeout\"), Text(\", \"), Code(\"Condvar\"), Text(\" with \"), Code(\"wait\"), Text(\" and \"), Code(\"wait_timeout\"), Text(\", \"), Code(\"Barrier\"), Text(\", \"), Code(\"Once::wait\")]], [[Text(\"Randomness\")], [Text(\"1\")], [Code(\"hash::RandomState\")]], [[Text(\"Input and output\")], [Text(\"12\")], [Code(\"io::stdin\"), Text(\", \"), Code(\"stdout\"), Text(\", \"), Code(\"stderr\"), Text(\", \"), Code(\"pipe\"), Text(\", the \"), Code(\"Stdin\"), Text(\", \"), Code(\"Stdout\"), Text(\" and \"), Code(\"Stderr\"), Text(\" types, and the five print macros\")]]] }, Paragraph([Text(\"A type entry replaces the narrower constructor entry it subsumes, as \"), Code(\"std::process::Command\"), Text(\" replaced \"), Code(\"Command::new\"), Text(\"; by rule 7 the effectful methods of those types are listed as well, so a value obtained from elsewhere is still caught.\")]), Paragraph([Strong([Text(\"Candidates this change did not take\")]), Text(\", each verified to resolve and fire, each a judgment about cost in ordinary code rather than about whether it is an effect: \"), Code(\"Mutex::lock\"), Text(\", \"), Code(\"RwLock::read\"), Text(\" and \"), Code(\"write\"), Text(\"; the atomics; \"), Code(\"Once::call_once\"), Text(\", \"), Code(\"OnceLock::get_or_init\"), Text(\" and \"), Code(\"set\"), Text(\", \"), Code(\"LazyLock\"), Text(\"; \"), Code(\"Condvar::wait_while\"), Text(\" and \"), Code(\"wait_timeout_while\"), Text(\", \"), Code(\"Once::wait_force\"), Text(\", \"), Code(\"mpsc::Sender::send\"), Text(\", \"), Code(\"SyncSender::send\"), Text(\", \"), Code(\"Receiver::iter\"), Text(\"; \"), Code(\"panic::panic_any\"), Text(\"; the \"), Code(\"Backtrace\"), Text(\" type, whose \"), Code(\"Display\"), Text(\" impl formats the captured stack; and the build-time filesystem reads \"), Code(\"include_str!\"), Text(\" and \"), Code(\"include_bytes!\"), Text(\". Extending the template is a protected change for Kennedy.\")]), List { start: None, items: [[Text(\"The attribute escape of rule 5 is closed one crate at a time, not workspace-wide. Every core crate carries \"), Code(\"#![forbid(clippy::disallowed_methods, clippy::disallowed_types, clippy::disallowed_macros)]\"), Text(\" at its crate root, which refuses an item-level allow (6d) and costs clean code nothing (7c). The root \"), Code(\"Cargo.toml\"), Text(\" keeps the deny level, because rule 8 makes a forbidding table incompatible with \"), Code(\"clap\"), Text(\"; it carries a comment saying so.\")], [Code(\"effect.core_clippy_template\"), Text(\" therefore reports a fourth problem, \"), Code(\"unforbidden\"), Text(\", naming the lints a crate root leaves out. The check is a text scan of the crate root for \"), Code(\"#![forbid(...)]\"), Text(\"; it does not understand a group forbid such as \"), Code(\"forbid(clippy::all)\"), Text(\", and it would be fooled by the text inside a comment.\")], [Code(\".cargo/config.toml\"), Text(\" is a protected surface from CHG-001.3, because \"), Code(\"--cap-lints=warn\"), Text(\" there disables every lint at once (6n).\")]] }, Heading { level: 2, slug: \"consequences\", children: [Text(\"Consequences\")] }, List { start: None, items: [[Text(\"A change to the root \"), Code(\"clippy.toml\"), Text(\" needs the same change in the template and in every core crate's copy. The root file and the template are protected surfaces; the crates' copies are not, but the rule reports any copy that differs. This costs more edits than \"), Code(\"CLIPPY_CONF_DIR\"), Text(\", but it keeps §12.1's commands and environment unchanged.\")], [Text(\"A \"), Code(\".clippy.toml\"), Text(\" at the repository root would shadow the root \"), Code(\"clippy.toml\"), Text(\" for every crate without its own file. The rule checks core crate directories only, so this remains a review item.\")], [Strong([Text(\"The deny list sees direct uses only\")]), Text(\" (§6.8). It does not see a call through a helper in another crate, a call the compiler generates from a macro, or an effect reached through a trait object. Experiment 4 proves each entry fires on a direct use, and nothing more.\")], [Strong([Text(\"What the list leaves out, on purpose.\")]), Text(\" \"), Code(\"HashMap::new\"), Text(\" and \"), Code(\"HashMap::with_capacity\"), Text(\" seed themselves from \"), Code(\"RandomState\"), Text(\", which the list catches only when a crate names it; iteration order stays a source of nondeterminism that a deny list cannot reach. Platform extension traits, such as \"), Code(\"std::os::unix::process::CommandExt::exec\"), Text(\" and \"), Code(\"std::os::unix::process::parent_id\"), Text(\", are effectful but would not resolve on another target, so listing them would trade a real check for a portability warning. Unstable APIs, such as \"), Code(\"std::random\"), Text(\", are left out until they are stable. Each is a candidate for a later change, not an oversight.\")], [Strong([Text(\"A core crate cannot allow these lints anywhere, including its own tests.\")]), Text(\" That is the point of the crate-root \"), Code(\"forbid\"), Text(\", and it is also its cost: a core-crate test that wants a real clock has to take one through a port, like the code it tests.\")], [Strong([Text(\"Four ways to switch it off\")]), Text(\" are now recorded. An attribute in the crate (rule 5), unless the lint table forbids the lint. An allow flag in \"), Code(\"RUSTFLAGS\"), Text(\", which works at the repository's current deny level (6e) and stops working under \"), Code(\"forbid\"), Text(\" (6l). \"), Code(\"--cap-lints=warn\"), Text(\" from \"), Code(\"RUSTFLAGS\"), Text(\" or \"), Code(\".cargo/config.toml\"), Text(\" (rule 6), which no lint level survives; \"), Code(\".cargo/config.toml\"), Text(\" is not on the protected list in \"), Code(\".rha/policy.toml\"), Text(\". And a crate whose \"), Code(\"clippy.toml\"), Text(\" is missing, edited, or shadowed, which is what \"), Code(\"effect.core_clippy_template\"), Text(\" reports.\")], [Text(\"Until CHG-003 wires the rule in and CHG-005 creates core crates, the template protects no product code: the deny list is demonstrated on fixtures only.\")]] }]",
    "headings": "[Heading { level: 1, text: \"ADR-0002: Clippy configuration discovery and the core-crate template\", slug: \"adr-0002-clippy-configuration-discovery-and-the-core-crate-template\", base_slug: \"adr-0002-clippy-configuration-discovery-and-the-core-crate-template\", line: 1 }, Heading { level: 2, text: \"Context\", slug: \"context\", base_slug: \"context\", line: 7 }, Heading { level: 2, text: \"Observations\", slug: \"observations\", base_slug: \"observations\", line: 11 }, Heading { level: 2, text: \"Observed rule\", slug: \"observed-rule\", base_slug: \"observed-rule\", line: 49 }, Heading { level: 2, text: \"Decision\", slug: \"decision\", base_slug: \"decision\", line: 67 }, Heading { level: 2, text: \"Consequences\", slug: \"consequences\", base_slug: \"consequences\", line: 92 }]",
    "markdown": "# ADR-0002: Clippy configuration discovery and the core-crate template\n\n- **Status:** proposed in CHG-001, revised in CHG-001.1 and CHG-001.2; accepted when Kennedy accepts CHG-001.\n- **Date:** 2026-09-19\n- **Environment:** `clippy 0.1.98 (48a229ceae 2026-09-01)`, rustc 1.98.1, Linux (WSL2). Every experiment runs with `CLIPPY_CONF_DIR`, `RUSTFLAGS`, `CARGO_ENCODED_RUSTFLAGS`, and `CARGO_BUILD_RUSTFLAGS` unset, unless it sets one on purpose. The same runs happen on GitHub `ubuntu24` in `L0.nextest`.\n\n## Context\n\nSpec §6.8 gives core crates a `clippy.toml` deny list of ambient-effect paths and says: \"Verify how the pinned Clippy locates and merges configuration files.\" The Clippy book says the search starts at `CLIPPY_CONF_DIR`, else `CARGO_MANIFEST_DIR`, else the current directory, and walks up until one file is found; it does not document merging. The plan expected \"nearest file wins, no merge\" and named a fallback, `CLIPPY_CONF_DIR` set per crate by `xtask ci`, in case the observation disagreed.\n\n## Observations\n\nEach experiment runs in a fixture workspace that [`xtask/tests/clippy_corpus.rs`](../../xtask/tests/clippy_corpus.rs) generates under `target/clippy-corpus/` from the three files that own the facts involved: the root `Cargo.toml` (`[workspace.package]` and the lint tables), the root `clippy.toml`, and [the template](../../xtask/templates/core-clippy.toml). Only the crate sources are committed, in [`xtask/tests/corpus/clippy/`](../../xtask/tests/corpus/clippy/). The fixture is emptied first, so no cached build stands in for a fresh one. The same file runs the experiments in every `L0.nextest` run and, with `RHA_CLIPPY_RECORD=<dir>`, writes one record per experiment: the generated files with their digests, the literal command, the exit status, and the captured output. The records of these experiments are in [`evidence/w1-clippy/20260919T221927Z-edd4bac756f9/`](../../evidence/w1-clippy/20260919T221927Z-edd4bac756f9/). Seven loose files beside that directory are the first run, at `28ae543`, written by the shell script CHG-001.1 replaced; they name the committed fixtures of the time, which no longer exist.\n\n**Discovery and merging.** Crates marked *template* hold a byte-equal copy of the template; *reduced* holds the template without the settings the root file sets.\n\n| # | Crate and command | Observed |\n| --- | --- | --- |\n| 1 | core, template, calls `std::time::SystemTime::now()`: `cargo clippy -p core-seeded` | Exit 101. ``error: use of a disallowed method `std::time::SystemTime::now` `` and `` = note: `-D clippy::disallowed-methods` implied by `-D clippy::all` ``. |\n| 2 | adapter, no local file, same call: `cargo clippy -p adapter-x` | Exit 0, no finding. |\n| 3 | core, reduced, a test calls `unwrap()`: `cargo clippy -p core-deny-only --all-targets` | Exit 0 with ``warning: used `unwrap()` on a `Result` value``: **`unwrap_used` fired**, although the fixture root sets `allow-unwrap-in-tests = true`. |\n| 3c | control for 3: the same test `unwrap()` in the adapter, whose nearest file is the fixture root | Exit 0, no `unwrap_used`: the root allowance works where the root file is the nearest. |\n| 3b | core, template, a test calls `unwrap()`: `cargo clippy -p core-clean --all-targets` | Exit 0, no `unwrap_used`: the template's repeat of the root settings restores the allowance. |\n| 2-control | core, template, the same call as 2: `cargo clippy -p core-seeded` | Exit 101 with the disallowed-method error: the deny list does fire in this fixture, which is what makes 2 and 3c meaningful. |\n| 4 | core, template, one use of every template entry: `cargo clippy -p core-every` | Exit 101; every entry reported as `use of a disallowed method/type/macro`, 122 findings for 111 entries, because a use that names a type and calls one of its methods fires both. No unresolved-path warning. |\n| 5 | core, `clippy.toml` the template and `.clippy.toml` the root file, calls the clock | Exit 0 and no finding, with ``warning: using config file `….clippy.toml`, `…clippy.toml` will be ignored``: the dotfile **silently replaces the deny list**. |\n\n**Lint attributes and flags (experiment 6).** Every crate holds the template. 6a–6e use the repository's lint table; 6f–6n use one where `disallowed_methods`, `disallowed_types`, and `disallowed_macros` are `forbid`.\n\n| # | Crate or flag | Observed |\n| --- | --- | --- |\n| 6-control | no attribute and no flag: `cargo clippy -p core-seeded` | Exit 101 with the disallowed-method error, so the silent runs below mean something. |\n| 6a | `#[allow(clippy::disallowed_methods)]` on the item | Exit 0, no finding: the deny list is off for that item. |\n| 6b | `#[expect(clippy::disallowed_methods)]` on the item | Exit 0, no finding. |\n| 6c | `#![allow(clippy::disallowed_methods)]` at the crate root | Exit 0, no finding. |\n| 6d | `#![forbid(...)]` at the crate root above an item `#[allow]` | Exit 101, `error[E0453]: allow(clippy::disallowed_methods) incompatible with previous forbid`. |\n| 6e | no attribute, `RUSTFLAGS=-Aclippy::disallowed_methods` | Exit 0, no finding. |\n| 6f, 6g, 6h | the three attribute forms of 6a, 6b, 6c, under the forbidding lint table | Exit 101, `E0453` in each. |\n| 6i | no attribute, under forbid | Exit 101, the ordinary disallowed-method error. |\n| 6j | core crate with nothing on the deny list, under forbid | Exit 0: no false alarm. |\n| 6k | adapter making the same call, under forbid | Exit 0: adapters are unaffected, because their configuration lists no paths. |\n| 6l | no attribute, under forbid, `RUSTFLAGS=-Aclippy::disallowed_methods` | Exit 101, the error stands: `forbid` is not lowered by an allow flag. |\n| 6m | no attribute, under forbid, `RUSTFLAGS=--cap-lints=warn` | Exit 0, the finding appears as a warning. |\n| 6n | the same `--cap-lints=warn` in the fixture's `.cargo/config.toml` | Exit 0, the finding appears as a warning. |\n| 7a | a crate whose macro expands to `#[allow(clippy::style)]`, under the forbidding lint table | Exit 101, `E0453`: a group allow a macro wrote is refused exactly as one written by hand. |\n| 7b | the same crate under the deny level | Exit 0. |\n| 7c | a core crate with `#![forbid(...)]` at its own root and nothing on the deny list | Exit 0: the per-crate route costs clean code nothing. |\n\n## Observed rule\n\nThis record owns these observations (§11.7.1). The specification states the consequence and points here; what a finding would change in its text is queued in [`docs/proposals/spec-v0.11.md`](../proposals/spec-v0.11.md) for the version bump.\n\n1. **Per-crate discovery.** Clippy configures each crate from the first configuration file found in the crate's directory or its ancestors (1, 2, 3c). A core crate's local file does not apply to a sibling adapter.\n2. **No merge.** The file found replaces every file further up; a crate with its own file loses the root file's settings (3; control 3c; 3b).\n3. **Dotfile precedence.** In one directory, `.clippy.toml` wins over `clippy.toml`. The warning does not change the exit status, so a deny list can be replaced silently (5).\n4. **Deny level.** Under the workspace lint table (`clippy::all` at deny), every template entry resolves and fails the build (1, 4).\n5. **Attributes outrank the configuration file.** The file decides *which paths* are reported; the lint level decides *whether* a report is an error, and `#[allow]`, `#[expect]`, and `#![allow]` in the crate set that level locally (6a, 6b, 6c). A `forbid` above them, from the crate root or the lint table, makes each of those attributes `E0453` (6d, 6f, 6g, 6h) without changing anything else (6i, 6j, 6k).\n6. **A level is not a ceiling.** `--cap-lints=warn`, from `RUSTFLAGS` or from `.cargo/config.toml`, lowers a forbidden finding to a warning (6m, 6n). An `-A` flag does not (6l).\n\n8. **A forbidding lint table refuses macro-generated group allows.** Where any macro in the crate expands to `#[allow(clippy::style)]`, forbidding a lint of that group is `E0453` (7a), and at the deny level the same crate is silent (7b). This is not hypothetical: setting the three lints to `forbid` in this repository's own `Cargo.toml` stopped `xtask` compiling with 15 such errors, all of them from `clap`'s `derive(Parser)` and `derive(Subcommand)`. A `serde` derive does not do this, so a core crate can forbid the lints at its own root (7c).\n7. **A type entry fires where the type is named.** It catches a type in a signature, a type annotation, or a qualified path such as `File::open`, but not a value a dependency hands back, and not a bare unit-struct value: `let a = std::alloc::System;` is silent where `let a: std::alloc::System = …;` fires. The effectful method of each such type is therefore listed beside it.\n\nRules 1 and 2 are what the plan pre-registered as its expectation, and both hold, so the `CLIPPY_CONF_DIR` fallback is not used. Rule 3 answers the plan's listed Unknown, and its answer is the outcome the plan told the Executor to avoid rather than one it expected. Rules 4 to 7 were not pre-registered.\n\n**Not observed.** Whether the upward search stops at the workspace root or continues to the filesystem root: in every fixture a file exists at the workspace root. `CLIPPY_CONF_DIR` itself. Whether the attribute results carry to `disallowed_types` and `disallowed_macros`: 6a to 6n name `clippy::disallowed_methods` only, although all three lints were forbidden in the second fixture. Other Clippy versions: the corpus reruns every experiment whenever the toolchain changes.\n\n## Decision\n\n- Every crate with role `core` has a `clippy.toml` byte-equal to [`xtask/templates/core-clippy.toml`](../../xtask/templates/core-clippy.toml). Adapters, apps, and tools have none and use the root file. The template holds the §6.8 entries, the plan's W1 additions, the entries added in CHG-001.2, and, because of rule 2, every setting of the root `clippy.toml`.\n- Rule `effect.core_clippy_template` reports, per core crate, a missing `clippy.toml`, one that differs from the template, and a `.clippy.toml` beside it (rule 3). Its witness is the path and the sha256 digests. It is implemented in [`xtask/src/clippy_template.rs`](../../xtask/src/clippy_template.rs) and runs in L0 once `cargo xtask architecture` exists (CHG-003).\n- Fixtures are generated, not committed (CHG-001.1). A committed fixture is a copy of the root lint table, the root `clippy.toml`, or the template, and copies need drift tests to stay true. The one copy left is the template's repeat of the root settings, which rule 2 forces; `template_repeats_every_root_setting` guards it.\n- The deny list is extended in CHG-001.2 from 15 entries to 111, and experiment 4 keeps every entry honest: a path that stops resolving stops firing, and the test fails. The entries come from the ten paths this record named in CHG-001, a sweep of the Rust 1.98.1 standard-library source, and two adversarial reviews of that sweep, all kept to the effects §6.8 names.\n\n| Category | Entries | Examples |\n| --- | --- | --- |\n| Clocks | 4 | `SystemTime::now`, `Instant::elapsed` |\n| Environment, arguments, working directory | 16 | `env::var_os`, `env::args`, `env::current_dir`, `path::absolute`, `IsTerminal::is_terminal` |\n| Storage | 36 | 19 `std::fs` functions, `OpenOptions::open`, `DirBuilder::create`, the 10 `Path` methods that touch the filesystem, and the types `File`, `OpenOptions`, `DirBuilder`, `ReadDir`, `DirEntry` |\n| Network | 4 | the types `TcpStream`, `TcpListener`, `UdpSocket`, and `ToSocketAddrs::to_socket_addrs` |\n| Process, process-global state, telemetry | 18 | the types `Command`, `Child` and `alloc::System`, the effectful methods of the first two, `process::exit`, `abort`, `id`, `panic::set_hook`, `take_hook`, `Backtrace::capture`, `force_capture`, `alloc::handle_alloc_error` |\n| Concurrency and scheduling | 20 | `thread::spawn`, `scope`, `sleep`, `park`, `current`, the `Builder` type, and the blocking parts of `std::sync`: `mpsc::channel`, `sync_channel`, the `Receiver` type with `recv` and `recv_timeout`, `Condvar` with `wait` and `wait_timeout`, `Barrier`, `Once::wait` |\n| Randomness | 1 | `hash::RandomState` |\n| Input and output | 12 | `io::stdin`, `stdout`, `stderr`, `pipe`, the `Stdin`, `Stdout` and `Stderr` types, and the five print macros |\n\n  A type entry replaces the narrower constructor entry it subsumes, as `std::process::Command` replaced `Command::new`; by rule 7 the effectful methods of those types are listed as well, so a value obtained from elsewhere is still caught.\n\n  **Candidates this change did not take**, each verified to resolve and fire, each a judgment about cost in ordinary code rather than about whether it is an effect: `Mutex::lock`, `RwLock::read` and `write`; the atomics; `Once::call_once`, `OnceLock::get_or_init` and `set`, `LazyLock`; `Condvar::wait_while` and `wait_timeout_while`, `Once::wait_force`, `mpsc::Sender::send`, `SyncSender::send`, `Receiver::iter`; `panic::panic_any`; the `Backtrace` type, whose `Display` impl formats the captured stack; and the build-time filesystem reads `include_str!` and `include_bytes!`. Extending the template is a protected change for Kennedy.\n- The attribute escape of rule 5 is closed one crate at a time, not workspace-wide. Every core crate carries `#![forbid(clippy::disallowed_methods, clippy::disallowed_types, clippy::disallowed_macros)]` at its crate root, which refuses an item-level allow (6d) and costs clean code nothing (7c). The root `Cargo.toml` keeps the deny level, because rule 8 makes a forbidding table incompatible with `clap`; it carries a comment saying so.\n- `effect.core_clippy_template` therefore reports a fourth problem, `unforbidden`, naming the lints a crate root leaves out. The check is a text scan of the crate root for `#![forbid(...)]`; it does not understand a group forbid such as `forbid(clippy::all)`, and it would be fooled by the text inside a comment.\n- `.cargo/config.toml` is a protected surface from CHG-001.3, because `--cap-lints=warn` there disables every lint at once (6n).\n\n## Consequences\n\n- A change to the root `clippy.toml` needs the same change in the template and in every core crate's copy. The root file and the template are protected surfaces; the crates' copies are not, but the rule reports any copy that differs. This costs more edits than `CLIPPY_CONF_DIR`, but it keeps §12.1's commands and environment unchanged.\n- A `.clippy.toml` at the repository root would shadow the root `clippy.toml` for every crate without its own file. The rule checks core crate directories only, so this remains a review item.\n- **The deny list sees direct uses only** (§6.8). It does not see a call through a helper in another crate, a call the compiler generates from a macro, or an effect reached through a trait object. Experiment 4 proves each entry fires on a direct use, and nothing more.\n- **What the list leaves out, on purpose.** `HashMap::new` and `HashMap::with_capacity` seed themselves from `RandomState`, which the list catches only when a crate names it; iteration order stays a source of nondeterminism that a deny list cannot reach. Platform extension traits, such as `std::os::unix::process::CommandExt::exec` and `std::os::unix::process::parent_id`, are effectful but would not resolve on another target, so listing them would trade a real check for a portability warning. Unstable APIs, such as `std::random`, are left out until they are stable. Each is a candidate for a later change, not an oversight.\n- **A core crate cannot allow these lints anywhere, including its own tests.** That is the point of the crate-root `forbid`, and it is also its cost: a core-crate test that wants a real clock has to take one through a port, like the code it tests.\n- **Four ways to switch it off** are now recorded. An attribute in the crate (rule 5), unless the lint table forbids the lint. An allow flag in `RUSTFLAGS`, which works at the repository's current deny level (6e) and stops working under `forbid` (6l). `--cap-lints=warn` from `RUSTFLAGS` or `.cargo/config.toml` (rule 6), which no lint level survives; `.cargo/config.toml` is not on the protected list in `.rha/policy.toml`. And a crate whose `clippy.toml` is missing, edited, or shadowed, which is what `effect.core_clippy_template` reports.\n- Until CHG-003 wires the rule in and CHG-005 creates core crates, the template protects no product code: the deny list is demonstrated on fixtures only.\n"
  },
  "docs/adr/BDR-0001-library-owns-page-identity.md": {
    "body": "[Heading { level: 1, slug: \"bdr-0001-library-owns-page-identity-and-the-source-repository-port\", children: [Text(\"BDR-0001: \"), Code(\"library\"), Text(\" owns page identity and the source repository port\")] }, BlockQuote { kind: None, children: [Paragraph([Strong([Text(\"Status: accepted at DP-1.3\")]), Text(\" by Kennedy on 2026-09-22, criteria as written. Originally proposed: Drafted by the Executor in P-A stage 1. Kennedy accepts or amends the refutation criterion before any product crate exists (§7.8 step 10). The candidate scoring is in \"), Link { href: \"../../evidence/CHG-005/bdr-candidates/\", children: [Code(\"evidence/CHG-005/bdr-candidates/\")] }, Text(\": three stub decompositions were run through \"), Code(\"cargo xtask architecture\"), Text(\". A is this plan's §3, B has fewer crates, C has more. All three pass the checker with zero errors. The choice therefore rests on the §7.2 evidence below, not on a rule violation.\")])] }, CodeBlock { lang: Some(\"text\"), text: \"Boundary: library  (page identity, source loading)\\nLevel:    crate\\n\\nHypothesis:\\n  Page identity (PageId, RelPath, Source, Digest, Corpus) and complete-or-fail loading\\n  change for different reasons than markdown syntax, and are consumed by more than\\n  the parser.\\n\\nEvidence (data source):\\n  - Invariant sets differ: unique page ids (library) versus unique slugs and total\\n    parsing (document)                                              (domain argument)\\n  - adapter-fs implements SourceRepository and must not pull in the parser\\n                                                (code structure: candidate B's closure)\\n  - Three consumers: document, graph, site; plus adapter-fs as implementer\\n                                                        (code structure: stub graph A)\\n\\nCounter-evidence:\\n  - Every parse consumes a Source, so document depends on library (BDR-0002).\\n  - Only one repository implementation exists until adapter-git (later).\\n\\nExpected benefit:\\n  dependency isolation for adapters; an owner for the duplicate-id invariant\\n\\nExpected cost:\\n  one crate and its public API; Source is passed by reference, so there is no conversion\\n\\nRefutation criterion (proposed; Kennedy decides at DP-1.3):\\n  metric:     share of qualifying changes to library's public API that also change\\n              document's public API\\n  source:     git history of the P-D feature stages and later changes, by stage commit\\n              range (plan §8.1 P-D); qualifying = touches crates/library/src\\n  window:     the five P-D feature stages plus the next 20 qualifying changes,\\n              or 3 months, whichever is later\\n  threshold:  > 50 %\\n  action:     merge-back proposal (library + document); a review, not automatic (§7.3)\\n\" }, Heading { level: 2, slug: \"candidates-considered-plan-24-rule-3\", children: [Text(\"Candidates considered (plan §2.4 rule 3)\")] }, Table { align: [None, None, None, None, None], head: [[Text(\"Candidate\")], [Text(\"Crates (cores)\")], [Text(\"Normal edges\")], [Text(\"Checker\")], [Text(\"Why not chosen\")]], rows: [[[Strong([Text(\"A\")]), Text(\", plan §3 (chosen)\")], [Text(\"8 (4)\")], [Text(\"18\")], [Text(\"0 errors, 1 warning (\"), Code(\"adapter.foreign_core\"), Text(\": \"), Code(\"adapter-html → document\"), Text(\")\")], [Text(\"–\")]], [[Text(\"B, \"), Code(\"library\"), Text(\" and \"), Code(\"document\"), Text(\" merged into \"), Code(\"content\")], [Text(\"7 (3)\")], [Text(\"14\")], [Text(\"0 errors, 1 warning\")], [Code(\"adapter-fs\"), Text(\" must depend on \"), Code(\"content\"), Text(\" to implement \"), Code(\"SourceRepository\"), Text(\", which puts the markdown parser in the dependency closure of an adapter that never parses (§7.2, \\\"dependencies worth keeping out of the parent\\\"). Page identity and syntax also have different invariants: unique ids versus unique slugs.\")]], [[Text(\"C, \"), Code(\"assembly\"), Text(\" and \"), Code(\"build\"), Text(\" as crates\")], [Text(\"10 (6)\")], [Text(\"28\")], [Text(\"0 errors, 2 warnings\")], [Text(\"Each of the two crates has exactly one consumer, \"), Code(\"site\"), Text(\", which is §7.2's \\\"one consumer and no enforcement benefit\\\" cost, and it adds 10 edges. It does buy enforced direction between them, which A lacks until P-C validates the module check. That is BDR-0003's refutation condition.\")]]] }]",
    "headings": "[Heading { level: 1, text: \"BDR-0001: library owns page identity and the source repository port\", slug: \"bdr-0001-library-owns-page-identity-and-the-source-repository-port\", base_slug: \"bdr-0001-library-owns-page-identity-and-the-source-repository-port\", line: 1 }, Heading { level: 2, text: \"Candidates considered (plan §2.4 rule 3)\", slug: \"candidates-considered-plan-24-rule-3\", base_slug: \"candidates-considered-plan-24-rule-3\", line: 43 }]",
    "markdown": "# BDR-0001: `library` owns page identity and the source repository port\n\n> **Status: accepted at DP-1.3** by Kennedy on 2026-09-22, criteria as written. Originally proposed: Drafted by the Executor in P-A stage 1. Kennedy accepts or amends the refutation criterion before any product crate exists (§7.8 step 10). The candidate scoring is in [`evidence/CHG-005/bdr-candidates/`](../../evidence/CHG-005/bdr-candidates/): three stub decompositions were run through `cargo xtask architecture`. A is this plan's §3, B has fewer crates, C has more. All three pass the checker with zero errors. The choice therefore rests on the §7.2 evidence below, not on a rule violation.\n\n```text\nBoundary: library  (page identity, source loading)\nLevel:    crate\n\nHypothesis:\n  Page identity (PageId, RelPath, Source, Digest, Corpus) and complete-or-fail loading\n  change for different reasons than markdown syntax, and are consumed by more than\n  the parser.\n\nEvidence (data source):\n  - Invariant sets differ: unique page ids (library) versus unique slugs and total\n    parsing (document)                                              (domain argument)\n  - adapter-fs implements SourceRepository and must not pull in the parser\n                                                (code structure: candidate B's closure)\n  - Three consumers: document, graph, site; plus adapter-fs as implementer\n                                                        (code structure: stub graph A)\n\nCounter-evidence:\n  - Every parse consumes a Source, so document depends on library (BDR-0002).\n  - Only one repository implementation exists until adapter-git (later).\n\nExpected benefit:\n  dependency isolation for adapters; an owner for the duplicate-id invariant\n\nExpected cost:\n  one crate and its public API; Source is passed by reference, so there is no conversion\n\nRefutation criterion (proposed; Kennedy decides at DP-1.3):\n  metric:     share of qualifying changes to library's public API that also change\n              document's public API\n  source:     git history of the P-D feature stages and later changes, by stage commit\n              range (plan §8.1 P-D); qualifying = touches crates/library/src\n  window:     the five P-D feature stages plus the next 20 qualifying changes,\n              or 3 months, whichever is later\n  threshold:  > 50 %\n  action:     merge-back proposal (library + document); a review, not automatic (§7.3)\n```\n\n## Candidates considered (plan §2.4 rule 3)\n\n| Candidate | Crates (cores) | Normal edges | Checker | Why not chosen |\n| --- | --- | --- | --- | --- |\n| **A**, plan §3 (chosen) | 8 (4) | 18 | 0 errors, 1 warning (`adapter.foreign_core`: `adapter-html → document`) | – |\n| B, `library` and `document` merged into `content` | 7 (3) | 14 | 0 errors, 1 warning | `adapter-fs` must depend on `content` to implement `SourceRepository`, which puts the markdown parser in the dependency closure of an adapter that never parses (§7.2, \"dependencies worth keeping out of the parent\"). Page identity and syntax also have different invariants: unique ids versus unique slugs. |\n| C, `assembly` and `build` as crates | 10 (6) | 28 | 0 errors, 2 warnings | Each of the two crates has exactly one consumer, `site`, which is §7.2's \"one consumer and no enforcement benefit\" cost, and it adds 10 edges. It does buy enforced direction between them, which A lacks until P-C validates the module check. That is BDR-0003's refutation condition. |\n"
  },
  "docs/adr/BDR-0002-document-depends-on-library.md": {
    "body": "[Heading { level: 1, slug: \"bdr-0002-document-depends-on-library\", children: [Text(\"BDR-0002: \"), Code(\"document\"), Text(\" depends on \"), Code(\"library\")] }, BlockQuote { kind: None, children: [Paragraph([Strong([Text(\"Status: accepted at DP-1.3\")]), Text(\" by Kennedy on 2026-09-22, criteria as written. Originally proposed: Drafted by the Executor in P-A stage 1. Kennedy accepts or amends the refutation criterion before any product crate exists (§7.8 step 10). The candidate scoring is in \"), Link { href: \"../../evidence/CHG-005/bdr-candidates/\", children: [Code(\"evidence/CHG-005/bdr-candidates/\")] }, Text(\": three stub decompositions were run through \"), Code(\"cargo xtask architecture\"), Text(\". A is this plan's §3, B has fewer crates, C has more. All three pass the checker with zero errors. The choice therefore rests on the §7.2 evidence below, not on a rule violation.\")])] }, CodeBlock { lang: Some(\"text\"), text: \"Boundary: document  (markdown to typed document), with the declared edge document -> library\\nLevel:    crate\\n\\nHypothesis:\\n  Parsing is a pure transformation of a library Source into document's own node tree;\\n  no vendor (pulldown-cmark) type crosses its public API, so the parser choice stays\\n  document's decision.\\n\\nEvidence (data source):\\n  - pulldown-cmark is needed by document only; the forbidden rule\\n    site -> pulldown-cmark (plan §5) enforces it                (code structure, checker)\\n  - Slug and heading invariants have an owner with witnesses (plan §3.2)\\n                                                                    (domain argument)\\n\\nCounter-evidence:\\n  - document needs PageId and Source from library: the edge is real, declared (D4),\\n    and acyclic, but it couples the two.\\n\\nExpected benefit:\\n  parser isolation; a replaceable parser behind document's own types\\n\\nExpected cost:\\n  one mapping from pulldown events to document::Node\\n\\nRefutation criterion (proposed; Kennedy decides at DP-1.3):\\n  metric:     (a) number of pulldown-cmark types in document's public API;\\n              (b) share of document changes that require a library API change\\n  source:     (a) cargo public-api or rustdoc JSON of document; (b) git, stage ranges\\n  window:     (a) every P-D stage; (b) the five P-D feature stages\\n  threshold:  (a) > 0; (b) > 50 %\\n  action:     (a) defect: wrap the type; (b) merge-back proposal (with BDR-0001)\\n\" }, Heading { level: 2, slug: \"candidates-considered-plan-24-rule-3\", children: [Text(\"Candidates considered (plan §2.4 rule 3)\")] }, Table { align: [None, None, None, None, None], head: [[Text(\"Candidate\")], [Text(\"Crates (cores)\")], [Text(\"Normal edges\")], [Text(\"Checker\")], [Text(\"Why not chosen\")]], rows: [[[Strong([Text(\"A\")]), Text(\", plan §3 (chosen)\")], [Text(\"8 (4)\")], [Text(\"18\")], [Text(\"0 errors, 1 warning (\"), Code(\"adapter.foreign_core\"), Text(\": \"), Code(\"adapter-html → document\"), Text(\")\")], [Text(\"–\")]], [[Text(\"B, \"), Code(\"library\"), Text(\" and \"), Code(\"document\"), Text(\" merged into \"), Code(\"content\")], [Text(\"7 (3)\")], [Text(\"14\")], [Text(\"0 errors, 1 warning\")], [Code(\"adapter-fs\"), Text(\" must depend on \"), Code(\"content\"), Text(\" to implement \"), Code(\"SourceRepository\"), Text(\", which puts the markdown parser in the dependency closure of an adapter that never parses (§7.2, \\\"dependencies worth keeping out of the parent\\\"). Page identity and syntax also have different invariants: unique ids versus unique slugs.\")]], [[Text(\"C, \"), Code(\"assembly\"), Text(\" and \"), Code(\"build\"), Text(\" as crates\")], [Text(\"10 (6)\")], [Text(\"28\")], [Text(\"0 errors, 2 warnings\")], [Text(\"Each of the two crates has exactly one consumer, \"), Code(\"site\"), Text(\", which is §7.2's \\\"one consumer and no enforcement benefit\\\" cost, and it adds 10 edges. It does buy enforced direction between them, which A lacks until P-C validates the module check. That is BDR-0003's refutation condition.\")]]] }]",
    "headings": "[Heading { level: 1, text: \"BDR-0002: document depends on library\", slug: \"bdr-0002-document-depends-on-library\", base_slug: \"bdr-0002-document-depends-on-library\", line: 1 }, Heading { level: 2, text: \"Candidates considered (plan §2.4 rule 3)\", slug: \"candidates-considered-plan-24-rule-3\", base_slug: \"candidates-considered-plan-24-rule-3\", line: 39 }]",
    "markdown": "# BDR-0002: `document` depends on `library`\n\n> **Status: accepted at DP-1.3** by Kennedy on 2026-09-22, criteria as written. Originally proposed: Drafted by the Executor in P-A stage 1. Kennedy accepts or amends the refutation criterion before any product crate exists (§7.8 step 10). The candidate scoring is in [`evidence/CHG-005/bdr-candidates/`](../../evidence/CHG-005/bdr-candidates/): three stub decompositions were run through `cargo xtask architecture`. A is this plan's §3, B has fewer crates, C has more. All three pass the checker with zero errors. The choice therefore rests on the §7.2 evidence below, not on a rule violation.\n\n```text\nBoundary: document  (markdown to typed document), with the declared edge document -> library\nLevel:    crate\n\nHypothesis:\n  Parsing is a pure transformation of a library Source into document's own node tree;\n  no vendor (pulldown-cmark) type crosses its public API, so the parser choice stays\n  document's decision.\n\nEvidence (data source):\n  - pulldown-cmark is needed by document only; the forbidden rule\n    site -> pulldown-cmark (plan §5) enforces it                (code structure, checker)\n  - Slug and heading invariants have an owner with witnesses (plan §3.2)\n                                                                    (domain argument)\n\nCounter-evidence:\n  - document needs PageId and Source from library: the edge is real, declared (D4),\n    and acyclic, but it couples the two.\n\nExpected benefit:\n  parser isolation; a replaceable parser behind document's own types\n\nExpected cost:\n  one mapping from pulldown events to document::Node\n\nRefutation criterion (proposed; Kennedy decides at DP-1.3):\n  metric:     (a) number of pulldown-cmark types in document's public API;\n              (b) share of document changes that require a library API change\n  source:     (a) cargo public-api or rustdoc JSON of document; (b) git, stage ranges\n  window:     (a) every P-D stage; (b) the five P-D feature stages\n  threshold:  (a) > 0; (b) > 50 %\n  action:     (a) defect: wrap the type; (b) merge-back proposal (with BDR-0001)\n```\n\n## Candidates considered (plan §2.4 rule 3)\n\n| Candidate | Crates (cores) | Normal edges | Checker | Why not chosen |\n| --- | --- | --- | --- | --- |\n| **A**, plan §3 (chosen) | 8 (4) | 18 | 0 errors, 1 warning (`adapter.foreign_core`: `adapter-html → document`) | – |\n| B, `library` and `document` merged into `content` | 7 (3) | 14 | 0 errors, 1 warning | `adapter-fs` must depend on `content` to implement `SourceRepository`, which puts the markdown parser in the dependency closure of an adapter that never parses (§7.2, \"dependencies worth keeping out of the parent\"). Page identity and syntax also have different invariants: unique ids versus unique slugs. |\n| C, `assembly` and `build` as crates | 10 (6) | 28 | 0 errors, 2 warnings | Each of the two crates has exactly one consumer, `site`, which is §7.2's \"one consumer and no enforcement benefit\" cost, and it adds 10 edges. It does buy enforced direction between them, which A lacks until P-C validates the module check. That is BDR-0003's refutation condition. |\n"
  },
  "docs/adr/BDR-0003-site-composite.md": {
    "body": "[Heading { level: 1, slug: \"bdr-0003-site-is-one-composite-crate-with-assembly-and-build-as-child-modules\", children: [Text(\"BDR-0003: \"), Code(\"site\"), Text(\" is one composite crate with \"), Code(\"assembly\"), Text(\" and \"), Code(\"build\"), Text(\" as child modules\")] }, BlockQuote { kind: None, children: [Paragraph([Strong([Text(\"Status: accepted at DP-1.3\")]), Text(\" by Kennedy on 2026-09-22, criteria as written. Originally proposed: Drafted by the Executor in P-A stage 1. Kennedy accepts or amends the refutation criterion before any product crate exists (§7.8 step 10). The candidate scoring is in \"), Link { href: \"../../evidence/CHG-005/bdr-candidates/\", children: [Code(\"evidence/CHG-005/bdr-candidates/\")] }, Text(\": three stub decompositions were run through \"), Code(\"cargo xtask architecture\"), Text(\". A is this plan's §3, B has fewer crates, C has more. All three pass the checker with zero errors. The choice therefore rests on the §7.2 evidence below, not on a rule violation.\")])] }, CodeBlock { lang: Some(\"text\"), text: \"Boundary: site  (composite; children assembly and build as modules; Assemble as a trait)\\nLevel:    crate for site; module for assembly and build\\n\\nHypothesis:\\n  Page assembly (TOC, breadcrumbs, backlinks) and build planning (the pure step\\n  transition) are distinct decisions with one consumer, the site glue; module privacy\\n  suffices between them, and the crate boundary buys isolation from adapters.\\n\\nEvidence (data source):\\n  - build must stay a pure (state, observation) -> (state', commands) transition;\\n    assembly is a pure projection                                    (domain argument)\\n  - C's crates each have exactly one consumer                  (code structure: stub C)\\n\\nCounter-evidence:\\n  - §7.5: a component whose correctness depends on enforced direction has earned a\\n    crate unless the module-graph check is in place and validated. Until P-C, the\\n    direction build -> assembly, never the reverse, is enforced by review only.\\n\\nExpected benefit:\\n  fewer crates and edges than C; one composite with a rha-modules.toml policy\\n\\nExpected cost:\\n  unenforced direction between children until P-C validates the module check\\n\\nRefutation criterion (proposed; Kennedy decides at DP-1.3):\\n  metric:     (a) the P-C module corpus result for the module cells (law3-d1, law6-b3,\\n              d2); (b) share of stages that change both assembly and build\\n  source:     (a) evidence/h4-module (P-C); (b) git, stage ranges (P-D)\\n  window:     (a) P-C acceptance; (b) the five P-D feature stages\\n  threshold:  (a) any module cell not validated; (b) > 60 %\\n  action:     (a) promote assembly and build to crates (candidate C);\\n              (b) merge the two modules\\n\" }, Heading { level: 2, slug: \"candidates-considered-plan-24-rule-3\", children: [Text(\"Candidates considered (plan §2.4 rule 3)\")] }, Table { align: [None, None, None, None, None], head: [[Text(\"Candidate\")], [Text(\"Crates (cores)\")], [Text(\"Normal edges\")], [Text(\"Checker\")], [Text(\"Why not chosen\")]], rows: [[[Strong([Text(\"A\")]), Text(\", plan §3 (chosen)\")], [Text(\"8 (4)\")], [Text(\"18\")], [Text(\"0 errors, 1 warning (\"), Code(\"adapter.foreign_core\"), Text(\": \"), Code(\"adapter-html → document\"), Text(\")\")], [Text(\"–\")]], [[Text(\"B, \"), Code(\"library\"), Text(\" and \"), Code(\"document\"), Text(\" merged into \"), Code(\"content\")], [Text(\"7 (3)\")], [Text(\"14\")], [Text(\"0 errors, 1 warning\")], [Code(\"adapter-fs\"), Text(\" must depend on \"), Code(\"content\"), Text(\" to implement \"), Code(\"SourceRepository\"), Text(\", which puts the markdown parser in the dependency closure of an adapter that never parses (§7.2, \\\"dependencies worth keeping out of the parent\\\"). Page identity and syntax also have different invariants: unique ids versus unique slugs.\")]], [[Text(\"C, \"), Code(\"assembly\"), Text(\" and \"), Code(\"build\"), Text(\" as crates\")], [Text(\"10 (6)\")], [Text(\"28\")], [Text(\"0 errors, 2 warnings\")], [Text(\"Each of the two crates has exactly one consumer, \"), Code(\"site\"), Text(\", which is §7.2's \\\"one consumer and no enforcement benefit\\\" cost, and it adds 10 edges. It does buy enforced direction between them, which A lacks until P-C validates the module check. That is BDR-0003's refutation condition.\")]]] }]",
    "headings": "[Heading { level: 1, text: \"BDR-0003: site is one composite crate with assembly and build as child modules\", slug: \"bdr-0003-site-is-one-composite-crate-with-assembly-and-build-as-child-modules\", base_slug: \"bdr-0003-site-is-one-composite-crate-with-assembly-and-build-as-child-modules\", line: 1 }, Heading { level: 2, text: \"Candidates considered (plan §2.4 rule 3)\", slug: \"candidates-considered-plan-24-rule-3\", base_slug: \"candidates-considered-plan-24-rule-3\", line: 40 }]",
    "markdown": "# BDR-0003: `site` is one composite crate with `assembly` and `build` as child modules\n\n> **Status: accepted at DP-1.3** by Kennedy on 2026-09-22, criteria as written. Originally proposed: Drafted by the Executor in P-A stage 1. Kennedy accepts or amends the refutation criterion before any product crate exists (§7.8 step 10). The candidate scoring is in [`evidence/CHG-005/bdr-candidates/`](../../evidence/CHG-005/bdr-candidates/): three stub decompositions were run through `cargo xtask architecture`. A is this plan's §3, B has fewer crates, C has more. All three pass the checker with zero errors. The choice therefore rests on the §7.2 evidence below, not on a rule violation.\n\n```text\nBoundary: site  (composite; children assembly and build as modules; Assemble as a trait)\nLevel:    crate for site; module for assembly and build\n\nHypothesis:\n  Page assembly (TOC, breadcrumbs, backlinks) and build planning (the pure step\n  transition) are distinct decisions with one consumer, the site glue; module privacy\n  suffices between them, and the crate boundary buys isolation from adapters.\n\nEvidence (data source):\n  - build must stay a pure (state, observation) -> (state', commands) transition;\n    assembly is a pure projection                                    (domain argument)\n  - C's crates each have exactly one consumer                  (code structure: stub C)\n\nCounter-evidence:\n  - §7.5: a component whose correctness depends on enforced direction has earned a\n    crate unless the module-graph check is in place and validated. Until P-C, the\n    direction build -> assembly, never the reverse, is enforced by review only.\n\nExpected benefit:\n  fewer crates and edges than C; one composite with a rha-modules.toml policy\n\nExpected cost:\n  unenforced direction between children until P-C validates the module check\n\nRefutation criterion (proposed; Kennedy decides at DP-1.3):\n  metric:     (a) the P-C module corpus result for the module cells (law3-d1, law6-b3,\n              d2); (b) share of stages that change both assembly and build\n  source:     (a) evidence/h4-module (P-C); (b) git, stage ranges (P-D)\n  window:     (a) P-C acceptance; (b) the five P-D feature stages\n  threshold:  (a) any module cell not validated; (b) > 60 %\n  action:     (a) promote assembly and build to crates (candidate C);\n              (b) merge the two modules\n```\n\n## Candidates considered (plan §2.4 rule 3)\n\n| Candidate | Crates (cores) | Normal edges | Checker | Why not chosen |\n| --- | --- | --- | --- | --- |\n| **A**, plan §3 (chosen) | 8 (4) | 18 | 0 errors, 1 warning (`adapter.foreign_core`: `adapter-html → document`) | – |\n| B, `library` and `document` merged into `content` | 7 (3) | 14 | 0 errors, 1 warning | `adapter-fs` must depend on `content` to implement `SourceRepository`, which puts the markdown parser in the dependency closure of an adapter that never parses (§7.2, \"dependencies worth keeping out of the parent\"). Page identity and syntax also have different invariants: unique ids versus unique slugs. |\n| C, `assembly` and `build` as crates | 10 (6) | 28 | 0 errors, 2 warnings | Each of the two crates has exactly one consumer, `site`, which is §7.2's \"one consumer and no enforcement benefit\" cost, and it adds 10 edges. It does buy enforced direction between them, which A lacks until P-C validates the module check. That is BDR-0003's refutation condition. |\n"
  },
  "docs/adr/BDR-0004-adapter-fs-two-ports.md": {
    "body": "[Heading { level: 1, slug: \"bdr-0004-adapter-fs-implements-two-ports\", children: [Text(\"BDR-0004: \"), Code(\"adapter-fs\"), Text(\" implements two ports\")] }, BlockQuote { kind: None, children: [Paragraph([Strong([Text(\"Status: accepted at DP-1.3\")]), Text(\" by Kennedy on 2026-09-22, criteria as written. Originally proposed: Drafted by the Executor in P-A stage 1. Kennedy accepts or amends the refutation criterion before any product crate exists (§7.8 step 10). The candidate scoring is in \"), Link { href: \"../../evidence/CHG-005/bdr-candidates/\", children: [Code(\"evidence/CHG-005/bdr-candidates/\")] }, Text(\": three stub decompositions were run through \"), Code(\"cargo xtask architecture\"), Text(\". A is this plan's §3, B has fewer crates, C has more. All three pass the checker with zero errors. The choice therefore rests on the §7.2 evidence below, not on a rule violation.\")])] }, CodeBlock { lang: Some(\"text\"), text: \"Boundary: adapter-fs  (FsSources: library::SourceRepository; FsSink: site::OutputSink)\\nLevel:    crate\\n\\nHypothesis:\\n  Reading sources and writing output are one filesystem concern: shared path\\n  normalization, the same error vocabulary, and the same platform quirks.\\n\\nEvidence (data source):\\n  - Both ports need /-separated relative paths and NFC handling     (domain argument)\\n  - Contract suites exist per port (library::contract, site::contract), so each\\n    implementation is checked on its own                              (code structure)\\n\\nCounter-evidence:\\n  - The two halves fail independently: a read-only source tree with a writable\\n    output directory.\\n  - adapter.foreign_core does not fire, because it implements both owners' ports,\\n    but the adapter depends on two cores.\\n\\nExpected benefit:\\n  one adapter to wire in app-cli; shared path code\\n\\nExpected cost:\\n  a change to one port's implementation rebuilds and retests the other\\n\\nRefutation criterion (proposed; Kennedy decides at DP-1.3):\\n  metric:     share of adapter-fs changes that touch only one of src/source.rs and\\n              src/sink.rs and nothing shared\\n  source:     git history, qualifying = touches crates/adapter-fs/src\\n  window:     the next 20 qualifying changes, or 6 months, whichever is later\\n  threshold:  > 80 %, together with no change to shared path code\\n  action:     split into adapter-fs-source and adapter-fs-sink\\n\" }, Heading { level: 2, slug: \"candidates-considered-plan-24-rule-3\", children: [Text(\"Candidates considered (plan §2.4 rule 3)\")] }, Table { align: [None, None, None, None, None], head: [[Text(\"Candidate\")], [Text(\"Crates (cores)\")], [Text(\"Normal edges\")], [Text(\"Checker\")], [Text(\"Why not chosen\")]], rows: [[[Strong([Text(\"A\")]), Text(\", plan §3 (chosen)\")], [Text(\"8 (4)\")], [Text(\"18\")], [Text(\"0 errors, 1 warning (\"), Code(\"adapter.foreign_core\"), Text(\": \"), Code(\"adapter-html → document\"), Text(\")\")], [Text(\"–\")]], [[Text(\"B, \"), Code(\"library\"), Text(\" and \"), Code(\"document\"), Text(\" merged into \"), Code(\"content\")], [Text(\"7 (3)\")], [Text(\"14\")], [Text(\"0 errors, 1 warning\")], [Code(\"adapter-fs\"), Text(\" must depend on \"), Code(\"content\"), Text(\" to implement \"), Code(\"SourceRepository\"), Text(\", which puts the markdown parser in the dependency closure of an adapter that never parses (§7.2, \\\"dependencies worth keeping out of the parent\\\"). Page identity and syntax also have different invariants: unique ids versus unique slugs.\")]], [[Text(\"C, \"), Code(\"assembly\"), Text(\" and \"), Code(\"build\"), Text(\" as crates\")], [Text(\"10 (6)\")], [Text(\"28\")], [Text(\"0 errors, 2 warnings\")], [Text(\"Each of the two crates has exactly one consumer, \"), Code(\"site\"), Text(\", which is §7.2's \\\"one consumer and no enforcement benefit\\\" cost, and it adds 10 edges. It does buy enforced direction between them, which A lacks until P-C validates the module check. That is BDR-0003's refutation condition.\")]]] }]",
    "headings": "[Heading { level: 1, text: \"BDR-0004: adapter-fs implements two ports\", slug: \"bdr-0004-adapter-fs-implements-two-ports\", base_slug: \"bdr-0004-adapter-fs-implements-two-ports\", line: 1 }, Heading { level: 2, text: \"Candidates considered (plan §2.4 rule 3)\", slug: \"candidates-considered-plan-24-rule-3\", base_slug: \"candidates-considered-plan-24-rule-3\", line: 39 }]",
    "markdown": "# BDR-0004: `adapter-fs` implements two ports\n\n> **Status: accepted at DP-1.3** by Kennedy on 2026-09-22, criteria as written. Originally proposed: Drafted by the Executor in P-A stage 1. Kennedy accepts or amends the refutation criterion before any product crate exists (§7.8 step 10). The candidate scoring is in [`evidence/CHG-005/bdr-candidates/`](../../evidence/CHG-005/bdr-candidates/): three stub decompositions were run through `cargo xtask architecture`. A is this plan's §3, B has fewer crates, C has more. All three pass the checker with zero errors. The choice therefore rests on the §7.2 evidence below, not on a rule violation.\n\n```text\nBoundary: adapter-fs  (FsSources: library::SourceRepository; FsSink: site::OutputSink)\nLevel:    crate\n\nHypothesis:\n  Reading sources and writing output are one filesystem concern: shared path\n  normalization, the same error vocabulary, and the same platform quirks.\n\nEvidence (data source):\n  - Both ports need /-separated relative paths and NFC handling     (domain argument)\n  - Contract suites exist per port (library::contract, site::contract), so each\n    implementation is checked on its own                              (code structure)\n\nCounter-evidence:\n  - The two halves fail independently: a read-only source tree with a writable\n    output directory.\n  - adapter.foreign_core does not fire, because it implements both owners' ports,\n    but the adapter depends on two cores.\n\nExpected benefit:\n  one adapter to wire in app-cli; shared path code\n\nExpected cost:\n  a change to one port's implementation rebuilds and retests the other\n\nRefutation criterion (proposed; Kennedy decides at DP-1.3):\n  metric:     share of adapter-fs changes that touch only one of src/source.rs and\n              src/sink.rs and nothing shared\n  source:     git history, qualifying = touches crates/adapter-fs/src\n  window:     the next 20 qualifying changes, or 6 months, whichever is later\n  threshold:  > 80 %, together with no change to shared path code\n  action:     split into adapter-fs-source and adapter-fs-sink\n```\n\n## Candidates considered (plan §2.4 rule 3)\n\n| Candidate | Crates (cores) | Normal edges | Checker | Why not chosen |\n| --- | --- | --- | --- | --- |\n| **A**, plan §3 (chosen) | 8 (4) | 18 | 0 errors, 1 warning (`adapter.foreign_core`: `adapter-html → document`) | – |\n| B, `library` and `document` merged into `content` | 7 (3) | 14 | 0 errors, 1 warning | `adapter-fs` must depend on `content` to implement `SourceRepository`, which puts the markdown parser in the dependency closure of an adapter that never parses (§7.2, \"dependencies worth keeping out of the parent\"). Page identity and syntax also have different invariants: unique ids versus unique slugs. |\n| C, `assembly` and `build` as crates | 10 (6) | 28 | 0 errors, 2 warnings | Each of the two crates has exactly one consumer, `site`, which is §7.2's \"one consumer and no enforcement benefit\" cost, and it adds 10 edges. It does buy enforced direction between them, which A lacks until P-C validates the module check. That is BDR-0003's refutation condition. |\n"
  },
  "docs/adr/BDR-0005-no-std-graph-experiment.md": {
    "body": "[Heading { level: 1, slug: \"bdr-0005-no_std-graph-experiment-uses-product-types\", children: [Text(\"BDR-0005: \"), Code(\"no_std\"), Text(\" graph experiment uses product types\")] }, BlockQuote { kind: None, children: [Paragraph([Strong([Text(\"Status: E1 refutation criterion accepted\")]), Text(\" by agent:executor under human:kennedy's delegation. This records the experiment criterion, not P-C acceptance or an Accepted maturity change. See \"), Link { href: \"../../.rha/tasks/CHG-007-module-level.toml\", children: [Text(\"task decision e1-boundary\")] }, Text(\" for the complete authority quotes.\")])] }, CodeBlock { lang: Some(\"text\"), text: \"Boundary: experiments/no-std-graph  (standalone experiment package)\\nLevel:    crate\\n\\nHypotheses:\\n  H1: the own resolver compiles under #![no_std] after import adaptation\\n  H2: existing graph property/oracle tests pass unchanged with real types\\n  H3: an intentional direct-std probe fails to compile\\n\\nRefutation criterion (accepted for E1; task decision: e1-boundary):\\n  refuted if algorithm edits are needed, behavior controls differ, or the\\n  direct-std negative probe compiles\\n\" }, Paragraph([Text(\"The experiment is a standalone package under \"), Code(\"experiments/no-std-graph\"), Text(\", with\"), SoftBreak, Text(\"its own \"), Code(\"[workspace]\"), Text(\" root and \"), Code(\"[lib] name = \\\"graph\\\"\"), Text(\". It copies the current\"), SoftBreak, Text(\"graph resolver and existing graph property/oracle tests byte-for-byte, adapting\"), SoftBreak, Text(\"only \"), Code(\"no_std\"), Text(\"/\"), Code(\"alloc\"), Text(\" imports. It uses actual product document/library types,\"), SoftBreak, Text(\"preserving real behavior rather than introducing local model projections.\")]), Paragraph([Text(\"There is no new product consumer, root workspace/member edit, or production\"), SoftBreak, Text(\"edit. This BDR precedes creation of the experiment \"), Code(\"Cargo.toml\"), Text(\" under the\"), SoftBreak, Text(\"new-crate rule. The future result path is\"), SoftBreak, Code(\"experiments/no-std-graph/result.md\"), Text(\"; it does not contain an existing pass.\"), SoftBreak, Text(\"Actual commands and outcomes will be recorded later. \"), Code(\"no_std\"), Text(\" permits\"), SoftBreak, Text(\"allocation, and actual dependencies may retain \"), Code(\"std\"), Text(\"; this makes no embedded,\"), SoftBreak, Text(\"heap-free, panic-free, specification, or method-change claim.\")]), Heading { level: 2, slug: \"candidates-considered\", children: [Text(\"Candidates considered\")] }, Table { align: [None, Right, Right, Right, Right, Right, Right], head: [[Text(\"Candidate\")], [Text(\"Semantic fidelity\")], [Text(\"Transitive isolation\")], [Text(\"Scope fit\")], [Text(\"Effort\")], [Text(\"Oracle reuse\")], [Text(\"Total\")]], rows: [[[Strong([Text(\"Real product types snapshot (chosen)\")])], [Text(\"5\")], [Text(\"2\")], [Text(\"5\")], [Text(\"4\")], [Text(\"5\")], [Strong([Text(\"21/25\")])]], [[Text(\"Dependency-free minimal model projection\")], [Text(\"3\")], [Text(\"5\")], [Text(\"5\")], [Text(\"2\")], [Text(\"3\")], [Text(\"18/25\")]], [[Text(\"Convert production closure to \"), Code(\"no_std\")], [Text(\"5\")], [Text(\"5\")], [Text(\"1\")], [Text(\"1\")], [Text(\"5\")], [Text(\"17/25\")]]] }, Paragraph([Text(\"Scores are qualitative judgments, five criteria at five points each. The\"), SoftBreak, Text(\"chosen snapshot scores highest overall because it preserves real behavior while\"), SoftBreak, Text(\"remaining experimental. Stronger isolation changes the model and weakens the\"), SoftBreak, Text(\"E1 signal; production conversion exceeds experiment scope. Responsibility\"), SoftBreak, Text(\"remains with the same graph resolver; the snapshot is frozen; existing acyclic\"), SoftBreak, Text(\"direct dependency direction and the oracle remain unchanged. No new logical\"), SoftBreak, Text(\"product boundary is introduced.\")])]",
    "headings": "[Heading { level: 1, text: \"BDR-0005: no_std graph experiment uses product types\", slug: \"bdr-0005-no_std-graph-experiment-uses-product-types\", base_slug: \"bdr-0005-no_std-graph-experiment-uses-product-types\", line: 1 }, Heading { level: 2, text: \"Candidates considered\", slug: \"candidates-considered\", base_slug: \"candidates-considered\", line: 33 }]",
    "markdown": "# BDR-0005: `no_std` graph experiment uses product types\n\n> **Status: E1 refutation criterion accepted** by agent:executor under human:kennedy's delegation. This records the experiment criterion, not P-C acceptance or an Accepted maturity change. See [task decision e1-boundary](../../.rha/tasks/CHG-007-module-level.toml) for the complete authority quotes.\n\n```text\nBoundary: experiments/no-std-graph  (standalone experiment package)\nLevel:    crate\n\nHypotheses:\n  H1: the own resolver compiles under #![no_std] after import adaptation\n  H2: existing graph property/oracle tests pass unchanged with real types\n  H3: an intentional direct-std probe fails to compile\n\nRefutation criterion (accepted for E1; task decision: e1-boundary):\n  refuted if algorithm edits are needed, behavior controls differ, or the\n  direct-std negative probe compiles\n```\n\nThe experiment is a standalone package under `experiments/no-std-graph`, with\nits own `[workspace]` root and `[lib] name = \"graph\"`. It copies the current\ngraph resolver and existing graph property/oracle tests byte-for-byte, adapting\nonly `no_std`/`alloc` imports. It uses actual product document/library types,\npreserving real behavior rather than introducing local model projections.\n\nThere is no new product consumer, root workspace/member edit, or production\nedit. This BDR precedes creation of the experiment `Cargo.toml` under the\nnew-crate rule. The future result path is\n`experiments/no-std-graph/result.md`; it does not contain an existing pass.\nActual commands and outcomes will be recorded later. `no_std` permits\nallocation, and actual dependencies may retain `std`; this makes no embedded,\nheap-free, panic-free, specification, or method-change claim.\n\n## Candidates considered\n\n| Candidate | Semantic fidelity | Transitive isolation | Scope fit | Effort | Oracle reuse | Total |\n| --- | ---: | ---: | ---: | ---: | ---: | ---: |\n| **Real product types snapshot (chosen)** | 5 | 2 | 5 | 4 | 5 | **21/25** |\n| Dependency-free minimal model projection | 3 | 5 | 5 | 2 | 3 | 18/25 |\n| Convert production closure to `no_std` | 5 | 5 | 1 | 1 | 5 | 17/25 |\n\nScores are qualitative judgments, five criteria at five points each. The\nchosen snapshot scores highest overall because it preserves real behavior while\nremaining experimental. Stronger isolation changes the model and weakens the\nE1 signal; production conversion exceeds experiment scope. Responsibility\nremains with the same graph resolver; the snapshot is frozen; existing acyclic\ndirect dependency direction and the oracle remain unchanged. No new logical\nproduct boundary is introduced.\n"
  },
  "docs/adr/BDR-0006-adapter-json-immutable-index.md": {
    "body": "[Heading { level: 1, slug: \"bdr-0006-adapter-json-is-a-presentation-renderer-with-an-immutable-search-index\", children: [Text(\"BDR-0006: \"), Code(\"adapter-json\"), Text(\" is a presentation renderer with an immutable search index\")] }, BlockQuote { kind: None, children: [Paragraph([Strong([Text(\"Status: decided\")]), Text(\" by \"), Code(\"agent:executor\"), Text(\" under \"), Code(\"human:kennedy\"), Text(\"'s delegation\"), SoftBreak, Text(\"on 2026-09-23. This accepts the refutation criterion; it is not a PR or\"), SoftBreak, Text(\"artifact acceptance. No \"), Code(\"adapter-json\"), Text(\" crate exists yet.\")])] }, CodeBlock { lang: Some(\"text\"), text: \"Boundary: adapter-json  (JsonRenderer: site::PageRenderer)\\nLevel:    crate\\n\\nDecision:\\n  adapter-json owns presentation JSON and the immutable search-index asset.\\n  It depends normally only on site and serde_json 1.0.151. app-cli constructs\\n  it from the same corpus used by site::build_all.\\n\" }, Paragraph([Text(\"The governing task decision is \"), Code(\"json-adapter-boundary\"), Text(\" in\"), SoftBreak, Link { href: \"../../.rha/tasks/CHG-008-growth.toml\", children: [Text(\"CHG-008\")] }, Text(\". Its two instructions are\"), SoftBreak, Text(\"quoted in full:\")]), BlockQuote { kind: None, children: [Paragraph([Text(\"“Determine a large slice to hand off to gpt-6-astra ultra. Make sure it has\"), SoftBreak, Text(\"a goal to implement its work and fix any and all issue in accordance with\"), SoftBreak, Text(\"rha design. (Let it flag and improve the architecture and tooling if it can\"), SoftBreak, Text(\"justify it no need for my approval). I approve all PRs as long as both an\"), SoftBreak, Text(\"opus 5.5 and gpt-6 agent has reviewed and approved the code.”\")])] }, BlockQuote { kind: None, children: [Paragraph([Text(\"“Use recommended. Shift using codex to validate that the deterministic schema\"), SoftBreak, Text(\"is used according to rha guidelines rather than regenerating code as thats\"), SoftBreak, Text(\"the contract that should determine the qiality of the code. Reviee the PR\"), SoftBreak, Text(\"codex reviews. Accept the recommend and most correct implementations going\"), SoftBreak, Text(\"forward instead of escalating to me.”\")])] }, Heading { level: 2, slug: \"decision\", children: [Text(\"Decision\")] }, Paragraph([Code(\"JsonRenderer\"), Text(\" implements \"), Code(\"site::PageRenderer\"), Text(\". Its constructor is:\")]), CodeBlock { lang: Some(\"text\"), text: \"JsonRenderer::new(&[PageModel])\\n  -> Result<Self, JsonRendererError>\\n\" }, Paragraph([Text(\"The only constructor error is:\")]), CodeBlock { lang: Some(\"text\"), text: \"JsonRendererError::DuplicatePageId { id: String }\\n\" }, Paragraph([Text(\"The constructor rejects duplicate page IDs, copies the corpus-derived data\"), SoftBreak, Text(\"needed by the index, orders search entries by page ID, and precomputes\"), SoftBreak, Code(\"assets/search-index.json\"), Text(\". It does not mutate the caller's page-model slice.\"), SoftBreak, Text(\"Page and node vectors retain their input order; the search-index \"), Code(\"entries\"), SoftBreak, Text(\"array is the specified ID-sorted exception.\")]), Paragraph([Text(\"Each page renders to \"), Code(\"<id>.json\"), Text(\". \"), Code(\"assets()\"), Text(\" returns a stable clone of the\"), SoftBreak, Text(\"precomputed \"), Code(\"assets/search-index.json\"), Text(\" output on every call. Page IDs use the\"), SoftBreak, Text(\"same source-derived relative-path domain as the HTML renderer. A renderer\"), SoftBreak, Text(\"must be constructed again whenever the corpus changes and its input corpus\"), SoftBreak, Text(\"must be the corpus passed to the corresponding \"), Code(\"build_all\"), Text(\" call.\")]), Paragraph([Text(\"The adapter has no clock, filesystem, parser, graph, or core-production\"), SoftBreak, Text(\"responsibility. It uses normal dependencies only:\")]), CodeBlock { lang: Some(\"text\"), text: \"site\\nserde_json = \\\"1.0.151\\\"\\n\" }, Paragraph([Text(\"There is no core/API/allowlist change in this boundary decision. Adapters are\"), SoftBreak, Text(\"constructed only in \"), Code(\"app-cli\"), Text(\".\")]), Paragraph([Text(\"The fixed JSON contract is\"), SoftBreak, Link { href: \"../architecture/json-renderer-contract.md\", children: [Text(\"JSON Renderer Contract v1\")] }, Text(\".\"), SoftBreak, Code(\"build --format html|json\"), Text(\" selects the renderer, with HTML as the default.\"), SoftBreak, Code(\"check --format json\"), Text(\" keeps its existing v1 output contract unchanged.\")]), Heading { level: 2, slug: \"rationale\", children: [Text(\"Rationale\")] }, Table { align: [None, Right, Right, Right, Right, Right], head: [[Text(\"Candidate\")], [Text(\"Purity\")], [Text(\"Core isolation\")], [Text(\"Lifecycle\")], [Text(\"Simplicity\")], [Text(\"Total\")]], rows: [[[Strong([Text(\"Immutable JSON renderer and precomputed index (chosen)\")])], [Text(\"5\")], [Text(\"5\")], [Text(\"5\")], [Text(\"3\")], [Strong([Text(\"18/20\")])]], [[Text(\"Batch port returning pages and an index\")], [Text(\"5\")], [Text(\"1\")], [Text(\"5\")], [Text(\"5\")], [Text(\"16/20\")]], [[Text(\"Stateful renderer accumulating an index during \"), Code(\"render\")], [Text(\"1\")], [Text(\"5\")], [Text(\"1\")], [Text(\"1\")], [Text(\"8/20\")]]] }, Paragraph([Text(\"The chosen design gives a third renderer without changing core production\"), SoftBreak, Text(\"code. The cost is index memory and a second pure \"), Code(\"site::analyse\"), Text(\" plus\"), SoftBreak, Code(\"DefaultAssembler\"), Text(\" pass in \"), Code(\"app-cli\"), Text(\" before the existing \"), Code(\"build_all\"), Text(\" call:\"), SoftBreak, Text(\"duplicate orchestration of \"), Code(\"O(n+e)\"), Text(\" work. The second pass is accepted as the\"), SoftBreak, Text(\"small-scope lifecycle cost of keeping the renderer immutable and compatible\"), SoftBreak, Text(\"with the existing \"), Code(\"PageRenderer\"), Text(\" port.\")]), Paragraph([Text(\"The existing \"), Code(\"Inv_K\"), Text(\" check rejects a page/asset path collision before any\"), SoftBreak, Text(\"write is applied. In particular, a page whose source-derived output collides\"), SoftBreak, Text(\"with \"), Code(\"assets/search-index.json\"), Text(\" is a failed build with no writes, not a\"), SoftBreak, Text(\"renderer-specific overwrite rule.\")]), Heading { level: 2, slug: \"h3-and-assurance-scope\", children: [Text(\"H3 and assurance scope\")] }, Paragraph([Text(\"W8 requires the owner contract's three seeded violators to remain observable:\")]), List { start: Some(1), items: [[Text(\"a nondeterministic renderer;\")], [Text(\"a panicking renderer; and\")], [Text(\"a renderer that drops the TOC.\")]] }, Paragraph([Text(\"The sampled metamorphic TOC guard in \"), Code(\"site::contract::page_renderer\"), Text(\" is not a\"), SoftBreak, Text(\"semantic proof. \"), Code(\"adapter-json\"), Text(\" therefore requires an exact golden assertion\"), SoftBreak, Text(\"for its TOC projection, independent of that sampled guard.\")]), Paragraph([Text(\"Pre-existing assurance repairs are measured separately from the adapter\"), SoftBreak, Text(\"change. The change record must retain raw, uncombined outputs for:\")]), List { start: Some(1), items: [[Text(\"the assurance-repair range;\")], [Text(\"the adapter-stage range; and\")], [Text(\"the whole P-C-to-H3 core range:\")]] }, CodeBlock { lang: Some(\"text\"), text: \"git diff --numstat <before>..<after> -- \\\\\\n  crates/library crates/document crates/graph crates/site\\n\" }, Paragraph([Text(\"No counts, hashes, or results are asserted by this proposal.\")]), Heading { level: 2, slug: \"refutation-criterion\", children: [Text(\"Refutation criterion\")] }, Paragraph([Text(\"The criterion accepted under the delegation is:\")]), CodeBlock { lang: Some(\"text\"), text: \"metric:\\n  (a) W8 requires any core production lines solely to add adapter-json in\\n      crates/library, crates/document, crates/graph, or crates/site; or\\n  (b) more than 50% of the next 10 changes touching adapter-json also modify\\n      PageRenderer or PageModel\\n\\nsource:\\n  raw stage numstat and repository history, with qualifying adapter changes\\n  identified from their touched adapter and port/model files\\n\\nwindow:\\n  W8 plus the next 10 qualifying adapter changes, or three months,\\n  whichever is later\\n\\naction:\\n  propose a batch/finalize PageRenderer port, and record the zero-core-change\\n  half of H3 as refuted for this adapter class\\n\" }, Paragraph([Text(\"The criterion is a boundary-design measurement, not a requirement that a PR\"), SoftBreak, Text(\"or implementation artifact already exist.\")]), Heading { level: 2, slug: \"implementation-observation-and-cost-qualification-2026-09-23\", children: [Text(\"Implementation observation and cost qualification, 2026-09-23\")] }, Paragraph([Text(\"W8 is implemented through bd708fd3277afcb80a3898b6a498ea3c010e0e4c; the earlier statement that no crate existed records the pre-creation decision point. \"), Link { href: \"../observations/h3-json.md\", children: [Text(\"The H3 observation\")] }, Text(\" records the stage result and its separate prerequisite repairs.\")]), Paragraph([Text(\"The earlier O(n+e) description was an unsupported bound. \"), Link { href: \"../../evidence/CHG-008/stage-1/analysis-cost-inspection.txt\", children: [Text(\"Source inspection\")] }, Text(\" shows that assembly performs a vector scan for each link resolution, so a linear total-runtime claim is not warranted. The observed design cost is one additional pure analysis/assembly pass under the existing implementation, plus retained index memory. No performance benchmark or linear bound is claimed. Decision json-analysis-cost-qualification corrects this description; it does not change the accepted refutation criterion or any registered grade.\")])]",
    "headings": "[Heading { level: 1, text: \"BDR-0006: adapter-json is a presentation renderer with an immutable search index\", slug: \"bdr-0006-adapter-json-is-a-presentation-renderer-with-an-immutable-search-index\", base_slug: \"bdr-0006-adapter-json-is-a-presentation-renderer-with-an-immutable-search-index\", line: 1 }, Heading { level: 2, text: \"Decision\", slug: \"decision\", base_slug: \"decision\", line: 33 }, Heading { level: 2, text: \"Rationale\", slug: \"rationale\", base_slug: \"rationale\", line: 76 }, Heading { level: 2, text: \"H3 and assurance scope\", slug: \"h3-and-assurance-scope\", base_slug: \"h3-and-assurance-scope\", line: 96 }, Heading { level: 2, text: \"Refutation criterion\", slug: \"refutation-criterion\", base_slug: \"refutation-criterion\", line: 122 }, Heading { level: 2, text: \"Implementation observation and cost qualification, 2026-09-23\", slug: \"implementation-observation-and-cost-qualification-2026-09-23\", base_slug: \"implementation-observation-and-cost-qualification-2026-09-23\", line: 150 }]",
    "markdown": "# BDR-0006: `adapter-json` is a presentation renderer with an immutable search index\n\n> **Status: decided** by `agent:executor` under `human:kennedy`'s delegation\n> on 2026-09-23. This accepts the refutation criterion; it is not a PR or\n> artifact acceptance. No `adapter-json` crate exists yet.\n\n```text\nBoundary: adapter-json  (JsonRenderer: site::PageRenderer)\nLevel:    crate\n\nDecision:\n  adapter-json owns presentation JSON and the immutable search-index asset.\n  It depends normally only on site and serde_json 1.0.151. app-cli constructs\n  it from the same corpus used by site::build_all.\n```\n\nThe governing task decision is `json-adapter-boundary` in\n[CHG-008](../../.rha/tasks/CHG-008-growth.toml). Its two instructions are\nquoted in full:\n\n> “Determine a large slice to hand off to gpt-6-astra ultra. Make sure it has\n> a goal to implement its work and fix any and all issue in accordance with\n> rha design. (Let it flag and improve the architecture and tooling if it can\n> justify it no need for my approval). I approve all PRs as long as both an\n> opus 5.5 and gpt-6 agent has reviewed and approved the code.”\n\n> “Use recommended. Shift using codex to validate that the deterministic schema\n> is used according to rha guidelines rather than regenerating code as thats\n> the contract that should determine the qiality of the code. Reviee the PR\n> codex reviews. Accept the recommend and most correct implementations going\n> forward instead of escalating to me.”\n\n## Decision\n\n`JsonRenderer` implements `site::PageRenderer`. Its constructor is:\n\n```text\nJsonRenderer::new(&[PageModel])\n  -> Result<Self, JsonRendererError>\n```\n\nThe only constructor error is:\n\n```text\nJsonRendererError::DuplicatePageId { id: String }\n```\n\nThe constructor rejects duplicate page IDs, copies the corpus-derived data\nneeded by the index, orders search entries by page ID, and precomputes\n`assets/search-index.json`. It does not mutate the caller's page-model slice.\nPage and node vectors retain their input order; the search-index `entries`\narray is the specified ID-sorted exception.\n\nEach page renders to `<id>.json`. `assets()` returns a stable clone of the\nprecomputed `assets/search-index.json` output on every call. Page IDs use the\nsame source-derived relative-path domain as the HTML renderer. A renderer\nmust be constructed again whenever the corpus changes and its input corpus\nmust be the corpus passed to the corresponding `build_all` call.\n\nThe adapter has no clock, filesystem, parser, graph, or core-production\nresponsibility. It uses normal dependencies only:\n\n```text\nsite\nserde_json = \"1.0.151\"\n```\n\nThere is no core/API/allowlist change in this boundary decision. Adapters are\nconstructed only in `app-cli`.\n\nThe fixed JSON contract is\n[JSON Renderer Contract v1](../architecture/json-renderer-contract.md).\n`build --format html|json` selects the renderer, with HTML as the default.\n`check --format json` keeps its existing v1 output contract unchanged.\n\n## Rationale\n\n| Candidate | Purity | Core isolation | Lifecycle | Simplicity | Total |\n| --- | ---: | ---: | ---: | ---: | ---: |\n| **Immutable JSON renderer and precomputed index (chosen)** | 5 | 5 | 5 | 3 | **18/20** |\n| Batch port returning pages and an index | 5 | 1 | 5 | 5 | 16/20 |\n| Stateful renderer accumulating an index during `render` | 1 | 5 | 1 | 1 | 8/20 |\n\nThe chosen design gives a third renderer without changing core production\ncode. The cost is index memory and a second pure `site::analyse` plus\n`DefaultAssembler` pass in `app-cli` before the existing `build_all` call:\nduplicate orchestration of `O(n+e)` work. The second pass is accepted as the\nsmall-scope lifecycle cost of keeping the renderer immutable and compatible\nwith the existing `PageRenderer` port.\n\nThe existing `Inv_K` check rejects a page/asset path collision before any\nwrite is applied. In particular, a page whose source-derived output collides\nwith `assets/search-index.json` is a failed build with no writes, not a\nrenderer-specific overwrite rule.\n\n## H3 and assurance scope\n\nW8 requires the owner contract's three seeded violators to remain observable:\n\n1. a nondeterministic renderer;\n2. a panicking renderer; and\n3. a renderer that drops the TOC.\n\nThe sampled metamorphic TOC guard in `site::contract::page_renderer` is not a\nsemantic proof. `adapter-json` therefore requires an exact golden assertion\nfor its TOC projection, independent of that sampled guard.\n\nPre-existing assurance repairs are measured separately from the adapter\nchange. The change record must retain raw, uncombined outputs for:\n\n1. the assurance-repair range;\n2. the adapter-stage range; and\n3. the whole P-C-to-H3 core range:\n\n```text\ngit diff --numstat <before>..<after> -- \\\n  crates/library crates/document crates/graph crates/site\n```\n\nNo counts, hashes, or results are asserted by this proposal.\n\n## Refutation criterion\n\nThe criterion accepted under the delegation is:\n\n```text\nmetric:\n  (a) W8 requires any core production lines solely to add adapter-json in\n      crates/library, crates/document, crates/graph, or crates/site; or\n  (b) more than 50% of the next 10 changes touching adapter-json also modify\n      PageRenderer or PageModel\n\nsource:\n  raw stage numstat and repository history, with qualifying adapter changes\n  identified from their touched adapter and port/model files\n\nwindow:\n  W8 plus the next 10 qualifying adapter changes, or three months,\n  whichever is later\n\naction:\n  propose a batch/finalize PageRenderer port, and record the zero-core-change\n  half of H3 as refuted for this adapter class\n```\n\nThe criterion is a boundary-design measurement, not a requirement that a PR\nor implementation artifact already exist.\n\n\n## Implementation observation and cost qualification, 2026-09-23\n\nW8 is implemented through bd708fd3277afcb80a3898b6a498ea3c010e0e4c; the earlier statement that no crate existed records the pre-creation decision point. [The H3 observation](../observations/h3-json.md) records the stage result and its separate prerequisite repairs.\n\nThe earlier O(n+e) description was an unsupported bound. [Source inspection](../../evidence/CHG-008/stage-1/analysis-cost-inspection.txt) shows that assembly performs a vector scan for each link resolution, so a linear total-runtime claim is not warranted. The observed design cost is one additional pure analysis/assembly pass under the existing implementation, plus retained index memory. No performance benchmark or linear bound is claimed. Decision json-analysis-cost-qualification corrects this description; it does not change the accepted refutation criterion or any registered grade.\n"
  },
  "docs/adr/BDR-0007-site-owns-page-tree.md": {
    "body": "[Heading { level: 1, slug: \"bdr-0007-siteassembly-owns-the-renderable-page-tree\", children: [Text(\"BDR-0007: \"), Code(\"site::assembly\"), Text(\" owns the renderable page tree\")] }, BlockQuote { kind: None, children: [Paragraph([Strong([Text(\"Status: decided\")]), Text(\" by \"), Code(\"agent:executor\"), Text(\" under \"), Code(\"human:kennedy\"), Text(\"'s delegation\"), SoftBreak, Text(\"on 2026-09-23. This accepts the refutation criterion; it is not a PR or\"), SoftBreak, Text(\"artifact acceptance. No new crate is proposed.\")])] }, CodeBlock { lang: Some(\"text\"), text: \"Boundary: site::assembly  (PageNode and transclusion expansion)\\nLevel:    module facade within site\\n\\nDecision:\\n  site::assembly owns PageNode, using the existing renderable variants and\\n  fields of document::Node. site exports PageNode as site::Node. PageModel.body\\n  is Vec<PageNode>. document owns parser syntax, including Transclusion, but\\n  no parser-only variant reaches a renderer.\\n\" }, Paragraph([Text(\"The current public model has \"), Code(\"document::Document\"), Text(\" with \"), Code(\"body: Vec<Node>\"), Text(\",\"), SoftBreak, Text(\"navigation-only \"), Code(\"links\"), Text(\", headings, and diagnostics. \"), Code(\"site::assembly\"), Text(\" currently\"), SoftBreak, Text(\"projects that document into \"), Code(\"PageModel\"), Text(\", whose body is consumed by the\"), SoftBreak, Code(\"PageRenderer\"), Text(\" port. \"), Code(\"graph\"), Text(\" owns \"), Code(\"Resolved\"), Text(\", navigation witnesses, backlinks,\"), SoftBreak, Text(\"and resolution lookup. This decision keeps the renderer-facing source shape\"), SoftBreak, Text(\"stable while moving the final semantic boundary to \"), Code(\"site::assembly\"), Text(\".\")]), Heading { level: 2, slug: \"delegation\", children: [Text(\"Delegation\")] }, Paragraph([Text(\"The task instructions are quoted in full:\")]), BlockQuote { kind: None, children: [Paragraph([Text(\"“Determine a large slice to hand off to gpt-6-astra ultra. Make sure it has\"), SoftBreak, Text(\"a goal to implement its work and fix any and all issue in accordance with\"), SoftBreak, Text(\"rha design. (Let it flag and improve the architecture and tooling if it can\"), SoftBreak, Text(\"justify it no need for my approval). I approve all PRs as long as both an\"), SoftBreak, Text(\"opus 5.5 and gpt-6 agent has reviewed and approved the code.”\")])] }, BlockQuote { kind: None, children: [Paragraph([Text(\"“Use recommended. Shift using codex to validate that the deterministic schema\"), SoftBreak, Text(\"is used according to rha guidelines rather than regenerating code as thats\"), SoftBreak, Text(\"the contract that should determine the qiality of the code. Reviee the PR\"), SoftBreak, Text(\"codex reviews. Accept the recommend and most correct implementations going\"), SoftBreak, Text(\"forward instead of escalating to me.”\")])] }, Paragraph([Text(\"Only the refutation criterion below is accepted under that delegation. This\"), SoftBreak, Text(\"document does not accept a PR, claim implementation evidence, or change the\"), SoftBreak, Text(\"method/spec authority.\")]), Heading { level: 2, slug: \"owned-page-vocabulary\", children: [Text(\"Owned page vocabulary\")] }, Paragraph([Code(\"PageNode\"), Text(\" is defined at the \"), Code(\"site::assembly\"), Text(\" facade and retains the existing\"), SoftBreak, Text(\"renderable variants and fields:\")]), CodeBlock { lang: Some(\"text\"), text: \"Heading { level, slug, children }\\nParagraph(children)\\nText(text)\\nCode(text)\\nCodeBlock { lang, text }\\nEmphasis(children)\\nStrong(children)\\nStrikethrough(children)\\nLink { href, children }\\nWikiLink { index, children }\\nImage { src, alt }\\nList { start, items }\\nBlockQuote { kind, children }\\nTable { align, head, rows }\\nRule\\nSoftBreak\\nHardBreak\\nHtml(text)\\nTaskMarker(checked)\\n\" }, Paragraph([Code(\"Align\"), Text(\" and \"), Code(\"CalloutKind\"), Text(\" may remain shared semantic enums. The crate root\"), SoftBreak, Text(\"exports \"), Code(\"PageNode\"), Text(\" as \"), Code(\"site::Node\"), Text(\", preserving the existing renderer import\"), SoftBreak, Text(\"shape. \"), Code(\"PageModel.body\"), Text(\" is \"), Code(\"Vec<PageNode>\"), Text(\". \"), Code(\"PageNode\"), Text(\" has no \"), Code(\"Transclusion\"), SoftBreak, Text(\"variant.\")]), Paragraph([Code(\"document::Node\"), Text(\" remains the parser-owned tree and gains the owned\"), SoftBreak, Code(\"Transclusion { id, target, anchor, display, line }\"), Text(\" representation described\"), SoftBreak, Text(\"by the transclusion contract. Assembly alone converts parser syntax and\"), SoftBreak, Text(\"removes transclusions. There is no blanket \"), Code(\"From<document::Node>\"), Text(\":\"), SoftBreak, Text(\"conversion depends on origin and host pages, graph results, occurrence identity,\"), SoftBreak, Text(\"heading allocation, and expansion context.\")]), Paragraph([Text(\"The existing HTML and JSON production renderers remain unchanged. Their setup\"), SoftBreak, Text(\"tests may need the explicit document-slice argument added to \"), Code(\"Assemble\"), Text(\"; those\"), SoftBreak, Text(\"call-site migrations are recorded separately and are not hidden as renderer\"), SoftBreak, Text(\"changes. \"), Code(\"site::build\"), Text(\" passes its existing complete document slice downward;\"), SoftBreak, Text(\"assembly never reaches build or glue.\")]), Heading { level: 2, slug: \"alternatives\", children: [Text(\"Alternatives\")] }, Paragraph([Text(\"Scores are out of five for ownership, syntax cannot leak, migration\"), SoftBreak, Text(\"compatibility, W9 extensibility, and diagnosis respectively.\")]), Table { align: [None, Right, Right, Right, Right, Right, Right], head: [[Text(\"Alternative\")], [Text(\"Ownership\")], [Text(\"Syntax cannot leak\")], [Text(\"Migration\")], [Text(\"W9 extensibility\")], [Text(\"Diagnosis\")], [Text(\"Total\")]], rows: [[[Text(\"Shared document AST\")], [Text(\"2\")], [Text(\"1\")], [Text(\"5\")], [Text(\"2\")], [Text(\"3\")], [Text(\"13/25\")]], [[Strong([Text(\"Owned \"), Code(\"PageNode\")])], [Strong([Text(\"5\")])], [Strong([Text(\"5\")])], [Strong([Text(\"4\")])], [Strong([Text(\"5\")])], [Strong([Text(\"5\")])], [Strong([Text(\"24/25\")])]], [[Text(\"Flat render-event stream\")], [Text(\"4\")], [Text(\"5\")], [Text(\"1\")], [Text(\"4\")], [Text(\"2\")], [Text(\"16/25\")]]] }, Paragraph([Text(\"Sharing the document AST is cheap to migrate and avoids duplicate vocabulary,\"), SoftBreak, Text(\"but it makes parser semantics part of the renderer contract: a transclusion,\"), SoftBreak, Text(\"section-reference, or later parser-only node must be understood by every\"), SoftBreak, Text(\"consumer. It also leaves origin-aware conversion and diagnostics without a\"), SoftBreak, Text(\"clear owner.\")]), Paragraph([Text(\"An owned page tree duplicates the vocabulary at the semantic boundary and\"), SoftBreak, Text(\"requires explicit conversion. That cost is genuine: conversion must preserve\"), SoftBreak, Text(\"renderer fields while resolving links, importing headings, and rebasing paths.\"), SoftBreak, Text(\"It buys a stable renderer model, keeps parser syntax private, gives assembly a\"), SoftBreak, Text(\"place to diagnose expansion failures, and leaves later W9 features additive.\")]), Paragraph([Text(\"A flat render-event stream isolates syntax well, but its event balancing and\"), SoftBreak, Text(\"rewrites make existing renderer migration substantially larger. Nested imports,\"), SoftBreak, Text(\"heading allocation, link rewriting, and partial section selection would need\"), SoftBreak, Text(\"event buffering or repair passes, weakening diagnosis and source ownership.\")]), Heading { level: 2, slug: \"refutation-criterion\", children: [Text(\"Refutation criterion\")] }, Paragraph([Text(\"The accepted criterion is:\")]), CodeBlock { lang: Some(\"text\"), text: \"metric:\\n  During the transclusion and section-reference stages, renderer production\\n  files require zero edits solely because of new parser-only semantic variants.\\n\\nsource:\\n  Exact source-path diffs for those stages, with every renderer-file change\\n  classified as solely parser-variant-driven or as a justified exception.\\n\\naction:\\n  Reopen the site::assembly ownership boundary if the zero-edit condition is\\n  violated without a justified exception.\\n\" }, Paragraph([Text(\"This does not prohibit later presentation capabilities such as citation\"), SoftBreak, Text(\"anchors. No implementation result, hash, count, or PR acceptance is asserted.\")])]",
    "headings": "[Heading { level: 1, text: \"BDR-0007: site::assembly owns the renderable page tree\", slug: \"bdr-0007-siteassembly-owns-the-renderable-page-tree\", base_slug: \"bdr-0007-siteassembly-owns-the-renderable-page-tree\", line: 1 }, Heading { level: 2, text: \"Delegation\", slug: \"delegation\", base_slug: \"delegation\", line: 25 }, Heading { level: 2, text: \"Owned page vocabulary\", slug: \"owned-page-vocabulary\", base_slug: \"owned-page-vocabulary\", line: 45 }, Heading { level: 2, text: \"Alternatives\", slug: \"alternatives\", base_slug: \"alternatives\", line: 90 }, Heading { level: 2, text: \"Refutation criterion\", slug: \"refutation-criterion\", base_slug: \"refutation-criterion\", line: 118 }]",
    "markdown": "# BDR-0007: `site::assembly` owns the renderable page tree\n\n> **Status: decided** by `agent:executor` under `human:kennedy`'s delegation\n> on 2026-09-23. This accepts the refutation criterion; it is not a PR or\n> artifact acceptance. No new crate is proposed.\n\n```text\nBoundary: site::assembly  (PageNode and transclusion expansion)\nLevel:    module facade within site\n\nDecision:\n  site::assembly owns PageNode, using the existing renderable variants and\n  fields of document::Node. site exports PageNode as site::Node. PageModel.body\n  is Vec<PageNode>. document owns parser syntax, including Transclusion, but\n  no parser-only variant reaches a renderer.\n```\n\nThe current public model has `document::Document` with `body: Vec<Node>`,\nnavigation-only `links`, headings, and diagnostics. `site::assembly` currently\nprojects that document into `PageModel`, whose body is consumed by the\n`PageRenderer` port. `graph` owns `Resolved`, navigation witnesses, backlinks,\nand resolution lookup. This decision keeps the renderer-facing source shape\nstable while moving the final semantic boundary to `site::assembly`.\n\n## Delegation\n\nThe task instructions are quoted in full:\n\n> “Determine a large slice to hand off to gpt-6-astra ultra. Make sure it has\n> a goal to implement its work and fix any and all issue in accordance with\n> rha design. (Let it flag and improve the architecture and tooling if it can\n> justify it no need for my approval). I approve all PRs as long as both an\n> opus 5.5 and gpt-6 agent has reviewed and approved the code.”\n\n> “Use recommended. Shift using codex to validate that the deterministic schema\n> is used according to rha guidelines rather than regenerating code as thats\n> the contract that should determine the qiality of the code. Reviee the PR\n> codex reviews. Accept the recommend and most correct implementations going\n> forward instead of escalating to me.”\n\nOnly the refutation criterion below is accepted under that delegation. This\ndocument does not accept a PR, claim implementation evidence, or change the\nmethod/spec authority.\n\n## Owned page vocabulary\n\n`PageNode` is defined at the `site::assembly` facade and retains the existing\nrenderable variants and fields:\n\n```text\nHeading { level, slug, children }\nParagraph(children)\nText(text)\nCode(text)\nCodeBlock { lang, text }\nEmphasis(children)\nStrong(children)\nStrikethrough(children)\nLink { href, children }\nWikiLink { index, children }\nImage { src, alt }\nList { start, items }\nBlockQuote { kind, children }\nTable { align, head, rows }\nRule\nSoftBreak\nHardBreak\nHtml(text)\nTaskMarker(checked)\n```\n\n`Align` and `CalloutKind` may remain shared semantic enums. The crate root\nexports `PageNode` as `site::Node`, preserving the existing renderer import\nshape. `PageModel.body` is `Vec<PageNode>`. `PageNode` has no `Transclusion`\nvariant.\n\n`document::Node` remains the parser-owned tree and gains the owned\n`Transclusion { id, target, anchor, display, line }` representation described\nby the transclusion contract. Assembly alone converts parser syntax and\nremoves transclusions. There is no blanket `From<document::Node>`:\nconversion depends on origin and host pages, graph results, occurrence identity,\nheading allocation, and expansion context.\n\nThe existing HTML and JSON production renderers remain unchanged. Their setup\ntests may need the explicit document-slice argument added to `Assemble`; those\ncall-site migrations are recorded separately and are not hidden as renderer\nchanges. `site::build` passes its existing complete document slice downward;\nassembly never reaches build or glue.\n\n## Alternatives\n\nScores are out of five for ownership, syntax cannot leak, migration\ncompatibility, W9 extensibility, and diagnosis respectively.\n\n| Alternative | Ownership | Syntax cannot leak | Migration | W9 extensibility | Diagnosis | Total |\n| --- | ---: | ---: | ---: | ---: | ---: | ---: |\n| Shared document AST | 2 | 1 | 5 | 2 | 3 | 13/25 |\n| **Owned `PageNode`** | **5** | **5** | **4** | **5** | **5** | **24/25** |\n| Flat render-event stream | 4 | 5 | 1 | 4 | 2 | 16/25 |\n\nSharing the document AST is cheap to migrate and avoids duplicate vocabulary,\nbut it makes parser semantics part of the renderer contract: a transclusion,\nsection-reference, or later parser-only node must be understood by every\nconsumer. It also leaves origin-aware conversion and diagnostics without a\nclear owner.\n\nAn owned page tree duplicates the vocabulary at the semantic boundary and\nrequires explicit conversion. That cost is genuine: conversion must preserve\nrenderer fields while resolving links, importing headings, and rebasing paths.\nIt buys a stable renderer model, keeps parser syntax private, gives assembly a\nplace to diagnose expansion failures, and leaves later W9 features additive.\n\nA flat render-event stream isolates syntax well, but its event balancing and\nrewrites make existing renderer migration substantially larger. Nested imports,\nheading allocation, link rewriting, and partial section selection would need\nevent buffering or repair passes, weakening diagnosis and source ownership.\n\n## Refutation criterion\n\nThe accepted criterion is:\n\n```text\nmetric:\n  During the transclusion and section-reference stages, renderer production\n  files require zero edits solely because of new parser-only semantic variants.\n\nsource:\n  Exact source-path diffs for those stages, with every renderer-file change\n  classified as solely parser-variant-driven or as a justified exception.\n\naction:\n  Reopen the site::assembly ownership boundary if the zero-edit condition is\n  violated without a justified exception.\n```\n\nThis does not prohibit later presentation capabilities such as citation\nanchors. No implementation result, hash, count, or PR acceptance is asserted.\n\n"
  },
  "docs/architecture/components.md": {
    "body": "[Heading { level: 1, slug: \"components\", children: [Text(\"Components\")] }, Heading { level: 2, slug: \"at-this-revision\", children: [Text(\"At this revision\")] }, Paragraph([Text(\"The workspace has one member, \"), Code(\"xtask\"), Text(\", with role \"), Code(\"tool\"), Text(\": repository automation that runs the policy's lanes, writes evidence records, and generates docs. It is not a component of the product, and nothing may depend on it (rule \"), Code(\"dir.tool_depended_on\"), Text(\", CHG-003).\")]), Paragraph([Text(\"There are no core, adapter, or app crates yet.\")]), Heading { level: 2, slug: \"planned\", children: [Text(\"Planned\")] }, Paragraph([Text(\"The \"), Code(\"rhawiki\"), Text(\" components, their ports, and the crate dependency direction are designed in \"), Link { href: \"../plan/IMPLEMENTATION-PLAN.md\", children: [Text(\"the plan, §3.1\")] }, Text(\". They are created in CHG-005, after Kennedy writes boundary decision records BDR-0001 to BDR-0004 with refutation criteria (spec §7.8 step 10, DP-1.3). From CHG-005 on, this page owns the component table and the plan's copy becomes history.\")])]",
    "headings": "[Heading { level: 1, text: \"Components\", slug: \"components\", base_slug: \"components\", line: 1 }, Heading { level: 2, text: \"At this revision\", slug: \"at-this-revision\", base_slug: \"at-this-revision\", line: 3 }, Heading { level: 2, text: \"Planned\", slug: \"planned\", base_slug: \"planned\", line: 9 }]",
    "markdown": "# Components\n\n## At this revision\n\nThe workspace has one member, `xtask`, with role `tool`: repository automation that runs the policy's lanes, writes evidence records, and generates docs. It is not a component of the product, and nothing may depend on it (rule `dir.tool_depended_on`, CHG-003).\n\nThere are no core, adapter, or app crates yet.\n\n## Planned\n\nThe `rhawiki` components, their ports, and the crate dependency direction are designed in [the plan, §3.1](../plan/IMPLEMENTATION-PLAN.md). They are created in CHG-005, after Kennedy writes boundary decision records BDR-0001 to BDR-0004 with refutation criteria (spec §7.8 step 10, DP-1.3). From CHG-005 on, this page owns the component table and the plan's copy becomes history.\n"
  },
  "docs/architecture/json-renderer-contract.md": {
    "body": "[Heading { level: 1, slug: \"json-renderer-contract-v1\", children: [Text(\"JSON renderer contract v1\")] }, Paragraph([Text(\"This is the normative contract for the planned \"), Code(\"adapter-json\"), Text(\" crate. It is\"), SoftBreak, Text(\"written before implementation so an independent writer can register cases and\"), SoftBreak, Text(\"goldens from it. No implementation result is asserted here. Required state\"), SoftBreak, Text(\"tests are not passed by this proposal.\")]), Paragraph([Text(\"The adapter implements \"), Code(\"site::PageRenderer\"), Text(\", is constructed only by \"), Code(\"app-cli\"), Text(\",\"), SoftBreak, Text(\"and follows \"), Link { href: \"../adr/BDR-0006-adapter-json-immutable-index.md\", children: [Text(\"BDR-0006\")] }, Text(\".\")]), Heading { level: 2, slug: \"encoding-and-determinism\", children: [Text(\"Encoding and determinism\")] }, Paragraph([Text(\"Every JSON output is:\")]), List { start: None, items: [[Text(\"UTF-8;\")], [Text(\"deterministic for equal page-model inputs;\")], [Text(\"pretty-printed with \"), Code(\"\\\\n\"), Text(\" line endings;\")], [Text(\"terminated by exactly one trailing newline.\")]] }, Paragraph([Text(\"Object member order is not graded; decoded member names, types and values are\"), SoftBreak, Text(\"exact. Repeated outputs must have identical bytes.\")]), Paragraph([Text(\"Arrays preserve their input order unless this contract explicitly says\"), SoftBreak, Text(\"otherwise. JSON objects contain exactly the listed members; no implementation\"), SoftBreak, Text(\"metadata or HTML markup is added.\")]), Paragraph([Text(\"The page renderer is clock-independent except for the \"), Code(\"built_at\"), Text(\" value already\"), SoftBreak, Text(\"present in the supplied \"), Code(\"PageModel\"), Text(\". The search index never contains or\"), SoftBreak, Text(\"depends on \"), Code(\"built_at\"), Text(\".\")]), Heading { level: 2, slug: \"page-output\", children: [Text(\"Page output\")] }, Paragraph([Code(\"render(page)\"), Text(\" writes \"), Code(\"<id>.json\"), Text(\". Its root object is:\")]), CodeBlock { lang: Some(\"json\"), text: \"{\\n  \\\"schema_version\\\": 1,\\n  \\\"kind\\\": \\\"rhawiki_page\\\",\\n  \\\"id\\\": \\\"page-id\\\",\\n  \\\"title\\\": \\\"Title\\\",\\n  \\\"toc\\\": [\\n    {\\\"level\\\": 1, \\\"text\\\": \\\"Heading\\\", \\\"anchor\\\": \\\"heading\\\"}\\n  ],\\n  \\\"breadcrumbs\\\": [\\\"section\\\"],\\n  \\\"backlinks\\\": [\\n    {\\\"id\\\": \\\"other-page\\\", \\\"title\\\": \\\"Other page\\\"}\\n  ],\\n  \\\"links\\\": [\\n    {\\\"status\\\": \\\"resolved\\\", \\\"page\\\": \\\"target\\\", \\\"anchor\\\": \\\"section\\\"},\\n    {\\\"status\\\": \\\"unresolved\\\", \\\"page\\\": null, \\\"anchor\\\": null}\\n  ],\\n  \\\"body\\\": [],\\n  \\\"built_at\\\": null\\n}\\n\" }, Paragraph([Text(\"The \"), Code(\"built_at\"), Text(\" member is always present and is either a string or \"), Code(\"null\"), Text(\".\"), SoftBreak, Code(\"toc\"), Text(\", \"), Code(\"breadcrumbs\"), Text(\", \"), Code(\"backlinks\"), Text(\", \"), Code(\"links\"), Text(\", and \"), Code(\"body\"), Text(\" preserve the\"), SoftBreak, Text(\"corresponding \"), Code(\"PageModel\"), Text(\" vector order. A resolved link has a required string\"), SoftBreak, Code(\"page\"), Text(\" and a nullable string \"), Code(\"anchor\"), Text(\". An unresolved link has both \"), Code(\"page\"), Text(\" and\"), SoftBreak, Code(\"anchor\"), Text(\" set to \"), Code(\"null\"), Text(\".\")]), Paragraph([Text(\"The \"), Code(\"body\"), Text(\" array is an exhaustive projection of \"), Code(\"site::Node\"), Text(\":\")]), Table { align: [None, None], head: [[Text(\"Node\")], [Text(\"JSON shape\")]], rows: [[[Code(\"Heading\")], [Code(\"{\\\"type\\\":\\\"heading\\\",\\\"level\\\":u8,\\\"anchor\\\":String,\\\"children\\\":[Node]}\")]], [[Code(\"Paragraph\")], [Code(\"{\\\"type\\\":\\\"paragraph\\\",\\\"children\\\":[Node]}\")]], [[Code(\"Emphasis\")], [Code(\"{\\\"type\\\":\\\"emphasis\\\",\\\"children\\\":[Node]}\")]], [[Code(\"Strong\")], [Code(\"{\\\"type\\\":\\\"strong\\\",\\\"children\\\":[Node]}\")]], [[Code(\"Strikethrough\")], [Code(\"{\\\"type\\\":\\\"strikethrough\\\",\\\"children\\\":[Node]}\")]], [[Code(\"Text\")], [Code(\"{\\\"type\\\":\\\"text\\\",\\\"text\\\":String}\")]], [[Code(\"Code\")], [Code(\"{\\\"type\\\":\\\"code\\\",\\\"text\\\":String}\")]], [[Code(\"Html\")], [Code(\"{\\\"type\\\":\\\"html\\\",\\\"text\\\":String}\")]], [[Code(\"CodeBlock\")], [Code(\"{\\\"type\\\":\\\"code_block\\\",\\\"language\\\":null|String,\\\"text\\\":String}\")]], [[Code(\"Link\")], [Code(\"{\\\"type\\\":\\\"link\\\",\\\"href\\\":String,\\\"children\\\":[Node]}\")]], [[Code(\"WikiLink\")], [Code(\"{\\\"type\\\":\\\"wiki_link\\\",\\\"link_index\\\":usize,\\\"children\\\":[Node]}\")]], [[Code(\"Image\")], [Code(\"{\\\"type\\\":\\\"image\\\",\\\"src\\\":String,\\\"alt\\\":String}\")]], [[Code(\"List\")], [Code(\"{\\\"type\\\":\\\"list\\\",\\\"start\\\":null|u64,\\\"items\\\":[[Node]]}\")]], [[Code(\"BlockQuote\")], [Code(\"{\\\"type\\\":\\\"block_quote\\\",\\\"kind\\\":null|CalloutKind,\\\"children\\\":[Node]}\")]], [[Code(\"Table\")], [Code(\"{\\\"type\\\":\\\"table\\\",\\\"align\\\":[Alignment],\\\"head\\\":[[Node]],\\\"rows\\\":[[[Node]]]}\")]], [[Code(\"TaskMarker\")], [Code(\"{\\\"type\\\":\\\"task_marker\\\",\\\"checked\\\":bool}\")]], [[Code(\"Rule\")], [Code(\"{\\\"type\\\":\\\"rule\\\"}\")]], [[Code(\"SoftBreak\")], [Code(\"{\\\"type\\\":\\\"soft_break\\\"}\")]], [[Code(\"HardBreak\")], [Code(\"{\\\"type\\\":\\\"hard_break\\\"}\")]]] }, Paragraph([Code(\"CalloutKind\"), Text(\" is encoded as one of the lowercase strings \"), Code(\"note\"), Text(\", \"), Code(\"tip\"), Text(\",\"), SoftBreak, Code(\"important\"), Text(\", \"), Code(\"warning\"), Text(\", or \"), Code(\"caution\"), Text(\", or \"), Code(\"null\"), Text(\". Alignment is encoded as one of\"), SoftBreak, Code(\"none\"), Text(\", \"), Code(\"left\"), Text(\", \"), Code(\"center\"), Text(\", or \"), Code(\"right\"), Text(\". Table vector order and cell vector order\"), SoftBreak, Text(\"are preserved. \"), Code(\"wiki_link.link_index\"), Text(\" is the source node's link index; its\"), SoftBreak, Text(\"resolved target is represented separately in \"), Code(\"links\"), Text(\".\")]), Paragraph([Text(\"Raw HTML remains a JSON string under \"), Code(\"{\\\"type\\\":\\\"html\\\",\\\"text\\\":...}\"), Text(\". No HTML\"), SoftBreak, Text(\"rendering, escaping-to-markup, link rewriting, or generated callout label is\"), SoftBreak, Text(\"performed in JSON. Ordinary link \"), Code(\"href\"), Text(\" values are copied unchanged.\")]), Heading { level: 2, slug: \"search-index-asset\", children: [Text(\"Search-index asset\")] }, Paragraph([Code(\"assets()\"), Text(\" returns exactly one deterministic asset, \"), Code(\"assets/search-index.json\"), Text(\". Its\"), SoftBreak, Text(\"root object is:\")]), CodeBlock { lang: Some(\"json\"), text: \"{\\n  \\\"schema_version\\\": 1,\\n  \\\"kind\\\": \\\"rhawiki_search_index\\\",\\n  \\\"entries\\\": [\\n    {\\n      \\\"id\\\": \\\"page-id\\\",\\n      \\\"path\\\": \\\"page-id.json\\\",\\n      \\\"title\\\": \\\"Title\\\",\\n      \\\"headings\\\": [\\n        {\\\"level\\\": 1, \\\"text\\\": \\\"Heading\\\", \\\"anchor\\\": \\\"heading\\\"}\\n      ],\\n      \\\"text\\\": \\\"visible body text\\\"\\n    }\\n  ]\\n}\\n\" }, Paragraph([Text(\"Entries are sorted by \"), Code(\"id\"), Text(\" using the source page-ID ordering. Each \"), Code(\"path\"), Text(\" is\"), SoftBreak, Text(\"the page's relative JSON output path, \"), Code(\"<id>.json\"), Text(\". \"), Code(\"headings\"), Text(\" is the page's\"), SoftBreak, Text(\"TOC projection in its original order. The index excludes \"), Code(\"built_at\"), Text(\" and\"), SoftBreak, Code(\"backlinks\"), Text(\"; it contains no clock-derived data.\")]), Paragraph([Text(\"Search text is collected recursively from visible body content:\")]), List { start: None, items: [[Code(\"Text\"), Text(\", \"), Code(\"Code\"), Text(\", and \"), Code(\"Html\"), Text(\" append their text;\")], [Code(\"CodeBlock\"), Text(\" appends its \"), Code(\"text\"), Text(\" between whitespace boundaries; its language\"), SoftBreak, Text(\"does not contribute search text;\")], [Text(\"ordinary links and wikilinks append their child labels, not their targets;\")], [Text(\"images append \"), Code(\"alt\"), Text(\";\")], [Text(\"emphasis, strong, and strikethrough recurse without inserting spaces;\")], [Text(\"headings, paragraphs, code blocks, list items, quote sections, and table\"), SoftBreak, Text(\"cells add whitespace boundaries around their contents;\")], [Text(\"list, block-quote, and table nesting preserves the recursive order;\")], [Text(\"soft and hard breaks add a whitespace boundary;\")], [Text(\"rules add a whitespace boundary; and\")], [Text(\"task-marker state is ignored.\")]] }, Paragraph([Text(\"Adjacent inline fragments are concatenated without artificial spaces. A\"), SoftBreak, Text(\"boundary is an internal whitespace separator, not text visible in the final\"), SoftBreak, Text(\"value. After recursive collection, the result is normalized by\"), SoftBreak, Text(\"Rust's \"), Code(\"str::split_whitespace\"), Text(\" followed by joining with the ASCII space character. This\"), SoftBreak, Text(\"trims leading and trailing whitespace and normalizes Unicode whitespace.\"), SoftBreak, Text(\"Consequently, adjacent inline text/code/HTML/link-label/image-alt fragments\"), SoftBreak, Text(\"remain adjacent, while separate block content cannot run together.\")]), Heading { level: 2, slug: \"construction-and-build-lifecycle\", children: [Text(\"Construction and build lifecycle\")] }, Paragraph([Code(\"JsonRenderer::new(&[PageModel])\"), Text(\" rejects duplicate IDs with\"), SoftBreak, Code(\"DuplicatePageId { id }\"), Text(\", sorts the index entries by ID, and precomputes the\"), SoftBreak, Text(\"asset bytes. The supplied page models are not mutated. A new renderer is\"), SoftBreak, Text(\"required for every corpus change; reusing one after a page is added, removed,\"), SoftBreak, Text(\"or changed is invalid.\")]), Paragraph([Code(\"app-cli\"), Text(\" constructs the renderer from the same corpus that it passes to\"), SoftBreak, Code(\"site::build_all\"), Text(\". JSON mode performs the additional pure\"), SoftBreak, Code(\"site::analyse\"), Text(\"/\"), Code(\"DefaultAssembler\"), Text(\" pass needed to provide the constructor's\"), SoftBreak, Text(\"snapshot, then invokes the existing build pipeline. The existing \"), Code(\"Inv_K\"), SoftBreak, Text(\"check remains the owner of write validity. If a page output collides with\"), SoftBreak, Code(\"assets/search-index.json\"), Text(\", the build fails before applying any writes.\")]), Heading { level: 2, slug: \"cli-compatibility\", children: [Text(\"CLI compatibility\")] }, List { start: None, items: [[Code(\"rhawiki build --format html\"), Text(\" is the default and retains the existing HTML\"), SoftBreak, Text(\"renderer behavior.\")], [Code(\"rhawiki build --format json\"), Text(\" selects \"), Code(\"JsonRenderer\"), Text(\".\")], [Code(\"rhawiki check --format json\"), Text(\" retains its existing v1 witness schema and\"), SoftBreak, Text(\"semantics; this renderer contract does not change it.\")]] }, Heading { level: 2, slug: \"required-pre-implementation-cases\", children: [Text(\"Required pre-implementation cases\")] }, Paragraph([Text(\"Before implementation, an independent writer must register exact cases and\"), SoftBreak, Text(\"goldens covering:\")]), List { start: None, items: [[Text(\"every \"), Code(\"Node\"), Text(\" variant, including all callout kinds and all alignments;\")], [Text(\"nested nodes and every table/list shape;\")], [Text(\"preservation of page, TOC, backlink, link, table, row, and body ordering;\")], [Text(\"input permutations and constructor duplicate-ID rejection;\")], [Text(\"Unicode text and Unicode-whitespace normalization;\")], [Text(\"unchanged ordinary link \"), Code(\"href\"), Text(\" values and raw HTML-as-text behavior;\")], [Text(\"clock-independent search-index bytes;\")], [Text(\"a fresh index and deleted-page behavior across successive builds;\")], [Text(\"page/asset collision rejection with no writes;\")], [Text(\"the \"), Code(\"site::PageRenderer\"), Text(\" owner-contract controls;\")], [Text(\"exact JSON TOC golden semantics independent of the sampled metamorphic\"), SoftBreak, Text(\"TOC-drop guard;\")], [Code(\"build\"), Text(\" defaulting to HTML and selecting JSON explicitly; and\")], [Text(\"unchanged \"), Code(\"check --format json\"), Text(\" output compatibility.\")]] }, Paragraph([Text(\"Existing older corpora remain frozen and are not regenerated or regraded.\"), SoftBreak, Text(\"Required state tests are a future implementation gate and have not passed in\"), SoftBreak, Text(\"this contract-only proposal.\")])]",
    "headings": "[Heading { level: 1, text: \"JSON renderer contract v1\", slug: \"json-renderer-contract-v1\", base_slug: \"json-renderer-contract-v1\", line: 1 }, Heading { level: 2, text: \"Encoding and determinism\", slug: \"encoding-and-determinism\", base_slug: \"encoding-and-determinism\", line: 11 }, Heading { level: 2, text: \"Page output\", slug: \"page-output\", base_slug: \"page-output\", line: 31 }, Heading { level: 2, text: \"Search-index asset\", slug: \"search-index-asset\", base_slug: \"search-index-asset\", line: 97 }, Heading { level: 2, text: \"Construction and build lifecycle\", slug: \"construction-and-build-lifecycle\", base_slug: \"construction-and-build-lifecycle\", line: 148 }, Heading { level: 2, text: \"CLI compatibility\", slug: \"cli-compatibility\", base_slug: \"cli-compatibility\", line: 163 }, Heading { level: 2, text: \"Required pre-implementation cases\", slug: \"required-pre-implementation-cases\", base_slug: \"required-pre-implementation-cases\", line: 171 }]",
    "markdown": "# JSON renderer contract v1\n\nThis is the normative contract for the planned `adapter-json` crate. It is\nwritten before implementation so an independent writer can register cases and\ngoldens from it. No implementation result is asserted here. Required state\ntests are not passed by this proposal.\n\nThe adapter implements `site::PageRenderer`, is constructed only by `app-cli`,\nand follows [BDR-0006](../adr/BDR-0006-adapter-json-immutable-index.md).\n\n## Encoding and determinism\n\nEvery JSON output is:\n\n- UTF-8;\n- deterministic for equal page-model inputs;\n- pretty-printed with `\\n` line endings;\n- terminated by exactly one trailing newline.\n\nObject member order is not graded; decoded member names, types and values are\nexact. Repeated outputs must have identical bytes.\n\nArrays preserve their input order unless this contract explicitly says\notherwise. JSON objects contain exactly the listed members; no implementation\nmetadata or HTML markup is added.\n\nThe page renderer is clock-independent except for the `built_at` value already\npresent in the supplied `PageModel`. The search index never contains or\ndepends on `built_at`.\n\n## Page output\n\n`render(page)` writes `<id>.json`. Its root object is:\n\n```json\n{\n  \"schema_version\": 1,\n  \"kind\": \"rhawiki_page\",\n  \"id\": \"page-id\",\n  \"title\": \"Title\",\n  \"toc\": [\n    {\"level\": 1, \"text\": \"Heading\", \"anchor\": \"heading\"}\n  ],\n  \"breadcrumbs\": [\"section\"],\n  \"backlinks\": [\n    {\"id\": \"other-page\", \"title\": \"Other page\"}\n  ],\n  \"links\": [\n    {\"status\": \"resolved\", \"page\": \"target\", \"anchor\": \"section\"},\n    {\"status\": \"unresolved\", \"page\": null, \"anchor\": null}\n  ],\n  \"body\": [],\n  \"built_at\": null\n}\n```\n\nThe `built_at` member is always present and is either a string or `null`.\n`toc`, `breadcrumbs`, `backlinks`, `links`, and `body` preserve the\ncorresponding `PageModel` vector order. A resolved link has a required string\n`page` and a nullable string `anchor`. An unresolved link has both `page` and\n`anchor` set to `null`.\n\nThe `body` array is an exhaustive projection of `site::Node`:\n\n| Node | JSON shape |\n| --- | --- |\n| `Heading` | `{\"type\":\"heading\",\"level\":u8,\"anchor\":String,\"children\":[Node]}` |\n| `Paragraph` | `{\"type\":\"paragraph\",\"children\":[Node]}` |\n| `Emphasis` | `{\"type\":\"emphasis\",\"children\":[Node]}` |\n| `Strong` | `{\"type\":\"strong\",\"children\":[Node]}` |\n| `Strikethrough` | `{\"type\":\"strikethrough\",\"children\":[Node]}` |\n| `Text` | `{\"type\":\"text\",\"text\":String}` |\n| `Code` | `{\"type\":\"code\",\"text\":String}` |\n| `Html` | `{\"type\":\"html\",\"text\":String}` |\n| `CodeBlock` | `{\"type\":\"code_block\",\"language\":null\\|String,\"text\":String}` |\n| `Link` | `{\"type\":\"link\",\"href\":String,\"children\":[Node]}` |\n| `WikiLink` | `{\"type\":\"wiki_link\",\"link_index\":usize,\"children\":[Node]}` |\n| `Image` | `{\"type\":\"image\",\"src\":String,\"alt\":String}` |\n| `List` | `{\"type\":\"list\",\"start\":null\\|u64,\"items\":[[Node]]}` |\n| `BlockQuote` | `{\"type\":\"block_quote\",\"kind\":null\\|CalloutKind,\"children\":[Node]}` |\n| `Table` | `{\"type\":\"table\",\"align\":[Alignment],\"head\":[[Node]],\"rows\":[[[Node]]]}` |\n| `TaskMarker` | `{\"type\":\"task_marker\",\"checked\":bool}` |\n| `Rule` | `{\"type\":\"rule\"}` |\n| `SoftBreak` | `{\"type\":\"soft_break\"}` |\n| `HardBreak` | `{\"type\":\"hard_break\"}` |\n\n`CalloutKind` is encoded as one of the lowercase strings `note`, `tip`,\n`important`, `warning`, or `caution`, or `null`. Alignment is encoded as one of\n`none`, `left`, `center`, or `right`. Table vector order and cell vector order\nare preserved. `wiki_link.link_index` is the source node's link index; its\nresolved target is represented separately in `links`.\n\nRaw HTML remains a JSON string under `{\"type\":\"html\",\"text\":...}`. No HTML\nrendering, escaping-to-markup, link rewriting, or generated callout label is\nperformed in JSON. Ordinary link `href` values are copied unchanged.\n\n## Search-index asset\n\n`assets()` returns exactly one deterministic asset, `assets/search-index.json`. Its\nroot object is:\n\n```json\n{\n  \"schema_version\": 1,\n  \"kind\": \"rhawiki_search_index\",\n  \"entries\": [\n    {\n      \"id\": \"page-id\",\n      \"path\": \"page-id.json\",\n      \"title\": \"Title\",\n      \"headings\": [\n        {\"level\": 1, \"text\": \"Heading\", \"anchor\": \"heading\"}\n      ],\n      \"text\": \"visible body text\"\n    }\n  ]\n}\n```\n\nEntries are sorted by `id` using the source page-ID ordering. Each `path` is\nthe page's relative JSON output path, `<id>.json`. `headings` is the page's\nTOC projection in its original order. The index excludes `built_at` and\n`backlinks`; it contains no clock-derived data.\n\nSearch text is collected recursively from visible body content:\n\n- `Text`, `Code`, and `Html` append their text;\n- `CodeBlock` appends its `text` between whitespace boundaries; its language\n  does not contribute search text;\n- ordinary links and wikilinks append their child labels, not their targets;\n- images append `alt`;\n- emphasis, strong, and strikethrough recurse without inserting spaces;\n- headings, paragraphs, code blocks, list items, quote sections, and table\n  cells add whitespace boundaries around their contents;\n- list, block-quote, and table nesting preserves the recursive order;\n- soft and hard breaks add a whitespace boundary;\n- rules add a whitespace boundary; and\n- task-marker state is ignored.\n\nAdjacent inline fragments are concatenated without artificial spaces. A\nboundary is an internal whitespace separator, not text visible in the final\nvalue. After recursive collection, the result is normalized by\nRust's `str::split_whitespace` followed by joining with the ASCII space character. This\ntrims leading and trailing whitespace and normalizes Unicode whitespace.\nConsequently, adjacent inline text/code/HTML/link-label/image-alt fragments\nremain adjacent, while separate block content cannot run together.\n\n## Construction and build lifecycle\n\n`JsonRenderer::new(&[PageModel])` rejects duplicate IDs with\n`DuplicatePageId { id }`, sorts the index entries by ID, and precomputes the\nasset bytes. The supplied page models are not mutated. A new renderer is\nrequired for every corpus change; reusing one after a page is added, removed,\nor changed is invalid.\n\n`app-cli` constructs the renderer from the same corpus that it passes to\n`site::build_all`. JSON mode performs the additional pure\n`site::analyse`/`DefaultAssembler` pass needed to provide the constructor's\nsnapshot, then invokes the existing build pipeline. The existing `Inv_K`\ncheck remains the owner of write validity. If a page output collides with\n`assets/search-index.json`, the build fails before applying any writes.\n\n## CLI compatibility\n\n- `rhawiki build --format html` is the default and retains the existing HTML\n  renderer behavior.\n- `rhawiki build --format json` selects `JsonRenderer`.\n- `rhawiki check --format json` retains its existing v1 witness schema and\n  semantics; this renderer contract does not change it.\n\n## Required pre-implementation cases\n\nBefore implementation, an independent writer must register exact cases and\ngoldens covering:\n\n- every `Node` variant, including all callout kinds and all alignments;\n- nested nodes and every table/list shape;\n- preservation of page, TOC, backlink, link, table, row, and body ordering;\n- input permutations and constructor duplicate-ID rejection;\n- Unicode text and Unicode-whitespace normalization;\n- unchanged ordinary link `href` values and raw HTML-as-text behavior;\n- clock-independent search-index bytes;\n- a fresh index and deleted-page behavior across successive builds;\n- page/asset collision rejection with no writes;\n- the `site::PageRenderer` owner-contract controls;\n- exact JSON TOC golden semantics independent of the sampled metamorphic\n  TOC-drop guard;\n- `build` defaulting to HTML and selecting JSON explicitly; and\n- unchanged `check --format json` output compatibility.\n\nExisting older corpora remain frozen and are not regenerated or regraded.\nRequired state tests are a future implementation gate and have not passed in\nthis contract-only proposal.\n"
  },
  "docs/architecture/markdown-record-shape-amendment.md": {
    "body": "[Heading { level: 1, slug: \"markdown-corpus-record-shape-amendment\", children: [Text(\"Markdown corpus record shape amendment\")] }, Paragraph([Text(\"This is an after-data extension of the CHG-019.1 record schema contract, owned by ledger CHG-019.1-markdown-shape-amendment and the task decision markdown-shape-amendment. The original census, snapshots, 1,497 recipes and grading remain immutable.\")]), Paragraph([Text(\"The current producer's report evidence/md-corpus/20260923T044415Z-ed79a7a8e0ac.json contains two fields absent from the frozen census: cases\"), Text(\"[\"), Text(\"*\"), Text(\"]\"), Text(\".well_formed is boolean, and product.binary_sha256 is a string digest. These types are mechanically observed from that report. Both properties are optional so historical reports retain their registered shape. The enclosing objects remain closed to other keys, and existing digest semantics still apply.\")]), Paragraph([Text(\"Before schema projection code changes, a separate generator registers the new/old snapshots, observed additions, and controls for absent fields, both boolean values, wrong types, and unknown neighboring keys. This is not a claim that the shape preceded the current report data, and it does not change markdown corpus case grading. The original contract remains the base; this document and the separate supplement describe the extension.\")])]",
    "headings": "[Heading { level: 1, text: \"Markdown corpus record shape amendment\", slug: \"markdown-corpus-record-shape-amendment\", base_slug: \"markdown-corpus-record-shape-amendment\", line: 1 }]",
    "markdown": "# Markdown corpus record shape amendment\n\nThis is an after-data extension of the CHG-019.1 record schema contract, owned by ledger CHG-019.1-markdown-shape-amendment and the task decision markdown-shape-amendment. The original census, snapshots, 1,497 recipes and grading remain immutable.\n\nThe current producer's report evidence/md-corpus/20260923T044415Z-ed79a7a8e0ac.json contains two fields absent from the frozen census: cases[*].well_formed is boolean, and product.binary_sha256 is a string digest. These types are mechanically observed from that report. Both properties are optional so historical reports retain their registered shape. The enclosing objects remain closed to other keys, and existing digest semantics still apply.\n\nBefore schema projection code changes, a separate generator registers the new/old snapshots, observed additions, and controls for absent fields, both boolean values, wrong types, and unknown neighboring keys. This is not a claim that the shape preceded the current report data, and it does not change markdown corpus case grading. The original contract remains the base; this document and the separate supplement describe the extension.\n"
  },
  "docs/architecture/module-check-contract.md": {
    "body": "[Heading { level: 1, slug: \"module-checking-contract-p-c-before-extractor-code\", children: [Text(\"Module checking contract (P-C, before extractor code)\")] }, Paragraph([Text(\"The normative sources are spec §§3.3–3.5, 4.1, 6.13 and 9.10, plan W7, and the immutable module cases in \"), Code(\"xtask/tests/corpus/manifest.toml\"), Text(\". This document fixes representation and diagnostic choices, not new structural laws. Task decision \"), Code(\"module-diagnostics\"), Text(\" records why the cycle cases need a non-duplicative diagnostic.\")]), Heading { level: 2, slug: \"extraction-and-ownership\", children: [Text(\"Extraction and ownership\")] }, Paragraph([Text(\"Traverse legal Rust module sources, including inline modules, conventional files and \"), Code(\"#[path]\"), Text(\". Identify paths in imports (including nested/function imports), types, expressions and impl headers. Paths inside invocation arguments are heuristic and carry that flag. Do not expand macro definitions or \"), Code(\"include!\"), Text(\"; record those holes. Test-only edges are listed separately and do not enter the production graph. Unresolved internal paths are limitations, never an implicit proof of no dependency.\")]), Paragraph([Text(\"Resolve crate/self/super/uniform module names and local import bindings. Preserve the named facade: root \"), Code(\"pub use ordering::score\"), Text(\" followed by a child calling \"), Code(\"crate::score\"), Text(\" is an upward root reference, as M19 registers; do not replace it with the hidden ordering edge. External crate paths are outside this graph. Find module ownership by nearest declared component ancestor and classify item paths using the discovered module tree, not a fixed segment count. A facade type's associated item is not an internal module.\")]), Paragraph([Text(\"A declared composite's missing/malformed rules or source is a configuration/tool error with nonzero exit. A crate without a composite declaration has no module-check obligation; it must not receive a fabricated evaluated pass. The real site's declared check must actually run and pass.\")]), Heading { level: 2, slug: \"findings-and-deterministic-witnesses\", children: [Text(\"Findings and deterministic witnesses\")] }, List { start: None, items: [[Text(\"First classify containment. A child reference to ancestor glue/items or to an ancestor's sibling is D2 (\"), Code(\"modules.child_to_parent_private\"), Text(\"), even if public. The \"), Code(\"depth\"), Text(\" diagnostic is the number of canonical component-path segments, including the crate root as depth one; M05 \"), Code(\"x::ordering::scoring\"), Text(\" therefore has depth three. Such an edge is not a direct-sibling edge; \"), Code(\"[allow]\"), Text(\" cannot authorize it. Downward root/glue references and paths staying within one component are legitimate.\")], [Text(\"Direct-sibling edges form the complete observed component graph. Compute strongly connected components. Emit one \"), Code(\"modules.cycle\"), Text(\" per cyclic component set with a deterministic closed path: lexicographically least component start, then a lexicographically least shortest return path. Any heuristic edge in that witnessed cycle marks its extraction heuristic.\")], [Text(\"An undeclared edge inside that same cyclic set is retained as \"), Code(\"undeclared_edges\"), Text(\" and \"), Code(\"subsumed_rules\"), Text(\" on the cycle finding instead of another finding. This does not assert D4 holds; it avoids reporting the same cyclic structure twice. An undeclared edge outside a cyclic set remains \"), Code(\"modules.undeclared_dependency\"), Text(\".\")], [Text(\"A path below another component's facade is independently \"), Code(\"modules.foreign_internal\"), Text(\"; a cycle does not hide that separate consequence. The registered fixtures must isolate their intended violation under these rules; no expectation is silently changed.\")], [Text(\"Findings retain the exact top-level witness fields the manifest grades (\"), Code(\"rule\"), Text(\", \"), Code(\"from\"), Text(\", \"), Code(\"to\"), Text(\", closed \"), Code(\"path\"), Text(\", depth, heuristic \"), Code(\"extraction\"), Text(\" where applicable), plus source locations. Module findings enter the architecture report's top-level \"), Code(\"findings\"), Text(\" and its summary and exit status. Per-crate \"), Code(\"module_checks\"), Text(\" records evaluated outcomes and limits. M11's excluded test edge appears in top-level \"), Code(\"test_edges\"), Text(\" as a note.\")]] }, Paragraph([Text(\"The registered headline cases are the enforcement measure. Independent seeded graphs and their reference extractor supplement it. A missed seeded violation or unexplained alarm fails the run and downgrades the affected enforcement-map cell; no maturity proposal outruns the observed corpus.\")])]",
    "headings": "[Heading { level: 1, text: \"Module checking contract (P-C, before extractor code)\", slug: \"module-checking-contract-p-c-before-extractor-code\", base_slug: \"module-checking-contract-p-c-before-extractor-code\", line: 1 }, Heading { level: 2, text: \"Extraction and ownership\", slug: \"extraction-and-ownership\", base_slug: \"extraction-and-ownership\", line: 5 }, Heading { level: 2, text: \"Findings and deterministic witnesses\", slug: \"findings-and-deterministic-witnesses\", base_slug: \"findings-and-deterministic-witnesses\", line: 13 }]",
    "markdown": "# Module checking contract (P-C, before extractor code)\n\nThe normative sources are spec §§3.3–3.5, 4.1, 6.13 and 9.10, plan W7, and the immutable module cases in `xtask/tests/corpus/manifest.toml`. This document fixes representation and diagnostic choices, not new structural laws. Task decision `module-diagnostics` records why the cycle cases need a non-duplicative diagnostic.\n\n## Extraction and ownership\n\nTraverse legal Rust module sources, including inline modules, conventional files and `#[path]`. Identify paths in imports (including nested/function imports), types, expressions and impl headers. Paths inside invocation arguments are heuristic and carry that flag. Do not expand macro definitions or `include!`; record those holes. Test-only edges are listed separately and do not enter the production graph. Unresolved internal paths are limitations, never an implicit proof of no dependency.\n\nResolve crate/self/super/uniform module names and local import bindings. Preserve the named facade: root `pub use ordering::score` followed by a child calling `crate::score` is an upward root reference, as M19 registers; do not replace it with the hidden ordering edge. External crate paths are outside this graph. Find module ownership by nearest declared component ancestor and classify item paths using the discovered module tree, not a fixed segment count. A facade type's associated item is not an internal module.\n\nA declared composite's missing/malformed rules or source is a configuration/tool error with nonzero exit. A crate without a composite declaration has no module-check obligation; it must not receive a fabricated evaluated pass. The real site's declared check must actually run and pass.\n\n## Findings and deterministic witnesses\n\n- First classify containment. A child reference to ancestor glue/items or to an ancestor's sibling is D2 (`modules.child_to_parent_private`), even if public. The `depth` diagnostic is the number of canonical component-path segments, including the crate root as depth one; M05 `x::ordering::scoring` therefore has depth three. Such an edge is not a direct-sibling edge; `[allow]` cannot authorize it. Downward root/glue references and paths staying within one component are legitimate.\n- Direct-sibling edges form the complete observed component graph. Compute strongly connected components. Emit one `modules.cycle` per cyclic component set with a deterministic closed path: lexicographically least component start, then a lexicographically least shortest return path. Any heuristic edge in that witnessed cycle marks its extraction heuristic.\n- An undeclared edge inside that same cyclic set is retained as `undeclared_edges` and `subsumed_rules` on the cycle finding instead of another finding. This does not assert D4 holds; it avoids reporting the same cyclic structure twice. An undeclared edge outside a cyclic set remains `modules.undeclared_dependency`.\n- A path below another component's facade is independently `modules.foreign_internal`; a cycle does not hide that separate consequence. The registered fixtures must isolate their intended violation under these rules; no expectation is silently changed.\n- Findings retain the exact top-level witness fields the manifest grades (`rule`, `from`, `to`, closed `path`, depth, heuristic `extraction` where applicable), plus source locations. Module findings enter the architecture report's top-level `findings` and its summary and exit status. Per-crate `module_checks` records evaluated outcomes and limits. M11's excluded test edge appears in top-level `test_edges` as a note.\n\nThe registered headline cases are the enforcement measure. Independent seeded graphs and their reference extractor supplement it. A missed seeded violation or unexplained alarm fails the run and downgrades the affected enforcement-map cell; no maturity proposal outruns the observed corpus.\n"
  },
  "docs/architecture/module-test-source-boundary.md": {
    "body": "[Heading { level: 1, slug: \"test-source-boundary-for-workspace-module-checking\", children: [Text(\"Test source boundary for workspace module checking\")] }, Paragraph([Text(\"This additive checker contract is registered before its implementation. The original module-check contract and all M, L-M, EM-M and X-M01 expectations remain unchanged.\")]), Paragraph([Text(\"The ordinary extractor entry point and standalone module-check mode continue to accept module files only within the canonical crate root. Full-workspace checking may additionally supply the canonical Cargo workspace root as an explicit permitted test-source root.\")]), Paragraph([Text(\"Before reading or parsing any module file, canonicalize its path. Permit it exactly when it is within the crate root, or when its inherited module context is test-only and it is within the explicit permitted test-source root. An omitted test root grants no extra access. The canonical crate root must itself be inside the configured test root. The root module remains crate-confined. Production sources outside the crate remain refused. Symlink targets outside both permitted boundaries remain refused; a textual path prefix is insufficient.\")]), Paragraph([Text(\"Test classification remains inherited through nested modules. Only exact declaration ancestry \"), Code(\"#[cfg(test)]\"), Text(\" grants the wider boundary. An external file’s inner attribute cannot grant permission to read itself; non-exact conditions such as \"), Code(\"cfg(any(test, feature = \\\"x\\\"))\"), Text(\" and \"), Code(\"cfg_attr\"), Text(\" remain limitations and grant no wider read root. Logical Rust module names and nearest declared component ownership remain authoritative; sharing a physical helper file does not change ownership. Continue extracting and separately reporting test edges, including M11. Do not skip test modules or turn the shared helper into an opaque include macro.\")]), Paragraph([Text(\"The source-digest collector uses the same explicit read boundary and binds every actual traversed file, including a workspace-contained test helper outside the crate. Full-workspace architecture reports must therefore change the helper's source digest when its bytes change. No sibling project is an authorized source root for this task.\")]), Paragraph([Text(\"New independent controls must distinguish default refusal, explicit test-only allowance, production refusal, nested inherited test context, canonical symlink confinement, and complete helper digest provenance. Existing registered graphs retain their grades. The real site module check must run and pass after the repair.\")]), Paragraph([Text(\"Boundary diagnostics distinguish the failed authority: default or production module escapes contain \"), Code(\"outside crate root\"), Text(\"; an inherited test-only target beyond an explicitly configured test root contains \"), Code(\"outside permitted test source root\"), Text(\". A configured test root that excludes the crate itself reports the invalid crate-root relationship. These distinctions let the registered negatives reject an incorrect classification as well as a parse failure.\")])]",
    "headings": "[Heading { level: 1, text: \"Test source boundary for workspace module checking\", slug: \"test-source-boundary-for-workspace-module-checking\", base_slug: \"test-source-boundary-for-workspace-module-checking\", line: 1 }]",
    "markdown": "# Test source boundary for workspace module checking\n\nThis additive checker contract is registered before its implementation. The original module-check contract and all M, L-M, EM-M and X-M01 expectations remain unchanged.\n\nThe ordinary extractor entry point and standalone module-check mode continue to accept module files only within the canonical crate root. Full-workspace checking may additionally supply the canonical Cargo workspace root as an explicit permitted test-source root.\n\nBefore reading or parsing any module file, canonicalize its path. Permit it exactly when it is within the crate root, or when its inherited module context is test-only and it is within the explicit permitted test-source root. An omitted test root grants no extra access. The canonical crate root must itself be inside the configured test root. The root module remains crate-confined. Production sources outside the crate remain refused. Symlink targets outside both permitted boundaries remain refused; a textual path prefix is insufficient.\n\nTest classification remains inherited through nested modules. Only exact declaration ancestry `#[cfg(test)]` grants the wider boundary. An external file’s inner attribute cannot grant permission to read itself; non-exact conditions such as `cfg(any(test, feature = \"x\"))` and `cfg_attr` remain limitations and grant no wider read root. Logical Rust module names and nearest declared component ownership remain authoritative; sharing a physical helper file does not change ownership. Continue extracting and separately reporting test edges, including M11. Do not skip test modules or turn the shared helper into an opaque include macro.\n\nThe source-digest collector uses the same explicit read boundary and binds every actual traversed file, including a workspace-contained test helper outside the crate. Full-workspace architecture reports must therefore change the helper's source digest when its bytes change. No sibling project is an authorized source root for this task.\n\nNew independent controls must distinguish default refusal, explicit test-only allowance, production refusal, nested inherited test context, canonical symlink confinement, and complete helper digest provenance. Existing registered graphs retain their grades. The real site module check must run and pass after the repair.\n\nBoundary diagnostics distinguish the failed authority: default or production module escapes contain `outside crate root`; an inherited test-only target beyond an explicitly configured test root contains `outside permitted test source root`. A configured test root that excludes the crate itself reports the invalid crate-root relationship. These distinctions let the registered negatives reject an incorrect classification as well as a parse failure.\n"
  },
  "docs/architecture/record-schema-contract.md": {
    "body": "[Heading { level: 1, slug: \"record-schema-contract-chg-0191-p-c-stage-0\", children: [Text(\"Record schema contract (CHG-019.1, P-C stage 0)\")] }, Paragraph([Text(\"This fixes structural grading before JSON schemas or new lint implementation. It supplements \"), Link { href: \"verifier-contract.md\", children: [Text(\"the P-B contract\")] }, Text(\"; it does not regrade R001–R080 or rewrite historic records. Structural acceptance is not authenticity, eligibility, or acceptance.\")]), Heading { level: 2, slug: \"mechanical-derivation\", children: [Text(\"Mechanical derivation\")] }, Paragraph([Text(\"The source snapshot is \"), Code(\"99a74b042842b4d869ef341f58fdfcafb9c96e84\"), Text(\". Read committed \"), Code(\".rha/tasks/*.toml\"), Text(\", \"), Code(\".rha/acceptances/*.toml\"), Text(\", \"), Code(\".rha/policy.toml\"), Text(\", and \"), Code(\"evidence/**/*.json\"), Text(\" from that revision. JSON families are distinguished by \"), Code(\"record_kind = evidence\"), Text(\" or \"), Code(\"kind = h4\"), Text(\", \"), Code(\"markdown_corpus\"), Text(\", \"), Code(\"h5_conformance\"), Text(\". Other report kinds are outside this registration. The derivation inventory records every source path and SHA-256 and every observed object path, required key intersection, allowed key union and JSON type union. TOML converts to JSON first. Booleans are not integers. Integer and number are distinct observations; null is a type.\")]), Paragraph([Text(\"For each family and object path, keys present in every observed object are required; the union of observed keys is allowed. Pool array elements at a \"), Code(\"*\"), Text(\" path. Empty arrays supply no item observation; entirely unobserved items remain unconstrained and are disclosed, never guessed. Pool only objects at a path when null is another permitted type. Preserve discriminated variants: legacy direct \"), Code(\"agent_context\"), Text(\" versus sourced \"), Code(\"{source, provenance}\"), Text(\", and unsourced null \"), Code(\"change_claim.task/source\"), Text(\" versus a sourced claim with its declared intent fields. Record the variant rule in the inventory; do not accept a hybrid merely by unioning its keys. H5 corpus citations retain their historical string-or-object variants. Sourced \"), Code(\"agent_context.provenance\"), Text(\" preserves two branches: full provenance requires \"), Code(\"harness\"), Text(\", \"), Code(\"task_scope\"), Text(\", \"), Code(\"permissions\"), Text(\", \"), Code(\"budgets\"), Text(\", \"), Code(\"reasoning_effort\"), Text(\", with optional object-array instruction_sources; compact historical provenance requires exactly \"), Code(\"model_id\"), Text(\" and string-array \"), Code(\"instruction_sources\"), Text(\". Select full when any full required key is present, compact otherwise; an empty object is missing fields, not a new valid variant. H4 case fixture identity is exactly one of string \"), Code(\"fixture_path\"), Text(\" or an open string-valued \"), Code(\"fixture_inputs_sha256\"), Text(\" digest map: neither is \"), Code(\"schema.missing_field\"), Text(\", both is \"), Code(\"schema.invalid_value\"), Text(\". Schema versions are the exact observed versions per family (H4 historical versions included). Every L0 \"), Code(\"observed_checks[*].outcome\"), Text(\" and corpus \"), Code(\"summary.outcome\"), Text(\" is one of the five states in spec §11.4. Documentary fields such as historic H4 \"), Code(\"held_out.outcome = not_part_of_this_run\"), Text(\" retain their observed string type; they do not assert a new check outcome. Other strings are not enumerated from finite historical samples.\")]), Paragraph([Text(\"Known documentary extension tables are retained by that union. The allowed-key rule is closed for record objects at every observed path, except the explicit dynamic-map boundaries below. This extends the original \"), Code(\"schema.unknown_field\"), Text(\" top-level task/acceptance contract to nested record objects and every supported record family; no existing corpus case exercises unknown fields. New fields require registration before evaluation. Arbitrary semantic maps (\"), Code(\"params\"), Text(\", subject/input keyed dictionaries, policy strictness parameter names, and corpus embedded checker reports) cannot have requiredness inferred from their incidental map keys; inventory them, but validate them as typed objects with unconstrained keys. Array lists of checks, actors, work, decisions, evidence entries and corpus cases are records, not arbitrary maps.\")]), Paragraph([Text(\"An empty-object intersection must not erase the known element shape of a list: list elements are pooled records. The inventory is a conservative compatibility contract, not a claim that all historically optional fields are semantically optional. Future tightening needs an explicit contract revision and new registration before evaluation.\")]), Heading { level: 2, slug: \"explicit-extensions-and-diagnostics\", children: [Text(\"Explicit extensions and diagnostics\")] }, List { start: None, items: [[Code(\"observed_checks[*].outcome\"), Text(\" is required and has exactly \"), Code(\"passed\"), Text(\", \"), Code(\"failed\"), Text(\", \"), Code(\"not_run\"), Text(\", \"), Code(\"not_applicable\"), Text(\", \"), Code(\"inconclusive\"), Text(\"; missing yields \"), Code(\"schema.missing_field\"), Text(\", unsupported value \"), Code(\"schema.invalid_value\"), Text(\", wrong type \"), Code(\"schema.wrong_type\"), Text(\".\")], [Text(\"All JSON Schema required/type/additional-property/enum or const failures map respectively to \"), Code(\"schema.missing_field\"), Text(\", \"), Code(\"schema.wrong_type\"), Text(\", \"), Code(\"schema.unknown_field\"), Text(\", \"), Code(\"schema.invalid_value\"), Text(\". Parse errors are \"), Code(\"schema.wrong_type\"), Text(\". Reasons are a deduplicated set.\")], [Text(\"Structural and existing semantic reasons accumulate. No new schema digest/timestamp/revision regex is added: the P-B semantic codes own those checks, preserving the frozen corpus's exact expected reason sets.\")], [Text(\"L0 evidence permits optional \"), Code(\"policy_rule_id\"), Text(\" and \"), Code(\"rationale\"), Text(\" strings on an observed check, as already specified for \"), Code(\"not_applicable\"), Text(\". Nullable historic metadata stays nullable. A \"), Code(\"schema_version\"), Text(\" outside observed family versions is \"), Code(\"schema.invalid_value\"), Text(\".\")], [Text(\"Corpus reports have their own three registered families. They are never treated as L0 evidence requiring eight observed checks. Their required shape, summary outcome and per-case types are derived above. This shape check does not authenticate or rerun the corpus and must say so in its documentation.\")], [Text(\"Record kind is identified from structured discriminators, not a substring in a comment: JSON \"), Code(\"record_kind=evidence\"), Text(\" / supported \"), Code(\"kind\"), Text(\", TOML task \"), Code(\"id\"), Text(\" with \"), Code(\"mode\"), Text(\", acceptance \"), Code(\"acceptor\"), Text(\", policy \"), Code(\"authority\"), Text(\" with \"), Code(\"lanes\"), Text(\". Missing discriminator is \"), Code(\"schema.missing_field\"), Text(\"; an unsupported value is \"), Code(\"schema.invalid_value\"), Text(\"; a mistyped discriminator is \"), Code(\"schema.wrong_type\"), Text(\". For direct structural validation the declared fixture family selects its schema, so removing the discriminator cannot silently select another schema. CLI inference requires a supported discriminator before it may accept.\")], [Text(\"A cited \"), Code(\"{path, sha256}\"), Text(\" resolves beside the record first, then from git at its own snapshot/revision (acceptance \"), Code(\"subject.revision\"), Text(\" and corpus \"), Code(\"subject.revision\"), Text(\" included). An unresolved citation produces an explicit \"), Code(\"citation.unchecked\"), Text(\" note with the path and reason. Notes are not rejection codes and do not change R001–R080. Output with any unchecked citation must say \"), Code(\"accepted with unchecked citations\"), Text(\", never bare \"), Code(\"accepted\"), Text(\".\")]] }, Heading { level: 2, slug: \"pre-registration-corpus\", children: [Text(\"Pre-registration corpus\")] }, Paragraph([Text(\"A separate generator, forbidden to read the lint/schema implementation, derives the inventory and creates legitimate controls plus missing required fields, wrong types, unknown closed keys, unsupported versions/outcomes, absent/unknown evidence outcomes, policy malformed values, nested unknown fields, and all three corpus-report shapes. Expectations are fixed by these rules before the implementation exists. The registration pins sources, prompt, payload digest and exact case count. Existing 80 record fixtures remain unchanged and run alongside it.\")]), Paragraph([Text(\"The contract does not introduce an in-toto attestation channel, a trusted producer, or protected acceptance. Those remain DP-4.1's disclosed limit.\")]), Heading { level: 2, slug: \"variant-selection-and-exact-reason-sets\", children: [Text(\"Variant selection and exact reason sets\")] }, Paragraph([Text(\"Supplementary fixture family labels are \"), Code(\"task\"), Text(\", \"), Code(\"acceptance\"), Text(\", \"), Code(\"policy\"), Text(\", \"), Code(\"evidence\"), Text(\", \"), Code(\"h4\"), Text(\", \"), Code(\"markdown_corpus\"), Text(\", \"), Code(\"h5_conformance\"), Text(\". They name structural schemas, not claimed verdicts. Select the sourced agent-context branch when either \"), Code(\"source\"), Text(\" or \"), Code(\"provenance\"), Text(\" is present, otherwise select legacy direct context. Select sourced change-claim when \"), Code(\"intent\"), Text(\" is present or \"), Code(\"source\"), Text(\" is an object, otherwise the null-task/null-source/note branch. Do not union these branches into an empty required-key intersection. Full/compact provenance selection is specified above. Forbidden hybrid keys produce \"), Code(\"schema.unknown_field\"), Text(\"; missing selected-branch requirements produce \"), Code(\"schema.missing_field\"), Text(\"; mistyped members produce \"), Code(\"schema.wrong_type\"), Text(\". A branch-selection failure must not report every unrelated branch's errors.\")]), Paragraph([Text(\"The inventory fixes all exact dynamic-map JSON-pointer paths before schema implementation. These are explicit exceptions to record-key closure. Require string values for path-to-digest maps. Corpus embedded checker-report and witness payload objects are separately owned output vocabularies: preserve object/nullable types at that boundary but do not freeze future checker fields from a historical run. Missing owned corpus envelope and case fields remain errors. Unknown nested fields outside those declared maps remain errors.\")])]",
    "headings": "[Heading { level: 1, text: \"Record schema contract (CHG-019.1, P-C stage 0)\", slug: \"record-schema-contract-chg-0191-p-c-stage-0\", base_slug: \"record-schema-contract-chg-0191-p-c-stage-0\", line: 1 }, Heading { level: 2, text: \"Mechanical derivation\", slug: \"mechanical-derivation\", base_slug: \"mechanical-derivation\", line: 5 }, Heading { level: 2, text: \"Explicit extensions and diagnostics\", slug: \"explicit-extensions-and-diagnostics\", base_slug: \"explicit-extensions-and-diagnostics\", line: 15 }, Heading { level: 2, text: \"Pre-registration corpus\", slug: \"pre-registration-corpus\", base_slug: \"pre-registration-corpus\", line: 25 }, Heading { level: 2, text: \"Variant selection and exact reason sets\", slug: \"variant-selection-and-exact-reason-sets\", base_slug: \"variant-selection-and-exact-reason-sets\", line: 32 }]",
    "markdown": "# Record schema contract (CHG-019.1, P-C stage 0)\n\nThis fixes structural grading before JSON schemas or new lint implementation. It supplements [the P-B contract](verifier-contract.md); it does not regrade R001–R080 or rewrite historic records. Structural acceptance is not authenticity, eligibility, or acceptance.\n\n## Mechanical derivation\n\nThe source snapshot is `99a74b042842b4d869ef341f58fdfcafb9c96e84`. Read committed `.rha/tasks/*.toml`, `.rha/acceptances/*.toml`, `.rha/policy.toml`, and `evidence/**/*.json` from that revision. JSON families are distinguished by `record_kind = evidence` or `kind = h4`, `markdown_corpus`, `h5_conformance`. Other report kinds are outside this registration. The derivation inventory records every source path and SHA-256 and every observed object path, required key intersection, allowed key union and JSON type union. TOML converts to JSON first. Booleans are not integers. Integer and number are distinct observations; null is a type.\n\nFor each family and object path, keys present in every observed object are required; the union of observed keys is allowed. Pool array elements at a `*` path. Empty arrays supply no item observation; entirely unobserved items remain unconstrained and are disclosed, never guessed. Pool only objects at a path when null is another permitted type. Preserve discriminated variants: legacy direct `agent_context` versus sourced `{source, provenance}`, and unsourced null `change_claim.task/source` versus a sourced claim with its declared intent fields. Record the variant rule in the inventory; do not accept a hybrid merely by unioning its keys. H5 corpus citations retain their historical string-or-object variants. Sourced `agent_context.provenance` preserves two branches: full provenance requires `harness`, `task_scope`, `permissions`, `budgets`, `reasoning_effort`, with optional object-array instruction_sources; compact historical provenance requires exactly `model_id` and string-array `instruction_sources`. Select full when any full required key is present, compact otherwise; an empty object is missing fields, not a new valid variant. H4 case fixture identity is exactly one of string `fixture_path` or an open string-valued `fixture_inputs_sha256` digest map: neither is `schema.missing_field`, both is `schema.invalid_value`. Schema versions are the exact observed versions per family (H4 historical versions included). Every L0 `observed_checks[*].outcome` and corpus `summary.outcome` is one of the five states in spec §11.4. Documentary fields such as historic H4 `held_out.outcome = not_part_of_this_run` retain their observed string type; they do not assert a new check outcome. Other strings are not enumerated from finite historical samples.\n\nKnown documentary extension tables are retained by that union. The allowed-key rule is closed for record objects at every observed path, except the explicit dynamic-map boundaries below. This extends the original `schema.unknown_field` top-level task/acceptance contract to nested record objects and every supported record family; no existing corpus case exercises unknown fields. New fields require registration before evaluation. Arbitrary semantic maps (`params`, subject/input keyed dictionaries, policy strictness parameter names, and corpus embedded checker reports) cannot have requiredness inferred from their incidental map keys; inventory them, but validate them as typed objects with unconstrained keys. Array lists of checks, actors, work, decisions, evidence entries and corpus cases are records, not arbitrary maps.\n\nAn empty-object intersection must not erase the known element shape of a list: list elements are pooled records. The inventory is a conservative compatibility contract, not a claim that all historically optional fields are semantically optional. Future tightening needs an explicit contract revision and new registration before evaluation.\n\n## Explicit extensions and diagnostics\n\n- `observed_checks[*].outcome` is required and has exactly `passed`, `failed`, `not_run`, `not_applicable`, `inconclusive`; missing yields `schema.missing_field`, unsupported value `schema.invalid_value`, wrong type `schema.wrong_type`.\n- All JSON Schema required/type/additional-property/enum or const failures map respectively to `schema.missing_field`, `schema.wrong_type`, `schema.unknown_field`, `schema.invalid_value`. Parse errors are `schema.wrong_type`. Reasons are a deduplicated set.\n- Structural and existing semantic reasons accumulate. No new schema digest/timestamp/revision regex is added: the P-B semantic codes own those checks, preserving the frozen corpus's exact expected reason sets.\n- L0 evidence permits optional `policy_rule_id` and `rationale` strings on an observed check, as already specified for `not_applicable`. Nullable historic metadata stays nullable. A `schema_version` outside observed family versions is `schema.invalid_value`.\n- Corpus reports have their own three registered families. They are never treated as L0 evidence requiring eight observed checks. Their required shape, summary outcome and per-case types are derived above. This shape check does not authenticate or rerun the corpus and must say so in its documentation.\n- Record kind is identified from structured discriminators, not a substring in a comment: JSON `record_kind=evidence` / supported `kind`, TOML task `id` with `mode`, acceptance `acceptor`, policy `authority` with `lanes`. Missing discriminator is `schema.missing_field`; an unsupported value is `schema.invalid_value`; a mistyped discriminator is `schema.wrong_type`. For direct structural validation the declared fixture family selects its schema, so removing the discriminator cannot silently select another schema. CLI inference requires a supported discriminator before it may accept.\n- A cited `{path, sha256}` resolves beside the record first, then from git at its own snapshot/revision (acceptance `subject.revision` and corpus `subject.revision` included). An unresolved citation produces an explicit `citation.unchecked` note with the path and reason. Notes are not rejection codes and do not change R001–R080. Output with any unchecked citation must say `accepted with unchecked citations`, never bare `accepted`.\n\n## Pre-registration corpus\n\nA separate generator, forbidden to read the lint/schema implementation, derives the inventory and creates legitimate controls plus missing required fields, wrong types, unknown closed keys, unsupported versions/outcomes, absent/unknown evidence outcomes, policy malformed values, nested unknown fields, and all three corpus-report shapes. Expectations are fixed by these rules before the implementation exists. The registration pins sources, prompt, payload digest and exact case count. Existing 80 record fixtures remain unchanged and run alongside it.\n\nThe contract does not introduce an in-toto attestation channel, a trusted producer, or protected acceptance. Those remain DP-4.1's disclosed limit.\n\n\n## Variant selection and exact reason sets\n\nSupplementary fixture family labels are `task`, `acceptance`, `policy`, `evidence`, `h4`, `markdown_corpus`, `h5_conformance`. They name structural schemas, not claimed verdicts. Select the sourced agent-context branch when either `source` or `provenance` is present, otherwise select legacy direct context. Select sourced change-claim when `intent` is present or `source` is an object, otherwise the null-task/null-source/note branch. Do not union these branches into an empty required-key intersection. Full/compact provenance selection is specified above. Forbidden hybrid keys produce `schema.unknown_field`; missing selected-branch requirements produce `schema.missing_field`; mistyped members produce `schema.wrong_type`. A branch-selection failure must not report every unrelated branch's errors.\n\nThe inventory fixes all exact dynamic-map JSON-pointer paths before schema implementation. These are explicit exceptions to record-key closure. Require string values for path-to-digest maps. Corpus embedded checker-report and witness payload objects are separately owned output vocabularies: preserve object/nullable types at that boundary but do not freeze future checker fields from a historical run. Missing owned corpus envelope and case fields remain errors. Unknown nested fields outside those declared maps remain errors.\n"
  },
  "docs/architecture/section-reference-contract.md": {
    "body": "[Heading { level: 1, slug: \"section-references-nn-contract-for-chg-010\", children: [Text(\"Section references (§n.n): contract for CHG-010\")] }, Paragraph([Text(\"Status: written before any fixture or code for CHG-010 (plan W9, feature 2). Decisions: \"), Code(\"section-refs-scope\"), Text(\", \"), Code(\"section-refs-observation\"), Text(\", \"), Code(\"section-refs-check-surface\"), Text(\" in \"), Code(\".rha/tasks/CHG-008-growth.toml\"), Text(\".\")]), Heading { level: 2, slug: \"purpose\", children: [Text(\"Purpose\")] }, Paragraph([Text(\"A page that numbers its sections, like the spec, refers to them as \"), Code(\"§11.7.6\"), Text(\". rhawiki turns each such reference into a link to the heading it names, on the same page. The plan predicts that this touches the \"), Code(\"document\"), Text(\" crate only.\")]), Heading { level: 2, slug: \"1-section-numbers-of-headings\", children: [Text(\"1. Section numbers of headings\")] }, Paragraph([Text(\"A heading has a section number when its text (\"), Code(\"Heading::text\"), Text(\") begins with one of these forms, followed by whitespace:\")]), List { start: None, items: [[Code(\"N(.N)*\"), Text(\", optionally followed by one \"), Code(\".\"), Text(\": \"), Code(\"1. Problem, Scope…\"), Text(\" has number \"), Code(\"1\"), Text(\"; \"), Code(\"1.1 Target class…\"), Text(\" has \"), Code(\"1.1\"), Text(\"; \"), Code(\"11.7.6 Acceptance…\"), Text(\" has \"), Code(\"11.7.6\"), Text(\". Here \"), Code(\"N\"), Text(\" is one or more ASCII digits.\")], [Code(\"Appendix L(.N)*\"), Text(\", optionally followed by one \"), Code(\".\"), Text(\": \"), Code(\"Appendix A. Mathematical…\"), Text(\" has number \"), Code(\"A\"), Text(\". Here \"), Code(\"L\"), Text(\" is one ASCII uppercase letter.\")]] }, Paragraph([Text(\"The heading text may also end right after the number, which then has no trailing whitespace. Every heading counts, wherever it is nested: a heading inside a block quote or a list item has a section number by the same rule. Numbers are compared as exact strings: \"), Code(\"1.10\"), Text(\" and \"), Code(\"1.1\"), Text(\" differ, and \"), Code(\"01\"), Text(\" and \"), Code(\"1\"), Text(\" differ. When several headings carry the same number, the first in document order is the target.\")]), Heading { level: 2, slug: \"2-recognizing-a-reference\", children: [Text(\"2. Recognizing a reference\")] }, Paragraph([Text(\"References are recognized only in text content. That means paragraphs, list items, table cells and block quotes, at any depth. They are never recognized in headings, inline code, code blocks, raw HTML (an HTML block, or the characters of an inline HTML tag; text between two inline tags, as in \"), Code(\"<b>§7</b>\"), Text(\", is ordinary text and is recognized), the text or destination of a markdown link or a wikilink, a transclusion, or an image's alt text.\")]), Paragraph([Text(\"A reference is \"), Code(\"§\"), Text(\" or \"), Code(\"§§\"), Text(\", then at most one space (U+0020 only; a tab or a line break ends it), then a number: \"), Code(\"N(.N)*\"), Text(\" or \"), Code(\"L(.N)*\"), Text(\". An \"), Code(\"L\"), Text(\" counts only when the character after it is not a letter. The number is the longest match: a \"), Code(\".\"), Text(\" belongs to it only when a digit follows. So in \"), Code(\"see §7.\"), Text(\" the number is \"), Code(\"7\"), Text(\", and the final \"), Code(\".\"), Text(\" is sentence punctuation.\")]), Paragraph([Text(\"A \"), Strong([Text(\"range continuation\")]), Text(\" directly after a number is optional spaces (U+0020 only, never a line break), then \"), Code(\"–\"), Text(\" (U+2013) or \"), Code(\"-\"), Text(\", then optional spaces, then an optional \"), Code(\"§\"), Text(\", then a number. It makes that number a second reference, the range end. So \"), Code(\"§§3–7\"), Text(\", \"), Code(\"§3–§5\"), Text(\" and \"), Code(\"§§8.3–8.4\"), Text(\" each contain two references. A comma list after \"), Code(\"§§\"), Text(\", such as \"), Code(\"§§8.3, 8.4\"), Text(\", gives only the first reference; this is a known limitation. It also applies after a range: \"), Code(\"§§3–7, 9\"), Text(\" gives \"), Code(\"3\"), Text(\" and \"), Code(\"7\"), Text(\", and \"), Code(\"9\"), Text(\" is not a reference. Every other occurrence of \"), Code(\"§\"), Text(\" needs its own \"), Code(\"§\"), Text(\", so \"), Code(\"§6 and §12.1\"), Text(\" gives two references.\")]), Heading { level: 2, slug: \"3-resolution-and-rendering\", children: [Text(\"3. Resolution and rendering\")] }, Paragraph([Text(\"A reference whose number equals the number of a heading on the same page (section 1) \"), Strong([Text(\"resolves\")]), Text(\" to that heading. It is rendered as a markdown-style link (\"), Code(\"Node::Link\"), Text(\") to \"), Code(\"#<slug>\"), Text(\", where the slug is that heading's unique slug (\"), Code(\"Heading::slug\"), Text(\"). The link's visible text is exactly the characters the reference occupies, taken from the characters that were written:\")]), List { start: None, items: [[Code(\"§4.1\"), Text(\" becomes one link with text \"), Code(\"§4.1\"), Text(\";\")], [Code(\"§§3–7\"), Text(\" becomes a link with text \"), Code(\"§§3\"), Text(\", then the text \"), Code(\"–\"), Text(\", then a link with text \"), Code(\"7\"), Text(\";\")], [Code(\"§3–§5\"), Text(\" becomes a link \"), Code(\"§3\"), Text(\", the text \"), Code(\"–\"), Text(\", then a link \"), Code(\"§5\"), Text(\".\")]] }, Paragraph([Text(\"Surrounding text is kept byte for byte.\")]), Paragraph([Text(\"A reference that does not resolve stays text, unchanged. If the page has at least one numbered heading, the page also records \"), Code(\"Diagnostic::UnresolvedSectionRef { number, line }\"), Text(\". If the page has none, the reference is neither linked nor diagnosed, because the section numbers belong to another document. Cross-page references are out of scope (section 5).\")]), Heading { level: 2, slug: \"4-observation-surface\", children: [Text(\"4. Observation surface\")] }, Paragraph([Code(\"Document\"), Text(\" gains \"), Code(\"section_refs: Vec<SectionRef>\"), Text(\", one entry per recognized reference in document order:\")]), CodeBlock { lang: Some(\"rust\"), text: \"pub struct SectionRef {\\n    pub text: String,           // the characters rendered as the link text (or left as text)\\n    pub number: String,         // the section number referred to\\n    pub target: Option<String>, // the target heading's slug when resolved, else None\\n    pub line: usize,            // 1-based source line of the reference's first character\\n}\\n\" }, Paragraph([Text(\"The registered grader compares this list, and the diagnostics, with the registered expectations.\")]), Heading { level: 2, slug: \"5-non-goals-and-invariants\", children: [Text(\"5. Non-goals and invariants\")] }, List { start: None, items: [[Strong([Text(\"Out of scope:\")]), Text(\" cross-page references (for example \"), Code(\"index.md\"), Text(\" citing a spec section), comma lists after \"), Code(\"§§\"), Text(\", and surfacing \"), Code(\"UnresolvedSectionRef\"), Text(\" in \"), Code(\"rhawiki check\"), Text(\", whose JSON schema stays fixed (decision \"), Code(\"section-refs-check-surface\"), Text(\").\")], [Strong([Text(\"Nothing else changes:\")]), Text(\" headings, slugs, the TOC, wikilinks, transclusion, other links and \"), Code(\"rhawiki check\"), Text(\" output.\")], [Strong([Text(\"Invariant:\")]), Text(\" every resolved \"), Code(\"SectionRef::target\"), Text(\" is the slug of a heading on the same page, and the corresponding \"), Code(\"Node::Link\"), Text(\" has \"), Code(\"href == \\\"#\\\" + target\"), Text(\".\")], [Strong([Text(\"The real spec\")]), Text(\" (\"), Code(\"docs/spec/rha-spec-v0.10.md\"), Text(\") records no \"), Code(\"UnresolvedSectionRef\"), Text(\": every reference in its text content names one of its own numbered headings. A pre-registration count found 163 numbered headings and no unresolved number. The spec also has 10 \"), Code(\"§\"), Text(\" characters in contexts that are not references (1 in a heading, 9 in fenced code).\")]] }, Heading { level: 2, slug: \"amendment-before-registration-2026-09-24\", children: [Text(\"Amendment before registration (2026-09-24)\")] }, Paragraph([Text(\"The independent oracle author's registration review raised five questions the first draft left open. They were answered in the text above before any fixture was registered and before any code existed: nested headings count; text between inline HTML tags is recognized; the one space is U+0020 only, so a tab or a line break ends a reference; range whitespace never crosses a line break; a comma list after a range is not continued; and \"), Code(\"line\"), Text(\" is the line of the reference's first character.\")])]",
    "headings": "[Heading { level: 1, text: \"Section references (§n.n): contract for CHG-010\", slug: \"section-references-nn-contract-for-chg-010\", base_slug: \"section-references-nn-contract-for-chg-010\", line: 1 }, Heading { level: 2, text: \"Purpose\", slug: \"purpose\", base_slug: \"purpose\", line: 5 }, Heading { level: 2, text: \"1. Section numbers of headings\", slug: \"1-section-numbers-of-headings\", base_slug: \"1-section-numbers-of-headings\", line: 9 }, Heading { level: 2, text: \"2. Recognizing a reference\", slug: \"2-recognizing-a-reference\", base_slug: \"2-recognizing-a-reference\", line: 18 }, Heading { level: 2, text: \"3. Resolution and rendering\", slug: \"3-resolution-and-rendering\", base_slug: \"3-resolution-and-rendering\", line: 26 }, Heading { level: 2, text: \"4. Observation surface\", slug: \"4-observation-surface\", base_slug: \"4-observation-surface\", line: 38 }, Heading { level: 2, text: \"5. Non-goals and invariants\", slug: \"5-non-goals-and-invariants\", base_slug: \"5-non-goals-and-invariants\", line: 53 }, Heading { level: 2, text: \"Amendment before registration (2026-09-24)\", slug: \"amendment-before-registration-2026-09-24\", base_slug: \"amendment-before-registration-2026-09-24\", line: 60 }]",
    "markdown": "# Section references (§n.n): contract for CHG-010\n\nStatus: written before any fixture or code for CHG-010 (plan W9, feature 2). Decisions: `section-refs-scope`, `section-refs-observation`, `section-refs-check-surface` in `.rha/tasks/CHG-008-growth.toml`.\n\n## Purpose\n\nA page that numbers its sections, like the spec, refers to them as `§11.7.6`. rhawiki turns each such reference into a link to the heading it names, on the same page. The plan predicts that this touches the `document` crate only.\n\n## 1. Section numbers of headings\n\nA heading has a section number when its text (`Heading::text`) begins with one of these forms, followed by whitespace:\n\n- `N(.N)*`, optionally followed by one `.`: `1. Problem, Scope…` has number `1`; `1.1 Target class…` has `1.1`; `11.7.6 Acceptance…` has `11.7.6`. Here `N` is one or more ASCII digits.\n- `Appendix L(.N)*`, optionally followed by one `.`: `Appendix A. Mathematical…` has number `A`. Here `L` is one ASCII uppercase letter.\n\nThe heading text may also end right after the number, which then has no trailing whitespace. Every heading counts, wherever it is nested: a heading inside a block quote or a list item has a section number by the same rule. Numbers are compared as exact strings: `1.10` and `1.1` differ, and `01` and `1` differ. When several headings carry the same number, the first in document order is the target.\n\n## 2. Recognizing a reference\n\nReferences are recognized only in text content. That means paragraphs, list items, table cells and block quotes, at any depth. They are never recognized in headings, inline code, code blocks, raw HTML (an HTML block, or the characters of an inline HTML tag; text between two inline tags, as in `<b>§7</b>`, is ordinary text and is recognized), the text or destination of a markdown link or a wikilink, a transclusion, or an image's alt text.\n\nA reference is `§` or `§§`, then at most one space (U+0020 only; a tab or a line break ends it), then a number: `N(.N)*` or `L(.N)*`. An `L` counts only when the character after it is not a letter. The number is the longest match: a `.` belongs to it only when a digit follows. So in `see §7.` the number is `7`, and the final `.` is sentence punctuation.\n\nA **range continuation** directly after a number is optional spaces (U+0020 only, never a line break), then `–` (U+2013) or `-`, then optional spaces, then an optional `§`, then a number. It makes that number a second reference, the range end. So `§§3–7`, `§3–§5` and `§§8.3–8.4` each contain two references. A comma list after `§§`, such as `§§8.3, 8.4`, gives only the first reference; this is a known limitation. It also applies after a range: `§§3–7, 9` gives `3` and `7`, and `9` is not a reference. Every other occurrence of `§` needs its own `§`, so `§6 and §12.1` gives two references.\n\n## 3. Resolution and rendering\n\nA reference whose number equals the number of a heading on the same page (section 1) **resolves** to that heading. It is rendered as a markdown-style link (`Node::Link`) to `#<slug>`, where the slug is that heading's unique slug (`Heading::slug`). The link's visible text is exactly the characters the reference occupies, taken from the characters that were written:\n\n- `§4.1` becomes one link with text `§4.1`;\n- `§§3–7` becomes a link with text `§§3`, then the text `–`, then a link with text `7`;\n- `§3–§5` becomes a link `§3`, the text `–`, then a link `§5`.\n\nSurrounding text is kept byte for byte.\n\nA reference that does not resolve stays text, unchanged. If the page has at least one numbered heading, the page also records `Diagnostic::UnresolvedSectionRef { number, line }`. If the page has none, the reference is neither linked nor diagnosed, because the section numbers belong to another document. Cross-page references are out of scope (section 5).\n\n## 4. Observation surface\n\n`Document` gains `section_refs: Vec<SectionRef>`, one entry per recognized reference in document order:\n\n```rust\npub struct SectionRef {\n    pub text: String,           // the characters rendered as the link text (or left as text)\n    pub number: String,         // the section number referred to\n    pub target: Option<String>, // the target heading's slug when resolved, else None\n    pub line: usize,            // 1-based source line of the reference's first character\n}\n```\n\nThe registered grader compares this list, and the diagnostics, with the registered expectations.\n\n## 5. Non-goals and invariants\n\n- **Out of scope:** cross-page references (for example `index.md` citing a spec section), comma lists after `§§`, and surfacing `UnresolvedSectionRef` in `rhawiki check`, whose JSON schema stays fixed (decision `section-refs-check-surface`).\n- **Nothing else changes:** headings, slugs, the TOC, wikilinks, transclusion, other links and `rhawiki check` output.\n- **Invariant:** every resolved `SectionRef::target` is the slug of a heading on the same page, and the corresponding `Node::Link` has `href == \"#\" + target`.\n- **The real spec** (`docs/spec/rha-spec-v0.10.md`) records no `UnresolvedSectionRef`: every reference in its text content names one of its own numbered headings. A pre-registration count found 163 numbered headings and no unresolved number. The spec also has 10 `§` characters in contexts that are not references (1 in a heading, 9 in fenced code).\n\n## Amendment before registration (2026-09-24)\n\nThe independent oracle author's registration review raised five questions the first draft left open. They were answered in the text above before any fixture was registered and before any code existed: nested headings count; text between inline HTML tags is recognized; the one space is U+0020 only, so a tab or a line break ends a reference; range whitespace never crosses a line break; a comma list after a range is not continued; and `line` is the line of the reference's first character.\n"
  },
  "docs/architecture/transclusion-contract.md": {
    "body": "[Heading { level: 1, slug: \"transclusion-contract\", children: [Text(\"Transclusion contract\")] }, Paragraph([Text(\"This is the additive contract for transclusion and section selection. It is\"), SoftBreak, Text(\"written before implementation. It does not change the existing HTML/JSON\"), SoftBreak, Text(\"renderer contracts, silently regrade existing corpora, or propose a new crate.\")]), Heading { level: 2, slug: \"document-syntax-and-selection\", children: [Text(\"Document syntax and selection\")] }, Paragraph([Code(\"document\"), Text(\" owns:\")]), CodeBlock { lang: Some(\"text\"), text: \"Transclusion {\\n    id: usize,\\n    target: String,\\n    anchor: Option<String>,\\n    display: String,\\n    line: usize,\\n}\\n\" }, Paragraph([Code(\"Node::Transclusion(Transclusion)\"), Text(\" is parser-owned only. IDs are contiguous\"), SoftBreak, Text(\"preorder IDs among recognized embeds in one source document and are stable\"), SoftBreak, Text(\"under document permutation. \"), Code(\"Document.links\"), Text(\" remains navigation-only with its\"), SoftBreak, Text(\"existing indexing and semantics.\")]), Paragraph([Text(\"A pulldown WikiLink image is a transclusion candidate only when it has no alias,\"), SoftBreak, Text(\"has a nonempty page target, and, when \"), Code(\"#\"), Text(\" is supplied, has a nonempty anchor.\"), SoftBreak, Text(\"The candidate must be the sole meaningful content of a paragraph or tight list\"), SoftBreak, Text(\"item; surrounding spaces and tabs are permitted. Recognition is recursive\"), SoftBreak, Text(\"through quotes and list items. Code blocks and code spans are never candidates.\"), SoftBreak, Text(\"Candidates with other content remain \"), Code(\"Image\"), Text(\"; aliases remain \"), Code(\"Image\"), Text(\".\")]), Paragraph([Text(\"The returned document tree contains a recognized transclusion as a direct\"), SoftBreak, Text(\"block node. Its sole-image paragraph is replaced by that node; a tight item's\"), SoftBreak, Text(\"sole candidate likewise becomes a direct item child. A Transclusion never\"), SoftBreak, Text(\"remains inside a Paragraph or another inline container.\")]), Paragraph([Text(\"The measured exclusions are normative: escaped bangs and spaced bangs retain\"), SoftBreak, Text(\"their literal-bang plus navigation behavior; inline and aliased \"), Code(\"![[...]]\"), Text(\" are\"), SoftBreak, Text(\"images, not navigation links; two images separated by a soft break are not\"), SoftBreak, Text(\"standalone; and four-space indentation remains code. Recognition is structural,\"), SoftBreak, Text(\"not a blanket regular expression over raw text. Internal normalization is\"), SoftBreak, Text(\"permitted, but an unrecognized candidate must never leak as \"), Code(\"Transclusion\"), Text(\".\")]), Paragraph([Text(\"The public selector is:\")]), CodeBlock { lang: Some(\"text\"), text: \"section_nodes(&Document, Option<&str>) -> Option<Vec<document::Node>>\\n\" }, Paragraph([Code(\"None\"), Text(\" selects the full body. An anchor matches a final slug exactly and\"), SoftBreak, Text(\"case-sensitively; an absent slug returns \"), Code(\"None\"), Text(\". It selects that heading and following\"), SoftBreak, Text(\"siblings in that heading's immediate block sequence until the next heading of\"), SoftBreak, Text(\"less than or equal level. Nested headings do not terminate an outer section.\"), SoftBreak, Text(\"Selection recursively traverses quotes and lists, preserving ancestor shells\"), SoftBreak, Text(\"without unrelated siblings. A list wrapper retains only the selected item and\"), SoftBreak, Text(\"adjusts an ordered list's start by that item's position. Slugs remain the\"), SoftBreak, Text(\"original document slugs. Inputs are parsed \"), Code(\"Document\"), Text(\" values; no parser type is\"), SoftBreak, Text(\"exposed. A pure traversal helper supplies transclusion descriptors.\")]), Paragraph([Text(\"The traversal helper is \"), Code(\"transclusions(&[Node]) -> Vec<&Transclusion>\"), Text(\" in\"), SoftBreak, Text(\"deterministic tree order. It retains the recognized descriptors' source IDs.\")]), Heading { level: 2, slug: \"graph\", children: [Text(\"Graph\")] }, Paragraph([Text(\"Ordinary navigation remains unchanged. \"), Code(\"Resolved\"), Text(\" is unchanged and no\"), SoftBreak, Code(\"anchor_valid\"), Text(\" field is added. Transclusions are resolved separately using the\"), SoftBreak, Text(\"existing exact-ID and unique case-insensitive-basename rules. A page that\"), SoftBreak, Text(\"resolves contributes a backlink even when its requested anchor is missing,\"), SoftBreak, Text(\"consistent with navigation. Missing or ambiguous pages do not. Expansion is\"), SoftBreak, Text(\"valid only when \"), Code(\"section_nodes\"), Text(\" succeeds.\")]), Paragraph([Text(\"Transclusion diagnostics are distinct from navigation diagnostics:\")]), CodeBlock { lang: Some(\"text\"), text: \"BrokenTransclusion       { from, target, line }\\nAmbiguousTransclusion    { from, target, candidates, line }\\nMissingTransclusionAnchor{ from, target, heading, line }\\nTransclusionCycle        { path: Vec<TransclusionRegion> }\\n\" }, Paragraph([Text(\"The graph defines:\")]), CodeBlock { lang: Some(\"text\"), text: \"TransclusionRegion     { page: PageId, anchor: Option<String> }\\nTransclusionOccurrence { source: TransclusionRegion, transclusion_id: usize }\\nTransclusionCycle      { path: Vec<TransclusionRegion> }\\n\" }, Paragraph([Text(\"The following text abbreviates these as Region and Occurrence. \"), Code(\"SiteGraph\"), SoftBreak, Text(\"provides \"), Code(\"transclusion_target(&TransclusionOccurrence) -> Option<&TransclusionRegion>\"), SoftBreak, Text(\"and \"), Code(\"blocked_transclusion(&TransclusionOccurrence) -> Option<&TransclusionCycle>\"), Text(\".\")]), Paragraph([Text(\"Regions order by page, then \"), Code(\"None\"), Text(\" before \"), Code(\"Some(anchor)\"), Text(\". Edge target lookup is\"), SoftBreak, Text(\"keyed by the full source \"), Code(\"Occurrence\"), Text(\", never only by page and transclusion ID.\"), SoftBreak, Text(\"The graph may use a temporary global resolution map internally, while assembly\"), SoftBreak, Text(\"may follow only analyzed edges. Read-only target and blocked-cycle lookup is\"), SoftBreak, Text(\"available by \"), Code(\"Occurrence\"), Text(\".\")]), Paragraph([Text(\"The graph builds a region for every full page and every valid anchored target.\"), SoftBreak, Text(\"Each region contains only recognized embeds inside its selected fragment. Roots\"), SoftBreak, Text(\"order by \"), Code(\"Region\"), Text(\"; outgoing edges order by transclusion ID, then target.\"), SoftBreak, Text(\"Deterministic DFS blocks every edge to an active region. A cycle witness is the\"), SoftBreak, Text(\"active-stack suffix plus the repeated target, canonically rotated to the least\"), SoftBreak, Code(\"Region\"), Text(\"; closed paths are deduplicated. Removing blocked occurrences leaves a\"), SoftBreak, Text(\"DAG. Cycles are graph-owned witnesses.\")]), Paragraph([Text(\"The global check covers all pages and regions. Selecting a fragment excludes\"), SoftBreak, Text(\"unrelated edges from that region but does not hide a separate cycle elsewhere.\"), SoftBreak, Text(\"Thus page-level \"), Code(\"A -> B#keep\"), Text(\", with \"), Code(\"B#other -> A\"), Text(\", is acyclic when those are the\"), SoftBreak, Text(\"only edges in the selected regions; an independent \"), Code(\"B#other\"), Text(\" self-cycle remains\"), SoftBreak, Text(\"a global witness but produces no cycle marker in \"), Code(\"A\"), Text(\"'s selected \"), Code(\"keep\"), Text(\" region.\")]), Heading { level: 2, slug: \"assembly-and-page-model\", children: [Text(\"Assembly and page model\")] }, Paragraph([Code(\"Assemble\"), Text(\" receives the current document, the complete explicit \"), Code(\"&[Document]\"), Text(\",\"), SoftBreak, Text(\"graph, title lookup, and context. \"), Code(\"site::build\"), Text(\" passes its existing document\"), SoftBreak, Text(\"slice. Assembly never reaches build or glue.\")]), Paragraph([Text(\"Assembly replaces each recognized transclusion with its selected fragment and\"), SoftBreak, Text(\"recurses using the exact occurrence lookup. A blocked edge becomes a literal\"), SoftBreak, Text(\"semantic node:\")]), CodeBlock { lang: Some(\"text\"), text: \"Paragraph([Text(\\\"[transclusion cycle: A#one -> B#two -> A#one]\\\")])\\n\" }, Paragraph([Text(\"Missing, ambiguous, or invalid targets become:\")]), CodeBlock { lang: Some(\"text\"), text: \"Paragraph([Text(\\\"[transclusion unavailable: <display>]\\\")])\\n\" }, Paragraph([Text(\"The same semantic marker is consumed by HTML and JSON. The host TOC remains\"), SoftBreak, Text(\"host-only. \"), Code(\"PageNode\"), Text(\" contains no transclusion variant.\")]), Paragraph([Text(\"All original host navigation slots and indices are preserved as assembly\"), SoftBreak, Text(\"currently maps \"), Code(\"Document.links\"), Text(\". Imported visible wikilinks append deterministic\"), SoftBreak, Text(\"slots in traversal order, resolve against their origin document and origin\"), SoftBreak, Text(\"index, and append separately when an import repeats. The attempted hidden-alt\"), SoftBreak, Text(\"probe is not evidence of a bug and must not be represented as one.\")]), Paragraph([Text(\"Ordinary links and images in the original host body are copied unchanged.\"), SoftBreak, Text(\"Origin rebasing applies to imported content; a self-import still receives its\"), SoftBreak, Text(\"own heading namespace and the same explicit import-path rules.\")]), Paragraph([Text(\"Imported heading IDs use the occurrence chain:\")]), CodeBlock { lang: Some(\"text\"), text: \"tx-<outer id>-<nested id>-<original slug>\\n\" }, Paragraph([Text(\"Used IDs are seeded with every host heading. All mappings for one fragment are\"), SoftBreak, Text(\"allocated before copying it, in deterministic tree order; collisions receive\"), SoftBreak, Code(\"-2\"), Text(\", \"), Code(\"-3\"), Text(\", and so on globally. Fragment-only ordinary links whose decoded\"), SoftBreak, Text(\"fragment matches a selected heading are rewritten to its mapped host anchor.\"), SoftBreak, Text(\"Unmatched fragment-only links target the canonical origin page plus the\"), SoftBreak, Text(\"original fragment, not the host page.\")]), Heading { level: 2, slug: \"origin-aware-path-rewriting\", children: [Text(\"Origin-aware path rewriting\")] }, Paragraph([Text(\"Assembly rebases relative ordinary-link destinations and image sources from\"), SoftBreak, Text(\"origin directory to host directory using lexical POSIX dot-segment\"), SoftBreak, Text(\"normalization. The canonical origin URL is its percent-encoded page ID plus\"), SoftBreak, Code(\".md\"), Text(\"; the host base is the directory of its encoded page ID. This bounded\"), SoftBreak, Text(\"operation is not complete URI validation. It:\")]), List { start: None, items: [[Text(\"preserves scheme URLs, protocol-relative URLs and absolute paths byte for\"), SoftBreak, Text(\"byte; it preserves query and fragment suffixes except for the specified\"), SoftBreak, Text(\"fragment-only anchor rewrite;\")], [Text(\"treats empty and query-only destinations as references to the canonical\"), SoftBreak, Text(\"origin page;\")], [Text(\"retains \"), Code(\".md\"), Text(\" for the existing HTML renderer's \"), Code(\"rewrite_href\"), Text(\"; core does not\"), SoftBreak, Text(\"generate format-specific \"), Code(\".html\"), Text(\" or \"), Code(\".json\"), Text(\" suffixes;\")], [Text(\"percent-encodes known origin and host \"), Code(\"PageId\"), Text(\" path segments using unreserved\"), SoftBreak, Text(\"ASCII plus \"), Code(\"/\"), Text(\", while preserving already encoded destination segments;\")], [Text(\"requires valid UTF-8 when percent-decoding a fragment for matching; invalid\"), SoftBreak, Text(\"fragments remain unmatched;\")], [Text(\"percent-encodes generated anchors;\")], [Text(\"retains \"), Code(\"..\"), Text(\" above the logical root instead of silently clamping it;\")], [Text(\"preserves a directory trailing slash; and\")], [Text(\"prefixes \"), Code(\"./\"), Text(\" when the computed first relative segment could be interpreted\"), SoftBreak, Text(\"as a scheme, such as \"), Code(\"a:b.md\"), Text(\".\")]] }, Paragraph([Text(\"A scheme prefix is ASCII alphabetic followed by zero or more ASCII letters,\"), SoftBreak, Text(\"digits, \"), Code(\"+\"), Text(\", \"), Code(\"-\"), Text(\" or \"), Code(\".\"), Text(\", then \"), Code(\":\"), Text(\", before a path separator. Local heading-anchor\"), SoftBreak, Text(\"remapping applies only to ordinary links; image sources use origin rebasing\"), SoftBreak, Text(\"without that remapping.\")]), Paragraph([Text(\"PageId values are NFC-normalized by their library constructor; host and origin\"), SoftBreak, Text(\"inputs use that canonical domain. Newly generated percent escapes use uppercase\"), SoftBreak, Text(\"hex digits, matching the existing HTML encoding convention. Existing escapes in\"), SoftBreak, Text(\"supplied destinations retain their original spelling and case.\")]), Paragraph([Text(\"Expansion and cloning allocate memory, and repeated embeds may multiply output.\"), SoftBreak, Text(\"No linear bound or measured performance result is claimed.\")]), Heading { level: 2, slug: \"cli-compatibility\", children: [Text(\"CLI compatibility\")] }, Paragraph([Text(\"The CLI retains envelope \"), Code(\"schema_version: 1\"), Text(\", existing witness objects,\"), SoftBreak, Text(\"counts, ordering, and exit semantics. New witness kinds are additive:\")]), CodeBlock { lang: Some(\"text\"), text: \"broken_transclusion\\nambiguous_transclusion\\nmissing_transclusion_anchor\\ntransclusion_cycle\\n\" }, Paragraph([Text(\"Exact objects are \"), Code(\"{kind, from, target}\"), Text(\" for \"), Code(\"broken_transclusion\"), Text(\",\"), SoftBreak, Code(\"{kind, from, target, candidates}\"), Text(\" for \"), Code(\"ambiguous_transclusion\"), Text(\", and\"), SoftBreak, Code(\"{kind, from, target, heading}\"), Text(\" for \"), Code(\"missing_transclusion_anchor\"), Text(\". Candidates\"), SoftBreak, Text(\"are sorted canonical page-ID strings. Unlike the legacy ambiguous-link output,\"), SoftBreak, Text(\"the new ambiguity kind retains its candidate list. These objects omit \"), Code(\"line\"), Text(\".\"), SoftBreak, Code(\"transclusion_cycle\"), Text(\" is \"), Code(\"{kind, path}\"), Text(\", with closed path strings in \"), Code(\"page\"), Text(\" or\"), SoftBreak, Code(\"page#anchor\"), Text(\" form. Legacy witness objects keep every existing key and value.\"), SoftBreak, Text(\"Existing old markdown and JSON cases are not regraded or regenerated.\")]), Heading { level: 2, slug: \"planned-registration-and-checks\", children: [Text(\"Planned registration and checks\")] }, Paragraph([Text(\"Registration precedes implementation. A separate session authors Markdown,\"), SoftBreak, Text(\"section, and path fixtures plus independent references for section selection,\"), SoftBreak, Text(\"region blocking, and path rebasing. Seeded normalized graph cases are\"), SoftBreak, Text(\"algorithm-level synthetic graphs, not claims that every graph is realizable\"), SoftBreak, Text(\"Markdown. Exact fixed outputs are required for every case, together with\"), SoftBreak, Text(\"permutation and differential property checks; every necessarily triggered scope\"), SoftBreak, Text(\"failure is included. Existing corpus bytes remain unchanged.\")]), Paragraph([Text(\"Required scenario groups are:\")]), List { start: None, items: [[Text(\"whole-page expansion and exact parser exclusions;\")], [Text(\"tight, loose, nested-list, and quote contexts;\")], [Text(\"root-section bounds, nested-heading shells, and list ordinals;\")], [Text(\"nested embeds;\")], [Text(\"exact, basename, missing, ambiguous, and missing-anchor targets;\")], [Text(\"self-cycles, two-region cycles, same-page acyclic regions, and out-of-region\"), SoftBreak, Text(\"false-cycle controls;\")], [Text(\"multi-cycle SCCs;\")], [Text(\"repeated embeds, host-anchor collisions, imported navigation slots, and\"), SoftBreak, Text(\"inside/outside fragments; and\")], [Text(\"relative paths, queries, schemes, Unicode, colon-like names, and directory\"), SoftBreak, Text(\"cases.\")]] }, Paragraph([Text(\"The predeclared production spread is \"), Code(\"document\"), Text(\", \"), Code(\"graph\"), Text(\", \"), Code(\"site::assembly\"), Text(\",\"), SoftBreak, Code(\"site::build\"), Text(\", the \"), Code(\"site\"), Text(\" facade, and \"), Code(\"app-cli\"), Text(\". Renderer production remains\"), SoftBreak, Text(\"unchanged; renderer setup tests may migrate only for the added \"), Code(\"Assemble\"), SoftBreak, Text(\"document-slice argument. Owner-local JSON oracle tests may use \"), Code(\"serde_json\"), SoftBreak, Text(\"DEV-only metadata with an exact live \"), Code(\"core.dev_allow\"), Text(\" entry and task\"), SoftBreak, Text(\"justification; there is no normal core \"), Code(\"serde\"), Text(\" dependency. Frozen H4 uses its\"), SoftBreak, Text(\"own fixture rules, so a live dev allow does not alter frozen grades.\")])]",
    "headings": "[Heading { level: 1, text: \"Transclusion contract\", slug: \"transclusion-contract\", base_slug: \"transclusion-contract\", line: 1 }, Heading { level: 2, text: \"Document syntax and selection\", slug: \"document-syntax-and-selection\", base_slug: \"document-syntax-and-selection\", line: 7 }, Heading { level: 2, text: \"Graph\", slug: \"graph\", base_slug: \"graph\", line: 64 }, Heading { level: 2, text: \"Assembly and page model\", slug: \"assembly-and-page-model\", base_slug: \"assembly-and-page-model\", line: 114 }, Heading { level: 2, text: \"Origin-aware path rewriting\", slug: \"origin-aware-path-rewriting\", base_slug: \"origin-aware-path-rewriting\", line: 160 }, Heading { level: 2, text: \"CLI compatibility\", slug: \"cli-compatibility\", base_slug: \"cli-compatibility\", line: 198 }, Heading { level: 2, text: \"Planned registration and checks\", slug: \"planned-registration-and-checks\", base_slug: \"planned-registration-and-checks\", line: 219 }]",
    "markdown": "# Transclusion contract\n\nThis is the additive contract for transclusion and section selection. It is\nwritten before implementation. It does not change the existing HTML/JSON\nrenderer contracts, silently regrade existing corpora, or propose a new crate.\n\n## Document syntax and selection\n\n`document` owns:\n\n```text\nTransclusion {\n    id: usize,\n    target: String,\n    anchor: Option<String>,\n    display: String,\n    line: usize,\n}\n```\n\n`Node::Transclusion(Transclusion)` is parser-owned only. IDs are contiguous\npreorder IDs among recognized embeds in one source document and are stable\nunder document permutation. `Document.links` remains navigation-only with its\nexisting indexing and semantics.\n\nA pulldown WikiLink image is a transclusion candidate only when it has no alias,\nhas a nonempty page target, and, when `#` is supplied, has a nonempty anchor.\nThe candidate must be the sole meaningful content of a paragraph or tight list\nitem; surrounding spaces and tabs are permitted. Recognition is recursive\nthrough quotes and list items. Code blocks and code spans are never candidates.\nCandidates with other content remain `Image`; aliases remain `Image`.\n\nThe returned document tree contains a recognized transclusion as a direct\nblock node. Its sole-image paragraph is replaced by that node; a tight item's\nsole candidate likewise becomes a direct item child. A Transclusion never\nremains inside a Paragraph or another inline container.\n\nThe measured exclusions are normative: escaped bangs and spaced bangs retain\ntheir literal-bang plus navigation behavior; inline and aliased `![[...]]` are\nimages, not navigation links; two images separated by a soft break are not\nstandalone; and four-space indentation remains code. Recognition is structural,\nnot a blanket regular expression over raw text. Internal normalization is\npermitted, but an unrecognized candidate must never leak as `Transclusion`.\n\nThe public selector is:\n\n```text\nsection_nodes(&Document, Option<&str>) -> Option<Vec<document::Node>>\n```\n\n`None` selects the full body. An anchor matches a final slug exactly and\ncase-sensitively; an absent slug returns `None`. It selects that heading and following\nsiblings in that heading's immediate block sequence until the next heading of\nless than or equal level. Nested headings do not terminate an outer section.\nSelection recursively traverses quotes and lists, preserving ancestor shells\nwithout unrelated siblings. A list wrapper retains only the selected item and\nadjusts an ordered list's start by that item's position. Slugs remain the\noriginal document slugs. Inputs are parsed `Document` values; no parser type is\nexposed. A pure traversal helper supplies transclusion descriptors.\n\nThe traversal helper is `transclusions(&[Node]) -> Vec<&Transclusion>` in\ndeterministic tree order. It retains the recognized descriptors' source IDs.\n\n## Graph\n\nOrdinary navigation remains unchanged. `Resolved` is unchanged and no\n`anchor_valid` field is added. Transclusions are resolved separately using the\nexisting exact-ID and unique case-insensitive-basename rules. A page that\nresolves contributes a backlink even when its requested anchor is missing,\nconsistent with navigation. Missing or ambiguous pages do not. Expansion is\nvalid only when `section_nodes` succeeds.\n\nTransclusion diagnostics are distinct from navigation diagnostics:\n\n```text\nBrokenTransclusion       { from, target, line }\nAmbiguousTransclusion    { from, target, candidates, line }\nMissingTransclusionAnchor{ from, target, heading, line }\nTransclusionCycle        { path: Vec<TransclusionRegion> }\n```\n\nThe graph defines:\n\n```text\nTransclusionRegion     { page: PageId, anchor: Option<String> }\nTransclusionOccurrence { source: TransclusionRegion, transclusion_id: usize }\nTransclusionCycle      { path: Vec<TransclusionRegion> }\n```\n\nThe following text abbreviates these as Region and Occurrence. `SiteGraph`\nprovides `transclusion_target(&TransclusionOccurrence) -> Option<&TransclusionRegion>`\nand `blocked_transclusion(&TransclusionOccurrence) -> Option<&TransclusionCycle>`.\n\nRegions order by page, then `None` before `Some(anchor)`. Edge target lookup is\nkeyed by the full source `Occurrence`, never only by page and transclusion ID.\nThe graph may use a temporary global resolution map internally, while assembly\nmay follow only analyzed edges. Read-only target and blocked-cycle lookup is\navailable by `Occurrence`.\n\nThe graph builds a region for every full page and every valid anchored target.\nEach region contains only recognized embeds inside its selected fragment. Roots\norder by `Region`; outgoing edges order by transclusion ID, then target.\nDeterministic DFS blocks every edge to an active region. A cycle witness is the\nactive-stack suffix plus the repeated target, canonically rotated to the least\n`Region`; closed paths are deduplicated. Removing blocked occurrences leaves a\nDAG. Cycles are graph-owned witnesses.\n\nThe global check covers all pages and regions. Selecting a fragment excludes\nunrelated edges from that region but does not hide a separate cycle elsewhere.\nThus page-level `A -> B#keep`, with `B#other -> A`, is acyclic when those are the\nonly edges in the selected regions; an independent `B#other` self-cycle remains\na global witness but produces no cycle marker in `A`'s selected `keep` region.\n\n## Assembly and page model\n\n`Assemble` receives the current document, the complete explicit `&[Document]`,\ngraph, title lookup, and context. `site::build` passes its existing document\nslice. Assembly never reaches build or glue.\n\nAssembly replaces each recognized transclusion with its selected fragment and\nrecurses using the exact occurrence lookup. A blocked edge becomes a literal\nsemantic node:\n\n```text\nParagraph([Text(\"[transclusion cycle: A#one -> B#two -> A#one]\")])\n```\n\nMissing, ambiguous, or invalid targets become:\n\n```text\nParagraph([Text(\"[transclusion unavailable: <display>]\")])\n```\n\nThe same semantic marker is consumed by HTML and JSON. The host TOC remains\nhost-only. `PageNode` contains no transclusion variant.\n\nAll original host navigation slots and indices are preserved as assembly\ncurrently maps `Document.links`. Imported visible wikilinks append deterministic\nslots in traversal order, resolve against their origin document and origin\nindex, and append separately when an import repeats. The attempted hidden-alt\nprobe is not evidence of a bug and must not be represented as one.\n\nOrdinary links and images in the original host body are copied unchanged.\nOrigin rebasing applies to imported content; a self-import still receives its\nown heading namespace and the same explicit import-path rules.\n\nImported heading IDs use the occurrence chain:\n\n```text\ntx-<outer id>-<nested id>-<original slug>\n```\n\nUsed IDs are seeded with every host heading. All mappings for one fragment are\nallocated before copying it, in deterministic tree order; collisions receive\n`-2`, `-3`, and so on globally. Fragment-only ordinary links whose decoded\nfragment matches a selected heading are rewritten to its mapped host anchor.\nUnmatched fragment-only links target the canonical origin page plus the\noriginal fragment, not the host page.\n\n## Origin-aware path rewriting\n\nAssembly rebases relative ordinary-link destinations and image sources from\norigin directory to host directory using lexical POSIX dot-segment\nnormalization. The canonical origin URL is its percent-encoded page ID plus\n`.md`; the host base is the directory of its encoded page ID. This bounded\noperation is not complete URI validation. It:\n\n- preserves scheme URLs, protocol-relative URLs and absolute paths byte for\n  byte; it preserves query and fragment suffixes except for the specified\n  fragment-only anchor rewrite;\n- treats empty and query-only destinations as references to the canonical\n  origin page;\n- retains `.md` for the existing HTML renderer's `rewrite_href`; core does not\n  generate format-specific `.html` or `.json` suffixes;\n- percent-encodes known origin and host `PageId` path segments using unreserved\n  ASCII plus `/`, while preserving already encoded destination segments;\n- requires valid UTF-8 when percent-decoding a fragment for matching; invalid\n  fragments remain unmatched;\n- percent-encodes generated anchors;\n- retains `..` above the logical root instead of silently clamping it;\n- preserves a directory trailing slash; and\n- prefixes `./` when the computed first relative segment could be interpreted\n  as a scheme, such as `a:b.md`.\n\nA scheme prefix is ASCII alphabetic followed by zero or more ASCII letters,\ndigits, `+`, `-` or `.`, then `:`, before a path separator. Local heading-anchor\nremapping applies only to ordinary links; image sources use origin rebasing\nwithout that remapping.\n\nPageId values are NFC-normalized by their library constructor; host and origin\ninputs use that canonical domain. Newly generated percent escapes use uppercase\nhex digits, matching the existing HTML encoding convention. Existing escapes in\nsupplied destinations retain their original spelling and case.\n\nExpansion and cloning allocate memory, and repeated embeds may multiply output.\nNo linear bound or measured performance result is claimed.\n\n## CLI compatibility\n\nThe CLI retains envelope `schema_version: 1`, existing witness objects,\ncounts, ordering, and exit semantics. New witness kinds are additive:\n\n```text\nbroken_transclusion\nambiguous_transclusion\nmissing_transclusion_anchor\ntransclusion_cycle\n```\n\nExact objects are `{kind, from, target}` for `broken_transclusion`,\n`{kind, from, target, candidates}` for `ambiguous_transclusion`, and\n`{kind, from, target, heading}` for `missing_transclusion_anchor`. Candidates\nare sorted canonical page-ID strings. Unlike the legacy ambiguous-link output,\nthe new ambiguity kind retains its candidate list. These objects omit `line`.\n`transclusion_cycle` is `{kind, path}`, with closed path strings in `page` or\n`page#anchor` form. Legacy witness objects keep every existing key and value.\nExisting old markdown and JSON cases are not regraded or regenerated.\n\n## Planned registration and checks\n\nRegistration precedes implementation. A separate session authors Markdown,\nsection, and path fixtures plus independent references for section selection,\nregion blocking, and path rebasing. Seeded normalized graph cases are\nalgorithm-level synthetic graphs, not claims that every graph is realizable\nMarkdown. Exact fixed outputs are required for every case, together with\npermutation and differential property checks; every necessarily triggered scope\nfailure is included. Existing corpus bytes remain unchanged.\n\nRequired scenario groups are:\n\n- whole-page expansion and exact parser exclusions;\n- tight, loose, nested-list, and quote contexts;\n- root-section bounds, nested-heading shells, and list ordinals;\n- nested embeds;\n- exact, basename, missing, ambiguous, and missing-anchor targets;\n- self-cycles, two-region cycles, same-page acyclic regions, and out-of-region\n  false-cycle controls;\n- multi-cycle SCCs;\n- repeated embeds, host-anchor collisions, imported navigation slots, and\n  inside/outside fragments; and\n- relative paths, queries, schemes, Unicode, colon-like names, and directory\n  cases.\n\nThe predeclared production spread is `document`, `graph`, `site::assembly`,\n`site::build`, the `site` facade, and `app-cli`. Renderer production remains\nunchanged; renderer setup tests may migrate only for the added `Assemble`\ndocument-slice argument. Owner-local JSON oracle tests may use `serde_json`\nDEV-only metadata with an exact live `core.dev_allow` entry and task\njustification; there is no normal core `serde` dependency. Frozen H4 uses its\nown fixture rules, so a live dev allow does not alter frozen grades.\n"
  },
  "docs/backlog.md": {
    "body": "[Heading { level: 1, slug: \"backlog\", children: [Text(\"Backlog\")] }, List { start: None, items: [[TaskMarker(false), Text(\"Benchmark and, if justified, index per-link resolution — current M2 behavior passes its gates and no performance threshold is claimed — crates/graph/src/lib.rs:SiteGraph::resolution.\")]] }]",
    "headings": "[Heading { level: 1, text: \"Backlog\", slug: \"backlog\", base_slug: \"backlog\", line: 1 }]",
    "markdown": "# Backlog\n\n- [ ] Benchmark and, if justified, index per-link resolution — current M2 behavior passes its gates and no performance threshold is claimed — crates/graph/src/lib.rs:SiteGraph::resolution.\n"
  },
  "docs/changes/CHG-001-ambient-effect-deny-list.md": {
    "body": "[Heading { level: 1, slug: \"chg-001-ambient-effect-deny-list-and-clippy-configuration-discovery\", children: [Text(\"CHG-001: Ambient-effect deny list and Clippy configuration discovery\")] }, Paragraph([Text(\"Task record: \"), Link { href: \"../../.rha/tasks/CHG-001-ambient-effect-deny-list.toml\", children: [Code(\".rha/tasks/CHG-001-ambient-effect-deny-list.toml\")] }, Text(\". Plan: \"), WikiLink { index: 0, children: [Text(\"plan/IMPLEMENTATION-PLAN\")] }, Text(\" §8 W1. Decision record: \"), WikiLink { index: 1, children: [Text(\"adr/ADR-0002-clippy-config-discovery\")] }, Text(\".\")]), Paragraph([Text(\"The item has four parts. \"), Strong([Text(\"W1\")]), Text(\" is the work item the plan asks for. \"), Strong([Text(\"CHG-001.1\")]), Text(\", \"), Strong([Text(\"CHG-001.2\")]), Text(\" and \"), Strong([Text(\"CHG-001.3\")]), Text(\" are rework Kennedy asked for on the open pull request, before merging; their decisions are in the task record.\")]), Heading { level: 2, slug: \"intent-and-scope\", children: [Text(\"Intent and scope\")] }, Paragraph([Strong([Text(\"Behaviour.\")]), Text(\" A core crate whose \"), Code(\"clippy.toml\"), Text(\" is the template fails \"), Code(\"cargo clippy\"), Text(\" on a direct use of any of 111 ambient-effect paths, starting with the seeded \"), Code(\"std::time::SystemTime::now()\"), Text(\". The pinned Clippy's configuration discovery and merge behaviour is observed and recorded, together with what switches the deny list off. The rule \"), Code(\"effect.core_clippy_template\"), Text(\" reports a core crate whose Clippy configuration is not the template.\")]), Paragraph([Strong([Text(\"Affected components and contracts.\")]), Text(\" None: no product component exists. New: \"), Code(\"xtask/templates/core-clippy.toml\"), Text(\" (creation approved by Kennedy, extension approved for CHG-001.2), \"), Code(\"xtask/src/clippy_template.rs\"), Text(\", and the Clippy corpus, whose fixture workspaces are generated at test time under \"), Code(\"target/clippy-corpus/\"), Text(\".\")]), Paragraph([Strong([Text(\"Non-goals.\")]), Text(\" Product crates (CHG-005). Running the rule in L0, since \"), Code(\"cargo xtask architecture\"), Text(\" is still a stub (CHG-003). The \"), Code(\"no_std\"), Text(\" option (experiment E1). Any change to the policy, \"), Code(\"rha-crates.toml\"), Text(\", \"), Code(\"deny.toml\"), Text(\", \"), Code(\"rha-baseline.json\"), Text(\", the root \"), Code(\"clippy.toml\"), Text(\", the root \"), Code(\"Cargo.toml\"), Text(\", or \"), Code(\"xtask ci\"), Text(\".\")]), Paragraph([Strong([Text(\"Mode and responsibilities.\")]), Text(\" Mode \"), Code(\"agent\"), Text(\". Executor \"), Code(\"agent:executor\"), Text(\", a role, not a model: the task record's provenance names the model exposed in each span of the work. Planner, Integrator, and Acceptor \"), Code(\"human:kennedy\"), Text(\". Authority profile \"), Code(\"ECC-Solo\"), Text(\".\")]), Heading { level: 2, slug: \"deltas\", children: [Text(\"Deltas\")] }, Paragraph([Strong([Text(\"Architecture, API, schema, dependencies, effects, ledger.\")]), Text(\" A new public xtask module, \"), Code(\"clippy_template\"), Text(\", with \"), Code(\"check\"), Text(\", \"), Code(\"CoreCrate\"), Text(\", \"), Code(\"Finding\"), Text(\", and \"), Code(\"Problem\"), Text(\". No new dependency. The ledger is unchanged.\")]), Paragraph([Strong([Text(\"Unsafe, concurrency, authority, policy.\")]), Text(\" No unsafe code. The only protected surface touched is \"), Code(\"xtask/templates/core-clippy.toml\"), Text(\": created in W1 and extended in CHG-001.2, both approved by Kennedy in the conversation and recorded in the task record. \"), Code(\"git diff c554316 HEAD\"), Text(\" over every other protected surface is empty.\")]), Paragraph([Strong([Text(\"CHG-001.1, requested rework.\")])]), List { start: None, items: [[Code(\".claude/settings.json\"), Text(\" is ignored, so local records no longer report the working tree as dirty because of it.\")], [Text(\"The corpus held two committed fixture workspaces: 6 Clippy configuration files, which made 8 in the repository with the root file and the template, 2 copies of the root lint table, 2 lock files, and a shell script, kept true by drift tests. Seventeen committed fixture files and the script go. Each test now generates its fixture under \"), Code(\"target/clippy-corpus/\"), Text(\" from the three files that own the facts: the root \"), Code(\"Cargo.toml\"), Text(\", the root \"), Code(\"clippy.toml\"), Text(\", and the template. Eight committed crate sources remain. Two of the three drift tests go with the copies; \"), Code(\"template_repeats_every_root_setting\"), Text(\" stays, because the template's repeat of the root settings is a copy that rule 2 of ADR-0002 forces.\")], [Code(\"run-experiments.sh\"), Text(\" goes. \"), Code(\"RHA_CLIPPY_RECORD=<dir>\"), Text(\" makes the same test runs write the evidence records, so the experiments a reviewer reads and the experiments L0 runs are one definition.\")], [Text(\"The Executor is named by role, \"), Code(\"agent:executor\"), Text(\". The plan, the threat model, and the \"), Code(\"--principal\"), Text(\" help text follow. CHG-000's merged records keep the id they were written with.\")]] }, Paragraph([Strong([Text(\"CHG-001.2, deny list and escape hatches.\")])]), List { start: None, items: [[Text(\"Experiment 6, fourteen runs, records what switches the deny list off and what closes it: \"), Code(\"#[allow]\"), Text(\", \"), Code(\"#[expect]\"), Text(\", \"), Code(\"#![allow]\"), Text(\", and an allow flag in \"), Code(\"RUSTFLAGS\"), Text(\" all silence it; a crate-level \"), Code(\"#![forbid]\"), Text(\" or \"), Code(\"forbid\"), Text(\" in the lint table turns the attributes into \"), Code(\"E0453\"), Text(\"; \"), Code(\"--cap-lints=warn\"), Text(\" lowers even a forbidden finding. Experiment 6n shows the same flag works from \"), Code(\".cargo/config.toml\"), Text(\", which is not a protected surface.\")], [Text(\"The template grows from 15 entries to 111, closing the gaps ADR-0002 named. Ten of the additions are the paths that record listed; the rest come from a sweep of the Rust 1.98.1 standard-library source and two adversarial reviews of the result, all limited to the effects §6.8 names. ADR-0002 carries the per-category table, the candidates left for Kennedy, and what cannot be reached at all. Experiment 4 shows every entry resolve and fire.\")], [Code(\"std::env::set_var\"), Text(\" and \"), Code(\"std::env::remove_var\"), Text(\" are \"), Code(\"unsafe\"), Text(\" in edition 2024, so the fixture names them instead of calling them. The lint fires on the path reference, which is how both entries are demonstrated.\")]] }, Paragraph([Strong([Text(\"CHG-001.3, acceptance, the escape, and the front door.\")]), Text(\" Kennedy asked for the acceptance records, the recommended decisions, the findings in the specification, and a README worth reading.\")]), List { start: None, items: [[Link { href: \"../../.rha/acceptances/CHG-000.toml\", children: [Code(\".rha/acceptances/CHG-000.toml\")] }, Text(\" records the acceptance Kennedy made on 2026-09-19 by merging PR 1 and tagging \"), Code(\"v0.10-m0\"), Text(\": the subject, the evidence, the outcomes as recorded, the four predicates, and the six concerns it carries forward rather than closes. No acceptance record is written for CHG-001, which is neither merged nor accepted.\")], [Strong([Text(\"The recommended \"), Code(\"forbid\"), Text(\" was refused by the toolchain.\")]), Text(\" Set on the three lints in the root \"), Code(\"Cargo.toml\"), Text(\", it stopped \"), Code(\"xtask\"), Text(\" compiling with 15 \"), Code(\"E0453\"), Text(\" errors, all from \"), Code(\"clap\"), Text(\"'s derives, which expand to \"), Code(\"#[allow(clippy::style)]\"), Text(\". Experiments 7a and 7b reproduce the mechanism without a dependency; a \"), Code(\"serde\"), Text(\" derive is unaffected. The escape is closed per core crate instead, by a crate-root \"), Code(\"#![forbid(...)]\"), Text(\" that experiment 7c shows costs clean code nothing, and the rule reports a crate root that leaves any of the three unforbidden.\")], [Code(\".cargo/config.toml\"), Text(\" joins the protected surfaces in \"), Code(\".rha/policy.toml\"), Text(\", because \"), Code(\"--cap-lints=warn\"), Text(\" there disables every lint at once.\")], [Text(\"Spec §6.8 asked the implementer to verify how the pinned Clippy finds and merges configuration files. It is verified. The first edit put the five observations in §6.8 as a table, which duplicated ADR-0002 in a second editable place, against the one-owner rule of §11.7.1 and the defect §12.1 records against v0.7. Corrected on the same day: §6.8 keeps the normative consequence in its Hole column and a sentence saying where the observations live, and the proposals that would change its text are queued in \"), Link { href: \"../proposals/spec-v0.11.md\", children: [Code(\"docs/proposals/spec-v0.11.md\")] }, Text(\" for the version bump. That file also states the working rule it came from: a work item records findings in a decision record and evidence, the specification changes in one item per version, and a claim the specification makes about this repository gets a check. The §1.4 maturity table is untouched.\")], [Text(\"The README and the GitHub description describe the project, what works today, and what is only specified.\")]] }, Paragraph([Strong([Text(\"Removed, weakened, or reinterpreted tests.\")]), Text(\" The two fixture drift tests are removed with the copies they guarded: \"), Code(\"fixtures_copy_the_root_lint_tables_and_root_clippy_file\"), Text(\" had nothing left to compare, and the rule's \"), Code(\"copies_of_the_template_conform\"), Text(\", renamed \"), Code(\"a_copy_of_the_template_conforms\"), Text(\", now writes its own crate directories. No behavioural check was weakened; the corpus went from 4 tests to 7 and from 7 experiments to 21.\")]), Paragraph([Strong([Text(\"Beyond the plan's three experiments,\")]), Text(\" each added to make one of them decisive or to answer a listed unknown: 3c (control), 3b (the fix for 3), 4 (every entry), 5 (the plan's unknown, \"), Code(\"clippy.toml\"), Text(\" beside \"), Code(\".clippy.toml\"), Text(\"), and 6a to 6n (what a crate or its configuration can do to the deny list).\")]), Heading { level: 2, slug: \"evidence\", children: [Text(\"Evidence\")] }, Paragraph([Strong([Text(\"Experiments.\")]), Text(\" Twenty-six experiments, each recorded with its literal command, the digests of every generated file, the exit status, and the captured output, in \"), Link { href: \"../../evidence/w1-clippy/20260919T231239Z-8af79cb0f76a/\", children: [Code(\"evidence/w1-clippy/20260919T231239Z-8af79cb0f76a/\")] }, Text(\", written from the generated corpus at commit \"), Code(\"8af79cb0f76ad5ea0386f3a2af85dc9b65dd48b1\"), Text(\" with a clean working tree. That commit carries the template this record describes, so each record's digest of \"), Code(\"crates/core-every/clippy.toml\"), Text(\" equals the template's. Two earlier sets are superseded: one at \"), Code(\"edd4bac\"), Text(\" against an 80-entry template, and one at \"), Code(\"bf5aed9\"), Text(\" that predates experiments 7a to 7c. \"), Link { href: \"../../evidence/w1-clippy/\", children: [Code(\"evidence/w1-clippy/\")] }, Text(\" also keeps the seven records of the first run, at commit \"), Code(\"28ae543c297067ad5171654f490307b13995376a\"), Text(\", which the shell script produced before CHG-001.1 replaced it. ADR-0002 tabulates the experiments and states what they mean. \"), Code(\"xtask/tests/clippy_corpus.rs\"), Text(\" reruns all of them in every \"), Code(\"L0.nextest\"), Text(\" run, locally and on the GitHub runner.\")]), Paragraph([Strong([Text(\"L0 records.\")]), Text(\" Two matter, and they differ in one predicate. \"), Link { href: \"../../evidence/CHG-001/20260919T224504Z-bae4b34894a2.json\", children: [Code(\"evidence/CHG-001/20260919T224504Z-bae4b34894a2.json\")] }, Text(\" covers the deny-list work under the unchanged policy, where the digest equals the base policy's and \"), Strong([Text(\"Applicable holds\")]), Text(\". \"), Link { href: \"../../evidence/CHG-001/20260919T231351Z-602d2f953d0b.json\", children: [Code(\"evidence/CHG-001/20260919T231351Z-602d2f953d0b.json\")] }, Text(\" covers the head of this change, after \"), Code(\".cargo/config.toml\"), Text(\" joined the protected list, and reports \"), Strong([Text(\"Applicable false\")]), Text(\", naming the two digests. Both are class \"), Code(\"local\"), Text(\", principal \"), Code(\"agent:executor\"), Text(\", on a clean working tree. The table below is the later one.\")]), Table { align: [None, None, None], head: [[Text(\"Check\")], [Text(\"Outcome\")], [Text(\"Detail\")]], rows: [[[Code(\"L0.fmt\")], [Text(\"passed\")], []], [[Code(\"L0.clippy\")], [Text(\"passed\")], []], [[Code(\"L0.nextest\")], [Text(\"passed\")], [Text(\"33 tests, including the 7 corpus tests and 6 rule tests\")]], [[Code(\"L0.doctest\")], [Text(\"passed\")], [Text(\"1 doctest\")]], [[Code(\"L0.architecture\")], [Strong([Text(\"not_run\")])], [Code(\"not_implemented\"), Text(\" (stub, exit 4)\")]], [[Code(\"L0.deny\")], [Text(\"passed\")], []], [[Code(\"L0.machete\")], [Text(\"passed\")], []], [[Code(\"L0.typos\")], [Text(\"passed\")], [Text(\"the corpus sources are excluded by \"), Code(\"_typos.toml\")]]] }, Paragraph([Text(\"Eligibility \"), Code(\"blocked\"), Text(\": Authentic (no trusted producers), Applicable (the policy changed in this contribution), and Passed (\"), Code(\"L0.architecture\"), Text(\" not_run). Complete holds.\")]), Paragraph([Strong([Text(\"CI records.\")]), Text(\" Every push to the branch ran the lane on the GitHub runner, image \"), Code(\"ubuntu24\"), Text(\", class \"), Code(\"ci\"), Text(\". Two runs are committed, and each covers a different revision. Run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35470532056\", children: [Text(\"35470532056\")] }, Text(\", copied as \"), Link { href: \"../../evidence/ci/35470532056.json\", children: [Code(\"evidence/ci/35470532056.json\")] }, Text(\", covers the W1 head \"), Code(\"d011d13\"), Text(\" and its 28 tests; it predates the rework, so it says nothing about the escape hatches. Run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35474230710\", children: [Text(\"35474230710\")] }, Text(\", copied as \"), Link { href: \"../../evidence/ci/35474230710.json\", children: [Code(\"evidence/ci/35474230710.json\")] }, Text(\", covers PR head \"), Code(\"1d65ce72e8c52ddcadd9354795fad3c163d56fec\"), Text(\", the deny-list work. Run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35475553364\", children: [Text(\"35475553364\")] }, Text(\", copied as \"), Link { href: \"../../evidence/ci/35475553364.json\", children: [Code(\"evidence/ci/35475553364.json\")] }, Text(\", covers PR head \"), Code(\"df8ec0fc773bcc4f2a106b414540bffc4bd14808\"), Text(\". Run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35476272071\", children: [Text(\"35476272071\")] }, Text(\", copied as \"), Link { href: \"../../evidence/ci/35476272071.json\", children: [Code(\"evidence/ci/35476272071.json\")] }, Text(\", covers PR head \"), Code(\"5c24f54272eb8d90c878bf43ff96daf67c87d551\"), Text(\", the head this record describes, and shares its tree. Its outcomes match the local record, so the discovery rule and the escape-hatch results reproduce on a second machine.\")]), Paragraph([Strong([Text(\"Performance.\")]), Text(\" No trigger; nothing claimed.\")]), Heading { level: 3, slug: \"repair-attempts-1178\", children: [Text(\"Repair attempts (§11.7.8)\")] }, List { start: Some(1), items: [[Strong([Text(\"Experiment 3b was not decisive.\")]), Text(\" In the first scripted run, at \"), Code(\"a152d94\"), Text(\", 3b ran against a crate that also held the seeded clock call; that build failed, so the absent \"), Code(\"unwrap_used\"), Text(\" was weak evidence. Hypothesis: a crate with the template and nothing on the deny list would give a clean exit. Change: a separate fixture crate. Result: exit 0 and no \"), Code(\"unwrap_used\"), Text(\" (commit \"), Code(\"28ae543\"), Text(\").\")], [Strong([Text(\"Experiment 5 exposed a silent override.\")]), Text(\" Observation, not a repair: with both files present, \"), Code(\".clippy.toml\"), Text(\" wins with only a warning. Change: \"), Code(\"effect.core_clippy_template\"), Text(\" also reports \"), Code(\"Problem::Shadowed\"), Text(\". CHG-001.1 made the experiment decisive by putting the deny list in \"), Code(\"clippy.toml\"), Text(\", the root settings in \"), Code(\".clippy.toml\"), Text(\", and the seeded call in the crate: the call passes.\")], [Strong([Text(\"Helper \"), Code(\"unwrap()\"), Text(\" warnings in \"), Code(\"xtask/tests/clippy_corpus.rs\"), Text(\".\")]), Text(\" The root test allowances cover \"), Code(\"#[test]\"), Text(\" functions and \"), Code(\"#[cfg(test)]\"), Text(\" modules, not free helper functions in an integration test file. The helpers panic with a message that names the file or fixture.\")], [Strong([Text(\"The lane caught the rule code.\")]), Text(\" At \"), Code(\"043d7c1\"), Text(\", \"), Code(\"L0.clippy\"), Text(\" failed: the new crate-root check ended a closure's mutable borrow with \"), Code(\"drop(finding)\"), Text(\", which \"), Code(\"clippy::drop_non_drop\"), Text(\" rejects because a closure implements no \"), Code(\"Drop\"), Text(\". Hypothesis: a block scope would end the borrow without the call. Result: the lane passes at \"), Code(\"602d2f9\"), Text(\". The record of the failing run was deleted rather than committed, which was the wrong call in a repository that keeps what it observes; this paragraph stands in its place.\")]] }, Heading { level: 2, slug: \"acceptance-concerns\", children: [Text(\"Acceptance concerns\")] }, List { start: Some(1), items: [[Strong([Text(\"Still not Eligible.\")]), Text(\" \"), Code(\"L0.architecture\"), Text(\" is \"), Code(\"not_run\"), Text(\" (not implemented until CHG-003), and it is required and non-waivable under DP-0.5. Kennedy decided on 2026-09-19 to leave it \"), Code(\"not_run\"), Text(\" rather than move a minimal checker into W1: it would pass over zero core crates, it would not change eligibility while Authentic fails, and it would put checker code before the corpus pre-registration of W2. The options in CHG-000's acceptance concern 2 are unchanged, and the policy was not edited.\")], [Strong([Text(\"The policy changed in this contribution, so Applicable no longer holds.\")]), Text(\" Adding \"), Code(\".cargo/config.toml\"), Text(\" to the protected list changes the policy digest, and a record is Applicable only where the candidate's policy equals the base revision's (§11.7.6). The L0 record of CHG-001.2, taken before that edit, is the one that holds under the unchanged policy; the record taken after it reports Applicable false with that reason. §11.5 calls this a controlled transition, and it is yours to accept.\")], [Strong([Text(\"The specification carries two sentences of this change.\")]), Text(\" §6.8's Hole column states the consequence, and one sentence says where the observations live; everything else is queued for v0.11. The §1.4 maturity table is untouched, because those rows are yours to promote at a version bump from \"), Code(\"docs/maturity.md\"), Text(\". Reverting either sentence costs nothing but the text.\")], [Strong([Text(\"The attribute escape is closed by convention plus a rule, not by the compiler.\")]), Text(\" A core crate that carries the crate-root \"), Code(\"#![forbid(...)]\"), Text(\" cannot allow these lints anywhere, including its own tests; a core crate that omits the line is reported by \"), Code(\"effect.core_clippy_template\"), Text(\", which does not run in L0 until CHG-003. Until then nothing enforces the line, and no core crate exists to carry it.\")], [Strong([Text(\"What the deny list cannot reach.\")]), Text(\" Direct uses only: not a call through another crate, not one the compiler generates from a macro. \"), Code(\"HashMap::new\"), Text(\" seeds itself from \"), Code(\"RandomState\"), Text(\" without naming it, so iteration-order nondeterminism stays out of reach. Platform extension traits and unstable APIs are left out on purpose (ADR-0002).\")], [Strong([Text(\"The rule is not yet in L0.\")]), Text(\" \"), Code(\"effect.core_clippy_template\"), Text(\" is exercised only by its unit tests until CHG-003 calls it from \"), Code(\"cargo xtask architecture\"), Text(\".\")], [Strong([Text(\"Held-out cases.\")]), Text(\" None exist for this change. The fixtures and the expected rule were written by the Executor; the expectation was written into the task record before the experiments ran. The deny-list additions were proposed by advisory subagents reading the standard-library source, and every one of them is checked by experiment 4, which fails if an entry stops firing.\")]] }, Heading { level: 2, slug: \"acceptance-chg-0014\", children: [Text(\"Acceptance (CHG-001.4)\")] }, Paragraph([Text(\"Kennedy accepted this change at \"), Code(\"44b1e44\"), Text(\", the merge commit of \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/pull/2\", children: [Text(\"pull request 2\")] }, Text(\", on 2026-09-19. The record is \"), Link { href: \"../../.rha/acceptances/CHG-001.toml\", children: [Code(\".rha/acceptances/CHG-001.toml\")] }, Text(\", written by the Executor at his instruction, which is also the approval for that protected surface.\")]), Paragraph([Text(\"The merge added nothing of its own: \"), Code(\"44b1e44^{tree}\"), Text(\" equals \"), Code(\"a31cdd1^{tree}\"), Text(\", both \"), Code(\"336f32c8\"), Text(\", and \"), Code(\"git diff a31cdd1 44b1e44\"), Text(\" is empty.\")]), Paragraph([Strong([Text(\"The evidence that covers the accepted tree.\")]), Text(\" The records committed before the merge stop short of it. The last local record, \"), Link { href: \"../../evidence/CHG-001/20260919T232940Z-78fe0093c4a4.json\", children: [Code(\"evidence/CHG-001/20260919T232940Z-78fe0093c4a4.json\")] }, Text(\", was taken at \"), Code(\"78fe009\"), Text(\", two commits before the head; run 35476272071 covers head \"), Code(\"5c24f54\"), Text(\". CHG-001.4 therefore downloads the \"), Code(\"rha-evidence\"), Text(\" artifact of run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35476379661\", children: [Text(\"35476379661\")] }, Text(\", the run on head \"), Code(\"a31cdd1\"), Text(\", and commits it as \"), Link { href: \"../../evidence/ci/35476379661.json\", children: [Code(\"evidence/ci/35476379661.json\")] }, Text(\". Its \"), Code(\"artifact_identity.tree\"), Text(\" is \"), Code(\"336f32c8968d8f9b523cae4d96f168ba59c485c9\"), Text(\", equal to the accepted tree, so the lane ran on exactly the content now on \"), Code(\"main\"), Text(\"; its \"), Code(\"revision\"), Text(\" is GitHub's ephemeral merge commit \"), Code(\"5343431\"), Text(\", which no longer exists as a branch. Seven checks passed, \"), Code(\"L0.architecture\"), Text(\" is \"), Code(\"not_run\"), Text(\", and all four predicates match the local record.\")]), Paragraph([Text(\"That the record arrives after the item it describes is itself a weakness, listed in the acceptance record's \"), Code(\"carried_forward\"), Text(\": an acceptance whose evidence has to be fetched from a CI artifact is reproducible only while the artifact lives.\")]), Paragraph([Strong([Text(\"The transition closed.\")]), Text(\" Every cited record reports \"), Code(\"base_policy_digest\"), Text(\" \"), Code(\"sha256:f102c03a…\"), Text(\" against its own \"), Code(\"policy_digest\"), Text(\" \"), Code(\"sha256:dc2ef7e0…\"), Text(\", so Applicable is false on all of them. A lane run at \"), Code(\"44b1e44\"), Text(\" reports both as \"), Code(\"sha256:dc2ef7e0…\"), Text(\" and Applicable true: the base is now the policy this change proposed. That run is reported in the working conversation and is not committed, so \"), Code(\"main\"), Text(\" carries no record of its own.\")]), Heading { level: 3, slug: \"defect-found-at-acceptance\", children: [Text(\"Defect found at acceptance\")] }, Paragraph([Strong([Text(\"D-001.1, record integrity.\")]), Text(\" The task record shipped at \"), Code(\"44b1e44\"), Text(\" with the literal string \"), Code(\"SPEC_DIGEST\"), Text(\" where the sha256 of \"), Code(\"docs/spec/rha-spec-v0.10.md\"), Text(\" belonged, in the fifth \"), Code(\"instruction_sources\"), Text(\" entry. The record asserted a witness it did not hold, in the one field that lets a reader reproduce the Executor's inputs. Nothing caught it: no check reads task records, and the record-schema check that would reject a non-hex digest belongs to CHG-019. CHG-001.4 fills in \"), Code(\"bd96216829035ade64c43befa5e672c58078f6afa4b04145c6183227e4e1071a\"), Text(\", verifiable with \"), Code(\"git show 44b1e44:docs/spec/rha-spec-v0.10.md | sha256sum\"), Text(\", and the acceptance record states the defect in full.\")])]",
    "headings": "[Heading { level: 1, text: \"CHG-001: Ambient-effect deny list and Clippy configuration discovery\", slug: \"chg-001-ambient-effect-deny-list-and-clippy-configuration-discovery\", base_slug: \"chg-001-ambient-effect-deny-list-and-clippy-configuration-discovery\", line: 1 }, Heading { level: 2, text: \"Intent and scope\", slug: \"intent-and-scope\", base_slug: \"intent-and-scope\", line: 7 }, Heading { level: 2, text: \"Deltas\", slug: \"deltas\", base_slug: \"deltas\", line: 17 }, Heading { level: 2, text: \"Evidence\", slug: \"evidence\", base_slug: \"evidence\", line: 48 }, Heading { level: 3, text: \"Repair attempts (§11.7.8)\", slug: \"repair-attempts-1178\", base_slug: \"repair-attempts-1178\", line: 71 }, Heading { level: 2, text: \"Acceptance concerns\", slug: \"acceptance-concerns\", base_slug: \"acceptance-concerns\", line: 78 }, Heading { level: 2, text: \"Acceptance (CHG-001.4)\", slug: \"acceptance-chg-0014\", base_slug: \"acceptance-chg-0014\", line: 88 }, Heading { level: 3, text: \"Defect found at acceptance\", slug: \"defect-found-at-acceptance\", base_slug: \"defect-found-at-acceptance\", line: 100 }]",
    "markdown": "# CHG-001: Ambient-effect deny list and Clippy configuration discovery\n\nTask record: [`.rha/tasks/CHG-001-ambient-effect-deny-list.toml`](../../.rha/tasks/CHG-001-ambient-effect-deny-list.toml). Plan: [[plan/IMPLEMENTATION-PLAN]] §8 W1. Decision record: [[adr/ADR-0002-clippy-config-discovery]].\n\nThe item has four parts. **W1** is the work item the plan asks for. **CHG-001.1**, **CHG-001.2** and **CHG-001.3** are rework Kennedy asked for on the open pull request, before merging; their decisions are in the task record.\n\n## Intent and scope\n\n**Behaviour.** A core crate whose `clippy.toml` is the template fails `cargo clippy` on a direct use of any of 111 ambient-effect paths, starting with the seeded `std::time::SystemTime::now()`. The pinned Clippy's configuration discovery and merge behaviour is observed and recorded, together with what switches the deny list off. The rule `effect.core_clippy_template` reports a core crate whose Clippy configuration is not the template.\n\n**Affected components and contracts.** None: no product component exists. New: `xtask/templates/core-clippy.toml` (creation approved by Kennedy, extension approved for CHG-001.2), `xtask/src/clippy_template.rs`, and the Clippy corpus, whose fixture workspaces are generated at test time under `target/clippy-corpus/`.\n\n**Non-goals.** Product crates (CHG-005). Running the rule in L0, since `cargo xtask architecture` is still a stub (CHG-003). The `no_std` option (experiment E1). Any change to the policy, `rha-crates.toml`, `deny.toml`, `rha-baseline.json`, the root `clippy.toml`, the root `Cargo.toml`, or `xtask ci`.\n\n**Mode and responsibilities.** Mode `agent`. Executor `agent:executor`, a role, not a model: the task record's provenance names the model exposed in each span of the work. Planner, Integrator, and Acceptor `human:kennedy`. Authority profile `ECC-Solo`.\n\n## Deltas\n\n**Architecture, API, schema, dependencies, effects, ledger.** A new public xtask module, `clippy_template`, with `check`, `CoreCrate`, `Finding`, and `Problem`. No new dependency. The ledger is unchanged.\n\n**Unsafe, concurrency, authority, policy.** No unsafe code. The only protected surface touched is `xtask/templates/core-clippy.toml`: created in W1 and extended in CHG-001.2, both approved by Kennedy in the conversation and recorded in the task record. `git diff c554316 HEAD` over every other protected surface is empty.\n\n**CHG-001.1, requested rework.**\n\n- `.claude/settings.json` is ignored, so local records no longer report the working tree as dirty because of it.\n- The corpus held two committed fixture workspaces: 6 Clippy configuration files, which made 8 in the repository with the root file and the template, 2 copies of the root lint table, 2 lock files, and a shell script, kept true by drift tests. Seventeen committed fixture files and the script go. Each test now generates its fixture under `target/clippy-corpus/` from the three files that own the facts: the root `Cargo.toml`, the root `clippy.toml`, and the template. Eight committed crate sources remain. Two of the three drift tests go with the copies; `template_repeats_every_root_setting` stays, because the template's repeat of the root settings is a copy that rule 2 of ADR-0002 forces.\n- `run-experiments.sh` goes. `RHA_CLIPPY_RECORD=<dir>` makes the same test runs write the evidence records, so the experiments a reviewer reads and the experiments L0 runs are one definition.\n- The Executor is named by role, `agent:executor`. The plan, the threat model, and the `--principal` help text follow. CHG-000's merged records keep the id they were written with.\n\n**CHG-001.2, deny list and escape hatches.**\n\n- Experiment 6, fourteen runs, records what switches the deny list off and what closes it: `#[allow]`, `#[expect]`, `#![allow]`, and an allow flag in `RUSTFLAGS` all silence it; a crate-level `#![forbid]` or `forbid` in the lint table turns the attributes into `E0453`; `--cap-lints=warn` lowers even a forbidden finding. Experiment 6n shows the same flag works from `.cargo/config.toml`, which is not a protected surface.\n- The template grows from 15 entries to 111, closing the gaps ADR-0002 named. Ten of the additions are the paths that record listed; the rest come from a sweep of the Rust 1.98.1 standard-library source and two adversarial reviews of the result, all limited to the effects §6.8 names. ADR-0002 carries the per-category table, the candidates left for Kennedy, and what cannot be reached at all. Experiment 4 shows every entry resolve and fire.\n- `std::env::set_var` and `std::env::remove_var` are `unsafe` in edition 2024, so the fixture names them instead of calling them. The lint fires on the path reference, which is how both entries are demonstrated.\n\n**CHG-001.3, acceptance, the escape, and the front door.** Kennedy asked for the acceptance records, the recommended decisions, the findings in the specification, and a README worth reading.\n\n- [`.rha/acceptances/CHG-000.toml`](../../.rha/acceptances/CHG-000.toml) records the acceptance Kennedy made on 2026-09-19 by merging PR 1 and tagging `v0.10-m0`: the subject, the evidence, the outcomes as recorded, the four predicates, and the six concerns it carries forward rather than closes. No acceptance record is written for CHG-001, which is neither merged nor accepted.\n- **The recommended `forbid` was refused by the toolchain.** Set on the three lints in the root `Cargo.toml`, it stopped `xtask` compiling with 15 `E0453` errors, all from `clap`'s derives, which expand to `#[allow(clippy::style)]`. Experiments 7a and 7b reproduce the mechanism without a dependency; a `serde` derive is unaffected. The escape is closed per core crate instead, by a crate-root `#![forbid(...)]` that experiment 7c shows costs clean code nothing, and the rule reports a crate root that leaves any of the three unforbidden.\n- `.cargo/config.toml` joins the protected surfaces in `.rha/policy.toml`, because `--cap-lints=warn` there disables every lint at once.\n- Spec §6.8 asked the implementer to verify how the pinned Clippy finds and merges configuration files. It is verified. The first edit put the five observations in §6.8 as a table, which duplicated ADR-0002 in a second editable place, against the one-owner rule of §11.7.1 and the defect §12.1 records against v0.7. Corrected on the same day: §6.8 keeps the normative consequence in its Hole column and a sentence saying where the observations live, and the proposals that would change its text are queued in [`docs/proposals/spec-v0.11.md`](../proposals/spec-v0.11.md) for the version bump. That file also states the working rule it came from: a work item records findings in a decision record and evidence, the specification changes in one item per version, and a claim the specification makes about this repository gets a check. The §1.4 maturity table is untouched.\n- The README and the GitHub description describe the project, what works today, and what is only specified.\n\n**Removed, weakened, or reinterpreted tests.** The two fixture drift tests are removed with the copies they guarded: `fixtures_copy_the_root_lint_tables_and_root_clippy_file` had nothing left to compare, and the rule's `copies_of_the_template_conform`, renamed `a_copy_of_the_template_conforms`, now writes its own crate directories. No behavioural check was weakened; the corpus went from 4 tests to 7 and from 7 experiments to 21.\n\n**Beyond the plan's three experiments,** each added to make one of them decisive or to answer a listed unknown: 3c (control), 3b (the fix for 3), 4 (every entry), 5 (the plan's unknown, `clippy.toml` beside `.clippy.toml`), and 6a to 6n (what a crate or its configuration can do to the deny list).\n\n## Evidence\n\n**Experiments.** Twenty-six experiments, each recorded with its literal command, the digests of every generated file, the exit status, and the captured output, in [`evidence/w1-clippy/20260919T231239Z-8af79cb0f76a/`](../../evidence/w1-clippy/20260919T231239Z-8af79cb0f76a/), written from the generated corpus at commit `8af79cb0f76ad5ea0386f3a2af85dc9b65dd48b1` with a clean working tree. That commit carries the template this record describes, so each record's digest of `crates/core-every/clippy.toml` equals the template's. Two earlier sets are superseded: one at `edd4bac` against an 80-entry template, and one at `bf5aed9` that predates experiments 7a to 7c. [`evidence/w1-clippy/`](../../evidence/w1-clippy/) also keeps the seven records of the first run, at commit `28ae543c297067ad5171654f490307b13995376a`, which the shell script produced before CHG-001.1 replaced it. ADR-0002 tabulates the experiments and states what they mean. `xtask/tests/clippy_corpus.rs` reruns all of them in every `L0.nextest` run, locally and on the GitHub runner.\n\n**L0 records.** Two matter, and they differ in one predicate. [`evidence/CHG-001/20260919T224504Z-bae4b34894a2.json`](../../evidence/CHG-001/20260919T224504Z-bae4b34894a2.json) covers the deny-list work under the unchanged policy, where the digest equals the base policy's and **Applicable holds**. [`evidence/CHG-001/20260919T231351Z-602d2f953d0b.json`](../../evidence/CHG-001/20260919T231351Z-602d2f953d0b.json) covers the head of this change, after `.cargo/config.toml` joined the protected list, and reports **Applicable false**, naming the two digests. Both are class `local`, principal `agent:executor`, on a clean working tree. The table below is the later one.\n\n| Check | Outcome | Detail |\n| --- | --- | --- |\n| `L0.fmt` | passed | |\n| `L0.clippy` | passed | |\n| `L0.nextest` | passed | 33 tests, including the 7 corpus tests and 6 rule tests |\n| `L0.doctest` | passed | 1 doctest |\n| `L0.architecture` | **not_run** | `not_implemented` (stub, exit 4) |\n| `L0.deny` | passed | |\n| `L0.machete` | passed | |\n| `L0.typos` | passed | the corpus sources are excluded by `_typos.toml` |\n\nEligibility `blocked`: Authentic (no trusted producers), Applicable (the policy changed in this contribution), and Passed (`L0.architecture` not_run). Complete holds.\n\n**CI records.** Every push to the branch ran the lane on the GitHub runner, image `ubuntu24`, class `ci`. Two runs are committed, and each covers a different revision. Run [35470532056](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35470532056), copied as [`evidence/ci/35470532056.json`](../../evidence/ci/35470532056.json), covers the W1 head `d011d13` and its 28 tests; it predates the rework, so it says nothing about the escape hatches. Run [35474230710](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35474230710), copied as [`evidence/ci/35474230710.json`](../../evidence/ci/35474230710.json), covers PR head `1d65ce72e8c52ddcadd9354795fad3c163d56fec`, the deny-list work. Run [35475553364](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35475553364), copied as [`evidence/ci/35475553364.json`](../../evidence/ci/35475553364.json), covers PR head `df8ec0fc773bcc4f2a106b414540bffc4bd14808`. Run [35476272071](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35476272071), copied as [`evidence/ci/35476272071.json`](../../evidence/ci/35476272071.json), covers PR head `5c24f54272eb8d90c878bf43ff96daf67c87d551`, the head this record describes, and shares its tree. Its outcomes match the local record, so the discovery rule and the escape-hatch results reproduce on a second machine.\n\n**Performance.** No trigger; nothing claimed.\n\n### Repair attempts (§11.7.8)\n\n1. **Experiment 3b was not decisive.** In the first scripted run, at `a152d94`, 3b ran against a crate that also held the seeded clock call; that build failed, so the absent `unwrap_used` was weak evidence. Hypothesis: a crate with the template and nothing on the deny list would give a clean exit. Change: a separate fixture crate. Result: exit 0 and no `unwrap_used` (commit `28ae543`).\n2. **Experiment 5 exposed a silent override.** Observation, not a repair: with both files present, `.clippy.toml` wins with only a warning. Change: `effect.core_clippy_template` also reports `Problem::Shadowed`. CHG-001.1 made the experiment decisive by putting the deny list in `clippy.toml`, the root settings in `.clippy.toml`, and the seeded call in the crate: the call passes.\n3. **Helper `unwrap()` warnings in `xtask/tests/clippy_corpus.rs`.** The root test allowances cover `#[test]` functions and `#[cfg(test)]` modules, not free helper functions in an integration test file. The helpers panic with a message that names the file or fixture.\n4. **The lane caught the rule code.** At `043d7c1`, `L0.clippy` failed: the new crate-root check ended a closure's mutable borrow with `drop(finding)`, which `clippy::drop_non_drop` rejects because a closure implements no `Drop`. Hypothesis: a block scope would end the borrow without the call. Result: the lane passes at `602d2f9`. The record of the failing run was deleted rather than committed, which was the wrong call in a repository that keeps what it observes; this paragraph stands in its place.\n\n## Acceptance concerns\n\n1. **Still not Eligible.** `L0.architecture` is `not_run` (not implemented until CHG-003), and it is required and non-waivable under DP-0.5. Kennedy decided on 2026-09-19 to leave it `not_run` rather than move a minimal checker into W1: it would pass over zero core crates, it would not change eligibility while Authentic fails, and it would put checker code before the corpus pre-registration of W2. The options in CHG-000's acceptance concern 2 are unchanged, and the policy was not edited.\n2. **The policy changed in this contribution, so Applicable no longer holds.** Adding `.cargo/config.toml` to the protected list changes the policy digest, and a record is Applicable only where the candidate's policy equals the base revision's (§11.7.6). The L0 record of CHG-001.2, taken before that edit, is the one that holds under the unchanged policy; the record taken after it reports Applicable false with that reason. §11.5 calls this a controlled transition, and it is yours to accept.\n3. **The specification carries two sentences of this change.** §6.8's Hole column states the consequence, and one sentence says where the observations live; everything else is queued for v0.11. The §1.4 maturity table is untouched, because those rows are yours to promote at a version bump from `docs/maturity.md`. Reverting either sentence costs nothing but the text.\n4. **The attribute escape is closed by convention plus a rule, not by the compiler.** A core crate that carries the crate-root `#![forbid(...)]` cannot allow these lints anywhere, including its own tests; a core crate that omits the line is reported by `effect.core_clippy_template`, which does not run in L0 until CHG-003. Until then nothing enforces the line, and no core crate exists to carry it.\n5. **What the deny list cannot reach.** Direct uses only: not a call through another crate, not one the compiler generates from a macro. `HashMap::new` seeds itself from `RandomState` without naming it, so iteration-order nondeterminism stays out of reach. Platform extension traits and unstable APIs are left out on purpose (ADR-0002).\n6. **The rule is not yet in L0.** `effect.core_clippy_template` is exercised only by its unit tests until CHG-003 calls it from `cargo xtask architecture`.\n7. **Held-out cases.** None exist for this change. The fixtures and the expected rule were written by the Executor; the expectation was written into the task record before the experiments ran. The deny-list additions were proposed by advisory subagents reading the standard-library source, and every one of them is checked by experiment 4, which fails if an entry stops firing.\n\n## Acceptance (CHG-001.4)\n\nKennedy accepted this change at `44b1e44`, the merge commit of [pull request 2](https://github.com/kmosoti/recursive-hexagonal-architecture/pull/2), on 2026-09-19. The record is [`.rha/acceptances/CHG-001.toml`](../../.rha/acceptances/CHG-001.toml), written by the Executor at his instruction, which is also the approval for that protected surface.\n\nThe merge added nothing of its own: `44b1e44^{tree}` equals `a31cdd1^{tree}`, both `336f32c8`, and `git diff a31cdd1 44b1e44` is empty.\n\n**The evidence that covers the accepted tree.** The records committed before the merge stop short of it. The last local record, [`evidence/CHG-001/20260919T232940Z-78fe0093c4a4.json`](../../evidence/CHG-001/20260919T232940Z-78fe0093c4a4.json), was taken at `78fe009`, two commits before the head; run 35476272071 covers head `5c24f54`. CHG-001.4 therefore downloads the `rha-evidence` artifact of run [35476379661](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35476379661), the run on head `a31cdd1`, and commits it as [`evidence/ci/35476379661.json`](../../evidence/ci/35476379661.json). Its `artifact_identity.tree` is `336f32c8968d8f9b523cae4d96f168ba59c485c9`, equal to the accepted tree, so the lane ran on exactly the content now on `main`; its `revision` is GitHub's ephemeral merge commit `5343431`, which no longer exists as a branch. Seven checks passed, `L0.architecture` is `not_run`, and all four predicates match the local record.\n\nThat the record arrives after the item it describes is itself a weakness, listed in the acceptance record's `carried_forward`: an acceptance whose evidence has to be fetched from a CI artifact is reproducible only while the artifact lives.\n\n**The transition closed.** Every cited record reports `base_policy_digest` `sha256:f102c03a…` against its own `policy_digest` `sha256:dc2ef7e0…`, so Applicable is false on all of them. A lane run at `44b1e44` reports both as `sha256:dc2ef7e0…` and Applicable true: the base is now the policy this change proposed. That run is reported in the working conversation and is not committed, so `main` carries no record of its own.\n\n### Defect found at acceptance\n\n**D-001.1, record integrity.** The task record shipped at `44b1e44` with the literal string `SPEC_DIGEST` where the sha256 of `docs/spec/rha-spec-v0.10.md` belonged, in the fifth `instruction_sources` entry. The record asserted a witness it did not hold, in the one field that lets a reader reproduce the Executor's inputs. Nothing caught it: no check reads task records, and the record-schema check that would reject a non-hex digest belongs to CHG-019. CHG-001.4 fills in `bd96216829035ade64c43befa5e672c58078f6afa4b04145c6183227e4e1071a`, verifiable with `git show 44b1e44:docs/spec/rha-spec-v0.10.md | sha256sum`, and the acceptance record states the defect in full.\n"
  },
  "docs/changes/CHG-002-corpus-pre-registration.md": {
    "body": "[Heading { level: 1, slug: \"chg-002-pre-registered-h4-corpus-manifest\", children: [Text(\"CHG-002: Pre-registered H4 corpus manifest\")] }, Paragraph([Text(\"Task record: \"), Link { href: \"../../.rha/tasks/CHG-002-corpus-pre-registration.toml\", children: [Code(\".rha/tasks/CHG-002-corpus-pre-registration.toml\")] }, Text(\".\"), SoftBreak, Text(\"Plan: \"), Link { href: \"../plan/IMPLEMENTATION-PLAN.md\", children: [Text(\"§6 and §8 W2\")] }, Text(\".\"), SoftBreak, Text(\"Base: \"), Code(\"44b1e44\"), Text(\", the merge commit that accepted CHG-001.\")]), Heading { level: 2, slug: \"intent-and-scope\", children: [Text(\"Intent and scope\")] }, Paragraph([Text(\"Commit the corpus that judges the crate-graph checker \"), Strong([Text(\"before the checker exists\")]), Text(\". Spec §17 fixes cases and thresholds before data; §9.14 forbids weakening a test quietly. Both are easy to say and easy to lose: once a checker runs, every case it fails looks like a case that was written wrong. Pre-registration removes that option by putting the expectations on the record first, and the merge commit of this change is the identity every H4 evidence record cites.\")]), Paragraph([Text(\"Sixty-two cases, transcribed from plan §6:\")]), Table { align: [None, None, None, None, None, None], head: [[Text(\"Level\")], [Text(\"detect\")], [Text(\"no_alarm\")], [Text(\"expected_miss\")], [Text(\"reference\")], [Text(\"total\")]], rows: [[[Text(\"Crate\")], [Text(\"22 (21 for the checker, plus R01 for rustc)\")], [Text(\"11\")], [Text(\"2\")], [Text(\"–\")], [Strong([Text(\"35\")])]], [[Text(\"Module\")], [Text(\"17\")], [Text(\"6\")], [Text(\"3\")], [Text(\"1\")], [Strong([Text(\"27\")])]]] }, Paragraph([Text(\"The module cases are registered here and built in CHG-007; only their ids, rules, cells and expectations are fixed now, which is all pre-registration requires.\")]), Paragraph([Strong([Text(\"Non-goals, each checked by a test.\")]), Text(\" No checker code: \"), Code(\"xtask/src/architecture.rs\"), Text(\" is still the stub that reports \"), Code(\"not_run\"), Text(\", and no \"), Code(\"xtask/src/graph\"), Text(\" module exists. No fixture workspaces under \"), Code(\"xtask/tests/corpus/crate/\"), Text(\" or \"), Code(\"module/\"), Text(\". The last test in \"), Link { href: \"../../xtask/tests/corpus_manifest.rs\", children: [Code(\"xtask/tests/corpus_manifest.rs\")] }, Text(\" asserts all three, so a later change cannot quietly turn this item into W3.\")]), Heading { level: 2, slug: \"deltas\", children: [Text(\"Deltas\")] }, Paragraph([Strong([Link { href: \"../../xtask/tests/corpus/manifest.toml\", children: [Code(\"xtask/tests/corpus/manifest.toml\")] }]), Text(\" — the corpus. Each case carries \"), Code(\"id\"), Text(\", \"), Code(\"level\"), Text(\", \"), Code(\"generation\"), Text(\", \"), Code(\"expected\"), Text(\", \"), Code(\"rule\"), Text(\", \"), Code(\"cell\"), Text(\", \"), Code(\"seeded\"), Text(\", and a \"), Code(\"witness\"), Text(\".\")]), Paragraph([Code(\"cells\"), Text(\" names the rows of spec §4.1's enforcement map a case is evidence for, so a miss downgrades named cells rather than an unnamed feeling. It is a list because one seeded violation can carry more than one claim (CHG-002.1). The manifest adds one id the spec's table does not have, \"), Code(\"classification\"), Text(\", with a comment saying why: total classification (§11.2) is not a §4.1 row but the precondition for every row, and a member the checker cannot classify is a hole in all of them at once. C10, C11 and C20 sit there.\")]), Paragraph([Code(\"witness\"), Text(\" states what a finding must name. A rule id alone would score C01 for a checker that reported \"), Code(\"dir.core_to_adapter\"), Text(\" about the wrong pair of crates, so C01's witness is \"), Code(\"{ rule, from = \\\"core-a\\\", to = \\\"adapter-x\\\" }\"), Text(\", C20's names a crate and a port, and R01's names an error code.\")]), Paragraph([Strong([Text(\"Generated, not committed.\")]), Text(\" \"), Code(\"generation = \\\"declared\\\"\"), Text(\" on all 35 crate-level cases means CHG-004 writes each fixture workspace from this file: \"), Code(\"crates\"), Text(\" gives the members, their \"), Code(\"role\"), Text(\" metadata, and their dependencies with \"), Code(\"kind\"), Text(\", \"), Code(\"source\"), Text(\", \"), Code(\"version\"), Text(\", \"), Code(\"path\"), Text(\", \"), Code(\"target\"), Text(\", \"), Code(\"optional\"), Text(\", \"), Code(\"rename\"), Text(\" and \"), Code(\"inherit\"), Text(\". This is the CHG-001.1 decision applied before the drift exists — a committed fixture is a copy of the file that owns it, and the copy goes stale silently.\")]), Paragraph([Text(\"The 27 module cases are \"), Code(\"generation = \\\"authored\\\"\"), Text(\": their seeded construct \"), Emphasis([Text(\"is\")]), Text(\" Rust syntax (\"), Code(\"impl crate::ordering::Trait for X\"), Text(\", a macro body, an \"), Code(\"include!\"), Text(\"), and encoding that in TOML would be a worse copy than the Rust. CHG-007 writes them.\")]), Paragraph([Strong([Link { href: \"../../xtask/src/corpus.rs\", children: [Code(\"xtask/src/corpus.rs\")] }]), Text(\" — the manifest's types and its validation. No runner. \"), Code(\"Manifest::defects()\"), Text(\" reports a repeated id, an unknown cell, a detection with no rule or no witness, an expected miss citing no §4.1 hole, a declared case with no crates, and a dependency on a crate the generator could not write. \"), Code(\"#[serde(deny_unknown_fields)]\"), Text(\" throughout, so a typo in a field name is an error rather than a case silently missing that field.\")]), Paragraph([Strong([Link { href: \"../../xtask/tests/corpus_manifest.rs\", children: [Code(\"xtask/tests/corpus_manifest.rs\")] }]), Text(\" — nine tests pinning the manifest to plan §6: the id set, the outcome counts, every rule id drawn from the plan's list, no defects, every crate-level case generatable, the four dependency shapes of C15–C18 each actually declared, C10's absent metadata distinguished from C11's conflicting metadata, and the grading rules stating what a disagreement does.\")]), Paragraph([Text(\"The plan's id list is transcribed into the test a second time on purpose. Reading it out of the manifest would make the test agree with whatever the manifest says.\")]), Paragraph([Strong([Link { href: \"../../.rha/policy.toml\", children: [Code(\".rha/policy.toml\")] }]), Text(\" — one entry adding the manifest to \"), Code(\"[surface] protected\"), Text(\", pre-approved by Kennedy. A corpus that is not protected can have an expectation edited after the fact, which is the one thing pre-registration exists to prevent.\")]), Heading { level: 2, slug: \"what-the-cases-are-for\", children: [Text(\"What the cases are for\")] }, Paragraph([Text(\"Four groups do work beyond covering a rule.\")]), Paragraph([Strong([Text(\"C15–C18 are one violation, four times.\")]), Text(\" \"), Code(\"core-a → adapter-x\"), Text(\" hidden behind a target \"), Code(\"cfg\"), Text(\", an \"), Code(\"optional\"), Text(\", a rename, and a workspace inherit. They exist to fail a checker that parses \"), Code(\"Cargo.toml\"), Text(\" text instead of reading \"), Code(\"cargo metadata\"), Text(\"'s resolved fields — the shortcut W3 will be tempted by. C17 additionally requires the finding to name the package \"), Code(\"adapter-x\"), Text(\", not the rename \"), Code(\"ax\"), Text(\".\")]), Paragraph([Strong([Text(\"L07 and M11 expect a report that is not an alarm.\")]), Text(\" \"), Code(\"core-a\"), Text(\"'s dev-dependency on \"), Code(\"adapter-x\"), Text(\" is legitimate — a crate's own tests are a harness — but it must still be \"), Emphasis([Text(\"listed\")]), Text(\", under \"), Code(\"harness_edges\"), Text(\". Their witness carries \"), Code(\"listed_as\"), Text(\" rather than \"), Code(\"rule\"), Text(\", and \"), Code(\"[grading].no_alarm_scope\"), Text(\" says a run that raises it as a finding fails the case. Silence and a listed fact are different outcomes.\")]), Paragraph([Strong([Text(\"L11 guards the checker against itself.\")]), Text(\" \"), Code(\"planner-adapter-utils\"), Text(\" declares \"), Code(\"role = \\\"core\\\"\"), Text(\" and contains the string \"), Code(\"adapter-\"), Text(\" without starting with it. A checker matching the prefix as a substring reports C11's conflict here and fails.\")]), Paragraph([Strong([Text(\"EM-C02 records which mechanism covers a cell, not a failure.\")]), Text(\" \"), Code(\"core-a\"), Text(\" calls \"), Code(\"std::fs::read\"), Text(\" and declares no dependency: invisible to the crate graph, and caught by CHG-001's Clippy deny list at another level in another lane. Registering it keeps the enforcement map honest about where the coverage actually comes from.\")]), Heading { level: 2, slug: \"grading-and-what-is-still-open\", children: [Text(\"Grading, and what is still open\")] }, Paragraph([Code(\"[grading]\"), Text(\" is what stops H4 grading itself generously. Three values are the Executor's proposal and are \"), Strong([Text(\"open at DP-1.1\")]), Text(\":\")]), List { start: None, items: [[Code(\"detection_requires\"), Text(\" — every key of the case's \"), Code(\"witness\"), Text(\" table must equal the finding's corresponding field. There is no exempt list, because prose lives in \"), Code(\"witness_notes\"), Text(\" (CHG-002.1).\")], [Code(\"extra_findings\"), Text(\" — the case counts as detected, and every unmatched finding on that fixture is counted in the run's \"), Code(\"false_alarm\"), Text(\" total. Recall and precision are scored separately so an over-eager rule is neither hidden by a case it gets right nor punished twice for one defect.\")], [Code(\"no_alarm_scope\"), Text(\" — any finding on a legitimate fixture fails the case, whatever its rule, except a report carrying \"), Code(\"listed_as\"), Text(\".\")]] }, Paragraph([Text(\"Two more are not open, because they follow from §17 and §9.14, and both tests assert their wording: a \"), Code(\"detect\"), Text(\" case that is not detected fails the run and downgrades the §4.1 cell, and \"), Strong([Text(\"never edits this file\")]), Text(\"; an \"), Code(\"expected_miss\"), Text(\" that \"), Emphasis([Text(\"is\")]), Text(\" detected is escalated, not silently re-registered.\")]), Heading { level: 2, slug: \"evidence\", children: [Text(\"Evidence\")] }, Paragraph([Strong([Text(\"Local.\")]), Text(\" Five records under \"), Code(\"evidence/CHG-002/\"), Text(\", and all five are kept (this list was corrected in CHG-003.2; it had named three and \"), Code(\"13f665c\"), Text(\" as the head, a pull request 4 review finding):\")]), Table { align: [None, None, None], head: [[Text(\"Record\")], [Text(\"Revision\")], [Text(\"Result\")]], rows: [[[Code(\"20260920T001651Z-24ad1602720b.json\")], [Code(\"24ad160\")], [Code(\"L0.fmt\"), Text(\" \"), Strong([Text(\"failed\")]), Text(\"; repair attempt 1\")]], [[Code(\"20260920T001738Z-038a8ea2ba1c.json\")], [Code(\"038a8ea\")], [Text(\"7 passed, \"), Code(\"L0.architecture\"), Text(\" not_run, 50 tests; superseded\")]], [[Code(\"20260920T002204Z-13f665cb0893.json\")], [Code(\"13f665c\")], [Text(\"7 passed, \"), Code(\"L0.architecture\"), Text(\" not_run, 50 tests; the head before review\")]], [[Code(\"20260920T010039Z-cfb048e479fb.json\")], [Code(\"cfb048e\")], [Text(\"7 passed, \"), Code(\"L0.architecture\"), Text(\" not_run, 53 tests; after the CHG-002.1 determinations\")]], [[Code(\"20260920T011724Z-6518f6a89af5.json\")], [Code(\"6518f6a\")], [Text(\"7 passed, \"), Code(\"L0.architecture\"), Text(\" not_run, 54 tests; \"), Strong([Text(\"the head of the item\")]), Text(\", after the outside-crate repair\")]]] }, Paragraph([Text(\"A fourth run, at \"), Code(\"af1e5077\"), Text(\", failed \"), Code(\"L0.typos\"), Text(\" on the sentence repair attempt 2 was in the middle of correcting; its record is not committed, and repair attempt 2 says why.\")]), Paragraph([Strong([Text(\"CI.\")]), Text(\" Run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35478601881\", children: [Text(\"35478601881\")] }, Text(\" on head \"), Code(\"5972d91\"), Text(\", copied as \"), Link { href: \"../../evidence/ci/35478601881.json\", children: [Code(\"evidence/ci/35478601881.json\")] }, Text(\". Subject revision \"), Code(\"68b41dca\"), Text(\", GitHub's ephemeral merge commit. Seven passed, \"), Code(\"L0.architecture\"), Text(\" \"), Code(\"not_run\"), Text(\", 50 tests selected, and all four predicates agree with the local record. The earlier CI run 35478425099 failed \"), Code(\"L0.typos\"), Text(\" and is not committed; repair attempt 2 cites it. After the CHG-002.1 repairs: run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35480297907\", children: [Text(\"35480297907\")] }, Text(\", subject \"), Code(\"1c9f7e1\"), Text(\", 53 tests; run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35481014407\", children: [Text(\"35481014407\")] }, Text(\", subject \"), Code(\"6b2784b\"), Text(\", 54 tests, the last run on the branch; and run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35481271471\", children: [Text(\"35481271471\")] }, Text(\" on \"), Code(\"main\"), Text(\" at \"), Code(\"15d916a\"), Text(\", 54 tests, which \"), Code(\".rha/acceptances/CHG-002.toml\"), Text(\" cites. All three report seven passed and \"), Code(\"L0.architecture\"), Text(\" \"), Code(\"not_run\"), Text(\"; the two branch subjects are GitHub's ephemeral merge commits. (Added in CHG-003.2.)\")]), Paragraph([Code(\"L0.architecture\"), Text(\" is \"), Code(\"not_run\"), Text(\" and is reported as \"), Code(\"not_run\"), Text(\". Nothing here is reported passed that did not run.\")]), Paragraph([Strong([Text(\"The policy changed again, so Applicable is false again.\")]), Text(\" Adding the manifest to the protected list moves the policy digest from \"), Code(\"sha256:dc2ef7e0…\"), Text(\" to \"), Code(\"sha256:5497c29e…\"), Text(\", and a record is Applicable only where the candidate's policy equals the base revision's (§11.7.6). This is the second controlled transition in this repository; the first, CHG-001's, closed the moment it merged. Spec §11.5 makes it yours to accept.\")]), Heading { level: 3, slug: \"repair-attempts-1178\", children: [Text(\"Repair attempts (§11.7.8)\")] }, List { start: Some(1), items: [[Paragraph([Strong([Code(\"L0.fmt\"), Text(\" failed at \"), Code(\"24ad160\"), Text(\".\")]), Text(\" The first lane run on this branch reported \"), Code(\"L0.fmt failed: exited with 1\"), Text(\". Hypothesis: \"), Code(\"xtask/src/corpus.rs\"), Text(\" and \"), Code(\"xtask/tests/corpus_manifest.rs\"), Text(\" were written by hand and never formatted, and this toolchain's rustfmt reformats a chained \"), Code(\"assert!(expr.method()...)\"), Text(\" into a block form. Discriminating check: \"), Code(\"cargo fmt --all -- --check\"), Text(\", which named four sites in \"), Code(\"corpus.rs\"), Text(\" and one import ordering in \"), Code(\"corpus_manifest.rs\"), Text(\". Change: \"), Code(\"cargo fmt --all\"), Text(\", which rewrote 32 lines across the two files and reordered \"), Code(\"use xtask::corpus::{…, MANIFEST_PATH, Manifest}\"), Text(\" to the edition 2024 ordering. Result: \"), Code(\"cargo fmt --all -- --check\"), Text(\" is silent and the lane passes \"), Code(\"L0.fmt\"), Text(\" at \"), Code(\"4d5a48e\"), Text(\". The failing record is committed under \"), Code(\"evidence/CHG-002/\"), Text(\", not deleted — CHG-001's repair attempt 4 deleted one and said so; this is the corrected practice.\")])], [Paragraph([Strong([Code(\"L0.typos\"), Text(\" failed in CI at \"), Code(\"9e7bb89\"), Text(\" and had passed locally.\")]), Text(\" CI run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35478425099\", children: [Text(\"35478425099\")] }, Text(\" reported \"), Code(\"L0.typos failed: exited with 2\"), Text(\", on \"), Code(\"docs/evidence/index.md\"), Text(\": the generated index lists the record filename \"), Code(\"20260920T001738Z-038a8ea2ba1c.json\"), Text(\", and typos splits a hex digest on its digits and reads each letter run as a word. The two-letter run before \"), Code(\"1c\"), Text(\" is read as a misspelling of \\\"by\\\" or \\\"be\\\".\")]), Paragraph([Text(\"Hypothesis: the local run did not see it because of the order the commands were run in, not because the two machines differ. The lane ran, \"), Emphasis([Text(\"then\")]), Text(\" \"), Code(\"cargo xtask docs\"), Text(\" regenerated the index to include that very record, and the lane was never re-run. Discriminating check: \"), Code(\"typos\"), Text(\" on the working tree, which reproduced the CI failure exactly, and \"), Code(\"typos --version\"), Text(\", which reports 1.50.2 on both — the same tool, a different input.\")]), Paragraph([Text(\"Change: an \"), Code(\"extend-ignore-re\"), Text(\" of \"), Code(\"\\\\b[0-9a-f]{12,}\\\\b\"), Text(\" in \"), Link { href: \"../../_typos.toml\", children: [Code(\"_typos.toml\")] }, Text(\", which is not a protected surface. Excluding \"), Code(\"docs/evidence/index.md\"), Text(\" was the alternative and was rejected: digests also appear in change records, task records and acceptance records, so excluding one generated file moves the problem rather than solving it, and it would stop spell-checking that file's prose. Twelve is the shortest hex run this repository uses, the git short revision in a record filename, and no English word is twelve hex characters long.\")]), Paragraph([Text(\"Result: \"), Code(\"typos\"), Text(\" exits 0 on the tree. A probe file inside the repository holding one deliberately misspelled common word, a 12-character digest and a 40-character revision still reports the misspelling and ignores both digests, so the rule masks the false positive and nothing else. The first probe was written to a scratch directory outside the repository, where \"), Code(\"_typos.toml\"), Text(\" does not apply, and proved nothing; it was re-run inside the tree.\")]), Paragraph([Strong([Text(\"Process finding, not a tool finding.\")]), Text(\" A lane result is only about the tree that existed when it ran. \"), Code(\"cargo xtask docs\"), Text(\" changes tracked files, so running it after the lane invalidates the record the lane just wrote. CONTRIBUTING tells contributors to regenerate docs after changing \"), Code(\".rha/**\"), Text(\" or \"), Code(\"evidence/**\"), Text(\"; it does not say to re-run the lane afterwards. Queued for the v0.11 proposals as a workflow correction.\")])]] }, Heading { level: 2, slug: \"chg-0021-review-findings-and-their-determinations\", children: [Text(\"CHG-002.1: review findings and their determinations\")] }, Paragraph([Text(\"Requested by Kennedy on the open pull request, as eight commits. The Codex review of commits \"), Code(\"9e7bb89\"), Text(\" and \"), Code(\"8dc1969\"), Text(\" raised six findings, and a seventh P1 against the CHG-002.1 commits themselves. All seven are accepted and fixed. One further change is Kennedy's own.\")]), Table { align: [None, None, None, None], head: [[Text(\"#\")], [Text(\"Source\")], [Text(\"Finding\")], [Text(\"Determination\")]], rows: [[[Text(\"1\")], [Text(\"Codex \"), Strong([Text(\"P1\")])], [Emphasis([Text(\"Keep the transitive dependency out of the core member set.\")]), Text(\" EM-C01's \"), Code(\"pure-looking\"), Text(\" was a workspace member with \"), Code(\"role = \\\"core\\\"\"), Text(\" and a direct, un-allow-listed \"), Code(\"tokio\"), Text(\" dependency, so the checker would report an ordinary \"), Code(\"effect.core_disallowed_dependency\"), Text(\" about it.\")], [Strong([Text(\"Accepted, fixed.\")]), Text(\" The finding is correct and it invalidated the case: a real detection of a \"), Emphasis([Text(\"different\")]), Text(\" rule would have made the fixture non-silent, so it would no longer demonstrate that the transitive edge is invisible. \"), Code(\"pure-looking\"), Text(\" and \"), Code(\"tokio\"), Text(\" are now \"), Code(\"outside_crates\"), Text(\" reached by path, classified by nothing.\")]], [[Text(\"2\")], [Text(\"Codex \"), Strong([Text(\"P1\")])], [Emphasis([Text(\"Encode the Rust source required by R01.\")]), Text(\" Neither crate declared a body, so a generated R01 workspace would compile and the registered \"), Code(\"E0603\"), Text(\" could never fire.\")], [Strong([Text(\"Accepted, fixed.\")]), Text(\" Both bodies are declared. Verified by building the pair outside the repository: rustc 1.98.1 reports \"), Code(\"error[E0603]: module 'private_mod' is private\"), Text(\" at \"), Code(\"core-b/src/lib.rs\"), Text(\", so the witness holds against a real run.\")]], [[Text(\"3\")], [Text(\"Codex \"), Strong([Text(\"P1\")])], [Emphasis([Text(\"Match every outcome-bearing witness field.\")]), Text(\" \"), Code(\"detection_requires\"), Text(\" matched \\\"every key naming a crate, port, module path or error code\\\", which omits \"), Code(\"kind\"), Text(\", \"), Code(\"matched_rule\"), Text(\", \"), Code(\"depth\"), Text(\" and \"), Code(\"extraction\"), Text(\"; M20 could be scored detected by a checker reporting the cycle as exact.\")], [Strong([Text(\"Accepted, fixed differently than proposed.\")]), Text(\" Codex asked for the enumeration to be completed. Completing it would be correct today and silently wrong the next time a witness key is added. Instead \"), Code(\"witness\"), Text(\" now holds only matched keys and prose moved to \"), Code(\"witness_notes\"), Text(\", so position decides what is matched. \"), Code(\"Defect::ProseInWitness\"), Text(\" stops the split regressing.\")]], [[Text(\"4\")], [Text(\"Codex \"), Strong([Text(\"P2\")])], [Emphasis([Text(\"Pin each expected outcome to its case ID.\")]), Text(\" Aggregate counts cannot see a swap: flipping C01 to \"), Code(\"no_alarm\"), Text(\" and L01 to \"), Code(\"detect\"), Text(\" leaves every count identical.\")], [Strong([Text(\"Accepted, fixed, and verified against the stated scenario.\")]), Text(\" With exactly that swap applied, the counts test still passes and the new \"), Code(\"PINNED\"), Text(\" test fails naming both cases.\")]], [[Text(\"5\")], [Text(\"Codex \"), Strong([Text(\"P2\")])], [Emphasis([Text(\"Attribute core-to-adapter cases to the D1 cell.\")]), Text(\" C01 and C15–C18 named \"), Code(\"law6-d5\"), Text(\" alone, so a miss would downgrade the D5 claim and leave D1 credited.\")], [Strong([Text(\"Accepted, fixed more broadly than proposed.\")]), Text(\" Codex asked to move the cases from D5 to D1. A core-to-adapter edge is genuinely evidence for both rows, so \"), Code(\"cell\"), Text(\" became \"), Code(\"cells\"), Text(\", a list, and those five name both. Moving them would have left the other claim falsely standing, in the opposite direction.\")]], [[Text(\"6\")], [Text(\"Codex \"), Strong([Text(\"P2\")])], [Emphasis([Text(\"Map the undeclared-dependency case to Law 3/D1.\")]), Text(\" M02 seeds a directional edge, not a cycle, yet named \"), Code(\"law6-b3\"), Text(\".\")], [Strong([Text(\"Accepted, fixed.\")]), Text(\" M02 names \"), Code(\"law3-d1\"), Text(\".\")]], [[Text(\"8\")], [Text(\"Codex \"), Strong([Text(\"P1\")])], [Emphasis([Text(\"Point dependencies at the actual outside-crate location.\")]), Text(\" A member's \"), Code(\"path = \\\"../outside-crate\\\"\"), Text(\" is resolved by cargo relative to the \"), Strong([Text(\"depending crate's\")]), Text(\" directory, so it lands at \"), Code(\"<workspace>/outside-crate\"), Text(\" — inside the workspace — while \"), Code(\"outside_crates.at\"), Text(\" means beside it. C19 could not load, and EM-C01's new \"), Code(\"core-a → pure-looking\"), Text(\" edge had the same fault.\")], [Strong([Text(\"Accepted, fixed structurally.\")]), Text(\" Codex offered two remedies: write \"), Code(\"../../\"), Text(\" in the dep, or have the generator normalise. The first hard-codes the depth of a layout the manifest does not define, and would break the moment a fixture nested differently. Instead \"), Code(\"Dep::path\"), Text(\" is removed: \"), Code(\"outside_crates.at\"), Text(\" is the single owner of an outside crate's location (§11.7.1), a dependency names the crate and nothing more, and the generator computes each relative path. All three uses of \"), Code(\"path\"), Text(\" named an outside crate, so the field was pure duplication — and the duplication is what let the two disagree.\")]], [[Text(\"7\")], [Strong([Text(\"Kennedy\")])], [Text(\"C05's witness carried an \"), Code(\"alternative\"), Text(\" letting a Cargo error be recorded as \"), Code(\"graph.cycle\"), Text(\".\")], [Strong([Text(\"Accepted, fixed.\")]), Text(\" It is the corpus grading itself generously — crediting the xtask DFS for an error it never produced, and the Law 6/B3 cell to a rule that did not run. Removed; C05 now requires the checker's own finding.\")]]] }, Paragraph([Text(\"Finding 8 arrived on the CHG-002.1 commits themselves and is a fault I introduced in finding 1's fix: moving \"), Code(\"pure-looking\"), Text(\" outside the workspace added a second member-to-outside edge with the same broken path. It also exposed a gap in the validation — an outside crate's own dependencies were not checked at all, so \"), Code(\"pure-looking\"), Text(\" could name a crate the generator had no instruction to write. \"), Code(\"Manifest::defects()\"), Text(\" now walks outside crates too, with a test.\")]), Paragraph([Strong([Text(\"Approval note.\")]), Text(\" Kennedy approved manifest edits in commits 3, 5 and 6. Finding 8's fix is a ninth commit and edits the manifest again, outside that approval, on the instruction to resolve open review conversations. It changes no case's expected outcome, rule, cells or seeded description — only the removal of a redundant \"), Code(\"path\"), Text(\" key from three dependency entries. It is flagged here so it can be reverted as easily as it was made.\")]), Paragraph([Strong([Text(\"What did not change.\")]), Text(\" No case's \"), Code(\"expected\"), Text(\" outcome was altered, no case was added or dropped, and no rule id changed. Findings 1, 5, 6 and 7 are pre-registration corrections that Kennedy approved explicitly, before any checker exists; findings 2 and 3 are structural, changing how a case is expressed rather than what it expects.\")]), Paragraph([Text(\"Findings 1, 2 and 7 are worth separating from the rest: each described a fixture that \"), Strong([Text(\"could not produce the result it was registered for\")]), Text(\". A corpus whose cases cannot fire is worse than no corpus, because W4 would have reported numbers for it.\")]), Heading { level: 3, slug: \"a-wrong-number-in-a-commit-message\", children: [Text(\"A wrong number in a commit message\")] }, Paragraph([Text(\"The commit that added the CHG-002.1 evidence record, \"), Code(\"976a541\"), Text(\", says \\\"55 tests selected\\\". The record it commits, \"), Code(\"evidence/CHG-002/20260920T010039Z-cfb048e479fb.json\"), Text(\", reports \"), Strong([Text(\"53\")]), Text(\": fifty in CHG-002 plus the three added by CHG-002.1, for \"), Code(\"Defect::ProseInWitness\"), Text(\", \"), Code(\"Defect::NoCells\"), Text(\", and the per-case pinning test.\")]), Paragraph([Text(\"Fifty-five was not read from anything. It is the same class of defect as D-001.1 in CHG-001's acceptance record — a number in the position of a witness that no run produced — and it is worse, because a digest placeholder is visibly a placeholder and a plausible count is not.\")]), Paragraph([Text(\"Corrected here rather than by amending the commit. Rewriting the message would leave no trace that the number had been wrong, and this repository already decided, in CHG-001's repair attempt 4, that deleting an inconvenient observation is the wrong move. The commit message stands; this paragraph is the correction, and the record is the authority.\")]), Heading { level: 2, slug: \"acceptance-concerns\", children: [Text(\"Acceptance concerns\")] }, List { start: Some(1), items: [[Strong([Text(\"DP-1.1 is the point of this item.\")]), Text(\" Kennedy reviews the manifest, and authors two or three held-out crate-level cases that live outside this repository. The Executor must not read or write them (§9.14). Every case here was written by the Executor from the plan; the held-out set is the only part of H4 that is not self-assessment, and without it W4's numbers measure the checker against expectations the same party wrote.\")], [Strong([Text(\"The three \"), Code(\"[grading]\"), Text(\" values are a proposal.\")]), Text(\" They are the strict reading in each case. If any is relaxed, the relaxation belongs here and in the manifest before CHG-004 runs, not after a result is known.\")], [Strong([Text(\"Still not Eligible.\")]), Text(\" \"), Code(\"L0.architecture\"), Text(\" is \"), Code(\"not_run\"), Text(\" and required and non-waivable under DP-0.5, unchanged since CHG-000's acceptance.\")], [Strong([Text(\"A registered case is not a working fixture.\")]), Text(\" Nothing has generated a workspace from this manifest yet. The declaration vocabulary is validated for internal consistency only; CHG-004 is where it meets \"), Code(\"cargo metadata\"), Text(\", and a case that cannot be expressed is re-registered with its reason \"), Emphasis([Text(\"before\")]), Text(\" that run, per the task record's \"), Code(\"replan_when\"), Text(\".\")], [Strong([Text(\"C05 may never reach the checker.\")]), Text(\" Cargo rejects a package cycle while loading the workspace, so the fixture may fail before \"), Code(\"cargo xtask architecture\"), Text(\" sees it. CHG-002.1 removed the \"), Code(\"alternative\"), Text(\" that would have let a Cargo error count as the checker's detection. If the checker reports nothing, C05 is a \"), Strong([Text(\"miss\")]), Text(\", and W4 re-registers it with a stated reason or rebuilds the fixture — in the open, before a run.\")], [Strong([Code(\"cell = \\\"classification\\\"\"), Text(\" is not a §4.1 row.\")]), Text(\" The manifest says so in a comment. If Kennedy would rather §4.1 gained a classification row, that is a v0.11 proposal, not an edit here.\")]] }]",
    "headings": "[Heading { level: 1, text: \"CHG-002: Pre-registered H4 corpus manifest\", slug: \"chg-002-pre-registered-h4-corpus-manifest\", base_slug: \"chg-002-pre-registered-h4-corpus-manifest\", line: 1 }, Heading { level: 2, text: \"Intent and scope\", slug: \"intent-and-scope\", base_slug: \"intent-and-scope\", line: 7 }, Heading { level: 2, text: \"Deltas\", slug: \"deltas\", base_slug: \"deltas\", line: 22 }, Heading { level: 2, text: \"What the cases are for\", slug: \"what-the-cases-are-for\", base_slug: \"what-the-cases-are-for\", line: 42 }, Heading { level: 2, text: \"Grading, and what is still open\", slug: \"grading-and-what-is-still-open\", base_slug: \"grading-and-what-is-still-open\", line: 54 }, Heading { level: 2, text: \"Evidence\", slug: \"evidence\", base_slug: \"evidence\", line: 64 }, Heading { level: 3, text: \"Repair attempts (§11.7.8)\", slug: \"repair-attempts-1178\", base_slug: \"repair-attempts-1178\", line: 84 }, Heading { level: 2, text: \"CHG-002.1: review findings and their determinations\", slug: \"chg-0021-review-findings-and-their-determinations\", base_slug: \"chg-0021-review-findings-and-their-determinations\", line: 98 }, Heading { level: 3, text: \"A wrong number in a commit message\", slug: \"a-wrong-number-in-a-commit-message\", base_slug: \"a-wrong-number-in-a-commit-message\", line: 121 }, Heading { level: 2, text: \"Acceptance concerns\", slug: \"acceptance-concerns\", base_slug: \"acceptance-concerns\", line: 130 }]",
    "markdown": "# CHG-002: Pre-registered H4 corpus manifest\n\nTask record: [`.rha/tasks/CHG-002-corpus-pre-registration.toml`](../../.rha/tasks/CHG-002-corpus-pre-registration.toml).\nPlan: [§6 and §8 W2](../plan/IMPLEMENTATION-PLAN.md).\nBase: `44b1e44`, the merge commit that accepted CHG-001.\n\n## Intent and scope\n\nCommit the corpus that judges the crate-graph checker **before the checker exists**. Spec §17 fixes cases and thresholds before data; §9.14 forbids weakening a test quietly. Both are easy to say and easy to lose: once a checker runs, every case it fails looks like a case that was written wrong. Pre-registration removes that option by putting the expectations on the record first, and the merge commit of this change is the identity every H4 evidence record cites.\n\nSixty-two cases, transcribed from plan §6:\n\n| Level | detect | no_alarm | expected_miss | reference | total |\n| --- | --- | --- | --- | --- | --- |\n| Crate | 22 (21 for the checker, plus R01 for rustc) | 11 | 2 | – | **35** |\n| Module | 17 | 6 | 3 | 1 | **27** |\n\nThe module cases are registered here and built in CHG-007; only their ids, rules, cells and expectations are fixed now, which is all pre-registration requires.\n\n**Non-goals, each checked by a test.** No checker code: `xtask/src/architecture.rs` is still the stub that reports `not_run`, and no `xtask/src/graph` module exists. No fixture workspaces under `xtask/tests/corpus/crate/` or `module/`. The last test in [`xtask/tests/corpus_manifest.rs`](../../xtask/tests/corpus_manifest.rs) asserts all three, so a later change cannot quietly turn this item into W3.\n\n## Deltas\n\n**[`xtask/tests/corpus/manifest.toml`](../../xtask/tests/corpus/manifest.toml)** — the corpus. Each case carries `id`, `level`, `generation`, `expected`, `rule`, `cell`, `seeded`, and a `witness`.\n\n`cells` names the rows of spec §4.1's enforcement map a case is evidence for, so a miss downgrades named cells rather than an unnamed feeling. It is a list because one seeded violation can carry more than one claim (CHG-002.1). The manifest adds one id the spec's table does not have, `classification`, with a comment saying why: total classification (§11.2) is not a §4.1 row but the precondition for every row, and a member the checker cannot classify is a hole in all of them at once. C10, C11 and C20 sit there.\n\n`witness` states what a finding must name. A rule id alone would score C01 for a checker that reported `dir.core_to_adapter` about the wrong pair of crates, so C01's witness is `{ rule, from = \"core-a\", to = \"adapter-x\" }`, C20's names a crate and a port, and R01's names an error code.\n\n**Generated, not committed.** `generation = \"declared\"` on all 35 crate-level cases means CHG-004 writes each fixture workspace from this file: `crates` gives the members, their `role` metadata, and their dependencies with `kind`, `source`, `version`, `path`, `target`, `optional`, `rename` and `inherit`. This is the CHG-001.1 decision applied before the drift exists — a committed fixture is a copy of the file that owns it, and the copy goes stale silently.\n\nThe 27 module cases are `generation = \"authored\"`: their seeded construct *is* Rust syntax (`impl crate::ordering::Trait for X`, a macro body, an `include!`), and encoding that in TOML would be a worse copy than the Rust. CHG-007 writes them.\n\n**[`xtask/src/corpus.rs`](../../xtask/src/corpus.rs)** — the manifest's types and its validation. No runner. `Manifest::defects()` reports a repeated id, an unknown cell, a detection with no rule or no witness, an expected miss citing no §4.1 hole, a declared case with no crates, and a dependency on a crate the generator could not write. `#[serde(deny_unknown_fields)]` throughout, so a typo in a field name is an error rather than a case silently missing that field.\n\n**[`xtask/tests/corpus_manifest.rs`](../../xtask/tests/corpus_manifest.rs)** — nine tests pinning the manifest to plan §6: the id set, the outcome counts, every rule id drawn from the plan's list, no defects, every crate-level case generatable, the four dependency shapes of C15–C18 each actually declared, C10's absent metadata distinguished from C11's conflicting metadata, and the grading rules stating what a disagreement does.\n\nThe plan's id list is transcribed into the test a second time on purpose. Reading it out of the manifest would make the test agree with whatever the manifest says.\n\n**[`.rha/policy.toml`](../../.rha/policy.toml)** — one entry adding the manifest to `[surface] protected`, pre-approved by Kennedy. A corpus that is not protected can have an expectation edited after the fact, which is the one thing pre-registration exists to prevent.\n\n## What the cases are for\n\nFour groups do work beyond covering a rule.\n\n**C15–C18 are one violation, four times.** `core-a → adapter-x` hidden behind a target `cfg`, an `optional`, a rename, and a workspace inherit. They exist to fail a checker that parses `Cargo.toml` text instead of reading `cargo metadata`'s resolved fields — the shortcut W3 will be tempted by. C17 additionally requires the finding to name the package `adapter-x`, not the rename `ax`.\n\n**L07 and M11 expect a report that is not an alarm.** `core-a`'s dev-dependency on `adapter-x` is legitimate — a crate's own tests are a harness — but it must still be *listed*, under `harness_edges`. Their witness carries `listed_as` rather than `rule`, and `[grading].no_alarm_scope` says a run that raises it as a finding fails the case. Silence and a listed fact are different outcomes.\n\n**L11 guards the checker against itself.** `planner-adapter-utils` declares `role = \"core\"` and contains the string `adapter-` without starting with it. A checker matching the prefix as a substring reports C11's conflict here and fails.\n\n**EM-C02 records which mechanism covers a cell, not a failure.** `core-a` calls `std::fs::read` and declares no dependency: invisible to the crate graph, and caught by CHG-001's Clippy deny list at another level in another lane. Registering it keeps the enforcement map honest about where the coverage actually comes from.\n\n## Grading, and what is still open\n\n`[grading]` is what stops H4 grading itself generously. Three values are the Executor's proposal and are **open at DP-1.1**:\n\n- `detection_requires` — every key of the case's `witness` table must equal the finding's corresponding field. There is no exempt list, because prose lives in `witness_notes` (CHG-002.1).\n- `extra_findings` — the case counts as detected, and every unmatched finding on that fixture is counted in the run's `false_alarm` total. Recall and precision are scored separately so an over-eager rule is neither hidden by a case it gets right nor punished twice for one defect.\n- `no_alarm_scope` — any finding on a legitimate fixture fails the case, whatever its rule, except a report carrying `listed_as`.\n\nTwo more are not open, because they follow from §17 and §9.14, and both tests assert their wording: a `detect` case that is not detected fails the run and downgrades the §4.1 cell, and **never edits this file**; an `expected_miss` that *is* detected is escalated, not silently re-registered.\n\n## Evidence\n\n**Local.** Five records under `evidence/CHG-002/`, and all five are kept (this list was corrected in CHG-003.2; it had named three and `13f665c` as the head, a pull request 4 review finding):\n\n| Record | Revision | Result |\n| --- | --- | --- |\n| `20260920T001651Z-24ad1602720b.json` | `24ad160` | `L0.fmt` **failed**; repair attempt 1 |\n| `20260920T001738Z-038a8ea2ba1c.json` | `038a8ea` | 7 passed, `L0.architecture` not_run, 50 tests; superseded |\n| `20260920T002204Z-13f665cb0893.json` | `13f665c` | 7 passed, `L0.architecture` not_run, 50 tests; the head before review |\n| `20260920T010039Z-cfb048e479fb.json` | `cfb048e` | 7 passed, `L0.architecture` not_run, 53 tests; after the CHG-002.1 determinations |\n| `20260920T011724Z-6518f6a89af5.json` | `6518f6a` | 7 passed, `L0.architecture` not_run, 54 tests; **the head of the item**, after the outside-crate repair |\n\nA fourth run, at `af1e5077`, failed `L0.typos` on the sentence repair attempt 2 was in the middle of correcting; its record is not committed, and repair attempt 2 says why.\n\n**CI.** Run [35478601881](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35478601881) on head `5972d91`, copied as [`evidence/ci/35478601881.json`](../../evidence/ci/35478601881.json). Subject revision `68b41dca`, GitHub's ephemeral merge commit. Seven passed, `L0.architecture` `not_run`, 50 tests selected, and all four predicates agree with the local record. The earlier CI run 35478425099 failed `L0.typos` and is not committed; repair attempt 2 cites it. After the CHG-002.1 repairs: run [35480297907](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35480297907), subject `1c9f7e1`, 53 tests; run [35481014407](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35481014407), subject `6b2784b`, 54 tests, the last run on the branch; and run [35481271471](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35481271471) on `main` at `15d916a`, 54 tests, which `.rha/acceptances/CHG-002.toml` cites. All three report seven passed and `L0.architecture` `not_run`; the two branch subjects are GitHub's ephemeral merge commits. (Added in CHG-003.2.)\n\n`L0.architecture` is `not_run` and is reported as `not_run`. Nothing here is reported passed that did not run.\n\n**The policy changed again, so Applicable is false again.** Adding the manifest to the protected list moves the policy digest from `sha256:dc2ef7e0…` to `sha256:5497c29e…`, and a record is Applicable only where the candidate's policy equals the base revision's (§11.7.6). This is the second controlled transition in this repository; the first, CHG-001's, closed the moment it merged. Spec §11.5 makes it yours to accept.\n\n### Repair attempts (§11.7.8)\n\n1. **`L0.fmt` failed at `24ad160`.** The first lane run on this branch reported `L0.fmt failed: exited with 1`. Hypothesis: `xtask/src/corpus.rs` and `xtask/tests/corpus_manifest.rs` were written by hand and never formatted, and this toolchain's rustfmt reformats a chained `assert!(expr.method()...)` into a block form. Discriminating check: `cargo fmt --all -- --check`, which named four sites in `corpus.rs` and one import ordering in `corpus_manifest.rs`. Change: `cargo fmt --all`, which rewrote 32 lines across the two files and reordered `use xtask::corpus::{…, MANIFEST_PATH, Manifest}` to the edition 2024 ordering. Result: `cargo fmt --all -- --check` is silent and the lane passes `L0.fmt` at `4d5a48e`. The failing record is committed under `evidence/CHG-002/`, not deleted — CHG-001's repair attempt 4 deleted one and said so; this is the corrected practice.\n\n2. **`L0.typos` failed in CI at `9e7bb89` and had passed locally.** CI run [35478425099](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35478425099) reported `L0.typos failed: exited with 2`, on `docs/evidence/index.md`: the generated index lists the record filename `20260920T001738Z-038a8ea2ba1c.json`, and typos splits a hex digest on its digits and reads each letter run as a word. The two-letter run before `1c` is read as a misspelling of \"by\" or \"be\".\n\n   Hypothesis: the local run did not see it because of the order the commands were run in, not because the two machines differ. The lane ran, *then* `cargo xtask docs` regenerated the index to include that very record, and the lane was never re-run. Discriminating check: `typos` on the working tree, which reproduced the CI failure exactly, and `typos --version`, which reports 1.50.2 on both — the same tool, a different input.\n\n   Change: an `extend-ignore-re` of `\\b[0-9a-f]{12,}\\b` in [`_typos.toml`](../../_typos.toml), which is not a protected surface. Excluding `docs/evidence/index.md` was the alternative and was rejected: digests also appear in change records, task records and acceptance records, so excluding one generated file moves the problem rather than solving it, and it would stop spell-checking that file's prose. Twelve is the shortest hex run this repository uses, the git short revision in a record filename, and no English word is twelve hex characters long.\n\n   Result: `typos` exits 0 on the tree. A probe file inside the repository holding one deliberately misspelled common word, a 12-character digest and a 40-character revision still reports the misspelling and ignores both digests, so the rule masks the false positive and nothing else. The first probe was written to a scratch directory outside the repository, where `_typos.toml` does not apply, and proved nothing; it was re-run inside the tree.\n\n   **Process finding, not a tool finding.** A lane result is only about the tree that existed when it ran. `cargo xtask docs` changes tracked files, so running it after the lane invalidates the record the lane just wrote. CONTRIBUTING tells contributors to regenerate docs after changing `.rha/**` or `evidence/**`; it does not say to re-run the lane afterwards. Queued for the v0.11 proposals as a workflow correction.\n\n## CHG-002.1: review findings and their determinations\n\nRequested by Kennedy on the open pull request, as eight commits. The Codex review of commits `9e7bb89` and `8dc1969` raised six findings, and a seventh P1 against the CHG-002.1 commits themselves. All seven are accepted and fixed. One further change is Kennedy's own.\n\n| # | Source | Finding | Determination |\n| --- | --- | --- | --- |\n| 1 | Codex **P1** | *Keep the transitive dependency out of the core member set.* EM-C01's `pure-looking` was a workspace member with `role = \"core\"` and a direct, un-allow-listed `tokio` dependency, so the checker would report an ordinary `effect.core_disallowed_dependency` about it. | **Accepted, fixed.** The finding is correct and it invalidated the case: a real detection of a *different* rule would have made the fixture non-silent, so it would no longer demonstrate that the transitive edge is invisible. `pure-looking` and `tokio` are now `outside_crates` reached by path, classified by nothing. |\n| 2 | Codex **P1** | *Encode the Rust source required by R01.* Neither crate declared a body, so a generated R01 workspace would compile and the registered `E0603` could never fire. | **Accepted, fixed.** Both bodies are declared. Verified by building the pair outside the repository: rustc 1.98.1 reports `error[E0603]: module 'private_mod' is private` at `core-b/src/lib.rs`, so the witness holds against a real run. |\n| 3 | Codex **P1** | *Match every outcome-bearing witness field.* `detection_requires` matched \"every key naming a crate, port, module path or error code\", which omits `kind`, `matched_rule`, `depth` and `extraction`; M20 could be scored detected by a checker reporting the cycle as exact. | **Accepted, fixed differently than proposed.** Codex asked for the enumeration to be completed. Completing it would be correct today and silently wrong the next time a witness key is added. Instead `witness` now holds only matched keys and prose moved to `witness_notes`, so position decides what is matched. `Defect::ProseInWitness` stops the split regressing. |\n| 4 | Codex **P2** | *Pin each expected outcome to its case ID.* Aggregate counts cannot see a swap: flipping C01 to `no_alarm` and L01 to `detect` leaves every count identical. | **Accepted, fixed, and verified against the stated scenario.** With exactly that swap applied, the counts test still passes and the new `PINNED` test fails naming both cases. |\n| 5 | Codex **P2** | *Attribute core-to-adapter cases to the D1 cell.* C01 and C15–C18 named `law6-d5` alone, so a miss would downgrade the D5 claim and leave D1 credited. | **Accepted, fixed more broadly than proposed.** Codex asked to move the cases from D5 to D1. A core-to-adapter edge is genuinely evidence for both rows, so `cell` became `cells`, a list, and those five name both. Moving them would have left the other claim falsely standing, in the opposite direction. |\n| 6 | Codex **P2** | *Map the undeclared-dependency case to Law 3/D1.* M02 seeds a directional edge, not a cycle, yet named `law6-b3`. | **Accepted, fixed.** M02 names `law3-d1`. |\n| 8 | Codex **P1** | *Point dependencies at the actual outside-crate location.* A member's `path = \"../outside-crate\"` is resolved by cargo relative to the **depending crate's** directory, so it lands at `<workspace>/outside-crate` — inside the workspace — while `outside_crates.at` means beside it. C19 could not load, and EM-C01's new `core-a → pure-looking` edge had the same fault. | **Accepted, fixed structurally.** Codex offered two remedies: write `../../` in the dep, or have the generator normalise. The first hard-codes the depth of a layout the manifest does not define, and would break the moment a fixture nested differently. Instead `Dep::path` is removed: `outside_crates.at` is the single owner of an outside crate's location (§11.7.1), a dependency names the crate and nothing more, and the generator computes each relative path. All three uses of `path` named an outside crate, so the field was pure duplication — and the duplication is what let the two disagree. |\n| 7 | **Kennedy** | C05's witness carried an `alternative` letting a Cargo error be recorded as `graph.cycle`. | **Accepted, fixed.** It is the corpus grading itself generously — crediting the xtask DFS for an error it never produced, and the Law 6/B3 cell to a rule that did not run. Removed; C05 now requires the checker's own finding. |\n\nFinding 8 arrived on the CHG-002.1 commits themselves and is a fault I introduced in finding 1's fix: moving `pure-looking` outside the workspace added a second member-to-outside edge with the same broken path. It also exposed a gap in the validation — an outside crate's own dependencies were not checked at all, so `pure-looking` could name a crate the generator had no instruction to write. `Manifest::defects()` now walks outside crates too, with a test.\n\n**Approval note.** Kennedy approved manifest edits in commits 3, 5 and 6. Finding 8's fix is a ninth commit and edits the manifest again, outside that approval, on the instruction to resolve open review conversations. It changes no case's expected outcome, rule, cells or seeded description — only the removal of a redundant `path` key from three dependency entries. It is flagged here so it can be reverted as easily as it was made.\n\n**What did not change.** No case's `expected` outcome was altered, no case was added or dropped, and no rule id changed. Findings 1, 5, 6 and 7 are pre-registration corrections that Kennedy approved explicitly, before any checker exists; findings 2 and 3 are structural, changing how a case is expressed rather than what it expects.\n\nFindings 1, 2 and 7 are worth separating from the rest: each described a fixture that **could not produce the result it was registered for**. A corpus whose cases cannot fire is worse than no corpus, because W4 would have reported numbers for it.\n\n### A wrong number in a commit message\n\nThe commit that added the CHG-002.1 evidence record, `976a541`, says \"55 tests selected\". The record it commits, `evidence/CHG-002/20260920T010039Z-cfb048e479fb.json`, reports **53**: fifty in CHG-002 plus the three added by CHG-002.1, for `Defect::ProseInWitness`, `Defect::NoCells`, and the per-case pinning test.\n\nFifty-five was not read from anything. It is the same class of defect as D-001.1 in CHG-001's acceptance record — a number in the position of a witness that no run produced — and it is worse, because a digest placeholder is visibly a placeholder and a plausible count is not.\n\nCorrected here rather than by amending the commit. Rewriting the message would leave no trace that the number had been wrong, and this repository already decided, in CHG-001's repair attempt 4, that deleting an inconvenient observation is the wrong move. The commit message stands; this paragraph is the correction, and the record is the authority.\n\n\n## Acceptance concerns\n\n1. **DP-1.1 is the point of this item.** Kennedy reviews the manifest, and authors two or three held-out crate-level cases that live outside this repository. The Executor must not read or write them (§9.14). Every case here was written by the Executor from the plan; the held-out set is the only part of H4 that is not self-assessment, and without it W4's numbers measure the checker against expectations the same party wrote.\n2. **The three `[grading]` values are a proposal.** They are the strict reading in each case. If any is relaxed, the relaxation belongs here and in the manifest before CHG-004 runs, not after a result is known.\n3. **Still not Eligible.** `L0.architecture` is `not_run` and required and non-waivable under DP-0.5, unchanged since CHG-000's acceptance.\n4. **A registered case is not a working fixture.** Nothing has generated a workspace from this manifest yet. The declaration vocabulary is validated for internal consistency only; CHG-004 is where it meets `cargo metadata`, and a case that cannot be expressed is re-registered with its reason *before* that run, per the task record's `replan_when`.\n5. **C05 may never reach the checker.** Cargo rejects a package cycle while loading the workspace, so the fixture may fail before `cargo xtask architecture` sees it. CHG-002.1 removed the `alternative` that would have let a Cargo error count as the checker's detection. If the checker reports nothing, C05 is a **miss**, and W4 re-registers it with a stated reason or rebuilds the fixture — in the open, before a run.\n6. **`cell = \"classification\"` is not a §4.1 row.** The manifest says so in a comment. If Kennedy would rather §4.1 gained a classification row, that is a v0.11 proposal, not an edit here.\n"
  },
  "docs/changes/CHG-002.2-acceptance-protocol.md": {
    "body": "[Heading { level: 1, slug: \"chg-0022-acceptance-by-merge-a-decision-ledger-and-the-chg-002-acceptance-record\", children: [Text(\"CHG-002.2: Acceptance by merge, a decision ledger, and the CHG-002 acceptance record\")] }, Paragraph([Text(\"Task record: \"), Link { href: \"../../.rha/tasks/CHG-002.2-acceptance-protocol.toml\", children: [Code(\".rha/tasks/CHG-002.2-acceptance-protocol.toml\")] }, Text(\". Base: \"), Code(\"15d916a\"), Text(\", the merge of CHG-002.\")]), Heading { level: 2, slug: \"intent-and-scope\", children: [Text(\"Intent and scope\")] }, Paragraph([Text(\"Three items in a row ended with the same three open concerns: no acceptance record, decision points unanswered, and the corpus grading values still \\\"proposed\\\". Each was true, and each was a symptom of one defect: the acceptance decision was being made by merging, while every artifact said it had not been made. This change moves the decision into artifacts.\")]), Paragraph([Strong([Text(\"Behaviour.\")]), Text(\" A merge into \"), Code(\"main\"), Text(\" by the acceptance authority is the acceptance (\"), Code(\".rha/policy.toml [acceptance]\"), Text(\"). The Executor writes the acceptance record as the first commit of the next item, citing the merge commit, its tree, and the CI record for that revision; creating it is pre-approved, editing an existing one is not. \"), Code(\".rha/decisions.toml\"), Text(\" owns decision-point status; a point with a default that is unanswered when its trigger arrives defaults, a required one waits, and reports cite ids rather than restating questions.\")]), Paragraph([Strong([Text(\"Non-goals.\")]), Text(\" Checker code. The held-out cases of DP-1.1b, which no agent may author or read. Any lane or strictness change.\")]), Heading { level: 2, slug: \"deltas\", children: [Text(\"Deltas\")] }, List { start: None, items: [[Code(\".rha/policy.toml\"), Text(\": new \"), Code(\"[acceptance]\"), Text(\" section. Protected; approval in the task record; the merge of this PR is the approval.\")], [Code(\".rha/decisions.toml\"), Text(\": every plan §9 point with status; DP-1.1 split into 1.1a (decided by the merge of PR 4), 1.1b (open, required, closed by a commitment digest of the held-out cases), 1.1c (decided: strict grading).\")], [Code(\".rha/acceptances/CHG-002.toml\"), Text(\": the first record written under the rule; cites \"), Code(\"evidence/ci/35481271471.json\"), Text(\", the run on \"), Code(\"main\"), Text(\" for \"), Code(\"15d916a\"), Text(\", whose subject revision and tree equal the accepted ones. Two defects recorded: the seven review findings fixed in CHG-002.1, and the process defect this change repairs.\")], [Code(\"xtask/tests/corpus/manifest.toml [grading]\"), Text(\": \"), Code(\"open_at\"), Text(\" replaced by \"), Code(\"decided_by\"), Text(\" and \"), Code(\"decided_in\"), Text(\". \"), Code(\"Grading\"), Text(\" gains two optional fields; one test assertion follows.\")], [Text(\"Plan §2 item 3 and §9, CONTRIBUTING \\\"Submit and review\\\", AGENTS.md: the rule stated once each, pointing at the policy and the ledger.\")]] }, Paragraph([Strong([Text(\"Removed, weakened, or reinterpreted tests.\")]), Text(\" One assertion changed from \"), Code(\"open_at == \\\"DP-1.1\\\"\"), Text(\" to \"), Code(\"decided_by == Some(\\\"human:kennedy\\\")\"), Text(\": the test now checks that a run is graded by decided values, which is stronger.\")]), Heading { level: 2, slug: \"evidence\", children: [Text(\"Evidence\")] }, Paragraph([Text(\"Recorded after \"), Code(\"cargo xtask docs\"), Text(\", so the lane describes the tree that exists. See \"), Code(\"evidence/CHG-002.2/\"), Text(\". \"), Code(\"L0.architecture\"), Text(\" is \"), Code(\"not_run\"), Text(\"; Applicable is false because this change edits the policy (controlled transition, §11.5).\")]), Heading { level: 3, slug: \"repair-attempts-1178\", children: [Text(\"Repair attempts (§11.7.8)\")] }, List { start: Some(1), items: [[Strong([Code(\"cargo xtask docs\"), Text(\" reported a TOML error and the lane refused the policy.\")]), Text(\" Two causes, both mine. The task record was written through an unquoted shell heredoc, so a backtick-quoted command inside a string was executed and its output pasted into the record; and \"), Code(\"Policy\"), Text(\" uses \"), Code(\"deny_unknown_fields\"), Text(\", so the new \"), Code(\"[acceptance]\"), Text(\" section was rejected. Change: the string restored, and an \"), Code(\"Acceptance\"), Text(\" type added to \"), Code(\"xtask/src/policy.rs\"), Text(\" as an optional field so a base revision without the section still parses. Result: both parse.\")], [Strong([Code(\"L0.nextest\"), Text(\" failed at the first recorded run (\"), Code(\"evidence/CHG-002.2/20260920T020130Z-15d916a16e23-dirty.json\"), Text(\", kept).\")]), Text(\" Five \"), Code(\"corpus::tests\"), Text(\" build a manifest fixture inline, and that fixture still said \"), Code(\"open_at\"), Text(\". Hypothesis: the same schema change that the committed manifest already carries. Change: the fixture names \"), Code(\"decided_by\"), Text(\" and \"), Code(\"decided_in\"), Text(\". Result: the lane passes \"), Code(\"L0.nextest\"), Text(\" in the record that follows. The docs were regenerated before each lane run, so each record describes the tree that existed.\")]] }, Heading { level: 3, slug: \"review-findings-codex-on-pull-request-5-determinations\", children: [Text(\"Review findings (Codex on pull request 5), determinations\")] }, List { start: Some(1), items: [[Paragraph([Strong([Text(\"P1, acceptance recorded without Eligible or an exception. Confirmed.\")]), Text(\" \"), Code(\"MergeAllowed\"), Text(\" (§11.7.6) needs \"), Code(\"Eligible\"), Text(\" or \"), Code(\"ValidException\"), Text(\"; neither can hold before CHG-003 (a non-waivable check is unimplemented) and CHG-020 (no trusted producers), and CHG-000 and CHG-001 carry the same gap. Repair: the bootstrap is bounded in \"), Code(\".rha/policy.toml [acceptance.bootstrap]\"), Text(\": which predicates cannot hold, until which change, that every acceptance in the period is \"), Code(\"controlled_transition\"), Text(\" with \"), Code(\"merge_allowed = false\"), Text(\" and its failed predicates listed, and when the clause expires. The record says so explicitly. Nothing is waived.\")])], [Paragraph([Strong([Text(\"P1, the decision ledger was not protected. Confirmed.\")]), Text(\" Added to \"), Code(\"[surface] protected\"), Text(\", with two pre-approved edits in \"), Code(\"[acceptance.ledger_edits]\"), Text(\".\")])], [Paragraph([Strong([Text(\"P2, the basis cited PR 4's merge, which predates the rule. Confirmed.\")]), Text(\" The record now cites Kennedy's merge of PR 5 as the acceptance event, and the policy states that \"), Code(\"by_merge\"), Text(\" governs merges after its own approval.\")])], [Paragraph([Strong([Text(\"P1, CHG-003 was allowed to start before the held-out commitment existed. Confirmed.\")]), Text(\" A digest recorded after the checker is written cannot establish that the cases predate it, which is their only purpose. Repair: DP-1.1b now blocks CHG-003; the commitment goes on the ledger first, and the cases run at CHG-004 acceptance.\")])]] }, Heading { level: 2, slug: \"acceptance-concerns\", children: [Text(\"Acceptance concerns\")] }, List { start: Some(1), items: [[Text(\"Merging this PR is three approvals at once: the \"), Code(\"[acceptance]\"), Text(\" policy, the CHG-002 acceptance record, and the grading decision. If any one is wrong, do not merge; say which.\")], [Text(\"DP-1.1b remains yours and cannot be delegated. The ledger row says how to close it with a commitment digest, and \"), Strong([Text(\"CHG-003 is blocked until that digest is recorded\")]), Text(\": a commitment made after the checker exists cannot show the cases predate it (review finding 4). The cases themselves run at CHG-004 acceptance.\")], [Text(\"This record, like every one before CHG-003, is not Eligible.\")]] }]",
    "headings": "[Heading { level: 1, text: \"CHG-002.2: Acceptance by merge, a decision ledger, and the CHG-002 acceptance record\", slug: \"chg-0022-acceptance-by-merge-a-decision-ledger-and-the-chg-002-acceptance-record\", base_slug: \"chg-0022-acceptance-by-merge-a-decision-ledger-and-the-chg-002-acceptance-record\", line: 1 }, Heading { level: 2, text: \"Intent and scope\", slug: \"intent-and-scope\", base_slug: \"intent-and-scope\", line: 5 }, Heading { level: 2, text: \"Deltas\", slug: \"deltas\", base_slug: \"deltas\", line: 13 }, Heading { level: 2, text: \"Evidence\", slug: \"evidence\", base_slug: \"evidence\", line: 23 }, Heading { level: 3, text: \"Repair attempts (§11.7.8)\", slug: \"repair-attempts-1178\", base_slug: \"repair-attempts-1178\", line: 27 }, Heading { level: 3, text: \"Review findings (Codex on pull request 5), determinations\", slug: \"review-findings-codex-on-pull-request-5-determinations\", base_slug: \"review-findings-codex-on-pull-request-5-determinations\", line: 32 }, Heading { level: 2, text: \"Acceptance concerns\", slug: \"acceptance-concerns\", base_slug: \"acceptance-concerns\", line: 40 }]",
    "markdown": "# CHG-002.2: Acceptance by merge, a decision ledger, and the CHG-002 acceptance record\n\nTask record: [`.rha/tasks/CHG-002.2-acceptance-protocol.toml`](../../.rha/tasks/CHG-002.2-acceptance-protocol.toml). Base: `15d916a`, the merge of CHG-002.\n\n## Intent and scope\n\nThree items in a row ended with the same three open concerns: no acceptance record, decision points unanswered, and the corpus grading values still \"proposed\". Each was true, and each was a symptom of one defect: the acceptance decision was being made by merging, while every artifact said it had not been made. This change moves the decision into artifacts.\n\n**Behaviour.** A merge into `main` by the acceptance authority is the acceptance (`.rha/policy.toml [acceptance]`). The Executor writes the acceptance record as the first commit of the next item, citing the merge commit, its tree, and the CI record for that revision; creating it is pre-approved, editing an existing one is not. `.rha/decisions.toml` owns decision-point status; a point with a default that is unanswered when its trigger arrives defaults, a required one waits, and reports cite ids rather than restating questions.\n\n**Non-goals.** Checker code. The held-out cases of DP-1.1b, which no agent may author or read. Any lane or strictness change.\n\n## Deltas\n\n- `.rha/policy.toml`: new `[acceptance]` section. Protected; approval in the task record; the merge of this PR is the approval.\n- `.rha/decisions.toml`: every plan §9 point with status; DP-1.1 split into 1.1a (decided by the merge of PR 4), 1.1b (open, required, closed by a commitment digest of the held-out cases), 1.1c (decided: strict grading).\n- `.rha/acceptances/CHG-002.toml`: the first record written under the rule; cites `evidence/ci/35481271471.json`, the run on `main` for `15d916a`, whose subject revision and tree equal the accepted ones. Two defects recorded: the seven review findings fixed in CHG-002.1, and the process defect this change repairs.\n- `xtask/tests/corpus/manifest.toml [grading]`: `open_at` replaced by `decided_by` and `decided_in`. `Grading` gains two optional fields; one test assertion follows.\n- Plan §2 item 3 and §9, CONTRIBUTING \"Submit and review\", AGENTS.md: the rule stated once each, pointing at the policy and the ledger.\n\n**Removed, weakened, or reinterpreted tests.** One assertion changed from `open_at == \"DP-1.1\"` to `decided_by == Some(\"human:kennedy\")`: the test now checks that a run is graded by decided values, which is stronger.\n\n## Evidence\n\nRecorded after `cargo xtask docs`, so the lane describes the tree that exists. See `evidence/CHG-002.2/`. `L0.architecture` is `not_run`; Applicable is false because this change edits the policy (controlled transition, §11.5).\n\n### Repair attempts (§11.7.8)\n\n1. **`cargo xtask docs` reported a TOML error and the lane refused the policy.** Two causes, both mine. The task record was written through an unquoted shell heredoc, so a backtick-quoted command inside a string was executed and its output pasted into the record; and `Policy` uses `deny_unknown_fields`, so the new `[acceptance]` section was rejected. Change: the string restored, and an `Acceptance` type added to `xtask/src/policy.rs` as an optional field so a base revision without the section still parses. Result: both parse.\n2. **`L0.nextest` failed at the first recorded run (`evidence/CHG-002.2/20260920T020130Z-15d916a16e23-dirty.json`, kept).** Five `corpus::tests` build a manifest fixture inline, and that fixture still said `open_at`. Hypothesis: the same schema change that the committed manifest already carries. Change: the fixture names `decided_by` and `decided_in`. Result: the lane passes `L0.nextest` in the record that follows. The docs were regenerated before each lane run, so each record describes the tree that existed.\n\n### Review findings (Codex on pull request 5), determinations\n\n1. **P1, acceptance recorded without Eligible or an exception. Confirmed.** `MergeAllowed` (§11.7.6) needs `Eligible` or `ValidException`; neither can hold before CHG-003 (a non-waivable check is unimplemented) and CHG-020 (no trusted producers), and CHG-000 and CHG-001 carry the same gap. Repair: the bootstrap is bounded in `.rha/policy.toml [acceptance.bootstrap]`: which predicates cannot hold, until which change, that every acceptance in the period is `controlled_transition` with `merge_allowed = false` and its failed predicates listed, and when the clause expires. The record says so explicitly. Nothing is waived.\n2. **P1, the decision ledger was not protected. Confirmed.** Added to `[surface] protected`, with two pre-approved edits in `[acceptance.ledger_edits]`.\n3. **P2, the basis cited PR 4's merge, which predates the rule. Confirmed.** The record now cites Kennedy's merge of PR 5 as the acceptance event, and the policy states that `by_merge` governs merges after its own approval.\n\n4. **P1, CHG-003 was allowed to start before the held-out commitment existed. Confirmed.** A digest recorded after the checker is written cannot establish that the cases predate it, which is their only purpose. Repair: DP-1.1b now blocks CHG-003; the commitment goes on the ledger first, and the cases run at CHG-004 acceptance.\n\n## Acceptance concerns\n\n1. Merging this PR is three approvals at once: the `[acceptance]` policy, the CHG-002 acceptance record, and the grading decision. If any one is wrong, do not merge; say which.\n2. DP-1.1b remains yours and cannot be delegated. The ledger row says how to close it with a commitment digest, and **CHG-003 is blocked until that digest is recorded**: a commitment made after the checker exists cannot show the cases predate it (review finding 4). The cases themselves run at CHG-004 acceptance.\n3. This record, like every one before CHG-003, is not Eligible.\n"
  },
  "docs/changes/CHG-003-architecture-crate-level.md": {
    "body": "[Heading { level: 1, slug: \"chg-003-cargo-xtask-architecture-the-crate-graph-checker\", children: [Text(\"CHG-003: \"), Code(\"cargo xtask architecture\"), Text(\", the crate-graph checker\")] }, Paragraph([Text(\"Task record: \"), Link { href: \"../../.rha/tasks/CHG-003-architecture-crate-level.toml\", children: [Code(\".rha/tasks/CHG-003-architecture-crate-level.toml\")] }, Text(\".\"), SoftBreak, Text(\"Plan: \"), Link { href: \"../plan/IMPLEMENTATION-PLAN.md\", children: [Text(\"§5 and §8 W3\")] }, Text(\".\"), SoftBreak, Text(\"Base: \"), Code(\"4be4a19\"), Text(\", the merge that accepted CHG-002.2.\")]), Heading { level: 2, slug: \"intent\", children: [Text(\"Intent\")] }, Paragraph([Text(\"Implement the checker of plan §5 at crate level. It is the change that makes \"), Code(\"L0.architecture\"), Text(\" run, which has blocked the \"), Code(\"Passed\"), Text(\" predicate since CHG-000 — for three work items every acceptance record has carried the same sentence, that a required non-waivable check was \"), Code(\"not_run\"), Text(\".\")]), Heading { level: 2, slug: \"commit-order-and-why-it-is-the-order\", children: [Text(\"Commit order, and why it is the order\")] }, Paragraph([Text(\"The first two commits contain no checker code, and that is the point.\")]), List { start: Some(1), items: [[Strong([Code(\"2ce0651\")]), Text(\" — \"), Code(\".rha/acceptances/CHG-002.2.toml\"), Text(\". \"), Code(\".rha/policy.toml [acceptance]\"), Text(\" makes the merge the acceptance and the record the first commit of the next work item.\")], [Strong([Code(\"b0b0636\")]), Text(\" — \"), Link { href: \"../../.rha/decisions.toml\", children: [Code(\".rha/decisions.toml\")] }, Text(\" DP-1.1b, carrying Kennedy's commitment digest with \"), Code(\"status = \\\"decided\\\"\"), Text(\". The row said the digest must be recorded \"), Emphasis([Text(\"before any checker code exists\")]), Text(\", \\\"or it cannot show the cases predate the checker\\\". After this commit no open ledger row names \"), Code(\"CHG-003\"), Text(\" in \"), Code(\"blocks\"), Text(\".\")], [Strong([Code(\"7d07630\")]), Text(\" — the task record and the \"), Code(\"rha-crates.toml\"), Text(\" schema.\")], [Strong([Code(\"665d84e\")]), Text(\" — the checker.\")]] }, Paragraph([Code(\"git log\"), Text(\" shows the order. It is the evidence, not this paragraph.\")]), Heading { level: 2, slug: \"what-the-checker-is\", children: [Text(\"What the checker is\")] }, Paragraph([Strong([Link { href: \"../../xtask/src/metadata.rs\", children: [Code(\"xtask/src/metadata.rs\")] }]), Text(\" runs \"), Code(\"cargo metadata --format-version 1 --no-deps --offline\"), Text(\" and builds the graph from its fields and nothing else. Plan §8 W3 requires that renamed, optional, target-conditional and workspace-inherited dependencies be resolved from metadata and \"), Strong([Text(\"never\")]), Text(\" from \"), Code(\"Cargo.toml\"), Text(\" text. Corpus C15–C18 are four spellings of one edge written to fail a checker that parses the text; cargo has already resolved all four, so there is nothing to special-case.\")]), Paragraph([Strong([Link { href: \"../../xtask/src/graph/\", children: [Code(\"xtask/src/graph/\")] }]), Text(\" — \"), Code(\"model\"), Text(\" is the graph, \"), Code(\"classify\"), Text(\" decides roles, \"), Code(\"rules\"), Text(\" is the rules file, \"), Code(\"check\"), Text(\" evaluates, \"), Code(\"report\"), Text(\" renders.\")]), Paragraph([Text(\"Classification is \"), Strong([Text(\"total\")]), Text(\" (§11.2): metadata role, then prefix, then the tools and harness lists, else \"), Code(\"class.unclassified\"), Text(\" — an error, never a default role, because a default would let an unclassified crate pass every direction rule silently. The prefix is matched with \"), Code(\"starts_with\"), Text(\", so corpus L11's \"), Code(\"planner-adapter-utils\"), Text(\" is not an adapter.\")]), Paragraph([Text(\"Sixteen rules, each with a witness naming the edge, the manifest path and the declaring section: \"), Code(\"class.unclassified\"), Text(\", \"), Code(\"class.prefix_role_conflict\"), Text(\", \"), Code(\"dir.core_to_adapter\"), Text(\", \"), Code(\"dir.core_to_app\"), Text(\", \"), Code(\"dir.non_root_to_adapter\"), Text(\", \"), Code(\"dir.tool_depended_on\"), Text(\", \"), Code(\"effect.core_disallowed_dependency\"), Text(\", \"), Code(\"effect.core_disallowed_dev_dependency\"), Text(\", \"), Code(\"effect.core_build_script\"), Text(\", \"), Code(\"effect.core_clippy_template\"), Text(\" (wired in from CHG-001), \"), Code(\"adapter.missing_port_owner\"), Text(\", \"), Code(\"adapter.port_owner_wrong_kind\"), Text(\", \"), Code(\"adapter.foreign_core\"), Text(\" (warning), \"), Code(\"meta.unknown_port_owner\"), Text(\", \"), Code(\"forbidden.edge\"), Text(\", \"), Code(\"graph.cycle\"), Text(\".\")]), Paragraph([Text(\"Exit codes as §5 fixes them: \"), Code(\"0\"), Text(\" no errors, \"), Code(\"1\"), Text(\" error findings, \"), Code(\"2\"), Text(\" usage or configuration, \"), Code(\"3\"), Text(\" environment failure. Three matters most: a \"), Code(\"cargo metadata\"), Text(\" failure writes a report with \"), Code(\"error_class: tool_error\"), Text(\" and \"), Strong([Text(\"is never a pass\")]), Text(\". A checker that could not run is not a checker that found nothing.\")]), Paragraph([Strong([Text(\"A rule that cannot be evaluated says so.\")]), Text(\" \"), Code(\"transitive.*\"), Text(\" needs resolved metadata and is a stated limitation in \"), Code(\"--no-deps\"), Text(\" mode; \"), Code(\"effect.core_clippy_template\"), Text(\" examined nothing because no crate is classified core yet, and says that rather than letting an empty findings list read as a pass over a core crate.\")]), Heading { level: 2, slug: \"the-corpus-caught-a-real-bug\", children: [Text(\"The corpus caught a real bug\")] }, Paragraph([Text(\"L07 seeds a core's \"), Strong([Text(\"dev\")]), Text(\"-dependency on an adapter and expects \"), Code(\"no_alarm\"), Text(\". I wrote the dev-kind exclusion into \"), Code(\"dir.non_root_to_adapter\"), Text(\", which is where plan §5 mentions it, and missed that \"), Code(\"dir.core_to_adapter\"), Text(\" is checked first and returns early — so the edge still failed. Reading the plan, the implementation looked right.\")]), Paragraph([Text(\"The expectation was fixed in W2, before any of this code existed, so when the checker disagreed the checker was the thing in question. That is what pre-registration is for, and it is the first time in this program it has paid out against code rather than against documents.\")]), Paragraph([Text(\"The exclusion now covers D1 as well as D5, and the edge is listed under \"), Code(\"harness_edges\"), Text(\": silence and a listed fact are different outcomes.\")]), Heading { level: 2, slug: \"deliberate-changes-to-existing-files\", children: [Text(\"Deliberate changes to existing files\")] }, Paragraph([Strong([Code(\"xtask/Cargo.toml\"), Text(\" drops \"), Code(\"[package.metadata.rha] role = \\\"tool\\\"\"), Text(\".\")]), Text(\" It is classified by the \"), Code(\"tools\"), Text(\" list in \"), Code(\"rha-crates.toml\"), Text(\", which plan §5 makes the owner of that fact and which plan §8 W5's acceptance expects as \"), Code(\"xtask: tool (list)\"), Text(\". Stating it in both places meant metadata silently won, so removing \"), Code(\"xtask\"), Text(\" from the list would have had no effect — one fact, two owners, and the loser silent (§11.7.1). Not a protected surface.\")]), Paragraph([Strong([Text(\"A CHG-002 test is narrowed, under §9.14.\")]), Text(\" \"), Code(\"no_fixture_workspace_and_no_checker_is_committed_yet\"), Text(\" also asserted that \"), Code(\"xtask/src/graph\"), Text(\" did not exist and that \"), Code(\"architecture.rs\"), Text(\" still contained \"), Code(\"EXIT_NOT_RUN\"), Text(\". Both became false here. This is \"), Strong([Text(\"the approved requirement changing\")]), Text(\", not the test becoming inconvenient: CHG-002's non-goal was that \"), Emphasis([Text(\"that item\")]), Text(\" must not implement the checker, and the ordering it guarded is now a historical fact — the corpus merged at \"), Code(\"15d916a\"), Text(\" and every line of the checker is newer. The fixture half still runs, because CHG-004 has not happened. The test is renamed to what it now checks, and its doc comment records what was removed and why.\")]), Paragraph([Strong([Code(\"rha-crates.toml\")]), Text(\" receives the plan §5 schema with \"), Strong([Text(\"empty\")]), Text(\" allow-lists, pre-approved by Kennedy. Empty is not a placeholder: it is the strictest available statement, that no core crate may depend on anything external. Filling the lists from §5's example would pre-authorize dependencies nothing has asked for, and §11.1 wants each justified when it arrives.\")]), Heading { level: 2, slug: \"tests\", children: [Text(\"Tests\")] }, Paragraph([Text(\"Tests on synthetic crate graphs in \"), Link { href: \"../../xtask/tests/architecture_rules.rs\", children: [Code(\"xtask/tests/architecture_rules.rs\")] }, Text(\", one or more per rule, each named after the corpus case it mirrors so that a disagreement in W4 reads as a disagreement rather than as two unrelated failures.\")]), Paragraph([Text(\"They are synthetic \"), Strong([Text(\"on purpose\")]), Text(\". The checker must not read \"), Code(\"xtask/tests/corpus/manifest.toml\"), Text(\": a checker that reads the cases it is judged on can satisfy one without implementing the rule, which is what pre-registration exists to prevent. A test asserts that no checker module names the manifest. Two other places in xtask do name it legitimately and are out of that test's scope — \"), Code(\"corpus.rs\"), Text(\" holds the manifest's types for CHG-004's harness, and \"), Code(\"evidence/mod.rs\"), Text(\" lists it among the fixtures whose digests go into a record, which is the opposite of consulting the answers.\")]), Heading { level: 2, slug: \"repair-attempts-1178\", children: [Text(\"Repair attempts (§11.7.8)\")] }, List { start: Some(1), items: [[Paragraph([Strong([Code(\"L0.typos\"), Text(\" failed at \"), Code(\"102abc0\"), Text(\".\")]), Text(\" A test function in \"), Code(\"xtask/src/graph/rules.rs\"), Text(\" was named with a misspelled plural of \\\"repository\\\". The tool was right: unlike CHG-002's hex digests, this was a word, and it was wrong.\")]), Paragraph([Text(\"The first repair renamed the function and carried the misspelled word into the new name, so the check failed again on the same line — a rename has to change the \"), Emphasis([Text(\"word\")]), Text(\", not the identifier around it. Renamed to \"), Code(\"the_rules_file_of_this_repository_parses\"), Text(\".\")]), Paragraph([Code(\"_typos.toml\"), Text(\" was not touched. A real misspelling is not a false positive, and adding it to \"), Code(\"extend-words\"), Text(\" to make a check pass is exactly the shape §9.14 warns about. This paragraph also avoids quoting the misspelling, because CHG-002 learned that documenting one puts it back in the tree.\")])], [Paragraph([Strong([Text(\"A lane ran on an uncommitted tree during CHG-003.2.\")]), Text(\" The consolidation was staged as two commits, but the \"), Code(\"git add\"), Text(\" for each named a path that \"), Code(\"git rm\"), Text(\" had already removed, so the shell skipped both commits and the lane that followed ran on the working tree. Hypothesis: a shell ordering error, nothing about the code. Discriminating check: \"), Code(\"git log\"), Text(\" showed no new commit and the record is named \"), Code(\"-dirty\"), Text(\". The record, \"), Code(\"evidence/CHG-003/20260920T110910Z-920cca6b07b7-dirty.json\"), Text(\", is what CONTRIBUTING says a dirty record is, all eight checks \"), Code(\"passed\"), Text(\" over the not-yet-committed consolidation, and it is kept. Change: the two commits were made, then this record committed on its own, then the lane re-run on the clean tree. Result: the CHG-003.2 record under Evidence.\")])]] }, Heading { level: 3, slug: \"review-findings-codex-on-pull-request-6-reviewed-revision-776fa83-determinations\", children: [Text(\"Review findings (Codex on pull request 6, reviewed revision \"), Code(\"776fa83\"), Text(\"), determinations\")] }, Paragraph([Text(\"The rule applied, adopted on 2026-09-20 after Codex's audit of this repository's review history: a finding becomes a \"), Code(\"CHG-00n.k\"), Text(\" commit only when it names a concrete failure on the reviewed revision; bookkeeping findings are batched into one determination; a resolved thread reopens only on a new failure mode. All three findings here name a failure, so all three are repaired, in one commit.\")]), List { start: Some(1), items: [[Strong([Text(\"P1, a registry package with a member's name was classified as the member. Confirmed.\")]), Text(\" \"), Code(\"metadata::build\"), Text(\" decided membership by comparing the dependency's package name with the member names. A core depending on a registry crate named like a member (crates.io has a \"), Code(\"graph\"), Text(\"; this repository will have one) got a \"), Code(\"Target::Member\"), Text(\" edge, and \"), Code(\"effect_rules\"), Text(\" skips member targets, so the dependency escaped \"), Code(\"[core] allow\"), Text(\". Change: membership is decided by location. \"), Code(\"cargo metadata\"), Text(\" reports a path dependency with \"), Code(\"path\"), Text(\" set and \"), Code(\"source\"), Text(\" null; a dependency is a member only when its \"), Code(\"path\"), Text(\" is a member's manifest directory. A registry or git package, and a path package outside the workspace, are external whatever they are called, which also covers corpus C19. Result: a unit test seeds both shapes from a core and asserts both edges are external.\")], [Strong([Text(\"P1, \"), Code(\"[transitive] enabled = true\"), Text(\" removed the limitation while nothing evaluated the rule. Confirmed.\")]), Text(\" Metadata is always read with \"), Code(\"--no-deps\"), Text(\" and no transitive evaluator exists, so a rules file that enabled it got a clean summary and exit 0 for a check that was requested and never performed: on an EM-C01 workspace, a silent pass over the very hole the case registers. Change: \"), Code(\"architecture::run\"), Text(\" refuses \"), Code(\"enabled = true\"), Text(\" with exit 2, the plan §5 code for a configuration this version cannot honour, before \"), Code(\"cargo metadata\"), Text(\" runs; the lane records exit 2 as \"), Code(\"failed\"), Text(\" with \"), Code(\"error_class: config_error\"), Text(\", never \"), Code(\"passed\"), Text(\". \"), Code(\"check()\"), Text(\" states the limitation in both settings, so an in-process caller is told too. \"), Code(\"--transitive\"), Text(\" alone prints a notice and continues, as before. Result: a test with a rules file enabling transitive mode and no \"), Code(\"Cargo.toml\"), Text(\" gets 2, not the environment failure 3 it would get if anything had run first; the in-process limitation is asserted with \"), Code(\"enabled = true\"), Text(\".\")], [Strong([Text(\"P2, \"), Code(\"foreign_core_dependency\"), Text(\" accepted any string and read a misspelling as warn. Confirmed.\")]), Text(\" \"), Code(\"deny_unknown_fields\"), Text(\" guards this file's keys, not its values; a value such as \"), Code(\"\\\"fatal\\\"\"), Text(\" parsed and meant warn, so a rules file meant to be fatal produced warnings and exit 0. Change: the value is a closed enum, \"), Code(\"warn | error\"), Text(\"; anything else is a parse error and exit 2. Result: a unit test asserts \"), Code(\"error\"), Text(\" is fatal, \"), Code(\"warn\"), Text(\" is not, and two other spellings are rejected.\")]] }, Heading { level: 3, slug: \"chg-0031-follow-up-on-reviewed-revision-4e33733\", children: [Text(\"CHG-003.1 follow-up on reviewed revision \"), Code(\"4e33733\")] }, Paragraph([Text(\"Kennedy requested repairs for the remaining P1/P2 findings and authoring\"), SoftBreak, Text(\"guidance based on this review history. Seven open threads describe four\"), SoftBreak, Text(\"distinct defects; duplicate comments on the same revision are one repair\"), SoftBreak, Text(\"obligation. The three earlier CHG-003.1 repairs above are unchanged history.\")]), Table { align: [None, None, None], head: [[Text(\"Finding\")], [Text(\"Discriminating observation before the repair\")], [Text(\"Required result\")]], rows: [[[Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/pull/6#discussion_r4056688908\", children: [Text(\"Port-owner identity\")] }, Text(\", repeated in \"), Code(\"4056690736\")], [Text(\"An adapter claims a member's port but depends on an external package with the same name; the CLI exits 0 with no findings.\")], [Text(\"Only a normal dependency on the actual member satisfies the declaration. A valid normal edge must work even when a dev edge occurs first.\")]], [[Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/pull/6#discussion_r4056688909\", children: [Text(\"Malformed metadata\")] }], [Text(\"A prefixed adapter with a string-valued \"), Code(\"implements\"), Text(\" table entry exits 0 because the declaration is discarded.\")], [Text(\"Missing optional metadata remains valid; present malformed metadata reports its manifest and exits 2 as a configuration error. Cargo execution failures remain exit 3.\")]], [[Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/pull/6#discussion_r4056688910\", children: [Text(\"Unsupported schema\")] }, Text(\", repeated in \"), Code(\"4056690738\")], [Text(\"Rules declaring schema version 2 produce a clean report under version 1 semantics.\")], [Text(\"Reject unsupported versions at the parser, before loading Cargo metadata.\")]], [[Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/pull/6#discussion_r4056688912\", children: [Text(\"Checker identity\")] }, Text(\", repeated in \"), Code(\"4056690740\")], [Text(\"Successful JSON has only the tool name and package version; \"), Code(\"tool.git_rev\"), Text(\" is absent.\")], [Text(\"Success and failure reports identify the checker built from this source, independently of the checked workspace, and disclose dirty or unavailable identity.\")]]] }, Paragraph([Text(\"The baseline CLI was built from \"), Code(\"4e33733\"), Text(\"; all three invalid-input fixtures\"), SoftBreak, Text(\"exited 0, and the fourth observation confirmed the missing field. These\"), SoftBreak, Text(\"small external workspaces are regression probes, not the deferred H4 corpus\"), SoftBreak, Text(\"runner. The same probes after repair return exit 1 for the wrong member and\"), SoftBreak, Text(\"exit 2 for malformed metadata and the unsupported schema. A valid workspace\"), SoftBreak, Text(\"still exits 0; a missing Cargo manifest exits 3 with \"), Code(\"tool_error\"), Text(\". The compiled\"), SoftBreak, Text(\"checker identity is shared by success and failure reports, while a failed\"), SoftBreak, Text(\"load leaves the unknown subject root null. The observations are retained in\"), SoftBreak, Link { href: \"../../evidence/CHG-003/review-followup-probes.txt\", children: [Text(\"review-followup-probes.txt\")] }, Text(\".\")]), Paragraph([Text(\"The port check now prefers any normal edge to the actual member, regardless\"), SoftBreak, Text(\"of dev/build edge order. Metadata errors preserve their configuration or\"), SoftBreak, Text(\"transport classification, and the rules parser rejects unsupported versions.\"), SoftBreak, Text(\"Valid \"), Code(\"composite\"), Text(\" metadata and the declared \"), Code(\"--transitive\"), Text(\" flag-only behavior\"), SoftBreak, Text(\"remain supported. The old unit test that treated malformed metadata as absent\"), SoftBreak, Text(\"was corrected under §9.14: absence and invalid declarations have different\"), SoftBreak, Text(\"contracts, now checked separately. No corpus expectation was changed.\")]), Paragraph([Text(\"A standard-library-only build script in the \"), Code(\"xtask\"), Text(\" tool captures Git revision\"), SoftBreak, Text(\"and dirty source status. Its Git reads occur when building repository tooling,\"), SoftBreak, Text(\"not in a core crate; it adds no dependency. Resolved worktree HEAD/branch/index\"), SoftBreak, Text(\"paths refresh that identity without scanning the shared Git directory. A\"), SoftBreak, Text(\"binary keeps its compiled identity when used on another workspace.\")]), Paragraph([Text(\"Independent closure approved the corrected tests, exit/report semantics,\"), SoftBreak, Text(\"identity watches and guide changes. The package's targeted test run passed;\"), SoftBreak, Text(\"the final lane result is recorded below.\")]), Paragraph([Text(\"The authoring changes address the demonstrated causes: distinguish absence\"), SoftBreak, Text(\"from malformed input; preserve identity through downstream decisions; test\"), SoftBreak, Text(\"real input boundaries alongside synthetic graphs; and assert nested report\"), SoftBreak, Text(\"contracts. They do not add another approval stage or a fixed review quota.\")]), Heading { level: 3, slug: \"chg-0032-consolidation-determinations\", children: [Text(\"CHG-003.2: consolidation, determinations\")] }, Paragraph([Text(\"Authority: Kennedy's instruction of 2026-09-20, after disabling automated review, to bring this PR to a compliant and efficient state and fix the churn according to the spec. Task record decision \"), Code(\"consolidation-chg-003.2\"), Text(\".\")]), List { start: Some(1), items: [[Strong([Text(\"The follow-up's four repairs stand.\")]), Text(\" Port ownership requires the member, malformed metadata is a configuration error, \"), Code(\"schema_version\"), Text(\" must be 1, and every report identifies the checker. Their tests stay.\")], [Strong([Text(\"The build script is replaced by run-time identity. Determined against §11.7.1 and §6.8.\")]), Text(\" The tool's revision had two owners with two definitions of dirty: \"), Code(\"xtask/build.rs\"), Text(\" at build time over the tool's own paths, and \"), Code(\"xtask ci\"), Text(\" at run time over the whole tree for the evidence record's \"), Code(\"producer\"), Text(\". One lane run could say \"), Code(\"tool_from_dirty_tree: true\"), Text(\" in the evidence record and \"), Code(\"git_dirty: false\"), Text(\" in the architecture report for the same binary. §11.7.1 gives a fact one owner, and a build script that reads git is the build-time ambient effect the checker forbids in core crates (\"), Code(\"effect.core_build_script\"), Text(\"), which a tool should not model. Change: \"), Code(\"util::git_identity\"), Text(\" reads \"), Code(\"HEAD\"), Text(\" and \"), Code(\"git status --porcelain --untracked-files=all\"), Text(\" at run time; \"), Code(\"report::tool_identity(root)\"), Text(\" uses it for success and failure reports alike. \"), Code(\"cargo xtask\"), Text(\" rebuilds the binary from the working tree before every run, so the running checker is that checkout, and \"), Code(\"root\"), Text(\" is the tool's own workspace, never the subject under \"), Code(\"--manifest-path\"), Text(\". The 153-line build script is deleted, and xtask no longer rebuilds on every \"), Code(\"git add\"), Text(\". Result: the follow-up's CLI test compares the report's \"), Code(\"tool\"), Text(\" object with the same call in-process, and a unit test covers a checkout and a directory that is not one.\")], [Strong([Text(\"The PR template is restored to its state at \"), Code(\"4e33733\"), Text(\".\")]), Text(\" \"), Code(\".github/**\"), Text(\" is a protected surface and no recorded approval names it; the follow-up's decision cites a request for \\\"related agentic files\\\", which does not reach a protected path. The line it added is guidance CONTRIBUTING.md owns.\")], [Strong([Code(\"docs/orchestration-log.md\"), Text(\" is removed.\")]), Text(\" It journals the executor's internal worker roles and is not an artifact of §11.7.11 or plan §3.4; the wiki would render it as research content. Its provenance facts are on the task record's \"), Code(\"[[provenance.sessions]]\"), Text(\", and the text remains in history at \"), Code(\"920cca6\"), Text(\".\")], [Strong([Text(\"Guidance is owned once.\")]), Text(\" AGENTS.md returns to its entry-point shape; CONTRIBUTING.md owns the workflow rules, including the follow-up's authoring paragraph and its hashing rule; \"), Code(\"xtask/AGENTS.md\"), Text(\" owns the scoped rules, with \\\"workers\\\" and \\\"coordinator\\\" replaced by repository terms. The CONTRIBUTING bullet that allowed a final lane without a record is replaced by the ordering this repository uses: docs, then lane, then commit; a record is taken on a clean tree and committed in the next commit; commits that only add a record or an index need no record of their own, since the CI record for the pushed head covers them.\")], [Strong([Text(\"Older threads, batched as bookkeeping.\")]), Text(\" Pull request 3: the CHG-001 task record's provenance now names the W1.4 permission (\"), Code(\".rha/acceptances/CHG-001.toml\"), Text(\", approved in decision W1.4), the CHG-001.4 instruction, and the CHG-001.4 session; the W1.4 evidence record copied the earlier text and is not rewritten. Pull request 4: the CHG-002 task record's module-level totals read 17 violations, 6 legitimate, 3 expected misses and X-M01, which is what the manifest holds; its change record's evidence section lists all five local records and the three later CI runs; EM-M03's cell needs a protected-manifest edit and waits for Kennedy (acceptance concern 9). Pull request 5: the digest finding is D-002.2.1; the repeated bootstrap objection names no new failure mode beyond the one \"), Code(\"[acceptance.bootstrap]\"), Text(\" discloses and bounds, and the label question is proposal row 8, so nothing changes.\")], [Strong([Text(\"Churn.\")]), Text(\" The lane ran once per settled packet in this round, not once per commit. The final head carries one clean local record and one CI record; the follow-up's dirty snapshot record is kept as what it is.\")]] }, Heading { level: 3, slug: \"chg-0033-two-findings-from-the-last-automated-pass-on-920cca6-determinations\", children: [Text(\"CHG-003.3: two findings from the last automated pass on \"), Code(\"920cca6\"), Text(\", determinations\")] }, Paragraph([Text(\"Posted at 10:43 UTC, before the review was disabled, and found after the CHG-003.2 threads were closed. Both reproduced on a temporary workspace before repair.\")]), List { start: Some(1), items: [[Strong([Text(\"P1, \"), Code(\"effect.core_clippy_template\"), Text(\" had never examined a crate. Confirmed.\")]), Text(\" \"), Code(\"architecture::run\"), Text(\" handed the rule the graph as loaded, whose roles are all \"), Code(\"None\"), Text(\": \"), Code(\"check::check\"), Text(\" classifies a clone and returns only the outcome. A workspace with one crate declaring \"), Code(\"role = \\\"core\\\"\"), Text(\" and no \"), Code(\"clippy.toml\"), Text(\" exited 0 and reported the limitation \\\"no crate is classified core\\\". Change: the rule takes its cores from \"), Code(\"outcome.classification\"), Text(\", which the check recorded with each crate's role and manifest path. Result: the same workspace exits 1 with two \"), Code(\"effect.core_clippy_template\"), Text(\" findings, no template and no crate-root forbid; with the template copied in and the forbid line present it exits 0. CLI regression \"), Code(\"a_core_without_the_clippy_template_is_reported_and_with_it_is_not\"), Text(\". This repository has no core crate, so its own reports were not wrong; the mechanism was, and CHG-005's cores would have gone unexamined.\")], [Strong([Text(\"P2, an unknown \"), Code(\"role:\"), Text(\" selector was a rule that could never match. Confirmed.\")]), Text(\" \"), Code(\"from = \\\"role:cor\\\"\"), Text(\" parsed, and the check exited 0 with no finding. Change: \"), Code(\"Rules::parse\"), Text(\" rejects a \"), Code(\"role:\"), Text(\" selector that names none of the five roles, exit 2 \"), Code(\"config_error\"), Text(\", the same closure at parse time as CHG-003.1's \"), Code(\"foreign_core_dependency\"), Text(\". Unit test \"), Code(\"a_forbidden_selector_with_an_unknown_role_is_a_parse_error\"), Text(\".\")]] }, Heading { level: 2, slug: \"evidence\", children: [Text(\"Evidence\")] }, Paragraph([Strong([Text(\"Local\")]), Text(\": \"), Code(\"evidence/CHG-003/20260920T093725Z-53563857be6c.json\"), Text(\" at \"), Code(\"5356385\"), Text(\".\"), SoftBreak, Strong([Text(\"CI\")]), Text(\": run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35502859792\", children: [Text(\"35502859792\")] }, Text(\", copied as \"), Link { href: \"../../evidence/ci/35502859792.json\", children: [Code(\"evidence/ci/35502859792.json\")] }, Text(\", subject \"), Code(\"329dcc4d\"), Text(\".\")]), Paragraph([Text(\"Both agree, on two machines:\")]), Table { align: [None, None], head: [[Text(\"Check\")], [Text(\"Outcome\")]], rows: [[[Code(\"L0.fmt\"), Text(\", \"), Code(\"L0.clippy\"), Text(\", \"), Code(\"L0.nextest\"), Text(\", \"), Code(\"L0.doctest\"), Text(\", \"), Code(\"L0.deny\"), Text(\", \"), Code(\"L0.machete\"), Text(\", \"), Code(\"L0.typos\")], [Text(\"passed\")]], [[Strong([Code(\"L0.architecture\")])], [Strong([Text(\"passed\")])]]] }, Paragraph([Text(\"108 tests selected. \"), Code(\"cargo xtask architecture\"), Text(\" on the CI runner reports \"), Code(\"{\\\"crates\\\": 1, \\\"errors\\\": 0, \\\"outcome\\\": \\\"passed\\\", \\\"warnings\\\": 0}\"), Text(\", the same as locally.\")]), Table { align: [None, None, None], head: [[Text(\"Predicate\")], [Text(\"Holds\")], [Text(\"Why\")]], rows: [[[Text(\"Authentic\")], [Strong([Text(\"false\")])], [Text(\"no trusted producers in \"), Code(\".rha/policy.toml\"), Text(\" until CHG-019 and CHG-020\")]], [[Text(\"Applicable\")], [Text(\"true\")], [Text(\"the candidate's policy equals the base's; this change edits no policy\")]], [[Text(\"Complete\")], [Text(\"true\")], [Text(\"every L0 check appears with a unique id\")]], [[Strong([Text(\"Passed\")])], [Strong([Text(\"true\")])], [Strong([Text(\"first time in this repository\")])]]] }, Paragraph([Strong([Text(\"CHG-003.1, local\")]), Text(\": \"), Code(\"evidence/CHG-003/20260920T100013Z-1cbc47ec97f2.json\"), Text(\" at \"), Code(\"1cbc47e\"), Text(\", the repaired revision, on a clean tree: all eight checks \"), Code(\"passed\"), Text(\", 112 tests selected, eligibility \"), Code(\"blocked\"), Text(\" on \"), Code(\"Authentic\"), Text(\" alone.\"), SoftBreak, Strong([Text(\"CHG-003.1, CI\")]), Text(\": run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35503860516\", children: [Text(\"35503860516\")] }, Text(\" on the pushed head \"), Code(\"b4adcf2\"), Text(\", copied as \"), Link { href: \"../../evidence/ci/35503860516.json\", children: [Code(\"evidence/ci/35503860516.json\")] }, Text(\", subject \"), Code(\"1731b0d6\"), Text(\": all eight \"), Code(\"passed\"), Text(\", 112 tests.\")]), Paragraph([Strong([Text(\"CHG-003.1 follow-up, local snapshot\")]), Text(\": \"), Link { href: \"../../evidence/CHG-003/20260920T103310Z-4e337339c121-dirty.json\", children: [Text(\"20260920T103310Z-4e337339c121-dirty.json\")] }, SoftBreak, Text(\"records all eight L0 checks \"), Code(\"passed\"), Text(\", with 121 tests selected. It identifies\"), SoftBreak, Text(\"an explicitly dirty snapshot based on \"), Code(\"4e337339c121\"), Text(\";\"), SoftBreak, Text(\"it does not claim to cover later evidence/index edits. After those edits are\"), SoftBreak, Text(\"committed, the final lane is run again without \"), Code(\"--record\"), Text(\", keeping its clean\"), SoftBreak, Text(\"candidate result in \"), Code(\"target/rha/evidence.json\"), Text(\" without another archive cycle.\")]), Paragraph([Strong([Text(\"CHG-003.1 follow-up, CI\")]), Text(\": run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35505682590\", children: [Text(\"35505682590\")] }, Text(\" on the pushed head \"), Code(\"920cca6\"), Text(\", copied as \"), Link { href: \"../../evidence/ci/35505682590.json\", children: [Code(\"evidence/ci/35505682590.json\")] }, Text(\", subject \"), Code(\"de02534d\"), Text(\": all eight \"), Code(\"passed\"), Text(\", 121 tests. \"), Code(\"920cca6\"), Text(\" has no committed local record: the snapshot record above carries \"), Code(\"snapshot_tree\"), Text(\" \"), Code(\"1fb5abcd…\"), Text(\", which is not that commit's tree, \"), Code(\"041fcd1d…\"), Text(\" (CHG-003.2 added this record and this note).\")]), Paragraph([Strong([Text(\"CHG-003.2, local\")]), Text(\": \"), Code(\"evidence/CHG-003/20260920T111037Z-956707e0b1da.json\"), Text(\" at \"), Code(\"956707e\"), Text(\", the consolidated revision, on a clean tree: all eight \"), Code(\"passed\"), Text(\", 122 tests, eligibility \"), Code(\"blocked\"), Text(\" on \"), Code(\"Authentic\"), Text(\" alone.\"), SoftBreak, Strong([Text(\"CHG-003.2, CI\")]), Text(\": run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35507106318\", children: [Text(\"35507106318\")] }, Text(\" on the pushed head \"), Code(\"26e7519\"), Text(\", copied as \"), Link { href: \"../../evidence/ci/35507106318.json\", children: [Code(\"evidence/ci/35507106318.json\")] }, Text(\", subject \"), Code(\"7a419405\"), Text(\": all eight \"), Code(\"passed\"), Text(\", 122 tests. This local record and this CI record are the evidence for the code this PR proposes; every earlier record is kept as what it was.\")]), Paragraph([Strong([Text(\"CHG-003.3, local\")]), Text(\": \"), Code(\"evidence/CHG-003/20260920T111702Z-a925cfcda441.json\"), Text(\" at \"), Code(\"a925cfc\"), Text(\", on a clean tree: all eight \"), Code(\"passed\"), Text(\", 124 tests, eligibility \"), Code(\"blocked\"), Text(\" on \"), Code(\"Authentic\"), Text(\" alone. With its CI record below, this is the evidence for the code this PR proposes; the CHG-003.2 records describe the revision before the two repairs.\"), SoftBreak, Strong([Text(\"CHG-003.3, CI\")]), Text(\": run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35507402582\", children: [Text(\"35507402582\")] }, Text(\" on the pushed head \"), Code(\"be5ec86\"), Text(\", copied as \"), Link { href: \"../../evidence/ci/35507402582.json\", children: [Code(\"evidence/ci/35507402582.json\")] }, Text(\", subject \"), Code(\"ae2c66a2\"), Text(\": all eight \"), Code(\"passed\"), Text(\", 124 tests.\")]), Paragraph([Text(\"Eligibility remains \"), Code(\"blocked\"), Text(\", on \"), Code(\"Authentic\"), Text(\" alone. \"), Code(\".rha/policy.toml [acceptance.bootstrap]\"), Text(\" records \"), Code(\"passed_unsatisfiable_until = \\\"CHG-003: L0.architecture is not implemented\\\"\"), Text(\"; that clause expires with this merge, and its \"), Code(\"expires\"), Text(\" condition now waits only on CHG-020.\")]), Heading { level: 2, slug: \"acceptance-concerns\", children: [Text(\"Acceptance concerns\")] }, List { start: Some(1), items: [[Strong([Code(\"Passed\"), Text(\" can hold; \"), Code(\"Authentic\"), Text(\" still cannot.\")]), Text(\" With \"), Code(\"L0.architecture\"), Text(\" running, seven of the eight blockers on eligibility are gone. \"), Code(\"Authentic\"), Text(\" needs trusted producers in \"), Code(\".rha/policy.toml\"), Text(\", which arrive with CHG-019 and CHG-020, so no record is Eligible yet and every acceptance stays a controlled transition under \"), Code(\"[acceptance.bootstrap]\"), Text(\".\")], [Strong([Text(\"The checker has one crate to check.\")]), Text(\" \"), Code(\"xtask\"), Text(\" is the only member, and it is a tool, so most rules examined nothing here. They are exercised by unit tests on synthetic graphs, and by the corpus in CHG-004. Nothing runs them against real product crates until CHG-005.\")], [Strong([Code(\"effect.core_clippy_template\"), Text(\" examined nothing\")]), Text(\", because no crate is classified core. It is wired in and will fire from CHG-005; until then its limitation line says so, and a reader should not read its silence as a pass. CHG-003.3 repaired the wiring itself: before it, the rule was given the unclassified graph and would not have fired for any workspace.\")], [Strong([Text(\"The cycle rule may be unreachable in practice.\")]), Text(\" Cargo rejects a package cycle while resolving, so a cyclic workspace can fail before the checker sees it. Corpus C05 requires this rule's own finding — CHG-002.1 removed the \"), Code(\"alternative\"), Text(\" that would have let a cargo error count — so W4 will show whether the case reaches the checker at all.\")], [Strong([Code(\"--transitive\"), Text(\" is accepted and does nothing, and \"), Code(\"[transitive] enabled = true\"), Text(\" is refused\")]), Text(\" with exit 2 (CHG-003.1). This version has no transitive evaluator; the flag exists so the L2 interface is stable, and the rules-file setting is refused rather than run as if it had been honoured. The limitation line names the rule in both settings.\")], [Strong([Text(\"Glob matching is one trailing \"), Code(\"*\"), Text(\".\")]), Text(\" Plan §5's examples need no more, and a full glob engine would be a dependency and a second syntax. A rule needing more will say so.\")], [Strong([Text(\"CHG-000 and CHG-001 were merged with no completed automated review.\")]), Text(\" Pull requests 1 and 2 received review usage-limit messages and zero completed passes (Codex's audit of the review history, 2026-09-20). The absence of review findings in those two acceptance records is not a clean review and should not be read as one. Nothing here changes those records.\")], [Strong([Text(\"The \"), Code(\"controlled_transition\"), Text(\" label is borrowed.\")]), Text(\" Spec §11.5 uses the term for a policy migration; \"), Code(\"[acceptance.bootstrap]\"), Text(\" applies it to every acceptance in the bootstrap period, including ordinary changes that touch no policy, such as the one this item's first commit recorded. A distinct kind is proposed in \"), Code(\"docs/proposals/spec-v0.11.md\"), Text(\" row 8 and needs a policy edit that only Kennedy makes; until then each record's \"), Code(\"basis\"), Text(\" says what the acceptance is, and the label stays.\")], [Strong([Text(\"EM-M03's cell.\")]), Text(\" \"), Code(\"xtask/tests/corpus/manifest.toml\"), Text(\" assigns EM-M03 only \"), Code(\"law6-b3\"), Text(\", while its own hole describes a Law 3/D1 extraction miss (pull request 4 review finding, open at the time). Proposed edit, for your approval as a pre-registration correction made before any module checker exists: \"), Code(\"cells = [\\\"law3-d1\\\", \\\"law6-b3\\\"]\"), Text(\". Until approved the manifest stands, and an observed miss on EM-M03 would be charged to the no-cycles cell alone.\")]] }]",
    "headings": "[Heading { level: 1, text: \"CHG-003: cargo xtask architecture, the crate-graph checker\", slug: \"chg-003-cargo-xtask-architecture-the-crate-graph-checker\", base_slug: \"chg-003-cargo-xtask-architecture-the-crate-graph-checker\", line: 1 }, Heading { level: 2, text: \"Intent\", slug: \"intent\", base_slug: \"intent\", line: 7 }, Heading { level: 2, text: \"Commit order, and why it is the order\", slug: \"commit-order-and-why-it-is-the-order\", base_slug: \"commit-order-and-why-it-is-the-order\", line: 11 }, Heading { level: 2, text: \"What the checker is\", slug: \"what-the-checker-is\", base_slug: \"what-the-checker-is\", line: 22 }, Heading { level: 2, text: \"The corpus caught a real bug\", slug: \"the-corpus-caught-a-real-bug\", base_slug: \"the-corpus-caught-a-real-bug\", line: 36 }, Heading { level: 2, text: \"Deliberate changes to existing files\", slug: \"deliberate-changes-to-existing-files\", base_slug: \"deliberate-changes-to-existing-files\", line: 44 }, Heading { level: 2, text: \"Tests\", slug: \"tests\", base_slug: \"tests\", line: 52 }, Heading { level: 2, text: \"Repair attempts (§11.7.8)\", slug: \"repair-attempts-1178\", base_slug: \"repair-attempts-1178\", line: 58 }, Heading { level: 3, text: \"Review findings (Codex on pull request 6, reviewed revision 776fa83), determinations\", slug: \"review-findings-codex-on-pull-request-6-reviewed-revision-776fa83-determinations\", base_slug: \"review-findings-codex-on-pull-request-6-reviewed-revision-776fa83-determinations\", line: 68 }, Heading { level: 3, text: \"CHG-003.1 follow-up on reviewed revision 4e33733\", slug: \"chg-0031-follow-up-on-reviewed-revision-4e33733\", base_slug: \"chg-0031-follow-up-on-reviewed-revision-4e33733\", line: 76 }, Heading { level: 3, text: \"CHG-003.2: consolidation, determinations\", slug: \"chg-0032-consolidation-determinations\", base_slug: \"chg-0032-consolidation-determinations\", line: 123 }, Heading { level: 3, text: \"CHG-003.3: two findings from the last automated pass on 920cca6, determinations\", slug: \"chg-0033-two-findings-from-the-last-automated-pass-on-920cca6-determinations\", base_slug: \"chg-0033-two-findings-from-the-last-automated-pass-on-920cca6-determinations\", line: 135 }, Heading { level: 2, text: \"Evidence\", slug: \"evidence\", base_slug: \"evidence\", line: 142 }, Heading { level: 2, text: \"Acceptance concerns\", slug: \"acceptance-concerns\", base_slug: \"acceptance-concerns\", line: 183 }]",
    "markdown": "# CHG-003: `cargo xtask architecture`, the crate-graph checker\n\nTask record: [`.rha/tasks/CHG-003-architecture-crate-level.toml`](../../.rha/tasks/CHG-003-architecture-crate-level.toml).\nPlan: [§5 and §8 W3](../plan/IMPLEMENTATION-PLAN.md).\nBase: `4be4a19`, the merge that accepted CHG-002.2.\n\n## Intent\n\nImplement the checker of plan §5 at crate level. It is the change that makes `L0.architecture` run, which has blocked the `Passed` predicate since CHG-000 — for three work items every acceptance record has carried the same sentence, that a required non-waivable check was `not_run`.\n\n## Commit order, and why it is the order\n\nThe first two commits contain no checker code, and that is the point.\n\n1. **`2ce0651`** — `.rha/acceptances/CHG-002.2.toml`. `.rha/policy.toml [acceptance]` makes the merge the acceptance and the record the first commit of the next work item.\n2. **`b0b0636`** — [`.rha/decisions.toml`](../../.rha/decisions.toml) DP-1.1b, carrying Kennedy's commitment digest with `status = \"decided\"`. The row said the digest must be recorded *before any checker code exists*, \"or it cannot show the cases predate the checker\". After this commit no open ledger row names `CHG-003` in `blocks`.\n3. **`7d07630`** — the task record and the `rha-crates.toml` schema.\n4. **`665d84e`** — the checker.\n\n`git log` shows the order. It is the evidence, not this paragraph.\n\n## What the checker is\n\n**[`xtask/src/metadata.rs`](../../xtask/src/metadata.rs)** runs `cargo metadata --format-version 1 --no-deps --offline` and builds the graph from its fields and nothing else. Plan §8 W3 requires that renamed, optional, target-conditional and workspace-inherited dependencies be resolved from metadata and **never** from `Cargo.toml` text. Corpus C15–C18 are four spellings of one edge written to fail a checker that parses the text; cargo has already resolved all four, so there is nothing to special-case.\n\n**[`xtask/src/graph/`](../../xtask/src/graph/)** — `model` is the graph, `classify` decides roles, `rules` is the rules file, `check` evaluates, `report` renders.\n\nClassification is **total** (§11.2): metadata role, then prefix, then the tools and harness lists, else `class.unclassified` — an error, never a default role, because a default would let an unclassified crate pass every direction rule silently. The prefix is matched with `starts_with`, so corpus L11's `planner-adapter-utils` is not an adapter.\n\nSixteen rules, each with a witness naming the edge, the manifest path and the declaring section: `class.unclassified`, `class.prefix_role_conflict`, `dir.core_to_adapter`, `dir.core_to_app`, `dir.non_root_to_adapter`, `dir.tool_depended_on`, `effect.core_disallowed_dependency`, `effect.core_disallowed_dev_dependency`, `effect.core_build_script`, `effect.core_clippy_template` (wired in from CHG-001), `adapter.missing_port_owner`, `adapter.port_owner_wrong_kind`, `adapter.foreign_core` (warning), `meta.unknown_port_owner`, `forbidden.edge`, `graph.cycle`.\n\nExit codes as §5 fixes them: `0` no errors, `1` error findings, `2` usage or configuration, `3` environment failure. Three matters most: a `cargo metadata` failure writes a report with `error_class: tool_error` and **is never a pass**. A checker that could not run is not a checker that found nothing.\n\n**A rule that cannot be evaluated says so.** `transitive.*` needs resolved metadata and is a stated limitation in `--no-deps` mode; `effect.core_clippy_template` examined nothing because no crate is classified core yet, and says that rather than letting an empty findings list read as a pass over a core crate.\n\n## The corpus caught a real bug\n\nL07 seeds a core's **dev**-dependency on an adapter and expects `no_alarm`. I wrote the dev-kind exclusion into `dir.non_root_to_adapter`, which is where plan §5 mentions it, and missed that `dir.core_to_adapter` is checked first and returns early — so the edge still failed. Reading the plan, the implementation looked right.\n\nThe expectation was fixed in W2, before any of this code existed, so when the checker disagreed the checker was the thing in question. That is what pre-registration is for, and it is the first time in this program it has paid out against code rather than against documents.\n\nThe exclusion now covers D1 as well as D5, and the edge is listed under `harness_edges`: silence and a listed fact are different outcomes.\n\n## Deliberate changes to existing files\n\n**`xtask/Cargo.toml` drops `[package.metadata.rha] role = \"tool\"`.** It is classified by the `tools` list in `rha-crates.toml`, which plan §5 makes the owner of that fact and which plan §8 W5's acceptance expects as `xtask: tool (list)`. Stating it in both places meant metadata silently won, so removing `xtask` from the list would have had no effect — one fact, two owners, and the loser silent (§11.7.1). Not a protected surface.\n\n**A CHG-002 test is narrowed, under §9.14.** `no_fixture_workspace_and_no_checker_is_committed_yet` also asserted that `xtask/src/graph` did not exist and that `architecture.rs` still contained `EXIT_NOT_RUN`. Both became false here. This is **the approved requirement changing**, not the test becoming inconvenient: CHG-002's non-goal was that *that item* must not implement the checker, and the ordering it guarded is now a historical fact — the corpus merged at `15d916a` and every line of the checker is newer. The fixture half still runs, because CHG-004 has not happened. The test is renamed to what it now checks, and its doc comment records what was removed and why.\n\n**`rha-crates.toml`** receives the plan §5 schema with **empty** allow-lists, pre-approved by Kennedy. Empty is not a placeholder: it is the strictest available statement, that no core crate may depend on anything external. Filling the lists from §5's example would pre-authorize dependencies nothing has asked for, and §11.1 wants each justified when it arrives.\n\n## Tests\n\nTests on synthetic crate graphs in [`xtask/tests/architecture_rules.rs`](../../xtask/tests/architecture_rules.rs), one or more per rule, each named after the corpus case it mirrors so that a disagreement in W4 reads as a disagreement rather than as two unrelated failures.\n\nThey are synthetic **on purpose**. The checker must not read `xtask/tests/corpus/manifest.toml`: a checker that reads the cases it is judged on can satisfy one without implementing the rule, which is what pre-registration exists to prevent. A test asserts that no checker module names the manifest. Two other places in xtask do name it legitimately and are out of that test's scope — `corpus.rs` holds the manifest's types for CHG-004's harness, and `evidence/mod.rs` lists it among the fixtures whose digests go into a record, which is the opposite of consulting the answers.\n\n## Repair attempts (§11.7.8)\n\n1. **`L0.typos` failed at `102abc0`.** A test function in `xtask/src/graph/rules.rs` was named with a misspelled plural of \"repository\". The tool was right: unlike CHG-002's hex digests, this was a word, and it was wrong.\n\n   The first repair renamed the function and carried the misspelled word into the new name, so the check failed again on the same line — a rename has to change the *word*, not the identifier around it. Renamed to `the_rules_file_of_this_repository_parses`.\n\n   `_typos.toml` was not touched. A real misspelling is not a false positive, and adding it to `extend-words` to make a check pass is exactly the shape §9.14 warns about. This paragraph also avoids quoting the misspelling, because CHG-002 learned that documenting one puts it back in the tree.\n\n2. **A lane ran on an uncommitted tree during CHG-003.2.** The consolidation was staged as two commits, but the `git add` for each named a path that `git rm` had already removed, so the shell skipped both commits and the lane that followed ran on the working tree. Hypothesis: a shell ordering error, nothing about the code. Discriminating check: `git log` showed no new commit and the record is named `-dirty`. The record, `evidence/CHG-003/20260920T110910Z-920cca6b07b7-dirty.json`, is what CONTRIBUTING says a dirty record is, all eight checks `passed` over the not-yet-committed consolidation, and it is kept. Change: the two commits were made, then this record committed on its own, then the lane re-run on the clean tree. Result: the CHG-003.2 record under Evidence.\n\n### Review findings (Codex on pull request 6, reviewed revision `776fa83`), determinations\n\nThe rule applied, adopted on 2026-09-20 after Codex's audit of this repository's review history: a finding becomes a `CHG-00n.k` commit only when it names a concrete failure on the reviewed revision; bookkeeping findings are batched into one determination; a resolved thread reopens only on a new failure mode. All three findings here name a failure, so all three are repaired, in one commit.\n\n1. **P1, a registry package with a member's name was classified as the member. Confirmed.** `metadata::build` decided membership by comparing the dependency's package name with the member names. A core depending on a registry crate named like a member (crates.io has a `graph`; this repository will have one) got a `Target::Member` edge, and `effect_rules` skips member targets, so the dependency escaped `[core] allow`. Change: membership is decided by location. `cargo metadata` reports a path dependency with `path` set and `source` null; a dependency is a member only when its `path` is a member's manifest directory. A registry or git package, and a path package outside the workspace, are external whatever they are called, which also covers corpus C19. Result: a unit test seeds both shapes from a core and asserts both edges are external.\n2. **P1, `[transitive] enabled = true` removed the limitation while nothing evaluated the rule. Confirmed.** Metadata is always read with `--no-deps` and no transitive evaluator exists, so a rules file that enabled it got a clean summary and exit 0 for a check that was requested and never performed: on an EM-C01 workspace, a silent pass over the very hole the case registers. Change: `architecture::run` refuses `enabled = true` with exit 2, the plan §5 code for a configuration this version cannot honour, before `cargo metadata` runs; the lane records exit 2 as `failed` with `error_class: config_error`, never `passed`. `check()` states the limitation in both settings, so an in-process caller is told too. `--transitive` alone prints a notice and continues, as before. Result: a test with a rules file enabling transitive mode and no `Cargo.toml` gets 2, not the environment failure 3 it would get if anything had run first; the in-process limitation is asserted with `enabled = true`.\n3. **P2, `foreign_core_dependency` accepted any string and read a misspelling as warn. Confirmed.** `deny_unknown_fields` guards this file's keys, not its values; a value such as `\"fatal\"` parsed and meant warn, so a rules file meant to be fatal produced warnings and exit 0. Change: the value is a closed enum, `warn | error`; anything else is a parse error and exit 2. Result: a unit test asserts `error` is fatal, `warn` is not, and two other spellings are rejected.\n\n### CHG-003.1 follow-up on reviewed revision `4e33733`\n\nKennedy requested repairs for the remaining P1/P2 findings and authoring\nguidance based on this review history. Seven open threads describe four\ndistinct defects; duplicate comments on the same revision are one repair\nobligation. The three earlier CHG-003.1 repairs above are unchanged history.\n\n| Finding | Discriminating observation before the repair | Required result |\n| --- | --- | --- |\n| [Port-owner identity](https://github.com/kmosoti/recursive-hexagonal-architecture/pull/6#discussion_r4056688908), repeated in `4056690736` | An adapter claims a member's port but depends on an external package with the same name; the CLI exits 0 with no findings. | Only a normal dependency on the actual member satisfies the declaration. A valid normal edge must work even when a dev edge occurs first. |\n| [Malformed metadata](https://github.com/kmosoti/recursive-hexagonal-architecture/pull/6#discussion_r4056688909) | A prefixed adapter with a string-valued `implements` table entry exits 0 because the declaration is discarded. | Missing optional metadata remains valid; present malformed metadata reports its manifest and exits 2 as a configuration error. Cargo execution failures remain exit 3. |\n| [Unsupported schema](https://github.com/kmosoti/recursive-hexagonal-architecture/pull/6#discussion_r4056688910), repeated in `4056690738` | Rules declaring schema version 2 produce a clean report under version 1 semantics. | Reject unsupported versions at the parser, before loading Cargo metadata. |\n| [Checker identity](https://github.com/kmosoti/recursive-hexagonal-architecture/pull/6#discussion_r4056688912), repeated in `4056690740` | Successful JSON has only the tool name and package version; `tool.git_rev` is absent. | Success and failure reports identify the checker built from this source, independently of the checked workspace, and disclose dirty or unavailable identity. |\n\nThe baseline CLI was built from `4e33733`; all three invalid-input fixtures\nexited 0, and the fourth observation confirmed the missing field. These\nsmall external workspaces are regression probes, not the deferred H4 corpus\nrunner. The same probes after repair return exit 1 for the wrong member and\nexit 2 for malformed metadata and the unsupported schema. A valid workspace\nstill exits 0; a missing Cargo manifest exits 3 with `tool_error`. The compiled\nchecker identity is shared by success and failure reports, while a failed\nload leaves the unknown subject root null. The observations are retained in\n[review-followup-probes.txt](../../evidence/CHG-003/review-followup-probes.txt).\n\nThe port check now prefers any normal edge to the actual member, regardless\nof dev/build edge order. Metadata errors preserve their configuration or\ntransport classification, and the rules parser rejects unsupported versions.\nValid `composite` metadata and the declared `--transitive` flag-only behavior\nremain supported. The old unit test that treated malformed metadata as absent\nwas corrected under §9.14: absence and invalid declarations have different\ncontracts, now checked separately. No corpus expectation was changed.\n\nA standard-library-only build script in the `xtask` tool captures Git revision\nand dirty source status. Its Git reads occur when building repository tooling,\nnot in a core crate; it adds no dependency. Resolved worktree HEAD/branch/index\npaths refresh that identity without scanning the shared Git directory. A\nbinary keeps its compiled identity when used on another workspace.\n\nIndependent closure approved the corrected tests, exit/report semantics,\nidentity watches and guide changes. The package's targeted test run passed;\nthe final lane result is recorded below.\n\nThe authoring changes address the demonstrated causes: distinguish absence\nfrom malformed input; preserve identity through downstream decisions; test\nreal input boundaries alongside synthetic graphs; and assert nested report\ncontracts. They do not add another approval stage or a fixed review quota.\n\n### CHG-003.2: consolidation, determinations\n\nAuthority: Kennedy's instruction of 2026-09-20, after disabling automated review, to bring this PR to a compliant and efficient state and fix the churn according to the spec. Task record decision `consolidation-chg-003.2`.\n\n1. **The follow-up's four repairs stand.** Port ownership requires the member, malformed metadata is a configuration error, `schema_version` must be 1, and every report identifies the checker. Their tests stay.\n2. **The build script is replaced by run-time identity. Determined against §11.7.1 and §6.8.** The tool's revision had two owners with two definitions of dirty: `xtask/build.rs` at build time over the tool's own paths, and `xtask ci` at run time over the whole tree for the evidence record's `producer`. One lane run could say `tool_from_dirty_tree: true` in the evidence record and `git_dirty: false` in the architecture report for the same binary. §11.7.1 gives a fact one owner, and a build script that reads git is the build-time ambient effect the checker forbids in core crates (`effect.core_build_script`), which a tool should not model. Change: `util::git_identity` reads `HEAD` and `git status --porcelain --untracked-files=all` at run time; `report::tool_identity(root)` uses it for success and failure reports alike. `cargo xtask` rebuilds the binary from the working tree before every run, so the running checker is that checkout, and `root` is the tool's own workspace, never the subject under `--manifest-path`. The 153-line build script is deleted, and xtask no longer rebuilds on every `git add`. Result: the follow-up's CLI test compares the report's `tool` object with the same call in-process, and a unit test covers a checkout and a directory that is not one.\n3. **The PR template is restored to its state at `4e33733`.** `.github/**` is a protected surface and no recorded approval names it; the follow-up's decision cites a request for \"related agentic files\", which does not reach a protected path. The line it added is guidance CONTRIBUTING.md owns.\n4. **`docs/orchestration-log.md` is removed.** It journals the executor's internal worker roles and is not an artifact of §11.7.11 or plan §3.4; the wiki would render it as research content. Its provenance facts are on the task record's `[[provenance.sessions]]`, and the text remains in history at `920cca6`.\n5. **Guidance is owned once.** AGENTS.md returns to its entry-point shape; CONTRIBUTING.md owns the workflow rules, including the follow-up's authoring paragraph and its hashing rule; `xtask/AGENTS.md` owns the scoped rules, with \"workers\" and \"coordinator\" replaced by repository terms. The CONTRIBUTING bullet that allowed a final lane without a record is replaced by the ordering this repository uses: docs, then lane, then commit; a record is taken on a clean tree and committed in the next commit; commits that only add a record or an index need no record of their own, since the CI record for the pushed head covers them.\n6. **Older threads, batched as bookkeeping.** Pull request 3: the CHG-001 task record's provenance now names the W1.4 permission (`.rha/acceptances/CHG-001.toml`, approved in decision W1.4), the CHG-001.4 instruction, and the CHG-001.4 session; the W1.4 evidence record copied the earlier text and is not rewritten. Pull request 4: the CHG-002 task record's module-level totals read 17 violations, 6 legitimate, 3 expected misses and X-M01, which is what the manifest holds; its change record's evidence section lists all five local records and the three later CI runs; EM-M03's cell needs a protected-manifest edit and waits for Kennedy (acceptance concern 9). Pull request 5: the digest finding is D-002.2.1; the repeated bootstrap objection names no new failure mode beyond the one `[acceptance.bootstrap]` discloses and bounds, and the label question is proposal row 8, so nothing changes.\n7. **Churn.** The lane ran once per settled packet in this round, not once per commit. The final head carries one clean local record and one CI record; the follow-up's dirty snapshot record is kept as what it is.\n\n### CHG-003.3: two findings from the last automated pass on `920cca6`, determinations\n\nPosted at 10:43 UTC, before the review was disabled, and found after the CHG-003.2 threads were closed. Both reproduced on a temporary workspace before repair.\n\n1. **P1, `effect.core_clippy_template` had never examined a crate. Confirmed.** `architecture::run` handed the rule the graph as loaded, whose roles are all `None`: `check::check` classifies a clone and returns only the outcome. A workspace with one crate declaring `role = \"core\"` and no `clippy.toml` exited 0 and reported the limitation \"no crate is classified core\". Change: the rule takes its cores from `outcome.classification`, which the check recorded with each crate's role and manifest path. Result: the same workspace exits 1 with two `effect.core_clippy_template` findings, no template and no crate-root forbid; with the template copied in and the forbid line present it exits 0. CLI regression `a_core_without_the_clippy_template_is_reported_and_with_it_is_not`. This repository has no core crate, so its own reports were not wrong; the mechanism was, and CHG-005's cores would have gone unexamined.\n2. **P2, an unknown `role:` selector was a rule that could never match. Confirmed.** `from = \"role:cor\"` parsed, and the check exited 0 with no finding. Change: `Rules::parse` rejects a `role:` selector that names none of the five roles, exit 2 `config_error`, the same closure at parse time as CHG-003.1's `foreign_core_dependency`. Unit test `a_forbidden_selector_with_an_unknown_role_is_a_parse_error`.\n\n## Evidence\n\n**Local**: `evidence/CHG-003/20260920T093725Z-53563857be6c.json` at `5356385`.\n**CI**: run [35502859792](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35502859792), copied as [`evidence/ci/35502859792.json`](../../evidence/ci/35502859792.json), subject `329dcc4d`.\n\nBoth agree, on two machines:\n\n| Check | Outcome |\n| --- | --- |\n| `L0.fmt`, `L0.clippy`, `L0.nextest`, `L0.doctest`, `L0.deny`, `L0.machete`, `L0.typos` | passed |\n| **`L0.architecture`** | **passed** |\n\n108 tests selected. `cargo xtask architecture` on the CI runner reports `{\"crates\": 1, \"errors\": 0, \"outcome\": \"passed\", \"warnings\": 0}`, the same as locally.\n\n| Predicate | Holds | Why |\n| --- | --- | --- |\n| Authentic | **false** | no trusted producers in `.rha/policy.toml` until CHG-019 and CHG-020 |\n| Applicable | true | the candidate's policy equals the base's; this change edits no policy |\n| Complete | true | every L0 check appears with a unique id |\n| **Passed** | **true** | **first time in this repository** |\n\n**CHG-003.1, local**: `evidence/CHG-003/20260920T100013Z-1cbc47ec97f2.json` at `1cbc47e`, the repaired revision, on a clean tree: all eight checks `passed`, 112 tests selected, eligibility `blocked` on `Authentic` alone.\n**CHG-003.1, CI**: run [35503860516](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35503860516) on the pushed head `b4adcf2`, copied as [`evidence/ci/35503860516.json`](../../evidence/ci/35503860516.json), subject `1731b0d6`: all eight `passed`, 112 tests.\n\n**CHG-003.1 follow-up, local snapshot**: [20260920T103310Z-4e337339c121-dirty.json](../../evidence/CHG-003/20260920T103310Z-4e337339c121-dirty.json)\nrecords all eight L0 checks `passed`, with 121 tests selected. It identifies\nan explicitly dirty snapshot based on `4e337339c121`;\nit does not claim to cover later evidence/index edits. After those edits are\ncommitted, the final lane is run again without `--record`, keeping its clean\ncandidate result in `target/rha/evidence.json` without another archive cycle.\n\n**CHG-003.1 follow-up, CI**: run [35505682590](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35505682590) on the pushed head `920cca6`, copied as [`evidence/ci/35505682590.json`](../../evidence/ci/35505682590.json), subject `de02534d`: all eight `passed`, 121 tests. `920cca6` has no committed local record: the snapshot record above carries `snapshot_tree` `1fb5abcd…`, which is not that commit's tree, `041fcd1d…` (CHG-003.2 added this record and this note).\n\n**CHG-003.2, local**: `evidence/CHG-003/20260920T111037Z-956707e0b1da.json` at `956707e`, the consolidated revision, on a clean tree: all eight `passed`, 122 tests, eligibility `blocked` on `Authentic` alone.\n**CHG-003.2, CI**: run [35507106318](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35507106318) on the pushed head `26e7519`, copied as [`evidence/ci/35507106318.json`](../../evidence/ci/35507106318.json), subject `7a419405`: all eight `passed`, 122 tests. This local record and this CI record are the evidence for the code this PR proposes; every earlier record is kept as what it was.\n\n**CHG-003.3, local**: `evidence/CHG-003/20260920T111702Z-a925cfcda441.json` at `a925cfc`, on a clean tree: all eight `passed`, 124 tests, eligibility `blocked` on `Authentic` alone. With its CI record below, this is the evidence for the code this PR proposes; the CHG-003.2 records describe the revision before the two repairs.\n**CHG-003.3, CI**: run [35507402582](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35507402582) on the pushed head `be5ec86`, copied as [`evidence/ci/35507402582.json`](../../evidence/ci/35507402582.json), subject `ae2c66a2`: all eight `passed`, 124 tests.\n\nEligibility remains `blocked`, on `Authentic` alone. `.rha/policy.toml [acceptance.bootstrap]` records `passed_unsatisfiable_until = \"CHG-003: L0.architecture is not implemented\"`; that clause expires with this merge, and its `expires` condition now waits only on CHG-020.\n\n## Acceptance concerns\n\n1. **`Passed` can hold; `Authentic` still cannot.** With `L0.architecture` running, seven of the eight blockers on eligibility are gone. `Authentic` needs trusted producers in `.rha/policy.toml`, which arrive with CHG-019 and CHG-020, so no record is Eligible yet and every acceptance stays a controlled transition under `[acceptance.bootstrap]`.\n2. **The checker has one crate to check.** `xtask` is the only member, and it is a tool, so most rules examined nothing here. They are exercised by unit tests on synthetic graphs, and by the corpus in CHG-004. Nothing runs them against real product crates until CHG-005.\n3. **`effect.core_clippy_template` examined nothing**, because no crate is classified core. It is wired in and will fire from CHG-005; until then its limitation line says so, and a reader should not read its silence as a pass. CHG-003.3 repaired the wiring itself: before it, the rule was given the unclassified graph and would not have fired for any workspace.\n4. **The cycle rule may be unreachable in practice.** Cargo rejects a package cycle while resolving, so a cyclic workspace can fail before the checker sees it. Corpus C05 requires this rule's own finding — CHG-002.1 removed the `alternative` that would have let a cargo error count — so W4 will show whether the case reaches the checker at all.\n5. **`--transitive` is accepted and does nothing, and `[transitive] enabled = true` is refused** with exit 2 (CHG-003.1). This version has no transitive evaluator; the flag exists so the L2 interface is stable, and the rules-file setting is refused rather than run as if it had been honoured. The limitation line names the rule in both settings.\n6. **Glob matching is one trailing `*`.** Plan §5's examples need no more, and a full glob engine would be a dependency and a second syntax. A rule needing more will say so.\n7. **CHG-000 and CHG-001 were merged with no completed automated review.** Pull requests 1 and 2 received review usage-limit messages and zero completed passes (Codex's audit of the review history, 2026-09-20). The absence of review findings in those two acceptance records is not a clean review and should not be read as one. Nothing here changes those records.\n8. **The `controlled_transition` label is borrowed.** Spec §11.5 uses the term for a policy migration; `[acceptance.bootstrap]` applies it to every acceptance in the bootstrap period, including ordinary changes that touch no policy, such as the one this item's first commit recorded. A distinct kind is proposed in `docs/proposals/spec-v0.11.md` row 8 and needs a policy edit that only Kennedy makes; until then each record's `basis` says what the acceptance is, and the label stays.\n9. **EM-M03's cell.** `xtask/tests/corpus/manifest.toml` assigns EM-M03 only `law6-b3`, while its own hole describes a Law 3/D1 extraction miss (pull request 4 review finding, open at the time). Proposed edit, for your approval as a pre-registration correction made before any module checker exists: `cells = [\"law3-d1\", \"law6-b3\"]`. Until approved the manifest stands, and an observed miss on EM-M03 would be charged to the no-cycles cell alone.\n"
  },
  "docs/changes/CHG-004-h4-crate-harness.md": {
    "body": "[Heading { level: 1, slug: \"chg-004-h4-crate-level-harness\", children: [Text(\"CHG-004: H4 crate-level harness\")] }, Paragraph([Text(\"Task: \"), Link { href: \"../../.rha/tasks/CHG-004-h4-crate-harness.toml\", children: [Text(\"CHG-004\")] }, Text(\". Plan: §6 and §8 W4.\"), SoftBreak, Text(\"Base: \"), Code(\"140fdfb92d62d3481d95f3e2efc1d24a0206fcc4\"), Text(\", merge of PR 6.\")]), Heading { level: 2, slug: \"intent-and-scope\", children: [Text(\"Intent and scope\")] }, Paragraph([Text(\"Generate the crate-level fixtures the manifest pre-registered, run the accepted checker on them through the public command, grade every case under DP-1.1c, record the result whatever it is, and project it into the enforcement map. Result: 22/22 detected; the run is graded \"), Code(\"failed\"), Text(\" because C13 carries one accurate warning the registration did not name; no V is proposed (acceptance concern 1).\")]), Heading { level: 3, slug: \"required-order-and-approval\", children: [Text(\"Required order and approval\")] }, List { start: Some(1), items: [[Code(\"dedc8ccda78f6b35e5e30ce2cf5db97fdede240b\"), Text(\" records CHG-003 acceptance and archives the CI evidence for the accepted merge.\")], [Text(\"This commit changes EM-M03's cell association in the manifest to \"), Code(\"cells = [\\\"law3-d1\\\", \\\"law6-b3\\\"]\"), Text(\", with its pinned test row and this task record.\")], [Text(\"Harness implementation follows those commits.\")]] }, Paragraph([Text(\"Kennedy approved the protected manifest edit in the working conversation on 2026-09-20: “I approved the EM-M03 cell edit on the manifest: cells = \"), Text(\"[\"), Text(\"\\\"law3-d1\\\", \\\"law6-b3\\\"\"), Text(\"]\"), Text(\".” Under §9.14 this corrects the test's cell attribution with explicit authority. Every other case and all grading values remain fixed (DP-1.1c).\")]), Paragraph([Text(\"Kennedy runs his held-out cases at acceptance (DP-1.1b); they are never read by the Executor. Accepted maturity remains his decision (DP-1.6).\")]), Heading { level: 2, slug: \"deltas\", children: [Text(\"Deltas\")] }, Paragraph([Text(\"Kennedy's explicit W4 objective requires committed generated fixtures. \"), Code(\"cargo xtask corpus generate\"), Text(\" materializes the registered crate cases under \"), Code(\"xtask/tests/corpus/crate/{violations,legitimate,expected_miss}/<id>/\"), Text(\"; outside packages remain beside the associated workspace as their declared paths require. \"), Code(\"corpus generate --check\"), Text(\" detects added, changed and missing input files without repairing them. Only Cargo output (target directories and lockfiles) is excluded. The committed snapshots are derivative data; the manifest remains their sole semantic owner.\")]), Paragraph([Text(\"The earlier crate-fixture prohibition in \"), Code(\"corpus_manifest.rs\"), Text(\" is replaced by the drift gate and complete \"), Code(\"corpus_crate.rs\"), Text(\" test, explicitly changing the assurance strategy under §9.14 as instructed. Module fixture absence is still checked. No case or grading value changes.\")]), Paragraph([Code(\"cargo xtask corpus run --level crate\"), Text(\" runs the committed cases using actual \"), Code(\"cargo xtask architecture --manifest-path … --rules … --format json\"), Text(\" commands and \"), Code(\"cargo check --offline\"), Text(\" for R01. H4 schema 2 combines all 22 detect cases while retaining checker/compiler subtotals. It records the manifest's registration identity, digest, exact correction commit, tool identity, raw outputs, witnesses and holes. Every unmatched finding remains a false alarm under DP-1.1c. \"), Code(\"cargo xtask docs\"), Text(\" alone projects the latest archived H4 record into the enforcement map; every module cell remains not_run until CHG-007.\")]), Paragraph([Text(\"The first harness packet uses the accepted checker unchanged. Its failed run is retained before the checker repair determination and CHG-004.1 repair commit. Prior draft records remain archival observations of their named revisions; the final handoff will identify the replacement records explicitly.\")]), Heading { level: 2, slug: \"evidence\", children: [Text(\"Evidence\")] }, Heading { level: 3, slug: \"chg-0041-determination-before-repair\", children: [Text(\"CHG-004.1 determination, before repair\")] }, Paragraph([Link { href: \"../../evidence/h4-crate/20260920T121342Z-3c6c758911ff-1462546.json\", children: [Text(\"The first clean committed-fixture run\")] }, Text(\" describes \"), Code(\"3c6c758911ffa418fbe5b2678e7b538c36a69521\"), Text(\". It exits 1: \"), Strong([Text(\"13/22 detected\")]), Text(\", \"), Strong([Text(\"10/11 unmatched findings\")]), Text(\", two documented holes. R01 and all 11 legitimate cases pass; C04, C05, C08, C09, C10, C11, C13, C14 and C20 fail exact witness grading.\")]), Paragraph([Text(\"The raw reports show those checker violations, but their structured JSON omits registered \"), Code(\"crate\"), Text(\", cycle \"), Code(\"members\"), Text(\", \"), Code(\"matched_rule\"), Text(\", conflicting-role values, \"), Code(\"port\"), Text(\" or \"), Code(\"owner\"), Text(\" fields. Add those facts at the producer from its graph/rules observations; never derive an answer from the corpus inside checker code. The separate CHG-004.1 commit applies this repair after this determination and failed record. C13's additional \"), Code(\"adapter.foreign_core\"), Text(\" warning is accurate and must remain an unmatched finding under DP-1.1c; it is not suppressed to obtain a green H4 result.\")]), Heading { level: 3, slug: \"repaired-result\", children: [Text(\"Repaired result\")] }, Paragraph([Link { href: \"../../evidence/h4-crate/20260920T121505Z-f1c2c55a5550-1465488.json\", children: [Text(\"Clean H4 record\")] }, Text(\" describes \"), Code(\"f1c2c55a5550f5ee8852f83a46f03d5d2ac86292\"), Text(\": \"), Strong([Text(\"22/22 detected\")]), Text(\" (21 checker and R01), \"), Strong([Text(\"1/11 false alarms\")]), Text(\", \"), Strong([Text(\"2 documented holes\")]), Text(\", exit \"), Strong([Text(\"1\")]), Text(\". C13 alone fails because its accurate \"), Code(\"adapter.foreign_core\"), Text(\" warning is unmatched under DP-1.1c. No V promotion is proposed, and \"), Code(\"law3-d1\"), Text(\" is downgraded. All 16 corpus tests pass, including the full public command, byte-drift negative controls, and exact-witness/report validation.\")]), Heading { level: 3, slug: \"handoff-provenance\", children: [Text(\"Handoff provenance\")] }, Paragraph([Text(\"The approval decision owns the full EM-M03 correction commit. H4 reads this fixed provenance instead of treating the newest manifest edit as the historical correction. The integration test independently loads that commit and its parent and proves that their only manifest difference is the approved EM-M03 cell edit. Fixture input hashes are labelled as generated expectations; an actual mismatch is recorded as drift and fails the harness before execution.\")]), Paragraph([Text(\"Outside packages remain siblings of the C19 and EM-C01 workspace roots, as \"), Code(\"outside_crates.at\"), Text(\" requires; every case workspace itself is at its requested category/id path. The full fixture tree and its drift check include those external packages.\")]), Heading { level: 3, slug: \"final-h4-handoff\", children: [Text(\"Final H4 handoff\")] }, Paragraph([Link { href: \"../../evidence/h4-crate/20260920T121858Z-ac6f7c67af63-1471693.json\", children: [Text(\"Final clean H4 record\")] }, Text(\" identifies \"), Code(\"ac6f7c67af6384395a3710fc4805edd4fcf01903\"), Text(\". It reports 22/22 detected, 1/11 false alarms, two documented holes, 229 generated input hashes and no drift. The command exits 1; C13 is the sole failure. Every module cell remains not_run under CHG-007; no V promotion is proposed (DP-1.6). Kennedy alone runs DP-1.1b.\")]), Table { align: [None, None], head: [[Text(\"Case\")], [Text(\"Actual outcome\")]], rows: [[[Text(\"C01\")], [Text(\"Detected; passed\")]], [[Text(\"C02\")], [Text(\"Detected; passed\")]], [[Text(\"C03\")], [Text(\"Detected; passed\")]], [[Text(\"C04\")], [Text(\"Detected; passed\")]], [[Text(\"C05\")], [Text(\"Detected; passed\")]], [[Text(\"C06\")], [Text(\"Detected; passed\")]], [[Text(\"C07\")], [Text(\"Detected; passed\")]], [[Text(\"C08\")], [Text(\"Detected; passed\")]], [[Text(\"C09\")], [Text(\"Detected; passed\")]], [[Text(\"C10\")], [Text(\"Detected; passed\")]], [[Text(\"C11\")], [Text(\"Detected; passed\")]], [[Text(\"C12\")], [Text(\"Detected; passed\")]], [[Text(\"C13\")], [Text(\"Detected; failed: unmatched adapter.foreign_core warning\")]], [[Text(\"C14\")], [Text(\"Detected; passed\")]], [[Text(\"C15\")], [Text(\"Detected; passed\")]], [[Text(\"C16\")], [Text(\"Detected; passed\")]], [[Text(\"C17\")], [Text(\"Detected; passed\")]], [[Text(\"C18\")], [Text(\"Detected; passed\")]], [[Text(\"C19\")], [Text(\"Detected; passed\")]], [[Text(\"C20\")], [Text(\"Detected; passed\")]], [[Text(\"C21\")], [Text(\"Detected; passed\")]], [[Text(\"L01\")], [Text(\"No alarm; passed\")]], [[Text(\"L02\")], [Text(\"No alarm; passed\")]], [[Text(\"L03\")], [Text(\"No alarm; passed\")]], [[Text(\"L04\")], [Text(\"No alarm; passed\")]], [[Text(\"L05\")], [Text(\"No alarm; passed\")]], [[Text(\"L06\")], [Text(\"No alarm; passed\")]], [[Text(\"L07\")], [Text(\"No alarm; passed\")]], [[Text(\"L08\")], [Text(\"No alarm; passed\")]], [[Text(\"L09\")], [Text(\"No alarm; passed\")]], [[Text(\"L10\")], [Text(\"No alarm; passed\")]], [[Text(\"L11\")], [Text(\"No alarm; passed\")]], [[Text(\"EM-C01\")], [Text(\"Documented hole; passed with no findings\")]], [[Text(\"EM-C02\")], [Text(\"Documented hole; passed with no findings\")]], [[Text(\"R01\")], [Text(\"Detected E0603; passed (cargo exit 101)\")]]] }, Heading { level: 3, slug: \"local-l0-handoff\", children: [Text(\"Local L0 handoff\")] }, Paragraph([Link { href: \"../../evidence/CHG-004/20260920T122054Z-bef220d57566.json\", children: [Text(\"Clean local L0 record\")] }, Text(\" identifies \"), Code(\"bef220d57566868d5407fb03b9e19389b04c1d5f\"), Text(\": all eight checks passed and nextest selected 141 tests, including the complete committed corpus and drift controls. Authentic remains false under the bootstrap policy; the separate H4 run remains failed because of C13.\")]), Table { align: [None, None], head: [[Text(\"Check\")], [Text(\"Actual outcome\")]], rows: [[[Text(\"L0.fmt\")], [Text(\"passed\")]], [[Text(\"L0.clippy\")], [Text(\"passed\")]], [[Text(\"L0.nextest\")], [Text(\"passed\")]], [[Text(\"L0.doctest\")], [Text(\"passed\")]], [[Text(\"L0.architecture\")], [Text(\"passed\")]], [[Text(\"L0.deny\")], [Text(\"passed\")]], [[Text(\"L0.machete\")], [Text(\"passed\")]], [[Text(\"L0.typos\")], [Text(\"passed\")]]] }, Heading { level: 3, slug: \"chg-0043-shallow-checkout-provenance-determination\", children: [Text(\"CHG-004.3: shallow-checkout provenance determination\")] }, Paragraph([Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35510429218\", children: [Text(\"CI run 35510429218\")] }, Text(\", retained \"), Link { href: \"../../evidence/ci/35510429218.json\", children: [Text(\"unchanged\")] }, Text(\", failed only L0.nextest: 140 tests passed and the full-corpus test failed when \"), Code(\"15d916a\"), Text(\" could not be resolved. The protected workflow uses fetch-depth 2; the local checkout had those older objects, so its lane passed.\")]), Paragraph([Text(\"Historical citations must not require history downloads to grade the current fixtures. Read the original revision from CHG-002's existing acceptance record, and the correction from its approval decision. The decision now also records the parent and corrected manifest byte digests, measured from the actual correction commit and its parent before this repair. The test reconstructs only the approved cell change and compares both byte digests. Under §9.14 this replaces an unconditional deep-history dependency with a fixed provenance check that also runs in CI; it skips no assertion about the cases or their outcomes. The protected workflow remains unchanged. New clean H4 records also use the requested timestamp–revision filename without a process-id suffix; create-new semantics still prevent overwrites.\")]), Heading { level: 3, slug: \"delivery-h4-after-the-ci-repair\", children: [Text(\"Delivery H4 after the CI repair\")] }, Paragraph([Link { href: \"../../evidence/h4-crate/20260920T122638Z-eec489c71f25.json\", children: [Text(\"Clean delivery H4\")] }, Text(\" identifies \"), Code(\"eec489c71f259423bb4cc75a5650ecaa8c10c0c3\"), Text(\" and supersedes the earlier H4 records for this handoff. Every case has the same actual outcome as the full table above: 22/22 detected, 1/11 unmatched findings, two documented holes, C13 alone failed, exit 1. The registration and correction identities are cited from their owning records and verified byte digests; neither requires older Git objects on the CI runner.\")]), Paragraph([Link { href: \"../../evidence/CHG-004/20260920T122732Z-885a709f7f49.json\", children: [Text(\"Delivery local L0\")] }, Text(\" records the settled CI repair at \"), Code(\"885a709f7f49\"), Text(\": all eight checks passed, 141 nextest tests. Earlier records, including failed CI run 35510429218, remain unchanged and are superseded by these delivery records for this handoff.\")]), Heading { level: 3, slug: \"chg-0044-make-ready-review\", children: [Text(\"CHG-004.4: make-ready review\")] }, Paragraph([Text(\"Verified on a second machine at \"), Code(\"2ad75c8\"), Text(\": \"), Code(\"cargo xtask corpus generate --check\"), Text(\" reports 229 inputs and no drift; \"), Code(\"cargo xtask docs --check\"), Text(\" reports every generated file current; 141 tests pass; \"), Code(\"cargo xtask corpus run --level crate\"), Text(\" reproduces 22/22 detected, one unmatched finding, C13 the sole failure, exit 1.\")]), Paragraph([Strong([Text(\"CI\")]), Text(\": run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35510728858\", children: [Text(\"35510728858\")] }, Text(\" on the draft's head \"), Code(\"2ad75c8\"), Text(\", copied as \"), Link { href: \"../../evidence/ci/35510728858.json\", children: [Code(\"evidence/ci/35510728858.json\")] }, Text(\": all eight \"), Code(\"passed\"), Text(\", 141 tests; its subject tree \"), Code(\"a6012b56\"), Text(\" equals the head's tree. Determinations 1 to 5 are in the task record's decision \"), Code(\"make-ready-chg-004.4\"), Text(\": two journals removed, the task record reshaped, this section and the concerns below added, the CI record committed. Two commits carry this review, because a shell error committed the two removals and this record's restructuring under the message meant for the record commit: \"), Code(\"d13ff10\"), Text(\", whose message reads \\\"Commit the L0 record for CHG-004.4\\\" and which holds no record, and the commit after it with the task record, the tasks index and the CI record. The message is left as history rather than rewritten. The local record for the make-ready commit follows.\")]), Paragraph([Strong([Text(\"CHG-004.4, local\")]), Text(\": \"), Code(\"evidence/CHG-004/20260920T133957Z-921970a0e964.json\"), Text(\" at \"), Code(\"921970a\"), Text(\", on a clean tree: all eight \"), Code(\"passed\"), Text(\", 141 tests, eligibility \"), Code(\"blocked\"), Text(\" on \"), Code(\"Authentic\"), Text(\" alone.\")]), Heading { level: 3, slug: \"chg-0045-two-review-findings-on-2fe6732-determinations\", children: [Text(\"CHG-004.5: two review findings on \"), Code(\"2fe6732\"), Text(\", determinations\")] }, Paragraph([Text(\"Posted by the automated review when the PR left draft (that trigger is still on). Both name a concrete failure on the reviewed revision and are repaired in one commit, determination first; neither touches a rule, a case, a grading value, or the committed fixtures, and the H4 result is unchanged.\")]), List { start: Some(1), items: [[Strong([Text(\"P2, the generator could not converge. Confirmed.\")]), Text(\" \"), Code(\"corpus generate\"), Text(\" wrote every expected input and never removed a committed input the registration no longer produces, so after a manifest change the drift check kept failing and only manual deletion could clear it. Change: \"), Code(\"fixture::materialize\"), Text(\" makes the committed tree equal the declaration, writing and pruning; Cargo outputs (\"), Code(\"target\"), Text(\", \"), Code(\"Cargo.lock\"), Text(\") are neither inputs nor pruned, and a case directory emptied by pruning goes with its last input. \"), Code(\"sync\"), Text(\" uses it and names each pruned path on stderr. Result: \"), Code(\"materialize_prunes_inputs_the_registration_no_longer_produces\"), Text(\" seeds a stray input, an obsolete case directory and Cargo output; the stray input and the directory go, the output stays, drift is empty, and a second run prunes nothing.\")], [Strong([Text(\"P2, pre-observation failures lost their class. Confirmed.\")]), Text(\" A case that produced no observation was recorded as one untyped \"), Code(\"execution_error\"), Text(\" string, so a bad registration (an unsupported detector, fixture drift) and a detector that could not be spawned looked the same in evidence, losing the \"), Code(\"config_error\"), Text(\" and \"), Code(\"tool_error\"), Text(\" distinction the checker's own reports keep (plan §5). Change: \"), Code(\"runner::execute\"), Text(\" returns an \"), Code(\"ExecutionError\"), Text(\" with a class; the record carries \"), Code(\"error_class\"), Text(\" beside the message and the grade's reason names it. Result: \"), Code(\"execution_errors_keep_their_class\"), Text(\" asserts \"), Code(\"config_error\"), Text(\" for an unsupported detector and \"), Code(\"tool_error\"), Text(\" for a detector spawned in a missing directory.\")]] }, Paragraph([Strong([Text(\"CHG-004.5, local\")]), Text(\": \"), Code(\"evidence/CHG-004/20260920T185033Z-f14a4e4dde1b.json\"), Text(\" at \"), Code(\"f14a4e4\"), Text(\", on a clean tree: all eight \"), Code(\"passed\"), Text(\", 143 tests, eligibility \"), Code(\"blocked\"), Text(\" on \"), Code(\"Authentic\"), Text(\" alone; no fixture drift.\")]), Heading { level: 2, slug: \"acceptance-concerns\", children: [Text(\"Acceptance concerns\")] }, List { start: Some(1), items: [[Strong([Text(\"C13 is your decision.\")]), Text(\" The harness applied DP-1.1c as written: C13 is detected, and the checker's accurate \"), Code(\"adapter.foreign_core\"), Text(\" warning about \"), Code(\"adapter-x -> core-b\"), Text(\" is an unmatched finding, so the run is \"), Code(\"failed\"), Text(\", \"), Code(\"law3-d1\"), Text(\" is downgraded, and no V is proposed. The warning is not over-eager: plan §5 listed \"), Code(\"adapter.foreign_core\"), Text(\" (warn) before any data, and C13's seeded shape, an adapter depending on a core whose port it does not implement, is exactly its trigger. The registration is what did not anticipate it. Three ways to close this, all yours: (a) amend the registration, a protected edit recorded on the ledger first as a DP-1.1c amendment and disclosed as made after the data: give C13 \"), Code(\"expected_findings = [{ rule = \\\"adapter.foreign_core\\\", crate = \\\"adapter-x\\\", to = \\\"core-b\\\" }]\"), Text(\" and extend \"), Code(\"[grading] extra_findings\"), Text(\" so a finding matching an entry of a case's \"), Code(\"expected_findings\"), Text(\" is a registered fact, neither a detection nor a false alarm; a CHG-004.5 re-run is then recorded beside this one, and a V proposal for the crate level becomes honest with that disclosure; (b) re-seed C13 so the adapter's other dependency is external rather than a core, which also changes a pre-registered shape after the data and loses the two-fact case; (c) leave the result as failed, with \"), Code(\"law3-d1\"), Text(\" downgraded until a later item. The supervising session recommends (a).\")], [Strong([Text(\"Four records cite revisions absent from this branch.\")]), Text(\" The draft's history was rewritten before its first push; the two CHG-004 records for \"), Code(\"762ab47\"), Text(\" and \"), Code(\"540ce6a\"), Text(\" and the two H4 records for \"), Code(\"defbfb7\"), Text(\" and \"), Code(\"b8c3785\"), Text(\" describe commits nobody can check out. They are kept as observations, not deleted, and they are not the evidence for this candidate; the delivery records above are.\")], [Strong([Text(\"The integration test pins the failed outcome.\")]), Text(\" \"), Code(\"xtask/tests/corpus_crate.rs\"), Text(\" asserts \"), Code(\"failed_cases == [\\\"C13\\\"]\"), Text(\", so L0.nextest guards the observation, not a pass. Decision (a) or (b) changes that assertion in the same commit as the amendment.\")], [Strong([Text(\"Held-out cases (DP-1.1b) and the maturity proposal (DP-1.6) are yours.\")]), Text(\" The record's \"), Code(\"held_out\"), Text(\" entry is \"), Code(\"not_run\"), Text(\" with the reason; the maturity row proposes nothing beyond I.\")], [Strong([Text(\"Two journals were removed again\")]), Text(\" (CHG-004.4 determinations 1 and 2). If the executor's harness needs a place for its own lessons, it is outside \"), Code(\"docs/\"), Text(\", which the wiki renders.\")]] }]",
    "headings": "[Heading { level: 1, text: \"CHG-004: H4 crate-level harness\", slug: \"chg-004-h4-crate-level-harness\", base_slug: \"chg-004-h4-crate-level-harness\", line: 1 }, Heading { level: 2, text: \"Intent and scope\", slug: \"intent-and-scope\", base_slug: \"intent-and-scope\", line: 6 }, Heading { level: 3, text: \"Required order and approval\", slug: \"required-order-and-approval\", base_slug: \"required-order-and-approval\", line: 10 }, Heading { level: 2, text: \"Deltas\", slug: \"deltas\", base_slug: \"deltas\", line: 20 }, Heading { level: 2, text: \"Evidence\", slug: \"evidence\", base_slug: \"evidence\", line: 30 }, Heading { level: 3, text: \"CHG-004.1 determination, before repair\", slug: \"chg-0041-determination-before-repair\", base_slug: \"chg-0041-determination-before-repair\", line: 32 }, Heading { level: 3, text: \"Repaired result\", slug: \"repaired-result\", base_slug: \"repaired-result\", line: 38 }, Heading { level: 3, text: \"Handoff provenance\", slug: \"handoff-provenance\", base_slug: \"handoff-provenance\", line: 42 }, Heading { level: 3, text: \"Final H4 handoff\", slug: \"final-h4-handoff\", base_slug: \"final-h4-handoff\", line: 48 }, Heading { level: 3, text: \"Local L0 handoff\", slug: \"local-l0-handoff\", base_slug: \"local-l0-handoff\", line: 90 }, Heading { level: 3, text: \"CHG-004.3: shallow-checkout provenance determination\", slug: \"chg-0043-shallow-checkout-provenance-determination\", base_slug: \"chg-0043-shallow-checkout-provenance-determination\", line: 105 }, Heading { level: 3, text: \"Delivery H4 after the CI repair\", slug: \"delivery-h4-after-the-ci-repair\", base_slug: \"delivery-h4-after-the-ci-repair\", line: 111 }, Heading { level: 3, text: \"CHG-004.4: make-ready review\", slug: \"chg-0044-make-ready-review\", base_slug: \"chg-0044-make-ready-review\", line: 117 }, Heading { level: 3, text: \"CHG-004.5: two review findings on 2fe6732, determinations\", slug: \"chg-0045-two-review-findings-on-2fe6732-determinations\", base_slug: \"chg-0045-two-review-findings-on-2fe6732-determinations\", line: 125 }, Heading { level: 2, text: \"Acceptance concerns\", slug: \"acceptance-concerns\", base_slug: \"acceptance-concerns\", line: 134 }]",
    "markdown": "# CHG-004: H4 crate-level harness\n\nTask: [CHG-004](../../.rha/tasks/CHG-004-h4-crate-harness.toml). Plan: §6 and §8 W4.\nBase: `140fdfb92d62d3481d95f3e2efc1d24a0206fcc4`, merge of PR 6.\n\n## Intent and scope\n\nGenerate the crate-level fixtures the manifest pre-registered, run the accepted checker on them through the public command, grade every case under DP-1.1c, record the result whatever it is, and project it into the enforcement map. Result: 22/22 detected; the run is graded `failed` because C13 carries one accurate warning the registration did not name; no V is proposed (acceptance concern 1).\n\n### Required order and approval\n\n1. `dedc8ccda78f6b35e5e30ce2cf5db97fdede240b` records CHG-003 acceptance and archives the CI evidence for the accepted merge.\n2. This commit changes EM-M03's cell association in the manifest to `cells = [\"law3-d1\", \"law6-b3\"]`, with its pinned test row and this task record.\n3. Harness implementation follows those commits.\n\nKennedy approved the protected manifest edit in the working conversation on 2026-09-20: “I approved the EM-M03 cell edit on the manifest: cells = [\"law3-d1\", \"law6-b3\"].” Under §9.14 this corrects the test's cell attribution with explicit authority. Every other case and all grading values remain fixed (DP-1.1c).\n\nKennedy runs his held-out cases at acceptance (DP-1.1b); they are never read by the Executor. Accepted maturity remains his decision (DP-1.6).\n\n## Deltas\n\nKennedy's explicit W4 objective requires committed generated fixtures. `cargo xtask corpus generate` materializes the registered crate cases under `xtask/tests/corpus/crate/{violations,legitimate,expected_miss}/<id>/`; outside packages remain beside the associated workspace as their declared paths require. `corpus generate --check` detects added, changed and missing input files without repairing them. Only Cargo output (target directories and lockfiles) is excluded. The committed snapshots are derivative data; the manifest remains their sole semantic owner.\n\nThe earlier crate-fixture prohibition in `corpus_manifest.rs` is replaced by the drift gate and complete `corpus_crate.rs` test, explicitly changing the assurance strategy under §9.14 as instructed. Module fixture absence is still checked. No case or grading value changes.\n\n`cargo xtask corpus run --level crate` runs the committed cases using actual `cargo xtask architecture --manifest-path … --rules … --format json` commands and `cargo check --offline` for R01. H4 schema 2 combines all 22 detect cases while retaining checker/compiler subtotals. It records the manifest's registration identity, digest, exact correction commit, tool identity, raw outputs, witnesses and holes. Every unmatched finding remains a false alarm under DP-1.1c. `cargo xtask docs` alone projects the latest archived H4 record into the enforcement map; every module cell remains not_run until CHG-007.\n\nThe first harness packet uses the accepted checker unchanged. Its failed run is retained before the checker repair determination and CHG-004.1 repair commit. Prior draft records remain archival observations of their named revisions; the final handoff will identify the replacement records explicitly.\n\n## Evidence\n\n### CHG-004.1 determination, before repair\n\n[The first clean committed-fixture run](../../evidence/h4-crate/20260920T121342Z-3c6c758911ff-1462546.json) describes `3c6c758911ffa418fbe5b2678e7b538c36a69521`. It exits 1: **13/22 detected**, **10/11 unmatched findings**, two documented holes. R01 and all 11 legitimate cases pass; C04, C05, C08, C09, C10, C11, C13, C14 and C20 fail exact witness grading.\n\nThe raw reports show those checker violations, but their structured JSON omits registered `crate`, cycle `members`, `matched_rule`, conflicting-role values, `port` or `owner` fields. Add those facts at the producer from its graph/rules observations; never derive an answer from the corpus inside checker code. The separate CHG-004.1 commit applies this repair after this determination and failed record. C13's additional `adapter.foreign_core` warning is accurate and must remain an unmatched finding under DP-1.1c; it is not suppressed to obtain a green H4 result.\n\n### Repaired result\n\n[Clean H4 record](../../evidence/h4-crate/20260920T121505Z-f1c2c55a5550-1465488.json) describes `f1c2c55a5550f5ee8852f83a46f03d5d2ac86292`: **22/22 detected** (21 checker and R01), **1/11 false alarms**, **2 documented holes**, exit **1**. C13 alone fails because its accurate `adapter.foreign_core` warning is unmatched under DP-1.1c. No V promotion is proposed, and `law3-d1` is downgraded. All 16 corpus tests pass, including the full public command, byte-drift negative controls, and exact-witness/report validation.\n\n### Handoff provenance\n\nThe approval decision owns the full EM-M03 correction commit. H4 reads this fixed provenance instead of treating the newest manifest edit as the historical correction. The integration test independently loads that commit and its parent and proves that their only manifest difference is the approved EM-M03 cell edit. Fixture input hashes are labelled as generated expectations; an actual mismatch is recorded as drift and fails the harness before execution.\n\nOutside packages remain siblings of the C19 and EM-C01 workspace roots, as `outside_crates.at` requires; every case workspace itself is at its requested category/id path. The full fixture tree and its drift check include those external packages.\n\n### Final H4 handoff\n\n[Final clean H4 record](../../evidence/h4-crate/20260920T121858Z-ac6f7c67af63-1471693.json) identifies `ac6f7c67af6384395a3710fc4805edd4fcf01903`. It reports 22/22 detected, 1/11 false alarms, two documented holes, 229 generated input hashes and no drift. The command exits 1; C13 is the sole failure. Every module cell remains not_run under CHG-007; no V promotion is proposed (DP-1.6). Kennedy alone runs DP-1.1b.\n\n| Case | Actual outcome |\n| --- | --- |\n| C01 | Detected; passed |\n| C02 | Detected; passed |\n| C03 | Detected; passed |\n| C04 | Detected; passed |\n| C05 | Detected; passed |\n| C06 | Detected; passed |\n| C07 | Detected; passed |\n| C08 | Detected; passed |\n| C09 | Detected; passed |\n| C10 | Detected; passed |\n| C11 | Detected; passed |\n| C12 | Detected; passed |\n| C13 | Detected; failed: unmatched adapter.foreign_core warning |\n| C14 | Detected; passed |\n| C15 | Detected; passed |\n| C16 | Detected; passed |\n| C17 | Detected; passed |\n| C18 | Detected; passed |\n| C19 | Detected; passed |\n| C20 | Detected; passed |\n| C21 | Detected; passed |\n| L01 | No alarm; passed |\n| L02 | No alarm; passed |\n| L03 | No alarm; passed |\n| L04 | No alarm; passed |\n| L05 | No alarm; passed |\n| L06 | No alarm; passed |\n| L07 | No alarm; passed |\n| L08 | No alarm; passed |\n| L09 | No alarm; passed |\n| L10 | No alarm; passed |\n| L11 | No alarm; passed |\n| EM-C01 | Documented hole; passed with no findings |\n| EM-C02 | Documented hole; passed with no findings |\n| R01 | Detected E0603; passed (cargo exit 101) |\n\n### Local L0 handoff\n\n[Clean local L0 record](../../evidence/CHG-004/20260920T122054Z-bef220d57566.json) identifies `bef220d57566868d5407fb03b9e19389b04c1d5f`: all eight checks passed and nextest selected 141 tests, including the complete committed corpus and drift controls. Authentic remains false under the bootstrap policy; the separate H4 run remains failed because of C13.\n\n| Check | Actual outcome |\n| --- | --- |\n| L0.fmt | passed |\n| L0.clippy | passed |\n| L0.nextest | passed |\n| L0.doctest | passed |\n| L0.architecture | passed |\n| L0.deny | passed |\n| L0.machete | passed |\n| L0.typos | passed |\n\n### CHG-004.3: shallow-checkout provenance determination\n\n[CI run 35510429218](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35510429218), retained [unchanged](../../evidence/ci/35510429218.json), failed only L0.nextest: 140 tests passed and the full-corpus test failed when `15d916a` could not be resolved. The protected workflow uses fetch-depth 2; the local checkout had those older objects, so its lane passed.\n\nHistorical citations must not require history downloads to grade the current fixtures. Read the original revision from CHG-002's existing acceptance record, and the correction from its approval decision. The decision now also records the parent and corrected manifest byte digests, measured from the actual correction commit and its parent before this repair. The test reconstructs only the approved cell change and compares both byte digests. Under §9.14 this replaces an unconditional deep-history dependency with a fixed provenance check that also runs in CI; it skips no assertion about the cases or their outcomes. The protected workflow remains unchanged. New clean H4 records also use the requested timestamp–revision filename without a process-id suffix; create-new semantics still prevent overwrites.\n\n### Delivery H4 after the CI repair\n\n[Clean delivery H4](../../evidence/h4-crate/20260920T122638Z-eec489c71f25.json) identifies `eec489c71f259423bb4cc75a5650ecaa8c10c0c3` and supersedes the earlier H4 records for this handoff. Every case has the same actual outcome as the full table above: 22/22 detected, 1/11 unmatched findings, two documented holes, C13 alone failed, exit 1. The registration and correction identities are cited from their owning records and verified byte digests; neither requires older Git objects on the CI runner.\n\n[Delivery local L0](../../evidence/CHG-004/20260920T122732Z-885a709f7f49.json) records the settled CI repair at `885a709f7f49`: all eight checks passed, 141 nextest tests. Earlier records, including failed CI run 35510429218, remain unchanged and are superseded by these delivery records for this handoff.\n\n### CHG-004.4: make-ready review\n\nVerified on a second machine at `2ad75c8`: `cargo xtask corpus generate --check` reports 229 inputs and no drift; `cargo xtask docs --check` reports every generated file current; 141 tests pass; `cargo xtask corpus run --level crate` reproduces 22/22 detected, one unmatched finding, C13 the sole failure, exit 1.\n\n**CI**: run [35510728858](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35510728858) on the draft's head `2ad75c8`, copied as [`evidence/ci/35510728858.json`](../../evidence/ci/35510728858.json): all eight `passed`, 141 tests; its subject tree `a6012b56` equals the head's tree. Determinations 1 to 5 are in the task record's decision `make-ready-chg-004.4`: two journals removed, the task record reshaped, this section and the concerns below added, the CI record committed. Two commits carry this review, because a shell error committed the two removals and this record's restructuring under the message meant for the record commit: `d13ff10`, whose message reads \"Commit the L0 record for CHG-004.4\" and which holds no record, and the commit after it with the task record, the tasks index and the CI record. The message is left as history rather than rewritten. The local record for the make-ready commit follows.\n\n**CHG-004.4, local**: `evidence/CHG-004/20260920T133957Z-921970a0e964.json` at `921970a`, on a clean tree: all eight `passed`, 141 tests, eligibility `blocked` on `Authentic` alone.\n\n### CHG-004.5: two review findings on `2fe6732`, determinations\n\nPosted by the automated review when the PR left draft (that trigger is still on). Both name a concrete failure on the reviewed revision and are repaired in one commit, determination first; neither touches a rule, a case, a grading value, or the committed fixtures, and the H4 result is unchanged.\n\n1. **P2, the generator could not converge. Confirmed.** `corpus generate` wrote every expected input and never removed a committed input the registration no longer produces, so after a manifest change the drift check kept failing and only manual deletion could clear it. Change: `fixture::materialize` makes the committed tree equal the declaration, writing and pruning; Cargo outputs (`target`, `Cargo.lock`) are neither inputs nor pruned, and a case directory emptied by pruning goes with its last input. `sync` uses it and names each pruned path on stderr. Result: `materialize_prunes_inputs_the_registration_no_longer_produces` seeds a stray input, an obsolete case directory and Cargo output; the stray input and the directory go, the output stays, drift is empty, and a second run prunes nothing.\n2. **P2, pre-observation failures lost their class. Confirmed.** A case that produced no observation was recorded as one untyped `execution_error` string, so a bad registration (an unsupported detector, fixture drift) and a detector that could not be spawned looked the same in evidence, losing the `config_error` and `tool_error` distinction the checker's own reports keep (plan §5). Change: `runner::execute` returns an `ExecutionError` with a class; the record carries `error_class` beside the message and the grade's reason names it. Result: `execution_errors_keep_their_class` asserts `config_error` for an unsupported detector and `tool_error` for a detector spawned in a missing directory.\n\n**CHG-004.5, local**: `evidence/CHG-004/20260920T185033Z-f14a4e4dde1b.json` at `f14a4e4`, on a clean tree: all eight `passed`, 143 tests, eligibility `blocked` on `Authentic` alone; no fixture drift.\n\n## Acceptance concerns\n\n1. **C13 is your decision.** The harness applied DP-1.1c as written: C13 is detected, and the checker's accurate `adapter.foreign_core` warning about `adapter-x -> core-b` is an unmatched finding, so the run is `failed`, `law3-d1` is downgraded, and no V is proposed. The warning is not over-eager: plan §5 listed `adapter.foreign_core` (warn) before any data, and C13's seeded shape, an adapter depending on a core whose port it does not implement, is exactly its trigger. The registration is what did not anticipate it. Three ways to close this, all yours: (a) amend the registration, a protected edit recorded on the ledger first as a DP-1.1c amendment and disclosed as made after the data: give C13 `expected_findings = [{ rule = \"adapter.foreign_core\", crate = \"adapter-x\", to = \"core-b\" }]` and extend `[grading] extra_findings` so a finding matching an entry of a case's `expected_findings` is a registered fact, neither a detection nor a false alarm; a CHG-004.5 re-run is then recorded beside this one, and a V proposal for the crate level becomes honest with that disclosure; (b) re-seed C13 so the adapter's other dependency is external rather than a core, which also changes a pre-registered shape after the data and loses the two-fact case; (c) leave the result as failed, with `law3-d1` downgraded until a later item. The supervising session recommends (a).\n2. **Four records cite revisions absent from this branch.** The draft's history was rewritten before its first push; the two CHG-004 records for `762ab47` and `540ce6a` and the two H4 records for `defbfb7` and `b8c3785` describe commits nobody can check out. They are kept as observations, not deleted, and they are not the evidence for this candidate; the delivery records above are.\n3. **The integration test pins the failed outcome.** `xtask/tests/corpus_crate.rs` asserts `failed_cases == [\"C13\"]`, so L0.nextest guards the observation, not a pass. Decision (a) or (b) changes that assertion in the same commit as the amendment.\n4. **Held-out cases (DP-1.1b) and the maturity proposal (DP-1.6) are yours.** The record's `held_out` entry is `not_run` with the reason; the maturity row proposes nothing beyond I.\n5. **Two journals were removed again** (CHG-004.4 determinations 1 and 2). If the executor's harness needs a place for its own lessons, it is outside `docs/`, which the wiki renders.\n"
  },
  "docs/changes/CHG-004.6-c13-registration.md": {
    "body": "[Heading { level: 1, slug: \"chg-0046-the-c13-registration-amendment-the-h4-re-run-and-the-held-out-runner-under-xtask\", children: [Text(\"CHG-004.6: the C13 registration amendment, the H4 re-run, and the held-out runner under xtask\")] }, Paragraph([Text(\"Task record: \"), Link { href: \"../../.rha/tasks/CHG-004.6-c13-registration.toml\", children: [Code(\".rha/tasks/CHG-004.6-c13-registration.toml\")] }, Text(\".\"), SoftBreak, Text(\"Plan: §6 and §8 W4; CHG-004 acceptance concern 1, option (a).\"), SoftBreak, Text(\"Base: \"), Code(\"434b706\"), Text(\", the merge that accepted CHG-004.\")]), Heading { level: 2, slug: \"intent-and-scope\", children: [Text(\"Intent and scope\")] }, Paragraph([Text(\"CHG-004's corpus run detected 22 of 22 violations and was graded \"), Code(\"failed\"), Text(\" on one case: C13's fixture, an adapter that claims a port of \"), Code(\"core-a\"), Text(\" but depends only on \"), Code(\"core-b\"), Text(\", necessarily makes the checker emit \"), Code(\"adapter.foreign_core\"), Text(\" for that dependency, a warning plan §5 listed before any data existed, and the registration never named it. Kennedy chose option (a): amend the registration, on the record, after the data. This item applies that amendment, grades registered facts by the same every-key rule as witnesses, re-runs the corpus, and proposes crate-level V only if the amended run passes, with the amendment disclosed wherever the result is cited.\")]), Paragraph([Text(\"It also carries the held-out runner Kennedy asked for, under xtask. A Python draft under a new \"), Code(\"scripts/\"), Text(\" directory, prepared in the executor's worktree and never committed, was not adopted: the plan's layout has no \"), Code(\"scripts/\"), Text(\" directory, the tool baseline lists no Python, and the lane's checks would not cover it (task record decision \"), Code(\"delegated-held-out-execution\"), Text(\").\")]), Heading { level: 3, slug: \"required-order\", children: [Text(\"Required order\")] }, List { start: Some(1), items: [[Code(\"11900ef\"), Text(\": the CHG-004 acceptance record, with the CI record of run 35530722005 on \"), Code(\"main\"), Text(\" and a clean baseline H4 run on the accepted merge. The held-out cases were not run at acceptance and the record says so.\")], [Code(\"6fe9340\"), Text(\": the DP-1.1c amendment (the registered-fact sentence, disclosed as made after the data) and the DP-1.1b amendment (who operates the held-out run, and the commitment command), plus this item's task record.\")], [Code(\"b671fb773a753d2c7f30116d3703fd7c99048bb3\"), Text(\": the protected manifest amendment, exactly as approved, with the corpus model, its validation, the pin test and the runner's amendment chain, so the commit parses and grades on its own; C13 still graded failed there.\")], [Text(\"This commit: grading honours registered facts; the integration test moves to the amended expectation under §9.14; \"), Code(\"cargo xtask corpus held-out\"), Text(\"; the CONTRIBUTING paragraph; the commit pinned in the approval decision.\")], [Text(\"The measured re-run, its evidence, the regenerated enforcement map and the Proposed maturity row.\")]] }, Heading { level: 2, slug: \"deltas\", children: [Text(\"Deltas\")] }, List { start: None, items: [[Strong([Text(\"Manifest\")]), Text(\" (protected, approved): C13 gains \"), Code(\"expected_findings = [{ rule = \\\"adapter.foreign_core\\\", crate = \\\"adapter-x\\\", to = \\\"core-b\\\" }]\"), Text(\"; \"), Code(\"[grading] extra_findings\"), Text(\" gains one sentence: \\\"A finding matching an entry of a case's expected_findings is a registered fact, neither a detection nor a false alarm.\\\" Nothing else changes.\")], [Strong([Text(\"Model and validation\")]), Text(\": \"), Code(\"Case.expected_findings\"), Text(\"; a registered fact on a non-detect case, an empty entry, or a prose key is a manifest defect. \"), Code(\"xtask/tests/corpus_manifest.rs\"), Text(\" pins C13 as the only case that registers a fact, with exactly those three keys.\")], [Strong([Text(\"Grading\")]), Text(\": \"), Code(\"grade::registered\"), Text(\" matches by every key. On a detect case a finding that matches the witness is the detection; one that matches a registered entry is a registered fact; anything else is a false alarm. A registered fact never supplies a detection. The grade carries \"), Code(\"registered_facts\"), Text(\", and the run summary counts them.\")], [Strong([Text(\"Provenance\")]), Text(\": the runner records the manifest's amendment chain, pre-registration then EM-M03 (CHG-004) then C13 (CHG-004.6), each link owned by its approval decision, and refuses to run if the manifest's digest is not the last approved one. \"), Code(\"xtask/tests/corpus_crate.rs\"), Text(\" reconstructs both amendments from the manifest's bytes and checks every digest.\")], [Strong([Text(\"Held-out runner\")]), Text(\": \"), Code(\"cargo xtask corpus held-out --archive <tar>\"), Text(\" or \"), Code(\"--cases <dir>\"), Text(\", with \"), Code(\"--purpose public_control\"), Text(\" for exercising it on public fixtures and \"), Code(\"--private-dir\"), Text(\" for tests. It verifies the commitment (archive bytes, or the committed tar stream re-created for a directory), refuses a mismatched archive, checks archive members before extracting, finds ready workspaces without following links, runs the accepted checker through the same command the corpus uses, prints only opaque ids, exit statuses, counts and the checker's identity, and keeps raw reports and the id-to-path map in a private directory. It grades nothing.\")], [Strong([Text(\"Ledger\")]), Text(\": DP-1.1c and DP-1.1b amendment keys. \"), Strong([Text(\"CONTRIBUTING\")]), Text(\": one paragraph on the held-out command.\")]] }, Heading { level: 3, slug: \"test-expectation-change-under-914\", children: [Text(\"Test-expectation change under §9.14\")] }, Paragraph([Text(\"Until this item, \"), Code(\"xtask/tests/corpus_crate.rs\"), Text(\" asserted command exit 1, one unmatched finding, \"), Code(\"failed_cases == [\\\"C13\\\"]\"), Text(\" and an overall \"), Code(\"failed\"), Text(\" result, and \"), Code(\"xtask/tests/corpus_harness.rs\"), Text(\" counted C13's warning as an unmatched finding in the headline. Those assertions were correct for the registration as it stood. The approved amendment changes the requirement, not the observation: the integration test now asserts exit 0, zero unmatched findings, no failed case, an overall \"), Code(\"passed\"), Text(\" result, 22 detections, both documented holes, and C13's warning under \"), Code(\"registered_facts\"), Text(\" and nowhere else. The negative controls in \"), Code(\"a_registered_fact_is_neither_a_detection_nor_a_false_alarm\"), Text(\" show that a changed or missing key is not the registered fact, that an unregistered extra finding still fails the case, that a registered fact cannot stand in for the witness, and that the same warning on C01 is still a false alarm.\")]), Heading { level: 2, slug: \"evidence\", children: [Text(\"Evidence\")] }, List { start: None, items: [[Paragraph([Text(\"CHG-004 acceptance: \"), Link { href: \"../../evidence/ci/35530722005.json\", children: [Code(\"evidence/ci/35530722005.json\")] }, Text(\", all eight L0 checks passed at the merge; baseline H4 on the merge, \"), Link { href: \"../../evidence/h4-crate/20260920T191132Z-434b7064e3f2.json\", children: [Code(\"evidence/h4-crate/20260920T191132Z-434b7064e3f2.json\")] }, Text(\": 22/22, C13 the sole graded failure under the registration as it stood.\")])], [Paragraph([Text(\"The failed runs of CHG-004 stay beside the amended run and are cited by the acceptance record and the DP-1.1c row.\")])], [Paragraph([Strong([Text(\"Amended H4 run\")]), Text(\": \"), Link { href: \"../../evidence/h4-crate/20260921T041705Z-f4dac8dc5710.json\", children: [Code(\"evidence/h4-crate/20260921T041705Z-f4dac8dc5710.json\")] }, Text(\" at \"), Code(\"f4dac8d\"), Text(\", clean tree: 22/22 detected, 0 unmatched findings, 0 legitimate cases with an alarm, 1 registered fact (C13, \"), Code(\"adapter.foreign_core\"), Text(\"), EM-C01 and EM-C02 documented, exit 0; the record's \"), Code(\"manifest.amendments\"), Text(\" chain names both approved amendments. \"), Code(\"docs/enforcement-map.md\"), Text(\" regenerated from it: \"), Code(\"law3-d1\"), Text(\" validated again.\")])], [Paragraph([Strong([Text(\"L0, local\")]), Text(\": \"), Code(\"evidence/CHG-004.6/20260921T041712Z-7e4a70cc3759.json\"), Text(\" at \"), Code(\"7e4a70c\"), Text(\", clean tree: all eight \"), Code(\"passed\"), Text(\", 149 tests, no fixture drift, eligibility \"), Code(\"blocked\"), Text(\" on \"), Code(\"Authentic\"), Text(\" alone.\")])], [Paragraph([Strong([Text(\"L0, CI\")]), Text(\": run \"), Link { href: \"https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35560448177\", children: [Text(\"35560448177\")] }, Text(\" on the pushed head \"), Code(\"c16f3ca\"), Text(\", copied as \"), Link { href: \"../../evidence/ci/35560448177.json\", children: [Code(\"evidence/ci/35560448177.json\")] }, Text(\", subject \"), Code(\"ef4102a7\"), Text(\": all eight \"), Code(\"passed\"), Text(\", 149 tests.\")])]] }, Heading { level: 3, slug: \"chg-00461-local-codex-review-of-5ef607c-determinations\", children: [Text(\"CHG-004.6.1: local Codex review of \"), Code(\"5ef607c\"), Text(\", determinations\")] }, Paragraph([Text(\"Kennedy asked for a review by the local Codex agent (gpt-5.6-sol, medium effort, read-only). It requested changes with five findings; each names a concrete failure on the reviewed revision, and each is repaired in one commit after this one.\")]), List { start: Some(1), items: [[Strong([Text(\"P1, a mismatched directory still ran.\")]), Text(\" \"), Code(\"--cases\"), Text(\" observed the cases whatever the commitment check said, so altered cases could be reported with exit 0. For the \"), Code(\"held_out\"), Text(\" purpose anything but \"), Code(\"matched\"), Text(\", including \"), Code(\"unverified\"), Text(\", now refuses with exit 2, as the archive path already did; \"), Code(\"public_control\"), Text(\" still runs on public fixtures.\")], [Strong([Text(\"P1, the printed summary named the private report directory.\")]), Text(\" Printing it invites reading raw reports. The printed summary drops \"), Code(\"private_reports\"), Text(\"; \"), Code(\"summary.json\"), Text(\" inside that directory keeps it for Kennedy.\")], [Strong([Text(\"P1, archive members were checked by name only.\")]), Text(\" A symlink or hard-link member could redirect a later member outside the extraction directory. Members other than regular files and directories are refused, from \"), Code(\"tar -tvf\"), Text(\", before anything is extracted.\")], [Strong([Text(\"P1, the amendment chain was not anchored and skipped a link.\")]), Text(\" The manifest at the pre-registration \"), Code(\"15d916a\"), Text(\" hashes to \"), Code(\"f4fcef6f…\"), Text(\"; CHG-002.2's DP-1.1c grading decision changed it to \"), Code(\"9f5c1a93…\"), Text(\" at \"), Code(\"4be4a19\"), Text(\", before EM-M03, and the chain never recorded that. The chain now starts at the pre-registration digest, records the CHG-002.2 link, and where a link's commit object exists verifies that the manifest at that commit hashes to its corrected digest (\"), Code(\"commit_verified\"), Text(\"); a shallow checkout reports the object absent instead of assuming it.\")], [Strong([Text(\"P2, the approved sentence ran on from the previous one.\")]), Text(\" It read \\\"derived from the other A finding…\\\". A full stop is inserted: punctuation inside the approved change, not a new amendment. The corrected digest and the pinned commit move with it.\")]] }, List { start: None, items: [[Paragraph([Strong([Text(\"Amended H4 after CHG-004.6.1\")]), Text(\": \"), Link { href: \"../../evidence/h4-crate/20260922T194055Z-0d0356aa1d9c.json\", children: [Code(\"evidence/h4-crate/20260922T194055Z-0d0356aa1d9c.json\")] }, Text(\" at \"), Code(\"0d0356a\"), Text(\", clean tree: 22/22, 0 unmatched findings, 1 registered fact, exit 0; the amendment chain has three links from the pre-registration digest, CHG-002.2, CHG-004 and CHG-004.6, each \"), Code(\"commit_verified: true\"), Text(\". (Corrected from \\\"four links\\\" after the Codex re-review; the anchor and the CHG-002.2 link now also fail closed, and the next H4 record carries \"), Code(\"anchor_verified\"), Text(\".) It supersedes the earlier amended run for this item.\")])], [Paragraph([Strong([Text(\"L0 after CHG-004.6.1\")]), Text(\": \"), Code(\"evidence/CHG-004.6/20260922T194102Z-8a723508c627.json\"), Text(\" at \"), Code(\"8a72350\"), Text(\", clean tree: all eight \"), Code(\"passed\"), Text(\", 151 tests.\")])]] }, Heading { level: 3, slug: \"chg-00461-re-review-of-b423829\", children: [Text(\"CHG-004.6.1, re-review of \"), Code(\"b423829\")] }, Paragraph([Text(\"The local Codex agent confirmed findings 1, 2, 3 and 5 resolved. On 4 it found the anchor constant was never compared with the manifest at \"), Code(\"15d916a\"), Text(\", and a mismatch on the CHG-002.2 link was only reported, not refused: both now fail closed where the commits are present, and the record carries \"), Code(\"anchor_verified\"), Text(\". It also found this record claimed four links where the evidence holds three; corrected above.\")]), List { start: None, items: [[Strong([Text(\"Final amended H4\")]), Text(\": \"), Link { href: \"../../evidence/h4-crate/20260922T194637Z-f13f2bd5df1d.json\", children: [Code(\"evidence/h4-crate/20260922T194637Z-f13f2bd5df1d.json\")] }, Text(\" at \"), Code(\"f13f2bd\"), Text(\": 22/22, 0 unmatched, 1 registered fact, exit 0; \"), Code(\"anchor_verified\"), Text(\" and every \"), Code(\"commit_verified\"), Text(\" true.\")]] }, Heading { level: 2, slug: \"acceptance-concerns\", children: [Text(\"Acceptance concerns\")] }, List { start: Some(1), items: [[Strong([Text(\"The amendment follows the data.\")]), Text(\" It is disclosed on the DP-1.1c row, in every H4 record graded under it (the \"), Code(\"amendments\"), Text(\" chain and the \"), Code(\"registered_facts\"), Text(\" count), in the maturity row, and here. The failed records are kept. If you would rather the crate level stayed at I until a case authored after the amendment confirms it, decline the V proposal at DP-1.6; the harness and the amendment stand either way.\")], [Strong([Text(\"The held-out run has not happened.\")]), Text(\" No agent has the cases. When you supply the original archive or the directory, the executor's process runs \"), Code(\"cargo xtask corpus held-out\"), Text(\", and the observation is recorded as post-acceptance evidence for you to grade (DP-1.1b as amended). The DP-1.1b amendment itself is a protected edit made on your instructions; veto it at merge if the delegation is not what you meant.\")], [Strong([Text(\"DP-1.6 is yours.\")]), Text(\" The maturity row proposes; you accept.\")]] }]",
    "headings": "[Heading { level: 1, text: \"CHG-004.6: the C13 registration amendment, the H4 re-run, and the held-out runner under xtask\", slug: \"chg-0046-the-c13-registration-amendment-the-h4-re-run-and-the-held-out-runner-under-xtask\", base_slug: \"chg-0046-the-c13-registration-amendment-the-h4-re-run-and-the-held-out-runner-under-xtask\", line: 1 }, Heading { level: 2, text: \"Intent and scope\", slug: \"intent-and-scope\", base_slug: \"intent-and-scope\", line: 7 }, Heading { level: 3, text: \"Required order\", slug: \"required-order\", base_slug: \"required-order\", line: 13 }, Heading { level: 2, text: \"Deltas\", slug: \"deltas\", base_slug: \"deltas\", line: 21 }, Heading { level: 3, text: \"Test-expectation change under §9.14\", slug: \"test-expectation-change-under-914\", base_slug: \"test-expectation-change-under-914\", line: 30 }, Heading { level: 2, text: \"Evidence\", slug: \"evidence\", base_slug: \"evidence\", line: 34 }, Heading { level: 3, text: \"CHG-004.6.1: local Codex review of 5ef607c, determinations\", slug: \"chg-00461-local-codex-review-of-5ef607c-determinations\", base_slug: \"chg-00461-local-codex-review-of-5ef607c-determinations\", line: 44 }, Heading { level: 3, text: \"CHG-004.6.1, re-review of b423829\", slug: \"chg-00461-re-review-of-b423829\", base_slug: \"chg-00461-re-review-of-b423829\", line: 58 }, Heading { level: 2, text: \"Acceptance concerns\", slug: \"acceptance-concerns\", base_slug: \"acceptance-concerns\", line: 64 }]",
    "markdown": "# CHG-004.6: the C13 registration amendment, the H4 re-run, and the held-out runner under xtask\n\nTask record: [`.rha/tasks/CHG-004.6-c13-registration.toml`](../../.rha/tasks/CHG-004.6-c13-registration.toml).\nPlan: §6 and §8 W4; CHG-004 acceptance concern 1, option (a).\nBase: `434b706`, the merge that accepted CHG-004.\n\n## Intent and scope\n\nCHG-004's corpus run detected 22 of 22 violations and was graded `failed` on one case: C13's fixture, an adapter that claims a port of `core-a` but depends only on `core-b`, necessarily makes the checker emit `adapter.foreign_core` for that dependency, a warning plan §5 listed before any data existed, and the registration never named it. Kennedy chose option (a): amend the registration, on the record, after the data. This item applies that amendment, grades registered facts by the same every-key rule as witnesses, re-runs the corpus, and proposes crate-level V only if the amended run passes, with the amendment disclosed wherever the result is cited.\n\nIt also carries the held-out runner Kennedy asked for, under xtask. A Python draft under a new `scripts/` directory, prepared in the executor's worktree and never committed, was not adopted: the plan's layout has no `scripts/` directory, the tool baseline lists no Python, and the lane's checks would not cover it (task record decision `delegated-held-out-execution`).\n\n### Required order\n\n1. `11900ef`: the CHG-004 acceptance record, with the CI record of run 35530722005 on `main` and a clean baseline H4 run on the accepted merge. The held-out cases were not run at acceptance and the record says so.\n2. `6fe9340`: the DP-1.1c amendment (the registered-fact sentence, disclosed as made after the data) and the DP-1.1b amendment (who operates the held-out run, and the commitment command), plus this item's task record.\n3. `b671fb773a753d2c7f30116d3703fd7c99048bb3`: the protected manifest amendment, exactly as approved, with the corpus model, its validation, the pin test and the runner's amendment chain, so the commit parses and grades on its own; C13 still graded failed there.\n4. This commit: grading honours registered facts; the integration test moves to the amended expectation under §9.14; `cargo xtask corpus held-out`; the CONTRIBUTING paragraph; the commit pinned in the approval decision.\n5. The measured re-run, its evidence, the regenerated enforcement map and the Proposed maturity row.\n\n## Deltas\n\n- **Manifest** (protected, approved): C13 gains `expected_findings = [{ rule = \"adapter.foreign_core\", crate = \"adapter-x\", to = \"core-b\" }]`; `[grading] extra_findings` gains one sentence: \"A finding matching an entry of a case's expected_findings is a registered fact, neither a detection nor a false alarm.\" Nothing else changes.\n- **Model and validation**: `Case.expected_findings`; a registered fact on a non-detect case, an empty entry, or a prose key is a manifest defect. `xtask/tests/corpus_manifest.rs` pins C13 as the only case that registers a fact, with exactly those three keys.\n- **Grading**: `grade::registered` matches by every key. On a detect case a finding that matches the witness is the detection; one that matches a registered entry is a registered fact; anything else is a false alarm. A registered fact never supplies a detection. The grade carries `registered_facts`, and the run summary counts them.\n- **Provenance**: the runner records the manifest's amendment chain, pre-registration then EM-M03 (CHG-004) then C13 (CHG-004.6), each link owned by its approval decision, and refuses to run if the manifest's digest is not the last approved one. `xtask/tests/corpus_crate.rs` reconstructs both amendments from the manifest's bytes and checks every digest.\n- **Held-out runner**: `cargo xtask corpus held-out --archive <tar>` or `--cases <dir>`, with `--purpose public_control` for exercising it on public fixtures and `--private-dir` for tests. It verifies the commitment (archive bytes, or the committed tar stream re-created for a directory), refuses a mismatched archive, checks archive members before extracting, finds ready workspaces without following links, runs the accepted checker through the same command the corpus uses, prints only opaque ids, exit statuses, counts and the checker's identity, and keeps raw reports and the id-to-path map in a private directory. It grades nothing.\n- **Ledger**: DP-1.1c and DP-1.1b amendment keys. **CONTRIBUTING**: one paragraph on the held-out command.\n\n### Test-expectation change under §9.14\n\nUntil this item, `xtask/tests/corpus_crate.rs` asserted command exit 1, one unmatched finding, `failed_cases == [\"C13\"]` and an overall `failed` result, and `xtask/tests/corpus_harness.rs` counted C13's warning as an unmatched finding in the headline. Those assertions were correct for the registration as it stood. The approved amendment changes the requirement, not the observation: the integration test now asserts exit 0, zero unmatched findings, no failed case, an overall `passed` result, 22 detections, both documented holes, and C13's warning under `registered_facts` and nowhere else. The negative controls in `a_registered_fact_is_neither_a_detection_nor_a_false_alarm` show that a changed or missing key is not the registered fact, that an unregistered extra finding still fails the case, that a registered fact cannot stand in for the witness, and that the same warning on C01 is still a false alarm.\n\n## Evidence\n\n- CHG-004 acceptance: [`evidence/ci/35530722005.json`](../../evidence/ci/35530722005.json), all eight L0 checks passed at the merge; baseline H4 on the merge, [`evidence/h4-crate/20260920T191132Z-434b7064e3f2.json`](../../evidence/h4-crate/20260920T191132Z-434b7064e3f2.json): 22/22, C13 the sole graded failure under the registration as it stood.\n- The failed runs of CHG-004 stay beside the amended run and are cited by the acceptance record and the DP-1.1c row.\n- **Amended H4 run**: [`evidence/h4-crate/20260921T041705Z-f4dac8dc5710.json`](../../evidence/h4-crate/20260921T041705Z-f4dac8dc5710.json) at `f4dac8d`, clean tree: 22/22 detected, 0 unmatched findings, 0 legitimate cases with an alarm, 1 registered fact (C13, `adapter.foreign_core`), EM-C01 and EM-C02 documented, exit 0; the record's `manifest.amendments` chain names both approved amendments. `docs/enforcement-map.md` regenerated from it: `law3-d1` validated again.\n\n- **L0, local**: `evidence/CHG-004.6/20260921T041712Z-7e4a70cc3759.json` at `7e4a70c`, clean tree: all eight `passed`, 149 tests, no fixture drift, eligibility `blocked` on `Authentic` alone.\n\n- **L0, CI**: run [35560448177](https://github.com/kmosoti/recursive-hexagonal-architecture/actions/runs/35560448177) on the pushed head `c16f3ca`, copied as [`evidence/ci/35560448177.json`](../../evidence/ci/35560448177.json), subject `ef4102a7`: all eight `passed`, 149 tests.\n\n### CHG-004.6.1: local Codex review of `5ef607c`, determinations\n\nKennedy asked for a review by the local Codex agent (gpt-5.6-sol, medium effort, read-only). It requested changes with five findings; each names a concrete failure on the reviewed revision, and each is repaired in one commit after this one.\n\n1. **P1, a mismatched directory still ran.** `--cases` observed the cases whatever the commitment check said, so altered cases could be reported with exit 0. For the `held_out` purpose anything but `matched`, including `unverified`, now refuses with exit 2, as the archive path already did; `public_control` still runs on public fixtures.\n2. **P1, the printed summary named the private report directory.** Printing it invites reading raw reports. The printed summary drops `private_reports`; `summary.json` inside that directory keeps it for Kennedy.\n3. **P1, archive members were checked by name only.** A symlink or hard-link member could redirect a later member outside the extraction directory. Members other than regular files and directories are refused, from `tar -tvf`, before anything is extracted.\n4. **P1, the amendment chain was not anchored and skipped a link.** The manifest at the pre-registration `15d916a` hashes to `f4fcef6f…`; CHG-002.2's DP-1.1c grading decision changed it to `9f5c1a93…` at `4be4a19`, before EM-M03, and the chain never recorded that. The chain now starts at the pre-registration digest, records the CHG-002.2 link, and where a link's commit object exists verifies that the manifest at that commit hashes to its corrected digest (`commit_verified`); a shallow checkout reports the object absent instead of assuming it.\n5. **P2, the approved sentence ran on from the previous one.** It read \"derived from the other A finding…\". A full stop is inserted: punctuation inside the approved change, not a new amendment. The corrected digest and the pinned commit move with it.\n\n- **Amended H4 after CHG-004.6.1**: [`evidence/h4-crate/20260922T194055Z-0d0356aa1d9c.json`](../../evidence/h4-crate/20260922T194055Z-0d0356aa1d9c.json) at `0d0356a`, clean tree: 22/22, 0 unmatched findings, 1 registered fact, exit 0; the amendment chain has three links from the pre-registration digest, CHG-002.2, CHG-004 and CHG-004.6, each `commit_verified: true`. (Corrected from \"four links\" after the Codex re-review; the anchor and the CHG-002.2 link now also fail closed, and the next H4 record carries `anchor_verified`.) It supersedes the earlier amended run for this item.\n\n- **L0 after CHG-004.6.1**: `evidence/CHG-004.6/20260922T194102Z-8a723508c627.json` at `8a72350`, clean tree: all eight `passed`, 151 tests.\n\n### CHG-004.6.1, re-review of `b423829`\n\nThe local Codex agent confirmed findings 1, 2, 3 and 5 resolved. On 4 it found the anchor constant was never compared with the manifest at `15d916a`, and a mismatch on the CHG-002.2 link was only reported, not refused: both now fail closed where the commits are present, and the record carries `anchor_verified`. It also found this record claimed four links where the evidence holds three; corrected above.\n\n- **Final amended H4**: [`evidence/h4-crate/20260922T194637Z-f13f2bd5df1d.json`](../../evidence/h4-crate/20260922T194637Z-f13f2bd5df1d.json) at `f13f2bd`: 22/22, 0 unmatched, 1 registered fact, exit 0; `anchor_verified` and every `commit_verified` true.\n\n## Acceptance concerns\n\n1. **The amendment follows the data.** It is disclosed on the DP-1.1c row, in every H4 record graded under it (the `amendments` chain and the `registered_facts` count), in the maturity row, and here. The failed records are kept. If you would rather the crate level stayed at I until a case authored after the amendment confirms it, decline the V proposal at DP-1.6; the harness and the amendment stand either way.\n2. **The held-out run has not happened.** No agent has the cases. When you supply the original archive or the directory, the executor's process runs `cargo xtask corpus held-out`, and the observation is recorded as post-acceptance evidence for you to grade (DP-1.1b as amended). The DP-1.1b amendment itself is a protected edit made on your instructions; veto it at merge if the delegation is not what you meant.\n3. **DP-1.6 is yours.** The maturity row proposes; you accept.\n"
  },
  "docs/changes/CHG-005-rhawiki-m1.md": {
    "body": "[Heading { level: 1, slug: \"chg-005-p-a-the-rhawiki-product-and-the-m1-close\", children: [Text(\"CHG-005 (P-A): the rhawiki product and the M1 close\")] }, Paragraph([Text(\"Task record: \"), Link { href: \"../../.rha/tasks/CHG-005-rhawiki-m1.toml\", children: [Code(\".rha/tasks/CHG-005-rhawiki-m1.toml\")] }, Text(\". Plan: §8.1 P-A; §8.2 W5 and W6. Base: \"), Code(\"72c5cfd\"), Text(\", the merge that approved plan revision 2.\")]), Heading { level: 2, slug: \"intent-and-scope\", children: [Text(\"Intent and scope\")] }, Paragraph([Text(\"Packet P-A of plan revision 2 covers CHG-005 (W5) and CHG-006 (W6). It builds the guards revision 2 names, then the product and the M1 close, in gated stages.\")]), Heading { level: 2, slug: \"deltas\", children: [Text(\"Deltas\")] }, Heading { level: 3, slug: \"stage-0-guards\", children: [Text(\"Stage 0, guards\")] }, List { start: None, items: [[Code(\"cargo xtask scope --task <id>\"), Text(\" (plan §2.2 M4): every changed path, committed or not, must match the task's \"), Code(\"scope_globs\"), Text(\", sit in the §4 layout, and, if protected, match \"), Code(\"protected_scope\"), Text(\".\")], [Text(\"The enforcement map claims no cell from an H4 record that graded an earlier manifest.\")], [Text(\"Mechanical pedantic warnings in \"), Code(\"xtask/src/corpus\"), Text(\" are fixed; length and naming warnings stay advisory under §6.4.\")], [Code(\"corpus held-out --kind check\"), Text(\" moves to stage 3, where \"), Code(\"rhawiki check\"), Text(\" exists (task record decision \"), Code(\"held-out-check-kind-in-stage-3\"), Text(\").\")]] }, Paragraph([Strong([Text(\"The guard's first run caught this packet.\")]), Text(\" Stage 0 had added a line about the new command to CONTRIBUTING.md, which \"), Code(\".rha/policy.toml\"), Text(\" has protected since CHG-000, and no approval quoted it. The line is reverted. It is proposed for Kennedy's approval with DP-5.2.\")]), Heading { level: 3, slug: \"stage-1-boundary-decisions-78-step-10-and-corpus-registration\", children: [Text(\"Stage 1, boundary decisions (§7.8 step 10) and corpus registration\")] }, List { start: None, items: [[Paragraph([Strong([Text(\"Best-of-N (§2.4 rule 3).\")]), Text(\" Three decompositions were built as stub workspaces and run through \"), Code(\"cargo xtask architecture\"), Text(\"; the reports are in \"), Code(\"evidence/CHG-005/bdr-candidates/\"), Text(\". A is the plan's §3, B merges \"), Code(\"library\"), Text(\" and \"), Code(\"document\"), Text(\", C splits \"), Code(\"assembly\"), Text(\" and \"), Code(\"build\"), Text(\" into crates. All pass with zero errors, so the §7.2 evidence decides. A is chosen, and each BDR records B and C with the reasons they lost.\")])], [Paragraph([Strong([Text(\"BDR-0001 to BDR-0004\")]), Text(\" in \"), Code(\"docs/adr/\"), Text(\" each carry a proposed refutation criterion (metric, source, window, threshold, action). \"), Strong([Text(\"Gate DP-1.3:\")]), Text(\" Kennedy accepts or amends the four criteria before any product crate exists. BDR-0003 records the real trade-off. Until P-C validates the module check, the direction between \"), Code(\"assembly\"), Text(\" and \"), Code(\"build\"), Text(\" is enforced by review only. Candidate C is the fallback its criterion names.\")])], [Paragraph([Strong([Text(\"Markdown corpus registered (§2.4 rules 2 and 5).\")]), Text(\" A separate Codex session (\"), Code(\"gpt-6-astra\"), Text(\", extra-high) generated 60 sites, 300 pages and 99 planted witnesses by construction, with 20 clean sites. It ran before any product crate existed. \"), Code(\"xtask/tests/corpus/markdown/registration.toml\"), Text(\" records the generator, the prompt digest (the prompt itself is committed beside it), and the tree digest, which \"), Code(\"xtask/tests/corpus_markdown.rs\"), Text(\" pins. The implementing session has not edited a site. The witness keys it uses fix part of the \"), Code(\"check --format json\"), Text(\" schema in advance; stage 3 conforms to them.\")])]] }, Heading { level: 3, slug: \"stage-2-cores\", children: [Text(\"Stage 2, cores\")] }, Paragraph([Text(\"Decomposition A, as BDR-0001 to BDR-0004 accepted it. Every core has the template \"), Code(\"clippy.toml\"), Text(\", the crate-root \"), Code(\"forbid\"), Text(\" line, and \"), Code(\"role = \\\"core\\\"\"), Text(\". \"), Code(\"cargo xtask architecture\"), Text(\" classifies them by metadata and reports 0 errors and 0 warnings.\")]), List { start: None, items: [[Strong([Code(\"library\")]), Text(\": \"), Code(\"PageId\"), Text(\" (NFC, case kept), \"), Code(\"RelPath\"), Text(\", \"), Code(\"Digest\"), Text(\" (sha256), \"), Code(\"Source\"), Text(\", and \"), Code(\"Corpus\"), Text(\". \"), Code(\"Corpus\"), Text(\" is strict through \"), Code(\"new\"), Text(\"; \"), Code(\"with_witnesses\"), Text(\" keeps the first path per id and returns one \"), Code(\"DuplicatePageId\"), Text(\" per duplicated id. \"), Code(\"load\"), Text(\" is complete-or-fail. The \"), Code(\"SourceRepository\"), Text(\" contract suite catches all four seeded violators of the W5 brief, and \"), Code(\"MemorySources\"), Text(\" passes it.\")], [Strong([Code(\"document\")]), Text(\": a total parser to an owned node tree. It keeps headings with base and final slugs, where a repeat gets \"), Code(\"-2\"), Text(\", \"), Code(\"-3\"), Text(\" and a \"), Code(\"DuplicateSlug\"), Text(\" diagnostic. Wikilinks are split into target, anchor and alias; links in code spans and blocks are ignored. \"), Code(\"ENABLE_HEADING_ATTRIBUTES\"), Text(\" is off (decision \"), Code(\"heading-attributes-off\"), Text(\").\")], [Strong([Code(\"graph\")]), Text(\": exact-id, then unique case-insensitive basename resolution; broken, ambiguous and missing-anchor witnesses; symmetric backlinks. Properties: every link resolves or is witnessed, backlinks are symmetric, and results do not depend on input order.\")], [Strong([Code(\"site\")]), Text(\", the composite: \"), Code(\"assembly\"), Text(\" (\"), Code(\"Assemble\"), Text(\", \"), Code(\"PageModel\"), Text(\", the TOC, backlinks and link targets) and \"), Code(\"build\"), Text(\" (\"), Code(\"PageRenderer\"), Text(\", the pure \"), Code(\"step\"), Text(\", \"), Code(\"Inv_K\"), Text(\" checked by a separate function), glued to \"), Code(\"OutputSink\"), Text(\" and \"), Code(\"Clock\"), Text(\". \"), Code(\"rha-modules.toml\"), Text(\" has the plan §3.1 content, and a scoped \"), Code(\"AGENTS.md\"), Text(\" states the child rules. The contract suites catch a panicking and a nondeterministic renderer, and the fakes pass.\")], [Code(\"check\"), Text(\" witnesses use the registered corpus's key shape (\"), Code(\"site::CheckWitness\"), Text(\").\")]] }, Paragraph([Strong([Text(\"Differential oracles (§2.4 rule 4).\")]), Text(\" A separate Codex session (\"), Code(\"gpt-6-astra\"), Text(\", extra-high) wrote independent reference implementations of \"), Code(\"slugify\"), Text(\" and of resolution. It worked from the contract alone and was told not to read \"), Code(\"crates/*/src\"), Text(\". The results are \"), Code(\"crates/document/tests/oracle_slug.rs\"), Text(\" and \"), Code(\"crates/graph/tests/oracle_resolve.rs\"), Text(\", compared under proptest.\")]), List { start: None, items: [[Strong([Text(\"The resolver oracle agreed from the first run.\")])], [Strong([Text(\"The slug oracle found a real defect.\")]), Text(\" I lowercased character by character, so a word-final \"), Code(\"Σ\"), Text(\" became \"), Code(\"σ\"), Text(\". The oracle lowercased the whole string, which applies Unicode's context-sensitive final sigma and gives \"), Code(\"ς\"), Text(\", as GitHub's slugger does. The contract's \\\"Unicode-lowercase\\\" is best read as the full string mapping, so the product changed. The minimal inputs were \"), Code(\"HΣ२Z\"), Text(\" and \"), Code(\"e\\\\u{301}Σ\"), Text(\". No registered site contains \"), Code(\"Σ\"), Text(\", so the corpus result is unaffected.\")], [Strong([Text(\"One edit to the oracle.\")]), Text(\" It had been given the W5 brief's NFC step, and its NFC line was removed to match the registered contract (decision \"), Code(\"slug-without-nfc\"), Text(\"). The file marks that edit.\")]] }, Heading { level: 3, slug: \"stage-3-adapters-and-cli\", children: [Text(\"Stage 3, adapters and CLI\")] }, List { start: None, items: [[Strong([Code(\"adapter-fs\")]), Text(\": \"), Code(\"FsSources\"), Text(\" and \"), Code(\"FsSink\"), Text(\". It walks directories itself, never follows symlinks, and writes through a temporary file and a rename. Both owners' contract suites pass.\")], [Strong([Code(\"adapter-html\")]), Text(\": one escape function, with a property test that decodes its output. It renders the TOC, backlinks, relative wikilink hrefs, broken links as marked spans, \"), Code(\"pre.mermaid\"), Text(\" blocks, and the footer. The renderer contract passes. It depends on \"), Code(\"site\"), Text(\" only, through re-exports, so the checker reports \"), Strong([Text(\"0 errors and 0 warnings\")]), Text(\" over the whole product.\")], [Strong([Code(\"adapter-sys\")]), Text(\": \"), Code(\"SystemClock\"), Text(\" with an RFC 3339 formatter (DP-1.2 recorded as its default, yes).\")], [Strong([Code(\"app-cli\")]), Text(\" is the \"), Code(\"rhawiki\"), Text(\" binary with \"), Code(\"build\"), Text(\" and \"), Code(\"check --format json\"), Text(\". The check schema is \"), Code(\"{schema_version, pages, witnesses[{kind, …}], counts}\"), Text(\", with witness keys exactly as the registered corpus uses them. \"), Strong([Text(\"Gate DP-1.4:\")]), Text(\" Kennedy's private markdown fixtures can now be written against this schema. \"), Code(\"cargo xtask corpus held-out --kind check --archive <tar>\"), Text(\" runs them, one site per subdirectory, printing only opaque ids, exit statuses and counts.\")], [Strong([Text(\"On this repository:\")]), Text(\" \"), Code(\"rhawiki check --root docs\"), Text(\" finds 26 pages and 0 witnesses (DP-1.5's expectation). \"), Code(\"rhawiki build\"), Text(\" renders the spec with exactly \"), Strong([Text(\"182\")]), Text(\" headings and \"), Strong([Text(\"2\")]), Text(\" mermaid blocks, as W5 requires. \"), Code(\"xtask/tests/\"), Text(\" in app-cli pins both.\")]] }, Heading { level: 3, slug: \"stage-4-markdown-corpus-run\", children: [Text(\"Stage 4, markdown corpus run\")] }, Paragraph([Text(\"The first run graded \"), Strong([Text(\"59 of 60\")]), Text(\" sites as registered. MD051 failed: the product applied NFC to slugs, which the W5 brief asks for. The registered contract derives slugs without NFC, so a combining mark is dropped. The registration was fixed first and grades the product, so the product now follows it (decision \"), Code(\"slug-without-nfc\"), Text(\"). The disclosed defect is the Executor's: the brief's NFC was dropped when the registration contract was written. After the change, \"), Strong([Text(\"60 of 60\")]), Text(\" sites pass. The first run was a check, not a recorded harness run, so no failed record exists for it; this paragraph is its record. \"), Code(\"cargo xtask corpus run --level markdown\"), Text(\" writes the evidence below, and an app-cli test pins it under \"), Code(\"L0.nextest\"), Text(\".\")]), Paragraph([Strong([Text(\"Evidence:\")]), Text(\" \"), Code(\"evidence/md-corpus/20260922T210717Z-58e45f444c9f.json\"), Text(\" at \"), Code(\"58e45f4\"), Text(\", clean tree: 60 of 60 sites graded as registered, with the registration's tree digest verified before the run.\")]), Heading { level: 3, slug: \"stage-5-m1-close\", children: [Text(\"Stage 5, M1 close\")] }, List { start: None, items: [[Code(\".rha/assumptions.toml\"), Text(\": nine entries from plan §3.3, each naming the test that discharges it. Two are undischarged at the composition root.\")], [Code(\"docs/conformance/self-assessment.md\"), Text(\": every §15 item answered, with the gaps named.\")], [Code(\"docs/maturity.md\"), Text(\" proposes: the crate-graph checker at V (crate level, from CHG-004.6); the deny list at V (crate level); the fast lane at V; held-out checks at I.\")], [Strong([Text(\"L0 at the settled head:\")]), Text(\" \"), Code(\"evidence/CHG-005/20260922T211312Z-e213c0b52b98.json\"), Text(\" at \"), Code(\"e213c0b\"), Text(\", clean tree, all eight \"), Code(\"passed\"), Text(\", 188 tests. Tag \"), Code(\"v0.10-m1\"), Text(\" follows Kennedy's merge.\")]] }, Heading { level: 3, slug: \"review-round-1-codex-gpt-6-astra-extra-high-read-only-on-3c4a41f-determinations\", children: [Text(\"Review round 1 (Codex, \"), Code(\"gpt-6-astra\"), Text(\", extra-high, read-only) on \"), Code(\"3c4a41f\"), Text(\", determinations\")] }, Paragraph([Text(\"REQUEST_CHANGES: 11 findings, 2 P1 and 9 P2. Each names a concrete failing input or scenario on the reviewed revision, and all are confirmed and repaired in the commit after this one.\")]), List { start: Some(1), items: [[Strong([Text(\"P1: writes escape through directory symlinks.\")]), Text(\" \"), Code(\"FsSink::write\"), Text(\" used \"), Code(\"create_dir_all\"), Text(\" and \"), Code(\"write\"), Text(\", which follow a symlinked directory under \"), Code(\"--out\"), Text(\". Repair: every existing component of the target path is checked with \"), Code(\"symlink_metadata\"), Text(\", a link is refused, and directories are created one level at a time. \"), Code(\"delete\"), Text(\" gets the same check.\")], [Strong([Text(\"P1: predictable temporary names.\")]), Text(\" A pre-planted \"), Code(\".rhawiki-tmp-…\"), Text(\" symlink was truncated through, and a source page named like a temporary file had its output overwritten and renamed. Repair: temporary files are \"), Code(\".rhawiki-tmp-<pid>-<counter>.tmp\"), Text(\", opened with \"), Code(\"create_new\"), Text(\", so an existing file or link fails. \"), Code(\"list\"), Text(\" hides only that exact pattern, which no renderer output can take, because pages become \"), Code(\".html\"), Text(\".\")], [Strong([Text(\"P2: generated slug suffixes collided with natural slugs.\")]), Text(\" \"), Code(\"# A\"), Text(\", \"), Code(\"# A\"), Text(\", \"), Code(\"# A-2\"), Text(\" gave \"), Code(\"a\"), Text(\", \"), Code(\"a-2\"), Text(\", \"), Code(\"a-2\"), Text(\". Repair: the allocator tracks every final slug, and both a generated suffix and a colliding natural slug advance to the next unused one: \"), Code(\"a\"), Text(\", \"), Code(\"a-2\"), Text(\", \"), Code(\"a-2-2\"), Text(\". \"), Code(\"DuplicateSlug\"), Text(\" still fires only for a repeated base, as the contract says.\")], [Strong([Text(\"P2: wikilink hrefs were not URL-encoded.\")]), Text(\" \"), Code(\"Budget?2026\"), Text(\" became a query. Repair: path segments and fragments are percent-encoded outside RFC 3986's unreserved set, for wikilinks and backlinks alike.\")], [Strong([Text(\"P2: ordinary relative \"), Code(\".md\"), Text(\" links pointed at files that are never produced.\")]), Text(\" Repair: a relative destination with no scheme and a \"), Code(\".md\"), Text(\" path becomes \"), Code(\".html\"), Text(\", keeping any fragment. Absolute and scheme URLs are unchanged.\")], [Strong([Text(\"P2: the markdown held-out mode verified against the crate-level commitment.\")]), Text(\" Repair: \"), Code(\"--kind check\"), Text(\" reads DP-1.4's commitment, and it is \"), Code(\"not_run\"), Text(\" while that row carries none.\")], [Strong([Text(\"P2: the markdown harness could run a stale binary.\")]), Text(\" It ran \"), Code(\"target/debug/rhawiki\"), Text(\" whatever \"), Code(\"CARGO_TARGET_DIR\"), Text(\" said. Repair: the executable path comes from Cargo's own build output (\"), Code(\"--message-format=json\"), Text(\").\")], [Strong([Text(\"P2: invalid checker output passed clean sites.\")]), Text(\" Empty stdout, \"), Code(\"{}\"), Text(\", or a non-array \"), Code(\"witnesses\"), Text(\" became an empty multiset. Repair: a case is graded only when the output is JSON with \"), Code(\"schema_version == 1\"), Text(\" and a \"), Code(\"witnesses\"), Text(\" array; anything else fails the case.\")], [Strong([Text(\"P2: renames hid protected sources from the scope guard.\")]), Text(\" Git's rename detection listed only the destination. Repair: \"), Code(\"--no-renames\"), Text(\", so both endpoints are checked.\")], [Strong([Text(\"P2: the source contract could not detect consistent truncation.\")]), Text(\" The seeded \"), Code(\"TruncatingSource\"), Text(\" alternated, so only instability was tested, and the change record's claim about the W5 violators was unsupported. Repair: the suite takes the fixture's expected contents and compares every read with them. The seeded violator now truncates every read, as the W5 brief describes. The unstable-read case is kept as its own violator.\")], [Strong([Text(\"P2: the oracles lacked generation provenance (§2.4 rule 1).\")]), Text(\" Repair: the oracle prompt is committed beside the markdown corpus's prompt. The task record gains a \"), Code(\"[[provenance.generations]]\"), Text(\" entry for each generated artifact (the corpus and both oracles), with model, effort, prompt path and digest.\")]] }, Paragraph([Strong([Text(\"Repairs committed in \"), Code(\"25b035e\"), Text(\".\")]), Text(\" L0 at that revision: \"), Code(\"evidence/CHG-005/20260922T213429Z-25b035eac999.json\"), Text(\", clean tree, all eight \"), Code(\"passed\"), Text(\", 192 tests. The earlier stage 5 record describes the reviewed revision and is kept.\")]), Heading { level: 3, slug: \"review-round-2-on-52ceb20-determinations\", children: [Text(\"Review round 2 on \"), Code(\"52ceb20\"), Text(\", determinations\")] }, Paragraph([Text(\"Ten of 11 were confirmed resolved. On finding 5, and one new P2 in the same function: \"), Code(\"rewrite_href\"), Text(\" took the query as part of the path (\"), Code(\"x.md?print=1\"), Text(\" kept \"), Code(\".md\"), Text(\", and \"), Code(\"download?file=manual.md\"), Text(\" was rewritten), and it took any colon as a scheme (\"), Code(\"./a:b.md\"), Text(\"). \"), Strong([Text(\"Confirmed.\")]), Text(\" Repair: the reference is split as RFC 3986 parses it, fragment then query. A scheme is recognized only by §3.1's syntax before any \"), Code(\"/\"), Text(\", \"), Code(\"?\"), Text(\" or \"), Code(\"#\"), Text(\". Only the path's \"), Code(\".md\"), Text(\" suffix is rewritten, and the query and fragment are kept byte for byte. All four inputs are regression tests.\")]), Paragraph([Strong([Text(\"A void review run.\")]), Text(\" The first attempt at round 3 reviewed \"), Code(\"52ceb20\"), Text(\" unchanged, because the Executor's script failed before committing the repair: a rustfmt-reformatted anchor did not match. Its verdict describes the old code and is not counted as a round under M6. Round 3 below reviews the committed repair.\")]), Heading { level: 3, slug: \"automated-review-threads-on-pr-16-chatgpt-codex-connector-on-8b81dd5-determinations\", children: [Text(\"Automated review threads on PR 16 (\"), Code(\"chatgpt-codex-connector\"), Text(\", on \"), Code(\"8b81dd5\"), Text(\"), determinations\")] }, Paragraph([Text(\"Two threads were open after round 3. Each was checked against the code before any repair.\")]), List { start: Some(1), items: [[Strong([Text(\"A symlinked output root (P1). Confirmed.\")]), Text(\" \"), Code(\"prepare\"), Text(\" and \"), Code(\"delete\"), Text(\" checked every segment \"), Emphasis([Text(\"below\")]), Text(\" the root and \"), Code(\"list\"), Text(\" walked the root with \"), Code(\"read_dir\"), Text(\", which follows a link. So \"), Code(\"--out\"), Text(\" pointing at a symbolic link to a populated directory would inventory that directory, and the build would delete what it did not produce there. Repair: \"), Code(\"FsSink\"), Text(\" refuses a root that is itself a symbolic link, before \"), Code(\"list\"), Text(\", \"), Code(\"write\"), Text(\" and \"), Code(\"delete\"), Text(\". Discriminating check: a new contract test, \"), Code(\"a_symlinked_output_root_is_refused_before_listing_writing_or_deleting\"), Text(\", fails on \"), Code(\"8b81dd5\"), Text(\" (the target is inventoried) and passes on the repair; the seeded file is untouched. A symbolic link \"), Emphasis([Text(\"source\")]), Text(\" root (\"), Code(\"--root\"), Text(\") stays allowed: reading through it deletes nothing.\")], [Strong([Text(\"A panic on a non-ASCII glob segment (P2). Refuted.\")]), Text(\" \"), Code(\"glob\"), Text(\"'s \"), Code(\"segment\"), Text(\" matches \"), Code(\"&[u8]\"), Text(\", and a byte slice has no character boundary to split, so \"), Code(\"&text[i..]\"), Text(\" cannot panic. Discriminating check: \"), Code(\"glob(\\\"docs/*.md\\\", \\\"docs/café.md\\\")\"), Text(\", \"), Code(\"glob(\\\"docs/c*é.md\\\", \\\"docs/café.md\\\")\"), Text(\" and the negative \"), Code(\"docs/*.rs\"), Text(\" ran against \"), Code(\"8b81dd5\"), Text(\" and gave the expected answers without a panic. Byte-wise matching agrees with character-wise matching here because the only wildcard is \"), Code(\"*\"), Text(\" and literals compare whole UTF-8 sequences. The three assertions are kept as a regression test; no code changed.\")]] }, Heading { level: 3, slug: \"conformance-validation-of-the-repair-range-8b81dd5bf590fc-codex-as-validator-dp-54-determinations\", children: [Text(\"Conformance validation of the repair range \"), Code(\"8b81dd5..bf590fc\"), Text(\" (Codex as validator, DP-5.4), determinations\")] }, Paragraph([Text(\"Codex now validates conformance to the registered contracts and the RHA guidelines; it does not generate (DP-5.4). Verdict on \"), Code(\"bf590fc\"), Text(\": does not conform, two findings.\")]), List { start: Some(1), items: [[Strong([Code(\"--out out-link/\"), Text(\" passes the root check (P1). Confirmed by running it.\")]), Text(\" With a trailing \"), Code(\"/\"), Text(\", \"), Code(\"symlink_metadata\"), Text(\" resolves the link. The extended contract test fails on \"), Code(\"bf590fc\"), Text(\" at that spelling. Repair: \"), Code(\"FsSink::new\"), Text(\" keeps the root as its components, which drops a trailing \"), Code(\"/\"), Text(\" and \"), Code(\".\"), Text(\", so the check sees the link itself. The test covers \"), Code(\"out-link\"), Text(\", \"), Code(\"out-link/\"), Text(\" and \"), Code(\"out-link/.\"), Text(\".\")], [Strong([Code(\"docs/evidence/index.md\"), Text(\" stale at \"), Code(\"bf590fc\"), Text(\" (P1). Confirmed.\")]), Text(\" The Executor ran the regeneration with an unsupported flag and discarded its error output, so the index missed the new record. Repair: regenerated; \"), Code(\"cargo xtask docs --check\"), Text(\" is part of the pre-push steps from here on (repair attempt 3).\")]] }, Heading { level: 3, slug: \"repair-attempts-11710\", children: [Text(\"Repair attempts (§11.7.10)\")] }, List { start: Some(3), items: [[Paragraph([Strong([Text(\"The evidence index was stale in the pushed head.\")]), Text(\" Hypothesis: the regeneration had not run. Discriminating check: \"), Code(\"cargo xtask docs -q\"), Text(\" is a usage error, and \"), Code(\"2>/dev/null\"), Text(\" had hidden it; \"), Code(\"docs --check\"), Text(\" exits 1 naming the index. Change: regenerate without the flag, and check with \"), Code(\"docs --check\"), Text(\" before every push. Result: \"), Code(\"docs --check\"), Text(\" exits 0.\")])], [Paragraph([Strong([Code(\"L0.typos\"), Text(\" failed in CI run 35784806281, and the lane had not been run locally.\")]), Text(\" Hypothesis: the spell checker splits Unicode escapes such as \"), Code(\"\\\\u{e9}\"), Text(\" and reads the letters before them as a word; one variable name also read as a misspelling. (This note does not quote it, which is CHG-002's lesson.) Discriminating check: \"), Code(\"typos --format brief\"), Text(\" reproduced all nine findings locally. Change: the characters are written literally and the variable is renamed. No dictionary exception was added, following CHG-003's precedent. Result: \"), Code(\"typos\"), Text(\" is clean. A method lesson too: M5's full lane belongs before the first push, not only before the record.\")])], [Paragraph([Strong([Text(\"The scope guard reported 52 false findings on the corpus commit.\")]), Text(\" The hypothesis was git's default path quoting. The discriminating check: every flagged path was a non-ASCII name, such as \"), Code(\"café.md\"), Text(\", printed as a quoted string of octal escapes. The change turns off \"), Code(\"core.quotePath\"), Text(\" in every git call the guard makes. Result: 0 findings. The adversarial corpus found a defect in the guard before any product code existed.\")])]] }, Heading { level: 2, slug: \"acceptance-concerns\", children: [Text(\"Acceptance concerns\")] }, List { start: Some(1), items: [[Strong([Text(\"An earlier protected edit without a quoted approval, found by the new guard.\")]), Text(\" CHG-004.6's commit \"), Code(\"ad13efa\"), Text(\" added the held-out paragraph to CONTRIBUTING.md; its approval was recorded only as owned scope. Resolved: approved retroactively under Kennedy's delegation of 2026-09-22 (task record decision \"), Code(\"contributing-scope-line\"), Text(\"; ledger DP-5.2).\")], [Strong([Text(\"The CONTRIBUTING line for \"), Code(\"cargo xtask scope\"), Text(\".\")]), Text(\" Resolved: restored, with DP-5.2 decided as a pre-PR command.\")], [Strong([Text(\"DP-1.4, the private markdown held-out fixtures, is not_run.\")]), Text(\" Authoring them is Kennedy's alone (§9.14), so the delegation cannot close it. The row stays open; a run after the merge is dated as a post-acceptance observation.\")]] }]",
    "headings": "[Heading { level: 1, text: \"CHG-005 (P-A): the rhawiki product and the M1 close\", slug: \"chg-005-p-a-the-rhawiki-product-and-the-m1-close\", base_slug: \"chg-005-p-a-the-rhawiki-product-and-the-m1-close\", line: 1 }, Heading { level: 2, text: \"Intent and scope\", slug: \"intent-and-scope\", base_slug: \"intent-and-scope\", line: 5 }, Heading { level: 2, text: \"Deltas\", slug: \"deltas\", base_slug: \"deltas\", line: 9 }, Heading { level: 3, text: \"Stage 0, guards\", slug: \"stage-0-guards\", base_slug: \"stage-0-guards\", line: 11 }, Heading { level: 3, text: \"Stage 1, boundary decisions (§7.8 step 10) and corpus registration\", slug: \"stage-1-boundary-decisions-78-step-10-and-corpus-registration\", base_slug: \"stage-1-boundary-decisions-78-step-10-and-corpus-registration\", line: 20 }, Heading { level: 3, text: \"Stage 2, cores\", slug: \"stage-2-cores\", base_slug: \"stage-2-cores\", line: 27 }, Heading { level: 3, text: \"Stage 3, adapters and CLI\", slug: \"stage-3-adapters-and-cli\", base_slug: \"stage-3-adapters-and-cli\", line: 43 }, Heading { level: 3, text: \"Stage 4, markdown corpus run\", slug: \"stage-4-markdown-corpus-run\", base_slug: \"stage-4-markdown-corpus-run\", line: 51 }, Heading { level: 3, text: \"Stage 5, M1 close\", slug: \"stage-5-m1-close\", base_slug: \"stage-5-m1-close\", line: 57 }, Heading { level: 3, text: \"Review round 1 (Codex, gpt-6-astra, extra-high, read-only) on 3c4a41f, determinations\", slug: \"review-round-1-codex-gpt-6-astra-extra-high-read-only-on-3c4a41f-determinations\", base_slug: \"review-round-1-codex-gpt-6-astra-extra-high-read-only-on-3c4a41f-determinations\", line: 64 }, Heading { level: 3, text: \"Review round 2 on 52ceb20, determinations\", slug: \"review-round-2-on-52ceb20-determinations\", base_slug: \"review-round-2-on-52ceb20-determinations\", line: 82 }, Heading { level: 3, text: \"Automated review threads on PR 16 (chatgpt-codex-connector, on 8b81dd5), determinations\", slug: \"automated-review-threads-on-pr-16-chatgpt-codex-connector-on-8b81dd5-determinations\", base_slug: \"automated-review-threads-on-pr-16-chatgpt-codex-connector-on-8b81dd5-determinations\", line: 88 }, Heading { level: 3, text: \"Conformance validation of the repair range 8b81dd5..bf590fc (Codex as validator, DP-5.4), determinations\", slug: \"conformance-validation-of-the-repair-range-8b81dd5bf590fc-codex-as-validator-dp-54-determinations\", base_slug: \"conformance-validation-of-the-repair-range-8b81dd5bf590fc-codex-as-validator-dp-54-determinations\", line: 95 }, Heading { level: 3, text: \"Repair attempts (§11.7.10)\", slug: \"repair-attempts-11710\", base_slug: \"repair-attempts-11710\", line: 102 }, Heading { level: 2, text: \"Acceptance concerns\", slug: \"acceptance-concerns\", base_slug: \"acceptance-concerns\", line: 110 }]",
    "markdown": "# CHG-005 (P-A): the rhawiki product and the M1 close\n\nTask record: [`.rha/tasks/CHG-005-rhawiki-m1.toml`](../../.rha/tasks/CHG-005-rhawiki-m1.toml). Plan: §8.1 P-A; §8.2 W5 and W6. Base: `72c5cfd`, the merge that approved plan revision 2.\n\n## Intent and scope\n\nPacket P-A of plan revision 2 covers CHG-005 (W5) and CHG-006 (W6). It builds the guards revision 2 names, then the product and the M1 close, in gated stages.\n\n## Deltas\n\n### Stage 0, guards\n\n- `cargo xtask scope --task <id>` (plan §2.2 M4): every changed path, committed or not, must match the task's `scope_globs`, sit in the §4 layout, and, if protected, match `protected_scope`.\n- The enforcement map claims no cell from an H4 record that graded an earlier manifest.\n- Mechanical pedantic warnings in `xtask/src/corpus` are fixed; length and naming warnings stay advisory under §6.4.\n- `corpus held-out --kind check` moves to stage 3, where `rhawiki check` exists (task record decision `held-out-check-kind-in-stage-3`).\n\n**The guard's first run caught this packet.** Stage 0 had added a line about the new command to CONTRIBUTING.md, which `.rha/policy.toml` has protected since CHG-000, and no approval quoted it. The line is reverted. It is proposed for Kennedy's approval with DP-5.2.\n\n### Stage 1, boundary decisions (§7.8 step 10) and corpus registration\n\n- **Best-of-N (§2.4 rule 3).** Three decompositions were built as stub workspaces and run through `cargo xtask architecture`; the reports are in `evidence/CHG-005/bdr-candidates/`. A is the plan's §3, B merges `library` and `document`, C splits `assembly` and `build` into crates. All pass with zero errors, so the §7.2 evidence decides. A is chosen, and each BDR records B and C with the reasons they lost.\n- **BDR-0001 to BDR-0004** in `docs/adr/` each carry a proposed refutation criterion (metric, source, window, threshold, action). **Gate DP-1.3:** Kennedy accepts or amends the four criteria before any product crate exists. BDR-0003 records the real trade-off. Until P-C validates the module check, the direction between `assembly` and `build` is enforced by review only. Candidate C is the fallback its criterion names.\n\n- **Markdown corpus registered (§2.4 rules 2 and 5).** A separate Codex session (`gpt-6-astra`, extra-high) generated 60 sites, 300 pages and 99 planted witnesses by construction, with 20 clean sites. It ran before any product crate existed. `xtask/tests/corpus/markdown/registration.toml` records the generator, the prompt digest (the prompt itself is committed beside it), and the tree digest, which `xtask/tests/corpus_markdown.rs` pins. The implementing session has not edited a site. The witness keys it uses fix part of the `check --format json` schema in advance; stage 3 conforms to them.\n\n### Stage 2, cores\n\nDecomposition A, as BDR-0001 to BDR-0004 accepted it. Every core has the template `clippy.toml`, the crate-root `forbid` line, and `role = \"core\"`. `cargo xtask architecture` classifies them by metadata and reports 0 errors and 0 warnings.\n\n- **`library`**: `PageId` (NFC, case kept), `RelPath`, `Digest` (sha256), `Source`, and `Corpus`. `Corpus` is strict through `new`; `with_witnesses` keeps the first path per id and returns one `DuplicatePageId` per duplicated id. `load` is complete-or-fail. The `SourceRepository` contract suite catches all four seeded violators of the W5 brief, and `MemorySources` passes it.\n- **`document`**: a total parser to an owned node tree. It keeps headings with base and final slugs, where a repeat gets `-2`, `-3` and a `DuplicateSlug` diagnostic. Wikilinks are split into target, anchor and alias; links in code spans and blocks are ignored. `ENABLE_HEADING_ATTRIBUTES` is off (decision `heading-attributes-off`).\n- **`graph`**: exact-id, then unique case-insensitive basename resolution; broken, ambiguous and missing-anchor witnesses; symmetric backlinks. Properties: every link resolves or is witnessed, backlinks are symmetric, and results do not depend on input order.\n- **`site`**, the composite: `assembly` (`Assemble`, `PageModel`, the TOC, backlinks and link targets) and `build` (`PageRenderer`, the pure `step`, `Inv_K` checked by a separate function), glued to `OutputSink` and `Clock`. `rha-modules.toml` has the plan §3.1 content, and a scoped `AGENTS.md` states the child rules. The contract suites catch a panicking and a nondeterministic renderer, and the fakes pass.\n- `check` witnesses use the registered corpus's key shape (`site::CheckWitness`).\n\n**Differential oracles (§2.4 rule 4).** A separate Codex session (`gpt-6-astra`, extra-high) wrote independent reference implementations of `slugify` and of resolution. It worked from the contract alone and was told not to read `crates/*/src`. The results are `crates/document/tests/oracle_slug.rs` and `crates/graph/tests/oracle_resolve.rs`, compared under proptest.\n\n- **The resolver oracle agreed from the first run.**\n- **The slug oracle found a real defect.** I lowercased character by character, so a word-final `Σ` became `σ`. The oracle lowercased the whole string, which applies Unicode's context-sensitive final sigma and gives `ς`, as GitHub's slugger does. The contract's \"Unicode-lowercase\" is best read as the full string mapping, so the product changed. The minimal inputs were `HΣ२Z` and `e\\u{301}Σ`. No registered site contains `Σ`, so the corpus result is unaffected.\n- **One edit to the oracle.** It had been given the W5 brief's NFC step, and its NFC line was removed to match the registered contract (decision `slug-without-nfc`). The file marks that edit.\n\n### Stage 3, adapters and CLI\n\n- **`adapter-fs`**: `FsSources` and `FsSink`. It walks directories itself, never follows symlinks, and writes through a temporary file and a rename. Both owners' contract suites pass.\n- **`adapter-html`**: one escape function, with a property test that decodes its output. It renders the TOC, backlinks, relative wikilink hrefs, broken links as marked spans, `pre.mermaid` blocks, and the footer. The renderer contract passes. It depends on `site` only, through re-exports, so the checker reports **0 errors and 0 warnings** over the whole product.\n- **`adapter-sys`**: `SystemClock` with an RFC 3339 formatter (DP-1.2 recorded as its default, yes).\n- **`app-cli`** is the `rhawiki` binary with `build` and `check --format json`. The check schema is `{schema_version, pages, witnesses[{kind, …}], counts}`, with witness keys exactly as the registered corpus uses them. **Gate DP-1.4:** Kennedy's private markdown fixtures can now be written against this schema. `cargo xtask corpus held-out --kind check --archive <tar>` runs them, one site per subdirectory, printing only opaque ids, exit statuses and counts.\n- **On this repository:** `rhawiki check --root docs` finds 26 pages and 0 witnesses (DP-1.5's expectation). `rhawiki build` renders the spec with exactly **182** headings and **2** mermaid blocks, as W5 requires. `xtask/tests/` in app-cli pins both.\n\n### Stage 4, markdown corpus run\n\nThe first run graded **59 of 60** sites as registered. MD051 failed: the product applied NFC to slugs, which the W5 brief asks for. The registered contract derives slugs without NFC, so a combining mark is dropped. The registration was fixed first and grades the product, so the product now follows it (decision `slug-without-nfc`). The disclosed defect is the Executor's: the brief's NFC was dropped when the registration contract was written. After the change, **60 of 60** sites pass. The first run was a check, not a recorded harness run, so no failed record exists for it; this paragraph is its record. `cargo xtask corpus run --level markdown` writes the evidence below, and an app-cli test pins it under `L0.nextest`.\n\n**Evidence:** `evidence/md-corpus/20260922T210717Z-58e45f444c9f.json` at `58e45f4`, clean tree: 60 of 60 sites graded as registered, with the registration's tree digest verified before the run.\n\n### Stage 5, M1 close\n\n- `.rha/assumptions.toml`: nine entries from plan §3.3, each naming the test that discharges it. Two are undischarged at the composition root.\n- `docs/conformance/self-assessment.md`: every §15 item answered, with the gaps named.\n- `docs/maturity.md` proposes: the crate-graph checker at V (crate level, from CHG-004.6); the deny list at V (crate level); the fast lane at V; held-out checks at I.\n- **L0 at the settled head:** `evidence/CHG-005/20260922T211312Z-e213c0b52b98.json` at `e213c0b`, clean tree, all eight `passed`, 188 tests. Tag `v0.10-m1` follows Kennedy's merge.\n\n### Review round 1 (Codex, `gpt-6-astra`, extra-high, read-only) on `3c4a41f`, determinations\n\nREQUEST_CHANGES: 11 findings, 2 P1 and 9 P2. Each names a concrete failing input or scenario on the reviewed revision, and all are confirmed and repaired in the commit after this one.\n\n1. **P1: writes escape through directory symlinks.** `FsSink::write` used `create_dir_all` and `write`, which follow a symlinked directory under `--out`. Repair: every existing component of the target path is checked with `symlink_metadata`, a link is refused, and directories are created one level at a time. `delete` gets the same check.\n2. **P1: predictable temporary names.** A pre-planted `.rhawiki-tmp-…` symlink was truncated through, and a source page named like a temporary file had its output overwritten and renamed. Repair: temporary files are `.rhawiki-tmp-<pid>-<counter>.tmp`, opened with `create_new`, so an existing file or link fails. `list` hides only that exact pattern, which no renderer output can take, because pages become `.html`.\n3. **P2: generated slug suffixes collided with natural slugs.** `# A`, `# A`, `# A-2` gave `a`, `a-2`, `a-2`. Repair: the allocator tracks every final slug, and both a generated suffix and a colliding natural slug advance to the next unused one: `a`, `a-2`, `a-2-2`. `DuplicateSlug` still fires only for a repeated base, as the contract says.\n4. **P2: wikilink hrefs were not URL-encoded.** `Budget?2026` became a query. Repair: path segments and fragments are percent-encoded outside RFC 3986's unreserved set, for wikilinks and backlinks alike.\n5. **P2: ordinary relative `.md` links pointed at files that are never produced.** Repair: a relative destination with no scheme and a `.md` path becomes `.html`, keeping any fragment. Absolute and scheme URLs are unchanged.\n6. **P2: the markdown held-out mode verified against the crate-level commitment.** Repair: `--kind check` reads DP-1.4's commitment, and it is `not_run` while that row carries none.\n7. **P2: the markdown harness could run a stale binary.** It ran `target/debug/rhawiki` whatever `CARGO_TARGET_DIR` said. Repair: the executable path comes from Cargo's own build output (`--message-format=json`).\n8. **P2: invalid checker output passed clean sites.** Empty stdout, `{}`, or a non-array `witnesses` became an empty multiset. Repair: a case is graded only when the output is JSON with `schema_version == 1` and a `witnesses` array; anything else fails the case.\n9. **P2: renames hid protected sources from the scope guard.** Git's rename detection listed only the destination. Repair: `--no-renames`, so both endpoints are checked.\n10. **P2: the source contract could not detect consistent truncation.** The seeded `TruncatingSource` alternated, so only instability was tested, and the change record's claim about the W5 violators was unsupported. Repair: the suite takes the fixture's expected contents and compares every read with them. The seeded violator now truncates every read, as the W5 brief describes. The unstable-read case is kept as its own violator.\n11. **P2: the oracles lacked generation provenance (§2.4 rule 1).** Repair: the oracle prompt is committed beside the markdown corpus's prompt. The task record gains a `[[provenance.generations]]` entry for each generated artifact (the corpus and both oracles), with model, effort, prompt path and digest.\n\n**Repairs committed in `25b035e`.** L0 at that revision: `evidence/CHG-005/20260922T213429Z-25b035eac999.json`, clean tree, all eight `passed`, 192 tests. The earlier stage 5 record describes the reviewed revision and is kept.\n\n### Review round 2 on `52ceb20`, determinations\n\nTen of 11 were confirmed resolved. On finding 5, and one new P2 in the same function: `rewrite_href` took the query as part of the path (`x.md?print=1` kept `.md`, and `download?file=manual.md` was rewritten), and it took any colon as a scheme (`./a:b.md`). **Confirmed.** Repair: the reference is split as RFC 3986 parses it, fragment then query. A scheme is recognized only by §3.1's syntax before any `/`, `?` or `#`. Only the path's `.md` suffix is rewritten, and the query and fragment are kept byte for byte. All four inputs are regression tests.\n\n**A void review run.** The first attempt at round 3 reviewed `52ceb20` unchanged, because the Executor's script failed before committing the repair: a rustfmt-reformatted anchor did not match. Its verdict describes the old code and is not counted as a round under M6. Round 3 below reviews the committed repair.\n\n### Automated review threads on PR 16 (`chatgpt-codex-connector`, on `8b81dd5`), determinations\n\nTwo threads were open after round 3. Each was checked against the code before any repair.\n\n1. **A symlinked output root (P1). Confirmed.** `prepare` and `delete` checked every segment *below* the root and `list` walked the root with `read_dir`, which follows a link. So `--out` pointing at a symbolic link to a populated directory would inventory that directory, and the build would delete what it did not produce there. Repair: `FsSink` refuses a root that is itself a symbolic link, before `list`, `write` and `delete`. Discriminating check: a new contract test, `a_symlinked_output_root_is_refused_before_listing_writing_or_deleting`, fails on `8b81dd5` (the target is inventoried) and passes on the repair; the seeded file is untouched. A symbolic link *source* root (`--root`) stays allowed: reading through it deletes nothing.\n2. **A panic on a non-ASCII glob segment (P2). Refuted.** `glob`'s `segment` matches `&[u8]`, and a byte slice has no character boundary to split, so `&text[i..]` cannot panic. Discriminating check: `glob(\"docs/*.md\", \"docs/café.md\")`, `glob(\"docs/c*é.md\", \"docs/café.md\")` and the negative `docs/*.rs` ran against `8b81dd5` and gave the expected answers without a panic. Byte-wise matching agrees with character-wise matching here because the only wildcard is `*` and literals compare whole UTF-8 sequences. The three assertions are kept as a regression test; no code changed.\n\n### Conformance validation of the repair range `8b81dd5..bf590fc` (Codex as validator, DP-5.4), determinations\n\nCodex now validates conformance to the registered contracts and the RHA guidelines; it does not generate (DP-5.4). Verdict on `bf590fc`: does not conform, two findings.\n\n1. **`--out out-link/` passes the root check (P1). Confirmed by running it.** With a trailing `/`, `symlink_metadata` resolves the link. The extended contract test fails on `bf590fc` at that spelling. Repair: `FsSink::new` keeps the root as its components, which drops a trailing `/` and `.`, so the check sees the link itself. The test covers `out-link`, `out-link/` and `out-link/.`.\n2. **`docs/evidence/index.md` stale at `bf590fc` (P1). Confirmed.** The Executor ran the regeneration with an unsupported flag and discarded its error output, so the index missed the new record. Repair: regenerated; `cargo xtask docs --check` is part of the pre-push steps from here on (repair attempt 3).\n\n### Repair attempts (§11.7.10)\n\n3. **The evidence index was stale in the pushed head.** Hypothesis: the regeneration had not run. Discriminating check: `cargo xtask docs -q` is a usage error, and `2>/dev/null` had hidden it; `docs --check` exits 1 naming the index. Change: regenerate without the flag, and check with `docs --check` before every push. Result: `docs --check` exits 0.\n\n2. **`L0.typos` failed in CI run 35784806281, and the lane had not been run locally.** Hypothesis: the spell checker splits Unicode escapes such as `\\u{e9}` and reads the letters before them as a word; one variable name also read as a misspelling. (This note does not quote it, which is CHG-002's lesson.) Discriminating check: `typos --format brief` reproduced all nine findings locally. Change: the characters are written literally and the variable is renamed. No dictionary exception was added, following CHG-003's precedent. Result: `typos` is clean. A method lesson too: M5's full lane belongs before the first push, not only before the record.\n\n1. **The scope guard reported 52 false findings on the corpus commit.** The hypothesis was git's default path quoting. The discriminating check: every flagged path was a non-ASCII name, such as `café.md`, printed as a quoted string of octal escapes. The change turns off `core.quotePath` in every git call the guard makes. Result: 0 findings. The adversarial corpus found a defect in the guard before any product code existed.\n\n## Acceptance concerns\n\n1. **An earlier protected edit without a quoted approval, found by the new guard.** CHG-004.6's commit `ad13efa` added the held-out paragraph to CONTRIBUTING.md; its approval was recorded only as owned scope. Resolved: approved retroactively under Kennedy's delegation of 2026-09-22 (task record decision `contributing-scope-line`; ledger DP-5.2).\n2. **The CONTRIBUTING line for `cargo xtask scope`.** Resolved: restored, with DP-5.2 decided as a pre-PR command.\n3. **DP-1.4, the private markdown held-out fixtures, is not_run.** Authoring them is Kennedy's alone (§9.14), so the delegation cannot close it. The row stays open; a run after the merge is dated as a post-acceptance observation.\n"
  },
  "docs/changes/CHG-019-trust.md": {
    "body": "[Heading { level: 1, slug: \"chg-019-p-b-record-lint-and-the-1176-verifier\", children: [Text(\"CHG-019 (P-B): record lint and the §11.7.6 verifier\")] }, Paragraph([Text(\"Task record: \"), Link { href: \"../../.rha/tasks/CHG-019-trust.toml\", children: [Code(\".rha/tasks/CHG-019-trust.toml\")] }, Text(\". Plan: §8.1 P-B; §8.2 W15 and W16. Base: \"), Code(\"72c5cfd\"), Text(\".\")]), Heading { level: 2, slug: \"intent-and-scope\", children: [Text(\"Intent and scope\")] }, Paragraph([Text(\"Packet P-B covers CHG-019 and CHG-020. It builds the record lint and the acceptance model of spec §11.7.6, so that \"), Code(\"Authentic\"), Text(\" can hold once keys exist and acceptances stop being bootstrap exceptions (lesson L10). It runs concurrently with P-A, which depends on nothing here (task record decision \"), Code(\"concurrent-with-p-a\"), Text(\").\")]), Heading { level: 2, slug: \"deltas\", children: [Text(\"Deltas\")] }, Heading { level: 3, slug: \"stage-1-contract-and-registration-before-any-code\", children: [Text(\"Stage 1, contract and registration (before any code)\")] }, List { start: None, items: [[Code(\"docs/architecture/verifier-contract.md\"), Text(\" fixes the fixture interface: obligations, predicates, exceptions and MergeAllowed as executable terms of §11.7.6, plus the lint's reason codes. Choices the spec leaves open are marked and apply uniformly.\")], [Text(\"A separate Codex session (\"), Code(\"gpt-6-astra\"), Text(\", extra-high) generated \"), Strong([Text(\"152 verifier fixtures\")]), Text(\" and \"), Strong([Text(\"80 record fixtures\")]), Text(\" against that contract, while no lint or verifier code existed. Each corpus has a \"), Code(\"registration.toml\"), Text(\" with the prompt digest and tree digest, and \"), Code(\"xtask/tests/corpus_trust.rs\"), Text(\" pins both. The generator wrote down every case the contract left undetermined, including schema requiredness, nested unknown keys, comparison intervals, and the permission, graph and team rows of §11.7.9. None were invented.\")]] }, Heading { level: 3, slug: \"stage-3-the-verifier-toolsrha-verifier\", children: [Text(\"Stage 3, the verifier (\"), Code(\"tools/rha-verifier\"), Text(\")\")] }, Paragraph([Code(\"evaluate(fixture)\"), Text(\" computes \"), Code(\"R_eff\"), Text(\" (triggers, total classification, local policies that only add, the strictness preorders, the join and its conflicts), the four predicates, \"), Code(\"Eligible\"), Text(\", \"), Code(\"ValidException\"), Text(\" with the ECC-Solo cooling-off path, and \"), Code(\"MergeAllowed\"), Text(\". \"), Strong([Text(\"It decides all 152 registered fixtures exactly as registered on its first run\")]), Text(\", every key of every expected object included. It is a \"), Code(\"tool\"), Text(\" by metadata. \"), Code(\"Cargo.toml\"), Text(\" gains the plan's \"), Code(\"tools/*\"), Text(\" workspace member (decision \"), Code(\"workspace-tools-member\"), Text(\").\")]), Paragraph([Strong([Text(\"Differential and model properties (§2.4 rule 4; W16).\")]), Text(\" A separate Codex session wrote an independent reference verifier, \"), Code(\"tests/reference/mod.rs\"), Text(\", from the contract alone. It was told not to read this crate's code or the corpus. \"), Code(\"model.rs\"), Text(\" had been committed 6.5 minutes earlier, so the claim is independence by instruction, not by order (corrected after the Opus 5.5 review). The two implementations agree on all 152 fixtures and on 512 random mutations: surface, outcomes, test counts, duplicate entries, integrity, time, revocation, parameters, acceptor and producer. Property tests over the same mutations check Lemma 1 (obligations only grow), Lemma 3 (evidence cannot choose the obligations) and Proposition 2 (no silent pass). \"), Code(\"rha-verifier corpus <dir> <out>\"), Text(\" writes the H5 record to \"), Code(\"evidence/h5/\"), Text(\".\")]), Heading { level: 3, slug: \"stage-2-the-record-lint-cargo-xtask-rha-lint\", children: [Text(\"Stage 2, the record lint (\"), Code(\"cargo xtask rha lint\"), Text(\")\")] }, Paragraph([Text(\"The lint implements the contract's reason codes over evidence (JSON), task and acceptance (TOML) records. Its field rules are stated at the top of \"), Code(\"xtask/src/record_lint.rs\"), Text(\". The first run graded \"), Strong([Text(\"77 of 80\")]), Text(\" as registered. All three disagreements were defects in the lint:\")]), List { start: None, items: [[Strong([Text(\"R025:\")]), Text(\" it let \"), Code(\"unknown\"), Text(\" pass as any digest, including a policy digest. It is now accepted only inside \"), Code(\"instruction_sources\"), Text(\", where spec §11.6.5 allows it for messages with no file.\")], [Strong([Text(\"R037:\")]), Text(\" it accepted the offset \"), Code(\"+25:00\"), Text(\". Offsets and date and time fields are now range-checked.\")], [Strong([Text(\"R038:\")]), Text(\" it skipped a non-numeric per-check \"), Code(\"started_at\"), Text(\". In tool-written evidence every \"), Code(\"_at\"), Text(\" field is now checked.\")]] }, Paragraph([Text(\"After the fixes it grades \"), Strong([Text(\"80 of 80\")]), Text(\".\")]), Paragraph([Strong([Text(\"Run over this repository's own records\")]), Text(\", the lint accepts 62 and rejects 9. These are real defects in merged history, disclosed here and not rewritten:\")]), Table { align: [None, None, None], head: [[Text(\"Records\")], [Text(\"Reason\")], [Text(\"Cause\")]], rows: [[[Code(\".rha/acceptances/CHG-002.toml\"), Text(\"; \"), Code(\".rha/tasks/CHG-004.6-c13-registration.toml\"), Text(\"; two \"), Code(\"evidence/CHG-004.6/\"), Text(\" records that copied the latter's provenance\")], [Code(\"revision.malformed\")], [Text(\"abbreviated revisions (\"), Code(\"6518f6a89af5\"), Text(\", \"), Code(\"5ef607c\"), Text(\") where a full identity is required\")]], [[Code(\"evidence/CHG-000/\"), Text(\", \"), Code(\"evidence/CHG-001/\"), Text(\", three early \"), Code(\"evidence/ci/\"), Text(\" records\")], [Code(\"digest.malformed\")], [Code(\"base_policy_digest = \\\"absent\\\"\"), Text(\" where the field needed \"), Code(\"null\"), Text(\", from W0 when no base policy existed\")]]] }, Heading { level: 3, slug: \"review-round-1-codex-gpt-6-astra-extra-high-read-only-on-edf4ed1-determinations\", children: [Text(\"Review round 1 (Codex, \"), Code(\"gpt-6-astra\"), Text(\", extra-high, read-only) on \"), Code(\"edf4ed1\"), Text(\", determinations\")] }, Paragraph([Text(\"REQUEST_CHANGES: seven P1 and one P2.\")]), List { start: Some(1), items: [[Strong([Text(\"P1, fractional seconds were truncated. Confirmed.\")]), Text(\" A \"), Code(\"logged_at\"), Text(\" or \"), Code(\"issued_at\"), Text(\" with \"), Code(\".900\"), Text(\" validated an exception 0.9 s early. Repair: timestamps are compared in nanoseconds.\")], [Strong([Text(\"P1, integer parameters were compared as \"), Code(\"f64\"), Text(\". Confirmed.\")]), Text(\" Above 2^53, \"), Code(\"n = 9007199254740992\"), Text(\" satisfied a required \"), Code(\"9007199254740993\"), Text(\", and a join could drop the stricter value. Repair: two integers compare exactly, and floating point is used only when either side is fractional.\")], [Strong([Text(\"P1, Lemma 1 was vacuous for a missing obligation. Confirmed.\")]), Text(\" Repair: the test derives the triggered root checks independently and asserts each is present in \"), Code(\"R_eff\"), Text(\".\")], [Strong([Text(\"P1, Proposition 2 was vacuous for a wrongly passed check. Confirmed.\")]), Text(\" Repair: the test recomputes every check's pass (outcome and kind validity) itself. When merge is allowed, either all pass or the exception waives every one that does not.\")], [Strong([Text(\"P1, the lint accepted 30 February. Confirmed.\")]), Text(\" Repair: days in month, with leap years.\")], [Strong([Text(\"P1, the lint panicked on a non-ASCII timestamp suffix. Confirmed.\")]), Text(\" Repair: the offset is parsed from bytes, never by slicing a string at a byte index.\")], [Strong([Text(\"P1, the \"), Code(\"Cargo.toml\"), Text(\" edit had no quoted approval. Confirmed.\")]), Text(\" Kennedy was asked and approved all three plan-derived protected edits (decision \"), Code(\"protected-edits-quoted\"), Text(\"). P-A's task record quotes the same answer.\")], [Strong([Text(\"P2, generation provenance for the Executor's own code. Declined.\")]), Text(\" Rule 1 governs artifacts from separate generator sessions; the Executor's work is covered by \"), Code(\"[[provenance.sessions]]\"), Text(\". Revision 3 of the plan should say so plainly.\")]] }, Paragraph([Text(\"The mutation generator also gains fractional timestamps and integer parameters near 2^53, so the differential test now reaches findings 1 and 2.\")]), Heading { level: 3, slug: \"review-round-2-on-10273a1-determinations\", children: [Text(\"Review round 2 on \"), Code(\"10273a1\"), Text(\", determinations\")] }, Paragraph([Text(\"Findings 3 to 7 were confirmed resolved. Two were incomplete, one was new, and one determination was overturned:\")]), List { start: None, items: [[Strong([Text(\"Finding 1, incomplete: digits past the ninth were dropped. Confirmed.\")]), Text(\" A timestamp with more than nine fractional digits is now refused rather than rounded, so an exception fails closed.\")], [Strong([Text(\"Finding 2, incomplete: mixed integer and decimal at 2^53. Confirmed.\")]), Text(\" JSON parsing already rounds such a decimal to \"), Code(\"f64\"), Text(\", so it cannot be ordered exactly. The comparison is now incomparable in that range, which is a conflict or a non-refinement. There is a regression test.\")], [Strong([Text(\"New P1: Proposition 2 had lost its validity assertions in round 1's rewrite. Confirmed.\")]), Text(\" Both the validity assertions and the independent pass checks now stand.\")], [Strong([Text(\"New P2: a wrong revision in the round 1 heading. Confirmed.\")]), Text(\" Corrected to \"), Code(\"edf4ed1\"), Text(\".\")], [Strong([Text(\"Finding 8, overturned.\")]), Text(\" The reviewer showed that plan §2.4 rule 1 names generated code explicitly, so the declination did not hold under the current text. The Executor's work now has a \"), Code(\"[[provenance.generations]]\"), Text(\" entry. Its prompt is Kennedy's quoted instruction in the task record's \"), Code(\"authority\"), Text(\" decision, and its digest is the sha256 of that exact quoted string. P-A's task record gets the same entry.\")]] }, Heading { level: 3, slug: \"automated-review-threads-on-pr-17-chatgpt-codex-connector-on-9bcffad-determinations\", children: [Text(\"Automated review threads on PR 17 (\"), Code(\"chatgpt-codex-connector\"), Text(\", on \"), Code(\"9bcffad\"), Text(\"), determinations\")] }, List { start: Some(1), items: [[Strong([Code(\"cargo xtask rha lint\"), Text(\" with no operands exits 0 (P2). Confirmed.\")]), Text(\" Clap gives an empty \"), Code(\"Vec\"), Text(\", the loop runs zero times, and the command reports success having validated nothing. Repair: the operand is required, so a bare invocation is a usage error (exit 2, xtask's usage code). Discovering every repository record by default was the alternative. It was not chosen because it would change the command's meaning, and a default run would fail on the nine historical defects this packet discloses without rewriting.\")], [Strong([Code(\"rha-verifier corpus\"), Text(\" passes an empty or partial directory (P1). Confirmed.\")]), Text(\" \"), Code(\"passed == cases.len()\"), Text(\" holds at 0/0, and nothing tied the directory to its registration, so a wrong path or deleted fixtures produced a \"), Code(\"passed\"), Text(\" H5 record. Repair: before deciding anything, the runner reads the directory's \"), Code(\"registration.toml\"), Text(\" and recomputes its \"), Code(\"tree_sha256\"), Text(\", the same sorted \"), Code(\"<path> <sha256>\"), Text(\" lines as \"), Code(\"xtask/tests/corpus_trust.rs\"), Text(\". A missing registration, a digest mismatch or zero fixtures is a refusal (exit 2) and writes no record. The record now carries the registered digest. Discriminating checks: integration tests on an empty directory, a copy with one fixture deleted, and the registered corpus. The first two fail on \"), Code(\"9bcffad\"), Text(\", which exits 0 after writing a \"), Code(\"passed\"), Text(\" record, and are refused after the repair; the third passes 152/152.\")]] }, Heading { level: 3, slug: \"conformance-validation-of-the-repair-range-9bcffad8db0531-codex-as-validator-dp-54-determinations\", children: [Text(\"Conformance validation of the repair range \"), Code(\"9bcffad..8db0531\"), Text(\" (Codex as validator, DP-5.4), determinations\")] }, Paragraph([Text(\"Verdict on \"), Code(\"8db0531\"), Text(\": does not conform, two findings, two questions.\")]), List { start: Some(1), items: [[Strong([Text(\"DP-4.1 and DP-5.3 decided in the task record but not on the ledger (P1). Confirmed.\")]), Text(\" Repair: DP-4.1's row is updated and a DP-5.3 row is added, both citing the task record decisions.\")], [Strong([Code(\"docs/evidence/index.md\"), Text(\" stale (P1). Confirmed.\")]), Text(\" The Executor ran the regeneration with an unsupported flag and hid its error. Repair: regenerated; \"), Code(\"docs --check\"), Text(\" is run before every push.\")]] }, Paragraph([Text(\"Questions answered. \"), Emphasis([Text(\"Before-repair evidence:\")]), Text(\" the new tests ran against the unrepaired code in the working tree, before the repair commit, with these results: \"), Code(\"rha-verifier --test cli\"), Text(\" gave \"), Code(\"FAILED. 1 passed; 2 failed\"), Text(\" (the two refusal tests failed, and the registered corpus passed), and \"), Code(\"xtask --test record_lint_cli\"), Text(\" gave \"), Code(\"FAILED. 0 passed; 1 failed\"), Text(\". After the repair both passed. The output was observed in the session and not retained as a file, so it is recorded here. \"), Emphasis([Text(\"Refuted thread:\")]), Text(\" none on this pull request; the prompt was generic.\")]), Heading { level: 3, slug: \"conformance-validation-of-the-merge-with-main-codex-as-validator-determinations\", children: [Text(\"Conformance validation of the merge with main (Codex as validator), determinations\")] }, Paragraph([Text(\"Verdict on \"), Code(\"9c139a6\"), Text(\": does not conform, one finding, one question.\")]), List { start: Some(1), items: [[Strong([Text(\"P-A's acceptance record is missing (P1). Confirmed.\")]), Text(\" Decision \"), Code(\"concurrent-with-p-a\"), Text(\" assigns it to the item that merges after P-A, which is this one. Repair: \"), Code(\".rha/acceptances/CHG-005.toml\"), Text(\" binds the merge \"), Code(\"dc564ea\"), Text(\" to CI run 35799644918 (\"), Code(\"evidence/ci/35799644918.json\"), Text(\", all eight passed, 193 tests), and \"), Code(\"docs/maturity.md\"), Text(\"'s Accepted column records DP-1.6. It is late. It should have been this packet's first commit after the merge; the record says so, and history is not rewritten. \"), Code(\"cargo xtask rha lint\"), Text(\" rejected the first draft for an abbreviated \"), Code(\"subject_revision\"), Text(\", the same defect as the nine historical ones. It was corrected before commit.\")], [Strong([Text(\"Question: is the lint limited to L0 evidence envelopes? Yes, by the contract.\")]), Text(\" Every JSON operand is read as an L0 evidence record (\"), Code(\"verifier-contract.md\"), Text(\" §2), and no contract is registered for corpus reports such as \"), Code(\"evidence/md-corpus/\"), Text(\", so linting one reports \"), Code(\"evidence.missing_required_check\"), Text(\". This is a disclosed limit, for the schema-contract follow-on.\")]] }, Paragraph([Text(\"Also found at the merge: P-A's scope guard, run here for the first time, flagged \"), Code(\"Cargo.toml\"), Text(\" missing from this record's \"), Code(\"scope_globs\"), Text(\" (approved in \"), Code(\"protected_scope\"), Text(\"); added, decision \"), Code(\"scope-cargo-toml\"), Text(\".\")]), Heading { level: 3, slug: \"approving-reviews-on-522445f-kennedys-rule-of-2026-09-22-an-opus-55-agent-and-a-gpt-6-agent-determinations\", children: [Text(\"Approving reviews on \"), Code(\"522445f\"), Text(\" (Kennedy's rule of 2026-09-22: an Opus 5.5 agent and a GPT-6 agent), determinations\")] }, Paragraph([Strong([Text(\"GPT-6 (\"), Code(\"gpt-6-astra\"), Text(\", extra-high, validator role): REQUEST_CHANGES.\")])]), List { start: Some(1), items: [[Strong([Code(\".rha/acceptances/CHG-005.toml\"), Text(\" lacks \"), Code(\"[[disposition.predicates]]\"), Text(\" (P1). Confirmed.\")]), Text(\" \"), Code(\"[acceptance.bootstrap]\"), Text(\" requires the failed predicates with their reasons in that table, as CHG-004 and CHG-004.6 have. The repair adds all four predicates. \"), Code(\".rha/acceptances/PLAN-2.toml\"), Text(\" on main has the same gap; it is a historical record and is disclosed here, not rewritten.\")]] }, Paragraph([Strong([Text(\"Opus 5.5 (an independent reviewer agent): APPROVE, with four findings and five questions.\")])]), List { start: Some(1), items: [[Strong([Text(\"The verifier fails open on malformed fixtures (P2). Confirmed by the reviewer's probe crate.\")]), Text(\" A missing \"), Code(\"revoked\"), Text(\" reads as \\\"not revoked\\\". A missing \"), Code(\"accountable_change_authority\"), Text(\" makes a self-issued exception look independent. Missing binding fields compare \"), Code(\"null == null\"), Text(\" and make \"), Code(\"applicable\"), Text(\" true. A missing or non-string \"), Code(\"surface\"), Text(\" gives an empty \"), Code(\"r_eff\"), Text(\", so merge is allowed with no evidence. No registered fixture is malformed, so this has no consequence on the corpus, but the verifier must not fail open before it reads real records. Repair: \"), Code(\"evaluate\"), Text(\" first checks the fixture against the shape of contract §1. Every member is present with its type, \"), Code(\"surface\"), Text(\" is non-empty strings, entries and checks are objects with string ids, and the exception object carries every §1.3 member. A fixture that fails is refused: every predicate is \"), Code(\"false\"), Text(\", \"), Code(\"merge_allowed\"), Text(\" is \"), Code(\"false\"), Text(\", and the output names the first missing or mistyped member under \"), Code(\"malformed\"), Text(\".\")], [Strong([Text(\"A negative \"), Code(\"cooling_off_hours\"), Text(\" passes the ECC-Solo check (P3). Confirmed.\")]), Text(\" Repair: a negative value is malformed policy, and the fixture is refused as in item 1.\")], [Strong([Code(\"digest.mismatch\"), Text(\" is never checked on repository records (P3). Confirmed.\")]), Text(\" Cited paths resolve beside the record, while repository records cite paths from the repository root, so the check was skipped silently. The change record's earlier \\\"accepts 62\\\" did not say so. Repair: when the file is not beside the record, the lint reads it from git at the record's own subject, \"), Code(\"artifact_identity.revision\"), Text(\", and compares. A revision git does not have (the synthetic corpus) keeps the beside-the-record rule. That is the contract's rule, and no registered outcome changes. Observed: across the repository's 71 L0 evidence records, the old and new lint give identical verdicts (64 accepted). Every cited file that git has at the record's subject hashes to its cited digest. A copy of the P-B head record with one digest zeroed is accepted by the old lint and rejected by the new one with \"), Code(\"digest.mismatch\"), Text(\". The regression test builds its record at \"), Code(\"HEAD\"), Text(\", because CI's depth-2 checkout does not have older subjects.\")], [Strong([Text(\"The differential test is weaker than described (P3). Confirmed.\")]), Text(\" Mutation arm 9 fell through to the producer arm, and no arm deletes a field. Repair: arm 9 is its own case. The reference is pinned by the registration, so the differential property still covers the contract's domain, well-formed fixtures. A new property deletes or retypes one member at a time and requires the verifier to refuse the result with \"), Code(\"merge_allowed = false\"), Text(\". Where the reference also mishandles malformed input (\"), Code(\"null == null\"), Text(\"), that is out of its domain and recorded here, not repaired in a pinned artifact. One disagreement is inside the domain. Registered fixture V031 makes a negative \"), Code(\"selected_tests\"), Text(\" well formed, failing \"), Code(\"passed\"), Text(\", so a fractional count is well formed too. The implementation does not pass it, because a test count is whole; the pinned reference passes it (\"), Code(\"0.5 > 0\"), Text(\"). The implementation's reading is the fail-closed one and is kept, with a test. Discriminating checks: with the refusal disabled, the new property and the review probes both fail; the registered corpus still decides 152/152.\")]] }, Paragraph([Text(\"Questions answered:\")]), List { start: Some(1), items: [[Emphasis([Text(\"Reference independence.\")]), Text(\" The reference was written by a session told not to read \"), Code(\"tools/rha-verifier/src/\"), Text(\"; \"), Code(\"model.rs\"), Text(\" was committed 6.5 minutes earlier. The claim is corrected to \\\"without reading the implementation\\\", not \\\"before it existed\\\".\")], [Emphasis([Text(\"A missing or unknown \"), Code(\"outcome\"), Text(\" in lint evidence.\")]), Text(\" The contract has no code for it; it belongs to the \"), Code(\"schema.*\"), Text(\" cases of the schema-contract follow-on (decision \"), Code(\"schema-contract\"), Text(\").\")], [Emphasis([Code(\"unknown\"), Text(\" revisions, uppercase hex, and bare 64-hex digests.\")]), Text(\" These are the deliberate choices stated in \"), Code(\"record_lint.rs\"), Text(\"'s header. The historical records use them, and the schema contract tightens them per field.\")], [Emphasis([Text(\"The entry's \"), Code(\"kind\"), Text(\".\")]), Text(\" The verifier judges validity by the policy check's kind, as \"), Code(\"Complete\"), Text(\" and the contract specify; the kind-equality of \"), Code(\"⊑\"), Text(\" is carried by the join, which refuses mixed kinds.\")], [Emphasis([Text(\"No L0 record at \"), Code(\"522445f\"), Text(\".\")]), Text(\" That commit changed only \"), Code(\"scope_globs\"), Text(\". The record for the repaired head follows these repairs.\")]] }, Heading { level: 3, slug: \"re-reviews-of-the-repair-range-522445f2dfc616-determinations\", children: [Text(\"Re-reviews of the repair range \"), Code(\"522445f..2dfc616\"), Text(\", determinations\")] }, Paragraph([Strong([Text(\"Opus 5.5: APPROVE.\")]), Text(\" It re-ran its probes: every fail-open case is refused, 0 of 152 registered fixtures are refused, V031 is as registered, and the lint catches the tampered records with no false mismatch across 76 L0 records. It left one P3 and two questions:\")]), List { start: None, items: [[Emphasis([Text(\"P3, the contract lags the code.\")]), Text(\" The fixture shape \"), Code(\"malformed()\"), Text(\" enforces (non-empty \"), Code(\"surface\"), Text(\", \"), Code(\"kind\"), Text(\" and \"), Code(\"params\"), Text(\" on every entry, an unsigned \"), Code(\"cooling_off_hours\"), Text(\", the exception's members, a whole test count for \"), Code(\"passed\"), Text(\") is written only in \"), Code(\"model.rs\"), Text(\" and this record. \"), Strong([Text(\"Confirmed.\")]), Text(\" A registered contract is not edited after its corpus exists. The amendment is carried to the schema-contract follow-on, which registers these rules with their malformed cases (acceptance concern 4).\")], [Emphasis([Text(\"Question, an unchecked citation is silent.\")]), Text(\" When git lacks a record's subject, the lint skips the digest check without saying so. \"), Strong([Text(\"Kept as disclosed\")]), Text(\"; the follow-on adds a note to the lint's output.\")], [Emphasis([Text(\"Nit.\")]), Text(\" The failed clippy record belongs to \"), Code(\"0a6b833\"), Text(\", not to \"), Code(\"7f9b4af\"), Text(\", the commit that added it. Corrected above.\")]] }, Paragraph([Strong([Text(\"GPT-6: REQUEST_CHANGES, one P1 and one question.\")])]), List { start: Some(1), items: [[Strong([Text(\"An absent \"), Code(\"exception\"), Text(\" key still allowed a merge (P1). Confirmed.\")]), Text(\" \"), Code(\"f[\\\"exception\\\"]\"), Text(\" reads an absent key as \"), Code(\"null\"), Text(\", so V001 without the key merged, and \"), Code(\"MEMBERS\"), Text(\" omitted the top-level \"), Code(\"exception\"), Text(\". Repair: the key must be present, as \"), Code(\"null\"), Text(\" or an object. It is added to \"), Code(\"MEMBERS\"), Text(\" and to the review probes. With the new check disabled, the property fails; with it, all pass.\")], [Emphasis([Text(\"Question: where is the non-empty \"), Code(\"surface\"), Text(\" registered?\")]), Text(\" \"), Strong([Text(\"Nowhere; it is a choice beyond the contract.\")]), Text(\" The contract gives \"), Code(\"surface\"), Text(\" no minimum. An empty surface triggers no obligation, so a fixture with no evidence would be eligible. Refusing it fails closed, and no registered fixture has an empty surface. The earlier claim is narrowed accordingly: no \"), Emphasis([Text(\"registered\")]), Text(\" fixture is newly refused, while an empty surface is refused by choice. It is registered with the other shape rules in the follow-on.\")]] }, Heading { level: 3, slug: \"repair-attempts-after-the-approving-reviews-11710\", children: [Text(\"Repair attempts after the approving reviews (§11.7.10)\")] }, List { start: Some(1), items: [[Strong([Code(\"L0.clippy\"), Text(\" failed in the record of \"), Code(\"0a6b833\"), Text(\" (\"), Code(\"evidence/CHG-019/20260923T001604Z-0a6b83370559.json\"), Text(\", committed in \"), Code(\"7f9b4af\"), Text(\"), and that head was pushed.\")]), Text(\" Hypothesis: the lint repair left a collapsible nested \"), Code(\"if\"), Text(\". Discriminating check: \"), Code(\"cargo clippy\"), Text(\" reported \"), Code(\"collapsible_if\"), Text(\" at \"), Code(\"record_lint.rs:164\"), Text(\", an error under the workspace lints. Change: the conditions are one \"), Code(\"let\"), Text(\" chain. Result: clippy is clean. The failed record is kept. The method lesson is the same as P-A's: run clippy before the commit, and never let a push follow a failed lane in one command chain.\")]] }, Heading { level: 3, slug: \"not-done-in-this-packet-and-why\", children: [Text(\"Not done in this packet, and why\")] }, List { start: None, items: [[Strong([Text(\"JSON Schema files under \"), Code(\".rha/schemas/\")]), Text(\" (W15). The generator found that the contract fixes no required fields, types or allowed keys, so the \"), Code(\"schema.*\"), Text(\" codes have no registered cases. Writing schemas now would be the invented contract §2.4 rule 6 forbids. The schema decision is taken under Kennedy's delegation of 2026-09-22 (task record decision \"), Code(\"schema-contract\"), Text(\"): required fields, types and allowed keys are derived mechanically from the record shapes committed so far and registered, with their malformed cases, before any schema file exists. That is its own follow-on item, not this packet.\")], [Strong([Text(\"Stage 4, keys and cut-over\")]), Text(\" (DP-4.1, DP-5.3): decided under Kennedy's delegation of 2026-09-22 (task record decisions \"), Code(\"dp-4.1\"), Text(\", \"), Code(\"dp-5.3\"), Text(\"). The key cut-over is \"), Strong([Text(\"not_run\")]), Text(\": no producer isolated from the candidate exists, and a key the Executor or a candidate-run CI job could use would make \"), Code(\"Authentic\"), Text(\" vacuous. The bootstrap is not retired; its acceptance kind is renamed \"), Code(\"bootstrap_acceptance\"), Text(\" (proposal row 8) in \"), Code(\".rha/policy.toml\"), Text(\".\")]] }, Heading { level: 2, slug: \"acceptance-concerns\", children: [Text(\"Acceptance concerns\")] }, List { start: Some(1), items: [[Strong([Code(\"Authentic\"), Text(\" cannot hold, by decision.\")]), Text(\" DP-4.1 creates no key (above). Every acceptance stays \"), Code(\"bootstrap_acceptance\"), Text(\" with \"), Code(\"merge_allowed = false\"), Text(\" until a producer isolated from the candidate exists. The spec's §11.7.6 model is implemented and graded at predicate level; the authentication channel stays in plan §10's deferred list.\")], [Strong([Text(\"The schema files are a follow-on item\")]), Text(\" (above). Until they exist, Codex validates against \"), Code(\"docs/architecture/verifier-contract.md\"), Text(\" and the two corpus registrations (DP-5.4).\")], [Strong([Text(\"This PR edits \"), Code(\".rha/policy.toml\"), Text(\", so \"), Code(\"Applicable\"), Text(\" is false for its records\")]), Text(\" (the candidate policy digest differs from the base; \"), Code(\"evidence/CHG-019/20260922T233629Z-5a768afbe364.json\"), Text(\"). That is the model working as specified: the edit is judged under the policy it replaces. Its acceptance is a controlled transition in the §11.5 sense, the first one this repository has had. The acceptance record says so and names the cause.\")], [Strong([Text(\"The verifier contract lags the enforced fixture shape\")]), Text(\" (re-reviews above): non-empty \"), Code(\"surface\"), Text(\", required \"), Code(\"exception\"), Text(\" key, entry \"), Code(\"kind\"), Text(\" and \"), Code(\"params\"), Text(\", unsigned \"), Code(\"cooling_off_hours\"), Text(\", a whole test count. They fail closed and change no registered decision. They are registered in the schema-contract follow-on through the amendment procedure, and the lint's note for unchecked citations arrives with them.\")]] }]",
    "headings": "[Heading { level: 1, text: \"CHG-019 (P-B): record lint and the §11.7.6 verifier\", slug: \"chg-019-p-b-record-lint-and-the-1176-verifier\", base_slug: \"chg-019-p-b-record-lint-and-the-1176-verifier\", line: 1 }, Heading { level: 2, text: \"Intent and scope\", slug: \"intent-and-scope\", base_slug: \"intent-and-scope\", line: 5 }, Heading { level: 2, text: \"Deltas\", slug: \"deltas\", base_slug: \"deltas\", line: 9 }, Heading { level: 3, text: \"Stage 1, contract and registration (before any code)\", slug: \"stage-1-contract-and-registration-before-any-code\", base_slug: \"stage-1-contract-and-registration-before-any-code\", line: 11 }, Heading { level: 3, text: \"Stage 3, the verifier (tools/rha-verifier)\", slug: \"stage-3-the-verifier-toolsrha-verifier\", base_slug: \"stage-3-the-verifier-toolsrha-verifier\", line: 16 }, Heading { level: 3, text: \"Stage 2, the record lint (cargo xtask rha lint)\", slug: \"stage-2-the-record-lint-cargo-xtask-rha-lint\", base_slug: \"stage-2-the-record-lint-cargo-xtask-rha-lint\", line: 22 }, Heading { level: 3, text: \"Review round 1 (Codex, gpt-6-astra, extra-high, read-only) on edf4ed1, determinations\", slug: \"review-round-1-codex-gpt-6-astra-extra-high-read-only-on-edf4ed1-determinations\", base_slug: \"review-round-1-codex-gpt-6-astra-extra-high-read-only-on-edf4ed1-determinations\", line: 39 }, Heading { level: 3, text: \"Review round 2 on 10273a1, determinations\", slug: \"review-round-2-on-10273a1-determinations\", base_slug: \"review-round-2-on-10273a1-determinations\", line: 54 }, Heading { level: 3, text: \"Automated review threads on PR 17 (chatgpt-codex-connector, on 9bcffad), determinations\", slug: \"automated-review-threads-on-pr-17-chatgpt-codex-connector-on-9bcffad-determinations\", base_slug: \"automated-review-threads-on-pr-17-chatgpt-codex-connector-on-9bcffad-determinations\", line: 64 }, Heading { level: 3, text: \"Conformance validation of the repair range 9bcffad..8db0531 (Codex as validator, DP-5.4), determinations\", slug: \"conformance-validation-of-the-repair-range-9bcffad8db0531-codex-as-validator-dp-54-determinations\", base_slug: \"conformance-validation-of-the-repair-range-9bcffad8db0531-codex-as-validator-dp-54-determinations\", line: 69 }, Heading { level: 3, text: \"Conformance validation of the merge with main (Codex as validator), determinations\", slug: \"conformance-validation-of-the-merge-with-main-codex-as-validator-determinations\", base_slug: \"conformance-validation-of-the-merge-with-main-codex-as-validator-determinations\", line: 78 }, Heading { level: 3, text: \"Approving reviews on 522445f (Kennedy's rule of 2026-09-22: an Opus 5.5 agent and a GPT-6 agent), determinations\", slug: \"approving-reviews-on-522445f-kennedys-rule-of-2026-09-22-an-opus-55-agent-and-a-gpt-6-agent-determinations\", base_slug: \"approving-reviews-on-522445f-kennedys-rule-of-2026-09-22-an-opus-55-agent-and-a-gpt-6-agent-determinations\", line: 87 }, Heading { level: 3, text: \"Re-reviews of the repair range 522445f..2dfc616, determinations\", slug: \"re-reviews-of-the-repair-range-522445f2dfc616-determinations\", base_slug: \"re-reviews-of-the-repair-range-522445f2dfc616-determinations\", line: 107 }, Heading { level: 3, text: \"Repair attempts after the approving reviews (§11.7.10)\", slug: \"repair-attempts-after-the-approving-reviews-11710\", base_slug: \"repair-attempts-after-the-approving-reviews-11710\", line: 118 }, Heading { level: 3, text: \"Not done in this packet, and why\", slug: \"not-done-in-this-packet-and-why\", base_slug: \"not-done-in-this-packet-and-why\", line: 122 }, Heading { level: 2, text: \"Acceptance concerns\", slug: \"acceptance-concerns\", base_slug: \"acceptance-concerns\", line: 127 }]",
    "markdown": "# CHG-019 (P-B): record lint and the §11.7.6 verifier\n\nTask record: [`.rha/tasks/CHG-019-trust.toml`](../../.rha/tasks/CHG-019-trust.toml). Plan: §8.1 P-B; §8.2 W15 and W16. Base: `72c5cfd`.\n\n## Intent and scope\n\nPacket P-B covers CHG-019 and CHG-020. It builds the record lint and the acceptance model of spec §11.7.6, so that `Authentic` can hold once keys exist and acceptances stop being bootstrap exceptions (lesson L10). It runs concurrently with P-A, which depends on nothing here (task record decision `concurrent-with-p-a`).\n\n## Deltas\n\n### Stage 1, contract and registration (before any code)\n\n- `docs/architecture/verifier-contract.md` fixes the fixture interface: obligations, predicates, exceptions and MergeAllowed as executable terms of §11.7.6, plus the lint's reason codes. Choices the spec leaves open are marked and apply uniformly.\n- A separate Codex session (`gpt-6-astra`, extra-high) generated **152 verifier fixtures** and **80 record fixtures** against that contract, while no lint or verifier code existed. Each corpus has a `registration.toml` with the prompt digest and tree digest, and `xtask/tests/corpus_trust.rs` pins both. The generator wrote down every case the contract left undetermined, including schema requiredness, nested unknown keys, comparison intervals, and the permission, graph and team rows of §11.7.9. None were invented.\n\n### Stage 3, the verifier (`tools/rha-verifier`)\n\n`evaluate(fixture)` computes `R_eff` (triggers, total classification, local policies that only add, the strictness preorders, the join and its conflicts), the four predicates, `Eligible`, `ValidException` with the ECC-Solo cooling-off path, and `MergeAllowed`. **It decides all 152 registered fixtures exactly as registered on its first run**, every key of every expected object included. It is a `tool` by metadata. `Cargo.toml` gains the plan's `tools/*` workspace member (decision `workspace-tools-member`).\n\n**Differential and model properties (§2.4 rule 4; W16).** A separate Codex session wrote an independent reference verifier, `tests/reference/mod.rs`, from the contract alone. It was told not to read this crate's code or the corpus. `model.rs` had been committed 6.5 minutes earlier, so the claim is independence by instruction, not by order (corrected after the Opus 5.5 review). The two implementations agree on all 152 fixtures and on 512 random mutations: surface, outcomes, test counts, duplicate entries, integrity, time, revocation, parameters, acceptor and producer. Property tests over the same mutations check Lemma 1 (obligations only grow), Lemma 3 (evidence cannot choose the obligations) and Proposition 2 (no silent pass). `rha-verifier corpus <dir> <out>` writes the H5 record to `evidence/h5/`.\n\n### Stage 2, the record lint (`cargo xtask rha lint`)\n\nThe lint implements the contract's reason codes over evidence (JSON), task and acceptance (TOML) records. Its field rules are stated at the top of `xtask/src/record_lint.rs`. The first run graded **77 of 80** as registered. All three disagreements were defects in the lint:\n\n- **R025:** it let `unknown` pass as any digest, including a policy digest. It is now accepted only inside `instruction_sources`, where spec §11.6.5 allows it for messages with no file.\n- **R037:** it accepted the offset `+25:00`. Offsets and date and time fields are now range-checked.\n- **R038:** it skipped a non-numeric per-check `started_at`. In tool-written evidence every `_at` field is now checked.\n\nAfter the fixes it grades **80 of 80**.\n\n**Run over this repository's own records**, the lint accepts 62 and rejects 9. These are real defects in merged history, disclosed here and not rewritten:\n\n| Records | Reason | Cause |\n| --- | --- | --- |\n| `.rha/acceptances/CHG-002.toml`; `.rha/tasks/CHG-004.6-c13-registration.toml`; two `evidence/CHG-004.6/` records that copied the latter's provenance | `revision.malformed` | abbreviated revisions (`6518f6a89af5`, `5ef607c`) where a full identity is required |\n| `evidence/CHG-000/`, `evidence/CHG-001/`, three early `evidence/ci/` records | `digest.malformed` | `base_policy_digest = \"absent\"` where the field needed `null`, from W0 when no base policy existed |\n\n### Review round 1 (Codex, `gpt-6-astra`, extra-high, read-only) on `edf4ed1`, determinations\n\nREQUEST_CHANGES: seven P1 and one P2.\n\n1. **P1, fractional seconds were truncated. Confirmed.** A `logged_at` or `issued_at` with `.900` validated an exception 0.9 s early. Repair: timestamps are compared in nanoseconds.\n2. **P1, integer parameters were compared as `f64`. Confirmed.** Above 2^53, `n = 9007199254740992` satisfied a required `9007199254740993`, and a join could drop the stricter value. Repair: two integers compare exactly, and floating point is used only when either side is fractional.\n3. **P1, Lemma 1 was vacuous for a missing obligation. Confirmed.** Repair: the test derives the triggered root checks independently and asserts each is present in `R_eff`.\n4. **P1, Proposition 2 was vacuous for a wrongly passed check. Confirmed.** Repair: the test recomputes every check's pass (outcome and kind validity) itself. When merge is allowed, either all pass or the exception waives every one that does not.\n5. **P1, the lint accepted 30 February. Confirmed.** Repair: days in month, with leap years.\n6. **P1, the lint panicked on a non-ASCII timestamp suffix. Confirmed.** Repair: the offset is parsed from bytes, never by slicing a string at a byte index.\n7. **P1, the `Cargo.toml` edit had no quoted approval. Confirmed.** Kennedy was asked and approved all three plan-derived protected edits (decision `protected-edits-quoted`). P-A's task record quotes the same answer.\n8. **P2, generation provenance for the Executor's own code. Declined.** Rule 1 governs artifacts from separate generator sessions; the Executor's work is covered by `[[provenance.sessions]]`. Revision 3 of the plan should say so plainly.\n\nThe mutation generator also gains fractional timestamps and integer parameters near 2^53, so the differential test now reaches findings 1 and 2.\n\n### Review round 2 on `10273a1`, determinations\n\nFindings 3 to 7 were confirmed resolved. Two were incomplete, one was new, and one determination was overturned:\n\n- **Finding 1, incomplete: digits past the ninth were dropped. Confirmed.** A timestamp with more than nine fractional digits is now refused rather than rounded, so an exception fails closed.\n- **Finding 2, incomplete: mixed integer and decimal at 2^53. Confirmed.** JSON parsing already rounds such a decimal to `f64`, so it cannot be ordered exactly. The comparison is now incomparable in that range, which is a conflict or a non-refinement. There is a regression test.\n- **New P1: Proposition 2 had lost its validity assertions in round 1's rewrite. Confirmed.** Both the validity assertions and the independent pass checks now stand.\n- **New P2: a wrong revision in the round 1 heading. Confirmed.** Corrected to `edf4ed1`.\n- **Finding 8, overturned.** The reviewer showed that plan §2.4 rule 1 names generated code explicitly, so the declination did not hold under the current text. The Executor's work now has a `[[provenance.generations]]` entry. Its prompt is Kennedy's quoted instruction in the task record's `authority` decision, and its digest is the sha256 of that exact quoted string. P-A's task record gets the same entry.\n\n### Automated review threads on PR 17 (`chatgpt-codex-connector`, on `9bcffad`), determinations\n\n1. **`cargo xtask rha lint` with no operands exits 0 (P2). Confirmed.** Clap gives an empty `Vec`, the loop runs zero times, and the command reports success having validated nothing. Repair: the operand is required, so a bare invocation is a usage error (exit 2, xtask's usage code). Discovering every repository record by default was the alternative. It was not chosen because it would change the command's meaning, and a default run would fail on the nine historical defects this packet discloses without rewriting.\n2. **`rha-verifier corpus` passes an empty or partial directory (P1). Confirmed.** `passed == cases.len()` holds at 0/0, and nothing tied the directory to its registration, so a wrong path or deleted fixtures produced a `passed` H5 record. Repair: before deciding anything, the runner reads the directory's `registration.toml` and recomputes its `tree_sha256`, the same sorted `<path> <sha256>` lines as `xtask/tests/corpus_trust.rs`. A missing registration, a digest mismatch or zero fixtures is a refusal (exit 2) and writes no record. The record now carries the registered digest. Discriminating checks: integration tests on an empty directory, a copy with one fixture deleted, and the registered corpus. The first two fail on `9bcffad`, which exits 0 after writing a `passed` record, and are refused after the repair; the third passes 152/152.\n\n### Conformance validation of the repair range `9bcffad..8db0531` (Codex as validator, DP-5.4), determinations\n\nVerdict on `8db0531`: does not conform, two findings, two questions.\n\n1. **DP-4.1 and DP-5.3 decided in the task record but not on the ledger (P1). Confirmed.** Repair: DP-4.1's row is updated and a DP-5.3 row is added, both citing the task record decisions.\n2. **`docs/evidence/index.md` stale (P1). Confirmed.** The Executor ran the regeneration with an unsupported flag and hid its error. Repair: regenerated; `docs --check` is run before every push.\n\nQuestions answered. *Before-repair evidence:* the new tests ran against the unrepaired code in the working tree, before the repair commit, with these results: `rha-verifier --test cli` gave `FAILED. 1 passed; 2 failed` (the two refusal tests failed, and the registered corpus passed), and `xtask --test record_lint_cli` gave `FAILED. 0 passed; 1 failed`. After the repair both passed. The output was observed in the session and not retained as a file, so it is recorded here. *Refuted thread:* none on this pull request; the prompt was generic.\n\n### Conformance validation of the merge with main (Codex as validator), determinations\n\nVerdict on `9c139a6`: does not conform, one finding, one question.\n\n1. **P-A's acceptance record is missing (P1). Confirmed.** Decision `concurrent-with-p-a` assigns it to the item that merges after P-A, which is this one. Repair: `.rha/acceptances/CHG-005.toml` binds the merge `dc564ea` to CI run 35799644918 (`evidence/ci/35799644918.json`, all eight passed, 193 tests), and `docs/maturity.md`'s Accepted column records DP-1.6. It is late. It should have been this packet's first commit after the merge; the record says so, and history is not rewritten. `cargo xtask rha lint` rejected the first draft for an abbreviated `subject_revision`, the same defect as the nine historical ones. It was corrected before commit.\n2. **Question: is the lint limited to L0 evidence envelopes? Yes, by the contract.** Every JSON operand is read as an L0 evidence record (`verifier-contract.md` §2), and no contract is registered for corpus reports such as `evidence/md-corpus/`, so linting one reports `evidence.missing_required_check`. This is a disclosed limit, for the schema-contract follow-on.\n\nAlso found at the merge: P-A's scope guard, run here for the first time, flagged `Cargo.toml` missing from this record's `scope_globs` (approved in `protected_scope`); added, decision `scope-cargo-toml`.\n\n### Approving reviews on `522445f` (Kennedy's rule of 2026-09-22: an Opus 5.5 agent and a GPT-6 agent), determinations\n\n**GPT-6 (`gpt-6-astra`, extra-high, validator role): REQUEST_CHANGES.**\n\n1. **`.rha/acceptances/CHG-005.toml` lacks `[[disposition.predicates]]` (P1). Confirmed.** `[acceptance.bootstrap]` requires the failed predicates with their reasons in that table, as CHG-004 and CHG-004.6 have. The repair adds all four predicates. `.rha/acceptances/PLAN-2.toml` on main has the same gap; it is a historical record and is disclosed here, not rewritten.\n\n**Opus 5.5 (an independent reviewer agent): APPROVE, with four findings and five questions.**\n\n1. **The verifier fails open on malformed fixtures (P2). Confirmed by the reviewer's probe crate.** A missing `revoked` reads as \"not revoked\". A missing `accountable_change_authority` makes a self-issued exception look independent. Missing binding fields compare `null == null` and make `applicable` true. A missing or non-string `surface` gives an empty `r_eff`, so merge is allowed with no evidence. No registered fixture is malformed, so this has no consequence on the corpus, but the verifier must not fail open before it reads real records. Repair: `evaluate` first checks the fixture against the shape of contract §1. Every member is present with its type, `surface` is non-empty strings, entries and checks are objects with string ids, and the exception object carries every §1.3 member. A fixture that fails is refused: every predicate is `false`, `merge_allowed` is `false`, and the output names the first missing or mistyped member under `malformed`.\n2. **A negative `cooling_off_hours` passes the ECC-Solo check (P3). Confirmed.** Repair: a negative value is malformed policy, and the fixture is refused as in item 1.\n3. **`digest.mismatch` is never checked on repository records (P3). Confirmed.** Cited paths resolve beside the record, while repository records cite paths from the repository root, so the check was skipped silently. The change record's earlier \"accepts 62\" did not say so. Repair: when the file is not beside the record, the lint reads it from git at the record's own subject, `artifact_identity.revision`, and compares. A revision git does not have (the synthetic corpus) keeps the beside-the-record rule. That is the contract's rule, and no registered outcome changes. Observed: across the repository's 71 L0 evidence records, the old and new lint give identical verdicts (64 accepted). Every cited file that git has at the record's subject hashes to its cited digest. A copy of the P-B head record with one digest zeroed is accepted by the old lint and rejected by the new one with `digest.mismatch`. The regression test builds its record at `HEAD`, because CI's depth-2 checkout does not have older subjects.\n4. **The differential test is weaker than described (P3). Confirmed.** Mutation arm 9 fell through to the producer arm, and no arm deletes a field. Repair: arm 9 is its own case. The reference is pinned by the registration, so the differential property still covers the contract's domain, well-formed fixtures. A new property deletes or retypes one member at a time and requires the verifier to refuse the result with `merge_allowed = false`. Where the reference also mishandles malformed input (`null == null`), that is out of its domain and recorded here, not repaired in a pinned artifact. One disagreement is inside the domain. Registered fixture V031 makes a negative `selected_tests` well formed, failing `passed`, so a fractional count is well formed too. The implementation does not pass it, because a test count is whole; the pinned reference passes it (`0.5 > 0`). The implementation's reading is the fail-closed one and is kept, with a test. Discriminating checks: with the refusal disabled, the new property and the review probes both fail; the registered corpus still decides 152/152.\n\nQuestions answered:\n1. *Reference independence.* The reference was written by a session told not to read `tools/rha-verifier/src/`; `model.rs` was committed 6.5 minutes earlier. The claim is corrected to \"without reading the implementation\", not \"before it existed\".\n2. *A missing or unknown `outcome` in lint evidence.* The contract has no code for it; it belongs to the `schema.*` cases of the schema-contract follow-on (decision `schema-contract`).\n3. *`unknown` revisions, uppercase hex, and bare 64-hex digests.* These are the deliberate choices stated in `record_lint.rs`'s header. The historical records use them, and the schema contract tightens them per field.\n4. *The entry's `kind`.* The verifier judges validity by the policy check's kind, as `Complete` and the contract specify; the kind-equality of `⊑` is carried by the join, which refuses mixed kinds.\n5. *No L0 record at `522445f`.* That commit changed only `scope_globs`. The record for the repaired head follows these repairs.\n\n### Re-reviews of the repair range `522445f..2dfc616`, determinations\n\n**Opus 5.5: APPROVE.** It re-ran its probes: every fail-open case is refused, 0 of 152 registered fixtures are refused, V031 is as registered, and the lint catches the tampered records with no false mismatch across 76 L0 records. It left one P3 and two questions:\n- *P3, the contract lags the code.* The fixture shape `malformed()` enforces (non-empty `surface`, `kind` and `params` on every entry, an unsigned `cooling_off_hours`, the exception's members, a whole test count for `passed`) is written only in `model.rs` and this record. **Confirmed.** A registered contract is not edited after its corpus exists. The amendment is carried to the schema-contract follow-on, which registers these rules with their malformed cases (acceptance concern 4).\n- *Question, an unchecked citation is silent.* When git lacks a record's subject, the lint skips the digest check without saying so. **Kept as disclosed**; the follow-on adds a note to the lint's output.\n- *Nit.* The failed clippy record belongs to `0a6b833`, not to `7f9b4af`, the commit that added it. Corrected above.\n\n**GPT-6: REQUEST_CHANGES, one P1 and one question.**\n1. **An absent `exception` key still allowed a merge (P1). Confirmed.** `f[\"exception\"]` reads an absent key as `null`, so V001 without the key merged, and `MEMBERS` omitted the top-level `exception`. Repair: the key must be present, as `null` or an object. It is added to `MEMBERS` and to the review probes. With the new check disabled, the property fails; with it, all pass.\n2. *Question: where is the non-empty `surface` registered?* **Nowhere; it is a choice beyond the contract.** The contract gives `surface` no minimum. An empty surface triggers no obligation, so a fixture with no evidence would be eligible. Refusing it fails closed, and no registered fixture has an empty surface. The earlier claim is narrowed accordingly: no *registered* fixture is newly refused, while an empty surface is refused by choice. It is registered with the other shape rules in the follow-on.\n\n### Repair attempts after the approving reviews (§11.7.10)\n\n1. **`L0.clippy` failed in the record of `0a6b833` (`evidence/CHG-019/20260923T001604Z-0a6b83370559.json`, committed in `7f9b4af`), and that head was pushed.** Hypothesis: the lint repair left a collapsible nested `if`. Discriminating check: `cargo clippy` reported `collapsible_if` at `record_lint.rs:164`, an error under the workspace lints. Change: the conditions are one `let` chain. Result: clippy is clean. The failed record is kept. The method lesson is the same as P-A's: run clippy before the commit, and never let a push follow a failed lane in one command chain.\n\n### Not done in this packet, and why\n\n- **JSON Schema files under `.rha/schemas/`** (W15). The generator found that the contract fixes no required fields, types or allowed keys, so the `schema.*` codes have no registered cases. Writing schemas now would be the invented contract §2.4 rule 6 forbids. The schema decision is taken under Kennedy's delegation of 2026-09-22 (task record decision `schema-contract`): required fields, types and allowed keys are derived mechanically from the record shapes committed so far and registered, with their malformed cases, before any schema file exists. That is its own follow-on item, not this packet.\n- **Stage 4, keys and cut-over** (DP-4.1, DP-5.3): decided under Kennedy's delegation of 2026-09-22 (task record decisions `dp-4.1`, `dp-5.3`). The key cut-over is **not_run**: no producer isolated from the candidate exists, and a key the Executor or a candidate-run CI job could use would make `Authentic` vacuous. The bootstrap is not retired; its acceptance kind is renamed `bootstrap_acceptance` (proposal row 8) in `.rha/policy.toml`.\n\n## Acceptance concerns\n\n1. **`Authentic` cannot hold, by decision.** DP-4.1 creates no key (above). Every acceptance stays `bootstrap_acceptance` with `merge_allowed = false` until a producer isolated from the candidate exists. The spec's §11.7.6 model is implemented and graded at predicate level; the authentication channel stays in plan §10's deferred list.\n2. **The schema files are a follow-on item** (above). Until they exist, Codex validates against `docs/architecture/verifier-contract.md` and the two corpus registrations (DP-5.4).\n3. **This PR edits `.rha/policy.toml`, so `Applicable` is false for its records** (the candidate policy digest differs from the base; `evidence/CHG-019/20260922T233629Z-5a768afbe364.json`). That is the model working as specified: the edit is judged under the policy it replaces. Its acceptance is a controlled transition in the §11.5 sense, the first one this repository has had. The acceptance record says so and names the cause.\n4. **The verifier contract lags the enforced fixture shape** (re-reviews above): non-empty `surface`, required `exception` key, entry `kind` and `params`, unsigned `cooling_off_hours`, a whole test count. They fail closed and change no registered decision. They are registered in the schema-contract follow-on through the amendment procedure, and the lint's note for unchecked citations arrives with them.\n"
  },
  "docs/changes/PLAN-2-plan-revision.md": {
    "body": "[Heading { level: 1, slug: \"plan-2-the-implementation-plan-revision-2\", children: [Text(\"PLAN-2: the implementation plan, revision 2\")] }, Paragraph([Text(\"Task record: \"), Link { href: \"../../.rha/tasks/PLAN-2-plan-revision.toml\", children: [Code(\".rha/tasks/PLAN-2-plan-revision.toml\")] }, Text(\". Base: \"), Code(\"bb1ee4e\"), Text(\", the merge that accepted CHG-004.6.\")]), Heading { level: 2, slug: \"intent-and-scope\", children: [Text(\"Intent and scope\")] }, Paragraph([Text(\"Kennedy asked that the lessons of W0 to W4 be taken into a redeveloped plan, with larger work items that make use of a capable model's generation. This change revises \"), Link { href: \"../plan/IMPLEMENTATION-PLAN.md\", children: [Text(\"the plan\")] }, Text(\" and nothing else, apart from the CHG-004.6 acceptance record, which is its first commit.\")]), Heading { level: 2, slug: \"deltas\", children: [Text(\"Deltas\")] }, List { start: None, items: [[Strong([Text(\"Front matter and §1.\")]), Text(\" Revision 2's provenance. A packet kickoff replaces the per-item kickoff. A status table covers W0 to W4.\")], [Strong([Text(\"§2, rewritten.\")]), Text(\" §2.1 lists twelve measured lessons, each tied to a rule. §2.2 gives the operating method M1 to M10: packets built in green stages; one writer per branch; a scope and layout guard; one L0 record per packet and no PR-head CI records; one local review with at most three rounds; registration before data; computed provenance; shell hygiene. §2.3 explains why items are larger now. §2.4 is the stochastic-generation protocol: generate wide and verify narrow; fix the grader before the generator runs; best-of-N with the losers kept; differential oracles; adversarial corpora at scale; bounds.\")], [Strong([Text(\"§8.1, new.\")]), Text(\" Six packets: P-A product and M1 close; P-B trust, the verifier moved from last to second; P-C module level; P-D growth under measurement; P-E efficiency; P-F model and close. Each has stages and gates. \"), Strong([Text(\"§8.2\")]), Text(\" keeps revision 1's briefs verbatim as the acceptance reference.\")], [Strong([Text(\"§9.\")]), Text(\" The catalogue is updated to packet triggers, and DP-5.1 to 5.3 are new. \"), Strong([Text(\"§11\")]), Text(\" is rewritten; revision 1's risks remain in force. \"), Strong([Text(\"§12\")]), Text(\": the W4 expectation now reflects what was observed.\")], [Strong([Text(\"Unchanged:\")]), Text(\" §3 to §7 and §10, and every CHG id.\")]] }, Heading { level: 2, slug: \"evidence\", children: [Text(\"Evidence\")] }, Paragraph([Text(\"The measurements behind §2.1 were taken on \"), Code(\"main\"), Text(\" at \"), Code(\"bb1ee4e\"), Text(\": 132 non-merge commits, 46 of them records-only, in 8 pull requests; 60 CI runs. The L0 record for this change is under \"), Code(\"evidence/PLAN-2/\"), Text(\".\")]), Heading { level: 3, slug: \"review-local-codex-gpt-56-sol-medium-read-only-round-1-on-b85e5c0-determinations\", children: [Text(\"Review (local Codex, gpt-5.6-sol, medium, read-only), round 1 on \"), Code(\"b85e5c0\"), Text(\", determinations\")] }, List { start: Some(1), items: [[Strong([Text(\"P1, \\\"the executor must use JJ, a separate workspace and the deep verify tier.\\\" Declined.\")]), Text(\" These rules come from the reviewer's own global instructions, not this repository. The repository's AGENTS.md and CONTRIBUTING.md say none of it, and Kennedy told the Codex executor to use Git here.\")], [Strong([Text(\"P2, the record-only count is not reproducible. Confirmed.\")]), Text(\" The count depends on which paths count as records. §2.1 now states the definition: 46 of 132 counting evidence, generated indexes and change records, or 61 also counting task and acceptance records. It also gives the recount method.\")], [Strong([Text(\"P1, §6 still ran held-out cases through the bare checker. Confirmed.\")]), Text(\" §6 now names \"), Code(\"cargo xtask corpus held-out\"), Text(\", which verifies the commitment and prints only opaque output.\")], [Strong([Text(\"P1, DP-1.2 had three triggers. Confirmed.\")]), Text(\" It is now one gate at the start of P-A stage 3, in the table, the stage and §9.\")], [Strong([Text(\"P2, DP-2.1 was gated only in P-C although P-D's mutation stage needs it. Confirmed.\")]), Text(\" P-D stage 3 now waits for it if P-C has not recorded it.\")], [Strong([Text(\"P2, graded corpora were generated after the code they grade. Confirmed; it contradicted §2.4 rule 2.\")]), Text(\" P-A registers the markdown corpus in stage 1, before any product code. P-B registers its lint and verifier corpora in stage 1, before any lint code. P-C generates its random graphs, and a separate session writes the reference extractor, before the real extractor exists.\")]] }, Heading { level: 3, slug: \"review-round-2-on-43539c1-determinations\", children: [Text(\"Review round 2 on \"), Code(\"43539c1\"), Text(\", determinations\")] }, Paragraph([Text(\"Findings 3, 4 and 5 were confirmed resolved, and the reviewer confirmed that this repository's files do not require the rules declined in round 1.\")]), List { start: None, items: [[Strong([Text(\"Finding 2, not resolved: the recount still lacked exact path filters. Confirmed.\")]), Text(\" §2.1 now lists every path in the set and the exact recount commands.\")], [Strong([Text(\"Finding 6, not resolved: the P-C reference extractor was not fixed before the real extractor existed. Confirmed.\")]), Text(\" It is now written and committed, with its expected outputs on the seeded graphs, before stage 1.\")], [Strong([Text(\"New P1: the task record's plan digest was stale. Confirmed.\")]), Text(\" It named the plan at \"), Code(\"b85e5c0\"), Text(\", the evidence record \"), Code(\"20260922T201134Z-87d53f35c7ac.json\"), Text(\" copied it, and the plan had since changed. This is lesson L6 recurring inside the change that records it. The digest is now set in the same commit as the final plan text. The earlier evidence record is kept as observed. P-B's lint is the lasting remedy.\")]] }, Heading { level: 2, slug: \"acceptance-concerns\", children: [Text(\"Acceptance concerns\")] }, List { start: Some(1), items: [[Strong([Text(\"Merging this approves revision 2\")]), Text(\" (DP-5.1): the packets, the method, and the generation protocol.\")], [Strong([Text(\"The verifier moves second.\")]), Text(\" Decline this at merge if you prefer the original order; §8.1's table is the only place it lives.\")], [Strong([Text(\"Larger PRs mean more to read per merge.\")]), Text(\" The stages, the gates and one review index per packet are the mitigation. If a packet's diff is too large to review, split it at a stage boundary.\")]] }]",
    "headings": "[Heading { level: 1, text: \"PLAN-2: the implementation plan, revision 2\", slug: \"plan-2-the-implementation-plan-revision-2\", base_slug: \"plan-2-the-implementation-plan-revision-2\", line: 1 }, Heading { level: 2, text: \"Intent and scope\", slug: \"intent-and-scope\", base_slug: \"intent-and-scope\", line: 5 }, Heading { level: 2, text: \"Deltas\", slug: \"deltas\", base_slug: \"deltas\", line: 9 }, Heading { level: 2, text: \"Evidence\", slug: \"evidence\", base_slug: \"evidence\", line: 17 }, Heading { level: 3, text: \"Review (local Codex, gpt-5.6-sol, medium, read-only), round 1 on b85e5c0, determinations\", slug: \"review-local-codex-gpt-56-sol-medium-read-only-round-1-on-b85e5c0-determinations\", base_slug: \"review-local-codex-gpt-56-sol-medium-read-only-round-1-on-b85e5c0-determinations\", line: 21 }, Heading { level: 3, text: \"Review round 2 on 43539c1, determinations\", slug: \"review-round-2-on-43539c1-determinations\", base_slug: \"review-round-2-on-43539c1-determinations\", line: 30 }, Heading { level: 2, text: \"Acceptance concerns\", slug: \"acceptance-concerns\", base_slug: \"acceptance-concerns\", line: 38 }]",
    "markdown": "# PLAN-2: the implementation plan, revision 2\n\nTask record: [`.rha/tasks/PLAN-2-plan-revision.toml`](../../.rha/tasks/PLAN-2-plan-revision.toml). Base: `bb1ee4e`, the merge that accepted CHG-004.6.\n\n## Intent and scope\n\nKennedy asked that the lessons of W0 to W4 be taken into a redeveloped plan, with larger work items that make use of a capable model's generation. This change revises [the plan](../plan/IMPLEMENTATION-PLAN.md) and nothing else, apart from the CHG-004.6 acceptance record, which is its first commit.\n\n## Deltas\n\n- **Front matter and §1.** Revision 2's provenance. A packet kickoff replaces the per-item kickoff. A status table covers W0 to W4.\n- **§2, rewritten.** §2.1 lists twelve measured lessons, each tied to a rule. §2.2 gives the operating method M1 to M10: packets built in green stages; one writer per branch; a scope and layout guard; one L0 record per packet and no PR-head CI records; one local review with at most three rounds; registration before data; computed provenance; shell hygiene. §2.3 explains why items are larger now. §2.4 is the stochastic-generation protocol: generate wide and verify narrow; fix the grader before the generator runs; best-of-N with the losers kept; differential oracles; adversarial corpora at scale; bounds.\n- **§8.1, new.** Six packets: P-A product and M1 close; P-B trust, the verifier moved from last to second; P-C module level; P-D growth under measurement; P-E efficiency; P-F model and close. Each has stages and gates. **§8.2** keeps revision 1's briefs verbatim as the acceptance reference.\n- **§9.** The catalogue is updated to packet triggers, and DP-5.1 to 5.3 are new. **§11** is rewritten; revision 1's risks remain in force. **§12**: the W4 expectation now reflects what was observed.\n- **Unchanged:** §3 to §7 and §10, and every CHG id.\n\n## Evidence\n\nThe measurements behind §2.1 were taken on `main` at `bb1ee4e`: 132 non-merge commits, 46 of them records-only, in 8 pull requests; 60 CI runs. The L0 record for this change is under `evidence/PLAN-2/`.\n\n### Review (local Codex, gpt-5.6-sol, medium, read-only), round 1 on `b85e5c0`, determinations\n\n1. **P1, \"the executor must use JJ, a separate workspace and the deep verify tier.\" Declined.** These rules come from the reviewer's own global instructions, not this repository. The repository's AGENTS.md and CONTRIBUTING.md say none of it, and Kennedy told the Codex executor to use Git here.\n2. **P2, the record-only count is not reproducible. Confirmed.** The count depends on which paths count as records. §2.1 now states the definition: 46 of 132 counting evidence, generated indexes and change records, or 61 also counting task and acceptance records. It also gives the recount method.\n3. **P1, §6 still ran held-out cases through the bare checker. Confirmed.** §6 now names `cargo xtask corpus held-out`, which verifies the commitment and prints only opaque output.\n4. **P1, DP-1.2 had three triggers. Confirmed.** It is now one gate at the start of P-A stage 3, in the table, the stage and §9.\n5. **P2, DP-2.1 was gated only in P-C although P-D's mutation stage needs it. Confirmed.** P-D stage 3 now waits for it if P-C has not recorded it.\n6. **P2, graded corpora were generated after the code they grade. Confirmed; it contradicted §2.4 rule 2.** P-A registers the markdown corpus in stage 1, before any product code. P-B registers its lint and verifier corpora in stage 1, before any lint code. P-C generates its random graphs, and a separate session writes the reference extractor, before the real extractor exists.\n\n### Review round 2 on `43539c1`, determinations\n\nFindings 3, 4 and 5 were confirmed resolved, and the reviewer confirmed that this repository's files do not require the rules declined in round 1.\n\n- **Finding 2, not resolved: the recount still lacked exact path filters. Confirmed.** §2.1 now lists every path in the set and the exact recount commands.\n- **Finding 6, not resolved: the P-C reference extractor was not fixed before the real extractor existed. Confirmed.** It is now written and committed, with its expected outputs on the seeded graphs, before stage 1.\n- **New P1: the task record's plan digest was stale. Confirmed.** It named the plan at `b85e5c0`, the evidence record `20260922T201134Z-87d53f35c7ac.json` copied it, and the plan had since changed. This is lesson L6 recurring inside the change that records it. The digest is now set in the same commit as the final plan text. The earlier evidence record is kept as observed. P-B's lint is the lasting remedy.\n\n## Acceptance concerns\n\n1. **Merging this approves revision 2** (DP-5.1): the packets, the method, and the generation protocol.\n2. **The verifier moves second.** Decline this at merge if you prefer the original order; §8.1's table is the only place it lives.\n3. **Larger PRs mean more to read per merge.** The stages, the gates and one review index per packet are the mitigation. If a packet's diff is too large to review, split it at a stage boundary.\n"
  },
  "docs/changes/PLAN-3-reviewer-alignment.md": {
    "body": "[Heading { level: 1, slug: \"plan-3-align-the-kickoff-prompt-and-m6-with-the-ledger\", children: [Text(\"PLAN-3: align the kickoff prompt and M6 with the ledger\")] }, Paragraph([Strong([Text(\"Intent.\")]), Text(\" A prompt audit on 2026-09-23 (target model Claude Opus 5.5) found the plan's packet kickoff prompt and its method rule M6 restating reviewer decisions that the ledger has since changed:\")]), List { start: None, items: [[Text(\"Codex's role: a validator, not a generator (DP-5.4).\")], [Text(\"The reviewer: \"), Code(\"gpt-6-sol\"), Text(\" at medium, with \"), Code(\"gpt-6-astra\"), Text(\" only after asking (DP-5.4 amendment).\")], [Text(\"The merge condition: an Opus 5.5 and a GPT-6 approval of the same head.\")], [Text(\"The gate rule: decide under Kennedy's delegation of 2026-09-22.\")]] }, Paragraph([Text(\"An executor that followed the plan literally would use the wrong reviewer, role and effort, and escalate what it was told to decide. DP-5.4 says the plan text follows at its next revision; this is that revision.\")]), Paragraph([Strong([Text(\"Deltas\")]), Text(\" (\"), Code(\"docs/plan/IMPLEMENTATION-PLAN.md\"), Text(\" only):\")]), Table { align: [None, None, None, None], head: [[Text(\"Line\")], [Text(\"Finding\")], [Text(\"Confidence\")], [Text(\"Change\")]], rows: [[[Text(\"7\")], [Text(\"F1\")], [Text(\"high\")], [Text(\"Reviewers are now named by reference to DP-5.4, not pinned.\")]], [[Text(\"7\")], [Text(\"F3\")], [Text(\"medium\")], [Text(\"Effort is left to the session configuration instead of \\\"largest … available\\\".\")]], [[Text(\"14\")], [Text(\"F4\")], [Text(\"medium\")], [Text(\"Gates are decided under the delegation; held-out material and Astra stay Kennedy's.\")]], [[Text(\"16\")], [Text(\"F5\")], [Text(\"medium\")], [Text(\"Two approving reviews of the same head.\")]], [[Text(\"67\")], [Text(\"F2\")], [Text(\"high\")], [Text(\"M6 matches DP-5.4 and the merge rule.\")]]] }, Paragraph([Strong([Text(\"Not changed.\")])]), List { start: None, items: [[Text(\"The frozen plan copies inside the registered corpus inputs; their digests are evidence.\")], [Text(\"F6 (\"), Code(\"crates/site/AGENTS.md:7\"), Text(\", an illustrative list of extractor holes), a low-confidence flag.\")]] }, Paragraph([Strong([Text(\"Checks.\")]), Text(\" No test or tool reads the plan's bytes. The only reference is a registration's historical digest, which remains correct as history. \"), Code(\"cargo xtask scope\"), Text(\" and the L0 lane are in \"), Code(\"evidence/PLAN-3/\"), Text(\".\")]), Paragraph([Strong([Text(\"Acceptance concerns.\")])]), List { start: None, items: [[Text(\"The GPT-6 half of the review cannot run until the Codex quota resets on 2026-09-29.\")], [Text(\"The acceptance record owed for CHG-007 (PR 18) is assigned to P-D by its resume point, not written here.\")]] }]",
    "headings": "[Heading { level: 1, text: \"PLAN-3: align the kickoff prompt and M6 with the ledger\", slug: \"plan-3-align-the-kickoff-prompt-and-m6-with-the-ledger\", base_slug: \"plan-3-align-the-kickoff-prompt-and-m6-with-the-ledger\", line: 1 }]",
    "markdown": "# PLAN-3: align the kickoff prompt and M6 with the ledger\n\n**Intent.** A prompt audit on 2026-09-23 (target model Claude Opus 5.5) found the plan's packet kickoff prompt and its method rule M6 restating reviewer decisions that the ledger has since changed:\n- Codex's role: a validator, not a generator (DP-5.4).\n- The reviewer: `gpt-6-sol` at medium, with `gpt-6-astra` only after asking (DP-5.4 amendment).\n- The merge condition: an Opus 5.5 and a GPT-6 approval of the same head.\n- The gate rule: decide under Kennedy's delegation of 2026-09-22.\n\nAn executor that followed the plan literally would use the wrong reviewer, role and effort, and escalate what it was told to decide. DP-5.4 says the plan text follows at its next revision; this is that revision.\n\n**Deltas** (`docs/plan/IMPLEMENTATION-PLAN.md` only):\n\n| Line | Finding | Confidence | Change |\n|---|---|---|---|\n| 7 | F1 | high | Reviewers are now named by reference to DP-5.4, not pinned. |\n| 7 | F3 | medium | Effort is left to the session configuration instead of \"largest … available\". |\n| 14 | F4 | medium | Gates are decided under the delegation; held-out material and Astra stay Kennedy's. |\n| 16 | F5 | medium | Two approving reviews of the same head. |\n| 67 | F2 | high | M6 matches DP-5.4 and the merge rule. |\n\n**Not changed.**\n- The frozen plan copies inside the registered corpus inputs; their digests are evidence.\n- F6 (`crates/site/AGENTS.md:7`, an illustrative list of extractor holes), a low-confidence flag.\n\n**Checks.** No test or tool reads the plan's bytes. The only reference is a registration's historical digest, which remains correct as history. `cargo xtask scope` and the L0 lane are in `evidence/PLAN-3/`.\n\n**Acceptance concerns.**\n- The GPT-6 half of the review cannot run until the Codex quota resets on 2026-09-29.\n- The acceptance record owed for CHG-007 (PR 18) is assigned to P-D by its resume point, not written here.\n"
  },
  "docs/conformance/self-assessment.md": {
    "body": "[Heading { level: 1, slug: \"rha-core-self-assessment-spec-15-at-the-close-of-m1\", children: [Text(\"RHA-Core self-assessment (spec §15), at the close of M1\")] }, Paragraph([Text(\"A recorded pass through the §15 checklist, with gaps named, is what an RHA-Core conformance claim consists of (§1.4). This pass covers \"), Code(\"rhawiki\"), Text(\" and its tooling at the P-A packet (CHG-005, CHG-006). The answers are \"), Strong([Text(\"yes\")]), Text(\", \"), Strong([Text(\"partial\")]), Text(\", \"), Strong([Text(\"no\")]), Text(\", or \"), Strong([Text(\"n/a\")]), Text(\", each with where to look.\")]), Heading { level: 2, slug: \"scope-and-structure\", children: [Text(\"Scope and structure\")] }, Table { align: [None, None, None], head: [[Text(\"Item\")], [Text(\"Answer\")], [Text(\"Where\")]], rows: [[[Text(\"Target class, non-target parts named\")], [Text(\"yes\")], [Text(\"plan §3: CLI with a separable decision core; HTML templates (\"), Code(\"adapter-html\"), Text(\") are the named non-target part\")]], [[Text(\"Each component states what it owns\")], [Text(\"yes\")], [Text(\"BDR-0001 to BDR-0004; crate docs\")]], [[Text(\"Outsiders use components without internals\")], [Text(\"yes\")], [Text(\"facades at crate roots; \"), Code(\"site\"), Text(\" re-exports what a renderer needs\")]], [[Text(\"Port owner and polarity named\")], [Text(\"yes\")], [Text(\"plan §3.1; \"), Code(\"implements\"), Text(\" metadata on each adapter\")]], [[Text(\"Composite children bound or exported; no adapter below the root\")], [Text(\"yes\")], [Code(\"site\"), Text(\": \"), Code(\"Assemble\"), Text(\" bound to its sibling, \"), Code(\"PageRenderer\"), Text(\" exported; adapters only in \"), Code(\"app-cli\")]], [[Text(\"Sibling graph acyclic, no child depends on its parent\")], [Text(\"partial\")], [Text(\"crate level checked (\"), Code(\"graph.cycle\"), Text(\"); inside \"), Code(\"site\"), Text(\", \"), Code(\"rha-modules.toml\"), Text(\" is declared but enforced by review until P-C (BDR-0003)\")]], [[Text(\"External effects explicit at ports\")], [Text(\"yes\")], [Code(\"SourceRepository\"), Text(\", \"), Code(\"OutputSink\"), Text(\", \"), Code(\"Clock\")]], [[Text(\"Ambient effects kept out of cores by a check\")], [Text(\"yes\")], [Text(\"the 111-entry deny list in every core's \"), Code(\"clippy.toml\"), Text(\", the crate-root \"), Code(\"forbid\"), Text(\", and \"), Code(\"effect.core_clippy_template\"), Text(\" in L0\")]], [[Text(\"Controller updates from observations (Law 15)\")], [Text(\"partial\")], [Code(\"build::step\"), Text(\" diffs against the sink's listing; the served reconciler and its traces are W14 (P-E)\")]], [[Text(\"Ports semantic, not vendor-named\")], [Text(\"yes\")], [Text(\"no pulldown-cmark type in any public API (BDR-0002 a)\")]], [[Text(\"Forbidden patterns executable in CI, enforcement map honest\")], [Text(\"yes\")], [Code(\"rha-crates.toml\"), Text(\" forbidden edges; \"), Code(\"docs/enforcement-map.md\"), Text(\" generated from H4 evidence\")]], [[Text(\"Simple when one component suffices\")], [Text(\"yes\")], [Text(\"no framework; four core crates\")]], [[Text(\"Experiments isolated\")], [Text(\"yes\")], [Code(\"experiments/\"), Text(\" (E1 is in P-C)\")]]] }, Heading { level: 2, slug: \"boundaries\", children: [Text(\"Boundaries\")] }, Table { align: [None, None, None], head: [[Text(\"Item\")], [Text(\"Answer\")], [Text(\"Where\")]], rows: [[[Text(\"Decision records with refutation criteria written before the boundary\")], [Text(\"yes\")], [Text(\"BDR-0001 to 0004, accepted at DP-1.3 before any product crate existed\")]], [[Text(\"Co-change statistics with §7.3 filters\")], [Text(\"no\")], [Text(\"no history yet; the first measurement is P-D's per-stage change spread\")]]] }, Heading { level: 2, slug: \"functional-assurance\", children: [Text(\"Functional assurance\")] }, Table { align: [None, None, None], head: [[Text(\"Item\")], [Text(\"Answer\")], [Text(\"Where\")]], rows: [[[Text(\"Important invariants have property tests\")], [Text(\"yes\")], [Text(\"plan §3.2 table: \"), Code(\"library\"), Text(\", \"), Code(\"document\"), Text(\", \"), Code(\"graph\"), Text(\", \"), Code(\"site\"), Text(\", \"), Code(\"adapter-html\"), Text(\" tests\")]], [[Text(\"Adapters share a contract suite\")], [Text(\"yes\")], [Code(\"library::contract\"), Text(\", \"), Code(\"site::contract\"), Text(\", run by every implementation and by seeded violators\")]], [[Text(\"Boundary transformations property-tested\")], [Text(\"yes\")], [Text(\"parser totality over arbitrary strings; escaping round trip\")]], [[Text(\"Mutation analysis\")], [Text(\"no\")], [Text(\"P-D stage 3 (CHG-014)\")]], [[Text(\"Mutation read as discrimination evidence\")], [Text(\"n/a\")], [Text(\"–\")]], [[Text(\"Composition tests\")], [Text(\"yes\")], [Code(\"crates/app-cli/tests/cli.rs\"), Text(\": the 60-site corpus through the binary; the spec build\")]], [[Text(\"Compact witnesses checkable independently\")], [Text(\"yes\")], [Text(\"§3.2 witnesses; the corpus grades them by construction\")]], [[Text(\"t-way coverage\")], [Text(\"no\")], [Text(\"not yet justified by a configuration space\")]], [[Text(\"Boundary values concentrated at limits\")], [Text(\"partial\")], [Text(\"the adversarial markdown corpus targets Unicode, case and anchor edges\")]], [[Text(\"Release-critical invariants tied to claim, evidence and owner\")], [Text(\"partial\")], [Text(\"the ledger and this table; no release record yet\")]], [[Text(\"Component assumptions recorded, undischarged ones listed\")], [Text(\"yes\")], [Code(\".rha/assumptions.toml\"), Text(\": seven current, two undischarged at the composition root\")]], [[Text(\"Held-out acceptance check for agent-authored consequential changes\")], [Text(\"partial\")], [Text(\"crate level: DP-1.1b, not yet run. Markdown: DP-1.4, fixtures pending; \"), Code(\"corpus held-out --kind check\"), Text(\" is ready\")]]] }, Heading { level: 2, slug: \"efficiency\", children: [Text(\"Efficiency\")] }, Paragraph([Text(\"All five items are \"), Strong([Text(\"no\")]), Text(\" at M1. P-E (DP-3.1, \"), Code(\"bench\"), Text(\", \"), Code(\"compare\"), Text(\", counts) is where they are answered.\")]), Heading { level: 2, slug: \"rust-profile\", children: [Text(\"Rust profile\")] }, Table { align: [None, None, None], head: [[Text(\"Item\")], [Text(\"Answer\")], [Text(\"Where\")]], rows: [[[Text(\"Invalid values prevented by types\")], [Text(\"yes\")], [Code(\"RelPath\"), Text(\", \"), Code(\"PageId\"), Text(\" constructors\")]], [[Text(\"Deliberate facades\")], [Text(\"yes\")], [Text(\"crate roots\")]], [[Text(\"Ports owned by their core, free of vendor types\")], [Text(\"yes\")], [Text(\"as above\")]], [[Text(\"Async confined\")], [Text(\"yes (n/a)\")], [Text(\"fully synchronous\")]], [[Code(\"unsafe\"), Text(\" forbidden in core and app crates\")], [Text(\"yes\")], [Text(\"workspace lint \"), Code(\"unsafe_code = \\\"deny\\\"\")]], [[Text(\"Compile-fail tests for costly guarantees\")], [Text(\"partial\")], [Text(\"the deny-list corpus (CHG-001) is compile-fail over fixtures; product crates have none yet\")]], [[Text(\"Crate graph enforces direction, including \\\"only roots depend on adapters\\\"\")], [Text(\"yes\")], [Code(\"cargo xtask architecture\"), Text(\", H4 crate level 22/22 (V proposed, DP-1.6)\")]], [[Text(\"Composite declares children and allowed edges, checked\")], [Text(\"partial\")], [Text(\"declared in \"), Code(\"crates/site/rha-modules.toml\"), Text(\"; checked from P-C\")]], [[Text(\"Specialized tools routed by risk\")], [Text(\"partial\")], [Text(\"mutation and bench arrive with their packets\")]], [[Text(\"Assurance tools separate from runtime dependencies\")], [Text(\"yes\")], [Text(\"xtask is a \"), Code(\"tool\"), Text(\"; \"), Code(\"dir.tool_depended_on\"), Text(\" and machete\")]], [[Text(\"Every PR runs L0\")], [Text(\"yes\")], [Code(\".github/workflows/ci.yml\"), Text(\", branch protection\")]], [[Text(\"Heavier tools activated by risk\")], [Text(\"partial\")], [Text(\"the L1 trigger mechanism is not implemented\")]], [[Text(\"Build flags measured before default\")], [Text(\"n/a\")], [Text(\"none adopted\")]]] }, Heading { level: 2, slug: \"work-architecture-and-coordination\", children: [Text(\"Work architecture and coordination\")] }, Table { align: [None, None, None], head: [[Text(\"Item\")], [Text(\"Answer\")], [Text(\"Where\")]], rows: [[[Text(\"Mode from the execution mechanism\")], [Text(\"yes\")], [Text(\"task records: \"), Code(\"mode = \\\"agent\\\"\"), Text(\" whatever the Git author\")]], [[Text(\"Actor, responsibility, authority recorded separately\")], [Text(\"yes\")], [Code(\"[[actors]]\"), Text(\", \"), Code(\"[roles]\"), Text(\", decisions with \"), Code(\"decided_by\")]], [[Text(\"Cross-owner handoffs with readiness conditions\")], [Text(\"yes\")], [Text(\"gates in plan §8.1; decisions quoted in task records\")]], [[Text(\"Team mode Integrator\")], [Text(\"n/a\")], [Text(\"Solo profile\")]], [[Text(\"Records no more elaborate than needed\")], [Text(\"partial\")], [Text(\"plan revision 2 cut recording churn (L1); the verifier (P-B) will lint records\")]]] }, Paragraph([Strong([Text(\"Claim.\")]), Text(\" Structure and functional assurance meet RHA-Core at crate level, with the gaps above. Efficiency is not claimed at M1. Enforcement inside \"), Code(\"site\"), Text(\" is by review until P-C. Every record remains advisory until P-B.\")])]",
    "headings": "[Heading { level: 1, text: \"RHA-Core self-assessment (spec §15), at the close of M1\", slug: \"rha-core-self-assessment-spec-15-at-the-close-of-m1\", base_slug: \"rha-core-self-assessment-spec-15-at-the-close-of-m1\", line: 1 }, Heading { level: 2, text: \"Scope and structure\", slug: \"scope-and-structure\", base_slug: \"scope-and-structure\", line: 5 }, Heading { level: 2, text: \"Boundaries\", slug: \"boundaries\", base_slug: \"boundaries\", line: 23 }, Heading { level: 2, text: \"Functional assurance\", slug: \"functional-assurance\", base_slug: \"functional-assurance\", line: 30 }, Heading { level: 2, text: \"Efficiency\", slug: \"efficiency\", base_slug: \"efficiency\", line: 47 }, Heading { level: 2, text: \"Rust profile\", slug: \"rust-profile\", base_slug: \"rust-profile\", line: 51 }, Heading { level: 2, text: \"Work architecture and coordination\", slug: \"work-architecture-and-coordination\", base_slug: \"work-architecture-and-coordination\", line: 69 }]",
    "markdown": "# RHA-Core self-assessment (spec §15), at the close of M1\n\nA recorded pass through the §15 checklist, with gaps named, is what an RHA-Core conformance claim consists of (§1.4). This pass covers `rhawiki` and its tooling at the P-A packet (CHG-005, CHG-006). The answers are **yes**, **partial**, **no**, or **n/a**, each with where to look.\n\n## Scope and structure\n\n| Item | Answer | Where |\n| --- | --- | --- |\n| Target class, non-target parts named | yes | plan §3: CLI with a separable decision core; HTML templates (`adapter-html`) are the named non-target part |\n| Each component states what it owns | yes | BDR-0001 to BDR-0004; crate docs |\n| Outsiders use components without internals | yes | facades at crate roots; `site` re-exports what a renderer needs |\n| Port owner and polarity named | yes | plan §3.1; `implements` metadata on each adapter |\n| Composite children bound or exported; no adapter below the root | yes | `site`: `Assemble` bound to its sibling, `PageRenderer` exported; adapters only in `app-cli` |\n| Sibling graph acyclic, no child depends on its parent | partial | crate level checked (`graph.cycle`); inside `site`, `rha-modules.toml` is declared but enforced by review until P-C (BDR-0003) |\n| External effects explicit at ports | yes | `SourceRepository`, `OutputSink`, `Clock` |\n| Ambient effects kept out of cores by a check | yes | the 111-entry deny list in every core's `clippy.toml`, the crate-root `forbid`, and `effect.core_clippy_template` in L0 |\n| Controller updates from observations (Law 15) | partial | `build::step` diffs against the sink's listing; the served reconciler and its traces are W14 (P-E) |\n| Ports semantic, not vendor-named | yes | no pulldown-cmark type in any public API (BDR-0002 a) |\n| Forbidden patterns executable in CI, enforcement map honest | yes | `rha-crates.toml` forbidden edges; `docs/enforcement-map.md` generated from H4 evidence |\n| Simple when one component suffices | yes | no framework; four core crates |\n| Experiments isolated | yes | `experiments/` (E1 is in P-C) |\n\n## Boundaries\n\n| Item | Answer | Where |\n| --- | --- | --- |\n| Decision records with refutation criteria written before the boundary | yes | BDR-0001 to 0004, accepted at DP-1.3 before any product crate existed |\n| Co-change statistics with §7.3 filters | no | no history yet; the first measurement is P-D's per-stage change spread |\n\n## Functional assurance\n\n| Item | Answer | Where |\n| --- | --- | --- |\n| Important invariants have property tests | yes | plan §3.2 table: `library`, `document`, `graph`, `site`, `adapter-html` tests |\n| Adapters share a contract suite | yes | `library::contract`, `site::contract`, run by every implementation and by seeded violators |\n| Boundary transformations property-tested | yes | parser totality over arbitrary strings; escaping round trip |\n| Mutation analysis | no | P-D stage 3 (CHG-014) |\n| Mutation read as discrimination evidence | n/a | – |\n| Composition tests | yes | `crates/app-cli/tests/cli.rs`: the 60-site corpus through the binary; the spec build |\n| Compact witnesses checkable independently | yes | §3.2 witnesses; the corpus grades them by construction |\n| t-way coverage | no | not yet justified by a configuration space |\n| Boundary values concentrated at limits | partial | the adversarial markdown corpus targets Unicode, case and anchor edges |\n| Release-critical invariants tied to claim, evidence and owner | partial | the ledger and this table; no release record yet |\n| Component assumptions recorded, undischarged ones listed | yes | `.rha/assumptions.toml`: seven current, two undischarged at the composition root |\n| Held-out acceptance check for agent-authored consequential changes | partial | crate level: DP-1.1b, not yet run. Markdown: DP-1.4, fixtures pending; `corpus held-out --kind check` is ready |\n\n## Efficiency\n\nAll five items are **no** at M1. P-E (DP-3.1, `bench`, `compare`, counts) is where they are answered.\n\n## Rust profile\n\n| Item | Answer | Where |\n| --- | --- | --- |\n| Invalid values prevented by types | yes | `RelPath`, `PageId` constructors |\n| Deliberate facades | yes | crate roots |\n| Ports owned by their core, free of vendor types | yes | as above |\n| Async confined | yes (n/a) | fully synchronous |\n| `unsafe` forbidden in core and app crates | yes | workspace lint `unsafe_code = \"deny\"` |\n| Compile-fail tests for costly guarantees | partial | the deny-list corpus (CHG-001) is compile-fail over fixtures; product crates have none yet |\n| Crate graph enforces direction, including \"only roots depend on adapters\" | yes | `cargo xtask architecture`, H4 crate level 22/22 (V proposed, DP-1.6) |\n| Composite declares children and allowed edges, checked | partial | declared in `crates/site/rha-modules.toml`; checked from P-C |\n| Specialized tools routed by risk | partial | mutation and bench arrive with their packets |\n| Assurance tools separate from runtime dependencies | yes | xtask is a `tool`; `dir.tool_depended_on` and machete |\n| Every PR runs L0 | yes | `.github/workflows/ci.yml`, branch protection |\n| Heavier tools activated by risk | partial | the L1 trigger mechanism is not implemented |\n| Build flags measured before default | n/a | none adopted |\n\n## Work architecture and coordination\n\n| Item | Answer | Where |\n| --- | --- | --- |\n| Mode from the execution mechanism | yes | task records: `mode = \"agent\"` whatever the Git author |\n| Actor, responsibility, authority recorded separately | yes | `[[actors]]`, `[roles]`, decisions with `decided_by` |\n| Cross-owner handoffs with readiness conditions | yes | gates in plan §8.1; decisions quoted in task records |\n| Team mode Integrator | n/a | Solo profile |\n| Records no more elaborate than needed | partial | plan revision 2 cut recording churn (L1); the verifier (P-B) will lint records |\n\n**Claim.** Structure and functional assurance meet RHA-Core at crate level, with the gaps above. Efficiency is not claimed at M1. Enforcement inside `site` is by review until P-C. Every record remains advisory until P-B.\n"
  },
  "docs/enforcement-map.md": {
    "body": "[Paragraph([Html(\"<!-- generated by cargo xtask docs from latest evidence/h4-crate and evidence/h4-module records and corpus manifest (sha256:28e68449135d6fd531f4e9873d499a4bcf9cc3bac92d2819e7f2638c61bb4b83); do not edit -->\\n\")]), Heading { level: 1, slug: \"enforcement-map\", children: [Text(\"Enforcement map\")] }, Paragraph([Text(\"Generated by \"), Code(\"cargo xtask docs\"), Text(\" from the \"), Link { href: \"../xtask/tests/corpus/manifest.toml\", children: [Text(\"public manifest\")] }, Text(\", crate evidence source \"), Link { href: \"../evidence/h4-crate/20260923T040735Z-f915daaf8dc1-dirty.json\", children: [Text(\"evidence/h4-crate/20260923T040735Z-f915daaf8dc1-dirty.json\")] }, Text(\", and module evidence source \"), Link { href: \"../evidence/h4-module/20260923T091106Z-f0d35ab1c60c.json\", children: [Text(\"evidence/h4-module/20260923T091106Z-f0d35ab1c60c.json\")] }, Text(\". A corpus result is advisory; held-out acceptance is Kennedy's (DP-1.1b), and Accepted maturity is unchanged (DP-1.6).\")]), Table { align: [None, None, None, None, None, None, None], head: [[Text(\"Cell\")], [Text(\"Claim\")], [Text(\"Crate cases\")], [Text(\"Crate status\")], [Text(\"Module cases\")], [Text(\"Module status\")], [Text(\"Documented holes\")]], rows: [[[Code(\"b4\")], [Text(\"B4: compatible contracts\")], [], [Text(\"not_run — no crate cases\")], [], [Text(\"not_run — no registered module cases\")], []], [[Code(\"classification\")], [Text(\"Not a §4.1 row: total classification (§11.2), the precondition for every row\")], [Text(\"C10, C11, C20, L11\")], [Text(\"validated on registered crate cases only\")], [], [Text(\"not_run — no registered module cases\")], []], [[Code(\"d2\")], [Text(\"D2: never upward\")], [], [Text(\"not_run — no crate cases\")], [Text(\"L-M02, M03, M04, M05, M08, M19, supplementary random graphs\")], [Text(\"corpus validated (advisory; not accepted)\")], []], [[Code(\"law2-d3\")], [Text(\"Law 2, D3: sovereignty\")], [Text(\"R01\")], [Text(\"validated on registered crate cases only\")], [Text(\"M06, M07, M10, supplementary random graphs\")], [Text(\"corpus validated (advisory; not accepted)\")], []], [[Code(\"law3-d1\")], [Text(\"Law 3, D1: direction\")], [Text(\"C01, C02, C08, C09, C12, C13, C14, C15, C16, C17, C18, L01, L09\")], [Text(\"validated on registered crate cases only\")], [Text(\"EM-M03, M02, supplementary random graphs\")], [Text(\"corpus validated (advisory; not accepted)\")], [Text(\"§4.1, Law 3/D1 row: resolving the method to its trait needs type inference, which the extractor does not perform.\")]], [[Code(\"law5\")], [Text(\"Law 5: explicit effects\")], [Text(\"C03, C04, C19, C21, EM-C01, EM-C02, L03, L04, L08, L10\")], [Text(\"validated on registered crate cases only\")], [], [Text(\"not_run — no registered module cases\")], [Text(\"§4.1, Law 5 row, Known hole: \\\"A deny list is incomplete by nature. Transitive dependencies can perform effects.\\\" The L0 mode reads \"), Code(\"cargo metadata --no-deps\"), Text(\", which shows core-a's direct edges only.; §4.1, Law 5 row: the crate level sees dependencies, not calls. Nothing in the crate graph distinguishes this core from a pure one.\")]], [[Code(\"law6-b3\")], [Text(\"Law 6, B3: no cycles\")], [Text(\"C05, L02\")], [Text(\"validated on registered crate cases only\")], [Text(\"EM-M01, EM-M02, EM-M03, L-M01, M01, M09, M11, M12, M13, M14, M15, M16, M17, M18, M20, M21, X-M01 (reference), supplementary random graphs\")], [Text(\"corpus validated (advisory; not accepted)\")], [Text(\"§4.1, Law 3/D1 and D2 rows, Known hole: \\\"Extraction is approximate for macro-generated paths.\\\" The path exists only after expansion, and the extractor reads unexpanded source.; §4.1, Law 3/D1 row, Known hole: extraction is approximate. include! is resolved by the compiler, not by the syn parse of the visible source.; §4.1, Law 3/D1 row: resolving the method to its trait needs type inference, which the extractor does not perform.\")]], [[Code(\"law6-d5\")], [Text(\"Law 6, D5: adapters only at roots\")], [Text(\"C01, C06, C07, C15, C16, C17, C18, L05, L06, L07\")], [Text(\"validated on registered crate cases only\")], [], [Text(\"not_run — no registered module cases\")], []]] }, Paragraph([Text(\"Module reference observations, including X-M01, are displayed as \"), Code(\"(reference)\"), Text(\" and are not validation evidence or an accuracy claim.\")])]",
    "headings": "[Heading { level: 1, text: \"Enforcement map\", slug: \"enforcement-map\", base_slug: \"enforcement-map\", line: 2 }]",
    "markdown": "<!-- generated by cargo xtask docs from latest evidence/h4-crate and evidence/h4-module records and corpus manifest (sha256:28e68449135d6fd531f4e9873d499a4bcf9cc3bac92d2819e7f2638c61bb4b83); do not edit -->\n# Enforcement map\n\nGenerated by `cargo xtask docs` from the [public manifest](../xtask/tests/corpus/manifest.toml), crate evidence source [evidence/h4-crate/20260923T040735Z-f915daaf8dc1-dirty.json](../evidence/h4-crate/20260923T040735Z-f915daaf8dc1-dirty.json), and module evidence source [evidence/h4-module/20260923T091106Z-f0d35ab1c60c.json](../evidence/h4-module/20260923T091106Z-f0d35ab1c60c.json). A corpus result is advisory; held-out acceptance is Kennedy's (DP-1.1b), and Accepted maturity is unchanged (DP-1.6).\n\n| Cell | Claim | Crate cases | Crate status | Module cases | Module status | Documented holes |\n| --- | --- | --- | --- | --- | --- | --- |\n| `b4` | B4: compatible contracts |  | not_run — no crate cases |  | not_run — no registered module cases |  |\n| `classification` | Not a §4.1 row: total classification (§11.2), the precondition for every row | C10, C11, C20, L11 | validated on registered crate cases only |  | not_run — no registered module cases |  |\n| `d2` | D2: never upward |  | not_run — no crate cases | L-M02, M03, M04, M05, M08, M19, supplementary random graphs | corpus validated (advisory; not accepted) |  |\n| `law2-d3` | Law 2, D3: sovereignty | R01 | validated on registered crate cases only | M06, M07, M10, supplementary random graphs | corpus validated (advisory; not accepted) |  |\n| `law3-d1` | Law 3, D1: direction | C01, C02, C08, C09, C12, C13, C14, C15, C16, C17, C18, L01, L09 | validated on registered crate cases only | EM-M03, M02, supplementary random graphs | corpus validated (advisory; not accepted) | §4.1, Law 3/D1 row: resolving the method to its trait needs type inference, which the extractor does not perform. |\n| `law5` | Law 5: explicit effects | C03, C04, C19, C21, EM-C01, EM-C02, L03, L04, L08, L10 | validated on registered crate cases only |  | not_run — no registered module cases | §4.1, Law 5 row, Known hole: \"A deny list is incomplete by nature. Transitive dependencies can perform effects.\" The L0 mode reads `cargo metadata --no-deps`, which shows core-a's direct edges only.; §4.1, Law 5 row: the crate level sees dependencies, not calls. Nothing in the crate graph distinguishes this core from a pure one. |\n| `law6-b3` | Law 6, B3: no cycles | C05, L02 | validated on registered crate cases only | EM-M01, EM-M02, EM-M03, L-M01, M01, M09, M11, M12, M13, M14, M15, M16, M17, M18, M20, M21, X-M01 (reference), supplementary random graphs | corpus validated (advisory; not accepted) | §4.1, Law 3/D1 and D2 rows, Known hole: \"Extraction is approximate for macro-generated paths.\" The path exists only after expansion, and the extractor reads unexpanded source.; §4.1, Law 3/D1 row, Known hole: extraction is approximate. include! is resolved by the compiler, not by the syn parse of the visible source.; §4.1, Law 3/D1 row: resolving the method to its trait needs type inference, which the extractor does not perform. |\n| `law6-d5` | Law 6, D5: adapters only at roots | C01, C06, C07, C15, C16, C17, C18, L05, L06, L07 | validated on registered crate cases only |  | not_run — no registered module cases |  |\n\nModule reference observations, including X-M01, are displayed as `(reference)` and are not validation evidence or an accuracy claim.\n"
  },
  "docs/evidence/index.md": {
    "body": "[Paragraph([Html(\"<!-- generated by cargo xtask docs from evidence/**/*.json (sha256:251668ab9ee75ec3f7f5f3028854214a3ec0ce26461ca707bccc0231bdf190d9); do not edit -->\\n\")]), Heading { level: 1, slug: \"evidence-records\", children: [Text(\"Evidence records\")] }, Paragraph([Text(\"Every record is advisory: class \"), Code(\"local\"), Text(\" or \"), Code(\"ci\"), Text(\", produced by candidate code and not authenticated (see \"), Link { href: \"../threat-model.md\", children: [Text(\"the threat model\")] }, Text(\"). Eligibility \"), Code(\"blocked\"), Text(\" lists its reasons per acceptance predicate inside the record.\")]), Table { align: [None, None, None, None, None, None, None, None], head: [[Text(\"Record\")], [Text(\"Lane\")], [Text(\"Class\")], [Text(\"Task\")], [Text(\"Revision\")], [Text(\"Dirty\")], [Text(\"Outcomes\")], [Text(\"Eligibility\")]], rows: [[[Link { href: \"../../evidence/CHG-000/20260919T205530Z-c48f1bd1f44d.json\", children: [Text(\"CHG-000/20260919T205530Z-c48f1bd1f44d.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-000\")], [Code(\"c48f1bd1f44d\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-001/20260919T212810Z-42c4860b5553-dirty.json\", children: [Text(\"CHG-001/20260919T212810Z-42c4860b5553-dirty.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-001\")], [Code(\"42c4860b5553\")], [Text(\"yes\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-001/20260919T222005Z-bcaf3090691d.json\", children: [Text(\"CHG-001/20260919T222005Z-bcaf3090691d.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-001\")], [Code(\"bcaf3090691d\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-001/20260919T224504Z-bae4b34894a2.json\", children: [Text(\"CHG-001/20260919T224504Z-bae4b34894a2.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-001\")], [Code(\"bae4b34894a2\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-001/20260919T231351Z-602d2f953d0b.json\", children: [Text(\"CHG-001/20260919T231351Z-602d2f953d0b.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-001\")], [Code(\"602d2f953d0b\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-001/20260919T232940Z-78fe0093c4a4.json\", children: [Text(\"CHG-001/20260919T232940Z-78fe0093c4a4.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-001\")], [Code(\"78fe0093c4a4\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-001/20260920T000645Z-da317050b06f.json\", children: [Text(\"CHG-001/20260920T000645Z-da317050b06f.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-001\")], [Code(\"da317050b06f\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-002/20260920T001651Z-24ad1602720b.json\", children: [Text(\"CHG-002/20260920T001651Z-24ad1602720b.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-002\")], [Code(\"24ad1602720b\")], [Text(\"no\")], [Text(\"1 failed, 6 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-002/20260920T001738Z-038a8ea2ba1c.json\", children: [Text(\"CHG-002/20260920T001738Z-038a8ea2ba1c.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-002\")], [Code(\"038a8ea2ba1c\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-002/20260920T002204Z-13f665cb0893.json\", children: [Text(\"CHG-002/20260920T002204Z-13f665cb0893.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-002\")], [Code(\"13f665cb0893\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-002/20260920T010039Z-cfb048e479fb.json\", children: [Text(\"CHG-002/20260920T010039Z-cfb048e479fb.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-002\")], [Code(\"cfb048e479fb\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-002/20260920T011724Z-6518f6a89af5.json\", children: [Text(\"CHG-002/20260920T011724Z-6518f6a89af5.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-002\")], [Code(\"6518f6a89af5\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-002.2/20260920T020130Z-15d916a16e23-dirty.json\", children: [Text(\"CHG-002.2/20260920T020130Z-15d916a16e23-dirty.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-002.2\")], [Code(\"15d916a16e23\")], [Text(\"yes\")], [Text(\"6 passed, 1 failed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-002.2/20260920T020203Z-15d916a16e23-dirty.json\", children: [Text(\"CHG-002.2/20260920T020203Z-15d916a16e23-dirty.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-002.2\")], [Code(\"15d916a16e23\")], [Text(\"yes\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-002.2/20260920T020252Z-1d40eaea5b4c.json\", children: [Text(\"CHG-002.2/20260920T020252Z-1d40eaea5b4c.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-002.2\")], [Code(\"1d40eaea5b4c\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-002.2/20260920T021545Z-8a736484fe6a.json\", children: [Text(\"CHG-002.2/20260920T021545Z-8a736484fe6a.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-002.2\")], [Code(\"8a736484fe6a\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-002.2/20260920T090804Z-b2c5ee1e8111.json\", children: [Text(\"CHG-002.2/20260920T090804Z-b2c5ee1e8111.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-002.2\")], [Code(\"b2c5ee1e8111\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-003/20260920T093725Z-53563857be6c.json\", children: [Text(\"CHG-003/20260920T093725Z-53563857be6c.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-003\")], [Code(\"53563857be6c\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-003/20260920T100013Z-1cbc47ec97f2.json\", children: [Text(\"CHG-003/20260920T100013Z-1cbc47ec97f2.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-003\")], [Code(\"1cbc47ec97f2\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-003/20260920T103310Z-4e337339c121-dirty.json\", children: [Text(\"CHG-003/20260920T103310Z-4e337339c121-dirty.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-003\")], [Code(\"4e337339c121\")], [Text(\"yes\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-003/20260920T110910Z-920cca6b07b7-dirty.json\", children: [Text(\"CHG-003/20260920T110910Z-920cca6b07b7-dirty.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-003\")], [Code(\"920cca6b07b7\")], [Text(\"yes\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-003/20260920T111037Z-956707e0b1da.json\", children: [Text(\"CHG-003/20260920T111037Z-956707e0b1da.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-003\")], [Code(\"956707e0b1da\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-003/20260920T111702Z-a925cfcda441.json\", children: [Text(\"CHG-003/20260920T111702Z-a925cfcda441.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-003\")], [Code(\"a925cfcda441\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-004/20260920T115809Z-762ab470f54b.json\", children: [Text(\"CHG-004/20260920T115809Z-762ab470f54b.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-004\")], [Code(\"762ab470f54b\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-004/20260920T120146Z-540ce6a70c43.json\", children: [Text(\"CHG-004/20260920T120146Z-540ce6a70c43.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-004\")], [Code(\"540ce6a70c43\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-004/20260920T122054Z-bef220d57566.json\", children: [Text(\"CHG-004/20260920T122054Z-bef220d57566.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-004\")], [Code(\"bef220d57566\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-004/20260920T122732Z-885a709f7f49.json\", children: [Text(\"CHG-004/20260920T122732Z-885a709f7f49.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-004\")], [Code(\"885a709f7f49\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-004/20260920T133957Z-921970a0e964.json\", children: [Text(\"CHG-004/20260920T133957Z-921970a0e964.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-004\")], [Code(\"921970a0e964\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-004/20260920T185033Z-f14a4e4dde1b.json\", children: [Text(\"CHG-004/20260920T185033Z-f14a4e4dde1b.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-004\")], [Code(\"f14a4e4dde1b\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-004.6/20260921T041712Z-7e4a70cc3759.json\", children: [Text(\"CHG-004.6/20260921T041712Z-7e4a70cc3759.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-004.6\")], [Code(\"7e4a70cc3759\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-004.6/20260922T194102Z-8a723508c627.json\", children: [Text(\"CHG-004.6/20260922T194102Z-8a723508c627.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-004.6\")], [Code(\"8a723508c627\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-004.6/20260922T194644Z-20d42d90b69e.json\", children: [Text(\"CHG-004.6/20260922T194644Z-20d42d90b69e.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-004.6\")], [Code(\"20d42d90b69e\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-005/20260922T211312Z-e213c0b52b98.json\", children: [Text(\"CHG-005/20260922T211312Z-e213c0b52b98.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-005\")], [Code(\"e213c0b52b98\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-005/20260922T213429Z-25b035eac999.json\", children: [Text(\"CHG-005/20260922T213429Z-25b035eac999.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-005\")], [Code(\"25b035eac999\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-005/20260922T214412Z-a5db1d494f04.json\", children: [Text(\"CHG-005/20260922T214412Z-a5db1d494f04.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-005\")], [Code(\"a5db1d494f04\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-005/20260922T233340Z-dc226687be84.json\", children: [Text(\"CHG-005/20260922T233340Z-dc226687be84.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-005\")], [Code(\"dc226687be84\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-005/20260922T234321Z-f4c0e4ce5f8e.json\", children: [Text(\"CHG-005/20260922T234321Z-f4c0e4ce5f8e.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-005\")], [Code(\"f4c0e4ce5f8e\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-005/bdr-candidates/A-plan.json\", children: [Text(\"CHG-005/bdr-candidates/A-plan.json\")] }], [Text(\"?\")], [Text(\"?\")], [Text(\"?\")], [Code(\"?\")], [Text(\"?\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/CHG-005/bdr-candidates/B-fewer.json\", children: [Text(\"CHG-005/bdr-candidates/B-fewer.json\")] }], [Text(\"?\")], [Text(\"?\")], [Text(\"?\")], [Code(\"?\")], [Text(\"?\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/CHG-005/bdr-candidates/C-more.json\", children: [Text(\"CHG-005/bdr-candidates/C-more.json\")] }], [Text(\"?\")], [Text(\"?\")], [Text(\"?\")], [Code(\"?\")], [Text(\"?\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/CHG-007/20260923T051219Z-d3cffbf96185.json\", children: [Text(\"CHG-007/20260923T051219Z-d3cffbf96185.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-007\")], [Code(\"d3cffbf96185\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-007/20260923T051643Z-8156828cb14a.json\", children: [Text(\"CHG-007/20260923T051643Z-8156828cb14a.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-007\")], [Code(\"8156828cb14a\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-007/20260923T054356Z-9c10298df368.json\", children: [Text(\"CHG-007/20260923T054356Z-9c10298df368.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-007\")], [Code(\"9c10298df368\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-007/20260923T083804Z-e3c6a08177bd.json\", children: [Text(\"CHG-007/20260923T083804Z-e3c6a08177bd.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-007\")], [Code(\"e3c6a08177bd\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-007/20260923T090022Z-d78a10baf78e.json\", children: [Text(\"CHG-007/20260923T090022Z-d78a10baf78e.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-007\")], [Code(\"d78a10baf78e\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-007/20260923T091129Z-3d86fce33e52.json\", children: [Text(\"CHG-007/20260923T091129Z-3d86fce33e52.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-007\")], [Code(\"3d86fce33e52\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-019/20260922T230837Z-5215fa938bc5-dirty.json\", children: [Text(\"CHG-019/20260922T230837Z-5215fa938bc5-dirty.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-019\")], [Code(\"5215fa938bc5\")], [Text(\"yes\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-019/20260922T231518Z-0d1826174f90-dirty.json\", children: [Text(\"CHG-019/20260922T231518Z-0d1826174f90-dirty.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-019\")], [Code(\"0d1826174f90\")], [Text(\"yes\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-019/20260922T233629Z-5a768afbe364.json\", children: [Text(\"CHG-019/20260922T233629Z-5a768afbe364.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-019\")], [Code(\"5a768afbe364\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-019/20260922T234353Z-2d2383abd96b.json\", children: [Text(\"CHG-019/20260922T234353Z-2d2383abd96b.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-019\")], [Code(\"2d2383abd96b\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-019/20260922T235723Z-782dd2bcc0df.json\", children: [Text(\"CHG-019/20260922T235723Z-782dd2bcc0df.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-019\")], [Code(\"782dd2bcc0df\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-019/20260923T000352Z-3e829f25c6b0.json\", children: [Text(\"CHG-019/20260923T000352Z-3e829f25c6b0.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-019\")], [Code(\"3e829f25c6b0\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-019/20260923T001604Z-0a6b83370559.json\", children: [Text(\"CHG-019/20260923T001604Z-0a6b83370559.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-019\")], [Code(\"0a6b83370559\")], [Text(\"no\")], [Text(\"7 passed, 1 failed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-019/20260923T001811Z-0412a34e13e5.json\", children: [Text(\"CHG-019/20260923T001811Z-0412a34e13e5.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-019\")], [Code(\"0412a34e13e5\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/CHG-019/20260923T002556Z-967f0c2cc8eb.json\", children: [Text(\"CHG-019/20260923T002556Z-967f0c2cc8eb.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"CHG-019\")], [Code(\"967f0c2cc8eb\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/PLAN-2/20260922T200051Z-57bfea4749ce.json\", children: [Text(\"PLAN-2/20260922T200051Z-57bfea4749ce.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"PLAN-2\")], [Code(\"57bfea4749ce\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/PLAN-2/20260922T201134Z-87d53f35c7ac.json\", children: [Text(\"PLAN-2/20260922T201134Z-87d53f35c7ac.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"PLAN-2\")], [Code(\"87d53f35c7ac\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/PLAN-2/20260922T201604Z-74d542f65eb5.json\", children: [Text(\"PLAN-2/20260922T201604Z-74d542f65eb5.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"PLAN-2\")], [Code(\"74d542f65eb5\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/PLAN-3/20260924T011000Z-de44948dd4f9.json\", children: [Text(\"PLAN-3/20260924T011000Z-de44948dd4f9.json\")] }], [Text(\"L0\")], [Text(\"local\")], [Text(\"?\")], [Code(\"de44948dd4f9\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35468943628.json\", children: [Text(\"ci/35468943628.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-000\")], [Code(\"fe465f02048a\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35470532056.json\", children: [Text(\"ci/35470532056.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-001\")], [Code(\"ad9334592544\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35473050927.json\", children: [Text(\"ci/35473050927.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-001\")], [Code(\"f7484bd15046\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35474230710.json\", children: [Text(\"ci/35474230710.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-001\")], [Code(\"0ddf6c3c8261\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35475553364.json\", children: [Text(\"ci/35475553364.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-001\")], [Code(\"0d8edd0d4c48\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35476272071.json\", children: [Text(\"ci/35476272071.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-001\")], [Code(\"a37d3c6c9970\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35476379661.json\", children: [Text(\"ci/35476379661.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-001\")], [Code(\"534343167ecb\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35478601881.json\", children: [Text(\"ci/35478601881.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-002\")], [Code(\"68b41dcabca2\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35480297907.json\", children: [Text(\"ci/35480297907.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-002\")], [Code(\"1c9f7e1f54e4\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35481014407.json\", children: [Text(\"ci/35481014407.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-002\")], [Code(\"6b2784b337a7\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35481271471.json\", children: [Text(\"ci/35481271471.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"?\")], [Code(\"15d916a16e23\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35501551020.json\", children: [Text(\"ci/35501551020.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"?\")], [Code(\"4be4a199422d\")], [Text(\"no\")], [Text(\"7 passed, 1 not_run\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35502859792.json\", children: [Text(\"ci/35502859792.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-003\")], [Code(\"329dcc4d5c94\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35503860516.json\", children: [Text(\"ci/35503860516.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-003\")], [Code(\"1731b0d648e5\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35505682590.json\", children: [Text(\"ci/35505682590.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-003\")], [Code(\"de02534d3efd\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35507106318.json\", children: [Text(\"ci/35507106318.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-003\")], [Code(\"7a419405ee06\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35507402582.json\", children: [Text(\"ci/35507402582.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-003\")], [Code(\"ae2c66a227a8\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35507880475.json\", children: [Text(\"ci/35507880475.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"?\")], [Code(\"140fdfb92d62\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35510429218.json\", children: [Text(\"ci/35510429218.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-004\")], [Code(\"d08b214d4cdf\")], [Text(\"no\")], [Text(\"7 passed, 1 failed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35510728858.json\", children: [Text(\"ci/35510728858.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"CHG-004\")], [Code(\"18fcfb07cd4c\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35530722005.json\", children: [Text(\"ci/35530722005.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"?\")], [Code(\"434b7064e3f2\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35560448177.json\", children: [Text(\"ci/35560448177.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"?\")], [Code(\"ef4102a7d341\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35776292128.json\", children: [Text(\"ci/35776292128.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"?\")], [Code(\"bb1ee4e1ec3a\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35781193848.json\", children: [Text(\"ci/35781193848.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"?\")], [Code(\"72c5cfd2fa96\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35799644918.json\", children: [Text(\"ci/35799644918.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"?\")], [Code(\"dc564ea9dc8a\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35802502127.json\", children: [Text(\"ci/35802502127.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"?\")], [Code(\"ec493b794040\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/ci/35841881942.json\", children: [Text(\"ci/35841881942.json\")] }], [Text(\"L0\")], [Text(\"ci\")], [Text(\"?\")], [Code(\"74aeb1d853fd\")], [Text(\"no\")], [Text(\"8 passed\")], [Text(\"blocked\")]], [[Link { href: \"../../evidence/h4-crate/20260920T114416Z-140fdfb92d62-1436687-dirty.json\", children: [Text(\"h4-crate/20260920T114416Z-140fdfb92d62-1436687-dirty.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"140fdfb92d62\")], [Text(\"yes\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260920T114848Z-140fdfb92d62-1439339-dirty.json\", children: [Text(\"h4-crate/20260920T114848Z-140fdfb92d62-1439339-dirty.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"140fdfb92d62\")], [Text(\"yes\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260920T115738Z-defbfb7196a6-1446859.json\", children: [Text(\"h4-crate/20260920T115738Z-defbfb7196a6-1446859.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"defbfb7196a6\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260920T120111Z-b8c37856c21a-1452921.json\", children: [Text(\"h4-crate/20260920T120111Z-b8c37856c21a-1452921.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"b8c37856c21a\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260920T121342Z-3c6c758911ff-1462546.json\", children: [Text(\"h4-crate/20260920T121342Z-3c6c758911ff-1462546.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"3c6c758911ff\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260920T121505Z-f1c2c55a5550-1465488.json\", children: [Text(\"h4-crate/20260920T121505Z-f1c2c55a5550-1465488.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f1c2c55a5550\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260920T121858Z-ac6f7c67af63-1471693.json\", children: [Text(\"h4-crate/20260920T121858Z-ac6f7c67af63-1471693.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"ac6f7c67af63\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260920T122638Z-eec489c71f25.json\", children: [Text(\"h4-crate/20260920T122638Z-eec489c71f25.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"eec489c71f25\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260920T191132Z-434b7064e3f2.json\", children: [Text(\"h4-crate/20260920T191132Z-434b7064e3f2.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"434b7064e3f2\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260921T041705Z-f4dac8dc5710.json\", children: [Text(\"h4-crate/20260921T041705Z-f4dac8dc5710.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f4dac8dc5710\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260922T194055Z-0d0356aa1d9c.json\", children: [Text(\"h4-crate/20260922T194055Z-0d0356aa1d9c.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"0d0356aa1d9c\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260922T194637Z-f13f2bd5df1d.json\", children: [Text(\"h4-crate/20260922T194637Z-f13f2bd5df1d.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f13f2bd5df1d\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260923T033857Z-f915daaf8dc1-dirty.json\", children: [Text(\"h4-crate/20260923T033857Z-f915daaf8dc1-dirty.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f915daaf8dc1\")], [Text(\"yes\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-crate/20260923T040735Z-f915daaf8dc1-dirty.json\", children: [Text(\"h4-crate/20260923T040735Z-f915daaf8dc1-dirty.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f915daaf8dc1\")], [Text(\"yes\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-module/20260923T032721Z-f915daaf8dc1.json\", children: [Text(\"h4-module/20260923T032721Z-f915daaf8dc1.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f915daaf8dc1\")], [Text(\"yes\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-module/20260923T032935Z-f915daaf8dc1.json\", children: [Text(\"h4-module/20260923T032935Z-f915daaf8dc1.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f915daaf8dc1\")], [Text(\"yes\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-module/20260923T034443Z-f915daaf8dc1.json\", children: [Text(\"h4-module/20260923T034443Z-f915daaf8dc1.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f915daaf8dc1\")], [Text(\"yes\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-module/20260923T040716Z-f915daaf8dc1.json\", children: [Text(\"h4-module/20260923T040716Z-f915daaf8dc1.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f915daaf8dc1\")], [Text(\"yes\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-module/20260923T083727Z-f7ddaff1d931.json\", children: [Text(\"h4-module/20260923T083727Z-f7ddaff1d931.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f7ddaff1d931\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-module/20260923T085959Z-ec31c408f587.json\", children: [Text(\"h4-module/20260923T085959Z-ec31c408f587.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"ec31c408f587\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h4-module/20260923T091106Z-f0d35ab1c60c.json\", children: [Text(\"h4-module/20260923T091106Z-f0d35ab1c60c.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"f0d35ab1c60c\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h5/0d1826174f90.json\", children: [Text(\"h5/0d1826174f90.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"?\")], [Text(\"?\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h5/48f3c23c087d.json\", children: [Text(\"h5/48f3c23c087d.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"?\")], [Text(\"?\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h5/5215fa938bc5.json\", children: [Text(\"h5/5215fa938bc5.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"?\")], [Text(\"?\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h5/b2861207b8d2.json\", children: [Text(\"h5/b2861207b8d2.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"?\")], [Text(\"?\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h5/c60a080de75b.json\", children: [Text(\"h5/c60a080de75b.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"?\")], [Text(\"?\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h5/ed79a7a8e0ac-dirty-1790138754224832114-2796874.json\", children: [Text(\"h5/ed79a7a8e0ac-dirty-1790138754224832114-2796874.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"?\")], [Text(\"?\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/h5/f2f970b1d150.json\", children: [Text(\"h5/f2f970b1d150.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"?\")], [Text(\"?\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/md-corpus/20260922T210717Z-58e45f444c9f.json\", children: [Text(\"md-corpus/20260922T210717Z-58e45f444c9f.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"58e45f444c9f\")], [Text(\"no\")], [], [Text(\"?\")]], [[Link { href: \"../../evidence/md-corpus/20260923T044415Z-ed79a7a8e0ac.json\", children: [Text(\"md-corpus/20260923T044415Z-ed79a7a8e0ac.json\")] }], [Text(\"?\")], [Text(\"local\")], [Text(\"?\")], [Code(\"ed79a7a8e0ac\")], [Text(\"no\")], [], [Text(\"?\")]]] }]",
    "headings": "[Heading { level: 1, text: \"Evidence records\", slug: \"evidence-records\", base_slug: \"evidence-records\", line: 3 }]",
    "markdown": "<!-- generated by cargo xtask docs from evidence/**/*.json (sha256:251668ab9ee75ec3f7f5f3028854214a3ec0ce26461ca707bccc0231bdf190d9); do not edit -->\n\n# Evidence records\n\nEvery record is advisory: class `local` or `ci`, produced by candidate code and not authenticated (see [the threat model](../threat-model.md)). Eligibility `blocked` lists its reasons per acceptance predicate inside the record.\n\n| Record | Lane | Class | Task | Revision | Dirty | Outcomes | Eligibility |\n| --- | --- | --- | --- | --- | --- | --- | --- |\n| [CHG-000/20260919T205530Z-c48f1bd1f44d.json](../../evidence/CHG-000/20260919T205530Z-c48f1bd1f44d.json) | L0 | local | CHG-000 | `c48f1bd1f44d` | no | 7 passed, 1 not_run | blocked |\n| [CHG-001/20260919T212810Z-42c4860b5553-dirty.json](../../evidence/CHG-001/20260919T212810Z-42c4860b5553-dirty.json) | L0 | local | CHG-001 | `42c4860b5553` | yes | 7 passed, 1 not_run | blocked |\n| [CHG-001/20260919T222005Z-bcaf3090691d.json](../../evidence/CHG-001/20260919T222005Z-bcaf3090691d.json) | L0 | local | CHG-001 | `bcaf3090691d` | no | 7 passed, 1 not_run | blocked |\n| [CHG-001/20260919T224504Z-bae4b34894a2.json](../../evidence/CHG-001/20260919T224504Z-bae4b34894a2.json) | L0 | local | CHG-001 | `bae4b34894a2` | no | 7 passed, 1 not_run | blocked |\n| [CHG-001/20260919T231351Z-602d2f953d0b.json](../../evidence/CHG-001/20260919T231351Z-602d2f953d0b.json) | L0 | local | CHG-001 | `602d2f953d0b` | no | 7 passed, 1 not_run | blocked |\n| [CHG-001/20260919T232940Z-78fe0093c4a4.json](../../evidence/CHG-001/20260919T232940Z-78fe0093c4a4.json) | L0 | local | CHG-001 | `78fe0093c4a4` | no | 7 passed, 1 not_run | blocked |\n| [CHG-001/20260920T000645Z-da317050b06f.json](../../evidence/CHG-001/20260920T000645Z-da317050b06f.json) | L0 | local | CHG-001 | `da317050b06f` | no | 7 passed, 1 not_run | blocked |\n| [CHG-002/20260920T001651Z-24ad1602720b.json](../../evidence/CHG-002/20260920T001651Z-24ad1602720b.json) | L0 | local | CHG-002 | `24ad1602720b` | no | 1 failed, 6 passed, 1 not_run | blocked |\n| [CHG-002/20260920T001738Z-038a8ea2ba1c.json](../../evidence/CHG-002/20260920T001738Z-038a8ea2ba1c.json) | L0 | local | CHG-002 | `038a8ea2ba1c` | no | 7 passed, 1 not_run | blocked |\n| [CHG-002/20260920T002204Z-13f665cb0893.json](../../evidence/CHG-002/20260920T002204Z-13f665cb0893.json) | L0 | local | CHG-002 | `13f665cb0893` | no | 7 passed, 1 not_run | blocked |\n| [CHG-002/20260920T010039Z-cfb048e479fb.json](../../evidence/CHG-002/20260920T010039Z-cfb048e479fb.json) | L0 | local | CHG-002 | `cfb048e479fb` | no | 7 passed, 1 not_run | blocked |\n| [CHG-002/20260920T011724Z-6518f6a89af5.json](../../evidence/CHG-002/20260920T011724Z-6518f6a89af5.json) | L0 | local | CHG-002 | `6518f6a89af5` | no | 7 passed, 1 not_run | blocked |\n| [CHG-002.2/20260920T020130Z-15d916a16e23-dirty.json](../../evidence/CHG-002.2/20260920T020130Z-15d916a16e23-dirty.json) | L0 | local | CHG-002.2 | `15d916a16e23` | yes | 6 passed, 1 failed, 1 not_run | blocked |\n| [CHG-002.2/20260920T020203Z-15d916a16e23-dirty.json](../../evidence/CHG-002.2/20260920T020203Z-15d916a16e23-dirty.json) | L0 | local | CHG-002.2 | `15d916a16e23` | yes | 7 passed, 1 not_run | blocked |\n| [CHG-002.2/20260920T020252Z-1d40eaea5b4c.json](../../evidence/CHG-002.2/20260920T020252Z-1d40eaea5b4c.json) | L0 | local | CHG-002.2 | `1d40eaea5b4c` | no | 7 passed, 1 not_run | blocked |\n| [CHG-002.2/20260920T021545Z-8a736484fe6a.json](../../evidence/CHG-002.2/20260920T021545Z-8a736484fe6a.json) | L0 | local | CHG-002.2 | `8a736484fe6a` | no | 7 passed, 1 not_run | blocked |\n| [CHG-002.2/20260920T090804Z-b2c5ee1e8111.json](../../evidence/CHG-002.2/20260920T090804Z-b2c5ee1e8111.json) | L0 | local | CHG-002.2 | `b2c5ee1e8111` | no | 7 passed, 1 not_run | blocked |\n| [CHG-003/20260920T093725Z-53563857be6c.json](../../evidence/CHG-003/20260920T093725Z-53563857be6c.json) | L0 | local | CHG-003 | `53563857be6c` | no | 8 passed | blocked |\n| [CHG-003/20260920T100013Z-1cbc47ec97f2.json](../../evidence/CHG-003/20260920T100013Z-1cbc47ec97f2.json) | L0 | local | CHG-003 | `1cbc47ec97f2` | no | 8 passed | blocked |\n| [CHG-003/20260920T103310Z-4e337339c121-dirty.json](../../evidence/CHG-003/20260920T103310Z-4e337339c121-dirty.json) | L0 | local | CHG-003 | `4e337339c121` | yes | 8 passed | blocked |\n| [CHG-003/20260920T110910Z-920cca6b07b7-dirty.json](../../evidence/CHG-003/20260920T110910Z-920cca6b07b7-dirty.json) | L0 | local | CHG-003 | `920cca6b07b7` | yes | 8 passed | blocked |\n| [CHG-003/20260920T111037Z-956707e0b1da.json](../../evidence/CHG-003/20260920T111037Z-956707e0b1da.json) | L0 | local | CHG-003 | `956707e0b1da` | no | 8 passed | blocked |\n| [CHG-003/20260920T111702Z-a925cfcda441.json](../../evidence/CHG-003/20260920T111702Z-a925cfcda441.json) | L0 | local | CHG-003 | `a925cfcda441` | no | 8 passed | blocked |\n| [CHG-004/20260920T115809Z-762ab470f54b.json](../../evidence/CHG-004/20260920T115809Z-762ab470f54b.json) | L0 | local | CHG-004 | `762ab470f54b` | no | 8 passed | blocked |\n| [CHG-004/20260920T120146Z-540ce6a70c43.json](../../evidence/CHG-004/20260920T120146Z-540ce6a70c43.json) | L0 | local | CHG-004 | `540ce6a70c43` | no | 8 passed | blocked |\n| [CHG-004/20260920T122054Z-bef220d57566.json](../../evidence/CHG-004/20260920T122054Z-bef220d57566.json) | L0 | local | CHG-004 | `bef220d57566` | no | 8 passed | blocked |\n| [CHG-004/20260920T122732Z-885a709f7f49.json](../../evidence/CHG-004/20260920T122732Z-885a709f7f49.json) | L0 | local | CHG-004 | `885a709f7f49` | no | 8 passed | blocked |\n| [CHG-004/20260920T133957Z-921970a0e964.json](../../evidence/CHG-004/20260920T133957Z-921970a0e964.json) | L0 | local | CHG-004 | `921970a0e964` | no | 8 passed | blocked |\n| [CHG-004/20260920T185033Z-f14a4e4dde1b.json](../../evidence/CHG-004/20260920T185033Z-f14a4e4dde1b.json) | L0 | local | CHG-004 | `f14a4e4dde1b` | no | 8 passed | blocked |\n| [CHG-004.6/20260921T041712Z-7e4a70cc3759.json](../../evidence/CHG-004.6/20260921T041712Z-7e4a70cc3759.json) | L0 | local | CHG-004.6 | `7e4a70cc3759` | no | 8 passed | blocked |\n| [CHG-004.6/20260922T194102Z-8a723508c627.json](../../evidence/CHG-004.6/20260922T194102Z-8a723508c627.json) | L0 | local | CHG-004.6 | `8a723508c627` | no | 8 passed | blocked |\n| [CHG-004.6/20260922T194644Z-20d42d90b69e.json](../../evidence/CHG-004.6/20260922T194644Z-20d42d90b69e.json) | L0 | local | CHG-004.6 | `20d42d90b69e` | no | 8 passed | blocked |\n| [CHG-005/20260922T211312Z-e213c0b52b98.json](../../evidence/CHG-005/20260922T211312Z-e213c0b52b98.json) | L0 | local | CHG-005 | `e213c0b52b98` | no | 8 passed | blocked |\n| [CHG-005/20260922T213429Z-25b035eac999.json](../../evidence/CHG-005/20260922T213429Z-25b035eac999.json) | L0 | local | CHG-005 | `25b035eac999` | no | 8 passed | blocked |\n| [CHG-005/20260922T214412Z-a5db1d494f04.json](../../evidence/CHG-005/20260922T214412Z-a5db1d494f04.json) | L0 | local | CHG-005 | `a5db1d494f04` | no | 8 passed | blocked |\n| [CHG-005/20260922T233340Z-dc226687be84.json](../../evidence/CHG-005/20260922T233340Z-dc226687be84.json) | L0 | local | CHG-005 | `dc226687be84` | no | 8 passed | blocked |\n| [CHG-005/20260922T234321Z-f4c0e4ce5f8e.json](../../evidence/CHG-005/20260922T234321Z-f4c0e4ce5f8e.json) | L0 | local | CHG-005 | `f4c0e4ce5f8e` | no | 8 passed | blocked |\n| [CHG-005/bdr-candidates/A-plan.json](../../evidence/CHG-005/bdr-candidates/A-plan.json) | ? | ? | ? | `?` | ? |  | ? |\n| [CHG-005/bdr-candidates/B-fewer.json](../../evidence/CHG-005/bdr-candidates/B-fewer.json) | ? | ? | ? | `?` | ? |  | ? |\n| [CHG-005/bdr-candidates/C-more.json](../../evidence/CHG-005/bdr-candidates/C-more.json) | ? | ? | ? | `?` | ? |  | ? |\n| [CHG-007/20260923T051219Z-d3cffbf96185.json](../../evidence/CHG-007/20260923T051219Z-d3cffbf96185.json) | L0 | local | CHG-007 | `d3cffbf96185` | no | 8 passed | blocked |\n| [CHG-007/20260923T051643Z-8156828cb14a.json](../../evidence/CHG-007/20260923T051643Z-8156828cb14a.json) | L0 | local | CHG-007 | `8156828cb14a` | no | 8 passed | blocked |\n| [CHG-007/20260923T054356Z-9c10298df368.json](../../evidence/CHG-007/20260923T054356Z-9c10298df368.json) | L0 | local | CHG-007 | `9c10298df368` | no | 8 passed | blocked |\n| [CHG-007/20260923T083804Z-e3c6a08177bd.json](../../evidence/CHG-007/20260923T083804Z-e3c6a08177bd.json) | L0 | local | CHG-007 | `e3c6a08177bd` | no | 8 passed | blocked |\n| [CHG-007/20260923T090022Z-d78a10baf78e.json](../../evidence/CHG-007/20260923T090022Z-d78a10baf78e.json) | L0 | local | CHG-007 | `d78a10baf78e` | no | 8 passed | blocked |\n| [CHG-007/20260923T091129Z-3d86fce33e52.json](../../evidence/CHG-007/20260923T091129Z-3d86fce33e52.json) | L0 | local | CHG-007 | `3d86fce33e52` | no | 8 passed | blocked |\n| [CHG-019/20260922T230837Z-5215fa938bc5-dirty.json](../../evidence/CHG-019/20260922T230837Z-5215fa938bc5-dirty.json) | L0 | local | CHG-019 | `5215fa938bc5` | yes | 8 passed | blocked |\n| [CHG-019/20260922T231518Z-0d1826174f90-dirty.json](../../evidence/CHG-019/20260922T231518Z-0d1826174f90-dirty.json) | L0 | local | CHG-019 | `0d1826174f90` | yes | 8 passed | blocked |\n| [CHG-019/20260922T233629Z-5a768afbe364.json](../../evidence/CHG-019/20260922T233629Z-5a768afbe364.json) | L0 | local | CHG-019 | `5a768afbe364` | no | 8 passed | blocked |\n| [CHG-019/20260922T234353Z-2d2383abd96b.json](../../evidence/CHG-019/20260922T234353Z-2d2383abd96b.json) | L0 | local | CHG-019 | `2d2383abd96b` | no | 8 passed | blocked |\n| [CHG-019/20260922T235723Z-782dd2bcc0df.json](../../evidence/CHG-019/20260922T235723Z-782dd2bcc0df.json) | L0 | local | CHG-019 | `782dd2bcc0df` | no | 8 passed | blocked |\n| [CHG-019/20260923T000352Z-3e829f25c6b0.json](../../evidence/CHG-019/20260923T000352Z-3e829f25c6b0.json) | L0 | local | CHG-019 | `3e829f25c6b0` | no | 8 passed | blocked |\n| [CHG-019/20260923T001604Z-0a6b83370559.json](../../evidence/CHG-019/20260923T001604Z-0a6b83370559.json) | L0 | local | CHG-019 | `0a6b83370559` | no | 7 passed, 1 failed | blocked |\n| [CHG-019/20260923T001811Z-0412a34e13e5.json](../../evidence/CHG-019/20260923T001811Z-0412a34e13e5.json) | L0 | local | CHG-019 | `0412a34e13e5` | no | 8 passed | blocked |\n| [CHG-019/20260923T002556Z-967f0c2cc8eb.json](../../evidence/CHG-019/20260923T002556Z-967f0c2cc8eb.json) | L0 | local | CHG-019 | `967f0c2cc8eb` | no | 8 passed | blocked |\n| [PLAN-2/20260922T200051Z-57bfea4749ce.json](../../evidence/PLAN-2/20260922T200051Z-57bfea4749ce.json) | L0 | local | PLAN-2 | `57bfea4749ce` | no | 8 passed | blocked |\n| [PLAN-2/20260922T201134Z-87d53f35c7ac.json](../../evidence/PLAN-2/20260922T201134Z-87d53f35c7ac.json) | L0 | local | PLAN-2 | `87d53f35c7ac` | no | 8 passed | blocked |\n| [PLAN-2/20260922T201604Z-74d542f65eb5.json](../../evidence/PLAN-2/20260922T201604Z-74d542f65eb5.json) | L0 | local | PLAN-2 | `74d542f65eb5` | no | 8 passed | blocked |\n| [PLAN-3/20260924T011000Z-de44948dd4f9.json](../../evidence/PLAN-3/20260924T011000Z-de44948dd4f9.json) | L0 | local | ? | `de44948dd4f9` | no | 8 passed | blocked |\n| [ci/35468943628.json](../../evidence/ci/35468943628.json) | L0 | ci | CHG-000 | `fe465f02048a` | no | 7 passed, 1 not_run | blocked |\n| [ci/35470532056.json](../../evidence/ci/35470532056.json) | L0 | ci | CHG-001 | `ad9334592544` | no | 7 passed, 1 not_run | blocked |\n| [ci/35473050927.json](../../evidence/ci/35473050927.json) | L0 | ci | CHG-001 | `f7484bd15046` | no | 7 passed, 1 not_run | blocked |\n| [ci/35474230710.json](../../evidence/ci/35474230710.json) | L0 | ci | CHG-001 | `0ddf6c3c8261` | no | 7 passed, 1 not_run | blocked |\n| [ci/35475553364.json](../../evidence/ci/35475553364.json) | L0 | ci | CHG-001 | `0d8edd0d4c48` | no | 7 passed, 1 not_run | blocked |\n| [ci/35476272071.json](../../evidence/ci/35476272071.json) | L0 | ci | CHG-001 | `a37d3c6c9970` | no | 7 passed, 1 not_run | blocked |\n| [ci/35476379661.json](../../evidence/ci/35476379661.json) | L0 | ci | CHG-001 | `534343167ecb` | no | 7 passed, 1 not_run | blocked |\n| [ci/35478601881.json](../../evidence/ci/35478601881.json) | L0 | ci | CHG-002 | `68b41dcabca2` | no | 7 passed, 1 not_run | blocked |\n| [ci/35480297907.json](../../evidence/ci/35480297907.json) | L0 | ci | CHG-002 | `1c9f7e1f54e4` | no | 7 passed, 1 not_run | blocked |\n| [ci/35481014407.json](../../evidence/ci/35481014407.json) | L0 | ci | CHG-002 | `6b2784b337a7` | no | 7 passed, 1 not_run | blocked |\n| [ci/35481271471.json](../../evidence/ci/35481271471.json) | L0 | ci | ? | `15d916a16e23` | no | 7 passed, 1 not_run | blocked |\n| [ci/35501551020.json](../../evidence/ci/35501551020.json) | L0 | ci | ? | `4be4a199422d` | no | 7 passed, 1 not_run | blocked |\n| [ci/35502859792.json](../../evidence/ci/35502859792.json) | L0 | ci | CHG-003 | `329dcc4d5c94` | no | 8 passed | blocked |\n| [ci/35503860516.json](../../evidence/ci/35503860516.json) | L0 | ci | CHG-003 | `1731b0d648e5` | no | 8 passed | blocked |\n| [ci/35505682590.json](../../evidence/ci/35505682590.json) | L0 | ci | CHG-003 | `de02534d3efd` | no | 8 passed | blocked |\n| [ci/35507106318.json](../../evidence/ci/35507106318.json) | L0 | ci | CHG-003 | `7a419405ee06` | no | 8 passed | blocked |\n| [ci/35507402582.json](../../evidence/ci/35507402582.json) | L0 | ci | CHG-003 | `ae2c66a227a8` | no | 8 passed | blocked |\n| [ci/35507880475.json](../../evidence/ci/35507880475.json) | L0 | ci | ? | `140fdfb92d62` | no | 8 passed | blocked |\n| [ci/35510429218.json](../../evidence/ci/35510429218.json) | L0 | ci | CHG-004 | `d08b214d4cdf` | no | 7 passed, 1 failed | blocked |\n| [ci/35510728858.json](../../evidence/ci/35510728858.json) | L0 | ci | CHG-004 | `18fcfb07cd4c` | no | 8 passed | blocked |\n| [ci/35530722005.json](../../evidence/ci/35530722005.json) | L0 | ci | ? | `434b7064e3f2` | no | 8 passed | blocked |\n| [ci/35560448177.json](../../evidence/ci/35560448177.json) | L0 | ci | ? | `ef4102a7d341` | no | 8 passed | blocked |\n| [ci/35776292128.json](../../evidence/ci/35776292128.json) | L0 | ci | ? | `bb1ee4e1ec3a` | no | 8 passed | blocked |\n| [ci/35781193848.json](../../evidence/ci/35781193848.json) | L0 | ci | ? | `72c5cfd2fa96` | no | 8 passed | blocked |\n| [ci/35799644918.json](../../evidence/ci/35799644918.json) | L0 | ci | ? | `dc564ea9dc8a` | no | 8 passed | blocked |\n| [ci/35802502127.json](../../evidence/ci/35802502127.json) | L0 | ci | ? | `ec493b794040` | no | 8 passed | blocked |\n| [ci/35841881942.json](../../evidence/ci/35841881942.json) | L0 | ci | ? | `74aeb1d853fd` | no | 8 passed | blocked |\n| [h4-crate/20260920T114416Z-140fdfb92d62-1436687-dirty.json](../../evidence/h4-crate/20260920T114416Z-140fdfb92d62-1436687-dirty.json) | ? | local | ? | `140fdfb92d62` | yes |  | ? |\n| [h4-crate/20260920T114848Z-140fdfb92d62-1439339-dirty.json](../../evidence/h4-crate/20260920T114848Z-140fdfb92d62-1439339-dirty.json) | ? | local | ? | `140fdfb92d62` | yes |  | ? |\n| [h4-crate/20260920T115738Z-defbfb7196a6-1446859.json](../../evidence/h4-crate/20260920T115738Z-defbfb7196a6-1446859.json) | ? | local | ? | `defbfb7196a6` | no |  | ? |\n| [h4-crate/20260920T120111Z-b8c37856c21a-1452921.json](../../evidence/h4-crate/20260920T120111Z-b8c37856c21a-1452921.json) | ? | local | ? | `b8c37856c21a` | no |  | ? |\n| [h4-crate/20260920T121342Z-3c6c758911ff-1462546.json](../../evidence/h4-crate/20260920T121342Z-3c6c758911ff-1462546.json) | ? | local | ? | `3c6c758911ff` | no |  | ? |\n| [h4-crate/20260920T121505Z-f1c2c55a5550-1465488.json](../../evidence/h4-crate/20260920T121505Z-f1c2c55a5550-1465488.json) | ? | local | ? | `f1c2c55a5550` | no |  | ? |\n| [h4-crate/20260920T121858Z-ac6f7c67af63-1471693.json](../../evidence/h4-crate/20260920T121858Z-ac6f7c67af63-1471693.json) | ? | local | ? | `ac6f7c67af63` | no |  | ? |\n| [h4-crate/20260920T122638Z-eec489c71f25.json](../../evidence/h4-crate/20260920T122638Z-eec489c71f25.json) | ? | local | ? | `eec489c71f25` | no |  | ? |\n| [h4-crate/20260920T191132Z-434b7064e3f2.json](../../evidence/h4-crate/20260920T191132Z-434b7064e3f2.json) | ? | local | ? | `434b7064e3f2` | no |  | ? |\n| [h4-crate/20260921T041705Z-f4dac8dc5710.json](../../evidence/h4-crate/20260921T041705Z-f4dac8dc5710.json) | ? | local | ? | `f4dac8dc5710` | no |  | ? |\n| [h4-crate/20260922T194055Z-0d0356aa1d9c.json](../../evidence/h4-crate/20260922T194055Z-0d0356aa1d9c.json) | ? | local | ? | `0d0356aa1d9c` | no |  | ? |\n| [h4-crate/20260922T194637Z-f13f2bd5df1d.json](../../evidence/h4-crate/20260922T194637Z-f13f2bd5df1d.json) | ? | local | ? | `f13f2bd5df1d` | no |  | ? |\n| [h4-crate/20260923T033857Z-f915daaf8dc1-dirty.json](../../evidence/h4-crate/20260923T033857Z-f915daaf8dc1-dirty.json) | ? | local | ? | `f915daaf8dc1` | yes |  | ? |\n| [h4-crate/20260923T040735Z-f915daaf8dc1-dirty.json](../../evidence/h4-crate/20260923T040735Z-f915daaf8dc1-dirty.json) | ? | local | ? | `f915daaf8dc1` | yes |  | ? |\n| [h4-module/20260923T032721Z-f915daaf8dc1.json](../../evidence/h4-module/20260923T032721Z-f915daaf8dc1.json) | ? | local | ? | `f915daaf8dc1` | yes |  | ? |\n| [h4-module/20260923T032935Z-f915daaf8dc1.json](../../evidence/h4-module/20260923T032935Z-f915daaf8dc1.json) | ? | local | ? | `f915daaf8dc1` | yes |  | ? |\n| [h4-module/20260923T034443Z-f915daaf8dc1.json](../../evidence/h4-module/20260923T034443Z-f915daaf8dc1.json) | ? | local | ? | `f915daaf8dc1` | yes |  | ? |\n| [h4-module/20260923T040716Z-f915daaf8dc1.json](../../evidence/h4-module/20260923T040716Z-f915daaf8dc1.json) | ? | local | ? | `f915daaf8dc1` | yes |  | ? |\n| [h4-module/20260923T083727Z-f7ddaff1d931.json](../../evidence/h4-module/20260923T083727Z-f7ddaff1d931.json) | ? | local | ? | `f7ddaff1d931` | no |  | ? |\n| [h4-module/20260923T085959Z-ec31c408f587.json](../../evidence/h4-module/20260923T085959Z-ec31c408f587.json) | ? | local | ? | `ec31c408f587` | no |  | ? |\n| [h4-module/20260923T091106Z-f0d35ab1c60c.json](../../evidence/h4-module/20260923T091106Z-f0d35ab1c60c.json) | ? | local | ? | `f0d35ab1c60c` | no |  | ? |\n| [h5/0d1826174f90.json](../../evidence/h5/0d1826174f90.json) | ? | local | ? | `?` | ? |  | ? |\n| [h5/48f3c23c087d.json](../../evidence/h5/48f3c23c087d.json) | ? | local | ? | `?` | ? |  | ? |\n| [h5/5215fa938bc5.json](../../evidence/h5/5215fa938bc5.json) | ? | local | ? | `?` | ? |  | ? |\n| [h5/b2861207b8d2.json](../../evidence/h5/b2861207b8d2.json) | ? | local | ? | `?` | ? |  | ? |\n| [h5/c60a080de75b.json](../../evidence/h5/c60a080de75b.json) | ? | local | ? | `?` | ? |  | ? |\n| [h5/ed79a7a8e0ac-dirty-1790138754224832114-2796874.json](../../evidence/h5/ed79a7a8e0ac-dirty-1790138754224832114-2796874.json) | ? | local | ? | `?` | ? |  | ? |\n| [h5/f2f970b1d150.json](../../evidence/h5/f2f970b1d150.json) | ? | local | ? | `?` | ? |  | ? |\n| [md-corpus/20260922T210717Z-58e45f444c9f.json](../../evidence/md-corpus/20260922T210717Z-58e45f444c9f.json) | ? | local | ? | `58e45f444c9f` | no |  | ? |\n| [md-corpus/20260923T044415Z-ed79a7a8e0ac.json](../../evidence/md-corpus/20260923T044415Z-ed79a7a8e0ac.json) | ? | local | ? | `ed79a7a8e0ac` | no |  | ? |\n"
  },
  "docs/index.md": {
    "body": "[Heading { level: 1, slug: \"rha-documentation\", children: [Text(\"RHA documentation\")] }, Paragraph([Text(\"The landing page of the docs that \"), Code(\"rhawiki\"), Text(\" renders (CHG-005). Links use wiki syntax, \"), Code(\"[[page-id]]\"), Text(\", where a page id is the path under \"), Code(\"docs/\"), Text(\" without \"), Code(\".md\"), Text(\".\")]), Heading { level: 2, slug: \"specification-and-plan\", children: [Text(\"Specification and plan\")] }, List { start: None, items: [[WikiLink { index: 0, children: [Text(\"spec/rha-spec-v0.10\")] }, Text(\": the Recursive Hexagonal Architecture working specification, v0.10.\")], [WikiLink { index: 1, children: [Text(\"plan/IMPLEMENTATION-PLAN\")] }, Text(\": the authorized work order for implementing and validating its mechanisms.\")]] }, Heading { level: 2, slug: \"status\", children: [Text(\"Status\")] }, List { start: None, items: [[WikiLink { index: 2, children: [Text(\"maturity\")] }, Text(\": the maturity ledger. The Executor proposes; Kennedy accepts.\")], [WikiLink { index: 3, children: [Text(\"proposals/spec-v0.11\")] }, Text(\": findings queued for the next version of the specification, with the evidence for each.\")], [WikiLink { index: 4, children: [Text(\"threat-model\")] }, Text(\": deviations from spec §11.0, the \"), Code(\"ECC-Solo\"), Text(\" profile, and why every evidence record is advisory.\")], [WikiLink { index: 5, children: [Text(\"toolchain\")] }, Text(\": tools and required versions (generated).\")]] }, Heading { level: 2, slug: \"records\", children: [Text(\"Records\")] }, List { start: None, items: [[WikiLink { index: 6, children: [Text(\"tasks/index\")] }, Text(\": task records (generated).\")], [WikiLink { index: 7, children: [Text(\"evidence/index\")] }, Text(\": evidence records (generated).\")], [WikiLink { index: 8, children: [Text(\"changes/CHG-000-bootstrap\")] }, Text(\": the bootstrap change record.\")], [WikiLink { index: 9, children: [Text(\"changes/CHG-001-ambient-effect-deny-list\")] }, Text(\": the ambient-effect deny list and Clippy discovery change record.\")]] }, Heading { level: 2, slug: \"decisions-and-architecture\", children: [Text(\"Decisions and architecture\")] }, List { start: None, items: [[WikiLink { index: 10, children: [Text(\"adr/ADR-0001-bootstrap-decisions\")] }, Text(\": repository bootstrap decisions.\")], [WikiLink { index: 11, children: [Text(\"adr/ADR-0002-clippy-config-discovery\")] }, Text(\": how the pinned Clippy finds its configuration, and the core-crate template.\")], [WikiLink { index: 12, children: [Text(\"architecture/components\")] }, Text(\": components and their ports.\")]] }]",
    "headings": "[Heading { level: 1, text: \"RHA documentation\", slug: \"rha-documentation\", base_slug: \"rha-documentation\", line: 1 }, Heading { level: 2, text: \"Specification and plan\", slug: \"specification-and-plan\", base_slug: \"specification-and-plan\", line: 5 }, Heading { level: 2, text: \"Status\", slug: \"status\", base_slug: \"status\", line: 10 }, Heading { level: 2, text: \"Records\", slug: \"records\", base_slug: \"records\", line: 17 }, Heading { level: 2, text: \"Decisions and architecture\", slug: \"decisions-and-architecture\", base_slug: \"decisions-and-architecture\", line: 24 }]",
    "markdown": "# RHA documentation\n\nThe landing page of the docs that `rhawiki` renders (CHG-005). Links use wiki syntax, `[[page-id]]`, where a page id is the path under `docs/` without `.md`.\n\n## Specification and plan\n\n- [[spec/rha-spec-v0.10]]: the Recursive Hexagonal Architecture working specification, v0.10.\n- [[plan/IMPLEMENTATION-PLAN]]: the authorized work order for implementing and validating its mechanisms.\n\n## Status\n\n- [[maturity]]: the maturity ledger. The Executor proposes; Kennedy accepts.\n- [[proposals/spec-v0.11]]: findings queued for the next version of the specification, with the evidence for each.\n- [[threat-model]]: deviations from spec §11.0, the `ECC-Solo` profile, and why every evidence record is advisory.\n- [[toolchain]]: tools and required versions (generated).\n\n## Records\n\n- [[tasks/index]]: task records (generated).\n- [[evidence/index]]: evidence records (generated).\n- [[changes/CHG-000-bootstrap]]: the bootstrap change record.\n- [[changes/CHG-001-ambient-effect-deny-list]]: the ambient-effect deny list and Clippy discovery change record.\n\n## Decisions and architecture\n\n- [[adr/ADR-0001-bootstrap-decisions]]: repository bootstrap decisions.\n- [[adr/ADR-0002-clippy-config-discovery]]: how the pinned Clippy finds its configuration, and the core-crate template.\n- [[architecture/components]]: components and their ports.\n"
  },
  "docs/maturity.md": {
    "body": "[Heading { level: 1, slug: \"maturity-ledger\", children: [Text(\"Maturity ledger\")] }, Paragraph([Text(\"One row per mechanism of spec §1.4. The Executor edits only \"), Strong([Text(\"Proposed\")]), Text(\"; Kennedy edits \"), Strong([Text(\"Accepted\")]), Text(\"; the spec's own §1.4 table changes only at a version bump. Maturity: \"), Strong([Text(\"S\")]), Text(\" specified, \"), Strong([Text(\"I\")]), Text(\" implemented (a named, versioned, retrievable artifact), \"), Strong([Text(\"V\")]), Text(\" validated (implemented, and its stated conformance or adversarial tests passed, with an evidence record). A proposal names the artifact, version, evidence, checking scope, and environment; it is not a permanent badge.\")]), Paragraph([Text(\"The \"), Strong([Text(\"Accepted\")]), Text(\" column starts with the values printed in spec v0.10 §1.4.\")]), Table { align: [None, None, None, None, None, None, None, None, None], head: [[Text(\"§1.4 mechanism\")], [Text(\"Spec §\")], [Text(\"Artifact path\")], [Text(\"Version\")], [Text(\"Proposed (Executor)\")], [Text(\"Evidence\")], [Text(\"Checking scope\")], [Text(\"Environment\")], [Text(\"Accepted (Kennedy, date, ref)\")]], rows: [[[Text(\"Structural laws as review criteria\")], [Text(\"§4\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S\")], [Text(\"–\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S (spec v0.10)\")]], [[Text(\"Crate-graph checker, \"), Code(\"cargo xtask architecture\")], [Text(\"§6.13\")], [Code(\"xtask/src/{architecture.rs,metadata.rs,graph/}\")], [Text(\"xtask 0.1.0 (CHG-003 to CHG-004.6)\")], [Strong([Text(\"V (crate level), proposed\")]), Text(\": the amended H4 run detects 22/22 with no unmatched finding and no alarm on a legitimate case; C13's accurate \"), Code(\"adapter.foreign_core\"), Text(\" warning is a registered fact under the DP-1.1c amendment Kennedy approved on 2026-09-20 \"), Strong([Text(\"after the data\")]), Text(\", disclosed on the ledger and in the record's amendment chain. Held-out cases not yet run (DP-1.1b as amended). V is Kennedy's to accept at DP-1.6\")], [Link { href: \"../evidence/h4-crate/20260923T040735Z-f915daaf8dc1-dirty.json\", children: [Text(\"latest dirty H4\")] }, Text(\"; \"), Link { href: \"../evidence/h4-crate/20260922T194637Z-f13f2bd5df1d.json\", children: [Text(\"amended H4\")] }, Text(\" at \"), Code(\"f13f2bd\"), Text(\" (the earlier amended run, 20260921T041705Z-f4dac8dc5710.json, predates the CHG-004.6.1 repair); the failed runs are kept: \"), Link { href: \"../evidence/h4-crate/20260920T122638Z-eec489c71f25.json\", children: [Text(\"delivery\")] }, Text(\" and \"), Link { href: \"../evidence/h4-crate/20260920T191132Z-434b7064e3f2.json\", children: [Text(\"baseline at the CHG-004 merge\")] }, Text(\"; \"), Link { href: \"changes/CHG-004.6-c13-registration.md\", children: [Text(\"CHG-004.6 determination\")] }], [Text(\"All 35 committed crate cases, exact witnesses plus one registered fact (C13), offline metadata and compiler R01; two documented holes; crate-scope check only (the declared module scope is tracked in the separate module row); held-out cases pending (DP-1.1b)\")], [Text(\"Rust 1.98.1, local WSL2 and the GitHub runner; clean subject and checker identity\")], [Text(\"V, crate level (DP-1.6, accepted under Kennedy's delegation, 2026-09-22; \"), Link { href: \"../.rha/acceptances/CHG-005.toml\", children: [Text(\"CHG-005 acceptance\")] }, Text(\")\")]], [[Code(\"cargo-generate\"), Text(\" baseline template\")], [Text(\"§6.16.7\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S: deferred (plan §10)\")], [Text(\"–\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S\"), Text(\"*\"), Text(\" (spec v0.10)\")]], [[Text(\"Module-graph check inside a crate\")], [Text(\"§6.13\")], [Code(\"xtask/src/modules/\"), Text(\", \"), Code(\"xtask/tests/module_corpus.rs\")], [Text(\"xtask 0.1.0 (CHG-007)\")], [Strong([Text(\"V, proposed\")]), Text(\" at registered source-based module scope: 17/17 violations detected, 0/6 alarms on legitimate cases, 3 expected misses, the live corpus passed, and 256 supplementary fixtures matched exactly. This scope does not claim macro expansion, include expansion, type inference, or transitive analysis.\")], [Link { href: \"../evidence/h4-module/20260923T040716Z-f915daaf8dc1.json\", children: [Text(\"latest H4\")] }, Text(\"; \"), Link { href: \"changes/CHG-007-module-level.md\", children: [Text(\"CHG-007 change record\")] }], [Text(\"Registered source-based module directions only; source extraction does not cover macro expansion, include expansion, or type inference; transitive analysis is unavailable\")], [Text(\"Rust 1.98.1, Linux x86_64; local dirty-snapshot provenance; no authentication or held-out claim\")], [Text(\"S (spec v0.10)\")]], [[Text(\"Ambient-effect deny list; \"), Code(\"no_std\"), Text(\" core option\")], [Text(\"§6.8\")], [Code(\"xtask/templates/core-clippy.toml\"), Text(\" (111 entries), \"), Code(\"xtask/src/clippy_template.rs\"), Text(\" (rule \"), Code(\"effect.core_clippy_template\"), Text(\", which also checks each core crate's crate-root \"), Code(\"#![forbid(...)]\"), Text(\"), \"), Code(\"xtask/tests/clippy_corpus.rs\")], [Text(\"xtask 0.1.0 (CHG-001, CHG-001.1 to CHG-001.3; CHG-007 source-guard repair)\")], [Strong([Text(\"V (crate level), proposed\")]), Text(\" for the deny list: all 111 entries fail the build in fixtures (CHG-001), and four real core crates (\"), Code(\"library\"), Text(\", \"), Code(\"document\"), Text(\", \"), Code(\"graph\"), Text(\", \"), Code(\"site\"), Text(\") compile under the template with the crate-root \"), Code(\"forbid\"), Text(\", which \"), Code(\"effect.core_clippy_template\"), Text(\" checks in L0 on every run. The source guard now evaluates Cargo production roots and syntax attributes (CHG-007 repair). The attribute escape of ADR-0002 rule 5 is closed by that \"), Code(\"forbid\"), Text(\". \"), Code(\"no_std\"), Text(\" production option: S; \"), Link { href: \"../experiments/no-std-graph/result.md\", children: [Text(\"E1\")] }, Text(\" validates import-only alloc adaptation and its direct-std negative control on the isolated resolver snapshot, with std-bearing dependencies retained\")], [Text(\"CHG-001 records (\"), Code(\"evidence/w1-clippy/\"), Text(\"); the P-A L0 record (\"), Code(\"evidence/CHG-005/\"), Text(\"); \"), Code(\"cargo xtask architecture\"), Text(\" on the product reports 0 errors\")], [Text(\"direct uses in Cargo production roots at crate level, including syntax attributes (CHG-007 repair); not transitive calls, not macro-generated calls, not paths missing from the list, not a build whose RUSTFLAGS or .cargo/config.toml lowers lint levels (ADR-0002 rules 5, 6, 8)\")], [Text(\"Clippy 0.1.98; local WSL2 and GitHub \"), Code(\"ubuntu24\"), Text(\" (the corpus runs in \"), Code(\"L0.nextest\"), Text(\" in both)\")], [Text(\"V, crate level, deny list only; \"), Code(\"no_std\"), Text(\" S (DP-1.6, accepted under Kennedy's delegation, 2026-09-22; \"), Link { href: \"../.rha/acceptances/CHG-005.toml\", children: [Text(\"CHG-005 acceptance\")] }, Text(\")\")]], [[Text(\"Fast-lane CI configuration\")], [Text(\"§12.1\")], [Code(\".rha/policy.toml\"), Text(\" \"), Code(\"[lanes.L0]\"), Text(\", \"), Code(\"xtask/src/lanes.rs\"), Text(\", \"), Code(\".github/workflows/ci.yml\")], [Text(\"xtask 0.1.0; tag \"), Code(\"v0.10-m0\")], [Strong([Text(\"V, proposed\")]), Text(\": the eight §12.1 checks run through one entry point on every PR and on main; CI run 35781193848 on main (72c5cfd) recorded all eight \"), Code(\"passed\"), Text(\", with 151 tests selected\")], [Code(\"evidence/ci/35781193848.json\"), Text(\"; policy drift test \"), Code(\"xtask/tests/policy_drift.rs\")], [Text(\"L0 lane on this repository\")], [Text(\"local WSL2 workstation; GitHub \"), Code(\"ubuntu-24.04\")], [Text(\"V (DP-1.6, accepted under Kennedy's delegation, 2026-09-22; \"), Link { href: \"../.rha/acceptances/CHG-005.toml\", children: [Text(\"CHG-005 acceptance\")] }, Text(\")\")]], [[Text(\"Statistical comparison protocol\")], [Text(\"§10.9\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S: CHG-016\")], [Text(\"–\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S (spec v0.10)\")]], [[Text(\"Held-out acceptance checks\")], [Text(\"§9.14\")], [Code(\"xtask/src/corpus/held_out.rs\"), Text(\" (\"), Text(\"`\"), Text(\"cargo xtask corpus held-out --kind architecture\")], [Text(\"check\"), Text(\"`\"), Text(\")\")], [Text(\"xtask 0.1.0 (CHG-004.6, CHG-005)\")], [Strong([Text(\"I\")]), Text(\": the runner exists and is tested on public controls (commitment check, link and escape refusal, opaque output). Not V: no held-out case has been run (DP-1.1b archive and DP-1.4 fixtures pending)\")], [Code(\"xtask/tests/held_out.rs\")], [Text(\"–\")], [Text(\"–\")]], [[Text(\"Review-efficacy audit with seeded changes\")], [Text(\"§11.4\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S: deferred (plan §10)\")], [Text(\"–\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S (spec v0.10)\")]], [[Text(\"Machine policy, evidence envelope, protected verifier\")], [Text(\"§11.7\")], [Code(\".rha/schemas/\"), Text(\", \"), Code(\"xtask/src/record_lint.rs\"), Text(\", \"), Code(\"tools/rha-verifier\")], [Text(\"xtask 0.1.0\")], [Strong([Text(\"I, proposed\")]), Text(\" for the implemented record/schema/fixture-model slice only: registered schemas, typed record linting, and fixture models exist. The full authenticated protected pipeline and evidence envelope remain S (DP-4.1); this is not a broad V or authenticity claim.\")], [Link { href: \"../evidence/CHG-007/stage-0/registered-lint-and-schema.txt\", children: [Text(\"registered lint and schema\")] }, Text(\"; \"), Link { href: \"../evidence/CHG-007/stage-0/registered-verifier.txt\", children: [Text(\"registered verifier\")] }, Text(\"; \"), Link { href: \"../xtask/tests/corpus/markdown-schema-supplement/registration.toml\", children: [Text(\"after-data optional markdown shape registration\")] }, Text(\", \"), Link { href: \"../evidence/CHG-007/closure/markdown-schema-controls.txt\", children: [Text(\"controls\")] }, Text(\" and \"), Link { href: \"../evidence/CHG-007/closure/markdown-schema-report-lint.txt\", children: [Text(\"real report lint\")] }], [Text(\"Implemented registered record/schema/fixture-model slice only; authenticated protected pipeline and full evidence envelope remain outside scope (DP-4.1)\")], [Text(\"Rust 1.98.1, Linux x86_64; local stage-0 records\")], [Text(\"S (spec v0.10)\")]], [[Text(\"Repository semantic model and five related contribution views\")], [Text(\"§§3.9, 8.4, 11.8\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S: CHG-021\")], [Text(\"–\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S (spec v0.10)\")]], [[Text(\"Human/Agent/Team mechanisms and actor-role records\")], [Text(\"§11.6\")], [Code(\".rha/tasks/\"), Text(\", \"), Code(\".rha/schemas/\"), Text(\", \"), Code(\"xtask/src/record_lint.rs\")], [Text(\"xtask 0.1.0 (CHG-019)\")], [Strong([Text(\"I, proposed\")]), Text(\" for typed task records and registered linting only; runtime actor-role authorization is not claimed.\")], [Link { href: \"../evidence/CHG-007/stage-0/registered-lint-and-schema.txt\", children: [Text(\"registered lint and schema\")] }], [Text(\"Typed task-record shape and registered linting; runtime actor-role authorization remains outside the claim\")], [Text(\"Rust 1.98.1, Linux x86_64; local stage-0 records\")], [Text(\"S (spec v0.10)\")]], [[Text(\"in-toto-based RHA verification predicate\")], [Text(\"§11.7.11\")], [Code(\"tools/rha-verifier\")], [Text(\"rha-verifier 0.1.0 (CHG-020)\")], [Text(\"S: the full authenticated in-toto/RHA envelope remains specified only; a fixture predicate is implemented for the registered fixture-model scope, without making a broad validator or authenticity claim.\")], [Link { href: \"../evidence/CHG-007/stage-0/registered-verifier.txt\", children: [Text(\"registered verifier\")] }], [Text(\"Registered fixture-predicate scope only; the full authenticated envelope and predicate pipeline remain outside scope (DP-4.1)\")], [Text(\"Rust 1.98.1, Linux x86_64; local stage-0 records\")], [Text(\"S (spec v0.10)\")]], [[Text(\"Impact/topology/context pilots\")], [Text(\"§§17.1–17.2\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S: deferred (plan §10)\")], [Text(\"–\")], [Text(\"–\")], [Text(\"–\")], [Text(\"S (spec v0.10)\")]]] }]",
    "headings": "[Heading { level: 1, text: \"Maturity ledger\", slug: \"maturity-ledger\", base_slug: \"maturity-ledger\", line: 1 }]",
    "markdown": "# Maturity ledger\n\nOne row per mechanism of spec §1.4. The Executor edits only **Proposed**; Kennedy edits **Accepted**; the spec's own §1.4 table changes only at a version bump. Maturity: **S** specified, **I** implemented (a named, versioned, retrievable artifact), **V** validated (implemented, and its stated conformance or adversarial tests passed, with an evidence record). A proposal names the artifact, version, evidence, checking scope, and environment; it is not a permanent badge.\n\nThe **Accepted** column starts with the values printed in spec v0.10 §1.4.\n\n| §1.4 mechanism | Spec § | Artifact path | Version | Proposed (Executor) | Evidence | Checking scope | Environment | Accepted (Kennedy, date, ref) |\n| --- | --- | --- | --- | --- | --- | --- | --- | --- |\n| Structural laws as review criteria | §4 | – | – | S | – | – | – | S (spec v0.10) |\n| Crate-graph checker, `cargo xtask architecture` | §6.13 | `xtask/src/{architecture.rs,metadata.rs,graph/}` | xtask 0.1.0 (CHG-003 to CHG-004.6) | **V (crate level), proposed**: the amended H4 run detects 22/22 with no unmatched finding and no alarm on a legitimate case; C13's accurate `adapter.foreign_core` warning is a registered fact under the DP-1.1c amendment Kennedy approved on 2026-09-20 **after the data**, disclosed on the ledger and in the record's amendment chain. Held-out cases not yet run (DP-1.1b as amended). V is Kennedy's to accept at DP-1.6 | [latest dirty H4](../evidence/h4-crate/20260923T040735Z-f915daaf8dc1-dirty.json); [amended H4](../evidence/h4-crate/20260922T194637Z-f13f2bd5df1d.json) at `f13f2bd` (the earlier amended run, 20260921T041705Z-f4dac8dc5710.json, predates the CHG-004.6.1 repair); the failed runs are kept: [delivery](../evidence/h4-crate/20260920T122638Z-eec489c71f25.json) and [baseline at the CHG-004 merge](../evidence/h4-crate/20260920T191132Z-434b7064e3f2.json); [CHG-004.6 determination](changes/CHG-004.6-c13-registration.md) | All 35 committed crate cases, exact witnesses plus one registered fact (C13), offline metadata and compiler R01; two documented holes; crate-scope check only (the declared module scope is tracked in the separate module row); held-out cases pending (DP-1.1b) | Rust 1.98.1, local WSL2 and the GitHub runner; clean subject and checker identity | V, crate level (DP-1.6, accepted under Kennedy's delegation, 2026-09-22; [CHG-005 acceptance](../.rha/acceptances/CHG-005.toml)) |\n| `cargo-generate` baseline template | §6.16.7 | – | – | S: deferred (plan §10) | – | – | – | S* (spec v0.10) |\n| Module-graph check inside a crate | §6.13 | `xtask/src/modules/`, `xtask/tests/module_corpus.rs` | xtask 0.1.0 (CHG-007) | **V, proposed** at registered source-based module scope: 17/17 violations detected, 0/6 alarms on legitimate cases, 3 expected misses, the live corpus passed, and 256 supplementary fixtures matched exactly. This scope does not claim macro expansion, include expansion, type inference, or transitive analysis. | [latest H4](../evidence/h4-module/20260923T040716Z-f915daaf8dc1.json); [CHG-007 change record](changes/CHG-007-module-level.md) | Registered source-based module directions only; source extraction does not cover macro expansion, include expansion, or type inference; transitive analysis is unavailable | Rust 1.98.1, Linux x86_64; local dirty-snapshot provenance; no authentication or held-out claim | S (spec v0.10) |\n| Ambient-effect deny list; `no_std` core option | §6.8 | `xtask/templates/core-clippy.toml` (111 entries), `xtask/src/clippy_template.rs` (rule `effect.core_clippy_template`, which also checks each core crate's crate-root `#![forbid(...)]`), `xtask/tests/clippy_corpus.rs` | xtask 0.1.0 (CHG-001, CHG-001.1 to CHG-001.3; CHG-007 source-guard repair) | **V (crate level), proposed** for the deny list: all 111 entries fail the build in fixtures (CHG-001), and four real core crates (`library`, `document`, `graph`, `site`) compile under the template with the crate-root `forbid`, which `effect.core_clippy_template` checks in L0 on every run. The source guard now evaluates Cargo production roots and syntax attributes (CHG-007 repair). The attribute escape of ADR-0002 rule 5 is closed by that `forbid`. `no_std` production option: S; [E1](../experiments/no-std-graph/result.md) validates import-only alloc adaptation and its direct-std negative control on the isolated resolver snapshot, with std-bearing dependencies retained | CHG-001 records (`evidence/w1-clippy/`); the P-A L0 record (`evidence/CHG-005/`); `cargo xtask architecture` on the product reports 0 errors | direct uses in Cargo production roots at crate level, including syntax attributes (CHG-007 repair); not transitive calls, not macro-generated calls, not paths missing from the list, not a build whose RUSTFLAGS or .cargo/config.toml lowers lint levels (ADR-0002 rules 5, 6, 8) | Clippy 0.1.98; local WSL2 and GitHub `ubuntu24` (the corpus runs in `L0.nextest` in both) | V, crate level, deny list only; `no_std` S (DP-1.6, accepted under Kennedy's delegation, 2026-09-22; [CHG-005 acceptance](../.rha/acceptances/CHG-005.toml)) |\n| Fast-lane CI configuration | §12.1 | `.rha/policy.toml` `[lanes.L0]`, `xtask/src/lanes.rs`, `.github/workflows/ci.yml` | xtask 0.1.0; tag `v0.10-m0` | **V, proposed**: the eight §12.1 checks run through one entry point on every PR and on main; CI run 35781193848 on main (72c5cfd) recorded all eight `passed`, with 151 tests selected | `evidence/ci/35781193848.json`; policy drift test `xtask/tests/policy_drift.rs` | L0 lane on this repository | local WSL2 workstation; GitHub `ubuntu-24.04` | V (DP-1.6, accepted under Kennedy's delegation, 2026-09-22; [CHG-005 acceptance](../.rha/acceptances/CHG-005.toml)) |\n| Statistical comparison protocol | §10.9 | – | – | S: CHG-016 | – | – | – | S (spec v0.10) |\n| Held-out acceptance checks | §9.14 | `xtask/src/corpus/held_out.rs` (`cargo xtask corpus held-out --kind architecture|check`) | xtask 0.1.0 (CHG-004.6, CHG-005) | **I**: the runner exists and is tested on public controls (commitment check, link and escape refusal, opaque output). Not V: no held-out case has been run (DP-1.1b archive and DP-1.4 fixtures pending) | `xtask/tests/held_out.rs` | – | – | I (DP-1.6, accepted under Kennedy's delegation, 2026-09-22; [CHG-005 acceptance](../.rha/acceptances/CHG-005.toml)) |\n| Review-efficacy audit with seeded changes | §11.4 | – | – | S: deferred (plan §10) | – | – | – | S (spec v0.10) |\n| Machine policy, evidence envelope, protected verifier | §11.7 | `.rha/schemas/`, `xtask/src/record_lint.rs`, `tools/rha-verifier` | xtask 0.1.0 | **I, proposed** for the implemented record/schema/fixture-model slice only: registered schemas, typed record linting, and fixture models exist. The full authenticated protected pipeline and evidence envelope remain S (DP-4.1); this is not a broad V or authenticity claim. | [registered lint and schema](../evidence/CHG-007/stage-0/registered-lint-and-schema.txt); [registered verifier](../evidence/CHG-007/stage-0/registered-verifier.txt); [after-data optional markdown shape registration](../xtask/tests/corpus/markdown-schema-supplement/registration.toml), [controls](../evidence/CHG-007/closure/markdown-schema-controls.txt) and [real report lint](../evidence/CHG-007/closure/markdown-schema-report-lint.txt) | Implemented registered record/schema/fixture-model slice only; authenticated protected pipeline and full evidence envelope remain outside scope (DP-4.1) | Rust 1.98.1, Linux x86_64; local stage-0 records | S (spec v0.10) |\n| Repository semantic model and five related contribution views | §§3.9, 8.4, 11.8 | – | – | S: CHG-021 | – | – | – | S (spec v0.10) |\n| Human/Agent/Team mechanisms and actor-role records | §11.6 | `.rha/tasks/`, `.rha/schemas/`, `xtask/src/record_lint.rs` | xtask 0.1.0 (CHG-019) | **I, proposed** for typed task records and registered linting only; runtime actor-role authorization is not claimed. | [registered lint and schema](../evidence/CHG-007/stage-0/registered-lint-and-schema.txt) | Typed task-record shape and registered linting; runtime actor-role authorization remains outside the claim | Rust 1.98.1, Linux x86_64; local stage-0 records | S (spec v0.10) |\n| in-toto-based RHA verification predicate | §11.7.11 | `tools/rha-verifier` | rha-verifier 0.1.0 (CHG-020) | S: the full authenticated in-toto/RHA envelope remains specified only; a fixture predicate is implemented for the registered fixture-model scope, without making a broad validator or authenticity claim. | [registered verifier](../evidence/CHG-007/stage-0/registered-verifier.txt) | Registered fixture-predicate scope only; the full authenticated envelope and predicate pipeline remain outside scope (DP-4.1) | Rust 1.98.1, Linux x86_64; local stage-0 records | S (spec v0.10) |\n| Impact/topology/context pilots | §§17.1–17.2 | – | – | S: deferred (plan §10) | – | – | – | S (spec v0.10) |\n"
  },
  "docs/observations/h3-json.md": {
    "body": "[Heading { level: 1, slug: \"h3-json-renderer-under-the-existing-port\", children: [Text(\"H3: JSON renderer under the existing port\")] }, Paragraph([Text(\"The adapter stage d2cc1cd793bbd820a130c9df1e120cc1c9f71e7f..bd708fd3277afcb80a3898b6a498ea3c010e0e4c adds adapter-json and app-cli wiring with an \"), Link { href: \"../../evidence/CHG-008/stage-1/h3-adapter-core-numstat.txt\", children: [Text(\"empty core numstat\")] }, Text(\". \"), Link { href: \"../../evidence/CHG-008/stage-1/h3-measurement.txt\", children: [Text(\"The measured commands and digests\")] }, Text(\" bind the endpoints; \"), Link { href: \"../../evidence/CHG-008/stage-1/h3-product-numstat.txt\", children: [Text(\"the product diff\")] }, Text(\" contains only the two predicted product components, including their tests. BDR-0006's W8 zero-core-change criterion is not refuted by this observation. Its longer history window remains open.\")]), Paragraph([Link { href: \"../../evidence/CHG-008/stage-1/h3-whole-core-numstat.txt\", children: [Text(\"The complete P-C-to-H3 core diff\")] }, Text(\", from 8e3037b5b4a342a3e2bec36af874246838c610b7, is not empty: the separately predeclared assurance stage repaired the owner contracts and unchanged-page count, and clarified graph/build API documentation. This complete measurement is retained alongside the isolated adapter measurement. The adapter did not require those core APIs to change.\")]), Paragraph([Text(\"The \"), Link { href: \"../../evidence/CHG-008/stage-1/pd-h3-detection.txt\", children: [Text(\"actual detection list\")] }, Text(\" contains NonDeterministic for the drifting renderer, Panicked for the panicking renderer and TocNotObserved for the TOC-dropping renderer. The same probe reports no violations for StubRenderer, HtmlRenderer and JsonRenderer. StubRenderer is an in-process fake; the other two are presentation adapters. The original contract failed the TOC case before the prerequisite repair, as recorded in \"), Link { href: \"../../evidence/CHG-008/stage-0/pd-assurance-negative.txt\", children: [Text(\"the stage-0 controls\")] }, Text(\".\")]), Paragraph([Link { href: \"../../evidence/CHG-008/stage-1/pd-json-registered-final.txt\", children: [Text(\"All 79 pre-registered JSON cases pass\")] }, Text(\": 75 adapter observations and four CLI scenarios. They cover exact decoded projections, search ordering/whitespace, clock-independent index data, fresh-index deletion, collision refusal and HTML/check compatibility. Supplemental controls observe deterministic bytes across constructor calls, exact post-value newline and zero sink mutation calls on collision. \"), Link { href: \"../../evidence/CHG-008/stage-1/pd-json-stage-gate.txt\", children: [Text(\"The stage gate\")] }, Text(\" passes 284 tests with no skips. Docs, architecture and scope pass.\")]), Paragraph([Text(\"The renderer's index is immutable configuration for one corpus. The composition root performs an additional pure analysis/assembly pass, then constructs a fresh renderer for each build. Its time and memory cost, constructor precondition and rejected alternatives are in \"), Link { href: \"../adr/BDR-0006-adapter-json-immutable-index.md\", children: [Text(\"BDR-0006\")] }, Text(\". The active assumption ledger records the third tested implementation only after the suite passed.\")]), Paragraph([Text(\"These are small descriptive observations of both halves of H3, with the prerequisite assurance repair explicitly separated. They are not a controlled study or a general proof of adapter substitutability. Registered corpora are unchanged; held-out runs remain Kennedy-owned and not_run. Assumption-ledger schema coverage remains outside the registered rha-lint families; its attempted invocation and direct TOML/path checks are disclosed in the change record.\")]), Paragraph([Text(\"The BDR's original O(n+e) estimate is explicitly qualified in its dated implementation note: current assembly uses a linear lookup per link. This observation claims an additional pipeline pass, not a proved linear-time cost.\")])]",
    "headings": "[Heading { level: 1, text: \"H3: JSON renderer under the existing port\", slug: \"h3-json-renderer-under-the-existing-port\", base_slug: \"h3-json-renderer-under-the-existing-port\", line: 1 }]",
    "markdown": "# H3: JSON renderer under the existing port\n\nThe adapter stage d2cc1cd793bbd820a130c9df1e120cc1c9f71e7f..bd708fd3277afcb80a3898b6a498ea3c010e0e4c adds adapter-json and app-cli wiring with an [empty core numstat](../../evidence/CHG-008/stage-1/h3-adapter-core-numstat.txt). [The measured commands and digests](../../evidence/CHG-008/stage-1/h3-measurement.txt) bind the endpoints; [the product diff](../../evidence/CHG-008/stage-1/h3-product-numstat.txt) contains only the two predicted product components, including their tests. BDR-0006's W8 zero-core-change criterion is not refuted by this observation. Its longer history window remains open.\n\n[The complete P-C-to-H3 core diff](../../evidence/CHG-008/stage-1/h3-whole-core-numstat.txt), from 8e3037b5b4a342a3e2bec36af874246838c610b7, is not empty: the separately predeclared assurance stage repaired the owner contracts and unchanged-page count, and clarified graph/build API documentation. This complete measurement is retained alongside the isolated adapter measurement. The adapter did not require those core APIs to change.\n\nThe [actual detection list](../../evidence/CHG-008/stage-1/pd-h3-detection.txt) contains NonDeterministic for the drifting renderer, Panicked for the panicking renderer and TocNotObserved for the TOC-dropping renderer. The same probe reports no violations for StubRenderer, HtmlRenderer and JsonRenderer. StubRenderer is an in-process fake; the other two are presentation adapters. The original contract failed the TOC case before the prerequisite repair, as recorded in [the stage-0 controls](../../evidence/CHG-008/stage-0/pd-assurance-negative.txt).\n\n[All 79 pre-registered JSON cases pass](../../evidence/CHG-008/stage-1/pd-json-registered-final.txt): 75 adapter observations and four CLI scenarios. They cover exact decoded projections, search ordering/whitespace, clock-independent index data, fresh-index deletion, collision refusal and HTML/check compatibility. Supplemental controls observe deterministic bytes across constructor calls, exact post-value newline and zero sink mutation calls on collision. [The stage gate](../../evidence/CHG-008/stage-1/pd-json-stage-gate.txt) passes 284 tests with no skips. Docs, architecture and scope pass.\n\nThe renderer's index is immutable configuration for one corpus. The composition root performs an additional pure analysis/assembly pass, then constructs a fresh renderer for each build. Its time and memory cost, constructor precondition and rejected alternatives are in [BDR-0006](../adr/BDR-0006-adapter-json-immutable-index.md). The active assumption ledger records the third tested implementation only after the suite passed.\n\nThese are small descriptive observations of both halves of H3, with the prerequisite assurance repair explicitly separated. They are not a controlled study or a general proof of adapter substitutability. Registered corpora are unchanged; held-out runs remain Kennedy-owned and not_run. Assumption-ledger schema coverage remains outside the registered rha-lint families; its attempted invocation and direct TOML/path checks are disclosed in the change record.\n\nThe BDR's original O(n+e) estimate is explicitly qualified in its dated implementation note: current assembly uses a linear lookup per link. This observation claims an additional pipeline pass, not a proved linear-time cost.\n"
  },
  "docs/orchestration-log.md": {
    "body": "[Heading { level: 1, slug: \"orchestration-log\", children: [Text(\"Orchestration log\")] }, Heading { level: 2, slug: \"atom-1--p-c-stage-0-chg-0191\", children: [Text(\"Atom #1 — P-C stage 0 (CHG-019.1)\")] }, List { start: None, items: [[Text(\"Tiers: coordination/verification in the root and Sol max; independent Astra xhigh corpus generation; Luna high implementation proposals. Those roles fit the work; wire labels should have been fixed before generation.\")], [Text(\"Reasoning that did not change the outcome: repeated discussion of historical variants after the mechanical census already identified them.\")], [Text(\"Mistakes caught: a guessed full-SHA fixture filename, missing null-vs-absent handling in a recipe harness, and a raw-string patch anchor. SOURCE-INDEX lookup and reading exact edit context prevent these.\")], [Text(\"Next rule: finish the contract, including diagnostic spelling, before launching a generator; use a unique log path per attempt.\")]] }, Heading { level: 2, slug: \"atom-2--p-c-stage-1-extraction\", children: [Text(\"Atom #2 — P-C stage 1 (extraction)\")] }, List { start: None, items: [[Paragraph([Text(\"Tiers: Luna high for decided patch proposals; root and Sol max for verification. Directory-resolution judgment belonged in verification, as used.\")])], [Paragraph([Text(\"Reasoning that did not change the outcome: broad namespace speculation before running the fixed differential corpus.\")])], [Paragraph([Text(\"Mistakes caught: missing path visitor, a macro scan that did not advance, incorrect test flags, and an assumed syn-2 tuple shape. Compile against pinned APIs, test progress/termination, and probe language layout with rustc.\")])], [Paragraph([Text(\"Next rule: keep write packets narrow and interfaces explicit; run registered comparisons early, then add concrete regression controls. Scope searches to source directories after corpus snapshots exist.\")])], [Paragraph([Text(\"Applied correction: inspect a returned exit status before queuing a dependent gate; an explicit approved layout entry needs both a positive control and the retained generic negative example.\")])]] }, Heading { level: 2, slug: \"atom-3--p-c-stage-2-rules-and-integration\", children: [Text(\"Atom #3 — P-C stage 2 (rules and integration)\")] }, List { start: None, items: [[Text(\"Tiers: Luna high for bounded patches; Sol max for independent rule review; root for integration and verification. The distribution fit the work.\")], [Text(\"Reasoning that did not change the outcome: debating source identity before exercising a custom Cargo target; the concrete CLI control settled it.\")], [Text(\"Mistakes caught: cycle evidence recomputation dropped parallel heuristic paths; the test harness forgot observed-edge deduplication; the Clippy guard assumed a conventional root and scanned comments as attributes. Reuse the observed edge map, derive roots from metadata, and parse syntax.\")], [Text(\"Next rule: run the frozen corpus early; normalize only documented representation differences, never expected facts. An explicit module-only CLI avoids mutating registered fixtures to satisfy unrelated crate checks.\")]] }, Heading { level: 2, slug: \"atom-4--p-c-stage-3-corpus-and-evidence\", children: [Text(\"Atom #4 — P-C stage 3 (corpus and evidence)\")] }, List { start: None, items: [[Text(\"Tiers: Luna high for decided harness patches, Sol max for independent review, root for tool-backed verification. The same split should be retained.\")], [Text(\"Reasoning that did not change the outcome: speculative interface and provenance discussion before comparing one real producer report with its consumer; both paths and digest prefixes were directly observable.\")], [Text(\"Mistakes caught: unordered witness arrays, a missing-oracle fallback, a filename-length bug, incomplete per-case hashes, a stale notice removed from a projection, and hashing a replaced executable pathname. Real positive controls and deliberate negative mutations caught each.\")], [Text(\"Next rule: normalize a producer/consumer contract from one actual report before expanding a harness; preserve registered finding scores and place stronger extraction checks in their separate registered assurance measure. Record the observed CLI version per generation rather than assuming it remains fixed during a long run.\")]] }, Heading { level: 2, slug: \"atom-5--p-c-stage-4-site-and-maturity\", children: [Text(\"Atom #5 — P-C stage 4 (site and maturity)\")] }, List { start: None, items: [[Text(\"Tiers: Luna high for decided documentation, root for exact observed-report checks. This was the appropriate split.\")], [Text(\"Reasoning that did not change the outcome: considering broader maturity claims where the existing evidence only supports a scoped proposal.\")], [Text(\"Mistakes caught: a BDR draft described acceptance too broadly; its status now explicitly accepts only the delegated experiment refutation criterion. A mechanical comparison preserves every Accepted maturity cell.\")], [Text(\"Next rule: keep actual enforcement, proposed maturity, and acceptance status distinct; record the experiment hypothesis and boundary before creating its package.\")]] }, Heading { level: 2, slug: \"atom-6--p-c-e1\", children: [Text(\"Atom #6 — P-C E1\")] }, List { start: None, items: [[Text(\"Tiers: Luna high for the fixed snapshot/verifier proposal; root for measured compiler and byte-identity checks. The experiment needed measurement, not more design expansion.\")], [Text(\"Reasoning that did not change the outcome: repeatedly considering future P-D implementations while this small experiment was already decided.\")], [Text(\"Mistakes caught: a proposal worker used jj status under old global guidance; it failed without a repository change and is disclosed. Future prompts explicitly forbid JJ, including status. Shell-script transport also tripped on Markdown backticks before execution; use patch files or unambiguous quoting.\")], [Text(\"Next rule: inspect and close the current gate before reasoning about a future stage. Keep the actual-type snapshot, direct-std check and transitive-std limitation together in the result.\")]] }, Heading { level: 2, slug: \"atom-7--p-c-record-closure\", children: [Text(\"Atom #7 — P-C record closure\")] }, List { start: None, items: [[Text(\"Tiers: Luna high for deterministic shape and storage edits; root for source/record comparisons. The supplemental generator was separate and forbidden implementation access.\")], [Text(\"Reasoning that did not change the outcome: discussing general schema evolution before comparing the actual producer's two new fields.\")], [Text(\"Mistakes caught: an H5 filename overwrote a prior observation; the original census could not describe newer producer fields; a proposal invented Python stat_result methods. Repeated real CLI runs and immediate projection execution distinguished all three.\")], [Text(\"Next rule: lint a freshly produced record from every supported producer before declaring schema closure; preserve original corpus grades and register additive shape controls before code.\")]] }, Heading { level: 2, slug: \"atom-8--p-c-ci-portability-repair\", children: [Text(\"Atom #8 — P-C CI portability repair\")] }, List { start: None, items: [[Text(\"Tiers: Luna high for the decided path mapping; root for CI diagnosis and positive/negative execution; separate validator reviews the repair diff.\")], [Text(\"Reasoning that did not change the outcome: none beyond locating the missing registered input.\")], [Text(\"Mistake caught: locally present generation staging hid a clean-checkout dependency. The CI artifact named the exact missing source.\")], [Text(\"Next rule: when a registration cites generated inputs, verify its committed archive from a filesystem layout without staging artifacts before the first push.\")]] }, Heading { level: 2, slug: \"atom-9--p-d-assurance-prerequisites\", children: [Text(\"Atom #9 — P-D assurance prerequisites\")] }, List { start: None, items: [[Text(\"Tiers: Luna high for fixed controls and repair proposal; root for running negative/positive checks and preserving stage boundaries.\")], [Text(\"Reasoning that did not change the outcome: considering later feature internals while the prerequisite gate was pending.\")], [Text(\"Mistakes caught: source inspection suggested five failures; actual execution confirmed all five and preserved three valid controls. A patch-fence extraction error was rejected before any file changed.\")], [Text(\"Next rule: run staged probes against existing built crates when a read-only review holds the branch, then apply the tested controls after the review closes; keep adapter and prerequisite measurements distinct.\")]] }, Heading { level: 2, slug: \"atom-10--json-adapter-and-h3\", children: [Text(\"Atom #10 — JSON adapter and H3\")] }, List { start: None, items: [[Text(\"Tiers: Luna high wrote the frozen independent reference/cases and disjoint implementation proposals; separate verification reviewed the grader; root ran all measurements and controls.\")], [Text(\"Reasoning that did not change the outcome: repeatedly expanding future feature designs while this stage was awaiting a bounded repair. The parser characterization was useful and changed the later transclusion design.\")], [Text(\"Mistakes caught: an invented prompt filename, copied hash typos, guessed type exports, and a stale deliberately faulty library after source restoration. Source reads, compiler output and paired controls caught them before a push.\")], [Text(\"Next rule: derive inventories from their digest-bound file instead of copying them; give exact paths on separate lines; rebuild restored source before linking any standalone probe. Keep source-based API characterization ahead of corpus registration.\")]] }]",
    "headings": "[Heading { level: 1, text: \"Orchestration log\", slug: \"orchestration-log\", base_slug: \"orchestration-log\", line: 1 }, Heading { level: 2, text: \"Atom #1 — P-C stage 0 (CHG-019.1)\", slug: \"atom-1--p-c-stage-0-chg-0191\", base_slug: \"atom-1--p-c-stage-0-chg-0191\", line: 3 }, Heading { level: 2, text: \"Atom #2 — P-C stage 1 (extraction)\", slug: \"atom-2--p-c-stage-1-extraction\", base_slug: \"atom-2--p-c-stage-1-extraction\", line: 9 }, Heading { level: 2, text: \"Atom #3 — P-C stage 2 (rules and integration)\", slug: \"atom-3--p-c-stage-2-rules-and-integration\", base_slug: \"atom-3--p-c-stage-2-rules-and-integration\", line: 17 }, Heading { level: 2, text: \"Atom #4 — P-C stage 3 (corpus and evidence)\", slug: \"atom-4--p-c-stage-3-corpus-and-evidence\", base_slug: \"atom-4--p-c-stage-3-corpus-and-evidence\", line: 23 }, Heading { level: 2, text: \"Atom #5 — P-C stage 4 (site and maturity)\", slug: \"atom-5--p-c-stage-4-site-and-maturity\", base_slug: \"atom-5--p-c-stage-4-site-and-maturity\", line: 29 }, Heading { level: 2, text: \"Atom #6 — P-C E1\", slug: \"atom-6--p-c-e1\", base_slug: \"atom-6--p-c-e1\", line: 35 }, Heading { level: 2, text: \"Atom #7 — P-C record closure\", slug: \"atom-7--p-c-record-closure\", base_slug: \"atom-7--p-c-record-closure\", line: 41 }, Heading { level: 2, text: \"Atom #8 — P-C CI portability repair\", slug: \"atom-8--p-c-ci-portability-repair\", base_slug: \"atom-8--p-c-ci-portability-repair\", line: 47 }, Heading { level: 2, text: \"Atom #9 — P-D assurance prerequisites\", slug: \"atom-9--p-d-assurance-prerequisites\", base_slug: \"atom-9--p-d-assurance-prerequisites\", line: 53 }, Heading { level: 2, text: \"Atom #10 — JSON adapter and H3\", slug: \"atom-10--json-adapter-and-h3\", base_slug: \"atom-10--json-adapter-and-h3\", line: 59 }]",
    "markdown": "# Orchestration log\n\n## Atom #1 — P-C stage 0 (CHG-019.1)\n- Tiers: coordination/verification in the root and Sol max; independent Astra xhigh corpus generation; Luna high implementation proposals. Those roles fit the work; wire labels should have been fixed before generation.\n- Reasoning that did not change the outcome: repeated discussion of historical variants after the mechanical census already identified them.\n- Mistakes caught: a guessed full-SHA fixture filename, missing null-vs-absent handling in a recipe harness, and a raw-string patch anchor. SOURCE-INDEX lookup and reading exact edit context prevent these.\n- Next rule: finish the contract, including diagnostic spelling, before launching a generator; use a unique log path per attempt.\n\n## Atom #2 — P-C stage 1 (extraction)\n- Tiers: Luna high for decided patch proposals; root and Sol max for verification. Directory-resolution judgment belonged in verification, as used.\n- Reasoning that did not change the outcome: broad namespace speculation before running the fixed differential corpus.\n- Mistakes caught: missing path visitor, a macro scan that did not advance, incorrect test flags, and an assumed syn-2 tuple shape. Compile against pinned APIs, test progress/termination, and probe language layout with rustc.\n- Next rule: keep write packets narrow and interfaces explicit; run registered comparisons early, then add concrete regression controls. Scope searches to source directories after corpus snapshots exist.\n\n- Applied correction: inspect a returned exit status before queuing a dependent gate; an explicit approved layout entry needs both a positive control and the retained generic negative example.\n\n## Atom #3 — P-C stage 2 (rules and integration)\n- Tiers: Luna high for bounded patches; Sol max for independent rule review; root for integration and verification. The distribution fit the work.\n- Reasoning that did not change the outcome: debating source identity before exercising a custom Cargo target; the concrete CLI control settled it.\n- Mistakes caught: cycle evidence recomputation dropped parallel heuristic paths; the test harness forgot observed-edge deduplication; the Clippy guard assumed a conventional root and scanned comments as attributes. Reuse the observed edge map, derive roots from metadata, and parse syntax.\n- Next rule: run the frozen corpus early; normalize only documented representation differences, never expected facts. An explicit module-only CLI avoids mutating registered fixtures to satisfy unrelated crate checks.\n\n## Atom #4 — P-C stage 3 (corpus and evidence)\n- Tiers: Luna high for decided harness patches, Sol max for independent review, root for tool-backed verification. The same split should be retained.\n- Reasoning that did not change the outcome: speculative interface and provenance discussion before comparing one real producer report with its consumer; both paths and digest prefixes were directly observable.\n- Mistakes caught: unordered witness arrays, a missing-oracle fallback, a filename-length bug, incomplete per-case hashes, a stale notice removed from a projection, and hashing a replaced executable pathname. Real positive controls and deliberate negative mutations caught each.\n- Next rule: normalize a producer/consumer contract from one actual report before expanding a harness; preserve registered finding scores and place stronger extraction checks in their separate registered assurance measure. Record the observed CLI version per generation rather than assuming it remains fixed during a long run.\n\n## Atom #5 — P-C stage 4 (site and maturity)\n- Tiers: Luna high for decided documentation, root for exact observed-report checks. This was the appropriate split.\n- Reasoning that did not change the outcome: considering broader maturity claims where the existing evidence only supports a scoped proposal.\n- Mistakes caught: a BDR draft described acceptance too broadly; its status now explicitly accepts only the delegated experiment refutation criterion. A mechanical comparison preserves every Accepted maturity cell.\n- Next rule: keep actual enforcement, proposed maturity, and acceptance status distinct; record the experiment hypothesis and boundary before creating its package.\n\n## Atom #6 — P-C E1\n- Tiers: Luna high for the fixed snapshot/verifier proposal; root for measured compiler and byte-identity checks. The experiment needed measurement, not more design expansion.\n- Reasoning that did not change the outcome: repeatedly considering future P-D implementations while this small experiment was already decided.\n- Mistakes caught: a proposal worker used jj status under old global guidance; it failed without a repository change and is disclosed. Future prompts explicitly forbid JJ, including status. Shell-script transport also tripped on Markdown backticks before execution; use patch files or unambiguous quoting.\n- Next rule: inspect and close the current gate before reasoning about a future stage. Keep the actual-type snapshot, direct-std check and transitive-std limitation together in the result.\n\n## Atom #7 — P-C record closure\n- Tiers: Luna high for deterministic shape and storage edits; root for source/record comparisons. The supplemental generator was separate and forbidden implementation access.\n- Reasoning that did not change the outcome: discussing general schema evolution before comparing the actual producer's two new fields.\n- Mistakes caught: an H5 filename overwrote a prior observation; the original census could not describe newer producer fields; a proposal invented Python stat_result methods. Repeated real CLI runs and immediate projection execution distinguished all three.\n- Next rule: lint a freshly produced record from every supported producer before declaring schema closure; preserve original corpus grades and register additive shape controls before code.\n\n## Atom #8 — P-C CI portability repair\n- Tiers: Luna high for the decided path mapping; root for CI diagnosis and positive/negative execution; separate validator reviews the repair diff.\n- Reasoning that did not change the outcome: none beyond locating the missing registered input.\n- Mistake caught: locally present generation staging hid a clean-checkout dependency. The CI artifact named the exact missing source.\n- Next rule: when a registration cites generated inputs, verify its committed archive from a filesystem layout without staging artifacts before the first push.\n\n## Atom #9 — P-D assurance prerequisites\n- Tiers: Luna high for fixed controls and repair proposal; root for running negative/positive checks and preserving stage boundaries.\n- Reasoning that did not change the outcome: considering later feature internals while the prerequisite gate was pending.\n- Mistakes caught: source inspection suggested five failures; actual execution confirmed all five and preserved three valid controls. A patch-fence extraction error was rejected before any file changed.\n- Next rule: run staged probes against existing built crates when a read-only review holds the branch, then apply the tested controls after the review closes; keep adapter and prerequisite measurements distinct.\n\n## Atom #10 — JSON adapter and H3\n- Tiers: Luna high wrote the frozen independent reference/cases and disjoint implementation proposals; separate verification reviewed the grader; root ran all measurements and controls.\n- Reasoning that did not change the outcome: repeatedly expanding future feature designs while this stage was awaiting a bounded repair. The parser characterization was useful and changed the later transclusion design.\n- Mistakes caught: an invented prompt filename, copied hash typos, guessed type exports, and a stale deliberately faulty library after source restoration. Source reads, compiler output and paired controls caught them before a push.\n- Next rule: derive inventories from their digest-bound file instead of copying them; give exact paths on separate lines; rebuild restored source before linking any standalone probe. Keep source-based API characterization ahead of corpus registration.\n"
  },
  "docs/proposals/spec-v0.11.md": {
    "body": "[Heading { level: 1, slug: \"proposed-changes-to-the-specification-v010-to-v011\", children: [Text(\"Proposed changes to the specification, v0.10 to v0.11\")] }, Paragraph([Text(\"A queue, not a decision. Every row is a proposal Kennedy accepts, amends, or drops at the version bump; until then \"), Code(\"docs/spec/rha-spec-v0.10.md\"), Text(\" stands as written.\")]), Heading { level: 2, slug: \"why-this-file-exists\", children: [Text(\"Why this file exists\")] }, Paragraph([Text(\"Spec §11.7.1 gives each fact one approved owner. The specification owns rationale and normative principles; a decision record owns the trade-off and the boundary assumption; an evidence record owns the observation; machine policy owns the parameter. Duplicating an observation into the specification creates a second editable copy that goes stale when the toolchain moves, which is the defect §12.1 records against v0.7 for lane membership.\")]), Paragraph([Text(\"So the working rule in this repository is:\")]), List { start: Some(1), items: [[Strong([Text(\"A work item records findings in a decision record and evidence, and does not edit the specification.\")]), Text(\" A change must not rewrite the document that judges it, for the same reason §11.5 forbids a change from redefining its own policy.\")], [Strong([Text(\"Findings that bear on a normative rule or a maturity row are appended here\")]), Text(\", which is outside \"), Code(\"docs/spec/\"), Text(\" on purpose, so appending needs no protected-surface approval while the specification itself keeps one. The rows follow the shape of the specification's own change-log appendices: what the current text says, what the finding is, the proposed delta, the section, and the evidence.\")], [Strong([Text(\"The specification changes in one work item per version bump\")]), Text(\", carrying nothing else, so the diff is reviewable and the maturity table moves with evidence beside it, as Law 8 requires.\")], [Strong([Text(\"A claim the specification makes about this repository gets a check.\")]), Text(\" \"), Code(\"xtask/tests/policy_drift.rs\"), Text(\" already pins §12.1's command block to \"), Code(\".rha/policy.toml\"), Text(\". Untested claims are the ones that rot.\")]] }, Paragraph([Text(\"Proposals below cite the evidence that supports them. A row with no evidence record is not ready.\")]), Heading { level: 2, slug: \"queue\", children: [Text(\"Queue\")] }, Table { align: [None, None, None, None, None, None], head: [[Text(\"#\")], [Text(\"Section\")], [Text(\"What v0.10 says\")], [Text(\"Finding\")], [Text(\"Proposed change\")], [Text(\"Evidence\")]], rows: [[[Text(\"1\")], [Text(\"§6.8\")], [Text(\"The illustrative \"), Code(\"clippy.toml\"), Text(\" tells the implementer to verify how the pinned Clippy locates and merges configuration files.\")], [Text(\"Verified for Clippy 0.1.98: each crate is configured by the first file found from its \"), Code(\"CARGO_MANIFEST_DIR\"), Text(\" upward, and files are never merged.\")], [Text(\"State the consequence normatively: a core crate's own configuration replaces the root file entirely, so it repeats every root setting it still wants, and a core crate's deny list does not reach a sibling adapter.\")], [WikiLink { index: 0, children: [Text(\"adr/ADR-0002-clippy-config-discovery\")] }, Text(\" rules 1 and 2; \"), Code(\"evidence/w1-clippy/*/2.txt\"), Text(\", \"), Code(\"3.txt\"), Text(\", \"), Code(\"3b.txt\"), Text(\", \"), Code(\"3c.txt\")]], [[Text(\"2\")], [Text(\"§6.8\")], [Text(\"Silent on a \"), Code(\".clippy.toml\"), Text(\" beside a \"), Code(\"clippy.toml\"), Text(\".\")], [Text(\"The dotfile wins, with a warning that does not change the exit status, so a deny list can be replaced by a file nobody reviewed.\")], [Text(\"Add the case to the Hole column, and require the crate-graph check to report the pair.\")], [Text(\"ADR-0002 rule 3; \"), Code(\"evidence/w1-clippy/*/5.txt\")]], [[Text(\"3\")], [Text(\"§6.8\")], [Text(\"The deny-list example lists types and methods without saying how a type entry behaves.\")], [Text(\"A type entry fires where the type is \"), Strong([Text(\"named\")]), Text(\", not where a value of it is used, so a value a dependency returns escapes it.\")], [Text(\"Say that a listed type carries its effectful methods beside it.\")], [Text(\"ADR-0002 rule 7; \"), Code(\"evidence/w1-clippy/*/4.txt\")]], [[Text(\"4\")], [Text(\"§6.4, §6.8\")], [Text(\"§6.4 is the only lint policy, at \"), Code(\"deny\"), Text(\". Neither section discusses \"), Code(\"forbid\"), Text(\".\")], [Text(\"A workspace lint table cannot \"), Code(\"forbid\"), Text(\" these lints where any macro expands to a group allow: \"), Code(\"clap\"), Text(\"'s derives emit \"), Code(\"#[allow(clippy::style)]\"), Text(\", which is then \"), Code(\"E0453\"), Text(\". A \"), Code(\"serde\"), Text(\" derive is unaffected.\")], [Text(\"Record that a core crate closes the attribute escape at its own crate root, and that a workspace-wide \"), Code(\"forbid\"), Text(\" is refused by ordinary derive macros.\")], [Text(\"ADR-0002 rule 8; \"), Code(\"evidence/w1-clippy/*/7a.txt\"), Text(\", \"), Code(\"7b.txt\"), Text(\", \"), Code(\"7c.txt\")]], [[Text(\"5\")], [Text(\"§6.8, §11.5\")], [Text(\"The Hole column now says a lint attribute or \"), Code(\"--cap-lints\"), Text(\" can lower the level. §11.5 does not name build configuration as a protected surface.\")], [Code(\"--cap-lints=warn\"), Text(\", from \"), Code(\"RUSTFLAGS\"), Text(\" or from any \"), Code(\".cargo/config.toml\"), Text(\" above the build, lowers every level at once, including \"), Code(\"forbid\"), Text(\". An \"), Code(\"-A\"), Text(\" flag does not survive \"), Code(\"forbid\"), Text(\".\")], [Text(\"Make it normative in §11.5: whatever can set lint levels for the build is part of the protected surface. This repository already protects \"), Code(\".cargo/config.toml\"), Text(\".\")], [Text(\"ADR-0002 rules 5 and 6; \"), Code(\"evidence/w1-clippy/*/6e.txt\"), Text(\", \"), Code(\"6l.txt\"), Text(\", \"), Code(\"6m.txt\"), Text(\", \"), Code(\"6n.txt\")]], [[Text(\"6\")], [Text(\"§1.4\")], [Text(\"The ambient-effect deny list and the \"), Code(\"no_std\"), Text(\" core option share one row at \"), Strong([Text(\"S\")]), Text(\".\")], [Text(\"The deny list has an artifact and passing tests; the \"), Code(\"no_std\"), Text(\" option has neither. They graduate on different evidence.\")], [Text(\"Split the row, and promote the deny list to the maturity \"), Code(\"docs/maturity.md\"), Text(\" proposes, with its checking scope.\")], [WikiLink { index: 1, children: [Text(\"maturity\")] }, Text(\"; the records it cites\")]], [[Text(\"7\")], [Text(\"§12.1, CONTRIBUTING \\\"Verify\\\"\")], [Text(\"A contributor runs the lane, then regenerates docs after changing \"), Code(\".rha/**\"), Text(\" or \"), Code(\"evidence/**\"), Text(\". Nothing says to re-run the lane.\")], [Code(\"cargo xtask docs\"), Text(\" writes tracked files, so a record taken before it describes a tree that no longer exists. CHG-002 passed \"), Code(\"L0.typos\"), Text(\" locally and failed it in CI on a digest the docs regeneration had introduced after the local run.\")], [Text(\"Say that the lane runs last, after every generated file is written, or have \"), Code(\"cargo xtask ci\"), Text(\" fail when \"), Code(\"cargo xtask docs --check\"), Text(\" reports a stale file.\")], [Text(\"CI run 35478425099; \"), Code(\"docs/changes/CHG-002-corpus-pre-registration.md\"), Text(\", repair attempt 2\")]], [[Text(\"8\")], [Text(\"§11.5, §11.7.6\")], [Text(\"A controlled transition is the mechanism for a policy migration: the change that alters policy is judged under the policy it replaces, and the transition is recorded.\")], [Code(\".rha/policy.toml [acceptance.bootstrap]\"), Text(\" labels every acceptance of the bootstrap period \"), Code(\"kind = \\\"controlled_transition\\\"\"), Text(\", including ordinary changes that touch no policy (the CHG-002.2 record; CHG-003's to come). The label borrows a §11.5 term for a different thing, developmental human acceptance outside the formal model as \"), Code(\"docs/threat-model.md\"), Text(\" discloses, and read strictly it implies a third \"), Code(\"MergeAllowed\"), Text(\" path.\")], [Text(\"Name the bootstrap case: an acceptance kind for a period in which named predicates cannot hold, carrying \"), Code(\"merge_allowed = false\"), Text(\", an expiry, and no claim of conformance; keep \\\"controlled transition\\\" for policy migrations. In this repository that is a policy edit only Kennedy makes.\")], [Code(\".rha/acceptances/CHG-002.2.toml [disposition]\"), Text(\"; pull request 5 threads r4055717571 and r4056593886; Codex's audit of the review history, 2026-09-20\")]], [[Text(\"9\")], [Text(\"§11.7.11\")], [Text(\"A task record's provenance lists instruction sources with digests, and the evidence record carries the agent context.\")], [Text(\"A digest entered by hand goes stale when the file changes later on the same branch, and \"), Code(\"xtask ci\"), Text(\" copies \"), Code(\"agent_context.provenance\"), Text(\" from the task record instead of hashing the file, so five evidence records echo a wrong value (D-002.2.1).\")], [Text(\"Require that a cited instruction source that is a tracked file be hashed by the record producer at the subject revision, and that the record lint (CHG-019) reject a mismatch; a hand-entered digest is for a source outside the tree.\")], [Code(\".rha/acceptances/CHG-002.2.toml\"), Text(\" D-002.2.1; \"), Code(\"evidence/CHG-002.2/*.json\"), Text(\" \"), Code(\"agent_context.provenance.instruction_sources\"), Text(\"; pull request 5 thread r4056593889\")]]] }, Heading { level: 2, slug: \"not-proposed\", children: [Text(\"Not proposed\")] }, Paragraph([Text(\"Kept out on purpose, so nobody re-derives them as gaps.\")]), List { start: None, items: [[Text(\"The 111 deny-list entries themselves. The specification's list is illustrative and says so; the template owns the real one.\")], [Text(\"Anything about Clippy versions other than 0.1.98. One measurement is one measurement.\")], [Text(\"The \"), Code(\"xtask\"), Text(\" rule ids and the \"), Code(\"Problem\"), Text(\" variants of \"), Code(\"effect.core_clippy_template\"), Text(\". Those belong to the implementation, and §6.13 already names the checker.\")]] }]",
    "headings": "[Heading { level: 1, text: \"Proposed changes to the specification, v0.10 to v0.11\", slug: \"proposed-changes-to-the-specification-v010-to-v011\", base_slug: \"proposed-changes-to-the-specification-v010-to-v011\", line: 1 }, Heading { level: 2, text: \"Why this file exists\", slug: \"why-this-file-exists\", base_slug: \"why-this-file-exists\", line: 5 }, Heading { level: 2, text: \"Queue\", slug: \"queue\", base_slug: \"queue\", line: 18 }, Heading { level: 2, text: \"Not proposed\", slug: \"not-proposed\", base_slug: \"not-proposed\", line: 32 }]",
    "markdown": "# Proposed changes to the specification, v0.10 to v0.11\n\nA queue, not a decision. Every row is a proposal Kennedy accepts, amends, or drops at the version bump; until then `docs/spec/rha-spec-v0.10.md` stands as written.\n\n## Why this file exists\n\nSpec §11.7.1 gives each fact one approved owner. The specification owns rationale and normative principles; a decision record owns the trade-off and the boundary assumption; an evidence record owns the observation; machine policy owns the parameter. Duplicating an observation into the specification creates a second editable copy that goes stale when the toolchain moves, which is the defect §12.1 records against v0.7 for lane membership.\n\nSo the working rule in this repository is:\n\n1. **A work item records findings in a decision record and evidence, and does not edit the specification.** A change must not rewrite the document that judges it, for the same reason §11.5 forbids a change from redefining its own policy.\n2. **Findings that bear on a normative rule or a maturity row are appended here**, which is outside `docs/spec/` on purpose, so appending needs no protected-surface approval while the specification itself keeps one. The rows follow the shape of the specification's own change-log appendices: what the current text says, what the finding is, the proposed delta, the section, and the evidence.\n3. **The specification changes in one work item per version bump**, carrying nothing else, so the diff is reviewable and the maturity table moves with evidence beside it, as Law 8 requires.\n4. **A claim the specification makes about this repository gets a check.** `xtask/tests/policy_drift.rs` already pins §12.1's command block to `.rha/policy.toml`. Untested claims are the ones that rot.\n\nProposals below cite the evidence that supports them. A row with no evidence record is not ready.\n\n## Queue\n\n| # | Section | What v0.10 says | Finding | Proposed change | Evidence |\n| --- | --- | --- | --- | --- | --- |\n| 1 | §6.8 | The illustrative `clippy.toml` tells the implementer to verify how the pinned Clippy locates and merges configuration files. | Verified for Clippy 0.1.98: each crate is configured by the first file found from its `CARGO_MANIFEST_DIR` upward, and files are never merged. | State the consequence normatively: a core crate's own configuration replaces the root file entirely, so it repeats every root setting it still wants, and a core crate's deny list does not reach a sibling adapter. | [[adr/ADR-0002-clippy-config-discovery]] rules 1 and 2; `evidence/w1-clippy/*/2.txt`, `3.txt`, `3b.txt`, `3c.txt` |\n| 2 | §6.8 | Silent on a `.clippy.toml` beside a `clippy.toml`. | The dotfile wins, with a warning that does not change the exit status, so a deny list can be replaced by a file nobody reviewed. | Add the case to the Hole column, and require the crate-graph check to report the pair. | ADR-0002 rule 3; `evidence/w1-clippy/*/5.txt` |\n| 3 | §6.8 | The deny-list example lists types and methods without saying how a type entry behaves. | A type entry fires where the type is **named**, not where a value of it is used, so a value a dependency returns escapes it. | Say that a listed type carries its effectful methods beside it. | ADR-0002 rule 7; `evidence/w1-clippy/*/4.txt` |\n| 4 | §6.4, §6.8 | §6.4 is the only lint policy, at `deny`. Neither section discusses `forbid`. | A workspace lint table cannot `forbid` these lints where any macro expands to a group allow: `clap`'s derives emit `#[allow(clippy::style)]`, which is then `E0453`. A `serde` derive is unaffected. | Record that a core crate closes the attribute escape at its own crate root, and that a workspace-wide `forbid` is refused by ordinary derive macros. | ADR-0002 rule 8; `evidence/w1-clippy/*/7a.txt`, `7b.txt`, `7c.txt` |\n| 5 | §6.8, §11.5 | The Hole column now says a lint attribute or `--cap-lints` can lower the level. §11.5 does not name build configuration as a protected surface. | `--cap-lints=warn`, from `RUSTFLAGS` or from any `.cargo/config.toml` above the build, lowers every level at once, including `forbid`. An `-A` flag does not survive `forbid`. | Make it normative in §11.5: whatever can set lint levels for the build is part of the protected surface. This repository already protects `.cargo/config.toml`. | ADR-0002 rules 5 and 6; `evidence/w1-clippy/*/6e.txt`, `6l.txt`, `6m.txt`, `6n.txt` |\n| 6 | §1.4 | The ambient-effect deny list and the `no_std` core option share one row at **S**. | The deny list has an artifact and passing tests; the `no_std` option has neither. They graduate on different evidence. | Split the row, and promote the deny list to the maturity `docs/maturity.md` proposes, with its checking scope. | [[maturity]]; the records it cites |\n| 7 | §12.1, CONTRIBUTING \"Verify\" | A contributor runs the lane, then regenerates docs after changing `.rha/**` or `evidence/**`. Nothing says to re-run the lane. | `cargo xtask docs` writes tracked files, so a record taken before it describes a tree that no longer exists. CHG-002 passed `L0.typos` locally and failed it in CI on a digest the docs regeneration had introduced after the local run. | Say that the lane runs last, after every generated file is written, or have `cargo xtask ci` fail when `cargo xtask docs --check` reports a stale file. | CI run 35478425099; `docs/changes/CHG-002-corpus-pre-registration.md`, repair attempt 2 |\n| 8 | §11.5, §11.7.6 | A controlled transition is the mechanism for a policy migration: the change that alters policy is judged under the policy it replaces, and the transition is recorded. | `.rha/policy.toml [acceptance.bootstrap]` labels every acceptance of the bootstrap period `kind = \"controlled_transition\"`, including ordinary changes that touch no policy (the CHG-002.2 record; CHG-003's to come). The label borrows a §11.5 term for a different thing, developmental human acceptance outside the formal model as `docs/threat-model.md` discloses, and read strictly it implies a third `MergeAllowed` path. | Name the bootstrap case: an acceptance kind for a period in which named predicates cannot hold, carrying `merge_allowed = false`, an expiry, and no claim of conformance; keep \"controlled transition\" for policy migrations. In this repository that is a policy edit only Kennedy makes. | `.rha/acceptances/CHG-002.2.toml [disposition]`; pull request 5 threads r4055717571 and r4056593886; Codex's audit of the review history, 2026-09-20 |\n| 9 | §11.7.11 | A task record's provenance lists instruction sources with digests, and the evidence record carries the agent context. | A digest entered by hand goes stale when the file changes later on the same branch, and `xtask ci` copies `agent_context.provenance` from the task record instead of hashing the file, so five evidence records echo a wrong value (D-002.2.1). | Require that a cited instruction source that is a tracked file be hashed by the record producer at the subject revision, and that the record lint (CHG-019) reject a mismatch; a hand-entered digest is for a source outside the tree. | `.rha/acceptances/CHG-002.2.toml` D-002.2.1; `evidence/CHG-002.2/*.json` `agent_context.provenance.instruction_sources`; pull request 5 thread r4056593889 |\n\n## Not proposed\n\nKept out on purpose, so nobody re-derives them as gaps.\n\n- The 111 deny-list entries themselves. The specification's list is illustrative and says so; the template owns the real one.\n- Anything about Clippy versions other than 0.1.98. One measurement is one measurement.\n- The `xtask` rule ids and the `Problem` variants of `effect.core_clippy_template`. Those belong to the implementation, and §6.13 already names the checker.\n"
  },
  "docs/tasks/index.md": {
    "body": "[Paragraph([Html(\"<!-- generated by cargo xtask docs from .rha/tasks/*.toml (sha256:89cb0525dab253d0c0847245bf34c9dbb8ced299f87b1d859fbad73ee56b7069); do not edit -->\\n\")]), Heading { level: 1, slug: \"tasks\", children: [Text(\"Tasks\")] }, Paragraph([Text(\"One task record per change (spec §11.7.11, stored as TOML). A task's status is the Executor's proposal; acceptance is recorded separately in \"), Code(\".rha/acceptances/\"), Text(\".\")]), Table { align: [None, None, None, None, None, None], head: [[Text(\"Task\")], [Text(\"Title\")], [Text(\"Mode\")], [Text(\"Executors\")], [Text(\"Authority\")], [Text(\"Status\")]], rows: [[[Link { href: \"../../.rha/tasks/CHG-000-bootstrap.toml\", children: [Text(\"CHG-000\")] }], [Text(\"Bootstrap: repository, fast lane, evidence records, contribution artifacts\")], [Text(\"agent\")], [Text(\"agent:claude-opus-5\")], [Text(\"ECC-Solo\")], [Text(\"proposed\")]], [[Link { href: \"../../.rha/tasks/CHG-001-ambient-effect-deny-list.toml\", children: [Text(\"CHG-001\")] }], [Text(\"Ambient-effect deny list and Clippy configuration discovery\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"accepted\")]], [[Link { href: \"../../.rha/tasks/CHG-002-corpus-pre-registration.toml\", children: [Text(\"CHG-002\")] }], [Text(\"Pre-registered H4 corpus manifest\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"proposed\")]], [[Link { href: \"../../.rha/tasks/CHG-002.2-acceptance-protocol.toml\", children: [Text(\"CHG-002.2\")] }], [Text(\"Acceptance by merge, a decision ledger, and the CHG-002 acceptance record\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"proposed\")]], [[Link { href: \"../../.rha/tasks/CHG-003-architecture-crate-level.toml\", children: [Text(\"CHG-003\")] }], [Text(\"cargo xtask architecture: the crate-graph checker\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"proposed\")]], [[Link { href: \"../../.rha/tasks/CHG-004-h4-crate-harness.toml\", children: [Text(\"CHG-004\")] }], [Text(\"H4 crate-level corpus harness and evidence\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"proposed\")]], [[Link { href: \"../../.rha/tasks/CHG-004.6-c13-registration.toml\", children: [Text(\"CHG-004.6\")] }], [Text(\"C13 registration amendment, H4 re-run, and the held-out runner under xtask\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"proposed\")]], [[Link { href: \"../../.rha/tasks/CHG-005-rhawiki-m1.toml\", children: [Text(\"CHG-005\")] }], [Text(\"P-A: the rhawiki product and the M1 close\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"in_progress\")]], [[Link { href: \"../../.rha/tasks/CHG-007-module-level.toml\", children: [Text(\"CHG-007\")] }], [Text(\"P-C: schema follow-on, module graph and no_std experiment\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"ready_for_review\")]], [[Link { href: \"../../.rha/tasks/CHG-008-growth.toml\", children: [Text(\"CHG-008\")] }], [Text(\"P-D: adapter-json, measured feature growth and mutation diagnosis\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"in_progress\")]], [[Link { href: \"../../.rha/tasks/CHG-019-trust.toml\", children: [Text(\"CHG-019\")] }], [Text(\"P-B: record lint and the §11.7.6 verifier\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"in_progress\")]], [[Link { href: \"../../.rha/tasks/PLAN-2-plan-revision.toml\", children: [Text(\"PLAN-2\")] }], [Text(\"Redevelop the implementation plan from the lessons of W0 to W4\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"proposed\")]], [[Link { href: \"../../.rha/tasks/PLAN-3-reviewer-alignment.toml\", children: [Text(\"PLAN-3\")] }], [Text(\"Align the plan's kickoff prompt and M6 with the ledger (prompt audit)\")], [Text(\"agent\")], [Text(\"agent:executor\")], [Text(\"ECC-Solo\")], [Text(\"proposed\")]]] }]",
    "headings": "[Heading { level: 1, text: \"Tasks\", slug: \"tasks\", base_slug: \"tasks\", line: 3 }]",
    "markdown": "<!-- generated by cargo xtask docs from .rha/tasks/*.toml (sha256:89cb0525dab253d0c0847245bf34c9dbb8ced299f87b1d859fbad73ee56b7069); do not edit -->\n\n# Tasks\n\nOne task record per change (spec §11.7.11, stored as TOML). A task's status is the Executor's proposal; acceptance is recorded separately in `.rha/acceptances/`.\n\n| Task | Title | Mode | Executors | Authority | Status |\n| --- | --- | --- | --- | --- | --- |\n| [CHG-000](../../.rha/tasks/CHG-000-bootstrap.toml) | Bootstrap: repository, fast lane, evidence records, contribution artifacts | agent | agent:claude-opus-5 | ECC-Solo | proposed |\n| [CHG-001](../../.rha/tasks/CHG-001-ambient-effect-deny-list.toml) | Ambient-effect deny list and Clippy configuration discovery | agent | agent:executor | ECC-Solo | accepted |\n| [CHG-002](../../.rha/tasks/CHG-002-corpus-pre-registration.toml) | Pre-registered H4 corpus manifest | agent | agent:executor | ECC-Solo | proposed |\n| [CHG-002.2](../../.rha/tasks/CHG-002.2-acceptance-protocol.toml) | Acceptance by merge, a decision ledger, and the CHG-002 acceptance record | agent | agent:executor | ECC-Solo | proposed |\n| [CHG-003](../../.rha/tasks/CHG-003-architecture-crate-level.toml) | cargo xtask architecture: the crate-graph checker | agent | agent:executor | ECC-Solo | proposed |\n| [CHG-004](../../.rha/tasks/CHG-004-h4-crate-harness.toml) | H4 crate-level corpus harness and evidence | agent | agent:executor | ECC-Solo | proposed |\n| [CHG-004.6](../../.rha/tasks/CHG-004.6-c13-registration.toml) | C13 registration amendment, H4 re-run, and the held-out runner under xtask | agent | agent:executor | ECC-Solo | proposed |\n| [CHG-005](../../.rha/tasks/CHG-005-rhawiki-m1.toml) | P-A: the rhawiki product and the M1 close | agent | agent:executor | ECC-Solo | in_progress |\n| [CHG-007](../../.rha/tasks/CHG-007-module-level.toml) | P-C: schema follow-on, module graph and no_std experiment | agent | agent:executor | ECC-Solo | ready_for_review |\n| [CHG-008](../../.rha/tasks/CHG-008-growth.toml) | P-D: adapter-json, measured feature growth and mutation diagnosis | agent | agent:executor | ECC-Solo | in_progress |\n| [CHG-019](../../.rha/tasks/CHG-019-trust.toml) | P-B: record lint and the §11.7.6 verifier | agent | agent:executor | ECC-Solo | in_progress |\n| [PLAN-2](../../.rha/tasks/PLAN-2-plan-revision.toml) | Redevelop the implementation plan from the lessons of W0 to W4 | agent | agent:executor | ECC-Solo | proposed |\n| [PLAN-3](../../.rha/tasks/PLAN-3-reviewer-alignment.toml) | Align the plan's kickoff prompt and M6 with the ledger (prompt audit) | agent | agent:executor | ECC-Solo | proposed |\n"
  },
  "docs/toolchain.md": {
    "body": "[Paragraph([Html(\"<!-- generated by cargo xtask docs from rha-baseline.json (sha256:a8d28cd3e40d045c4edcd281898705f1a2c93853427812e5d6ffe82386eb4937); do not edit -->\\n\")]), Heading { level: 1, slug: \"toolchain-and-tools\", children: [Text(\"Toolchain and tools\")] }, Paragraph([Code(\"rha-baseline.json\"), Text(\" owns the required version of every tool; the Rust toolchain is pinned in \"), Code(\"rust-toolchain.toml\"), Text(\". \"), Code(\"cargo xtask ci\"), Text(\" records a check as \"), Code(\"not_run\"), Text(\" when its tool is absent (\"), Code(\"tool_missing\"), Text(\") or at another version (\"), Code(\"version_mismatch\"), Text(\").\")]), Table { align: [None, None, None, None, None], head: [[Text(\"Tool\")], [Text(\"Status\")], [Text(\"Required\")], [Text(\"Used by\")], [Text(\"Install\")]], rows: [[[Text(\"rustc\")], [Text(\"required\")], [Text(\"1.98.1\")], [Text(\"L0.doctest\")], [Text(\"rustup toolchain install (reads rust-toolchain.toml)\")]], [[Text(\"rustfmt\")], [Text(\"required\")], [Text(\"1.9.0-stable\")], [Text(\"L0.fmt\")], [Text(\"rustup component (rust-toolchain.toml)\")]], [[Text(\"clippy\")], [Text(\"required\")], [Text(\"0.1.98\")], [Text(\"L0.clippy\")], [Text(\"rustup component (rust-toolchain.toml)\")]], [[Text(\"cargo-nextest\")], [Text(\"required\")], [Text(\"0.9.145\")], [Text(\"L0.nextest\")], [Text(\"cargo install --locked cargo-nextest@0.9.145\")]], [[Text(\"cargo-deny\")], [Text(\"required\")], [Text(\"0.20.2\")], [Text(\"L0.deny\")], [Text(\"cargo install --locked cargo-deny@0.20.2\")]], [[Text(\"cargo-machete\")], [Text(\"required\")], [Text(\"0.9.2\")], [Text(\"L0.machete\")], [Text(\"cargo install --locked cargo-machete@0.9.2\")]], [[Text(\"typos-cli\")], [Text(\"required\")], [Text(\"1.50.2\")], [Text(\"L0.typos\")], [Text(\"cargo install --locked typos-cli@1.50.2\")]], [[Text(\"cargo-mutants\")], [Text(\"required\")], [Text(\"27.1.0\")], [Text(\"L1.mutation, L2.mutation\")], [Text(\"cargo install --locked cargo-mutants@27.1.0\")]], [[Text(\"cargo-modules\")], [Text(\"required\")], [Text(\"0.27.0\")], [Text(\"H4.X-M01\")], [Text(\"cargo install --locked cargo-modules@0.27.0\")]]] }]",
    "headings": "[Heading { level: 1, text: \"Toolchain and tools\", slug: \"toolchain-and-tools\", base_slug: \"toolchain-and-tools\", line: 3 }]",
    "markdown": "<!-- generated by cargo xtask docs from rha-baseline.json (sha256:a8d28cd3e40d045c4edcd281898705f1a2c93853427812e5d6ffe82386eb4937); do not edit -->\n\n# Toolchain and tools\n\n`rha-baseline.json` owns the required version of every tool; the Rust toolchain is pinned in `rust-toolchain.toml`. `cargo xtask ci` records a check as `not_run` when its tool is absent (`tool_missing`) or at another version (`version_mismatch`).\n\n| Tool | Status | Required | Used by | Install |\n| --- | --- | --- | --- | --- |\n| rustc | required | 1.98.1 | L0.doctest | rustup toolchain install (reads rust-toolchain.toml) |\n| rustfmt | required | 1.9.0-stable | L0.fmt | rustup component (rust-toolchain.toml) |\n| clippy | required | 0.1.98 | L0.clippy | rustup component (rust-toolchain.toml) |\n| cargo-nextest | required | 0.9.145 | L0.nextest | cargo install --locked cargo-nextest@0.9.145 |\n| cargo-deny | required | 0.20.2 | L0.deny | cargo install --locked cargo-deny@0.20.2 |\n| cargo-machete | required | 0.9.2 | L0.machete | cargo install --locked cargo-machete@0.9.2 |\n| typos-cli | required | 1.50.2 | L0.typos | cargo install --locked typos-cli@1.50.2 |\n| cargo-mutants | required | 27.1.0 | L1.mutation, L2.mutation | cargo install --locked cargo-mutants@27.1.0 |\n| cargo-modules | required | 0.27.0 | H4.X-M01 | cargo install --locked cargo-modules@0.27.0 |\n"
  },
  "label-like-not-citation": {
    "body": "[Paragraph([Text(\"see \"), Text(\"[\"), Text(\"r1\"), Text(\"]\"), Text(\" and \"), Text(\"[\"), Text(\"R\"), Text(\"]\"), Text(\" and \"), Text(\"[\"), Text(\"R1A\"), Text(\"]\"), Text(\" and \"), Text(\"[\"), Text(\"Rx\"), Text(\"]\")])]",
    "headings": "[]",
    "markdown": "see [r1] and [R] and [R1A] and [Rx]\n"
  },
  "link-ref-def": {
    "body": "[Paragraph([Text(\"see \"), Link { href: \"http://example.com\", children: [Text(\"R1\")] }])]",
    "headings": "[]",
    "markdown": "see [R1]\n\n[R1]: http://example.com\n"
  },
  "section-refs-page": {
    "body": "[Heading { level: 2, slug: \"3-three\", children: [Text(\"3. Three\")] }, Paragraph([Text(\"see \"), Link { href: \"#3-three\", children: [Text(\"§3\")] }, Text(\" and \"), Text(\"[\"), Text(\"x\"), Text(\"]\")])]",
    "headings": "[Heading { level: 2, text: \"3. Three\", slug: \"3-three\", base_slug: \"3-three\", line: 1 }]",
    "markdown": "## 3. Three\n\nsee §3 and [x]\n"
  },
  "table": {
    "body": "[Table { align: [None, None], head: [[Text(\"[\"), Text(\"a\"), Text(\"]\")], [Text(\"b \"), Text(\"[\"), Text(\"c\")]], rows: [[[Text(\"[\"), Text(\"d\"), Text(\"]\")], [Text(\"e\")]]] }]",
    "headings": "[]",
    "markdown": "| [a] | b [c |\n| - | - |\n| [d] | e |\n"
  },
  "wikilink-and-link": {
    "body": "[Paragraph([WikiLink { index: 0, children: [Text(\"Page\")] }, Text(\" and \"), Link { href: \"u\", children: [Text(\"text\")] }, Text(\" and \"), Link { href: \"u\", children: [Text(\"R1\")] }])]",
    "headings": "[]",
    "markdown": "[[Page]] and [text](u) and [R1](u)\n"
  }
}