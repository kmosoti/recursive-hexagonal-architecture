You are the independent oracle author for feature CHG-010 (§-references) of the RHA repository at /home/kmosoti/projects/rha-m2. You write the registered grading corpus and its grader BEFORE any implementation exists. The implementation will be written later by a different model; you must not read or rely on parser internals.

## Read
- The contract, which is your specification: /home/kmosoti/projects/rha-m2/docs/architecture/section-reference-contract.md (read it in full; it is authoritative).
- Only these for API shapes (to write a Rust grader that will compile once the feature exists): crates/document/src/lib.rs (Document, Heading, Diagnostic, parse signature), crates/document/src/node.rs (Node::Link), crates/library/src/ (how a Source is constructed), and one existing owner test for style, e.g. crates/document/tests/transclusion.rs (only to see how tests build a Source and call parse). Do NOT read crates/document/src/parse.rs or section.rs.
- The real spec: docs/spec/rha-spec-v0.10.md (it is frozen).
- The registration convention: look at xtask/tests/corpus/transclusion/uris/ (registration.toml, SHA256SUMS, README.md, CASES.json, reference.py, source-snapshots/) and mirror it.

## Write (only under this staging folder; never write inside /home/kmosoti/projects/rha-m2)
/tmp/claude-1000/-home-kmosoti-projects-recursive-hexogonal-architecture/0ce0c856-7b1f-49da-afa8-4880e034d2d1/scratchpad/section-refs-package/
  xtask/tests/corpus/section-refs/
    reference.py        an independent Python implementation of the contract: given markdown text, it returns the expected section_refs and UnresolvedSectionRef diagnostics. Identify each resolved target by the 1-based source LINE of the target heading (not by slug), so you never re-implement slugify; the grader maps that line to the heading's slug through Document::headings.
    CASES.json          the cases, generated by reference.py: [{"id", "markdown", "refs": [{"text","number","target_heading_line"|null,"line"}], "diagnostics": [{"number","line"}]}]. Include the real spec as one case, identified by path and sha256 instead of inlining its text.
    README.md           what the package is, how it was generated, how to reproduce it.
    registration.toml   mirror the uris package: seed (if any), case_count, content_sha256, model = "claude-sonnet-5", effort = "medium", and [source_digests]/[source_paths]/[original_paths] for the contract and your prompt (save this prompt text as source-snapshots/prompt.md and the contract as source-snapshots/contract.md).
    SHA256SUMS          sha256 of every payload file.
    selftestreport.txt  the output of running reference.py's own self-checks.
  crates/document/tests/section_refs.rs   the Rust grader: loads CASES.json (path relative to CARGO_MANIFEST_DIR), verifies SHA256SUMS first, parses each case with document::parse, and asserts: (1) Document::section_refs equals the expected list exactly (text, number, line, and target = the slug of the heading at target_heading_line, or None); (2) the UnresolvedSectionRef diagnostics equal the expected set; (3) the invariant: every resolved ref has a Node::Link somewhere in the body with href "#"+slug and whose visible text is exactly the ref text, and the number of such section-ref links equals the number of resolved refs. For the spec case, read docs/spec/rha-spec-v0.10.md and check its sha256 against the registration first.

## Cases (aim for 60 to 120 synthetic cases plus the spec)
Cover, each deliberately: heading number forms (`1.`, `1.1`, `11.7.6`, `Appendix A.`, `Appendix B.2`, trailing text, number at end of heading, `01` vs `1`, `1.10` vs `1.1`, duplicate numbers → first wins); references in paragraphs, list items, table cells, nested block quotes; NOT in headings, inline code, fenced and indented code blocks, raw HTML, markdown link text and destination, wikilink text, transclusion, image alt; `§` vs `§§`; one optional space `§ 4.1`; two spaces (not a reference); sentence-final period `§7.`; `§7.1.` ; letters `§A`, `§A.1`, `§Also` (not a reference); ranges `§§3–7`, `§3–§5`, `§§8.3–8.4`, hyphen `-`, spaces around the dash, a range whose end does not resolve; comma list `§§8.3, 8.4` (only the first); `§6 and §12.1`; unresolved on a numbered page (diagnostic) vs on an unnumbered page (no diagnostic); multi-line paragraphs (line numbers); a `§` alone; `§` followed by a non-number. Where the contract is ambiguous for a case, do not guess: write the case's id and the ambiguity into README.md under "Open questions" and leave that case out.

## Rules
- Nothing you register may be generated from or checked against an implementation (there is none).
- Report: the file list, case count, the spec case's reference count, and every open question. Report only what you ran.
