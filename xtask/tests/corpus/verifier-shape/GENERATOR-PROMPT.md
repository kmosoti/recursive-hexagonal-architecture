You are an independent CORPUS GENERATOR for P-C stage 0 (CHG-019.1), not the production implementer and not the DP-5.4 validator. Work in /home/kmosoti/projects/rha-m2 only. Never read siblings or held-out material, never run git/jj mutations, never read xtask/src/** or tools/rha-verifier/src/** or any .rha/schemas/** implementation. Do not spawn agents. You may write only target/m2/schema-generation/**. Parent is the sole branch writer and will inspect then commit your artifacts before writing any schemas or new lint code.

Read docs/architecture/record-schema-contract.md and docs/architecture/verifier-contract.md, .rha/tasks/CHG-007-module-level.toml, relevant plan §2.4 and P-C and spec §11.4/11.7. The schema contract names a frozen source revision. Read ONLY committed sources from that revision for derivation using git show/ls-tree (read-only). Existing corpus records under xtask/tests/corpus/records and tools/rha-verifier/tests/corpus may be read, never modified. No schemas or validator implementation may be written even in staging.

Deliver two separate registrations:
A. record-schema/: deterministic Python standard-library derive.py, source snapshots/index with hashes, shape inventory (NOT JSON Schema), CASES.json describing legitimate controls and mutations with exact structural schema.* reason sets. Derive recursive observed type unions, required-key intersections and allowed-key unions per family/semantic path; arrays pool record items. Follow record-schema-contract.md including modern/legacy agent_context and null/full change_claim variants. Map shapes are not inferred as literal record keys; document exact map boundaries, notably H4 fixtures.*_sha256 maps, params, inputs, report/witness payloads. Preserve string/object H5 corpus citation and v1/v2 H4 shapes. The inventory must be machine consumable for root to translate directly into JSON Schema 2020-12 after registration. Define its format clearly. Do not generate schema files.

Use compact shared baselines plus JSON-pointer mutation descriptors to avoid duplicating large evidence records. Cases grade the STRUCTURAL layer only, so old semantic digest/revision defects do not change expectations. Every case needs id, family, baseline, mutations (remove/set), expected schema code set, purpose. Materialization semantics must be fixed and deterministic; no production lint/schema runs. Structural clean controls do not assert semantic lint acceptance. Register each required root deletion, representative nested deletion/wrong scalar/array/item type, nested unknown record fields, invalid version/discriminator, missing and unknown observed_checks outcome, both provenance variants and invalid hybrids, policy negative cooling_off if contract defines minimum (do not invent it), every corpus-report family including incorrect count types/outcomes/case types. Include named semantically-safe cases suitable for later CLI regression (unknown/missing outcome and unsupported report kind). Suggested controls: CHG-000/004/019/007 tasks, CHG-000/PLAN-2/004/019 acceptances, both current L0 variants, early/current H4/H5 and markdown corpus. Array empty-item domains should be disclosed, not guessed. No new enum domain inferred from one observed ordinary string; five outcomes are normative and H4 level supports crate/module.

B. verifier-shape/: new supplementary JSON fixtures against verifier-contract.md §3, IDs S001 onward. Source from original V fixtures, preserve original files. Include each absent/type-invalid required member category, empty/non-array/non-string surface, required exception key missing, entry id/kind/params/outcome and kind-specific member types, negative/fractional/overflow cooling_off, all exception member shapes, and negative/fractional/positive whole selected_tests. Expected outputs must be derived from contract and original baseline expected values, not running production evaluate. Malformed cases isolate one error and state expected malformed member; refused outputs per §3. Selected negative/fractional numeric counts are WELL FORMED and fail passed, no malformed field. Include valid null and object exception controls. Use tree_sha256 registration matching existing verifier corpus algorithm: hash sorted '<relative path> <sha256>\n' over all payloads excluding registration.toml and GENERATOR-PROMPT.md, but including explanatory README if present. Do not add helper JSON files at top level that runner will mistake for fixtures.

Both registrations record the exact prompt digest from target/m2/schema-generator-prompt.md, actual source hashes/counts, generator gpt-6-astra xhigh, sample 1, no random seed unless used. Validate JSON/TOML parsing and deterministic derivation only. Never invoke production lint/verifier or write schemas. Copy this prompt to each registration as GENERATOR-PROMPT.md. Preserve every limitation or unresolved ambiguity in README and final summary, do not invent an expectation. Stop only after delivering complete artifacts with exact counts/digests and reproduction commands.
