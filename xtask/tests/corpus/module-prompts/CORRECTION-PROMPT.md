Continue the independent generator task with its same boundaries: no production source/xtask/src reads, no schemas or implementation, no git/jj mutations, write only target/m2/module-generation. Parent is sole branch writer. A read-only pre-registration audit found a mismatch between your optional rule expectations and the now committed PRE-IMPLEMENTATION diagnostic contract docs/architecture/module-check-contract.md. Read that contract (it existed before any extractor code; still none exists). It resolves M01 by preserving every undeclared intra-SCC edge as structured subfacts of the cycle finding, not a redundant top-level finding. It also fixes M05 depth as canonical component path segments including crate root.

Your first final proposal was content_sha256 5cd3ff86e179d428c4800bb42a6585b44670248ad7bf5864bb18e41762cfbd29. No artifact has been committed or graded by production. Preserve that proposal identity in provenance notes. Update ONLY rule_expectations, optional random-rule outputs, and relevant documentation/registration/count artifacts to conform: compute SCC membership, canonical closed cycle paths (least start, lexicographically least shortest return); move undeclared edges within a cyclic SCC to undeclared_edges and subsumed_rules on that cycle; independent undeclared edges remain. Keep raw extracted edges and independent reference.py + R*.expected.json byte-for-byte unchanged. Do not edit headline fixture sources, frozen manifest, or primary extraction expectations. Amend CONTRACT-NOTES to resolve M01 under the contract, define M05 depth and preserve M21 disclosure. Bind the module-check contract digest in register.py/registration.toml, and bind both original prompt and this correction prompt with their exact hashes (target/m2/module-generator-correction.md).

Re-run artifact self-checks/reproduction and registration, compute counts from tools, and give final concise inventory/changes. The raw extraction oracle is still the primary supplementary comparison; optional rule facts must now agree with the diagnostic contract without losing any D4 fact. Registration is before real extractor implementation, not a retrospective regrade. Preserve the fact the first proposal used flat raw predicate findings and was corrected by pre-registration contract review. Do not spawn agents or read any implementation source.
