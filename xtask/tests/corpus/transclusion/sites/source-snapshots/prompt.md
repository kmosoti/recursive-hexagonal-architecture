You are an INDEPENDENT pre-code fixture/reference generator, profile write. Repository /home/kmosoti/projects/rha-m2. Root is the sole branch writer. Never invoke JJ (including status/init), mutate Git, spawn agents, access network, read sibling projects or held-out material, or edit docs/spec. Write ONLY the assigned staging package directory below. No new transclusion implementation exists yet. Do not read ANY implementation under crates/document/src/parse.rs, crates/graph/src, or crates/site/src/assembly. Do not read existing target tests or expected data. Allowed reads: current guides, docs/architecture/transclusion-contract.md, docs/adr/BDR-0007-site-owns-page-tree.md, the exact prompt archive named below, and the public document/node.rs and document/lib.rs vocabulary if needed. Follow the committed contract; report ambiguity rather than inventing a new rule.

Package requirements: reference.py (independent Python reference + deterministic generator), CASES.json, README.md, selftestreport.txt, registration.toml, SHA256SUMS, source-snapshots/contract.md, source-snapshots/bdr.md, source-snapshots/prompt.md. Freeze exact input bytes; metadata source paths point to eventual committed snapshot paths, original paths are documentary only. Never require future live source equality. Registration records actual computed seed, case/kind/payload counts, source/prompt digests, content_sha256=sha256(SHA256SUMS bytes), model gpt-5.6-luna/effort high (root verifies actual CLI header). SHA256SUMS lists ALL payload files in sorted relative-path order, excluding itself and registration.toml. No timestamps or staging-dependent paths in payloads. All metadata must reproduce byte-for-byte from frozen snapshots. reference.py defaults to READ-ONLY self-tests; explicit --reproduce-to writes only under the script package directory. README workflow: copy committed package to fresh target/m2 staging, run copied reference into its reproduced subdirectory, then clean that owned subdirectory. Do not run reproduction on committed data. Your own writes/reproduction stay only inside your assigned directory; remove your owned reproduction subdirectory afterward. Do not produce __pycache__ files. Compute hashes/counts with tools, never type guessed values. Verify every payload and hand-derived controls; report actual observations only. Do not run or grade a target implementation.

ASSIGNED directory: target/m2/transclusion-sites
EVENTUAL package: xtask/tests/corpus/transclusion/sites
EXACT prompt archive: /home/kmosoti/projects/rha-m2/xtask/tests/corpus/write-prompts/CHG-008/pd-transclusion-sites-generator-prompt.md
Additional allowed reads: evidence/CHG-008/stage-2/pd-transclusion-parser-probe.txt (measured EXISTING parser behavior). Never read parser/graph/assembly implementations. These are AUTHORED site scenarios, no PRNG required (record sample1). Write a deterministic generator storing12 scenario groups from the contract; it may have pure independent helpers for simple expected calculations, but no target tool calls or existing oracle reads. reference.py in this package may be a deterministic fixture author/validator rather than a complete Markdown reference; state that limit.

CASES rows {id,kind:"site",sources:[{path,text}],expected_documents:{page_id:{transclusions:[{id,target,anchor,display,line}],navigation_links:[{target,anchor,alias,line}],images:[{src,alt}],heading_slugs:[string]}},expected_check:{schema_version:1,pages:n,witnesses:[exact objects],counts:{kind:n}},expected_exit:0|1,expected_pages:{page_id:{toc_anchors:[string],heading_ids:[string],wiki_targets:[{page:string|null,anchor:string|null}],ordinary_hrefs:[string],image_sources:[string],text_includes:[string],text_excludes:[string]}}}. expected_pages may select the pages relevant to the scenario, but expected_documents/check cover EVERY source. Observations are in deterministic tree/vector order; wiki_targets are the complete PageModel.links vector (host original slots first, then copied visible links). Each expected field is later graded. Do not claim unmeasured full AST equivalence. Keep source Markdown simple enough to derive all fields independently. Transclusion descriptor display is its original plain image alt text. No parser-only variants may remain in rendered page nodes.

12 groups: TX01 whole-page with surrounding-space control; TX02 all exact exclusions (inline and aliases images; escaped/spaced navigation; inline/fenced code; two soft-break images) with existing target pages so register all navigation witnesses correctly; TX03 tight/loose/nested lists and quotes plus adjacent-text exclusion; TX04 root section bounds with lower heading included and next peer/higher excluded; TX05 nested quote/list heading selection preserving shell and ordered item position (include selector expectation/query information if needed in a separately fixed fields table); TX06 nested selected embeds; TX07 exact/basename and missing/ambiguous/missing-anchor target controls (new ambiguity includes candidates); TX08 self/two-region cycles; TX09 same-page acyclic regions and page-level false cycle (A embeds B#keep, B#other embeds A; no actual region cycle), plus if including an independent real cycle register its global witness even when not in A output; TX10 SCC needing >=2 blocked back edges; TX11 repeated embeds, host anchor collisions, imported navigation slot rebasing and matched/unmatched fragments; TX12 origin path/link/image/query/scheme cases, retaining .md at PageNode and naming HTML rewrite checks.

Use only simple ASCII headings for predictable existing slug behavior; avoid duplicate heading texts unless every DuplicateSlug witness is explicitly registered. Preserve original host navigation slots. Transclusions do not consume navigation slots. Global graph roots are ALL pages; cycle witnesses cannot be omitted just because only one page's render is observed. Canonical cycles order regions by page then None-before-Some anchor, root DFS sorted, outgoing IDs sorted. New CLI exact witness objects are in the contract; old escaped/spaced links use unchanged old kinds if broken. Unavailable/cycle visible markers must be exact strings. Namespace chain uses descriptor IDs, host heading set is seeded first, collision suffix -2/-3 in traversal order. Independent registration review will check all expected findings before code. If a group needs multiple small scenarios, represent variants within the group with an explicit deterministic subcase list and fixed grader description, rather than forcing contradictory expectations; root must approve the shape before freeze.

Prefer a flat list of12 rows; bundle controls through several source pages and expected_pages. Do not invent a new case kind. Avoid overly large cases. No HTML renderer is run; expected PageNode path fields are specified, and an additional expected_html_contains mapping may provide exact .html href markers for TX12; document every added expectation field. If you cannot derive an expectation confidently, report it before freezing rather than dropping the obligation.
