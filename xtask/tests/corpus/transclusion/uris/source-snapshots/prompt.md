You are an INDEPENDENT pre-code fixture/reference generator, profile write. Repository /home/kmosoti/projects/rha-m2. Root is the sole branch writer. Never invoke JJ (including status/init), mutate Git, spawn agents, access network, read sibling projects or held-out material, or edit docs/spec. Write ONLY the assigned staging package directory below. No new transclusion implementation exists yet. Do not read ANY implementation under crates/document/src/parse.rs, crates/graph/src, or crates/site/src/assembly. Do not read existing target tests or expected data. Allowed reads: current guides, docs/architecture/transclusion-contract.md, docs/adr/BDR-0007-site-owns-page-tree.md, the exact prompt archive named below, and the public document/node.rs and document/lib.rs vocabulary if needed. Follow the committed contract; report ambiguity rather than inventing a new rule.

Package requirements: reference.py (independent Python reference + deterministic generator), CASES.json, README.md, selftestreport.txt, registration.toml, SHA256SUMS, source-snapshots/contract.md, source-snapshots/bdr.md, source-snapshots/prompt.md. Freeze exact input bytes; metadata source paths point to eventual committed snapshot paths, original paths are documentary only. Never require future live source equality. Registration records actual computed seed, case/kind/payload counts, source/prompt digests, content_sha256=sha256(SHA256SUMS bytes), model gpt-5.6-luna/effort high (root verifies actual CLI header). SHA256SUMS lists ALL payload files in sorted relative-path order, excluding itself and registration.toml. No timestamps or staging-dependent paths in payloads. All metadata must reproduce byte-for-byte from frozen snapshots. reference.py defaults to READ-ONLY self-tests; explicit --reproduce-to writes only under the script package directory. README workflow: copy committed package to fresh target/m2 staging, run copied reference into its reproduced subdirectory, then clean that owned subdirectory. Do not run reproduction on committed data. Your own writes/reproduction stay only inside your assigned directory; remove your owned reproduction subdirectory afterward. Do not produce __pycache__ files. Compute hashes/counts with tools, never type guessed values. Verify every payload and hand-derived controls; report actual observations only. Do not run or grade a target implementation.

ASSIGNED directory: target/m2/transclusion-uris
EVENTUAL package: xtask/tests/corpus/transclusion/uris
EXACT prompt archive: /home/kmosoti/projects/rha-m2/xtask/tests/corpus/write-prompts/CHG-008/pd-transclusion-uris-generator-prompt.md
Seed: 90092028. Independent lexical URI rebasing reference and authored controls plus128 bounded seeded cases. May use Python posixpath for normalization, but no filesystem-dependent relpath/cwd behavior and no browser/network library behavior that changes the declared lexical rules. Contract controls the exact outputs.

CASES rows {id,kind:"uri",host:string,origin:string,destination:string,reference_kind:"link"|"image",anchors:{old_slug:mapped_slug},expected:string}. Host/origin are canonical source-derived page IDs (relative paths, NFC). Their known path segments are percent-encoded (unreserved ASCII and slash kept); canonical origin URL appends .md. Scheme/protocol-relative/absolute inputs remain byte-identical. ASCII scheme definition is in contract. Otherwise split query/fragment suffix without losing empty ?/# markers; relative path resolves against encoded origin directory, lexical POSIX . and .. normalize while retaining above-logical-root .., then express relative to encoded host directory. Preserve destination percent escapes/Unicode bytes and suffix. Preserve directory trailing slash. Empty/query-only path denotes canonical origin file. When resulting first relative segment can be mistaken for a scheme, prefix ./.

Only link references that are FRAGMENT-ONLY (no query/path) can map a percent-decoded UTF8-valid fragment through anchors; return # plus percent-encoded mapped anchor. Unmatched/invalid fragments rebase to canonical origin page+original fragment. Images NEVER apply heading remapping, even when anchors contains the fragment. Do not use unquote_plus; + is literal in fragments. Invalid percent escapes/invalid UTF8 remain unmatched. Fragment matching is exact/case-sensitive.

Hand expected controls: child.md from source/Source to guides/Host ->../source/child.md; ../peer.md#x ->../peer.md#x; asset.png for image; matched #inside ->mapped host anchor; unmatched #outside ->../source/Source.md#outside; ?q=1#outside ->origin file suffix; empty destination; absolute/scheme/protocol-relative unchanged; ./a:b.md with same-directory pages retains ./; directory trailing slash; above-root traversal; Unicode known page-ID segments; percent-encoded matching/unmatched invalid fragment; plus literal; image fragment ignores anchor map. Every expected value should be fixed by reference and reviewed hand controls. Seeded cases stay within bounded declared rules; no blanket complete-URI conformance claim. Reproduce CASES bytes.
