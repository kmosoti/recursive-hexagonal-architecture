Write a registered oracle package and grader for a feature that is not implemented yet, from its contract alone. Read existing code only to learn APIs, fixture conventions and style; never to learn how the feature will be implemented.

Repository (read-only for you): /home/kmosoti/projects/rha-chg013 (Rust workspace "rhawiki", branch chg/013-tags-front-matter, HEAD 779de1a).
Contract (authoritative): docs/architecture/front-matter-contract.md. Also read AGENTS.md, CONTRIBUTING.md.
Model your package on the registered citation package: xtask/tests/corpus/citations/ (reference.py, CASES.json, README.md, registration.toml, SHA256SUMS, selftestreport.txt, source-snapshots/) and its grader crates/document/tests/citations.rs, which embeds everything with include_bytes!/include_str! (core crates must not read files at test time: decision pure-fixture-inputs; the core clippy deny list forbids std::fs).

SCRATCH BOUNDARY (strict): write only under /tmp/claude-1000/-home-kmosoti-projects-recursive-hexogonal-architecture/0ce0c856-7b1f-49da-afa8-4880e034d2d1/scratchpad/chg013-oracle/ (create it). Deliver the package at chg013-oracle/package/xtask/tests/corpus/front-matter/ and the grader at chg013-oracle/package/crates/document/tests/front_matter.rs. Do not write into the repository. For compile checks use a worktree copy (`git -C /home/kmosoti/projects/rha-chg013 worktree add --detach <your folder>/wt HEAD`) with CARGO_TARGET_DIR in your folder and CARGO_PROFILE_DEV_DEBUG=0 CARGO_PROFILE_TEST_DEBUG=0 CARGO_INCREMENTAL=0 (/tmp is a 7.8G tmpfs). Delete only what you create in your folder; remove your worktree with `git worktree remove` at the end.

Package:
- reference.py: an independent Python implementation of contract sections 1 to 3 (recognition, the restricted grammar, the title override rule, InvalidFrontMatter lines, FrontMatter{title, tags, end_line}); for the body it only needs to say where markdown parsing starts (the line after the closing delimiter) and, for the "title derived as before" cases, the expected title (first heading text as the current parser derives it: read crates/document/src for how `title` is derived today, API facts only). With self-tests.
- CASES.json: at least 60 hand-authored cases covering every rule and boundary in sections 1 and 2: first-line rule (leading blank line, BOM, trailing spaces/tabs, `...` closer, missing closer, `---` mid-page, `+++`, empty front matter, closer with trailing whitespace), title (quotes, empty, duplicates first wins), tags (flow, block, scalar, quotes in flow, empty elements, ASCII-case duplicates, order, list items under a non-tags key, unindented `- x`, list item without a preceding empty-valued key), comments, blank lines, unknown keys, nested maps -> InvalidFrontMatter with exact lines, and line numbers of headings after the block (e.g. a heading on source line 6 reports line 6). Each case: id, markdown, expected front_matter (null or {title, tags, end_line}), expected title, expected InvalidFrontMatter lines, and expected heading lines/texts where relevant.
- README.md (what is covered, what is deliberately left open: list every ambiguity you find in the contract as a question, and keep ambiguous cases OUT of the corpus), registration.toml, selftestreport.txt, source-snapshots/contract.md (verbatim copy) and your prompt, SHA256SUMS over the payload files.

Grader crates/document/tests/front_matter.rs: verifies SHA256SUMS of the embedded payload; for each case parses the markdown with document::parse and asserts exactly `document.front_matter` (`Option<document::FrontMatter { title: Option<String>, tags: Vec<String>, end_line: usize }>`), `document.title`, the `Diagnostic::InvalidFrontMatter { line }` lines in order, and the heading lines/texts listed; plus: no body node is produced from front-matter lines (e.g. no `Node::Rule` as the first body node when front matter is recognised). These types do not exist yet: the grader will not compile against HEAD; stub-compile it in your worktree against a minimal scratch stub of the API (never delivered) and run it green against a scratch implementation of your reference to prove the grader is satisfiable and consistent, then revert. Also confirm it compiles against HEAD except for the missing items. It must pass `rustfmt --check` and `cargo clippy -p document --tests` with zero errors in your stubbed worktree (the workspace denies clippy::all; start the file with `#![allow(clippy::expect_used, clippy::unwrap_used)]` like citations.rs).

Report: files delivered, case count and coverage, the self-test and stub results, the fmt/clippy results, and the list of contract ambiguities (questions).
