You are a write-tier DOCUMENTATION worker proposing a decided edit. READ-ONLY /home/kmosoti/projects/rha-m2; root is sole branch writer. Never run JJ, including status/init, write/apply files, mutate Git, run builds/tests, spawn agents, read sibling projects or held-out material, or edit docs/spec. Return one apply_patch block ONLY adding docs/adr/BDR-0007-site-owns-page-tree.md and docs/architecture/transclusion-contract.md. No implementation exists yet. Read current public Node/Document/PageModel/Graph APIs, current guides, BDR-0006, the task's full delegation quote, and target/m2/pd-transclusion-parser-probe.txt. Do not invent test results, hashes or counts.

DECIDED boundary: site::assembly owns PageNode with the EXISTING renderable variants and fields of document::Node; crate root exports PageNode as site::Node, preserving renderer source shape. Align/CalloutKind may remain shared semantic enums. PageModel.body is Vec<PageNode>. Define PageNode at the assembly facade, not in parent glue. No blanket From<document::Node>: conversion needs origin/host, graph and expansion context. Assembly alone converts parser syntax and removes transclusions, emitting only stable renderable nodes. Existing HTML/JSON production code should remain unchanged. Their setup tests may need the added Assemble document-slice argument; record those separately, do not hide touched tests.

Three scored alternatives (criteria each /5: ownership, syntax cannot leak, migration compatibility, W9 extensibility, diagnosis): shared document AST 2+1+5+2+3=13/25; OWNED PageNode 5+5+4+5+5=24/25; flat render-event stream 4+5+1+4+2=16/25. Explain genuine tradeoffs, duplicate vocabulary/conversion cost, and flat stream balancing/rewrites. Accept ONLY refutation criterion under agent:executor under human:kennedy delegation, date2026-09-23, quote both instructions from task. Criterion: during transclusion and section-reference stages, renderer production files should require zero edits solely for new parser-only semantic variants; measure exact source-path diffs and any justified exception, reopen boundary if violated. This does not prohibit later new presentation capabilities such as citation anchors. It does not accept a PR. No method/spec change and no new crate is proposed.

TRANSCLUSION syntax, based on measured parser:
- Add document::Transclusion owned struct {id:usize,target:String,anchor:Option<String>,display:String,line:usize}; Node::Transclusion(Transclusion). IDs contiguous preorder among RECOGNIZED embeds per source document, stable across document permutation. Document.links stays navigation-only and retains its existing indexing/semantics.
- A pulldown WikiLink Image without alias and with nonempty page, plus nonempty anchor if # supplied, is a candidate. Recognize only when it is the sole meaningful content of a paragraph or tight list item; spaces/tabs around it allowed. Works recursively in quotes and list items. Code blocks/code spans never candidates. A candidate with other content stays original Image; aliases stay Image. Escaped bang and spaced bang retain their existing literal-bang + navigation behavior. Inline and alias ![[...]] are IMAGES, not navigation links. Two images separated by a soft break are not standalone. Four-space code indentation remains code. Preserve exact measured exclusions; no blanket regex on raw text. Intermediate candidates may be normalized internally, but no unrecognized candidate may leak as Transclusion.
- document::section_nodes(&Document, Option<&str>)->Option<Vec<document::Node>>: None selects full body. Selected heading and following siblings in its immediate block sequence until next heading <=level; nested headings do not terminate outer sections. Recursive selection through quote/list preserves ancestor shells but no unrelated siblings. List wrapper retains selected item only and adjusts ordered start by its item position. Slugs remain original here. Inputs are parsed Documents. Provide a pure traversal helper for transclusion descriptors; no parser types in public API.

GRAPH contract:
- Keep Resolved and ordinary navigation behavior unchanged. Resolve embeds separately with the EXISTING registered exact-ID/basename rules. A page that resolves contributes a backlink even if its requested anchor is missing, consistent with navigation; missing/ambiguous pages do not. Valid expansion requires section_nodes succeeds.
- To keep navigation diagnostics distinct, add BrokenTransclusion{from,target,line}, AmbiguousTransclusion{from,target,candidates,line}, MissingTransclusionAnchor{from,target,heading,line}, TransclusionCycle{path:Vec<TransclusionRegion>}. Do NOT reuse old navigation failures or add anchor_valid to Resolved.
- Region={page:PageId,anchor:Option<String>}, ordered page then None before Some. Occurrence={source:Region,transclusion_id:usize}. Edge target lookup is keyed by the FULL occurrence, not only page/id; assembly may follow only analyzed edges. Graph may use a temporary global resolution map internally.
- Build regions from every full page and valid anchored targets. For each region, its edges are recognized embeds INSIDE that selected fragment. Order roots by Region, outgoing edges by id then target. Deterministic DFS blocks every edge to an active region; cycle path is active-stack suffix + repeated target, canonically rotate to least Region, deduplicate closed paths. Removing all blocked occurrences leaves a DAG. Expose read-only target and blocked-cycle lookup by Occurrence. Cycles are graph-owned witnesses.
- Global check covers ALL pages/regions. Selection excludes unrelated edges from that region; it does not hide a separate real cycle elsewhere in the corpus. Distinguish the false page-level A->B#keep, B#other->A case (acyclic) from an independent real B#other self-cycle (global witness but no marker in A's selected keep region).

ASSEMBLY contract:
- Assemble receives explicit complete &[Document] in addition to the current doc, graph, title lookup and context. build passes its existing document slice downward; assembly never reaches build/glue.
- Replace recognized transclusion with selected fragment; recurse using exact occurrence lookup. Blocked edge -> Paragraph/Text literal [transclusion cycle: A#one -> B#two -> A#one]. Missing/ambiguous/invalid selected target -> [transclusion unavailable: <display>]. Same marker in HTML/JSON because PageNode is already semantic-free. Host TOC stays host-only. No Transclusion variant in PageNode.
- Preserve all original host navigation slots and indices (as current assembly maps Document.links). Imported visible wikilinks append new deterministic slots in traversal order, resolved against their ORIGIN doc/index; repeated imports append separately. Do not claim the attempted hidden-alt probe proved a bug; it did not.
- Imported heading IDs are namespaced by occurrence chain: tx-<outer id>-<nested id>-<original slug>. Seed used IDs with all host headings. Allocate all heading mappings for a fragment before copying, traverse deterministic tree order, resolve collisions with -2,-3... globally. Rewrite fragment-only ordinary links whose decoded fragment matches a selected heading to its mapped host anchor; otherwise they target canonical origin page + original fragment, not host.
- Rebase relative ordinary links and image src from origin directory to host directory with lexical POSIX dot-segment normalization; preserve schemes, protocol-relative URLs, absolute paths, query and fragment suffixes. Empty/query-only destinations refer to canonical origin page. Keep .md suffixes for HTML's existing rewrite_href; no format-specific .html/.json in core. Percent-encode known origin/host PageId path segments with unreserved ASCII + slash; preserve already encoded destination segments. Matching fragment percent decoding must be UTF8-valid or leave it unmatched. Encode generated anchor in rewritten href. Retain above-logical-root .. rather than silently clamping it. Preserve directory trailing slash; if computed first segment could be a scheme (e.g. a:b.md), prefix ./ to retain relative meaning. State this bounded lexical behavior, not complete URI validation.
- Resource implication: clones/expansion allocate and repeated embeds may multiply output. Do not claim a linear bound or a measured performance result.

CLI additions retain envelope schema_version1 and existing witness objects unchanged; kinds broken_transclusion, ambiguous_transclusion, missing_transclusion_anchor use corresponding old-style fields (from,target[,heading], no line); transclusion_cycle has path strings page or page#anchor. Counts/order/exit semantics unchanged. No grading change to old60 markdown sites or79 JSON cases. New vocab is an additive contract for new inputs; no silent regrading.

PREDECLARED spread: original plan document+graph+assembly; decided production document,graph,site::assembly,site::build,site facade,app-cli. Existing renderer production unchanged; renderer setup tests need Assemble argument migration. Tool/test metadata may add serde_json DEV-only where owner-local JSON oracle tests need it, with an exact live core.dev_allow entry and task justification; no normal core serde dependency. Separate auditor verified frozen H4 uses its own fixture rules, so live dev allow does not alter frozen grades.

Registration before code: separate session forbidden target implementation writes authored Markdown/section/path fixtures plus independent references for region blocking, section selection and path rebasing. Seeded normalized graph cases are algorithm-level synthetic graphs, not a claim that every graph is realizable Markdown. Require exact fixed outputs for all cases plus proptest permutation/differential checks; include every scope failure necessarily triggered. Required authored scenario groups (can bundle controls): basic whole page; exact exclusions; tight/loose/nested list and quote; root section bounds; nested heading shells/list ordinal; nested embeds; exact/basename/missing/ambiguous/missing-anchor targets; self and two-region cycles; same-page acyclic regions and out-of-region false cycle; multi-cycle SCC; repeated embeds/host anchor collisions/imported nav slots/inside-outside fragments; relative path/query/scheme/Unicode/colon/directory cases. Preserve old corpus bytes. These are future required checks, not claimed passes.

Keep documents concise and precise; no code implementation or fabricated measurements. Return patch only.
