You are a write-tier worker proposing a decided edit. Repository: /home/kmosoti/projects/rha-m2. Root is the sole branch writer. NEVER run JJ, including status/init. Do not write files, apply patches, run builds/tests, mutate Git/PRs, spawn agents, read sibling projects or held-out material, or edit the spec. Return only one apply_patch block. Read the relevant guides. Other workers own disjoint files; do not change their files.

P-D task is claimed and in progress. BDR-0006 and JSON contract were committed before the crate. Independent corpus was committed at 3ef4d8cceec55a6c7101900677902a0dd8436278 BEFORE any encoder code. Read docs/architecture/json-renderer-contract.md and docs/adr/BDR-0006-adapter-json-immutable-index.md. Corpus bytes and grading are immutable. No core production or test edit is permitted during this adapter stage.

Own ONLY crates/app-cli/Cargo.toml and crates/app-cli/src/main.rs. Add normal adapter-json path dependency. Add BuildFormat enum deriving clap::ValueEnum (Html,Json), with build --format value_enum default_value="html". Preserve check subcommand --format text|json and exact v1 witness output unchanged. Existing default HTML behavior stays.

In build, load corpus once and construct adapters only here. Html uses existing path. Json uses site::analyse(&corpus), a titles lookup over those documents, DefaultAssembler plus AssembleContext::default() to form immutable index PageModels; call JsonRenderer::new(&models); then existing site::build_all with the same corpus, SystemClock and sink. This intentionally performs a second pure analyse/assemble pass; BDR discloses the O(n+e) cost. Index excludes built_at so the default context does not invent time. Return constructor errors clearly via existing run Result<String>; preserve existing build error handling and report. No core or port edits, no dynamic state, no extra dependencies. Tests belong to another worker. DO NOT read independent reference.py. Root compiles and validates. Return patch only.
