Write-tier final bounded test edit, READ-ONLY proposal. Root is sole writer in /home/kmosoti/projects/rha-m2. Never JJ (including status), file/Git mutations, builds/tests, agents, sibling/held-out reads, or spec/corpus edits. Return apply_patch only for the three new JSON test files. Their current version compiles and passes all75 adapter plus4 CLI cases. Do not revisit their working grading logic or copy hashes.

Decided edits:
1. crates/adapter-json/tests/common/mod.rs verify_fixture: perform collect_files(root,...) and reject symlinks/special entries BEFORE reading CASES.json or other payloads. Reuse that actual inventory later instead of rescanning. Keep digest checks and exact closure unchanged. The only behavioral change is refusal before reading an invalid filesystem entry.
2. crates/app-cli/tests/json_renderer.rs: replace four hard-coded calls in registered_cli_cases_are_exactly_graded with a loop matching cli_json/cli_html/cli_check, explicitly skip five known non-CLI kinds, panic on unknown. Increment completed after each assertion, assert completed4, print the observed count. Preserve all existing per-case assertions exactly.
3. Use its existing OwnedTempDir guard: immediately after each temp_root() call in the three assert_cli_* functions, bind a guard owning a clone of that path; remove explicit final remove_dir_all calls so Drop cleans on success or panic. No dangling unused guard type.
4. In adapter-json/tests/registered.rs, add a Unix-only negative control using an owned temporary root and a symlink named CASES.json to a harmless file you create in that same temporary tree. verify_fixture must return Err containing symlink, before reading its content; old ordering would return digest mismatch instead. Do not touch real corpus. Use a local RAII cleanup guard for this test and the existing corrupted-copy test; distinct temp names/atomic counter to avoid collisions. No new dependencies.

All files can be read now; this is a tiny fixed patch. Do not add broad suppression, relax hashes, change fixture values or make validation claims. Root runs the negative before-repair and positive after-repair controls and the full stage gate.
