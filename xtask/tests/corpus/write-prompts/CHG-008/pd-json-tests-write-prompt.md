You are a write-tier worker proposing a decided edit. Repository: /home/kmosoti/projects/rha-m2. Root is the sole branch writer. NEVER run JJ, including status/init. Do not write files, apply patches, run builds/tests, mutate Git/PRs, spawn agents, read sibling projects or held-out material, or edit the spec. Return only one apply_patch block. Read the relevant guides. Other workers own disjoint files; do not change their files.

P-D task is claimed and in progress. BDR-0006 and JSON contract were committed before the crate. Independent corpus was committed at 3ef4d8cceec55a6c7101900677902a0dd8436278 BEFORE any encoder code. Read docs/architecture/json-renderer-contract.md and docs/adr/BDR-0006-adapter-json-immutable-index.md. Corpus bytes and grading are immutable. No core production or test edit is permitted during this adapter stage.

Own ONLY new crates/adapter-json/tests/registered.rs, crates/adapter-json/tests/common/mod.rs, and crates/app-cli/tests/json_renderer.rs. Renderer/CLI workers provide API described in contract; no crate exists yet, so do not infer it is missing work. Do not edit manifests or production code. Read committed registration.toml, CASES.json using compact Python summaries (not whole huge dumps), SHA256SUMS and current Site APIs. Do not modify corpus or derive expectations from encoder/reference output. DO NOT read reference.py algorithm; use stored CASES expectations.

Shared helper common/mod.rs may be included from app-cli test via #[path="../../adapter-json/tests/common/mod.rs"]. Both manifests have site, serde_json and library available (library dev dep in adapter-json). Helpers materialize source-model JSON into PageModel/Node using exact source fields in corpus, and load corpus. Verify fixed CASES SHA256, fixed SHA256SUMS content digest and registration-file SHA256 (MEASURE with tools), exact inventory closure, all per-payload hashes via library::Digest::of, unique79ids and fixed kind counts. No live source equality: archived snapshots are payloads. Reject symlinks/unsafe relative inventory paths. Keep verification functions accepting a root path where useful for an owned-temp corruption negative control. Root also already negatively tested its independent audit. A small integrity test should demonstrate corrupted CASES refusal without editing real fixtures, not merely assert constants. Avoid new dependencies.

Adapter registered.rs must grade ALL75 non-CLI cases: project70 (including comparison_pages and every expected_comparison_* field present), duplicate2, reconcile1, collision1, contract1. Assert exactly75 completed and no unknown kind silently skipped. Shared loader validates the full79 taxonomy; app test grades the other4. Every decoded expected object/array matches exactly (unknown/missing properties fail). Repeated render/assets bytes equal, UTF8 pretty LF output exactlyone trailing newline. Exactlyone index asset at assets/search-index.json. Preserve input models/no mutation. Page outputs and index values compare stored expected_pages/expected_index; comparison pages values/index compare all provided fields, and clock/order paired index bytes remain equal as registered. Duplicate error compares kind/id.

Lifecycle: build Corpus from registered sources; analyse/assemble models, fresh JsonRenderer each build, FixedClock registeredclock, RecordingSink. Compare all registered initial/final/deleted paths, index IDs, and unchanged count; assert metadata clock_type/fresh_renderer assumptions as stored. Collision must return registered substring in InvariantViolation and sink filecount0. Contract case must compare zero violations and expected owner_contract_passed. Source shapes include every Node variant/callout/alignment; parse all strictly and no fallback unknown tags.

App-cli json_renderer.rs grades exactly4 cli_* cases using CARGO_BIN_EXE_rhawiki, owned unique temp source/output dirs with cleanup. Write only registered relative sources. cli_json: exact output path set, exit, indexIDs, titles, schema/kind, every required_page_member and expectedTOC. cli_html default/explicit: exact paths/exit/markers as registered, no clock-dependent byte equality. cli_check: exact stdout JSON and exit. No subprocess cwd assumptions or fixed temp names prone to collisions. Existing app-cli cli.rs and old60 corpus stay untouched.

Keep code readable; assert each registered expectation, do not weaken grades. Avoid broad allow attributes; a narrow shared-test dead_code allowance is fine if needed. Root runs actual targeted/full tests and negative controls; no validation claims. Return patch only.
