Write-tier bounded strengthening after separate verification, READ-ONLY proposal only. Repo /home/kmosoti/projects/rha-m2. Never run JJ (including status/init), mutate files/Git, run builds/tests, spawn agents, read siblings/held-out, or edit corpus/spec. Root is sole writer. Return apply_patch ONLY for crates/adapter-json/tests/common/mod.rs and crates/adapter-json/tests/registered.rs. Current registered75+4 cases already pass. Do not copy hashes or alter expected data.

A separate verifier confirmed all registered fields are consumed and recommended these three narrow additional observations of the existing contract:
1. In assert_project_case, construct a SECOND JsonRenderer from identical models and compare complete Rendered values for every page against the first renderer, plus complete assets vectors. Keep same-instance repeat checks, all goldens and optional comparison handling. This observes equal-input byte determinism across constructor calls, not just one cached instance.
2. assert_json_bytes: every registered root is a JSON object. Require text.ends_with("}\n") in addition to valid decoded JSON, LF and pretty checks, so the suffix after the value is exactly one newline. Existing ends_with newline/notdouble-newline permits "}\n \n". Keep exact decoded comparisons.
3. In collision case, use a CountingSink wrapper over RecordingSink implementing site::OutputSink, tracking write and delete call counts. Delegate list/write/delete normally; types use library::RelPath and library::Digest, site::SinkError. Assert both mutation counters zero when the registered collision errors, while still comparing the registered final file count on inner.files. This directly observes the documented before-any-writes condition. No core implementation changes or new dependencies.

These are contract-strengthening observations, not evidence of a current renderer failure. Keep code small, use actual existing APIs, and do not claim checks ran. Root performs compilation, registered grading, negative controls and stage gates.
