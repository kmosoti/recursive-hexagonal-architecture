You are a write-tier worker proposing a decided edit. Repository: /home/kmosoti/projects/rha-m2. Root is the sole branch writer. NEVER run JJ, including status/init. Do not write files, apply patches, run builds/tests, mutate Git/PRs, spawn agents, read sibling projects or held-out material, or edit the spec. Return only one apply_patch block. Read the relevant guides. Other workers own disjoint files; do not change their files.

P-D task is claimed and in progress. BDR-0006 and JSON contract were committed before the crate. Independent corpus was committed at 3ef4d8cceec55a6c7101900677902a0dd8436278 BEFORE any encoder code. Read docs/architecture/json-renderer-contract.md and docs/adr/BDR-0006-adapter-json-immutable-index.md. Corpus bytes and grading are immutable. No core production or test edit is permitted during this adapter stage.

Own ONLY new crates/adapter-json/Cargo.toml and crates/adapter-json/src/lib.rs. Match manifest house style from adapter-html: version0.1.0, edition/rust-version workspace, publishfalse, metadata implements=["site::PageRenderer"], workspace lints. Normal dependencies ONLY site path../site and serde_json1.0.151. Dev dependency library path../library is needed by the separate test worker. No other dependencies or workspace/allowlist edits.

Implement public JsonRenderer holding immutable precomputed Rendered search index; JsonRenderer::new(&[PageModel])->Result<Self,JsonRendererError>; public error DuplicatePageId{id:String}, deriveDebug/Clone/PartialEq/Eq as useful. Sort input references by PageId without mutating input, reject duplicate IDs, then precompute exactly one asset assets/search-index.json. PageRenderer::render serializes supplied model to <id>.json with no mutation; assets returns stable clone. Source-derived PageId domain matches existing HtmlRenderer. Use site reexports Node,Align,CalloutKind,PageId,RelPath and site::assembly types; never depend on document/library normally.

Implement the exact contract's page and EVERY current Node variant projection, resolved/unresolved links, nullable fields, vector order, raw HTML as text, unchanged ordinary hrefs. Use serde_json::Value/json! and pretty UTF8 plus exactly one trailing newline, deterministic repeated bytes. Search entries sorted ID with id,path,title,headings and recursively collected body text. Preserve adjacent inline fragment concatenation; add boundaries around blocks/listitems/tablecells/quotes. CodeBlock contributes its text, not language. Ignore task state. Normalize using Rust str::split_whitespace then ASCII-space join. Index excludes clock/backlinks. No hidden state or core changes. Read current Node/PageModel definitions but DO NOT read independent reference.py. Contract is the authority; no need to read the huge case stream for implementation.

Document the fresh-constructor/same-corpus lifecycle and narrow source-ID precondition. Avoid broad lint suppression or silent fallback serialization. Root compiles and tests; do not claim validation. Return patch only.
