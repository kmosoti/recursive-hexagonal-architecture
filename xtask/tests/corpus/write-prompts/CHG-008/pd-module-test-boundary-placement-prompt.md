Final independent pre-registration refinement. Same isolation: write ONLY target/m2/module-test-boundary; no target code/tests, JJ, Git mutations, network, held-out material, sibling projects, or spec edits. No boundary implementation exists.

A separate reviewer recommends stronger isolation in the three no-grant controls: the inner cfg(test), cfg(any(test,...)) and cfg_attr rows currently point beyond the workspace, so they also encounter a second boundary. Change those three layouts to point to workspace/shared/helper.rs, inside the configured test root and outside the crate. Keep the non-test or non-exact declaration and put intentionally invalid Rust in the target (after the inner #![cfg(test)] marker for that case). The expected refusal remains "outside crate root". An incorrect test classification would then pass the boundary and fail parsing rather than accidentally satisfy the refusal.

Preserve all 12 case IDs and all other rows/expectations, including the newly distinct outside-test-root reasons. Adjust the independent reference only as necessary for these layouts, refresh measured checksums/metadata, and verify nine-file byte-identical reproduction with owned cleanup. The current additive contract already states these rules; freeze its current bytes unchanged. Root will archive this prompt separately. Finish promptly; no further redesign or new cases.
