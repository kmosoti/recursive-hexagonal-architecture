Independent pre-registration follow-up. Keep all original isolation rules; write ONLY target/m2/module-test-boundary. No target checker code/tests, JJ, Git mutations, network, held-out material, sibling projects, or spec edits. The package is not registered and no boundary implementation exists.

A separate reviewer found that the two configured test-root escapes share the production error substring, hiding wrong classification. The live additive contract now fixes distinct messages. Refresh contractaddendum.md to that exact document. Change ONLY the expected reason_contains for workspace_cfg_test_target_outside_workspace and workspace_cfg_test_canonical_symlink_outside to "outside permitted test source root". Update the independent predicate/reference to return that reason when inherited exact test context is true, mode is workspace, and the canonical target lies beyond the permitted test root. Strict/default and production/nonexact/cfg_attr refusals retain "outside crate root". The invalid configured root relationship retains its separate "crate root" substring. No other case outputs or source layouts change.

Preserve all 12 cases and the original prompt snapshot. Refresh all measured metadata/hashes; verify read-only self-tests and all nine files in fresh-child byte-identical reproduction, then clean that child. Root archives this follow-up separately. Finish promptly and report actual hashes/counts.
