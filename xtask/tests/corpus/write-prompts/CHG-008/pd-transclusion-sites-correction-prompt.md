Independent generator follow-up BEFORE registration or target implementation. Keep the original isolation rules: NO JJ, Git mutations, network, sibling projects, held-out access, docs/spec edits, agents, or target implementation/test reads. Write ONLY your assigned staging package. Read latest docs/architecture/transclusion-contract.md and your own original package/prompt. Refresh frozen contract snapshot and all derived metadata to current committed clarification. Archive provenance is handled by root. Default script remains read-only. Reproduction must recreate the ENTIRE package byte-identically (all nine current files, including reference, snapshots, README, selftestreport, registration, SHA256SUMS) in an explicitly requested strict child directory using only frozen inputs; document copied-package workflow. Verify and clean your own reproduction directory. Do not use target implementation results to choose expected outputs. Report actual hashes/counts with tools.

Assigned target/m2/transclusion-sites. This draft is NOT registered; there is still NO transclusion implementation. Root found concrete contract inconsistencies; independent review is ongoing and root will send its complete follow-up. Correct the following now from normative Markdown paragraph structure and the fixed contract, without reading/running target code:
- TX01/TX06: embed lines directly adjacent to before/after text share their paragraph, so cannot recognize standalone. Put blank lines around intended standalone paragraphs and recompute source lines.
- TX07/TX10/TX11: consecutive embed lines separated only by softbreak are excluded, so put blank lines between intended independent embed paragraphs. Review all12sources for same mistake and recompute descriptor/navigation line fields.
- TX02: expected no transclusions contradicts text_includes page body/other body. Those bodies must be absent. The final fourspace example must be a real indented codeblock after a blank paragraph separator, not lazy continuation; preserve other exclusion meanings and recompute lines.
- TX09 c fullpage→samefullpage edge is blocked immediately. No imported tx-0-c heading should appear, just host c and marker.
- TX10 root's second embed is not a backedge when its target DFS is already finished. It expands too; compute full exact heading observations and markers for BOTH occurrences from contract.
- TX11 same-directory origin tx11/source to host tx11/host gives unmatched href source.md#unknown, not tx11/source.md#unknown.
- TX12 same-directory query/empty references are origin.md?q=1 / origin.md, not a repeated directory prefix. expected_documents of origin MUST include both original images; currently empty. HTML marker observations must match corrected core hrefs.
- Registration kind_count is number of unique kinds (1), not number of cases12; seed is authored (no PRNG), not invented sample1 numeric seed. Exact observed counting only.
Review all expected fields for internal consistency and complete every necessarily triggered witness. reference.py remains an independent author/validator of12sites, not a fullMarkdownparser. Full-package reproduction must produce ALL9files and byteidentical metadata using frozen snapshots. Preserve original prompt snapshot. Root will archive this followup separately. Do not guess if rule ambiguous: report it.
