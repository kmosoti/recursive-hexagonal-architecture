Repair your proposed registered-case tests. Root applied them and the compiler failed; see target/m2/pd-json-tests-first-compile.txt. You remain READ-ONLY, returning apply_patch only for the same three test files. Never run JJ (including status), mutate files/Git, run builds/tests, spawn agents, read siblings/held-out, or edit corpus/spec. No production changes. Root owns validation.

Fix all of these concrete issues in one bounded patch:
1. LinkTarget lives in site::assembly, not site root. Remove unused find_case import in registered.rs.
2. Rust Vec<String>/Vec<&str> cannot compare to serde_json::Value. Convert the ACTUAL vector with serde_json::json! or decode expected strings explicitly, preserving exact ordered equality in every lifecycle/CLI assertion.
3. You hand-copied different incorrect hashes for reference.py into two constants. Remove redundant SHA256SUMS text, INVENTORY and ALL_FILES literal copies entirely. Keep only THREE pinned digest constants (CASES, registration file, SHA256SUMS), measured directly with sha256sum. Verify pinned sums bytes, parse the VERIFIED sums lines into an ordered map of safe relative paths to SHA256, reject duplicate/invalid paths/hashes, compare actual file closure to those paths plus registration.toml/SHA256SUMS, hash every payload against the parsed digest. No extra dependency; library::Digest is available. This keeps the committed inventory as its owner and avoids manually copying its facts. Reject symlinks and non-file special entries before reading payloads. Keep the corruption negative test; real fixture supplies the valid control.
4. validate_taxonomy must also require79 unique string IDs, not only kind counts.
5. source node materialization lacks Html; add exact {kind,text} -> Node::Html. Use checked u8/usize conversions rather than truncating source numbers.
6. assert_project_case's exact_keys currently rejects the registered comparison fields. Base five fields are required. When comparison_pages exists, require expected_comparison_index; accept and grade expected_comparison_pages IF it exists. The clock case intentionally supplies only expected_comparison_index, while the reversed-order case supplies both. Validate exact allowed key sets for those actual registered shapes. Never invent an expected field or ignore one present. Compare repeated full Rendered values (path and bytes). For comparison output maps, verify exact path set when expected pages are present. Always verify comparison index bytes/value and equality to baseline index bytes, as registered. All original stored expectations remain untouched.
7. CLI test should loop the registered cli_* cases and assert exactly4 graded, using kind dispatch; shared taxonomy rejects unknown kinds. Preserve each registered field assertion, expected paths/exit/schema/title/TOC/index IDs/markers/check JSON. Print concise completed counts in adapter and CLI tests so root's --nocapture evidence shows75+4 graded.
8. Keep temporary directories owned and cleaned; no broad lint suppression beyond the narrow shared test dead_code allowance.

Read the corpus compactly before changing shape assumptions. All79 cases were fixed before code, and the grader must conform to them. Do not read reference.py algorithm or change any data. Return only the repair patch and no claimed test result.
