Second decided extractor repair. Root applied repair1; cargo check now passes (syn3 trait_ PAIR is correct; DO NOT change to syn2 triple). Same read-only proposal boundaries; no tests, writes, git/jj, siblings/held-out or agents. Output only apply_patch updating xtask/src/modules/extract.rs.

Concrete root/deep-review findings and precise decisions:
1. There is NO visit_path override, so ordinary expression/type/impl paths are never recorded. Add visit_path that converts path names, calls record_path syntax/for_use=false, then syn::visit::visit_path(self,path) to traverse generic arguments. Report unknown for explicit crate/self/super prefixes; avoid treating arbitrary unresolved prelude/local bare names as module dependencies. Override visit_visibility no-op: pub(in crate::...) is visibility, not dependency. Existing visit_attribute no-op stays.
2. scan_macro_tokens Group arm recurses but never advances index -> infinite loop; increment after recursion. while bound must be end+2 < tokens.len before reading end+2. Keep paths in arguments heuristic and ignore strings.
3. Associated items: don't reject target.len > owner.len+1. Longest actual module prefix owns the path; validate the FIRST nonmodule segment (target[owner.len]) as a declared/exported item and preserve all associated suffixes. Explicit facade remains facade (M19). Foreign_internal is later based on MODULE tree, not segment count. Known internal unresolved target should have an explicit limitation.
4. Method inference: add visit_expr_method_call to emit method_type_inference/detail method name when a glob is active (the explicit EM-M03 hole), then default-traverse receiver/args. Don't blanket-noise every std method without this context.
5. Rust module lookup was MEASURED by pinned rustc1.98.1 (do not guess): carry both default child dir and path-attribute base.
   - Current file-backed module path attrs resolve against its file.parent; current inline module path attrs against its current default child dir.
   - ordinary file child mod name: lookup parent_default/{name.rs,name/mod.rs}; its default child dir is parent_default/name.
   - #[path=p] FILE child: lookup path_attr_base/p; its default child dir is selected.parent (NOT logical/name, NOT selected stem). Its own path-attr base is selected.parent.
   - ordinary inline child: default dir parent_default/name; its attr base is that same dir.
   - #[path=p] INLINE child: default dir path_attr_base/p; attr base same. It is supported, not ignored.
   Implement install_module with explicit directory + attr_base (or equivalent). Root both=file.parent. discover_file accepts caller-decided directory, not recomputed stem; install file attr_base=file.parent. Preserve canonical bounds/active recursion, no global seen-file skip. Keep a local canonical filename clone for cleanup, not a module_paths lookup that can select a prior file. Unusable child source stays Err (production contract controls, not supplementary reference invalid-input protocol).
6. exported() must resolve glob-reexported names recursively with (module,name) cycle guard. Resolve each exporting module's glob import in THAT module's import scope (not caller's block). Return boolean so caller records exporting facade::Thing, not hidden underlying::Thing. Also use that exported check when validating a qualified facade item in record_path. Avoid alias recursion loops. If all bindings at one frame were filtered test-only in production, continue outer frame search rather than immediately Unknown.
7. Preserve current block-use logic: visit_stmt unconditionally calls walk_item after a guard used only to avoid adding use aliases to definitions; it already emits edges. Do not duplicate bindings/emissions. Remove genuinely dead file/add-binding helpers if left unused and easy, not a refactor.

No corpus/schema/rules/test edits. Root compares frozen256 and all headline paths afterward. Output patch only; no claimed verification.
