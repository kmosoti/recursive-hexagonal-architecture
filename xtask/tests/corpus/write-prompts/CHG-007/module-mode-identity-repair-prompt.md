WRITE-tier decided repair, read-only root solewriter. Return apply_patch only; no writes/apply_patch/tests/agents/git/siblings/heldout. Patch current applied files xtask/src/metadata.rs, modules/single.rs, modules/workspace.rs, tests/module_only_cli.rs ONLY. Separate reviewfound duplicated external derivation inconsistentwithproduction and incomplete sourcebinding; exactdecisions:
1. Refactor metadata command parsing into private load_metadata(root,manifest)->Result<Metadata,LoadError> used currentload(). Add public SingleModule {package:String,manifest_path:PathBuf,source:CompositeSource} and load_single_module(root,manifest:&Path,rules:&Path)->Result<SingleModule,LoadError>. Require exactlyoneworkspace member(packageidmatch), selectedpackage manifest canonicalequals suppliedmanifest canonical (rejectvirtualworkspace), then derive source using EXISTING composite_source(package,&RhaMetadata{composite:Some(rulesdisplay),..Default},&members). This reuses identical edition/source/dependency helper including build exclusion/memberlibalias mapping. No manualCargo parsing/newdeps; single.rs remove duplicate metadata structs/helper/load code and call shared loader, map same config/tool errorclass. Existing load() behavior unchanged.
2. Add workspace::source_files(crate_root,&Extracted)->Result<BTreeMap<String,String>,String>: dedup extracted.modules filepaths, canonicalbound root beforehash, exactsha256 actualallsourcefiles. Normal workspace modulecheck records source_files_sha256; single subject and modulecheck records same map. Root source_sha256/manifests remainactual. All digests in checks rules_digest become sha256:<hex> (including normal workspace path), no unrunhasher invented.
3. module_only_cli multiworkspace rejection currently duplicates --module-rules in runextra. Call helper with root/Cargo manifest and frozenfixture rules, emptyextra; assert summary error message mentions exactlyoneworkspacepackage (inspect actualfailure JSON field). Add singlemember virtualworkspace errorcontrol or adapt tempfixture. Add builddependencyshadow control if easyusing helper fixture: private localmod name equalbuilddependency muststayinternal; cannotfake support externalcustomlibunder--no-deps. Productionmembercustomlib alias already metadata helpercovers; one-member modulemode cannot include secondmember. Existing renameddep control remains. Add allfollowed sourcefiles hashmap includes customlib+child path; tamperchild (legal content) changes sourcehash map, rootunchanged. No immutablefixture edits.
Return patch only. Root compiles/tests independently. No clippy suppressions.
