Mechanical WRITE-tier proposal for DECIDED module rules. Root is sole branch writer and integrates after stage1. Read-only in /home/kmosoti/projects/rha-m2; no files/git/jj/tests/agents/sibling or held-out reads. Final ONLY apply_patch patch adding xtask/src/modules/rules.rs. No other files or registered-data changes.

Read docs/architecture/module-check-contract.md, fixed module manifest cases, real crates/site/rha-modules.toml, and optional xtask/tests/corpus/module/random-rule-expectations.json/check.py for the registered diagnostic schema. Extractor API is public in modules/extract.rs: Extracted {modules:BTreeMap<String,PathBuf>,edges:Vec<Edge>,limitations:Vec<Limitation>}; Edge {source,target,test_only,extraction}; Limitation {source,code,detail}. Root is repairing extractor internals; your file consumes only this stable interface.

Public API:
#[derive(Debug)] pub struct Checked {pub findings:Vec<serde_json::Value>,pub test_edges:Vec<Value>,pub module_edges:Vec<Value>,pub limitations:Vec<Value>,pub rules_path:String,pub rules_digest:String}
pub fn check(crate_root:&Path,crate_name:&str,rules_path:&Path,graph:&Extracted)->Result<Checked,String>
Use existing serde/toml/serde_json/util::sha256_hex, no deps. Read rules only after canonicalize and enforce inside canonical crate_root; bytes hash. Parse strict top-level components/allow/deny tables; all three deny booleans required. Nonempty components, nonblank/unique component aliases/paths, correct normalized crate prefix and actual discovered module for each component; allow names and targets must refer to declared aliases; omitted allow entry means no permitted siblings. Nested component paths are VALID, duplicate exact paths invalid. Don't reject cycles in the permission matrix alone: observed dependencies define cycles, fixture isolation permits both directions. Unknown config or unreadable files =>Err, not pass.

Algorithm fixed:
- Semantic owner of a source module is deepest declared component prefix. Parent of a component is nearest declared strict ancestor, else crate root.
- Same owner references within that component are not cross-component findings. Parent/glue references downward to child facade are allowed; reaching below child facade is D3.
- For source component A, identify its parent scope P. A target outside P, or P's glue/items rather than a sibling component, is D2, even public. Emit ONLY D2 for containment/upward; don't insert it into sibling graph. To is canonical target replacing crate name with 'crate'; from A alias; depth=segment count of A path including crate root. M05 scoring->constraints is D2 even if allow lists constraints.
- Otherwise identify target's first declared component directly below P, call B. If B != A this is a sibling edge A->B. (If B contains a deeper target component, still project to B for this level.) Record it even when D3 occurs. D3 if target's longest discovered MODULE prefix is strictly below B's facade path. A type's associated item is not a module, so B::Type::method is facade usage. Preserve full canonical target in foreign_internal to. Root/unowned glue has no D4 allow requirement but still cannot reach below a child's facade.
- Test-only edges never participate in errors. Emit top-level listed note facts {rule:'modules.test_edge',severity:'note',from:<source minus crate prefix>,to:<target component alias, else normalized target>} for M11; normalized full four-field raw edges remain available in module_edges. Keep production raw edges there too.
- All observed sibling edges form directed graphs (nested levels naturally disjoint); compute SCCs by deterministic full traversal. One cycle per cyclic SCC: lexicographically least start, shortest simple return path then lexicographically least tie. closed path first==last. Use extraction='heuristic' if any edge on CHOSEN witness has a heuristic occurrence (off-witness heuristic must not taint it). Else syntax.
- Cycle diagnostic retains undeclared internal SCC edges as sorted undeclared_edges [{from,to}] and subsumed_rules ['modules.undeclared_dependency'] (empty when none). Don't emit those D4 duplicates separately. Independent undeclared edges outside cyclic SCCs remain findings. If deny.cycles=false, don't hide undeclared edges under an unreported cycle. D3 remains independent of cycle/D4. All original corpus grading stays unchanged.
- Finding top-level fields rule,severity='error',from, optional to,path,depth,extraction as applicable, message,manifest_path=crate_root/Cargo.toml,witness with actual source/target/module source file/rules file. Exact registered witness keys top-level, no prose in them. For cycles from canonical start, path alias vector. Deduplicate equivalent rule/from/to/depth or cycle facts deterministically, merge heuristic evidence conservatively.
- graph.limitations become limitations, NEVER findings (NoAlarm grades any finding). Include disabled-rule/approximation scope honestly if flags off. Invalid config is Err. No tool/metadata/CLI implementation in this file.

Keep it straightforward and deterministic. Use BFS/reachability or DFS/SCC; these graphs are small and scanned fully. Do not fabricate edges from allow entries. No self-loop from same-component internal cycles. No tests or validation claims in your role. Return patch only.
