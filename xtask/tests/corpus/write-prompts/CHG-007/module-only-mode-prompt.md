WRITE-tier decided stage3 CLI proposal, READ ONLY repository /home/kmosoti/projects/rha-m2. Root sole writer. Return apply_patch only, DO NOT invoke apply_patch or any writes/tests/agents/git/siblings/heldout. Proposed diff is queued until stage2 committed. Scope new xtask/src/modules/single.rs, modules/mod.rs exports, architecture.rs/cli.rs/main.rs options, new xtask/tests/module_only_cli.rs. No other files.
Design approved separate review: explicit architecture --module-rules PATH is MODULE-ONLY mode for frozen Rustfixtures that have rha-modules but no composite/Cargorules metadata. Require --manifest-path, exactly one workspace package and exactly one librarytarget. Reject --rules and --transitive together. Derive source target name, edition, Rust-visible dependency aliases from cargo metadata --no-deps --offline --format-version1 at manifest. Use actual source/name, normalized hyphens, external aliases; no Cargo.toml parsing. Bounds root/source/rules enforced by shared extract/rules. source sha, manifest sha, rules sha computed from actualfiles. If dependencies members outside package? explicit one member required; externals Cargo dependency rename or name normalized (librarytarget custom external alias not available --no-deps but direct deps metadata rename correct; source limitations if needed).
New single::report(root:&Path, manifest:&Path, rules:&Path)->Result<serde_json::Value, ModuleError> with public {class:&'static str,message:String} or equivalent. Tool error for cargo spawn/nonzero/invalidoutput (exit3), configerror for invalidselection/unsupportededition/source/rules (exit2). Uses SAME modules::extract::extract and rules::check; no fake crate graph/classification/Clippy. Canonical report schema_version1, tool=graph::report::tool_identity(root), subject {mode:"module_only",workspace_root,manifest_path,manifest_sha256,source,source_sha256,crate_name,edition,rules_path,rules_digest:"sha256:...",metadata_mode:"no_deps"}, module_checks actual crate/requiredtrue/outcome and counts/limits, findings,test_edges,module_edges,limitations, summary{crates:1,errors,warnings,outcome}. Tag moduleobservations package=package_name/crate=crate_name. Explicit source_approximation limitation. No duplicate extractionlimits. For failures architecture existing failure() can render/write config/tool report, must subject mode module_only? add appropriate metadata but no inventedpass. Expose early branch in architecture::run BEFORE crate rulesload; write_report/root target same existing, text/mdcompletedreport functions; common render helper if simple but no unnecessaryrefactor.
Add options module_rules_path Option<PathBuf> and CLI --module-rules conflicts/requires constraints, main wiring. Existing Options literal in tests may need default; root fixesoutside if needed.
Tests use actual CARGO_BIN_EXE_xtask and immutable M01/M11/EM-M01 fixture paths. Require cycleactualexit1 and exactfrozenwitness, M11 testnotes/noalarms0, expectedmiss0+macro limitation. Require fullrepo multiworkspace rejected nonzero configerror; requiredmanifest/conflictingflags parseexit2. On temp onecrate actual customlibname/source and renamed dependency, verifyboundidentity and allowedeffectpaths don't become moduleedges. Badmissingrules and malformedsource nonzero2. Keep frozenfiles immutable. No testsrunclaims. Return patch only.
