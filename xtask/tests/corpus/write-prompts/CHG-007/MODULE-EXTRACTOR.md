You are a mechanical WRITE-tier proposal generator for a decided implementation. The root Codex Executor is the sole branch writer and integrates only after its stage-0 gate. Work read-only in /home/kmosoti/projects/rha-m2: no filesystem writes, no git/jj mutations, no siblings or held-out material. Do not run tests or claim validation. Your final response must be a complete apply_patch-format patch adding ONLY xtask/src/modules/extract.rs (no markdown fences). Root will save/apply it later. Do not spawn agents.

The independent module corpus was committed in 08e560d BEFORE this code generation. Read AGENTS.md, xtask/AGENTS.md, docs/architecture/module-check-contract.md and xtask/tests/corpus/module/README.md, relevant reference.py and representative random/headline sources as necessary. Do not change registered data. The production implementation uses syn 3.0.6 with full,visit,extra-traits and proc-macro2 1.0.107. Do not add dependencies or edit Cargo/lib files; parent handles integration.

Implement source extraction ONLY, not architecture rules. Public API:
#[derive(Debug,Clone,PartialEq,Eq,PartialOrd,Ord,serde::Serialize)] pub struct Edge { pub source:String, pub target:String, pub test_only:bool, pub extraction:String }
Same derives pub struct Limitation { pub source:String,pub code:String,pub detail:String }
#[derive(Debug,Clone)] pub struct Extracted { pub modules:std::collections::BTreeMap<String,std::path::PathBuf>, pub edges:Vec<Edge>, pub limitations:Vec<Limitation> }
pub fn extract(crate_root:&std::path::Path, root_file:&std::path::Path, crate_name:&str, edition:&str, externals:&std::collections::BTreeSet<String>) -> Result<Extracted,String>

Implementation decisions fixed:
- Parse with syn, logical module tree separate from files. Two-phase discover declarations/imports then resolve paths (supports forward declarations). Collect module/item symbols, including facade reexports, for alias/glob resolution. Root logical path is normalized crate_name. BTree structures and canonical sorting for deterministic output.
- Follow inline/out-of-line name.rs and name/mod.rs, #[path], correct file vs inline module lookup; require canonical files inside crate_root and detect recursive file/module loading, missing/ambiguous modules as explicit errors/limitations, never silent omission. Ordinary syntax parse failures are Err.
- Resolve crate/self/repeated-super paths; bare use paths under edition2015 root-relative; edition2021/2024 uniform names need lexical imports/local modules, NOT arbitrary implicit sibling fallback. Track function/block scopes for local use aliases, parameters and locals, so shadowed variables aren't module edges and block imports do not leak to siblings/signatures. Distinguish local/imported symbols, std/core/alloc/externals, primitive/prelude/generic/Self names.
- Expand nested/grouped/self/glob/renamed use trees and resolve alias chains. Explicit qualified facade members stay at that facade (M19 crate::score is root, not the hidden ordering edge). Local binding Alias::Item resolves binding target; glob imports resolve unambiguous visible names, retain uncertainty when ambiguous. Record use/import references themselves as well as expression/type/impl paths.
- Exact paths in every AST position, using visitor traversal including impl headers/types and function bodies. Do not traverse same module twice. Inherit cfg(test) on modules AND items; retain test edges with test_only=true. Other conditional cfgs are conservative and documented; do not falsely claim configured compilation.
- Macro definitions and include! are not expanded; report limitations matching the independent finite-domain protocol where applicable. Scan invocation arguments as token streams recursively; recognize module/path chains including aliases; mark extraction heuristic. Strings/comments not paths. Visit macro invocation path only if a known internal binding (avoid noise for assert_eq etc). Token groups should not turn a heuristic occurrence into syntax.
- Drop references to external crates, local variables/types/primitive/prelude values, and paths whose owning logical module equals source. Retain module edges within a single component (collapse is another module). Deduplicate Edge by all four fields and sort exactly source,target,test_only,extraction. Limitations sorted by source,code,detail. Match the finite corpus normalization; unsupported/unresolved internal paths must remain explicit limitations outside that domain.
- Public fields/prototypes exactly above. Keep helper code cohesive and borrow-checker-friendly (owned scope maps/AST as needed). No panic on malformed user source; return Err or explicit limitation. For I/O errors distinguish path+failure in Err so the caller can classify. Filesystem I/O is allowed in xtask tool, not core crates.

This is a code proposal, not a completed stage. Root separately compiles and compares all registered cases. Return only the full add-file patch; no implementation results or claims.
