WRITE-tier decided source-guard repair, READ ONLY. Root solewriter. Return apply_patch only for xtask/src/clippy_template.rs. No actualedits/apply_patch/tests/agents/git/siblings/heldout.
Current unforbidden() substring-scans entire source for #![forbid(...)] and contains matches. It accepts commented/string/nested-module forbids and lint names with suffix, violating documented crate-root-only guarantee. Replace with syn::parse_file(source); parsefailure means all FORBIDDEN_LINTS missing (failclosed). Only file.attrs unconditional inner attrs with path exactly forbid. Parse argument list as syn::Meta punctuatedComma (reason = "..." allowed ignore); accept exact Meta::Path segments clippy::disallowed_methods/types/macros. Alternative broad reason hidden strings must ignored. Direct forbid(clippy::all) perhaps does not protect restrictionlints? onlyexplicitregisteredthree count. Ignore cfg_attr conditionalforbid (can'tproveenabled), outermoduleforbid irrelevant. Use existing syn3 dependency no newdep. Don't suppresslints. Precise exactpaths no prefixes/suffixes, don't substringrender tokens.
Add one test contrasts clean actual root allthree forbids passes vs each commentedtext, literaltext, nested moduleonly, cfg_attr(false,...), nameswithsuffix allremainmissing; mixedrootone +nestedother shows onlyothermissing; malformedsource failsclosed, rootforbid with reason string acceptedexactthree. Preserve original tests/data. Root will run NEW regression against oldimplementation first (splitpatch), thenapplyfix, capturing evidence. Return testchange distinct hunks and implementationhunk easyseparate. No validation claims.
