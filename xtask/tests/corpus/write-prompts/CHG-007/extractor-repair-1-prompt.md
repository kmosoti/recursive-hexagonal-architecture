Repair your first extractor proposal mechanically. Root applied it to xtask/src/modules/extract.rs and cargo check failed; read target/m2/stage-1-extractor-first-compile.log and the current file. Same read-only scope: no files, git/jj, siblings/held-out, tests or agents. Output ONLY an apply_patch patch updating xtask/src/modules/extract.rs. Root remains sole writer and verifier. Do not change corpus, expected values, API, Cargo or other files.

Fix all compiler diagnostics. Also implement these decided corrections before the first corpus run:
- Limitation derives serde::Serialize. Resolution derives Debug/Ord as needed, sort candidates directly rather than debug strings. Syn Item is non-exhaustive: fallback safely, recording unsupported source where meaningful. Fix UseTree::Rename self branch to unit behavior; no arbitrary clone. Remove custom Path method mistake (use syn_path_names(...).join("::") for Path; existing Meta helper can stay).
- ModuleData must carry the inherited test bool. install_module stores it; final Walker::new uses that bool, NOT data.file.exists() (the current code makes every edge test-only). Preserve test ancestry through inline and file modules.
- No panic on source read/parse failure. Delete return_to_error. Use Result propagation through discovery/install, OR a first error slot on Engine that extract converts to Err before traversal. Error includes source path/cause. Do not silently create a pass after error.
- Canonicalize EVERY discovered child file before reading, require within canonical crate_root, and use canonical paths for active-file cycle detection. Missing/ambiguous source paths must be explicit errors or documented limitations, never a successful empty module; error is preferred for a declared source. A file used at two separate non-recursive module paths is legal: don't skip it because loaded_files contains it. Use active recursion-stack detection, not a global seen-file exclusion. Preserve logical module paths.
- File module directory semantics: normal foo.rs owns foo/ for submodules; foo/mod.rs owns foo/. A #[path] source's nested lookup derives from its actual source filename/location as Rust does, not blindly from the logical module name. Inline modules append logical name to current lookup directory. Root source uses its containing directory. If unclear inspect the independently registered reference/fixture sources, but no data changes.
- walk_use must resolve with for_use=true. record_path currently always resolves false, which bypasses the edition2015 registered bare use case. Separate `for_use` and `report_unknown` flags or a helper accepting a Resolution; expression/type paths use false. Ensure function-body uses still emit edges, self/glob/rename leaves normalize as registered.
- Keep the public API as the original prompt. Root will build/compare the 256 cases; do not claim any result. No test code needed in this patch.
