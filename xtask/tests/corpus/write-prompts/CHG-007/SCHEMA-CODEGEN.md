You are a mechanical WRITE-tier proposal generator for a decided change. Root is sole writer on chg/007-module-level. Read-only in /home/kmosoti/projects/rha-m2; no file writes, git/jj mutations, siblings or held-out reads, agents or tests. Final response ONLY an apply_patch-format patch adding xtask/schema_codegen.py, no fences. Root applies and verifies independently. The record schema corpus is already committed before this code proposal; inspect git log/ls-tree to verify.

Read docs/architecture/record-schema-contract.md and xtask/tests/corpus/record-schema/FORMAT.md, INVENTORY.json and derive.py as needed. Implement a deterministic Python3 standard-library TRANSLATOR from the registered inventory to seven JSON Schema 2020-12 files .rha/schemas/{family}.schema.json, invoked python3 xtask/schema_codegen.py [--check]. --check never writes and exits nonzero if any output is missing/different; default writes schemas. Do not derive requiredness from live records and do not implement an evaluator. No tests or test execution in your role.

Decided translation: recursively use nodes at JSON-pointer paths, types unions, required_keys, allowed_keys; closed_record => properties and additionalProperties:false; open_map => object type plus additionalProperties:true or {type:string} for string maps (retain nullable type union). Arrays recurse at /*; unobserved items are {}. Observation-only payload descendants aren't constrained beyond the boundary type. Escape JSON pointers correctly. Extensions can add a property, set types and required_in_parent, enum values.

Variant paths use JSON Schema if/then/else with the inventory's exact selectors, branch node maps overriding base nodes. any_key_present: object and anyOf required([key]); key_present_or_type: object and (required(intent) OR required(source) with source type object). Build branch schemas with current variant temporarily disabled to prevent recursion, but apply descendant variants (notably full/compact provenance). Do not union branch requiredness. All variant branches retain their own additionalProperties closure.

Type errors take precedence over enum diagnostics for the SAME value: emit type directly and guard enum with if type then enum, so malformed type gets only schema.wrong_type. Normal valid-type but unsupported enum gets schema.invalid_value. H4 exactly_one_key_present: if object with neither key, then required first key (missing_field); if object with both keys, then not:{} (invalid_value). Guard these conditions by type object so a wrong whole item type doesn't add invalid_value. Other paths get normal recursive schemas. Preserve required fields from selected variants and optional extension fields. Read FORMAT for all formats, no guessing.

Schemas include $schema https://json-schema.org/draft/2020-12/schema, a descriptive title and metadata binding inventory SHA256/source revision/contract (custom x-rha-* metadata okay); avoid invented remote $id or external references. Serialize sort_keys, indent2, newline, no nondeterministic timestamps. Resolve root from script location independent of cwd. Include helpful errors for malformed inventory rather than silently falling through. No new corpus, fixtures, schema files, Cargo edits or runtime lint in your patch; ONLY translator code. Return complete add-file patch.
