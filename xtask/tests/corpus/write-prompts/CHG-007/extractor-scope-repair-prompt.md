Decided scope repair after real tests. Same read-only WRITE-tier proposal boundaries, only patch xtask/src/modules/extract.rs; no tests/writes/git/jj/agents/siblings/held-out. Root is sole writer/verifier. Root applied repair2 and repaired remaining directory-shadow/guard-type/alias-priority issues. Frozen256 differential now PASSES exactly5229 edges, plus safety/path/facade controls. New regression xtask/tests/module_extraction.rs::lexical_pattern_bindings_are_not_module_references FAILS on closure: local 'ordering' becomes edge caller->caller::ordering. Read target/m2/stage-1-scope-controls-run.log and current source/tests. Preserve passing corpus.

Implement scoped bindings for closure, for, match-arm, if-let/while-let (including && let-chains in evaluation order if practical). Use existing Frame/collect_pattern_names; push/pop lexical frames, visit iterator/initializer in outer scope, then bind pattern names for body/guard. Closure input type paths and return type are visited before value bindings. Match arm bindings cover guard and body. If/while condition let bindings apply to following && clauses and then/body, NEVER else or after expression. Do not just mark all locals for the rest of the module.

Pinned syn3 API has local docs generated by root at target/doc/syn/. Do not assume syn2:
ExprIf {attrs,cond:Box<Expr>,then_branch:Block,else_branch:Option<(Else,Box<Expr>)>}; ExprWhile {attrs,cond,body}; ExprForLoop {attrs,pat:Box<Pat>,expr:Box<Expr>,body:Block}; ExprClosure {attrs,lifetimes:Option<BoundLifetimes>,inputs:Punctuated<Pat,Comma>,output:ReturnType,body:Box<Expr>}; Arm {attrs,pat:Pat,body:Box<Expr>} has NO guard field (guard is in pattern). Read local PatGuard docs if needed.

Also distinguish value locals from type/generic locals so a value named 'ordering' does not mask qualified ordering::Item module paths. Add a type_locals set for generic type params (function/impl/trait frames) or equivalent. Normal value locals only suppress single-segment value-looking paths; type locals suppress qualified paths too. Existing block definitions may stay conservative, but no new heuristic loss for the frozen domain. Collect pattern identifiers without accidentally importing bindings from nested expressions/closures inside a guard. Do not expand inference scope beyond these lexical fixes.

Clean the already unused ModuleData.file and unnecessary parentheses if easy. No corpus or test edits. Return complete apply_patch only; root reruns all8 extraction tests independently.
