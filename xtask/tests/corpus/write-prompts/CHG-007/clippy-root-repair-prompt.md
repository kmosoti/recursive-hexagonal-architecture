WRITE-tier decided repair. Read-only repo, root sole writer. Return apply_patch only; DO NOT invoke apply_patch, edit, test, run agents, git, siblings or heldout. Scope xtask/src/{clippy_template.rs,metadata.rs,architecture.rs,graph/model.rs,graph/classify.rs,graph/report.rs}, xtask/tests/{architecture_rules.rs,modules_cli.rs}.
Confirmed legal core [lib] path src/custom.rs gets effect.core_clippy_template because check() hardcodes src/lib.rs. Fix exact roots from cargo metadata, never parse manifests. Add CrateNode.source_roots Vec<PathBuf>, filled from all production library target kinds (existing is_library_target) or bin; sort/dedup. CoreCrate has roots Vec<PathBuf>; architecture::clippy_template_rule takes graph too, finds matching node by name AND manifest identity from outcome classification, passes roots. check template once percore, unforbidden each root; no sources should fail closed with existing Unforbidden finding, no fabricated src/lib.rs coverage. Synthetic node/CoreCrate literals populate actual conventionalroot (tests) or empty when rootunimportant, preserving tests. Metadata legacy synthetic snapshots lacking targets remain loadable, no broad schema change.
Extend modules_cli standalone test to prove real customroot and an additional binroot both checked: production code in core lib src/custom.rs declares forbid. Add bin Cargo [[bin]] explicit src/runner.rs initially forbid +fnmain. Negative remove only bin forbids -> nonzero finding naming bin path; restore and test customlib missingforbid similarly; restore before current modulefailure cases. Existing invalid module test reverse edge actually forms cycle with ordering->constraints, so assert modules.cycle (path constraints,ordering,constraints) and witnesssource core_custom::constraints; never expect nonexistent standalone D4 per contract. Do not alter registered data or expectedheadline grading. Metadata comment composite reserved outdated update toactual.
Return patch only; don't validate or claim pass.
