Mechanical WRITE-tier proposal, read-only /home/kmosoti/projects/rha-m2, root sole writer. No file writes/git/jj/tests/agents/siblings/held-out. Return ONLY apply_patch patch for xtask/src/graph/model.rs, xtask/src/metadata.rs, and the existing CrateNode literal builders in xtask/src/graph/classify.rs, xtask/src/graph/report.rs, xtask/tests/architecture_rules.rs. Do not change other behavior/tests/registered data. Root integrates in stage2 after stage1 is green.

DECIDED interface: add #[derive(Debug,Clone,PartialEq,Eq,serde::Serialize)] pub struct CompositeSource {pub rules:String,pub source:PathBuf,pub crate_name:String,pub edition:String,pub externals:std::collections::BTreeSet<String>} in graph/model.rs; add pub composite:Option<CompositeSource> to CrateNode. None for noncomposite/synthetic old builders. This preserves cargo metadata's currently discarded RhaMetadata._composite.

metadata.rs: retain composite name (rename field directly composite). Package target retains name/src_path/kind; Package edition. Defaults may keep existing noncomposite synthetic metadata tests compatible, but declared composites require a nonblank rules name, a library/proc-macro target with nonblank name+source, and supported edition2015/2018/2021/2024. Invalid declaration -> Configuration error, never silent None. A composite on a package with no supported library target is explicit config refusal. No Cargo.toml text parsing or extra cargo metadata call.

Build CompositeSource from SAME metadata snapshot used by crate rules. Crate identifier comes target.name, not guessed package name. Dependency externals include dependency rename when present, otherwise normalized package name; for workspace path-member dependencies with a custom library target name and no rename, use that member library name. Preserve package/source identity when identifying member (same existing path/source matching), never treat registry package with same name as workspace member. Include dev names so cfg(test) paths are recognized; build-only names can be excluded. Always normalize '-' to '_' for Rust identifiers. Builtin std/core/alloc are extractor-owned, no need in metadata field.

Existing direction/classification fields and errors remain unchanged. Fill composite:None in exact old node literals (rg scoped to xtask/src and xtask/tests *.rs, exclude corpus paths; DO NOT broad-search historical evidence snapshots). If any old metadata unit explicitly uses composite with missing target info, preserve the intended assertion by adding correct metadata fields to that unit only, explaining in a comment that W7 now consumes source identity. Do not weaken expected errors. No dynamic filesystem source checks in build(); module runner will bound/read source and rules. Use Result/Error existing style, no panic on metadata input. Return patch only and no claims of compilation/validation.
