Mechanical WRITE-tier test proposal, read-only in /home/kmosoti/projects/rha-m2. Root sole branch writer. No files, git/jj, siblings/held-out, tests, agents. Return ONLY apply_patch patch adding xtask/tests/module_extraction.rs (no fences). Do not touch production code or registered data.

Read docs/architecture/module-check-contract.md and xtask/tests/corpus/module/README.md, registration.toml, representative random maps/expected JSON. Data committed before implementation. API is xtask::modules::extract::extract(crate_root:&Path,root_file:&Path,crate_name:&str,edition:&str,externals:&BTreeSet<String>)->Result<Extracted,String>. Extracted has modules:BTreeMap<String,PathBuf>, edges:Vec<Edge>, limitations:Vec<Limitation>, both serializable; Edge fields source,target,test_only,extraction; Limitation source,code,detail.

Tests decided:
1. Enumerate exactly 256 random/Rdddd.json excluding .expected.json. Materialize each files map under a unique temp directory, validate relative paths/no traversal before writes. API root is that crate dir and root_file dir/src/lib.rs. crate_name/edition come from JSON. Compare json!({schema_version:1,edges:got.edges,limitations:got.limitations}) EXACTLY to sibling Rdddd.expected.json (all four edge fields; no ignored limits); accumulate failure ids plus first useful missing/extra edges and limits. Print count and actual total normalized edges/test/heuristic counts. Assert registered counts (read actual registration, don't invent). No Rust compiler invocation here: generator already compiled all, root does headline Cargo legality in stage3. Keep frozen data unchanged and remove scratch files.
2. Small requirement-derived controls for cfg propagation: production sibling use false, same reference in nested cfg(test) module true; function-level cfg(test) edges true without marking surrounding module production edges true. A function-local unused use must emit an edge even without subsequent identifier usage. Named facade associated item path (crate::b::Type::method) remains an edge, not a foreign-module interpretation. Normal source valid control; malformed Rust source returns Err rather than panic.
3. Source boundary controls: #[path] escape to a syntactically invalid file OUTSIDE the provided crate_root but within the test scratch parent must return Err naming outside crate root (shows boundary refusal before parsing content); a symlink escape same under cfg(unix); missing source Err OR explicit nonempty limitation, never Ok with no limitation. Same physical source loaded at two independent logical module paths must yield both module names/edges (no global seen-file skip). Cyclic source declarations must terminate with Err/limitation. Keep all files generated under unique scratch paths, no real user files accessed.
4. Macro group/trailing-token regression: an invocation containing group/qualified args must terminate and label only those argument edges heuristic. Current prototype's scanner loop is being fixed; don't duplicate its logic in test. A legal token-stream macro declaration accepting arbitrary tokens can test trailing :: without parser panic if needed; no macro expansion assumption.

Use std + existing serde_json/toml; no new deps. Tests should state source-level semantics rather than branch details. Errors include case id and actual findings. TempDir cleanup Drop. No full workspace commands or code execution in your role. Output full patch only.
