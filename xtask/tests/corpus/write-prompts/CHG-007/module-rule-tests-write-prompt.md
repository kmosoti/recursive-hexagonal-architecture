WRITE-tier test proposal, read-only repo /home/kmosoti/projects/rha-m2. Root sole writer. No mutations/git/jj/tests/agents/siblings/held-out. Return ONLY apply_patch adding xtask/tests/module_rules.rs and xtask/tests/modules_cli.rs. Do not alter registered data or production. Root applies and validates.

Public APIs now in tree: modules::extract::extract(crate_root,root_file,crate_name,edition,&externals)->Result<Extracted,String>; modules::rules::check(crate_root,crate_name,rules_path,&Extracted)->Result<Checked,String>, Checked {findings,test_edges,module_edges,limitations:Vec<Value>,rules_path,rules_digest}. CrateNode.composite now retained; metadata::load root returns graph. architecture CLI integrates module checks. Inspect actual source signatures/contract and scope searches to src/tests, not historical evidence. Root still repairs implementation; don't copy its algorithms into expectations.

Decided tests:
1. Headline grading of every authored module fixture (25) using immutable manifest.cases and corpus::grade::architecture. Read fixture Cargo.toml only for fixture name/edition (allowed in harness; no production Cargo dependency parsing). Extract src/lib.rs, check rha-modules.toml, make report with schema_version1/findings/test_edges/module_edges and exact errors/warnings/outcome summary, exit0/1 accordingly. Compare registered grade.passed and record all failures, no ignored extra findings or altered witness. L-M01 live site: load root cargo metadata once, find node.name=='site', use composite metadata and exact same extractor/rules, require no findings and full registered grade. X-M01 belongs external harness later, do not fabricate its pass. EM-M03 additionally confirm visible constraints->ordering raw import/factory edge exists and no implicit constraints->model edge, per manifest.partial, while grading documented miss as registered.
2. Supplementary optional rule oracle: materialize256 random source maps safely in temp, extract/check, project findings to EXACT keys from registered random-rule-expectations.json. Inspect actual registered schema (violations/component_edges) before coding; compare every expected field, reject unexpected findings, never rebuild expectations from production. Register counts from actual registration. This supplements exact extraction test. No code/data copies into expected. Keep case ids and mismatch details; print totals. If production raw edges have extra informational keys, compare by registered field keys only for each expected object, plus multiset cardinality, and preserve all diagnostic facts/subfacts.
3. Rule-specific small controls: three-node SCC canonical shortest/lexical witness and off-witness heuristic not tainting it; undeclared edges inside SCC remain subfacts, independent edge remains finding; deny.cycles=false doesn't hide D4; nested source parent-sibling D2 cannot be permitted by allow; unknown component/missing rules rejected. Synthetic Extracted can isolate graph rules; sources/witness fixtures named as synthetic, not a production claim. Don't need exhaustive4096 test here unless simple and cost justified.
4. CLI real metadata: run xtask architecture on actual repo require site module check outcome passed, test_edges and module_edges present, summary matches all findings. Build a tiny standalone core composite workspace in temp with root rha-crates rules allowing no deps and exact core Clippy template/forbid line (inspect existing architecture_cli test helpers before inventing shape); legitimate case exits0 then add real undeclared or upward path legal Rust and require exit1/finding/module failed. Missing/malformed declared rha-modules returns exit2 config_error, not not_run/pass. Metadata cases exercise custom lib source/name, edition, and dependency alias where practical with a standalone member helper; assert source identity retained. No reading historical git objects, no full workspace commands or external cargo-modules execution in these tests. Use std temp helpers/existing deps only.

Return patch only, no validation claims. Root owns integration and all test runs.
