You are a mechanical WRITE-tier proposal generator. Root is sole branch writer; read-only in /home/kmosoti/projects/rha-m2. No file writes, git/jj mutations, sibling/held-out reads, agents or tests. Return ONLY a complete apply_patch-format patch (no fences), adding xtask/src/record_schema.rs and modifying xtask/src/record_lint.rs. No Cargo/lib/test/schema/data edits: root handles those. Registered schemas/corpus precede this code; read docs/architecture/record-schema-contract.md, verifier-contract.md sections 2–4, existing record_lint.rs and xtask error.rs. Preserve all original 80 corpus reason sets.

Decided implementation:
1. record_schema.rs embeds seven .rha/schemas/{task,acceptance,policy,evidence,h4,markdown_corpus,h5_conformance}.schema.json files via include_str. Root's separate translator generates those. Compile validators once using std::sync::OnceLock and jsonschema 0.57.0 with default features disabled. Actual API verified by root docs: jsonschema::draft202012::options().offline().build(&serde_json::Value); validator.iter_errors(value); error.kind(), error.instance_path(), error.schema_path() are methods. No remote refs or runtime schema file lookup. Provide pub fn validate(value:&Value,family:&str)->Vec<Issue>, Issue {pub code:&'static str,pub path:String,pub message:String}; pub fn configuration_error()->Option<&'static str>. Cache Result<BTreeMap<&'static str,jsonschema::Validator>,String>, fail closed with schema.definition_error on bad owned definitions (CLI returns a tool/config error via crate::error::Error). Unknown family => schema.invalid_value. Schema error mapping: required=>schema.missing_field; type=>schema.wrong_type; additionalProperties/unevaluatedProperties=>schema.unknown_field; enum/const/not and any other constraint=>schema.invalid_value. Can dispatch by final schema_path keyword to avoid unstable enum variant APIs. Schemas use type-guarded enums and conditional branches, so no irrelevant union errors should leak. Deterministic sorted/dedup issues.
2. Preserve pub lint/lint_in signatures returning BTreeSet<&'static str>, used by old corpus tests, but have them delegate to a structured report. Add pub struct LintReport {pub kind:String,pub reasons:BTreeSet<&'static str>,pub notes:Vec<String>,pub schema_issues:Vec<record_schema::Issue>} and pub fn report_in(record:&Path,kind:Option<&str>,l0:&[String],repo:Option<&Path>)->LintReport. Parse TOML by extension or explicit task/acceptance/policy kind; JSON for evidence/corpus kinds. Structural schema reasons ACCUMULATE with existing semantic walk/evidence reasons, not replace them. Keep digest/revision/timestamp definitions unchanged except citation resolution notes. Apply L0 check completeness/outcome semantics only for family evidence, never corpus reports. Tool-produced corpus timestamps can use strict timestamp checking.
3. Detect kind from PARSED STRUCTURE, never substring in comments. If record_kind exists require string 'evidence'; if kind exists require one of h4/markdown_corpus/h5_conformance. Wrong type=>schema.wrong_type, unsupported=>schema.invalid_value. TOML acceptance by top-level acceptor, policy by authority/lanes, otherwise task; JSON with no supported discriminator=>schema.missing_field (do not silently accept {}). Non-object root=>schema.wrong_type. Explicit family in library APIs selects schema even when a discriminator is removed, for exact registered structural diagnostics. Parse/read failure=>schema.wrong_type as existing contract.
4. Cited {path,sha256} bytes: beside the record first, else git cat-file blob at its subject. Subject candidates: artifact_identity.snapshot_tree then artifact_identity.revision, then subject.tree/revision (acceptance/H5). Require 40-hex as old code. Never use current unrelated workspace bytes as a substitute for historical subject. For file paths use std::path::Component to reject absolute/parent traversal, and canonicalize a beside-record file to ensure it stays inside canonical record directory; never follow a symlink escape. A '..' substring in a valid filename is not traversal. If safe file cannot be resolved at either location, add explicit sorted/deduplicated note 'citation.unchecked: <path> (<reason>)'. Do not add a rejection code for unresolved citations, preserving synthetic/historic corpus behavior. Digest mismatch still produces digest.mismatch. Notes should say if path unsafe, no subject or missing at subject; don't print file contents or raw git stderr. Unknown instruction_sources digest stays allowed as before.
5. CLI run checks schema configuration, loads l0 ids, report_in(auto). Rejected: print kind+reason set and schema path/message lines; print notes for rejected too. Accepted with any note MUST print 'accepted with unchecked citations', never bare accepted. Accepted without notes may print accepted. Exit1 iff any record rejected; configuration failure propagates crate error. No tests in this patch and no validation claims; root adds independent registered tests.

Use simple cohesive Rust, existing crate style, no panic for arbitrary records. The standalone module extractor proposal is unrelated and unavailable; do not touch it. Final response only full patch for the two owned files.
