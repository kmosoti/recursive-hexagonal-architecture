WRITE-tier decided integration proposal, read-only /home/kmosoti/projects/rha-m2, root sole writer. No mutations/tests/agents/siblings/held-out. Return ONLY apply_patch for new xtask/src/modules/workspace.rs plus xtask/src/architecture.rs and xtask/src/graph/report.rs. Do not edit mod.rs/metadata/model/data/tests; root integrates disjoint proposals after stage1.

Read existing architecture.rs, graph/report.rs, model.rs and docs/architecture/module-check-contract.md. Do not broad-search evidence/corpus snapshots. New metadata interface (another patch, may not be present yet): CrateNode.composite:Option<CompositeSource>, CompositeSource {rules:String,source:PathBuf,crate_name:String,edition:String,externals:BTreeSet<String>}. Extractor modules::extract::extract(crate_root,root_file,crate_name,edition,&externals)->Result<Extracted,String>. Rules modules::rules::check(crate_root,crate_name,rules_path,&extracted)->Result<Checked,String>, Checked fields findings/test_edges/module_edges/limitations Vec<Value>, rules_path/rules_digest String.

Decided design:
- workspace::apply(graph:&CrateGraph,report:&mut Value)->Result<(),String>. For each crate with composite metadata, crate_root=manifest_path.parent; bound source/rules via extractor/checker, actually extract/check; collect per-crate module_checks {crate,required:true,rules_path,rules_digest,outcome passed/failed,reason null,extraction scope/limits/counts as useful}. A source/rules error returns Err with crate context; architecture maps it to existing config_error nonzero path. Noncomposites get not_run,required:false,reason:'not_declared_composite: no module rules obligation'; never pretend evaluated. Do not run modules merely because filename exists.
- Extend top-level findings with module findings, test_edges with listed notes, module_edges with raw source references; tag crate/package on these extra values to disambiguate. Preserve exact from/to/path/depth/extraction witness fields. Extend limitations honestly, including source approximation; don't put limitations in findings. Recompute summary errors/warnings/outcome from ALL findings; module_checks site must pass only if evaluated and no module errors.
- architecture::run constructs mutable crate report, calls workspace::apply after crate checking/template rule and before write_report; errors go through existing failure(root,...config_error). JSON exit status computed from final report.summary.errors, not crate-only outcome. Preserve existing metadata transport/config distinction and transitive refusal.
- Text/Markdown must reflect combined report and final summary, without printing a misleading crate-only success line before module errors. Add new graph::report renderers for completed JSON reports (or adapt existing render functions with backwards-compatible wrappers). Preserve existing public text/markdown APIs for unit callers if practical. Show error[rule]: from -> to with message/location, module outcomes, and final total; keep crate classification/harness facts. JSON remains canonical report output. Do not treat module errors as warnings or swallow missing rules.
- Existing report::module_checks placeholder helper can remain only for low-level crate-report construction, but runtime CLI must replace it; update stale comments. No hardcoded site special case or corpus IDs. No changes to registered grading, lane argv/policy, or producer identity helper. No tests/validation claims from you.
