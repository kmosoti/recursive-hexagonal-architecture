WRITE-tier bounded proposal, read-only /home/kmosoti/projects/rha-m2. Root sole writer. Return apply_patch ONLY adding xtask/src/corpus/module_random.rs. No actual writes/apply_patch/tests/agents/git/siblings/heldout. Independent module corpus immutable.
API pub fn run(root:&Path)->crate::error::Result<serde_json::Value>. Called by module corpus runner for supplementary aggregate. Materialize frozen256 random source maps (safe relative paths refuse parent/absolute/symlink escapes) under unique root/target/rha/module-random-<pid>-<nonce> using createdir collisionloop; owned cleanup guard. Read registration counts/random/source/referenceexpected. Source maps under xtask/tests/corpus/module/random/<ID>.json plus .expected.json, optional random-rule-expectations.json. Compare exact extractor serialized edges and limitations to every registeredexpected, plus optional rules findings to every expectedviolation (match expectedkeys recursively plus equal cardinality, rejectunexpected, no rebuildingexpected). Canonical productiondiagnostic to crate::... maps to concrete source_map.crate_name+'::' ONLY for independentoptionaloracle spelling, no otherchanges. Collapse actualraw productionedges into deduplicated (componentowner alias nearest path prefix, differentowners, testexcluded, extraction retained) triples and compare frozencomponent_edges exactly. This is observedprojection only, expectedalwayscomesfromcommittedJSON. Use actualAPIs modules::extract/rules, existing module_extraction.rs/module_rules.rs helper patterns. Avoidcopyingcodealgorithm into expectations. All256 must examined, errors retained percase (no failfastforonecase), don't silentlyskipunknownfiles. Ensure expected id set equals source mapidset and .expected idset, all counts fromactualdata and match registeredcounts (randomcases256 edges5229 test512 heuristic116 limits0 optionalfindings192 totalundeclared128 inclsubfacts). registration counts inspectactual before assumptions, comparisonsmust fail.
Return object {outcome:"passed"|"failed", registration/path/digest info, counts actual, cases:[{id, passed, reasons, actual extraction + rules/facts or explicit error}], mismatch_ids, limits:["supplementary...",...]}. No recordkind becauseembedded in H4 existingopenreport. No inventedprocess exit in thisfunction. Reportscomputedobservations only; runtime type/IO errors ResultErr, discrepancies reportfailed. Inputhashchecking entirepackage done caller; thisfunction also pins registration counts and IDs. Don't modify production or tests. BTreeMaps/BTreeSets stable ordering, no newdeps. Return patch only.
