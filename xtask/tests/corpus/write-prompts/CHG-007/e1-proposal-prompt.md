WRITE-tier decided E1 proposal, read-only repository; root sole writer. Return apply_patch only; no writes/apply_patch/tests/agents/git/siblings/heldout. Task creates experiments/no-std-graph standalone package with empty [workspace], package no-std-graph version0.0.0 edition2024 rust-version1.98.1, [lib] name graph. Depend directly on ../../crates/document and ../../crates/library (actual production types, no fabricated model); dev proptest exact workspace existing version (inspect). [features] negative-std-probe=[] default[]. Copy crates/graph/src/lib.rs to experiment src/lib.rs byteforbyte except inserting #![no_std], extern crate alloc and replacing stdcollections with alloccollections plus alloc::{string::String,vec::Vec}. Keep original algorithm unchanged; add cfgfeature negativeprobe that references std::collections::BTreeMap, intentionally fails the feature build. Copy graph tests/oracle_resolve.rs and properties.rs byteidentical to experiment tests. Root will first commit ONLY hypothesis/manifests before running, then copy implementation.
hypothesis.md must be pre-run, no observedresults. H1 ownresolver compiles no_std after imports only, H2 original property/oracle tests same, H3 directstd injection fails. Document full production dependencyclosure stillstd via document/library; no embedded/heapfree/panicfree claim. This direct-coupling experiment intentionally retains realtypes/dependencyclosure for fidelity vs local model toy. Measure cargo check --lib --locked, cargo test --locked, cargo check --lib --locked --features negative-std-probe (expectednonzero), source SHA and deterministic transformationcompare. Root first runs naive no_std source retaining stdimports (expectedcompilefailure) then actualallocproposal. Explain original resolve assumes uniquePageId provided by Corpus; duplicate inputs not part of H2 permutationdomain. No productfile edits and no result.md invented. Give small README exact isolated commands and no_std limits. Return patch only for experimentsfiles; no claims testsrun.
