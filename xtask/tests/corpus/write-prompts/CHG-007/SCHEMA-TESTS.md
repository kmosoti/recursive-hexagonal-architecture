You are a mechanical WRITE-tier proposal generator for decided tests/harnesses. Root is sole branch writer. Read-only in /home/kmosoti/projects/rha-m2, no file writes, git/jj mutations, sibling/held-out reads, agents or test execution. Final ONLY apply_patch patch (no fences): add xtask/tests/record_schema.rs, xtask/tests/record_schema_cli.rs, tools/rha-verifier/tests/shape_corpus.rs, and extend xtask/tests/corpus_trust.rs. No source/schema/registered-data changes. Root compiles and verifies separately; do not claim passes.

Corpora already committed f7b5c62. Read frozen xtask/tests/corpus/record-schema/{FORMAT.md,CASES.json,registration.toml}, verifier-shape/registration.toml and existing corpus test style. Runtime API supplied by another proposal: xtask::record_schema::validate(&serde_json::Value,family:&str)->Vec<Issue>, fields code:&'static str,path:String,message:String; configuration_error()->Option<&'static str>. Existing record_lint::lint_in retains old signature; new report_in(path,kind:Option<&str>,l0:&[String],repo:Option<&Path>)->LintReport with kind,reasons,notes,schema_issues. CLI cargo xtask rha lint determines family from parsed discriminator/shape; unresolved citations produce 'citation.unchecked' and acceptance phrase 'accepted with unchecked citations'.

Decided tests:
1. Materialize every compact schema recipe independently in Rust: load baseline source bytes (verify source_sha256), parse TOML->serde_json or JSON, apply baseline mutations THEN case mutations per FORMAT (JSON Pointer '~1'/'~0'; set/replace/remove, root replacement; removals of absent members must fail harness). Reuse cached baseline values if useful. Assert registration case_count=1497 and actual length, exact expected_schema_codes set equals validate output code set for every recipe, accumulating useful id/path failures instead of stopping first. Assert schemas compile configuration_error none. Print the actual per-family/total tested counts for retained --nocapture evidence. Structural-only grading, not full semantic acceptance; original 80 lint tests remain intact. Make helper functions local in test file or a shared tests/support/record_recipes.rs if needed, but no standalone binary or new deps.
2. CLI regressions use cli-safe-l0 recipe baseline and its materialization: valid control exits0; missing outcome exits1 schema.missing_field; unknown exits1 schema.invalid_value; unsupported kind exits1 schema.invalid_value; actual markdown/H5 corpus report input exits0 without evidence.missing_required_check. A copied valid L0 citation pointing at definitely missing safe file with syntactically valid hash must print citation.unchecked + accepted with unchecked citations, not bare accepted line. Digest mismatch remains rejected; a matching beside file passes and a symlink escape must not be read (use cfg(unix), target file known content outside record directory, expected unchecked note rather than mismatch). TOML task containing '[acceptor]' in a comment must still be classified task. Use unique temporary dirs and clean them. Do not inject new expectations into registered corpus files; these are requirement-derived CLI regressions. Avoid redundant tests mirroring internal implementation.
3. Verifier shape test: enumerate top-level S*.json only (188); evaluate with rha_verifier::evaluate and compare all expected keys, including absence of 'malformed' on well-formed cases so a refused normal input cannot pass. Preserve original corpus.rs. Count malformed153/wellformed35 from expected; assert no unexpected shape; print observed count.
4. Extend corpus_trust.rs with pinned() calls for xtask/tests/corpus/record-schema and verifier-shape (both use existing tree_digest protocol). Add module corpus checksum check for its DIFFERENT SHA256SUMS protocol: safely parse relative lines, verify every payload file bytes, compare inventory hash with registration.content_sha256, ensure complete durable file set excluding registration.toml and SHA256SUMS. Also pin generator/ref/headline/random counts/contract digest where clearly registered. Negative-check the digest checker using a temporary miniature directory: a changed/missing payload must fail. Do not change any registered bytes or current graph/checkers to produce a pass.

For corpus fixture/helper paths resolve from CARGO_MANIFEST_DIR, not fixed /home paths. No historical git objects required, so shallow CI works. Core tests don't need target-wide build commands. Return complete patch only, with accurate Rust APIs (std, serde_json, toml, xtask::util::sha256_hex; no external tempfile dependency).
