WRITE-tier bounded decided repair. Read-only /home/kmosoti/projects/rha-m2, root sole writer. Return apply_patch only for xtask/src/modules/rules.rs and workspace.rs. No tools beyond those two file reads; no tests/agents/git or other code. Make these exact repairs:
1. Main undeclared BTreeMap actually stores every projected sibling EdgeSet. Rename to sibling_edges. Iterate it borrowed for D4 so it survives for cycles. Cycle witness path pairs and SCC undeclared subfacts must lookup this same map, preserving all raw parallel edge evidence and exact nested projection. Remove edge_pair() and undeclared_pair() helpers entirely. No fallback to arbitrary graph edge if a witness has no evidence: main adjacency is built from this map so direct get expect with invariant statement is fine. Cycle heuristic flag true iff ANY raw edge on selected path pairs heuristic. Choose first matching representative evidence for witness. Ensure borrowed from/to String comparisons compile.
2. raw_edge_value currently has redundant no-op prefix manipulation. Simply serialize raw source/target as stored; remove unnecessary normalized_crate argument and change call.
3. workspace currently pushes duplicate extracted limitations twice. Have top-level limits include source_approximation once and checked.limitations once (tag consistently using composite.crate_name and package name), and module-check extraction.limits contain identical complete set including disabled_rule. Scope text states rules configured by rules file, source approximate. Preserve pass for configured checks, no disabled obligation claim. Remove unused extraction_limits helper if appropriate.
Return patch, no claims checks ran.
