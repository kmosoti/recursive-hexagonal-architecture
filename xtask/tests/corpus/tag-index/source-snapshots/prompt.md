Write a registered grader for a feature that is not implemented yet, from its contract alone. Read existing code only for APIs, output shapes, harness conventions and style; never to learn how the feature will be implemented.

Repository (read-only for you): /home/kmosoti/projects/rha-chg013b (Rust workspace "rhawiki", branch chg/013b-tag-index, HEAD cdb9bcf).
Contract (authoritative): docs/architecture/tag-index-contract.md. Related: docs/architecture/front-matter-contract.md (stage A, implemented: `document::FrontMatter { title, tags, end_line }` — stage B adds `index`), docs/architecture/json-renderer-contract.md (the JSON output shape you will assert on). Read AGENTS.md, CONTRIBUTING.md, and crates/app-cli/tests/citations_render.rs and crates/app-cli/tests/callouts_render.rs for the end-to-end harness conventions (temp docs tree, CARGO_BIN_EXE_rhawiki, `rhawiki build --root --out [--format json]`, `rhawiki check`).

SCRATCH BOUNDARY (strict): write only under /tmp/claude-1000/-home-kmosoti-projects-recursive-hexogonal-architecture/0ce0c856-7b1f-49da-afa8-4880e034d2d1/scratchpad/chg013b-oracle/ (create it). Deliverables:
- chg013b-oracle/package/xtask/tests/corpus/tag-index/ : reference.py (an independent Python implementation of contract sections 1 to 3: which pages are tag indexes; the site's tags with ASCII-case identity, display spelling from the first page in page-id order; tag order; pages per tag in page-id order; the anchor rule with `tag-` + slugify (port document::slugify's exact rules from crates/document/src/slug.rs by reading it — it is an existing public function, reading it is allowed) and the -2/-3 de-duplication against the page's own heading slugs and earlier tags; the page titles as document::parse derives them); CASES.json with at least 25 site cases (each: a map of relative page path -> markdown, and for each tag index page the expected appended section as a list of {heading text, slug, [linked page ids with link text]} plus the expected TOC tail; sites with no tags; sites with tags but no index page; two index pages; an index page that is itself tagged; tags differing only in ASCII case across pages; non-ASCII tags; tags whose slug is empty or collides with a heading on the index page or with another tag; `index: "tags"` quoted; `index: other`; `index:` empty; pages in subdirectories); README.md (coverage, and every contract ambiguity as a question, kept OUT of the corpus); registration.toml; selftestreport.txt; source-snapshots/contract.md (verbatim) and your prompt; SHA256SUMS over the payload.
- chg013b-oracle/package/crates/app-cli/tests/tag_index.rs : for each case, write the site into a temp dir, run `rhawiki build --format json`, and assert on the index page's JSON: the body ends with exactly the expected appended nodes (level-2 headings with the expected slug and text; each followed by a list whose items are single wiki_link nodes whose link_index resolves, via the page JSON's `links` array, to the expected target page id with no anchor, and whose children text is the page title), the page's TOC ends with the expected entries, and the body before the appended part equals the body the same page renders with the `index` line removed from its front matter (build that variant too) — this pins "nothing else changes on the index page". Also assert that non-index pages' JSON is identical between the site and the same site with every `index:` line removed. Also one HTML check: the index page's HTML contains `<h2 id="SLUG">` for each tag heading and an `<a href=` to each linked page. Also `rhawiki check --format json` gives zero witnesses. The grader embeds CASES.json with include_str! (app-cli may use std::fs for temp dirs, as the existing app-cli graders do) and verifies SHA256SUMS of the embedded payload.

Compile check: in a worktree copy (`git -C /home/kmosoti/projects/rha-chg013b worktree add --detach <your folder>/wt HEAD`, CARGO_TARGET_DIR in your folder, CARGO_PROFILE_DEV_DEBUG=0 CARGO_PROFILE_TEST_DEBUG=0 CARGO_INCREMENTAL=0), the grader must compile at HEAD (the feature needs no new API for this end-to-end grader) and FAIL for the right reason (no appended section), not a harness error: report the first failing assertion. Then prove satisfiability with a scratch implementation in your worktree only (never delivered; revert it), run the grader green, and report. The file must pass rustfmt --check and `cargo clippy -p app-cli --tests` with zero errors (start with `#![allow(clippy::expect_used, clippy::unwrap_used)]`). Delete only what you create in your folder; remove your worktree at the end.

Report: files delivered, case count and coverage, self-test results, fmt/clippy, the failing assertion at HEAD, the result against your scratch implementation, and the list of contract ambiguities (questions).
