You are an INDEPENDENT ORACLE AUTHOR (plan docs/plan/IMPLEMENTATION-PLAN.md §2.4 rule 4). Do NOT open, read, grep or list anything under `crates/*/src/` — you must not see the implementation you will check. You may read xtask/tests/corpus/markdown/GENERATOR-PROMPT.md (the contract) and the public test files named below. Do not commit, push, or run cargo.

Write two test files, each containing an independent reference implementation written from the contract alone, plus a proptest that compares it with the real crate's public API:

1. `crates/document/tests/oracle_slug.rs`: a reference `ref_slugify(&str) -> String` per the contract (NFC via `unicode_normalization::UnicodeNormalization::nfc`, Unicode lowercase, keep letters/digits/space/`-`/`_`, space -> `-`, no collapsing). Proptest: for arbitrary strings, and for a strategy biased toward the edge characters (combining marks, `İ`, `ß`, `Σ`, digits from other scripts, punctuation, `_`, `-`, multiple spaces, tabs), `document::slugify(s) == ref_slugify(s)`. Also: for arbitrary sequences of heading texts, a page `## t1\n\n## t2...` parsed by `document::parse(&library::Source::new(library::RelPath::new("p.md").unwrap(), text))` yields headings whose `slug`s equal the reference: base slug, then `-2`, `-3` for repeats of the same base, in order. Keep heading texts to characters that cannot start other markdown syntax (letters, digits, spaces, a few punctuation marks), and state that restriction in a comment.
2. `crates/graph/tests/oracle_resolve.rs`: a reference resolver over a list of (page id, final slugs, links[(target, anchor)]) implementing the contract's resolution and witness rules (exact id; else unique case-insensitive basename of the page id against the whole target; else broken or ambiguous; anchors checked against the target's final slugs; `[[#h]]` targets the same page; one witness per occurrence). Proptest: generate small sites as markdown (a few pages under at most one directory level, headings, and wikilinks drawn from existing ids, basenames in other cases, missing names, and anchors both present and absent), parse them with `document::parse`, run `graph::resolve`, and compare the multiset of witnesses as (kind, from, target, heading) with the reference's. Public API you may use: `graph::resolve(&[document::Document]) -> graph::SiteGraph` with `witnesses: Vec<graph::Witness>`, where `Witness` is `BrokenLink { from: PageId, target: String, line }`, `AmbiguousLink { from, target, candidates, line }`, `MissingAnchor { from, target, heading, line }`; `document::Document { id: PageId, headings: Vec<Heading { slug, .. }>, links: Vec<Link { target, anchor, .. }>, .. }`; `library::PageId::as_str()`.

Use `#![allow(clippy::unwrap_used, clippy::expect_used)]` at the top of each test file. proptest and unicode-normalization are already dependencies (add `unicode-normalization = "0.1.25"` to [dev-dependencies] of crates/document/Cargo.toml if it is not already a dependency there; it is a normal dependency of document, so the test can use it). When done, print which files you wrote and one line on each oracle's design. Do not print file contents.
