# CHG-008 (P-D): growth under measurement

Task: [CHG-008](../../.rha/tasks/CHG-008-growth.toml). Covers CHG-008 through CHG-014; plan P-D/W8/W9/W10. Agent mode, ECC-Solo. Kennedy is Planner, Integrator and Acceptor; this Executor owns implementation and evidence, with separate GPT-6 and supervising Opus reviews.

## Intent and scope

Add adapter-json, measure H3, deliver the five W9 features as separately predicted and gated contributions, then diagnose mutation survivors. P-D starts from the unmerged P-C head and will open a stacked PR; no P-C acceptance is fabricated and no merge is performed.

## Deltas

Stage 0 is the declared assurance prerequisite investigation. Each later feature gets a committed expected touched set and contract/corpus before implementation. Protected deltas and alternatives will be recorded under the quoted delegation.

## Evidence

The assurance prerequisite gate passes 280 tests with current generated docs and zero scope findings. P-C evidence remains in its own record. Later feature stages, raw change-spread measurements, mutation questions and final lane/review will be linked here as they run.

## Decisions

Task decisions delegation-m2, assurance-prerequisites and protected-growth-scope state initial authority. Status lives in the decision ledger; DP-2.1 was decided in P-C, and DP-2.2 will adjudicate each observed survivor.

## Acceptance concerns

Held-out material remains not_run and Kennedy-owned. Both PRs remain unmerged. Authentic/protected producer limits remain DP-4.1. No inherited corpus grade will be silently changed.

### Stage 0: assurance prerequisites, before H3

P-C's forward CI repair was merged into this local stacked branch at 2fa29ae93ea71020113301f8e7deee3abf09a935. The only merge conflict was its generated task index, resolved by cargo xtask docs. Both remote PRs remain subject to review; no PR was merged.

**Repair attempt 1.** Hypothesis: the owner contract can pass a TOC-dropping renderer, lets asset panics escape, misses page/asset collisions and discards a post-delete listing error; build_all also subtracts asset writes from its source-page unchanged count. The [control source](../../evidence/CHG-008/stage-0/pd-assurance-controls.rs) was compiled against the unchanged built crates. [Observed result](../../evidence/CHG-008/stage-0/pd-assurance-negative.txt): three positive controls pass and five discriminating tests fail. In the concrete two-page asset-only update, unchanged is 1 instead of 2. Decision confirmed-assurance-repairs fixes those consequences before adding the JSON adapter. Implementation and repaired results follow in the next stage commit.

**Graph API determination.** [A separate executable probe](../../evidence/CHG-008/stage-0/pd-graph-precondition.txt) observes permutation-sensitive resolution for duplicate PageIds and confirms Corpus rejects the same duplicate sources. The product input domain is protected. The repair documents the uniqueness precondition on graph::resolve; it does not invent resolution semantics for invalid duplicate-ID slices.

The stage prediction was committed first in 25c75c6. Before production edits, it is refined to include site::build port documentation in addition to site::contract, site::testing, site glue and graph API documentation. The original prediction remains visible. Both the full P-C-to-H3 core diff and the isolated adapter-stage diff will be retained.

**Repair result and stage gate:** the owner contract now distinguishes the dropped TOC, contains asset panics, reports asset nondeterminism and duplicate paths, and propagates post-delete listing errors. The fake preserves the observed TOC. Asset-only writes no longer reduce unchanged source-page counts. The graph API states its existing uniqueness precondition. [Targeted positive/negative controls pass](../../evidence/CHG-008/stage-0/pd-stage0-controls.txt); [all 280 nextest tests pass](../../evidence/CHG-008/stage-0/pd-stage0-gate.txt), none skipped; [Clippy](../../evidence/CHG-008/stage-0/pd-stage0-clippy.txt), [docs](../../evidence/CHG-008/stage-0/pd-stage0-docs-check.txt) and [scope](../../evidence/CHG-008/stage-0/pd-stage0-scope.txt) pass. TOC sensitivity is a sampled discrimination check; each adapter still needs an exact semantic assertion. No old test or corpus grade was weakened.

Stage 0 implementation range: 25c75c6..e687b1ab4171935173d90dbf5fcbe568a9118e61. [Raw core numstat](../../evidence/CHG-008/stage-0/core-numstat.txt) records the exact changed files; build and graph changes in this prerequisite stage are documentation only. P-C PR18 now has green CI run 35824096298 on head 8e3037b5b4a342a3e2bec36af874246838c610b7 and independent GPT-6 approval of both implementation and repair.

### Stage 1 preparation: adapter-json and H3

[BDR-0006](../adr/BDR-0006-adapter-json-immutable-index.md) accepts the boundary's refutation criterion under Kennedy's delegation before a new crate exists. Three alternatives are scored; the immutable constructor/index wins 18/20. [The JSON contract](../architecture/json-renderer-contract.md) fixes exact page/node/search semantics and lifecycle behavior before the separate corpus generator and implementation. Object member order is not graded; values and deterministic output bytes are. The search index is fresh configuration for one corpus, avoiding hidden render-call state. Its cost is one additional pure analysis/assembly pass at the composition root. Decision json-adapter-boundary authorizes manifests and later assumption-ledger support; no core allow-list or port change is planned.

The adapter-stage predicted product set is adapter-json and app-cli, including their tests. The empty-core target applies to the full isolated stage diff; the earlier core repairs remain visible in the complete P-C-to-H3 diff. No JSON implementation or corpus evaluation has occurred at this preparation commit.

### JSON corpus registered before implementation

The separate generator, forbidden access to renderer implementations/tests, produced [79 registered cases](../../xtask/tests/corpus/json-renderer/registration.toml): 70 projection cases including 64 seeded random cases, two duplicate-ID cases, and the reconciliation, collision, owner-contract and four CLI cases. Seed 8072026 and every prompt digest are recorded in task provenance. [Root's inventory/source audit](../../evidence/CHG-008/stage-1/pd-json-registration-audit.txt) verifies all ten payloads, source snapshots and prompt digests. [Twelve package files reproduce byte-identically](../../evidence/CHG-008/stage-1/pd-json-reproduction.txt); [corrupting a copied case stream is refused](../../evidence/CHG-008/stage-1/pd-json-integrity-negative.txt). No target encoder or adapter has run at this registration commit.

**Pre-registration determinations.** A read-only audit identified live source citations that would become stale during W9, ambiguous code-block search-text wording and imprecise asset cardinality. The contract now names the intended text and exactly one asset; exact contract/Node/PageModel snapshots are frozen in the package. Future source changes do not regrade those snapshots. A [language probe](../../evidence/CHG-008/stage-1/pd-json-whitespace-reference-probe.txt) also showed Python split and Rust split_whitespace differ on four control characters. The independent reference now uses the explicit Unicode White_Space property with [hand-derived controls](../../evidence/CHG-008/stage-1/pd-json-reference-selftests.txt). Its original 79 case bytes did not change. No registered grade was changed and no implementation result informed the corpus.

**Generator repair attempt 2.** A whitespace follow-up [stopped on an invented filename](../../evidence/CHG-008/stage-1/pd-json-whitespace-correction-result.md), adding an extra generator segment to the actual supplied prompt path. The file existed at the supplied path. A bounded follow-up corrected that lookup, completed the normalization repair and fixed the documented staging reproduction command. Package metadata contains three instruction snapshots; the fourth finishing instruction is also archived and hashed in task provenance. This process failure is retained rather than presented as a completed repair.

The corpus fixes decoded JSON members/types/values and array order; repeated bytes and newline formatting are additional obligations. Every registered lifecycle/CLI case must run as written, and old corpus grades remain unchanged. Later feature extensions require separate registered expectations and must preserve these old cases.

### JSON implementation and H3 observations

The adapter now projects the complete PageModel into deterministic page JSON and a constructor-built search-index asset. The CLI selects HTML by default or JSON explicitly; check JSON remains v1. Only adapter-json and app-cli product paths change in this stage. The [79 registered cases pass](../../evidence/CHG-008/stage-1/pd-json-registered-final.txt): 75 adapter cases and four CLI cases, plus corruption and symlink controls. The assumption ledger names JsonRenderer as the third tested implementation alongside the in-process StubRenderer and HtmlRenderer; this means two presentation adapters plus the fake, not three production adapters.

[The H3 detection probe](../../evidence/CHG-008/stage-1/pd-h3-detection.txt) records actual violation lists for the nondeterministic, panicking and TOC-dropping renderers, and empty lists for all three conforming implementations. It binds the linked library artifacts by computed digests. The broader prerequisite repairs remain separately measured; this is a small descriptive observation, not a controlled study or universal proof.

**Repair attempt 3, integration typing.** [The initial compile](../../evidence/CHG-008/stage-1/pd-json-first-compile.txt) rejected an unconstrained collect result. Giving the index entries their Vec<Value> type resolves that concrete failure; [the compile then passes](../../evidence/CHG-008/stage-1/pd-json-compile.txt). No contract or expected value changes.

**Repair attempt 4, grader conformance.** [The first test compile](../../evidence/CHG-008/stage-1/pd-json-tests-first-compile.txt) exposed an invented LinkTarget export and invalid vector/JSON comparisons. Review also found copied hash typos, a missing Html input arm and rejection of legitimate comparison-case fields. The repair now reads the digest-bound inventory instead of copying it, imports the actual API, compares typed observations as JSON, handles every registered node and grades optional comparison outputs as registered. A second proposal's bad context was rejected; root reconciled it mechanically from actual file bytes and obtained separate verification. [All registered fields are now exercised](../../evidence/CHG-008/stage-1/pd-json-registered-final.txt). No corpus byte was edited to obtain this result.

**Repair attempt 5, observation strength.** [An injected TOC drop fails](../../evidence/CHG-008/stage-1/pd-json-toc-negative.txt). [The previous pre-read order failed the symlink control](../../evidence/CHG-008/stage-1/pd-json-symlink-negative.txt); file types are now checked before payload reads. A deliberately extra post-value newline [passed the initial grader](../../evidence/CHG-008/stage-1/pd-json-newline-before.txt) and is [rejected by the exact suffix check](../../evidence/CHG-008/stage-1/pd-json-newline-after.txt). Constructor byte variation was [already caught by reconciliation](../../evidence/CHG-008/stage-1/pd-json-constructor-before.txt), refuting the broad suggestion that no such distinction was observed; [the added equal-input constructor comparison localizes it earlier](../../evidence/CHG-008/stage-1/pd-json-constructor-after.txt). A counting sink directly verifies zero write/delete calls on collision. These strengthen observations of the existing contract without regrading the frozen data.

**Repair attempt 6, experimental artifact selection.** The first standalone H3 probe linked the deliberately faulty library left by the TOC negative test after its source had been restored. [That failed observation is retained](../../evidence/CHG-008/stage-1/pd-h3-detection-stale-negative.txt). Rebuilding the restored source before linking produces the [passing detection probe](../../evidence/CHG-008/stage-1/pd-h3-detection.txt). Subsequent fault controls always rebuild the restored candidate before any positive claim.

**Declared lint limit.** The assumption ledger is outside the seven registered rha-lint schema families. [The required attempted invocation rejects the pre-existing ledger as a task](../../evidence/CHG-008/stage-1/pd-assumption-ledger-lint-baseline.txt); it is not reported as passed. Its [TOML and support paths are checked directly](../../evidence/CHG-008/stage-1/assumption-ledger-shape-check.txt). Assumption-ledger schema validation remains not_run in the declared P-F tooling scope. The task record and supported evidence records continue to receive rha lint.

**Stage 1 gate:** [all 284 nextest tests passed](../../evidence/CHG-008/stage-1/pd-json-stage-gate.txt), none skipped; [docs](../../evidence/CHG-008/stage-1/pd-json-stage-docs-check.txt), [architecture](../../evidence/CHG-008/stage-1/pd-json-stage-architecture.txt) and [scope](../../evidence/CHG-008/stage-1/pd-json-stage-scope.txt) pass. Architecture reports eleven crates and site module enforcement passed. [Clippy passed](../../evidence/CHG-008/stage-1/pd-json-final-clippy.txt), with the repository's configured test warnings retained. Final commit-range numstat is recorded immediately after this stage commit.

**Stage 1 range and H3 closeout:** d2cc1cd793bbd820a130c9df1e120cc1c9f71e7f..bd708fd3277afcb80a3898b6a498ea3c010e0e4c. [The H3 observation](../observations/h3-json.md) records the empty isolated core diff, nonempty whole-packet core diff, exact product spread and both measured halves with their limitations. [The post-update assumption-lint attempt](../../evidence/CHG-008/stage-1/assumption-ledger-lint-final.txt) retains the declared unsupported-family result; no schema pass is claimed for it.

**Repair attempt 7, cost-claim qualification.** The original BDR/proposal described the added pass as O(n+e). [The actual lookup and caller](../../evidence/CHG-008/stage-1/analysis-cost-inspection.txt) show a vector scan per link; that bound was not established. A dated BDR qualification preserves the historical prediction and states the defensible cost of an additional pipeline pass and index memory. This repairs a misleading claim without changing core code to manufacture the H3 result. No benchmark result is invented.

**Repair attempt 8, required backlog layout.** Deferring the non-blocking lookup-scaling work to the global agreement's required docs/backlog.md produced [one scope.layout finding](../../evidence/CHG-008/stage-1/pd-backlog-scope-before.txt). Decision backlog-layout permits only that exact basename in the layout table; the existing rejection of unapproved journals remains tested. [Targeted scope checks](../../evidence/CHG-008/stage-1/pd-backlog-scope-tests.txt) and [the actual scope command](../../evidence/CHG-008/stage-1/pd-backlog-scope-after.txt) pass. The lookup optimization is recorded once and deferred. After the administrative fix, [all 284 tests pass](../../evidence/CHG-008/stage-1/pd-h3-ledger-gate.txt) and [generated docs are current](../../evidence/CHG-008/stage-1/pd-h3-ledger-docs-check.txt).

### Stage 2 preparation: transclusion

[Actual parser characterization](../../evidence/CHG-008/stage-2/pd-transclusion-parser-probe.txt) replaces the earlier advisory premise: a standalone embed is a WikiLink Image token, inline/aliased forms are images, and a tight list item has no paragraph event. Nested headings also appear inside container shells. The [separate attempted hidden-alt probe](../../evidence/CHG-008/stage-2/pd-transclusion-hidden-link-probe.txt) did not demonstrate a hidden-link defect and is not claimed as one.

Decision transclusion-page-model-boundary chooses a site-owned PageNode at the assembly facade. The three scored alternatives and the accepted refutation criterion are in BDR-0007. This keeps parser-only variants out of the renderer type. Decision transclusion-semantics fixes selected-region cycle handling, complete occurrence lookup, host-index compatibility, imported anchors and paths before fixtures/code. The original plan prediction and the broader justified prediction remain in the change-spread record. Decision owner-local-json-oracles limits JSON test dependencies to their owning cores; frozen H4 fixture rules remain unchanged.

No W9 implementation or semantic corpus exists at this preparation point. The separate references and exact cases must be committed before implementation.

**Pre-registration review, before W9 code.** The first section draft contains [117 transclusion nodes in mixed paragraphs across 58 of 80 cases](../../evidence/CHG-008/stage-2/sections-legality-before.txt), which parsed documents cannot contain under the fixed standalone rule. The independent generator must repair those inputs and expected selections before registration, with a legality control. Recognized embeds are now explicitly direct block nodes. URI review also requests destination-escape, Unicode-anchor and self-import discrimination; uppercase newly generated escapes is clarified from the existing encoder convention. A suggested raw NFD expectation is [refuted by the actual PageId constructor](../../evidence/CHG-008/stage-2/pd-page-id-domain-probe.txt), which normalizes NFC. No production transclusion implementation exists and no registered data is altered. Decision transclusion-pre-registration-review records these determinations.


### Transclusion corpora registered before implementation

Four independent packages are frozen: [262 region graphs](../../xtask/tests/corpus/transclusion/regions/registration.toml), [80 section trees](../../xtask/tests/corpus/transclusion/sections/registration.toml), [153 URI cases](../../xtask/tests/corpus/transclusion/uris/registration.toml), and [12 authored sites](../../xtask/tests/corpus/transclusion/sites/registration.toml). The sites contain 37 sources, 14 selected page observations, 30 embeds and eight necessarily triggered witnesses. These are fixed observation counts, not claims of complete AST equivalence. Every prompt, actual CLI model/effort/session header and seed is bound in task provenance. The [production diff is empty](../../evidence/CHG-008/stage-2/pre-registration-production-diff.txt) from the completed H3 stage through registration.

Each package's final audit and reproduction log in evidence/CHG-008/stage-2 verifies all seven payloads and all nine reproduced files. A valid copied package is accepted before deliberately corrupting CASES.json; corruption is then refused. The first region replay used a relative destination from the repository directory and was correctly refused by its containment rule; the retained invocation failure is repaired by running from the copied package directory. A later site replay correctly refused an absolute destination because that package requires a direct child name; the common replay now follows each package’s documented working-directory form. All four positive and negative controls were rerun before commit. An initial audit helper derived identity from a copied directory's basename; it now uses the declared kind, and both valid and corrupted copied controls were rerun. No old registered bytes or grades change.

**Pre-registration determinations.** The [separate site review](../../evidence/CHG-008/stage-2/sites-independent-review.md) found authored embeds sharing paragraphs, impossible imported text observations, an extra self-cycle expansion, an omitted nonblocked repeated expansion, missing images and duplicated origin directories. The independent author corrected these before freeze, added true-scheme and observable Unicode-directory controls, then repaired a duplicated directory in the new outer-host expectation. The URI reference retained its original 148 rows and added five controls; separate review found no discrepancy. Region CASES bytes are unchanged. No implementation result informed any correction.

**Repair attempt 9, section legality checker.** Hypothesis: the draft legality check forgets an inline ancestor when descending into a quote. [A discriminating nested paragraph/quote/transclusion is accepted](../../evidence/CHG-008/stage-2/pd-transclusion-sections-legality-negative-before.txt). The independent author enforces the restricted block/inline vocabulary and rejects unknown kinds; [the same defect is refused while all 80 valid trees pass](../../evidence/CHG-008/stage-2/pd-transclusion-sections-legality-negative-after.txt). Corrected CASES bytes remain unchanged through this checker repair. Implementation and registered grading follow this commit.

**Forward disclosure, registration inventory.** Commit 3dcedd4 stored the raw package-count inventory with a `.json` suffix under evidence, causing the generated evidence index to show an untyped row with question marks. The [lint attempt rejects it for a missing discriminator](../../evidence/CHG-008/stage-2/pd-transclusion-count-inventory-lint.txt). It is a raw measurement, not an L0 or corpus-report envelope. Its bytes are preserved under `registration-counts.txt`; the generated index is refreshed. No historical content, registered data, grading or passed outcome is rewritten.

**Repair attempt 10, shared test-source boundary (registered before tooling code).** The transclusion owner graders legitimately share one fixed loader through Rust `#[path]`. The [architecture command refuses it](../../evidence/CHG-008/stage-2/pd-transclusion-architecture-before.txt) because the file is outside site’s crate directory. Decision workspace-test-source-boundary and the [additive contract](../architecture/module-test-source-boundary.md) choose a canonical, explicitly permitted workspace boundary for test-only sources, preserving production/default confinement, logical ownership, test-edge notes and complete source hashing. The discriminating controls will be registered before the checker repair. The stage remains in progress until the real site check and all stage gates pass.

The [12 independent boundary controls](../../xtask/tests/corpus/module-test-boundary/registration.toml) are now registered before extractor/source-hashing changes: three accepted cases and nine refusals, with complete logical modules, canonical source digests, one helper-mutation observation and distinct authority failures. [The separate review](../../evidence/CHG-008/stage-2/module-test-boundary-draft-review.md) confirms path bases, inherited exact test context and canonical confinement. [Root’s audit](../../evidence/CHG-008/stage-2/pd-module-test-boundary-audit.txt) binds all seven payloads, reproduces all nine package files byte-identically, accepts a valid copy and rejects tampered CASES bytes. Draft metadata omissions, error-class ambiguity and controls that also hit a second boundary were corrected before freeze. No existing corpus or grade changes.

**Fixture purity determination.** [Clippy rejects runtime fixture I/O in the core graders](../../evidence/CHG-008/stage-2/pd-transclusion-clippy-before-fixture-split.txt). Decision pure-fixture-inputs selects a [single pure validator with embedded bytes and an app-cli filesystem harness](../../evidence/CHG-008/stage-2/transclusion-fixture-io-decision.md). No core forbid, Clippy policy, dependency, production API or registered expectation is relaxed. Implementation results and final stage gates will be recorded with the settled source change.

### Resumption by the supervising session (2026-09-24)

The previous Executor stopped at its usage limit with the tree snapshotted at 44ef02a and the [resume point](CHG-008-growth-RESUME.md) written. From here the supervising session is the sole writer (decision writer-handoff-2026-09-24); implementation is generated by supervised agy runs in isolated clones and integrated here (decision agy-supervised-implementation).

- **Main merged, not rebased** (a18253a). Only the generated `docs/tasks/index.md` conflicted; it was regenerated and `docs --check` passes. On the merged tree, `cargo test --workspace --no-fail-fast` gives 321 passed and 3 failed, the three failures listed in the resume point and no others; module H4 is 28/28 as registered.
- **P-C's acceptance record** is `.rha/acceptances/CHG-007.toml`, bound to merge 74aeb1d and main CI run 35841881942 (all eight L0 checks, 290 tests), with the two P3 notes from PR 18 carried forward. The record lint accepts it.
- **Merge rule while GPT-6 is unavailable** (decision merge-rule-gpt6-unavailable; DP-5.4's second amendment): the supervising session's approval of a head suffices until GPT-6 returns.
- **The `dev_allow` pin (§9.14).** `xtask/src/graph/rules.rs` pins the core dev allow list so that a widening is a visible change. The list now includes `serde_json` under decision owner-local-json-oracles. Before updating the pin, the decision's conditions were checked: `serde_json` 1.0.151 is a `[dev-dependencies]` entry only in document, graph and site, each of which parses the registered transclusion oracle data in its tests, and the normal core allow list is unchanged. `cargo xtask architecture` passes. The pin now states the approved requirement; the test is not weakened.

**Repair attempt 11, inner test attributes and external-read authority** (resume point item 1). Hypothesis: the extractor carried one flag for two concepts. The same `test` bit marked edges test-only and also granted authority to read a module source outside the crate root, so an inner `#![cfg(test)]` or `#![test]`, in the root or in a nested file, granted that authority. The two registered regression tests fail at 5760b7c. Change: a supervised agy run (Gemini 3.8 Flash, /boost, jailed; provenance entry in the task record) separated the two into a `ModuleContext { test, authority }`. Authority comes only from an outer `#[cfg(test)]` on a module declaration and is inherited through declaration ancestry. The crate root starts without it. An inner attribute still marks edges test-only but grants nothing. Result, rerun by the supervising session on this branch: the four boundary tests pass, including both regression tests and the in-crate classification test; the workspace gives 324 passed and 0 failed; module H4 is 28/28; `cargo xtask architecture` passes; fmt is clean; clippy exits 0. The patch also removes a pre-existing clippy error in `extract.rs` (`expect` after `is_some`), and a separate integrator commit fixes a needless borrow in the test file, which the agent escalated rather than edit. No new clippy warning kind appears in `extract.rs`: 14 warnings against 15 at the base. No registered test, corpus or contract document changed.

**Transclusion stage (CHG-009) closed.** The fix in 7ef5ac7 was reviewed by an independent Opus 5.5 verifier in its own worktree: VERDICT APPROVE, no P1 or P2. Its probes showed that the only behaviour differences from the base are the four inner-attribute grants now refused. Test-only classification is unchanged in every accepted case, and the real workspace's architecture report is structurally identical to the base's. Its P3 coverage gap (an inner `#![cfg(test)]` inside an inline module) is now pinned by a regression test that passes at the fix and fails at the base (79e89b3). Its second P3 is carried forward: `attr_info` does not treat an outer `#[test]` on a `mod` as test context, while `attrs_cfg_test` does. This predates the fix, does not affect authority, and is a note for the extractor follow-up. [Stage gate](../../evidence/CHG-008/stage-2/transclusion-stage-gate.txt): nextest (CI profile) 324 passed, docs current, architecture and scope with 0 findings, fmt and clippy clean, module H4 28/28. The observed change spread matches the predeclared production set; see [change-spread](../observations/change-spread.md).

### Stage 2, feature 2: §-references (CHG-010), prepared before corpus and code

The [contract](../architecture/section-reference-contract.md) defines section numbers of headings, recognition (text content only; ranges `§§3–7` and `§3–§5`), same-page resolution to the first matching heading, rendering as a link whose text is exactly the reference as written, and the observation surface `Document::section_refs`. Decisions section-refs-scope (same page, document only), section-refs-observation and section-refs-check-surface (no new `check` witness kind; the schema Kennedy's held-out fixtures use stays fixed) are in the task record. A rough pre-registration count found 163 numbered headings in the spec and no unresolved number among its `§` occurrences. The prediction (document only) is committed in [change-spread](../observations/change-spread.md) before any fixture or code.

**§-reference corpus registered before implementation.** A separate session, a Claude Sonnet 5 oracle author that read only the contract and public types, wrote [the package](../../xtask/tests/corpus/section-refs/registration.toml): 84 cases (83 synthetic plus the real spec), an independent Python reference with its own markdown scanner, and the grader `crates/document/tests/section_refs.rs`. The registration review had three parts:
- The first round left five questions open. They were settled in the contract (a3ca8de), and each now has its own case.
- Every expected spec reference was checked to lie outside headings and fenced code. The spec case expects 366 references, 9 of them range ends, and none unresolved.
- Nested headings were confirmed to be collected today, so the contract asks for no change to heading collection.

The package reproduces byte for byte from its reference, and its snapshot equals the committed contract. The grader was rustfmt-formatted by the integrator before registration; the change is layout only (verified token-identical). **Disclosure:** the contract's pre-registration footnote says the spec has one `§` in a heading. There are none: the occurrence on spec line 2070 is a comment inside a fenced block. The 9 non-reference occurrences are all in fenced code. The contract text is left as registered, and this correction is recorded here.

**§-references implemented (CHG-010).** A supervised agy run (Gemini 3.8 Flash, one agent; see the provenance correction below) implemented the contract in `document` only: a new `section_ref.rs` (heading numbers, scanning, ranges, resolution, rendering), integration in `parse.rs`, and `SectionRef`, `Document::section_refs` and `Diagnostic::UnresolvedSectionRef` in `lib.rs`. The one test that builds a `Document` literal gained the field. The agent escalated one question: the registered grader read its inputs with `std::fs`, which the core deny list forbids. It kept the grader unchanged as ruled, and the integrator moved the grader to `include_bytes!` in 4be1a7d, with every assertion unchanged plus one added guard that the only path-identified case is the spec. Rerun by the supervising session on the branch: the grader passes all 84 cases (the spec included); the workspace gives 326 passed and 0 failed; architecture passes; fmt is clean; clippy exits 0; module H4 is 28/28.

**Provenance correction.** Both supervised runs so far were recorded with the agy feature they were launched with (/boost, then /teamwork-preview). The transcripts show that neither feature ran. The command was passed as agy's initial prompt (`-i`), which is delivered as literal text: each run was a single Gemini agent with one conversation and no subagents. The work was verified the same way either way. The task record's provenance now says so, and the supervision kit starts later runs by pasting the command into the interactive session, where it is expanded (tested with `/usage`).

**Review of c556d01: REQUEST_CHANGES; determinations before repair (repair attempt 12).** An independent Opus 5.5 verifier found:
- **P1: a user link crashes `parse`.** The implementation carried references as links with the magic href `__rha_internal_section_ref_<n>__`. A user link with that destination panicked `parse` (index out of bounds), so `rhawiki check` exited 101, or it hijacked the user's link. `parse` is documented as total.
- **P2: adjacent text nodes were merged on every page.** This changed JSON build output on six pages with no `§` (HTML and `check` output were unchanged). No registered test pins JSON bodies.
- **P2: tabs were accepted in range whitespace.** The contract says U+0020 only. **Disclosure:** the registered oracle `reference.py` has the same deviation (`[ \t]*` in `RANGE_RE`); no registered case exercises it, so the grader could not see it. The registered package is not changed.
- **P3, determined as a known limitation:** under the contract as written, `§3 - A thing` makes `A` a range end. The contract is not changed after registration.

Hypothesis for the repair: carry references in a dedicated internal representation that user input cannot produce, and merge text only where a reference marker is flattened. Discriminating check: [section_refs_review.rs](../../crates/document/tests/section_refs_review.rs), written by the supervising session, a different model family from the implementer. Its three tests fail at c556d01 for exactly the reported reasons. The P2 "nothing else changes" test compares against the parser at 4be1a7d, the commit before the implementation: the body trees and headings of the review's inputs and of every docs page without `§`, captured at that commit into `crates/document/tests/data/pre_section_ref_trees.json`.

Repair attempt 12's change, from the first run in which agy's Boost pipeline actually engaged (a root, a DeepCoder coordinator, a first worker and an improvement worker):
- **P1:** references travel as a crate-private `BuildNode::SectionRef` variant, so user input cannot produce one and a user's link is always kept.
- **First P2:** text is merged only where an unresolved reference is flattened back into text; a page without references gets exactly the pre-feature tree.
- **Second P2:** range whitespace is U+0020 only.

Rerun on this branch: the three review tests pass, the registered grader passes all 84 cases, the workspace gives 329 passed and 0 failed, fmt and clippy are clean, and architecture passes.

**§-reference stage (CHG-010) closed.** The re-review of the repair (3d3d836) approves it. All three findings are fixed. With the old and new binaries run on the same docs, JSON build output differs only on the three pages that contain resolved references; HTML differs only by the 410 inserted anchors, all of which resolve; `check` output is byte-identical. A million-input fuzz found no lost or duplicated characters, and every user link is kept. Carried forward:
- **P3:** an unresolved reference at a pulldown-cmark text-event boundary merges two text nodes that the parser used to keep separate. Only JSON node boundaries change; visible text, HTML and every real page are unchanged. Follow-up: merge only within one scanned text event.
- **Pre-existing, a new follow-up item:** pulldown-cmark 0.13.4 panics on some wikilink inputs, which contradicts `parse`'s documented totality. It reproduces at 4be1a7d, before this feature.

[Stage gate](../../evidence/CHG-008/stage-2/section-refs-stage-gate.txt): nextest (CI profile) 328 passed; docs current; architecture and scope with 0 findings; fmt and clippy clean; module H4 28/28; `rhawiki check --root docs` reports zero witnesses. Change spread: document only, as the plan predicted ([change-spread](../observations/change-spread.md)).

### Stage 2, feature 3: citations (CHG-011), prepared before corpus and code

The [contract](../architecture/citation-contract.md) defines reference entries (paragraphs beginning `[Rn]`), their anchors (`ref-rn`), citations in text content, same-page resolution to links, and the observation surfaces `Document::reference_entries` and `Document::citations`. A new `Node::Anchor` is carried through site assembly and both renderers; anchors inside transcluded content are dropped. Decisions citations-anchor-node, citations-scope and citations-check-surface are in the task record. The **prediction is refined before code**. The plan says document only, but linking a citation to its entry needs an anchor on the entry, and every renderer must render it. So the predeclared production set is document, site::assembly, adapter-html and adapter-json, with the plan's original prediction kept alongside in [change-spread](../observations/change-spread.md). A pre-registration count found 168 entries in the spec, no duplicate label, and no unresolved citation.

**Citation stage (CHG-011) closed.** Implemented by the first real agy teamwork run (352b0f1: 42 conversations; milestones for document, site assembly and the renderers each passed the team's own gate; the Gemini quota cut off its final gate, so the supervisor integrated). Three independent review rounds followed, each adding regression tests before its repair:
- Round 1 (352b0f1): entries in tight list items registered after their nested blocks (first-wins and document order), and citations directly after `!` missed. Repaired by Claude Opus 4.6 in agy (e8dda29).
- Round 2 (e8dda29): the `!` repair split `![` into two text nodes on pages without citations, breaking the pre-feature tree (contract section 5); a bare label ending a tight item before a nested list or block quote was not an entry. Repaired by Claude Opus 4.6 in agy (14a1d57). The same review found that `xtask/tests/held_out.rs` rejected any summary containing `cafe`, which e8dda29's own hash contains; fixed in 4f789b5. e8dda29's message claims the suite passed; that run was on the uncommitted tree, and at e8dda29 itself one test fails.
- Round 3 (14a1d57): the bare-label rule covered only a nested list or block quote, not a code block, table, heading, rule or HTML block. Fixed by the supervising session (0339267), with expected values from the registered oracle.
- The final review (0339267) approves. Citation-free pages parse identically to the pre-feature parser (e2a2f95) over 8,000+ generated, fuzzed and repository pages, about 3,400 of them containing `![`. The spec has 168 unique entry anchors and 411 citation links, none dangling; `rhawiki check` output is byte-identical to 352b0f1.

Two registered graders were re-registered by the supervisor, not the implementer (decision citations-grader-reregistration): a doc-comment fix for clippy in `citations.rs` (0db8b76), and `section_refs.rs` counting exactly citation-shaped links out of its section-link count (c5d376b). Earlier tests received only the grader-compat-anchor edits. Carried forward:
- **P3:** `DuplicateReferenceEntry` diagnostics now follow the parse-time diagnostics instead of source order; no contract fixes the order and nothing reads it.
- **P3:** 0339267 lists a transclusion among the blocks that end an entry's paragraph; that arm cannot be reached after a label (the commit message overstates it; the arm is harmless).
- **P3, as in CHG-010:** on pages that have citations, an unresolved citation can merge adjacent text nodes (JSON node boundaries only; two real pages).
- **Pre-existing, a new follow-up item:** deeply nested input (about 2,000 levels in a debug build, 8,000 in release) overflows the parser's stack, contradicting `parse`'s documented totality; e2a2f95 fails at the same depths.
- **Rulings recorded, not defects:** escaped `\[R1\]` is not a citation; a line break after an entry label makes an entry; a label behind a task marker or emphasis is not an entry.

[Stage gate](../../evidence/CHG-008/stage-2/citations-stage-gate.txt): nextest (CI profile) 394 passed; docs current; architecture and scope with 0 findings; fmt and clippy clean; module H4 28/28; `rhawiki check --root docs` reports zero witnesses. Change spread: document, site::assembly, adapter-html and adapter-json, exactly the refined prediction; the plan's "document only" missed the render support a new link target needs ([change-spread](../observations/change-spread.md)).

### Stage 2, feature 4: callouts (CHG-012), prepared before corpus and code

The [contract](../architecture/callout-contract.md) keeps recognition as it is: GFM alerts, the five kinds, parsed since Phase 1. It adds what GFM renders: a title paragraph naming the kind, and per-kind styling. JSON output, search text, the document tree and `rhawiki check` are unchanged. Decisions callouts-gfm-only, callouts-html-title and callouts-check-surface are in the task record. A parser probe on 2026-09-25 showed that pulldown-cmark 0.13.4 recognises `[!note]` in any letter case, in lists and nested, but not a title on the marker line, other kinds, or fold markers. The **prediction is refined before code**: adapter-html only, against the plan's document plus adapter-html, with the plan's prediction kept alongside in [change-spread](../observations/change-spread.md). Implementation by agy Boost; verification by an agy teamwork review (Gemini) and a separate Claude verifier (decision agy-teamwork-verification-only).

**Callout stage (CHG-012) closed.** An agy Boost run on Gemini (4 conversations, about 15 minutes, no escalations) implemented it in c469d10, changing only adapter-html: a title paragraph per callout and seven additive stylesheet rules. Verification had two independent halves. Both approved, and neither found a P1 or P2:
- An agy teamwork run used only for verification (Gemini; everything but its probe folder frozen). It reported 0 findings: [report](../../evidence/CHG-008/stage-2/callouts-verification-gemini-team.md).
- A separate Claude verifier ran a differential over docs/, every repository page and 4,500 generated pages: JSON byte-identical, HTML differing only by exact title lines (30,370 titles), the stylesheet only extended, `rhawiki check` identical, and four negative controls failing as they should. It reported two P3s, which are carried forward:
  - `crates/document/src/node.rs:59` still says callout rendering is plain. A doc comment outside this stage's predicted set; follow-up.
  - `fn node` in adapter-html goes from 133 to 134 lines under the pedantic `too_many_lines`, a warning it already had.

[Stage gate](../../evidence/CHG-008/stage-2/callouts-stage-gate.txt): nextest (CI profile) 407 passed; docs current; architecture and scope with 0 findings; fmt and clippy clean; module H4 28/28; `rhawiki check --root docs` reports zero witnesses. Change spread: adapter-html only, as refined before code; the plan's document part was not needed ([change-spread](../observations/change-spread.md)).

### Review of the catch-up head (PR #21, 1122d6b)

A separate Claude verifier reviewed the combined head. It found L0 all passed, 407 tests, scope 0 findings, every corpus SHA256SUMS verifying, every cited link and revision present, CHG-007's acceptance values recomputed and correct, and `rhawiki check` with zero witnesses. Determinations:
- **P1, confirmed: the task record failed `rha lint`.** A field this session added in 352b0f1 (`resume_prompt_sha256`) is not in the schema, and no later stage gate linted the task record, so this record's claim that task records still receive the lint was false from 352b0f1 on. Repair: the value is folded into the generation's prompt text, and the record lints as accepted. From now on, each stage gate runs `cargo xtask rha lint .rha/tasks/CHG-008-growth.toml`.
- **P2, confirmed: "teamwork is reserved for verification" was cited without a decision.** Repair: decisions agy-teamwork-verification-only (Kennedy's words, narrowing agy-supervised-implementation) and agy-verification-second-family are recorded, and both citations now name the id.
- **P3, confirmed: stale resume point and missing spread headline.** The resume point now shows the review and PR row and the current merge rule. change-spread.md opens with the plan's original prediction accuracy (plan revision 3). Shrinking the H4 evidence records stays owed.

### Stage 2, feature 5, stage A: front matter and tags (CHG-013), prepared before corpus and code

This is the first stage delivered under plan revision 3. It is its own PR, `chg/013-tags-front-matter`, whose first commit is PLAN-4's acceptance record. The [contract](../architecture/front-matter-contract.md) recognises front matter only when it starts on the file's first line, parses a restricted YAML subset without a new dependency, removes the block from the body, lets its title replace the derived title, and records tags on the document. Two probes shaped it:
- **Current parser:** today a front-matter block renders as a rule plus a heading made of the metadata text.
- **pulldown-cmark's metadata option:** it recognises `---` blocks mid-page, so it is not used.

Decisions front-matter-first-line, front-matter-grammar, front-matter-check-surface, tags-stage-split and front-matter-compat are in the task record. **M13 split:** the tag index page (site assembly and both renderers) is stage B. **Prediction:** document only.

**Lessons applied from the previous stages (M11).** CHG-012's close predates M11, so its lessons are reconstructed here from the CHG-011 and CHG-012 records, each cited and applied or rejected:
1. *Probe the parser before writing the contract* (CHG-012). Applied: two probes, above.
2. *Register a revision differential with a negative control before code* (CHG-011's `![` defect; M12). Applied: `crates/app-cli/tests/front_matter_unchanged.rs`. It compares per-page document digests from the pre-feature parser over the spec, the repository docs and a seeded generated corpus. It requires every page with front matter to change, which is the negative control.
3. *Run clippy and fmt on registered graders at registration* (CHG-011, 0db8b76). Applied at registration.
4. *Lint the task record at every stage gate* (PR #21 review). Applied in this stage's gate.
5. *Write full hashes in prose; short ones can trip the typos check* (PLAN-4). Applied.
6. *Implementation by Boost or a single agent; verification by an agy verification-only team plus a Claude verifier* (decisions agy-teamwork-verification-only, agy-verification-second-family). Applied.
7. *Graders must not read files in core crates* (pure-fixture-inputs, the lesson lost between CHG-009 and CHG-010). Applied: the core grader embeds its data. The differential lives in app-cli, where I/O is allowed.

**Re-registration of the revision differential (CHG-013; the implementer's escalation).** The differential discovered its frozen inputs by walking `xtask/tests/corpus/**/*.md` at run time. Its goldens were captured before the oracle package was added in the same commit (7cbbfca), so that package's three `.md` files had no golden entry, and every later corpus would have broken it the same way. The supervisor re-registered it: the frozen inputs are now read by name from the golden file's keys (3,422, unchanged), and discovery is used only when capturing. The digests are not recaptured. At the re-registration commit, the key-set assertion passes, and the negative control still fails, as it must before the feature exists.

**Front matter stage A (CHG-013) closed.** An agy Boost run on Gemini implemented it (9a2e44e): a new `front_matter.rs` plus `parse.rs` and `lib.rs`. The one escalation was a real registration defect: the revision differential discovered corpus pages at run time. It was re-registered to read its inputs by name (ba73e8d). The gate's scope check found that the packet's task record did not own the PLAN-4 acceptance record (843e086).

Verification:
- **Claude verifier: APPROVE, one P3.** It ran a differential against the pre-feature parser over 23,500 pages without front matter (0 differences) and 60,000 fuzzed pages (no panic). The P3: carriage returns were not treated as trailing whitespace on front-matter lines (contract 2.1 item 5). Regression tests were registered first (d059d0e) and the repair is one line (8fe48d5). The re-review approved and noted that ` -	` is now an empty item, which is pinned (72cdaf3).
- **Gemini: APPROVE, 0 findings.** A single agent did the review, because `/teamwork-preview` did not form a team: there is no `.agents/teamwork` folder and no subagent call. It did not find the carriage-return P3 ([report](../../evidence/CHG-008/stage-2/front-matter-verification-gemini.md)).

Carried forward:
- **P3:** three new pedantic warnings (two `too_many_lines`, one `nonminimal_bool`).
- **P3:** the oracle's `reference.py` folds case with Unicode rules; the contract says ASCII. No registered case is affected.

[Stage gate](../../evidence/CHG-008/stage-2/front-matter-stage-gate.txt): nextest (CI profile) 426 passed; docs current; architecture and scope with 0 findings; task-record and PLAN-4 record lint accepted; fmt, clippy and typos clean; module H4 28/28; `rhawiki check --root docs` reports zero witnesses. Change spread: document only, as predicted.

**For the next stage (M11): stage B, the tag index page.** The next contract commit must cite each item and apply or reject it:
1. Stage B reads tags from `Document::front_matter`. Tags keep their first spelling and are de-duplicated with ASCII case, so the tag index needs its own rule for a page id or URL per tag (slug, case, non-ASCII). The contract must decide it.
2. The prediction (site::assembly, adapter-html, adapter-json) is exactly three production crates, M13's limit. A tag index needs a new page kind that no source file backs, and that may also touch `site::build` or `app-cli`. Probe it before the contract and split again if it exceeds three.
3. The revision differential must read its inputs by name from its golden keys, never discover them (the lesson of ba73e8d).
4. `/teamwork-preview` may not form a team. Before counting a verification run as a team, check for `.agents/teamwork` and a subagent call; otherwise record it as a single agent.
5. The single Gemini reviewer again found less than the Claude verifier (CHG-012, CHG-013). Keep both halves, and give the Gemini brief a checklist of the contract's amended items, such as section 2.1.
6. Supervisor answers to agy quote hashes only after the command that produces them has run.
