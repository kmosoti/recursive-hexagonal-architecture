# Backlog

- [ ] Address H4's advisory Clippy style warnings (similar names, string formatting, long functions, boolean result fields, item placement) — no grading or execution consequence; the required lint gate still applies — `xtask/src/corpus/`.
