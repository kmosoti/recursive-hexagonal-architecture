# Backlog

- [ ] Benchmark and, if justified, index per-link resolution — current M2 behavior passes its gates and no performance threshold is claimed — crates/graph/src/lib.rs:SiteGraph::resolution.
