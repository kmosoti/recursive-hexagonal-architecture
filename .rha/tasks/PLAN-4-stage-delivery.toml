# Task record (spec §11.7.11 shape, stored as TOML).
schema_version = 1
id = "PLAN-4"
slug = "stage-delivery"
title = "Revision 3: deliver stages on their own, carry lessons forward, register revision differentials, size stages"
mode = "agent"
executor_composition = "agent"
authority_profile = "ECC-Solo"
status = "proposed"
plan_ref = "docs/plan/IMPLEMENTATION-PLAN.md (revision 2, amended here as revision 3)"
branch = "chg/plan-4-stage-delivery"
base_revision = "ac1e460deeb327287e7e817c6bebd4bf465dc528"
intent = "Apply the findings of the study /home/kmosoti/experiments/studies/2026-09-rha-stage-division (commit 5ab36fc in ~/experiments): at CHG level, 0.375 of stages were delivered on their own with CI; 0.40 had CI before the next stage began; about 2 P1/P2 defects per P-D feature stage escaped the registered graders; 0.64 of transitions carried a lesson forward, by hand. Make the stage the delivery unit (M2), and add M11 lessons forward, M12 revision differential at contract time, M13 stage size; name agy verification-only teamwork in M6; split P-D's remaining work into stage PRs; make change-spread report the plan's original prediction accuracy."
non_goals = [
  "Any change outside docs/plan/IMPLEMENTATION-PLAN.md and this change's records",
  "The frozen plan copies in xtask/tests/corpus/{record-schema,verifier-shape}/inputs, pinned by registration digests",
  "Retroactive restructuring of merged packets or of P-D's completed stages, whose history stays as it is",
]
scope_globs = [".rha/tasks/PLAN-4-stage-delivery.toml", ".rha/acceptances/PLAN-3.toml", "evidence/ci/36008186429.json", ".rha/acceptances/CHG-008.toml", "evidence/ci/36150611033.json", "docs/plan/IMPLEMENTATION-PLAN.md", "docs/changes/PLAN-4-stage-delivery.md", "docs/tasks/index.md", "docs/evidence/index.md", "docs/enforcement-map.md", "evidence/PLAN-4/**"]
protected_scope = [".rha/acceptances/PLAN-3.toml", ".rha/acceptances/CHG-008.toml"]

[[actors]]
id = "human:kennedy"
type = "human"

[[actors]]
id = "agent:executor"
type = "agent"
accountable_to = "human:kennedy"

[roles]
planner = "human:kennedy"
executor = ["agent:executor"]
integrator = "human:kennedy"
verifier = ["automation:xtask-ci (local, advisory)", "automation:github-actions (class ci, advisory)", "Opus 5.5 reviewer agent; GPT-6 unavailable until 2026-09-29 (decision merge-rule-gpt6-unavailable)"]
acceptor = "human:kennedy"

[[work]]
id = "PLAN-4"
executor = "agent:executor"
depends_on = ["PLAN-3 (merged; acceptance record written here as the first commit)"]
output = "docs/plan/IMPLEMENTATION-PLAN.md M2, M6, M11 to M13 and the P-D paragraph; the PLAN-3 acceptance record; this task record and its change record."

[[decisions]]
id = "apply-stage-division-study"
question = "Apply the stage-division study's recommendations to the plan, open CI on the P-D branch and merge it?"
answer = "Kennedy, 2026-09-25: 'Now make more comprehensive edits ci rund and commit the larhe branch', following the study's recommendations and the offer to write them as a PLAN-4 amendment and open the P-D PR."
decided_by = "human:kennedy"
date = "2026-09-25"

[provenance]
harness = "Claude Code (model claude-opus-5-5)"
task_scope = "PLAN-4"
permissions = "Workspace files, shell, git and gh through the user's account; the protected acceptance record is created under [acceptance]'s pre-approval."
budgets = "unknown"
reasoning_effort = "unknown"
